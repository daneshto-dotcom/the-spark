═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — The Spark
Generated: 2026-06-20
Session: S93 — NONET (9-square Sudoku-lock event) built end-to-end, all modes, shipped + deployed
═══════════════════════════════════════════════════════════

## PROJECT
- Name: The Spark (geometric real-time builder duel; live: spark-online.space)
- Working directory: …/Founder DNA/Extension Projects/The Spark
- Git branch: master · Latest commit: 31a8aa6 (feat(s93): NONET … all modes, netcode, audio)
- Tech stack: TypeScript · Pixi.js v8 · Vite · Trystero/WebRTC netcode · Vitest · Playwright E2E
- Deploy: push to master → .github/workflows/deploy.yml → spark-online.space

## CURRENT STATE
- Build: PASSING — tsc 0; `npm run build` OK
- Tests: 1463/1463 vitest (82 files) — +30 new (sudoku gen/validators, event logic, 3 snapshot-roundtrip determinism)
- Bundle: main index 552.7 KiB — ⚠️ +2.7 over the 550 charter (overlay lazy-split out; residual = generator+event core, can't lazy-load)
- Deployment: pushed 31a8aa6 → deploy.yml building spark-online.space (verify live after CI)

## THIS SESSION'S WORK — NONET (the 9-square Sudoku-lock)
Connect EXACTLY 9 pure-Square primitives → duel FREEZES for all players → synced seeded 6×6 Sudoku trial (digits = the six SparkType colours) → first to solve gets score ×2, everyone else halved → ~3s → resume (120s timeout = no-op resume).
- `state/sudoku.ts` — deterministic seeded 6×6 generator + validators (unique-solution) + `SudokuEvent` type
- `state/sudokuEvent.ts` — detectNonet (9 pure squares) · startSudoku · submitSudokuSolve (first-valid-wins) · resolveSudoku (×2/÷2, host-only) · tickSudoku (timeout/resume) · mintNonetSeed
- `placePrimitive.ts` trigger (host, once/match) + `main.ts` freeze gate (whole sim pauses via early-continue; tick + tickSudoku + snapshot-send keep running)
- `render/sudokuOverlay.ts` — retro arcade-Tetris board (6-colour jewels, gold frame, scanlines) + vector kami/kodama; **LAZY code-split**
- Netcode: `NetSnapshot.sudoku` (seed-only; clients regen puzzle via mulberry32) + `SUDOKU_SOLVED` client intent + bench policy + PROTOCOL_VERSION 8→9 (+ HelloMsg literal-type tripwire)
- Audio: realm-shift theme swap — `public/audio/nonet-theme.ogg` (transcode of user's Cloudstep Caravan, off-bundle) on the music bus, all modes
- Concept art (Imagen, ~$0.24): assets-source/nonet-concepts/ (kami_2 hero, kodama_2, dusk_world_1) — Phase-2 sprite refs

## OPEN ISSUES
- ⚠️ Bundle +2.7 KiB over the 550 charter — trim generator OR raise charter (flagged, non-fatal; build/CI not blocked)
- 🐛 Combo order-symmetry (user-flagged): Triangle→Spiral = magic "Warped Anchor" but Spiral→Triangle = plain placeholder line; systemic (14/30 orderings cool). Fix = symmetric mirror except Wheel/Star — own PDR (LOCKED §6)
- NONET polish not done: anime SFX (need assets), resolve juice (shake/flood), =9-vs-≥9 trigger tuning
- 1v1 netcode is code-complete + unit-tested (snapshot roundtrip) but NOT live 2-peer tested — verify in playtest

## BLOCKED ON
- USER PLAYTEST on spark-online.space (all 3 modes) — report bugs before next session (per your direction)

## NEXT STEPS (priority order)
1. Playtest NONET live (solo + 1v1 + bots); confirm trigger/freeze/overlay/solve/×2÷2/audio; report bugs
2. Fix any playtest bugs (start of next session)
3. Combo order-symmetry fix — own PDR (LOCKED §6 amend + user go)
4. Bundle trim / charter review; then NONET polish (anime SFX, resolve juice, trigger tuning)
5. NONET Phase 2: illustrated Ghibli sprites off-bundle + parallax (own PDR)

## CHANGED FILES (commit 31a8aa6)
24 files, +1386/-9. New: state/sudoku.ts(+test), state/sudokuEvent.ts(+test), render/sudokuOverlay.ts, public/audio/nonet-theme.ogg, assets-source/audio + nonet-concepts. Touched: main.ts, world.ts, worldTypes.ts, gameMode.ts, placePrimitive.ts, controls.ts, save.ts, net/protocol.ts(+test), benchGate.ts, render/audioManager.ts.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: 1/1 complete | Full tier (user-path, no Council; user playtest = CHECK) | 607.7K/1M (60.8% YELLOW)
P1 NONET Sudoku-lock (all modes) — completed — 31a8aa6

## REFLEXION ENTRIES (this session)
- P1 #netcode-sync-a-host-event-by-mirroring-the-additive-optional-snapshot-pattern: additive-optional snapshot field + serialize seed-only & regen derived state client-side + one client INTENT → forces 5 tsc/test-enforced surfaces (GameAction+dispatch, exhaustive KNOWN record/parseNetMessage gate, CLIENT_INTENT allowlist, BENCH_INTENT_POLICY set-equality test, PROTOCOL_VERSION literal tripwire); bump version so cross-version matches hard-reject not desync.
- P1 #freeze-a-sim-loop-without-starving-the-network-or-the-clock: an early-continue full-sim freeze also skips the loop-bottom snapshot-send → 1v1 clients stall; replicate the host send + keep advancing tick inside the freeze. Heavy Pixi view lazy-splits (~+6KiB chunk); core gen+event can't (reducer/snapshot synchronous) → +2.7KiB over charter, flagged.

## CARRY-FORWARD PRIORITIES
1. Combo order-symmetry fix — not started — PDR: not drafted (LOCKED §6 amend)
2. NONET polish (anime SFX + resolve juice + trigger tuning) — not started
3. NONET Phase 2 illustrated sprites — not started — concept art ready in assets-source/nonet-concepts/
═══════════════════════════════════════════════════════════
