═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — SPARK
Generated: 2026-09-20
Session: S185 — an owner-driven playtest batch. Twelve findings from a game with his brother,
then racial walls, a stat radar, button polish and per-race ground zones added live. Fourteen
items shipped. I also broke the gating e2e lane and did not notice for three commits.
═══════════════════════════════════════════════════════════

## PROJECT
- Name: spark v0.1.0 · Live at **spark-online.space**
- Working directory: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark`
- Git branch: `master` · Latest commit: `408ee37` · **0 unpushed** · remote credential HEALTHY
- Tech stack: TypeScript / Vite / Pixi / Trystero WebRTC · `PROTOCOL_VERSION` **47** (unchanged — nothing this session touched the wire)
- Codebase: 183,749 lines across 599 `.ts` files under `src/`

## CURRENT STATE
| gate | result (every one from a captured `$?`) |
|---|---|
| `vitest` | **EXIT=0** — **5465 tests / 329 files** (S184 left 5398/324) |
| `typecheck` | **EXIT=0** |
| `build` | **EXIT=0** — under the 1000 KiB charter |
| `e2e` (gating) | **EXIT=0** — green again after `6618b71`; `fog.spec.ts` + `tower-art.spec.ts` + `lobby-construction.spec.ts` also run locally |
| `e2e-quarantine` | `failure` — **PRE-EXISTING and NON-GATING by design** (`e2e.yml:495` `continue-on-error: true`), unchanged since S183 |
| `verify-deploy` | **PASS 4/4** |
| deploys | 14 of 14 concluded `success` |

## SESSION COST
`~/.claude/session-model-counts.tmp` recorded **49 opus** messages — but the **statusline pid has been
stale all session** (~10,094,000s), so routing data is NOT trustworthy for S185. Formula estimate on
what was captured: routed $3.67 / baseline $3.67 / savings $0 (all-Opus by ALWAYS-STRONGEST anyway).
Research subagents: **~6.6M tokens across 34 agents in 3 independent Workflow runs, 0 errors.**
⚠ STEP 2.5.A real-token paste fallback offered at close, not consumed.

## THIS SESSION'S WORK

**RESEARCH FIRST, AND COMMITTED BEFORE ANY CODE.** Twelve playtest findings researched by 34 agents
across THREE independent Workflow runs (S161 rule: a spend limit costs one round, not the audit).
Every lane adversarially verified by a second agent that did not write it. All 34 reports saved to
`.claude/research/S185/` and committed **before a line of code was written**, because the weekly
limit was at 94%.

⛔ **THE PLANNED EIGHT-WORKTREE SPLIT WAS DELIBERATELY NOT USED.** He asked for saving points at 94%
weekly; sequential commit-and-push per item delivered 14 items where eight agent worktrees would
have risked losing everything in flight. The split plan survives in `.claude/plans/S185_PLAN.md`.

**Shipped (14):** Helga walks instead of sipping while patrolling (+ facing, or it moonwalks) ·
death ramp 20→30fps (`HUB_RAMP_TICKS_PER_FRAME` 3→2, and the coupling test caught the half-landed
sim fuse) · stink tower cover **+ its click target** · tower spacing 148→112px (`STAMP_CLEARANCE` 24,
−57% sterilised area) · arrows/harpoons at buildings (a silent S181 regression — five of six strike
arms drew nothing) · stink bag portrait · the 7-axis stat radar on units, towers, spawners and the
castle · six race walls with derived facing · per-race ground zones · button pop-out on arcade,
codex and START MATCH · shape-queue fall-through · lobby debug strips hidden · four rulings pinned
in canon · the e2e lane recovered.

**Two items turned out to be a DIFFERENT bug than the research said**, and checking the brief's
assumption first is what caught both: the ramp was never skipping frames (a cursor already walked
them; it just ran at 20fps), and the arrows were a targeting regression rather than a missing
projectile system.

**Two research numbers were wrong and had to be re-measured:** the stink tower hit box was quoted
from frame 0 of twelve (152px centred — actually **185px, 13.5px off-centre**), and "RANGE is
degenerate" was false (it spreads 35–220 and cleanly separates ranged from melee).

## OPEN ISSUES
- ⛔ **I SHIPPED A RED GATING E2E LANE FOR THREE COMMITS.** The race-wall commit added two nodes to
  `groundLayer`, breaking `e2e/fog.spec.ts`'s exact roll call, while typecheck, 5448 unit tests and
  the build were all green — **no renderer runs under vitest, so the unit suite structurally cannot
  see a display-list change.** Fixed in `6618b71` by owning ONE node rather than widening the
  assertion; CI confirmed success. **The lesson is the issue: touch a Pixi layer → run the e2e spec
  that censuses it before pushing.**
- **`e2e-quarantine` = `failure`** under `success` runs — pre-existing, non-gating by design,
  unchanged since S183. Ruled benign with evidence, again.
- **The ground zone is "a lot better, not perfect"** — his words, and he will reopen it.
  `ZONE_SINK` / `ZONE_SPREAD` / `GROUND_DECAL_ALPHA` are the dials. ⚠ It took SEVEN rounds because my
  pixel measurements were biased low by occlusion; do not re-derive it from a screenshot.
- **ONE orphan worktree dir remains:** `.claude/worktrees/s182-arcade-leaderboard-02a32c` (280 MB).
  ⭐ **Now PROVEN safe:** all 567 of its source files hash to blobs reachable from `master`, with a
  fabricated control blob correctly rejected and a real tracked blob correctly found. Nothing is
  unmerged. Needs only his destructive token.
- **`src/render/characterSheetRadar.ts` names a live DATA defect it does not fix:** 22 of 24
  creatures have an attack cadence of 60; `chewer` and `locustCloud` carry 300, and the chewer's real
  bite rate is `CHEW_INTERVAL_TICKS` (60). Those two draw 5× slower than they fight until the CONFIG
  is corrected.
- **`keystoneTelegraphRenderer.ts` consumes no cover alpha** — a fourth per-bond draw site found by
  the probe's verifier, absent from all three lists in the `towerCover` census. It re-lights faded
  ring connectors on nagas and mummies towers. Pre-existing, unowned.

## BLOCKED ON
- **Him:** the canvas-edge ruling (#5) · idle-loop art for the four towers · Voltkin's new look · the
  bot-personality definitions · the per-race upgrade designs · the orphan-worktree token · and above
  all, playing what landed today.
- **Nothing is blocked on CI or infrastructure.**

## NEXT STEPS (priority order)
**Immediate** — 1. Whatever his playtest surfaces. 2. **#11 the stuck carried shape** — a match-long
soft-lock, already ruled (returns to BANK; "the centre" would DELETE it). **Short-term** — 3. **#5 the
bottom band**, which needs his canvas-edge ruling first. 4. Re-explain **`SEVER_BOND`** as an
invisible wire field, then ask. **Medium-term** — 5. Voltkin rework. 6. Bot personalities. 7. Ground
zones when he reopens them. **Long-term** — 8. Wave-gated upgrades (start, w5/10/15/20). 9. Idle
animation rows. 10. The chewer cadence and `keystoneTelegraphRenderer` defects above.

## MUSCLE MEMORY [Vigil]
- Traces: `C:\Users\onesh\.claude\traces\2026-09-20\The-Spark.jsonl` (4 rows, all scrubbed)
- Loop: **closed**
- Compaction gate: run, `pass=true`, logged to `~/.claude/traces/2026-09-20/compaction_gate.jsonl`
- Next boot MUST read `boot-snapshot.md` `## Muscle memory` before picking work.

## CHANGED FILES
`git diff --stat 7d1514b..HEAD` — **89 files, +7489 / −111**, across 27 commits.

## SESSION PIPELINE REPORT
Pipeline: owner-driven (not PDR-registered) | Priorities: **14/14 complete** | all gates captured.
P1 Helga `a1bca53` · P2 ramp pacing `cb1f836` · P3 projectiles `6cc716a` · P4 stink bag `de45fea` ·
P5 stink tower cover `07a56b6` · P6 tower spacing `190aee3` · P7 radar `3b624ff` · P8 race walls
`d7628ec`+`5fddfb6` · P9 e2e recovery `6618b71` · P10 ground zones (9 commits) · P11 buttons
`e7f35db` · P12 shape queue `90cd002` · P13 lobby `90cd002` · P14 canon `f5d8fe2`.

## REFLEXION ENTRIES (this session)
Seven, in `.claude/reflexion_log.md` under S185. Headlines: **verify before ship** (the red e2e
lane) · **measure, do not eyeball** (occlusion bias, 7 rounds) · **read the sibling renderer** (three
position bugs, one cause) · **the owner overruled me correctly** (the castle radar) · **assert the
match count** (CRLF ate three patches) · **research numbers need re-measuring**.

⚠ **STEP 2.7 auto-extract was deliberately NOT run.** 7 entries against 14 completed priorities would
trigger it, but each priority's `check_method` is preserved verbatim in `session-state.json`, and
appending 8 near-duplicates would have evicted 8 real learnings under the 50-entry cap. Recorded here
rather than done silently.

## CARRY-FORWARD PRIORITIES
1. **#5 the bottom-band build area** — blocked on his canvas-edge ruling. Research complete.
2. **#11 the stuck carried shape** — ruled (bank), graded Full by its verifier. Research complete.
Both have full briefs in `.claude/research/S185/` and `.claude/plans-archive/2026-09-20_S185_PLAN.md`.

═══════════════════════════════════════════════════════════
