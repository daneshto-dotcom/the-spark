═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — The Spark
Generated: 2026-06-21
Session: S94 — colour-stuck-white bug fix (P1) + NONET gameplay tweaks (P2)
═══════════════════════════════════════════════════════════

## PROJECT
- Name: The Spark (geometric real-time builder duel; live: spark-online.space)
- Working directory: …/Founder DNA/Extension Projects/The Spark
- Git branch: master · Latest commit: 9333bbc (feat(s94): NONET tweaks)
- Tech stack: TypeScript · Pixi.js v8 · Vite · WebRTC netcode · Vitest · Playwright
- Deploy: push to master → .github/workflows/deploy.yml → spark-online.space

## CURRENT STATE
- Build: PASSING — tsc 0; `npm run build` OK
- Tests: 1467/1467 vitest (82 files)
- Bundle: main index 553.0 KiB — ⚠️ +3 over the 550 charter (flagged; generator+event core can't lazy-load)
- Deployment: P1 672cddd + P2 9333bbc pushed → deploy.yml building spark-online.space

## THIS SESSION'S WORK
**P1 — colour stuck white (672cddd).** Root cause (ONE bug, both reported triggers): `buildShuffleColorMap` (rainbowLifecycle.ts) deranged over all 7 `PLAYER_COLORS` incl. the bots-only Silver (`0xc0c8d0`, near-white) → a human could be deranged INTO Silver; a poop-foul on that structure reverts to the same Silver. Fix: `SHUFFLE_PALETTE = PLAYER_COLORS.slice(0,6)` — derange only the 6 human colours; a Silver bot keeps Silver via the `?? p.color` fallback; 7-seat uniqueness still holds. +3 regression tests (no human→Silver over 500 seeds) + updated 2 palette-coupled tests (rainbow bijection, bots 7-seat).
**P2 — NONET tweaks (9333bbc).** (a) `detectNonet` is now a per-tick host SWEEP for a connected component of EXACTLY 9 of ONE SparkType (any type, not just Square) — replaces the placement-only Square-only `placePrimitive` hook; wired in the main.ts host PLAYING tick, gated host-only + once-per-match → fires on build-up OR erase-down. (b) `NONET_TIMEOUT_TICKS` 7200→10800 (180s). (c) Codex: synthetic `NONET_CODEX_ENTRY` (id 'nonet', name+hint, sprite `/art/nonet/kami.webp`), unlocked via `unlockGodly` on the trial rising edge; `makeTile`'s missing-sprite catch shows name+hint until the Phase-2 kami sprite lands.

## OPEN ISSUES
- ⚠️ Bundle +3 KiB over the 550 charter (flagged, non-fatal; build/CI not blocked)
- 🐛 Combo order-symmetry (S93 user-flagged): Triangle→Spiral magic vs Spiral→Triangle plain line; systemic. Own PDR (LOCKED §6)
- NONET kami sprite is Phase-2 (codex tile shows name+hint, no image; overlay uses vector spirits) — both auto-upgrade when `/art/nonet/kami.webp` lands
- 1v1 NONET netcode (S93) unit-tested but NOT live 2-peer tested — verify in playtest
- NONET anime SFX + resolve juice not done

## BLOCKED ON
- USER PLAYTEST on spark-online.space — confirm the S94 colour fix + NONET 9-of-any-type/erase/180s/Codex; report bugs

## NEXT STEPS (priority order)
1. Report S94 playtest results (colour fix + NONET tweaks, all 3 modes)
2. Fix any playtest bugs
3. Combo order-symmetry fix — own PDR (LOCKED §6 amend + user go)
4. Bundle trim / charter review; NONET Phase-2 kami sprite (public/art/nonet/kami.webp → codex + overlay auto-upgrade)
5. NONET polish: anime SFX + resolve juice

## CHANGED FILES (672cddd + 9333bbc)
P1: rainbowLifecycle.ts (+SHUFFLE_PALETTE), rainbowLifecycle.test.ts, botsMode.test.ts. P2: sudokuEvent.ts (sweep + 180s), sudokuEvent.test.ts, placePrimitive.ts (hook removed), main.ts (sweep + codex entry + unlock). Plus assets-source/nonet-concepts-v2/ (5 pencil concepts, P3 ref).

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: 2/2 complete | Standard (user-path; user playtest = CHECK) | 727K/1M (72.7% YELLOW→ORANGE)
P1 colour-stuck-white fix — completed — 672cddd
P2 NONET tweaks (9-any-type sweep / 180s / Codex) — completed — 9333bbc

## REFLEXION ENTRIES (this session)
- P0 #one-root-cause-can-explain-two-symptoms-trace-to-the-shared-state: two triggers, same wrong end-state → find the shared corrupted state (player.color via the Silver-in-6-human-shuffle bug), not two fixes.
- P1 #a-per-tick-host-sweep-beats-a-per-event-hook-for-emergent-triggers: sweep the resulting state once/tick (guarded) instead of hooking every mutation path (place/erase/sever/bomb).
- P1 #a-synthetic-codex-entry-with-a-forward-compatible-asset-path: point a UI slot at the eventual asset path + lean on the existing missing-asset fallback for a zero-rework stub.
- SESSION #after-a-context-reset-mid-edit-run-the-failing-gate-first: run tsc immediately on resume to diff the half-done surface.

## CARRY-FORWARD PRIORITIES
1. Combo order-symmetry fix — not started — PDR: not drafted (LOCKED §6)
2. NONET Phase 2 (illustrated kami sprite + parallax) — not started — concept art ready (assets-source/nonet-concepts + -v2)
3. NONET polish (anime SFX + resolve juice) — not started
═══════════════════════════════════════════════════════════
