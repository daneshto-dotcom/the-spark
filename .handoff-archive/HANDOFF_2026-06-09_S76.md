═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — SPARK
Generated: 2026-06-09
Session: S76 — hunter 2.5× faster + rainbow more common + complexity-income scoring (autonomous batch)
═══════════════════════════════════════════════════════════

## PROJECT
- Name: SPARK (real-time multiplayer geometric-construction game)
- Working directory: C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark
- Git branch: master | Latest commit: 16023ae feat(scoring): complexity-INCOME model (S76 P3)
- Tech stack: TypeScript · Pixi.js v8 · Trystero/Nostr P2P (host-authoritative) · Vite · Vitest · Playwright
- Codebase: ~unchanged file count + new src/state/scoring.ts (+ scoring.test.ts)

## CURRENT STATE
- Build: PASSING — tsc -b 0 errors; vite build main bundle 534.35 KB (<550 charter)
- Tests: 1142/1142 vitest pass (+12 net); Playwright gating lane 32 pass + 1 pre-existing Sym-E skip
- Deployment: https://spark-online.space/ — Deploy ✓ green on 16023ae; E2E CI in-flight (confirm next boot, Rule 22)
- PROTOCOL_VERSION: 6 (unchanged — scoring is host-internal, no wire change)

## THIS SESSION'S WORK
- **P1 (Micro, `cbdf67c`)** — Hunter 2.5× faster. `HUNTER_MAX_SPEED` 1.4→3.5, `HUNTER_ACCEL` 0.12→0.30 (both ×2.5; preserves the juke ratio; terminal 3.0 < cap 3.5). Reverses ~half of S75's over-correction.
- **P2 (Micro, `7d7a2bd`)** — Rainbow more common. `RAINBOW_SPAWN_MIN/MAX_SPARKS` 35/60→15/28 (first rainbow ~1.5-3 min in vs ~4-7; still the rarest hazard). Test seam insulates rainbow.spec.
- **P3 (FULL, `16023ae`)** — Complexity-INCOME scoring. Replaced the monotonic per-placement accumulator with a per-tick income: `scoreByPlayer[p] += SCORE_INCOME_PER_COMPLEXITY_PER_SEC(0.15) × complexity(p) / PHYSICS_HZ`, complexity = #prims + 2×#magicBonds. Destroying a structure → lower complexity → slower gain (the ask). `scoreProgress = max(scoreByPlayer)` unified for all modes → fixes the player-1 inconsistency by construction. NEW `src/state/scoring.ts` (computeComplexity + tickScoring, host-only, before tickGameState); float-safe `Math.floor` WIN + hunter-trigger gates; placement scoring removed from placePrimitive (SCORE_TIER moved to tickScoring at the leader avatar); ui.ts floors the display; addScore unified. NO protocol bump.

## OPEN ISSUES
- CI E2E (2-browser) was IN-FLIGHT on `16023ae` at handoff — confirm green next boot (Rule 22). Deploy already ✓.
- **Game balance is un-playtested** (user was away). `SCORE_INCOME_PER_COMPLEXITY_PER_SEC=0.15` is the #1 tunable (range 0.05-0.25).
- [STEP 0 review-gate] the global review tracker showed a bogus card (`session S162 / P1_S162_crash_dump_diagnosis`) that does NOT match this project's S76 work — a global-tracker vs project-session-id mismatch (same class as the boot `<synthetic>` MODEL-DRIFT warning). Did NOT `--approve` (would bind a bogus card); review flag deferred to next boot (advisory window until 2026-07-15). S76 work is correctly captured in session-state + the 3 commits.

## BLOCKED ON
Nothing. User playtest will inform the income-rate / hunter / rainbow tunables.

## NEXT STEPS (priority order)
1. (Rule 22) Confirm CI E2E green on `16023ae` (`gh run list`).
2. Playtest → tune `SCORE_INCOME_PER_COMPLEXITY_PER_SEC` (and hunter/rainbow constants) by feel.
3. Pick ONE Standard PDR: #2 control-message sender-auth · #3 EYES fog/CVD a11y · #4 netcode infra (multi-session).
4. (Deferred) Re-validate + gate the S69 P2 lobby visual refactor before any Council/gate.

## CHANGED FILES (S76, vs 6652ba8)
src/constants.ts · src/state/scoring.ts (NEW) · src/state/scoring.test.ts (NEW) · src/state/gameMode.ts · src/state/placePrimitive.ts · src/main.ts · src/state/gameState.ts · src/render/ui.ts · src/game/session9|10|13|14|15.test.ts · src/state/gameState.test.ts · src/state/placeFromFree.test.ts · e2e/smoke.spec.ts

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: 3/3 complete | Token ~518K/1M (51.8% YELLOW)
- P1 Hunter 2.5× faster — completed — Micro — cbdf67c
- P2 Rainbow more common — completed — Micro — 7d7a2bd
- P3 Complexity-income scoring — completed — FULL — 16023ae

## REFLEXION ENTRIES (this session)
- P1 #hunter-tuning #2.5x-faster: playtest tuning is a two-way dial; scale the whole momentum model together.
- P2 #rainbow-rarity #spawn-math: a "spawn rate feels wrong" tune is a unit-conversion problem — compute first-appearance time.
- P3 #scoring-redesign #council-caught-formula-bug #gemini-refuted: model-redesign over patch when the ask is structurally incompatible; structural fix for consistency; refute a CHECK finding premised on a violated invariant with evidence.
- SESSION #s76-income-model-redesign: Council-before-code on a Full change; reverse-engineer the new metric to match the old gate to minimise blast radius.

## CARRY-FORWARD PRIORITIES
None incomplete (all 3 S76 priorities shipped). Forward = pre-existing backlog #2/#3/#4 + the deferred S69 P2 lobby refactor + the S76 playtest tunables.
═══════════════════════════════════════════════════════════
