═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — SPARK
Generated: 2026-09-22
Session: S186 — the owner's new **dynamic scoring** spec, the **spawn-rate step-up** he asked for in
the same breath, and the two playtest items carried from S185. Four priorities, all shipped, all
live. He pre-approved the batch and asked for autonomous execution.
═══════════════════════════════════════════════════════════

## PROJECT
- Name: spark v0.1.0 · Live at **spark-online.space**
- Working directory: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark`
- Git branch: `master` · Latest commit: `ddb240a` · **0 unpushed** · remote HEALTHY
- Tech: TypeScript / Vite / Pixi / Trystero WebRTC · `PROTOCOL_VERSION` **47 → 48**
- ⛔ **THE BUMP WAS THE AUDIT'S DOING, AND IT IS THE MOST IMPORTANT THING IN THIS HANDOFF.** The
  four priorities shipped claiming no bump was needed, reasoning only from *"no new field"*. See
  **THE AUDIT** below.

## CURRENT STATE
| gate | result (every one from a captured `$?`) |
|---|---|
| `npm run typecheck` | **EXIT=0** |
| `npx vitest run` | **EXIT=0** — **5515 tests / 333 files** (S185 left 5465 / 329) |
| `npm run build` | **EXIT=0** — 907.6 KiB, **92.4 KiB headroom** under the 1000 KiB charter |
| `npm run e2e:gating` | **EXIT=0** — 68 passed |
| `npm run e2e:races` | **EXIT=0** |
| `verify-session-claims.py` | **EXIT=0** — `hard_fail=0 warn=0`, **47/47 bindings pass** |
| `npm run verify-deploy` | **PASS 4/4 carriers** — live artifact traced to a successful run of `ddb240a` |
| CI at JOB level, HEAD | `e2e` · `e2e-protocol` · `e2e-races` · `e2e-lobby` · `atlas-guard` · `worker-typecheck` all **success** |

**Three non-success signals, all ruled BENIGN with evidence — none passed over:**

0. **One local `e2e:gating` run exited 1** on `zones-visual.spec.ts:257`, which read an EMPTY
   `band.chips`. It then passed **in isolation** and in a **full re-run of the same lane on the same
   commit** (`GATING_EXIT=0`, 68 passed). Verdict: a readiness race in that spec's fixed 3 s wait
   under parallel load, not a regression. ⚠ Worth tightening that spec one day — reading an empty
   array is a weak assertion shape.
1. `e2e-quarantine` = `failure` on every run. **Non-gating BY DESIGN** — `continue-on-error: true` at
   JOB level, documented at `.github/workflows/e2e.yml:48`. Unchanged since S183.
2. `38ba9e0` has **no CI run of its own**. Not a miss: it was an intermediate commit in a two-commit
   push, so Actions ran against the head (`0b5ccdd`), which contains it. Verified by `gh run list`.

## WHAT SHIPPED

### P1 · `fbe559b` — THE WIN BAR RISES WITH THE WAVE ("nikud dinami")

His spec, verbatim: *"It takes 2,500 points to win in the first five waves. After the fifth wave and
until the 10th fight wave, it's 5,000 … 10,000 until level 15 … 20,000 from 15 to 20 … from 20 to 25
it takes 50,000."* He closed the boundary himself — *"level five, still 2,500; level six, it's
already 5,000"* — so each band is **inclusive of its top wave**.

⭐ **THE BAR MOVES; THE BANKED SCORE IS NEVER RESET.** A seat on 3,000 wins at wave 5 and does **not**
win at wave 6. That IS the mechanic he asked for: *"you can't cheat by … just letting the points run
at level five."* `dynamicWinScore.test.ts` drives the REAL gate at both waves with the same 3,000
banked and asserts WIN then NOT-WIN.

⭐ **IT COST NO FIELD.** `world.waveNumber` was already hashed and already rode the wire (it drives
the spawn rate), so both peers **derive** the same bar: no new field, no four-sites work. Bands are
stored as MULTIPLIERS of `PHASE_1_WIN_SCORE` so the E2E seam still scales the whole ladder — absolute
literals would have set the seam to 50 and left the sim demanding 5,000.

⛔ **BUT IT DID EARN A PROTOCOL BUMP, AND I GOT THAT WRONG UNTIL THE AUDIT.** See THE AUDIT below.

### P2 · `31b01ce` — THE QUARRY STEPS UP AT WAVES 5 / 10 / 15 / 20

**Measured through `stepPhysics`, not derived** (the `castleGuns.test.ts` pattern):

| wave | mult | shapes/s | shapes per BUILD | pool cap |
|---|---:|---:|---:|---:|
| 5 | 1.80 | 2.02 | **184** (was 184) | 24 |
| 6 | 3.20 | 3.60 | **323** (was 202) | 36 |
| 10 | 4.48 | 5.04 | **463** (was 284) | 51 |
| 15 | 8.36 | 9.41 | **874** (was 385) | 95 |
| 20 | 13.44 | 15.12 | **1399** (was 486) | 96 |
| 25 | 19.72 | 22.19 | **2009** (was 587) | 96 |

⛔ **THE S157 RULING SURVIVES INTACT.** Band 1's factor is 1, so waves 1–5 are byte-identical to
*"wave 1 is normal, wave 2 is 1.2, wave 3 is 1.4x faster"*, and the RATE is still uncapped — the band
factor plateaus past 25 but the linear term never does. `waves.test.ts` now asserts unboundedness as
a **property out to wave 500**, a stronger guard than the two sample points it replaced.

⚠ **THE BAND FACTORS ARE MINE.** He gave the shape, not the numbers. Sized off the measured
crossover: at the old wave-6 rate, the **one shared quarry** fed about **ten** fully-upgraded
gatherers for the WHOLE TABLE — two or three per seat, which lands exactly on his "wave six or seven".

### P3 · `baa19db` — NOBODY CROSSES A WHISTLE STILL HOLDING A SHAPE

His #11: mid-drag when the FIGHT whistle blew, the shape stayed glued to his cursor for the rest of
the match and every further pickup was refused. Every `DROP_SPARK` producer on the human path is
gated behind a local pointer gesture, so one lost release was permanent — and the defensive arm then
*pinned the spark to the cursor every substep*, rendering the anomaly forever instead of recovering.

His ruling — **the BANK, not the ground** — is implemented, on **both** phase crossings. (The arm that
existed was FIGHT→BUILD; the whistle he actually hit is BUILD→FIGHT, which had no arm at all.)

⭐⭐ **AND BANKING IS WHY IT NEEDED NO CLIENT-SIDE COMPANION FIX.** A verifier caught that a host-side
*drop* leaves the spark Free, the client recomputes `mine` as true and keeps lerping it to the cursor.
Banking **deletes** the spark from `world.freeSparks` — the very map `mine` reads — so the stale
gesture falls through the `!mine` branch that already existed. The recovery closes itself.

### P4 · `38ba9e0` — THE BOTTOM BAND: 8 px FREED, AND THE REAL BLOCKER NAMED

⛔ **THE BOTTOM IS NOT SPECIAL.** The rule applies on all four edges; it only *reads* as a bottom
problem because `FOOTER_TOP_Y` is 996. ⚠ I first wrote that the bands were *symmetrical* and that was
**false for 15 of 19 recipes** (a tier-3 tower is 46 top / 29 bottom). The laser turret I measured
happens to be 56/56 — which is exactly how one measurement became a wrong generalisation, in the
canon and in a test that could not fail. Both corrected; the canon now prints the real table.

Shipped `EDGE_PAD` 8 → **0**: 8 px back on all four sides for all 19 recipes. The old 8 was an
aesthetic borrowed from a panel by its own comment, not a safety margin.

⛔ **AND I DID NOT LOWER IT FURTHER, BECAUSE GEOMETRY IS NOT THE BLOCKER.** The footer occupies the
bottom **84 px** and its opaque surfaces swallow clicks **on purpose** — planting a structure under an
invisible plate was reported **three separate times**. Lowering the rule would put towers under a
plate the guards refuse anyway. **See "BLOCKED ON HIM" for the one question this leaves.**

## THE METHOD THAT PAID, AND IT PAID EVERY TIME

**Every feature was probed by one agent and then adversarially verified by a different one, and the
verifier found something real in every single lane.** Not a style note — the list:

- **A divergence bug I had just written.** `hunterTriggerScoreForWave` read `window` per tick via the
  E2E seam. The `?worker=1` Web Worker has no `window`, so host and worker would compute different
  triggers and diverge on a hashed value — **invisible to vitest, which shares one jsdom window.**
- **A differential oracle that had silently diverged.** `hostTick.differential.test.ts` carries a
  hand-written mirror of the host tick; its hunter gate still read the static constant. The two agree
  **only at wave 1**, so any run crossing wave 5 had an oracle disagreeing with the code it polices —
  green the whole time.
- **A proposed spawn formula that was an algebraic NO-OP** (`0.2w + 0.8` = the existing constant),
  *and* whose own proposed guards all passed on it. It would have shipped green.
- **The fatal flaw in the obvious carry fix** (see P3).
- **Three false claims in my own verification bindings** (see below).
- **HUD bounds that no longer bounded** — tier reaches 100 at the top band.

⭐ **MUTATION-TESTED EVERY GUARD**: revert the feature, require RED, restore byte-identical. The
spawn-ramp mutation was deliberately *the exact no-op* the bad proposal would have shipped; four
guards caught it, including the measured one that cannot be fooled by source text.

## ⛔ THE AUDIT — AND IT FOUND ELEVEN THINGS, INCLUDING THE BIGGEST MISTAKE OF THE SESSION

The Rule 22 end-of-session audit ran four lanes over the whole diff, each adversarially verified.
**It was not a formality: it changed what shipped.**

⛔⛔ **1 · THE PROTOCOL BUMP I HAD ARGUED WAS UNNECESSARY.** All four priorities shipped claiming
47 was fine, reasoning only from *"no new field"*. But `tickGameState` is run by **every peer,
including the client**, and gates on `winScoreForWave` — so two builds both advertising 47 would
shake hands and then disagree about when the match ends. ⭐ **The precedent was already sitting in
`protocol.ts`:** 39→40 reads *"⛔ THE BUMP IS FOR THE RULE, NOT FOR THE FIELD … Both peers run that
function."* Identical mechanism, and I had not read it. **Now 48**, with all five checklist sites
updated and both changelog carriers satisfied (the repo's own chain guard caught the incomplete
first attempt).

**2 · MY OWN MOJIBAKE, SHIPPED.** A `unicode_escape` round-trip double-encoded 23 comment lines in
`constants.ts`, destroying the ⛔/⚠/⭐ markers this repo greps for — including the blocks saying the
band factors are MINE and that the 96 ceiling is a perf bound rather than a rate cap. Repaired.

**3 · A GUARD OF MINE THAT COULD NOT FAIL.** `buildableEdges.test.ts` never imported the rule it
claimed to bound and contained a literal tautology. Rewritten to binary-search the real predicate,
and mutation-tested at `EDGE_PAD = -44` — it now goes RED where before it was blind.

**4–6 · THREE FEATURES WERE ONLY GUARDED AT WAVE 1** — the one input where old and new behaviour
agree. The HUD denominators, the hunter trigger and the cap's *wiring* could each be reverted with
the whole suite green. All three now have guards that fail; each was mutation-proven.

**7–11 · AND THE REST:** `statsOverlay` would have read `free 96/24 !` all match from wave 6; a
canon test pinned a coincidence as an invariant; the 1075 melee floor (the whole safety case for
`EDGE_PAD 0`) was pinned to a copied literal; `hudLayout`'s row width was one character short; and
three docblocks cited bounds, tests or justifications that no longer held.

⛔ **ONE LANE DIED ON THE SPEND LIMIT AND WAS RUN BY HAND**, per the S161 rule that *"a hunt that
returns nothing is not a completed hunt"*: all 15 `readTest*` seams confirmed module-init, `SparkId`
confirmed number-branded so the eviction tie-break is a real total order, `runHostTick` confirmed to
have exactly two call sites (both host/worker, so the carry rescue cannot run on a client), and the
diff confirmed to touch no wire/hash/worker file.

---

## OPEN ISSUES / CARRY-FORWARD

1. ⭐ **THE EMPTY QUARRY AT EVERY BUILD WHISTLE — measured, pinned, and NOT fixed on purpose.**
   The spawn dispatch is BUILD-gated but `reapExpiredFreeSparks` is **unconditional**, and FIGHT is
   3600 ticks against a 600-tick TTL. Measured `pool@FIGHT-end` is **0 at every wave**. So the fleet
   is released on one tick onto nothing and walks ~870 px as one pack — **that is "waiting in line and
   not moving" verbatim, and no faucet number removes it.** The one-line fix (don't reap during
   FIGHT) is a balance change he has not been asked for. Canon §3c.
2. **`#5` is now ONE question, not a research task** — see BLOCKED ON HIM.
3. **A loose SHAPE has no edge rule at all** and can already be hand-placed in that band today; only a
   stamped TOWER is refused. ⚠ If what he was doing in the playtest was dropping shapes rather than
   stamping a tower, the blocker was a footer plate and never this rule. **Worth asking him.**
4. **`CASTLE_MAX_HP` (2500) no longer matches the points race** from wave 6 on — its docblock says the
   number is *"deliberately the SAME as `PHASE_1_WIN_SCORE`"*. So **castle-rush gets stronger the
   longer a match runs**. Left unchanged deliberately (R88 pins one castle constant per seat), and
   reported in canon §3b rather than absorbed. He may want to watch one long match and rule.
5. **Still open from S185, untouched here:** the chewer/`locustCloud` radar cadence data defect, and
   `keystoneTelegraphRenderer.ts` consuming no cover alpha.
6. **ONE orphan worktree** `.claude/worktrees/s182-arcade-leaderboard-02a32c` (280 MB). S185 proved
   all 567 source files hash to blobs reachable from `master`. Needs only his destructive token.

## ⛔ BLOCKED ON HIM — and it is ONE question now, with the case constructed

**Do you want the FOOTER moved, or the bottom band left as it is?**

The dead band and the footer stand on the same ground. Freeing the edge rule further puts towers
under an opaque plate, which the guards then refuse — and which he reported as a bug three times.
So the options are: a side rail, auto-hide the footer while a tower is armed, or leave it.

⚠ Ask #3 above **first** — it may turn out he was never blocked by this rule at all.

Also on him: idle-loop art for the four towers · Voltkin's new look · bot-personality definitions ·
per-race upgrade designs · the orphan-worktree token · **and above all, playing what landed today.**

## NEXT STEPS (priority order)
1. **His playtest of the dynamic scoring and the faster quarry.** Both are balance changes he will
   feel immediately, and both carry numbers that are MINE and easy for him to retune.
2. **The empty-quarry fix** — one line, already measured, needs only his word.
3. **#5's one question** (above).
4. Re-explain `SEVER_BOND` as an invisible wire field — still owed from S185.
5. Voltkin rework · bot personalities · wave-gated upgrades · idle animation rows.

## CHANGED FILES
`git diff --stat 5d699b6..HEAD` — **26 files, +2466 / −481**, across 5 commits.

## SESSION PIPELINE REPORT
Owner-driven, pre-approved batch | **4/4 priorities complete** | every gate captured from `$?`.
P1 `fbe559b` · P2 `31b01ce` · P3 `baa19db` · P4 `38ba9e0` · bindings `0b5ccdd`.
**42 verification bindings, verifier RUN, exit 0.**

⭐ **AND THE BINDINGS CAUGHT THREE FALSE CLAIMS OF MINE** — one was a green assertion that could never
fail, which is worse than no assertion because it reads as coverage. Authoring bindings is not
verifying; running them is.

## REFLEXION
Eight entries in `session-state.json` → `reflexion_entries_to_archive`. Headlines: **the verifier
found a divergence bug I had just written** · **a required parameter beats a tolerant default** ·
**two analyses disagreed and only the real loop settled it** · **a no-op that would have shipped
green** · **the owner's ruling was also the simpler mechanism** · **the change exposed a test that
had never worked** · **the honest answer was that geometry was not the blocker** · **running the
bindings is the verification**.

═══════════════════════════════════════════════════════════
