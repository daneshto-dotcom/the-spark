═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — SPARK
Generated: 2026-08-27
Session: S153 — the playtest batch: targeting, art, feel, and four owner-caught defects in my own work
═══════════════════════════════════════════════════════════

## PROJECT
- Working directory: C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark
- Git branch: master | Latest commit: ca4ec97 chore(s153): handoff â€” record the context-tracker discrepa
- Tech: TypeScript / Pixi / Vite / Playwright / deterministic host-authoritative sim + opt-in worker sim
- PROTOCOL_VERSION: 31 (unchanged all session — nothing shipped needed a bump)

## CURRENT STATE
- Build: PASS. Bundle ~738 KiB against a 900 KiB cap.
- Tests: 3008 unit (179 files) + 54 e2e gating — all green.
- Deploy: verify-deploy PASS 4/4 — live entry asset hash equals the local build.
- MCV: 121 assertions, hard_fail=0.

## SESSION COST
- Model: claude-opus-5 throughout (ALWAYS-STRONGEST).
- External API: Grok 1 call (~$0.01), Gemini 1 call (~$0.00). One 3-agent A.0 workflow completed;
  an earlier 15-agent fan-out died on a spend limit and returned NOTHING (see OPEN ISSUES).
- Cumulative log: ~/.claude/usage-log.csv

## THIS SESSION'S WORK — 10 priorities, all live
- P5b MOVE the palette + queue to the footer, right of tier 8, always visible  — in_progress — ~34K — N/A
- P5c The goblin speed ladder was a NO-OP — hopSpeedMul had no consumer (R82,  — completed — ~9K — 2b70337
- P1 Speeds re-tier + units-first targeting + separation + castle march — completed — ~24K — 2095d3f
- P2 Archer projectile VFX - plain arrow vs units, flaming vs structures — completed — ~12K — 8786d12
- P3 FEED during FIGHT — completed — ~10K — 3c212e2
- P4 Spark above the HUD + finish the affordance pass — completed — ~20K — 883d1b2
- P5a R91 — ordering a tower shortfall from the footer no longer needs the cas — completed — ~14K — 953a968
- P6 Bots build towers, not random shapes — pending — ~30K — N/A
- P7 Goblin hound atlas - the last FEED output without art — completed — ~15K — b4f3a69
- P8 worker-bots tick-based wait — pending — ~6K — N/A
- A1 Spark genuinely above the tier number (R81 unfinished) — completed — ~8K — b4dcff2
- A2 In-match exit — there was NO way to leave a running match (owner) — completed — ~14K — 055883b
- A3 Armies retreat before FIGHT ends and idle at their tower, spawn-fresh tr — pending — ~18K — N/A
- A4 THE CASTLE IS DESTRUCTIBLE - castle OR 1500 points wins (R89) — pending — ~35K — N/A
- A5 Bots build towers - medium and up (R86 / CF-S149-f) — pending — ~30K — N/A

Highlights:
- P1 units-first navigation (acquisition radius + leash hysteresis), the arrival ring that fixes the
  unit "stack", and march-on-castle when no enemy shapes remain. Gave `creatureCanTarget` — the R72
  matrix that had ZERO production callers — its first real consumer.
- P2 the archer's arrow on the existing UNHASHED effect channel: plain at units, flaming at
  structures. Zero wire cost.
- P3 FEED reachable during FIGHT (R79) with its own predicate; R19 keeps FIX/SCRAP BUILD-only.
- P4 hover/press/sound across the footer + popover.
- P5a ordering a tower's shapes from the footer no longer requires the castle to have been opened.
- P5c THE SPEED LADDER MADE REAL — see OPEN ISSUES; this was a defect in my own P1.
- P7 the goblin hound (3 owner-directed art passes) — the last FEED output off the procedural puppet.
- A1 the player's cruiser above the HUD (P4 had raised the wrong layer).
- A2 leave a live match without reloading the page (double-Escape).

## OPEN ISSUES
- ⛔ P5b NOT DONE. The owner asked THREE times to move the shape palette + queue to the footer
  (right of tier 8, always visible). P5a fixed the ENQUEUE bug underneath it; the UI did not move.
  Audited plan, ~34K, files + breaking tests enumerated. START HERE.
- ⚠ CONTEXT TRACKER DISCREPANCY. Owner's UI read 52% while real-context-tokens.py read 71%. That
  script is the named source of truth and it GATED two scope cuts this session (the bots work at
  61%, P5b at 69%). If it over-reports, both cuts were unnecessarily conservative. Unresolved.
- ⚠ PDCA WRITE GATE blocks every edit when the in_progress priority sits late in session-state.json
  (awk brace counter drifts on a long file — measured depth 9, expected 0). Keep in_progress at
  index 0. Cost ~15 minutes this session.
- ⚠ A 15-agent A.0 workflow died on an individual spend limit with zero salvageable results;
  journal.jsonl confirmed no agent completed. Cap fan-outs at ~3.
- Stink tower reported by owner as "not flinging poop bags". NOT reproduced — the mechanic IS fully
  implemented (magazine, lob, aura, death blast, bagsRemaining on the wire) and targets enemy UNITS
  only, so it correctly does nothing with no hostiles in range. Needs a real reproduction.

## BLOCKED ON
- Nothing external. Billing topped up (art lane verified open); Actions capacity recovered.
- Owner decision only: whether the TWO-hound-variants idea still stands after the first dog was
  reassigned to the Zombie castle.

## NEXT STEPS (priority order)
0. ⛔ STILL NOT DONE — move the palette + queue to the footer, right of tier 8 (R80)
   The owner has now asked THREE times and it is the oldest open request in the batch. S153 P5a fixed the ENQUEUE bug (ordering from the footer no longer needs the castle to have been opened) but did NOT move the UI, and the owner said so plainly. Audited plan: ~34K, files and breaking tests fully enumerated in the P5b carry-forward below. ⭐ START HERE next session, before any new feature. Implementation shape already c
1. BAT RIDER: ranged harpoons + a flying read (owner, root-caused)
   ⭐ HE IS CONFIGURED AS MELEE. GOBLIN_BAT_CONFIG.attackRange is GOBLIN_ATTACK_RANGE = 35, the SAME constant the melee swordsman and the chewer use ("true melee - closes onto its target"). The archer by contrast has its own GOBLIN_ARCHER_RANGE. So the bat rider is not failing to fire a projectile - he has no ranged behaviour at all; he flies up to contact and hits. That is why nothing appears to leave him. || (1) give h
2. The context tracker may over-report, and it gates scope decisions
   CLAUDE.md names that script the SOURCE OF TRUTH for the GREEN/YELLOW/ORANGE/RED scope thresholds, and this session made TWO real scope cuts on its reading: deferring the Full-tier bots work at a measured 61 percent, and then declining to start P5b at a measured 69 percent. If the script over-reports, both cuts were more conservative than the budget actually required, and the owner waited an extra session for work tha
3. Armies must retreat before FIGHT ends and idle spawn-fresh at their tower
   APPROVED, NOT STARTED, and the owner has now raised it TWICE with a screenshot - goblins standing in enemy territory at BUILD 1:25. Owner: "during buld phase your own spawn stil lstay in enemy lands! thats inherently wrong - they would be killed" and earlier "they should run back 2 or 3 sec before end of fight and stay near their tower as if they were just built (like halfway transparent)... this is the mode for all 
4. THE CASTLE IS DESTRUCTIBLE - castle OR 1500 points wins (R89)
   OWNER MAIN GOAL, approved, NOT STARTED. A.0 BANKED: no castle entity exists, only castleBanks (an inventory Map); damageEntity has no castle arm; the match is won on scoreByPlayer alone. S153 P1 ALREADY makes goblins WALK to the enemy keep when no enemy shapes remain - they arrive with nothing to attack, which is exactly the owner symptom, so the pathing half is done. Needs a damageable castle entity, a damageEntity 
5. Bots build towers - medium and up (R86 / CF-S149-f)
   Approved, NOT STARTED. Owner re-reported it this session: "bots still not building towers.... medium bots should at least build some" - that adds a DIFFICULTY FLOOR. BUILD_BLUEPRINT is already an allowlisted client intent so no protocol bump. WARNING: the S150 structural claim (GROWTH_STEP 48 inside AUTO_BOND_RADIUS 60) was NOT re-verified - GROWTH_STEP is not in constants.ts. Littering in scope.
6. Stink tower reported as not lobbing - REPRODUCE FIRST, it is implemented
   The mechanic exists in full (magazine, throw interval, bag damage/radius, aggro aura, death blast, bagsRemaining on the wire). First hypothesis: DEFENDER_TARGETS.stinkTower is UNITS_ONLY, so with no enemy creature in range doing nothing is CORRECT. Reproduce with an enemy unit in range before changing anything.
7. Finish the clickable / back-menu consistency sweep
   A2 proved the four TITLE entries and the lobby back button work. NOT covered: the OVERLAY back paths (codex, arcade, bot setup), which return without changing gameState so the probe could not observe them. They need their own assertions.
8. TWO hound variants per tower - ASK FIRST, may be superseded
   The owner asked for two dog types, then reassigned the rejected dog to the Zombie castle instead. IF it still stands: variant choice must be DETERMINISTIC from the creature id (the spreadTargetPos golden-angle idiom), never a draw - all three determinism gates are self-comparing and would NOT catch a divergence.
9. Zombie hound has no ATTACK clip
   idle + walk exist under assets-source/zombie-castle/. The attack clip failed twice with "high demand" - transient veo backpressure, NOT credits. No consumer yet, not blocking.
10. worker-bots tick-based wait (CF-S152-a)
   Poll world.tick; NEVER raise the timeout - that rope already failed twice here.

## CHANGED FILES (a68ffed..HEAD)
 src/render/castlePanel.ts                          |   38 +-
 src/render/footerBand.ts                           |   57 +-
 src/render/goblinRenderer.ts                       |   22 +
 src/render/renderer.ts                             |   22 +
 src/render/structurePanel.test.ts                  |   86 ++
 src/render/structurePanel.ts                       |  127 +-
 src/state/creatures/creatureAI.ts                  |   93 ++
 src/state/creatures/creatureLifecycle.ts           |   29 +-
 src/state/creatures/creatureSpeedLadder.test.ts    |   77 ++
 src/state/creatures/unitFirstTargeting.test.ts     |  213 ++++
 src/state/creatures/voltkin-config.ts              |   37 +-
 src/state/goblinKinds.ts                           |   42 +-
 src/state/hostTick.ts                              |   57 +-
 39 files changed, 2530 insertions(+), 606 deletions(-)

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: 9/15 complete | Tier: Full (batch) + 2 amendments
Owner rulings recorded this session: R79–R92 (14), including R89 (castle OR 1500 points wins) and
R88 (castle races share ONE stat line — design banked, deferred by the owner).

## REFLEXION ENTRIES (this session)
15 entries appended to .claude/reflexion_log.md; log pruned to 48 entries / 2 session blocks.
The three sharpest are all about my own work being confidently wrong: a config field with no
consumer (P5c), the wrong render layer proven rigorously (A1), and two scope cuts made on an
unvalidated instrument (SESSION).

## CARRY-FORWARD PRIORITIES
6 incomplete: P5b, P6, P8, A3, A4, A5
Full detail in session-state.json → carry_forward_next_session.
═══════════════════════════════════════════════════════════
