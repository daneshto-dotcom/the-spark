═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — SPARK
Generated: 2026-09-17 | Session S180
Focus: character sheets (shipped, rejected in play, finished) · the keep onto the stat ladder ·
a canonical doc that cannot rot
═══════════════════════════════════════════════════════════

## PROJECT
- Working dir: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark`
- Branch `master` · HEAD `eb1eb7f` · **0 unpushed**, credential healthy (`ls-remote` exit 0)
- Stack: TypeScript / Vite / Pixi / Trystero WebRTC · live at **spark-online.space**

## CURRENT STATE
- typecheck **exit 0** · vitest **293 files exit 0** · build **exit 0** (845.5 KiB / 1000 charter,
  154.5 KiB headroom) · check:atlas **exit 0** (31 clean) · **verify-deploy 4/4 on `c0dead0`**
- 9 commits, 20 files, +3066 / −312. Exactly ONE is sim code (`creatureAttack.ts`).

## THIS SESSION'S WORK
1. **THE CHARACTER SHEET** (`e363c93`, fixed in `1cf4ee2`). Click any unit, building or castle,
   yours or theirs: portrait, name, live health as bar AND number, stats. Your own building keeps
   FIX/SCRAP/FEED beneath the card — he ruled the card ABSORBS the popover rather than competing
   with it. An enemy's has no buttons. A building that fields a unit shows it underneath with its
   own health, clickable. The card FREEZES on death or fog instead of vanishing (Council). No skills
   row — he ruled it out twice, and there is zero ability text in the tree.
2. **THE KEEP IS ON THE ONE LADDER** (`c0dead0`). `GOBLIN_DAMAGE_VS_CASTLE` — a flat 6 every
   creature dealt to a castle, the last bespoke damage constant in the game — retired unread. An
   attacker deals its own `attackFifths(atk, pen)`. RE-MEASURED, not relaxed: goblins needed to fell
   a keep moved from ~15 to **between 8 and 10** (8 → 264 left, 10 → falls, 1 → nothing).
3. **`SPARK_CANON.md` + `src/canon.test.ts`** (`ca39c83`). One page for what is live vs archived,
   the ladder, what can be attacked. Every number pinned to its constant — **proven to have teeth**:
   one stale digit turns it red. `CLAUDE.md` now opens with a mandatory-read pointer.
4. **Bundle charter 900 → 1000 KiB** (`66ce005`), in its own commit BEFORE the feature, so the
   sheet's real cost (9.7 KiB) stays visible.
5. **Backlog, targeting table, and THE TEN for S181** (`f96c0d8`, `831aaf9`).

## OPEN ISSUES
- ⛔ **NOTHING CAN ATTACK A BUILDING — still live.** S179's lone-shape commit (`00e02bf`) made the
  shape scan skip connected shapes while the same branch nulls the connector target. **21 of 24 unit
  types**; only Voltkin, the chewer and the drone can break a building. His targeting rulings fix it
  and are COMPLETE; the build was not authorised.
- `DEFENDER_TARGETS.turret` still declares BOTH. He CHANGED R72 this session to units-only. One
  line, not authorised.
- Castle and Helga portraits on the card are a labelled plate — their art exists but is unwired.
- ⚠ One vitest run died with "Worker exited unexpectedly" (tinypool). Re-run on the same tree: exit
  0. **VERDICT: benign infrastructure flake**, not reproducible — named, not silenced.
- ⚠ statusline dead all session; no trustworthy real-token reading. Recorded as unavailable rather
  than estimated.

## BLOCKED ON
Nothing is blocked on me. He tests in the morning; his bug list outranks the ten.
Art only he can commission: per-race border walls, boss ability VFX, the two Voltkin TV videos.

## NEXT STEPS
**Immediate:** he plays and reports. Then he picks from **THE TEN** in `S180_BACKLOG.md`.
**#1 of the ten is the targeting fix** — fully ruled, blocked on nothing.
⛔ Do NOT present the ten as a plan. He picks, after playing.

## SESSION AUDIT (he asked for three agents; all three died to the spend limit)
Per the CONSTITUTIONAL S161 rule, the lanes were **hand-run**. All three PASS.
- **Did it land** — all 11 wire points present in `main.ts` / `controls.ts`.
- **Regressions** — no RNG or clock in the sheet; no world mutation; **S180 touched NO wire file**
  (protocol/save/both hashes outside `f96c0d8~1..HEAD`); castle damage is `atk × (5 + pen)`,
  integers only, so `damageEntity` cannot throw.
- **Obedience** — PROTOCOL_VERSION still 46; castleGuns, potato, `nextPulledSparkId` untouched in
  source; no skills row; actions gated on ownership; **TARGETING UNTOUCHED** and the turret entry
  unchanged, because he ruled but did not authorise.

## MUSCLE MEMORY [Vigil]
- Traces: `C:\Users\onesh\.claude\traces\2026-09-17\The-Spark.jsonl`
- Loop: **closed**
- Next boot MUST read `SPARK_CANON.md`, then `boot-snapshot.md` `## Muscle memory`.

## CHANGED FILES
`src/render/characterSheet{,Model}.ts` + 3 test files · `src/input/controls.ts` · `src/main.ts` ·
`src/render/goblinRenderer.ts` · `src/state/creatures/creatureAttack.ts` · `src/constants.ts` ·
`src/state/castleGuns.test.ts` · `src/canon.test.ts` · `SPARK_CANON.md` · `CLAUDE.md` ·
`LOCKED_DECISIONS.md` · `scripts/check-bundle-size.mjs` · backlog + table + PDR.

## SESSION PIPELINE REPORT
Priorities: **4 approved, 4 completed** · MCV exit 0 · review gate approved · close gate exit 0
P1 character sheets `1cf4ee2` · P3 backlog+table `f96c0d8` · P4 canon `ca39c83` ·
P5 keep-on-ladder `c0dead0`

⛔ **TARGETING IS NOT IN THAT LIST, AND THAT IS THE HONEST MODELLING.** It was tracked as a blocked
session priority, which tripped the close gate — rightly. The fix was NOT to mark it done: it is
genuinely unbuilt, and he ruled it without authorising the build, so `pdr_approved` was false for its
entire life. An item never approved to execute is a CARRY-FORWARD, not a session priority. Its full
record is kept at `session-state.p2_targeting_never_approved`, and it is item #1 of THE TEN.

## REFLEXION ENTRIES (this session)
11 appended (45 total, pruned under the 50 cap). Headline: **green gates are not proof a feature is
wired** · **a patch that does not apply is the same bug twice** · **I shipped a half-feature and
called it done** · **prove provenance with git** · **a dead agent run is not a verdict**.

## CARRY-FORWARD
1. **TARGETING** — fully ruled (S180-A2), not built, not authorised. ⭐ The cascade half already
   exists: `damageConnector` banks structure-wide and spends overkill into the next connector.
2. Turret → units-only · SOUL chip verdict · castle/Helga portraits · castle BUY functions on the
   card · per-race border art · the stat board.
═══════════════════════════════════════════════════════════
