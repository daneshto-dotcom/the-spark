═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — SPARK
Generated: 2026-09-07
Session: S165 — W1-C wired, the race art finished, a 5-lane sweep, seven owner playtest fixes, and two new features
═══════════════════════════════════════════════════════════

## PROJECT
- Name: spark 0.1.0 · live at **spark-online.space**
- Working directory: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark`
- Git branch: `master` (project convention — commit direct, no feature branches)
- Latest commit: `bee38a1` chore(s165): close all ten carry-forwards
- Tech stack: TypeScript / Vite / Pixi / Trystero WebRTC · deterministic host-authoritative sim
- Codebase: ~133,141 lines across 476 `.ts` files

## CURRENT STATE
- `tsc -b --noEmit`: **0**
- Unit tests: **3756 / 3756** across 241 files
- `e2e:gating`: **exit 0**, 62 passed / 16 files · `e2e:races`: **exit 0**, 5 passed (new lane)
- `check:atlas`: **0** — 30 atlases clean on all three checks
- Bundle: 784.8 / 900 KiB · static assets 76.0 MiB (printed, deliberately NOT gated)
- Deploy: `verify-deploy` **PASS 4/4** at `843f025`; CI green (E2E + Deploy) on the last three commits
- MCV `verify-session-claims --strict`: `hard_fail=0 warn=0`, 0 UNBOUND
- PROTOCOL_VERSION: **42**

## SESSION COST
- Model split: 5 recorded messages, all **opus** (counter under-records; the session ran long)
- Estimated routed cost ≈ baseline — no downgrades taken, per ALWAYS-STRONGEST
- Cumulative log: `~/.claude/usage-log.csv`

## THIS SESSION'S WORK

**Shipped features**
- **W1-C castle emitter** (`src/state/raceUnitEmit.ts`) — each castle produces its race's unit on a
  1800-tick cadence, per-seat negative sentinel, its own cap family. PROTOCOL 41→42.
- **Twelve per-race zone backdrops** + a settings toggle back to cosmos black (per viewer, all four
  quadrants, never synced). Turning it off fetches nothing at all.
- **Per-race music** — six covers at 78–82 kbps to match the shipped track, a URL-keyed cache
  **capped at two buffers** (decoded PCM is 384 KB/s; uncapped would reach ~736 MB), a new
  `stopMusic()` closing a pre-existing gap, and a toggle that bites on the click.
- **Art**: race-unit clips regenerated without the baked shadow; three tier-3 atlases rebuilt after
  the guard was fixed; the naga walk row un-frozen (motion 1.41 → 4.03).

**Seven owner playtest reports, all real, all fixed**
1. Castle gatherer upgrades unclickable — the panel built 2 Pixi rows for a 3-row model, so S164's
   CASTLE REGEN row was never drawn. At 100 VP it was the only affordable purchase.
2. Chewer spawn rate — the pentagram banked blocked cadence slots then drained them one per tick.
3. BACK TO MAIN overlapped seat 1's keep — moved the BUTTON, not the castle.
4. The quarry spawn portal was split by the backdrops — now black on both boards.
5. Goblins whose tower died recall to the castle — already true, now pinned.
6. Beetle walk row 0.82× — **the guard was sampling frame 0 only**; fixed, then found two more.
7. Nagas twitching — 20× less motion than any sibling; a 16-frame sampling window, not the video.

**Five-lane sweep — 20 findings closed.** The serious one: `nextPulledSparkId` was rebuilt by
NOTHING despite `worldTypes.ts` documenting the rebuild, so a host takeover / worker adoption /
save-load resumed the descending allocator at −1 while live shapes sat at −1,−2,−3 — silently
overwriting one. Also: `pickHostTargetPrimitive` had no id tie-break (Map order decided world
topology), `nextStinkCloudId` was marked hashed with no projection, `players` hid `castleHp` from
both oracles while it gates emission, `recallArmies` parked a seat's whole army on one pixel, and
`@visual` never kept anything out of the gating lane despite saying so for sessions.

**Test integrity** — four tests that asserted nothing now observe their subject; one was *named*
after behaviour the code deliberately refuses. New guards: `ci.e2eLanes.test.ts` (tag→lane map),
`zone-backdrop.spec.ts` and `settings-toggles.spec.ts`, plus enumerated coverage for hashed scalars,
effect lifetimes, race-unit caps and the spawner-ignition registry.

## OPEN ISSUES
- ⚠ `worker-bots.spec.ts:78` failed ONCE in a full-suite local run, then passed standalone, passed a
  second full-suite run, and passed on CI. **Unreproduced — recorded, NOT ruled benign.**
- `origin/gh-pages` still exists. Left alone deliberately: it is deploy infrastructure and the
  project charter warns that `gh api .../pages` reports stale gh-pages info. Deleting it is the
  owner's call, not a cleanup.
- `die` rows exist in all 12 unit atlases and cannot play (`syncSprite` maps to attack/walk/idle).
- 56 files in `.claude/plans/` — the S165 batch PDR is now archived; the rest predate this session.

## BLOCKED ON
Owner rulings only: **tier-3 unit stats (R135)**, the **tower's build cost**, and the **wave-5 perk
numbers (R112's named trigger)**. Nothing is blocked on code or infrastructure.

## NEXT STEPS (priority order)
**Immediate** — 1. Tier-3 tower + six units. Owner's explicit first priority. The research is in
`S165_OPEN_ITEMS.md` → "FIRST PRIORITY NEXT SESSION": R134 puts the units on the TOWER not the
castle; R135's stats are unruled and the owner will decide by testing (so ship provisional and mark
them MINE); ONE recipe with six race-keyed outputs; a new recipe needs an unoccupied (hub, degree)
pair re-derived from the live registry; owes PROTOCOL 42→43; e2e goes in the `@races` lane. Ask the
build cost first.
**Short-term** — 2. Wave-5 tech draft (R101–R112). 3. The two music decisions named at their code
(swap restarts at 0:00; HELGA/NONET outrank the race theme).
**Medium** — 4. Make the `die` row playable, and move the atlas guard's `die` exclusion back into
its verdict the same day.
**Long** — 5. Race perks for waves 10/15/20; the six castle-upgrade branches (owner deferred both).

## CHANGED FILES
21 commits this session (`b0c1aa3..bee38a1`). Principal: `src/state/raceUnitEmit.ts`,
`src/render/zoneBackgroundRenderer.ts`, `src/render/raceMusic.ts` (new),
`src/render/displayPrefs.ts` (new), `src/render/audioManager.ts`, `src/render/settingsOverlay.ts`,
`src/render/castlePanel.ts`, `src/state/hostTick.ts`, `src/net/migrationClaim.ts`,
`src/state/stateHashFull.ts`, `src/ci.e2eLanes.test.ts` (new), `e2e/zone-backdrop.spec.ts` (new),
`e2e/settings-toggles.spec.ts` (new), `scripts/check-atlas-scenery.mjs`,
`scripts/build-sprite-atlas.mjs`, `.github/workflows/e2e.yml`, `S165_OPEN_ITEMS.md` (new),
plus 18 regenerated art atlases and 6 new audio tracks.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **9/9** (P2 superseded by P7) | MCV `hard_fail=0`
- P1–P6 art + specs — completed · P7 W1-C wiring — completed (`ef944bd`) · P8 zone backdrops —
  completed (`8aff165`) · P9 CI/deploy recovery + sweep + playtest fixes + two features — completed
  (`5f2a1fa` → `bee38a1`)

## REFLEXION ENTRIES (this session)
14 entries appended to `.claude/reflexion_log.md` (log pruned 63 → 34 to stay under the cap; the
pruned S160/S162 blocks survive in archived handoffs). The four worth reading first:
`#a-partial-negative-control-accuses-a-working-test`,
`#never-assert-on-anything-a-wall-clock-touches`, `#a-lane-budget-is-a-shared-resource`,
`#scout-the-subsystem-before-designing-against-it`.

## CARRY-FORWARD PRIORITIES
**All ten S165 carry-forwards are CLOSED** — eight by this session's own code/art, two by owner
answers (the 65 KB wire question, and the eliminated-seat ruling that already existed as
`cf_s161_a`). Kept with `resolution` fields rather than deleted, because this project has twice been
bitten by a next session inheriting resolved work as open work.

The only live carry-forward is **the tier-3 build**, and it is Next Step 1 rather than a footnote.

═══════════════════════════════════════════════════════════
