# SPARK — HANDOFF S175 (2026-09-13)

**Six of six approved priorities shipped, pushed and verified live (4/4 carriers).**
`typecheck 0` · `vitest 4455/4455` (baseline 4416, **+39**) · `build` 828.5 KiB / 900 cap ·
`gitleaks` 0 · `MCV` 0 (35/35 bindings) · **PROTOCOL_VERSION unchanged at 46** ·
**zero art generation — $0.00 spent on veo/imagen**

## What shipped

| | |
|---|---|
| **P1** | Warlord RAGE: a washed-red tint, the animation rate halved with the cadence, and the first test ever to cover the ×2 *wiring* |
| **P2** | The direwolf packed and wired off the goblin-hound stand-in — **$0** |
| **P5** | Codex ordered by CONNECTOR count (R175-A) + S174's owed discovery tests |
| **P6** | Connectors/shapes phase out under a standing tower, back in when it breaks |
| **P4a** | The Voltkin TV is a real building standing on its chain |
| **P4b** | Cutscene removed; Voltkin climbs out of the TV in-game |

## Owner rulings recorded this session
- **R175-A** codex tier = **connector count**; if a tower lands oddly, fix the GAME not the sort.
- **R175-B** connector hiding includes defenders — but they have no art yet, so *"focus on the race ones we have."*
- **R175-C** the direwolf's die row is his purpose-drawn corpse, not a clip window.
- **R175-D** veo really costs ~**$3.10/clip** (~$25 for 8), not the $0.50 in the doc.
- **R175-E** rage must *look* 2× faster, not just be 2× faster.
- **R175-F** the cutscene goes **when** the in-game emergence replaces it.
- **R175-G** the TV must become a real tower with a build/emerge/damage/destroy loop.

## ⛔ NOT DONE — say it out loud, do not bury it
1. **P7, the atlas debt — NOT APPROVED.** Explained in plain language at close; awaiting his word.
   ⚠ The tool is **not** `scripts/matte-*.py` (those are unrelated one-offs). The real matte is
   embedded Python in `build-sprite-atlas.mjs:203-286`, and there is **no tighter-edge knob** —
   the erosion is hardcoded at `:284`. P7 means **adding the knob first**, then re-cutting 9 sheets. $0.
2. **The Warlord's attack-CADENCE wiring is untested** (arithmetic only). Movement *is* behaviourally
   proven. Said in `warlordRage.integration.test.ts`'s header, not hidden.
3. **Voltkin attack + die clips**: unpacked, un-re-rolled, per the cost ruling.
   ⭐ A.0 measured **voltkin/walk is recoverable at $0** (frames 36–71 clean) — the S173 "must re-roll"
   claim is WRONG, which takes the eventual bill from $1.50 to $1.00.
4. **`ART_VEO_PROTOCOL.md` is stale in two places** and both cost money: its S173 verdict table
   condemns `direwolf/walk` which now measures a clean PASS, and §5's cost table is ~5× low.
5. **tv-4-critical / tv-5-explosion** are flattened but not in the atlas — the destruction beat is unbuilt.
6. `atlas-guard` still red (9 fringe + 6 size) — unchanged, not worsened; both new sheets pass.

## Next session, in order
1. **P7** if he approves — get `atlas-guard` green so a real failure can be seen again.
2. The TV's **destruction beat** (tv-4 → tv-5 → tv-6), frame-driven, copying `TOWER_CRUMBLE_FRAMES`.
3. General/goblin tower art when he generates it → connector hiding covers each with **one** call.
4. R173-B · B7+B8 damage numbers · Pharaoh stances + Ra · Vlad life-sap · NONET stages.

## Traps this session paid for
- **A Pixi tint is a MULTIPLY.** `0xff0000` would have made the Warlord black, repeating S151.
- **A pre-matted still packs inside a black box** — the matte keys near-WHITE; building stills are
  RGB on 254-white. Three rebuilds before I looked at the PNG.
- **`ignitedAtTick` is stripped on the wire.** It is the obvious ramp anchor and it is a trap;
  `Bond.createdTick` is the correct one.
- **Reconcile per-frame state at the frame boundary, not on read** — otherwise a fog-skipped shape
  phases in *as you look at it*.
- **MCV needs ABSOLUTE paths** for diff-binding; a relative one passes as an assertion and still fails the gate.
