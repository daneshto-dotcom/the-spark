# HANDOFF — S170 — 2026-09-09

**Status: CLOSED. 6/6 approved priorities complete, verified, pushed and live.** `master` clean,
0 unpushed, `verify-deploy` 4/4. `PROTOCOL_VERSION` 45 (unchanged).

⭐ **This session existed because S169 auto-compacted mid-close and wrote a handoff full of false
claims. The first thing done here was auditing it — 12 agents plus hand-verification — and that
audit is why everything below is trustworthy: nothing in it is asserted from memory.**

---

## PROJECT
- `The Spark` · `master` @ `b5a7e8d` · TypeScript / Vite / Pixi / Trystero WebRTC
- Live: **spark-online.space** (Pages; pushing `master` IS shipping)

## CURRENT STATE — every exit code from a captured `$?`
| gate | result |
|---|---|
| typecheck | 0 |
| vitest | 0 — **4202** tests / **267** files (from 4159/262) |
| e2e:gating | 0 — 65 passed |
| e2e:races | 0 — 5 passed |
| check:atlas | 0 — 30 atlases clean |
| build | 0 — 814 KiB of the 900 KiB charter |
| verify-deploy | 0 — 4/4 carriers |
| MCV | 0 — **75 bindings**, hard_fail=0 |

Static payload **114.3 → 104.8 MiB** (the dead 10 MB mp3 removed).

---

## ⛔ THE ONE THING THAT NEEDS THE OWNER'S EYES

**Fog of war was rebuilt FOUR times this session.** He confirmed the final one ("ok better"), but the
**outcome is not machine-verified**, and the reason is now logged in `e2e/fog.spec.ts` rather than
hidden: Pixi implements alpha masks as filters, and `renderer.extract` does not apply filter effects,
so this harness **cannot read a composed frame**. The culling itself IS unit-tested
(`concealment.test.ts`, 12 tests).

Three ways to close that gap are written into the spec: a Playwright screenshot comparison, a
RenderTexture readback instead of `extract`, or asserting per-renderer visibility through the display
list. **Until one lands, the owner's eyes are the only check on the outcome.**

---

## WHAT THE S169 AUDIT FOUND — 7 false claims, verified against the tree

| S169 claimed | truth |
|---|---|
| "the ONLY defect is the backdrop" | Three. Backdrop, border walls, and the backdrop still painting over the shapes. |
| "P2 the Pharaoh — NOT STARTED, no code was written" | False. 237 lines in `333e036` with 6 passing tests. |
| "two pharaohs — FIXED" | False. It fixed one of **two** gates; the one he hit survived. |
| "Nothing in the codebase is called sudoku" | False. ~339 refs across 30+ files, plus a shipped bundle chunk. |
| "No boss skill draws anything at all" | False. The zombie death explosion draws AND sounds. |
| "tier-3 towers produce … limitless" | False. They silently stopped at 10 live units, permanently. |
| fix was in `src/state/creatureLifecycle.ts` | Wrong path — it is `src/state/creatures/creatureLifecycle.ts`. |

**And it recovered two whole design conversations the compaction had destroyed** — the
tower/connector consolidation and the art COST pipeline — verbatim from the raw transcript, plus the
Voltkin rework which had left no trace anywhere.

⚠ **Four of my OWN claims were also wrong, all from trusting memory files over the tree**: elemental
castle keeps (decided out — the memory file was the only surviving carrier), die animations (they DO
play for tier-3 and bosses), bot-intelligence §7 (answered in S154), and the mp3. Both memory files
corrected.

---

## THIS SESSION'S WORK (14 commits)

**Owner bugs, all live in production before this session:**
- **The second boss no longer waits.** Two gates existed; S169 removed the direct-dispatch latch
  (provable in a unit test, invisible in play) and left the `bossAlive` gate in `hostTick`'s release
  arm. `t9BossTower.test.ts` had pinned the WAITING as correct — **the suite was green ON the
  defect** — and is now inverted, asserting two bosses alive through the real host tick.
- **Tier-3 towers are truly limitless** (R158). `GOBLIN_MAX_PER_SPAWNER = 10` silenced them forever
  once ten units were alive; nothing culls at a phase edge. Fixed as an EXEMPTION, so the goblin
  tower keeps its own bought ceiling — both halves asserted.
- **The sudoku fires on 12+ CONNECTORS** (R159), counting bonds with `>=` instead of shapes with
  `===`. Twelve connectors is not a fixed shape count, which is why the unit mattered.
- **The drone stops faking an explosion** (R170) in its own zone — gated on hitting something, not on
  the fuse expiring.
- **The 10 MB `blue-steppe-orbit.mp3` deleted** (R168), verified safe first: the toggle plays the
  original from the `.ogg`.

**Fog of war — four attempts, and the fourth is the C&C model:**
1. Ground layer at the stage bottom (fixed the backdrop compositing over shapes — his S166 report).
2. An inverse Pixi mask. Concealed nothing: the mask texture was tinted `FOG_COLOR`, and an alpha
   mask samples a colour CHANNEL — the red channel of black is zero everywhere.
3. Tint fixed. Still wrong mechanism, and **untestable**.
4. ⭐ **Per-entity culling** (`render/concealment.ts`) — the way every RTS since the nineties does it.
   The classic model is three parts and this repo already had two (`exploredMemory` for the grid,
   S60's ghosts for last-seen buildings); the missing third was "don't draw it".
   **15 of 18 board renderers now cull.** The 3 that do not are each owner-ruled: `dragPreviewRenderer`
   (local drag only), `zoneBackgroundRenderer` + `wallRenderer` (must always be visible, R162).
   Also: your own quadrant is fully lit corner-to-corner via a zone-rect erase, not just around
   structures.

**Boss ability VFX (the code-only half):** the zombie rot aura (boiling ground, deliberately not a
ring — he said the death blast "looks just like a stink tower radius"), the Kraken's water crescent
whose foam edge coincides with the knockback, Vlad's inward life-sap drain, and two stun-star fixes —
the lift was unscaled so on bosses the stars sat *inside* the sprite, and three unit types the Kraken
stuns drew no stars at all.

⭐ **The sonar wave costs no new state at all**, because the determinism rule forced phase-spread by
id: `(world.tick + bossId) % KRAKEN_SONAR_INTERVAL_TICKS` is a pure function of two synced values, so
the renderer reconstructs when each Kraken fired — and even the aim axis re-derives.

**One new synced field**, `Creature.sapFlashUntilTick`: additive-optional (no protocol bump), at all
four sites plus the three hash sub-sites. It exists only because the owner required the effect be
visible to ENEMIES, and the sap's use-count is host-local.

---

## OPEN ISSUES
- ⛔ **The fog's outcome is not machine-verified** (above). Highest-value test debt in the repo.
- **`FOG_SHROUD_ALPHA = 0.55` is MINE**, not the owner's — how dark unexplored terrain reads.
- **6 untargetable bypass paths** are open but LATENT (nothing sets the flag). They go live the
  instant the Pharaoh's ritual does. Close them first.
- `LOCKED_DECISIONS.md` still re-ratifies the deleted energy mechanic (4 false claims in the register
  CLAUDE.md treats as the spec); `archive/energy/RESTORE.md`'s restore command names a commit that
  never touched 2 of the 3 files it lists.
- `origin/gh-pages` is deliberately preserved by consolidation — deleting it would take the site down.

## BLOCKED ON (owner decisions, not work)
1. **Vlad's tether** — R140 as ruled has NO victim (pure self-heal). Does the sap start draining a
   target? That is a mechanic change, not a visual one.
2. **Boss stats** — still mine; he ruled they scale to each boss's skill load.
3. **P10 wave ladder** — his recollection had 10 and 15 swapped vs the spec (R111: 5 ATTACK /
   10 DEFENCE / 15 HP / 20 PEN), and magnitude is **+1 point** (R118), not 20%, on HP/ATK.

## NEXT STEPS
1. **Owner looks at the fog**, then discuss **P11** (fully specified — see carry-forward).
2. **The Pharaoh** — six bypass paths first, then locust cone + Ra ritual.
3. **Redo Voltkin completely** (the lost ask).
4. **Boss VFX needing generation rounds** — zombie burst, dire wolves, rage stance, Archdemon pit.
5. **P15 the art cost pipeline** — his own accounts / free generators, one asset at a time.

## CHANGED FILES
43 files, +3116 / −384. Full list: `git diff --stat 14aeac2..HEAD`.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities **6/6 complete** | MCV 75 bindings hard_fail=0
- P1 LAYER STACK + FOG (4 attempts) — completed — `842f93f`+`7e86fba`+`af251a3`+`7558308`+`89ca7b1`+`38d494d`
- P2 SECOND BOSS + host-migration fuse — completed — `5164080`
- P2b DRONE FIZZLE (R170) — completed — `5164080`
- P3 TIER-3 LIMITLESS (R158) — completed — `5164080`
- P4 SUDOKU = 12+ CONNECTORS (R159) — completed — `5164080`
- P5 BOSS ABILITY VFX — completed — `a5b9fab`+`5884e8d`+`d4c2b3d`

## REFLEXION ENTRIES (6, in `.claude/reflexion_log.md`)
The one that matters: **`#research-the-standard-solution-before-inventing-a-mechanism`** — three
shipped-and-wrong fog attempts, and the owner had to ask twice ("did you look into them or no? I said,
look at Red Alert") before I looked. Sharper half: **prefer the mechanism the SUITE CAN SEE** — all
three failures passed every gate because masks and compositing are invisible to this harness.
Others: a green test can be DEFENDING the bug · gate the effect on the OUTCOME, not the trigger ·
one population's ceiling silently gating another · the UNIT of a ruling is part of the ruling ·
a private renderer method silently scopes a condition to one renderer.

## CARRY-FORWARD
**17 items in `session-state.json.carry_forward_s170`**, each with its own verdict — what SHIPPED (so
nobody rebuilds it), what is blocked on a decision vs merely unstarted, and which numbers are mine.
⚠ The stale `carry_forward_s169` block was REMOVED: several of its items shipped today, and a stale
carry-forward read as current is exactly what cost this session.
