═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — SPARK
Generated: 2026-09-23
Session: S187 — the owner's upgrade-draft spec, built end to end
═══════════════════════════════════════════════════════════

## PROJECT
- Name: SPARK — deterministic, host-authoritative multiplayer builder game
- Working directory: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark`
- Git branch: `master` · Latest commit: `a5ba767` (+ handoff commits)
- Tech: TypeScript / Vite / Pixi / Trystero WebRTC · Live at **spark-online.space**
- Codebase: 342 test files, 5,615 tests

## CURRENT STATE
- **Typecheck: 0** · **Vitest: 5,615 / 342 files, all passing** · **Build: 0**, 919 KiB against the 1000 KiB charter
- **MCV claim verifier: exit 0**, 49 verification bindings passing
- Deployed: all five priorities live. `master` pushed, **0 unpushed**, remote reachable.
- ⛔⛔ **PROTOCOL_VERSION 48 → 49.** A pre-S187 tab is refused at HELLO; both players need the new build.

## SESSION COST
Model routing data unavailable (statusline reported dead all session; `statusline.pid` stale ~10.3M s).
Cumulative log: `~/.claude/usage-log.csv`.

## THIS SESSION'S WORK

**P1 — the draft substrate.** His floor-at-one percentage rule (`applyDraftPercent`): 10 % of the
ladder number, floored, minimum 1, compounding. The castle unit is 1/1/1/1 (R125) so its pool is 6
fifths; 10 % is 0.6 and `damageEntity` throws on non-integers. His rule is what makes a percentage
expressible at all, and it supersedes R118's flat `+1 POINT` (the same step for a chewer and a Kraken).
⛔ **It exposed a wire bug:** `serializeCreature` emitted `ehp` only when *below* the config pool, so a
buffed unit at 7 against a pool of 6 wrote nothing and the peer rebuilt it unbuffed — a host-only buff
with no error. Fixed by storing `maxEhp` behind one accessor, `creatureMaxEhp`; the max had been
re-derived at 13 sites and the four that CLAMP now read the creature's own.

**P2 — the general track.** HP → DEF → ATK → PEN, cycling, so four options serve every level forever.
Wired into all five spawn sites in `applySpawnCreature`, the single choke point every creature is born
through.

**P3 — the draft panel.** 560 × 270 centred on the spawn disc, computed from `SPAWNER_RADIUS`, clearing
the footer by 321 px. Left tile choosable; right tile drawn but **absent from the hit-test** because
his instruction is that it is "not choosable". Verified by opening the game — which found two bugs
every test was green over (a shared `TextStyle` repainting the wrong tile; the dead tile drawing the
live tile's emblem).

**P4 — castle upgrades.** HP/ATK/DEF/PEN at 100 VP, capped 10 per axis, HP on his band table
(250/350/450/550/650). The gain is **baked at purchase**, never re-derived. DEF applies the ladder's
ratio to incoming damage, floored, never below 1 so a keep stays fellable. ⭐ This closes the
consequence canon §3b had to record and could not fix.

**P5 — the footer collapse.** His arrow design. ⛔ The half that matters is `isOverBandSurface`, not
`isOverChip`: teaching only the cursor would hide the menu while still refusing every placement
underneath it.

**Art + canon.** 17 upgrade cards ingested, cropped where needed and verified by eye. Canon §3d (the
draft + castle bands + "the racial buffs are NOT built") and §9d (four recurring questions closed).
280 MB orphan worktree deleted.

## OPEN ISSUES
- **RULED, NOT YET BUILT** (canon §9d, decisions final — do not re-ask): the lightning hub blast
  becomes **120 fifths** replacing the radial clear; the health bar's **three rules** (one number
  across board bar / damage art / character sheet, plus a bounded proportional WIDTH whose two bounds
  must be MEASURED).
- **No racial mechanic exists.** 13 ruled and illustrated, zero built.
- `l10-vampires.png` (THE SWARM) is very dark and may read as a near-black rectangle at 251 px.
- Statusline dead all session — real-token budget figures unavailable.

## BLOCKED ON
Him: the L10–L20 racial designs (16 slots) · judging THE SWARM in game · playing what shipped.
Nothing blocked on CI or infrastructure.

## NEXT STEPS (priority order)
1. **Play it** — the draft before wave 1, the footer arrow at the bottom.
2. **Wire the 17 cards.** ⛔ Read `assets-source/upgrade-cards/MANIFEST.md` first: the cards carry baked
   titles and the overlay draws one too, so they collide; the manifest states the resolution and that
   `drawAxisGlyph` is deleted rather than left dormant.
3. **Build racial mechanics** — 12 at L0/L5 plus vampires L10 are ruled and illustrated.
4. **The two RULED-NOT-BUILT items** in §9d.
5. **Design L10–L20** — 16 slots.

## MUSCLE MEMORY [Vigil]
- Traces: `C:\Users\onesh\.claude\traces\2026-09-23\The-Spark.jsonl` (2 rows)
- Loop: **closed**
- Next boot MUST read `boot-snapshot.md` ## Muscle memory before picking work.

## CHANGED FILES
28 commits this session. Principal new modules: `state/draft.ts`, `state/draftEvent.ts`,
`state/castleUpgrades.ts`, `render/draftOverlay.ts`, `scripts/check-upgrade-cards.mjs`,
`scripts/render-chewer-reference.mjs`, plus 7 new test files and 17 card assets.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **5/5 complete**
- P1 draft substrate — completed — `eb1cdd7`
- P2 general track — completed — `79dc7e8`
- P3 draft panel — completed — `6d08951`
- P4 castle upgrades — completed — `a145db4`
- P5 footer collapse — completed — `0a1e5f7`

## REFLEXION ENTRIES (this session)
9 entries in `.claude/reflexion_log.md` under the S187 block. Headline learnings: widening a value's
range invalidates every guard on it · mutation-test the guard · prove the feature REACHES what it
buffs · open the game · the forcing functions ARE the design review · a blocked command is not a
closed item · **check the canon before calling anything open.**

## CARRY-FORWARD PRIORITIES
None. All five priorities completed, checked and bound.

═══════════════════════════════════════════════════════════
