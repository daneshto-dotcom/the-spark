═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — SPARK
Generated: 2026-09-02
Session: S160 — races W1-A + the castle shoots back; the whole pre-defined list closed
═══════════════════════════════════════════════════════════

## PROJECT
- Name: SPARK — deterministic, host-authoritative multiplayer builder
- Working directory: `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark`
- Git branch: `master` (0 unpushed, credential healthy)
- Latest CODE commit: `afc63de` feat(s160-p5): the drone finally spends its 5 atk / 1 pen
- Session close commits after it are docs/bookkeeping only (`b854173`, `627961e`, `907bc66`) —
  a handoff cannot cite the commit that contains itself, so this line names the last commit that
  changed `src/`. `verify-deploy` confirms the live bytes match that build.
- Tech stack: TypeScript / Vite / Pixi / Trystero WebRTC · live at **spark-online.space**
- Codebase: ~122,590 lines across 446 `.ts` files
- **PROTOCOL_VERSION: 39** (bumped 38 → 39 this session — a v38 peer is REFUSED, no degraded path)

## CURRENT STATE
- Build: **exit 0** — entry 765.0 / 900 KiB, headroom 135.0
- Tests: **3458 / 3458 passing across 221 files** (boot baseline was 3399/218)
- `e2e:gating`: **exit 0, 62 passed** — exit code read from a redirect, never a pipe
- Deployment: **verify-deploy PASS 4/4** with content-hash equality · CI **8/8 success**, zero cancelled
- MCV claims verifier: **100 bindings, hard_fail=0, exit 0**
- Rule 22 runtime audit: clean (no config placeholders, no missing refs, castle gun stores nothing ⇒ restart-safe by construction)

## SESSION COST
- Model split: **all Opus** (631 messages) — ALWAYS-STRONGEST is the standing policy, so there is no
  routing spread to report and **no savings to claim**. Quality > cost, deliberately.
- Real context at close: **698,197 / 1,000,000 (69.8 % YELLOW)** · output tokens 815,417
- Cumulative log: `~/.claude/usage-log.csv`

## THIS SESSION'S WORK

**P1 — TURN: proved the code path, and found the instruction I had written you was FALSE.**
Traced `VITE_TURN_*` from `deploy.yml:149-156` → the Vite `define` → `iceConfig.turnFromEnv` →
`RTCConfiguration.iceServers` at `transport.ts:376-379` + `quickmatch.ts:173`, and proved the
inlining **from the emitted bundle** rather than from comments. Two real defects: `boot-snapshot.md`
said *"then push anything"* — false, because `deploy.yml` gates its push trigger on a paths allowlist
and **adding secrets changes no file**; and the inlining was true by *measurement* not by
*construction* (an aliased `import.meta.env` read has no textual match for the dotted `define`, and
worked only via a Vite-internal merge). Now dotted, with three new `ci.deployGate` assertions.

**P2 — both tower dials ruled KEEP, and three pieces of my own paperwork corrected.**
Stink: measured the drain through the real FSM — throws at ticks 254/538/822/1106/1390, gaps of
**284** not 240 (WINDUP+FIRE+RECOVER cost 44 a throw). Deleted a **false** justification (the per-bag
rack only draws in the atlas-null fallback, and the atlas ships). Drone: the dial advice in three
documents was **backwards** — the cap is inert slack (3→2 changes nothing, peak live is 2 of 3); the
cadence is the only linear lever. The 9-per-fight and the cap's slack are now **asserted**.

**P3 — all four numbers ruled KEEP; four of the five statements about them were wrong.**
`≤ 29 connectors` re-derived EXACT and asserted; "six connectors" re-framed as a **ceiling** (units
and bonds share the six link slots, a creature wins an exact tie); the R77 citation pointed at a file
where `grep max6` returns **zero** hits; four sites had silently typo-fixed the owner's *"maywe"* →
*"maybe"* inside quotation marks; and the hop-range's second justification **contradicted its own
citation** (120 > the ~90 px spacing it cited) — withdrawn. Bag floor documented **and** pinned
(goblin shield deals exactly 5 into a 5 pool — zero margin). Aura "12×" is the unit half only.

**P4 — RACES W1-A + THE CASTLE SHOOTS BACK · PROTOCOL 38 → 39.**
Executed the owner's pre-approved PDR from `SPARK_RACES_SPEC.md` §15, with every `file:line`
re-verified first by a 32-agent read-only pass (0 errors). New `src/state/races.ts` leaf module,
`Player.raceId`, save round-trip (additive-optional, byte-identical when all-default), the wire +
validator, all six bump sites, **B6** (race follows the peer, not the dense seat) and **B7** (the
`else` arm, or the host's own race is discarded at Begin). **B5 decided and recorded**: `raceId` is
NOT hashed because it is *immutable after Begin* — explicitly not the `raidPoints` precedent the spec
forbids — and that argument does **not** transfer to the tech perks.
⭐ **And the castle weapon the spec assumed existed:** `src/state/castleGuns.ts`. Owner ruled
**NEAREST ENEMY IN RANGE**, superseding Q4's retaliation-only. No stored timer — the schedule derives
from `world.tick`, so nothing to hash, no bump, and **W1-B's per-race attack VFX needs no wire field**.

**P5 — DRONE AoE. R77's deferred-mechanics list is now EMPTY.**
`DRONE_ATK`/`DRONE_PEN` had reached the config and stopped for six sessions. Now spent: 30 fifths to
units, 418 of 1000 to shapes, owner spared. **Closed ADDITIVELY** — the unconditional 3-connector
sever is the owner's separate COUNT ruling and stat-gating it would have made the drone *weaker*
against big fortresses, exactly as `constants.ts` had warned six sessions earlier.
`pinnedDeadStats.test.ts` fired as designed and was **inverted, not deleted**.

## OPEN ISSUES
- ⛔ **B2 — seat elimination does not exist.** The first castle to reach 0 HP ends the match for
  everyone, victory to `survivors[0]` = Map iteration order. Gates all 4-player reasoning in the races
  spec, and it bit S160 twice as a test-repair trap. **Needs an owner ruling.**
- ⛔ **B1** blocks the castle emitter (W1-C); **B3** blocks the selection UI (W1-A item 5).
- ⚠ **N2 raid parity** — needs an owner observation, not a fix (carried unchanged).
- ⚠ **Held debt:** `Creature` carries four parallel nullable committed-target fields
  (`creature.ts:237` records why a discriminated union was rejected). Amendment-only.
- ⚠ **OS-level, diagnosed not fixed:** `s96-p2-state-inject.test.sh` fails 1 of 12, identically before
  and after. Needs a RULE decision this session lacked authority for.
- ⚠ **Stale artefact found, left in place:** the project-root `reflexion_log.md` is frozen at Session
  54 (May) while the live log is `.claude/reflexion_log.md`. Not deleted — flagged for a decision.
- ⚠ **[STEP 5 check failed: MCV re-fired AFTER the handoff regenerated boot-snapshot.md]** — one P2
  binding asserted `boot-snapshot.md` contained the corrected drone dial order, and STEP 2.95
  regenerates that file from scratch, so the binding was guaranteed to break at close. **Same class
  as S159 P9's BACKLOG-status-text lesson, which I had re-appended to the reflexion log an hour
  earlier in this session.** Fixed twice over: the binding is rebound to `src/constants.ts:1960`
  (durable), and the SUBSTANCE was added to `carry_forward_s160` because the regeneration had
  silently dropped the correction out of the next session's PRIMARY boot file — the bigger miss of
  the two. MCV re-run to a fresh path: **hard_fail=0, exit 0**.
- ⚠ `origin/claude/castle-races-unique-towers-xb9uzi` is **fully merged** into master and left in
  place deliberately: it is the owner's branch from another account, so pruning it is their call.
  `origin/gh-pages` untouched for the fourth session running (deleting it could disturb Pages).

## BLOCKED ON
- **TURN provisioning (owner).** Three repository secrets, then **Actions → Deploy to GitHub Pages →
  Run workflow**. ⚠ NOT "push anything" — S160 P1 proved that false.
- **Owner playtest + rulings:** the castle cadence (240 t) and chain-lightning falloff, both ruled
  keep-and-play-first.
- **Owner ruling on B2** before the races spec's 4-player scenarios mean anything.

## NEXT STEPS (priority order)
**Immediate — next session is the race expansion, on fresh context, PDR already approved:**
1. Read `.claude/plans/2026-09-02_PDR_RACES_W1_PREAPPROVED.md` (stamped **IN-PROGRESS** with exactly
   where S160 left it) then `SPARK_RACES_SPEC.md` §6 **W1-B**.
2. **W1-B — generate the art.** 57 pieces, **six** castles × three states (`assets-source/zombie-castle/`
   contains a HOUND, not a castle). ⚠ Standing rule: **spike it and show the owner before wiring**;
   original style only; clean transparent matte. The zombie hound attack clip has failed twice (CF-S153-c).
3. Per-race gatherer silhouettes (render-only, zero wire cost — the cheapest identity win in W1-B).
4. Per-race castle attack VFX — now real, and needs **no** wire field.

**Short-term:** the **selection UI** (gated on B3's transport decision — follow the `LOBBY_READY`
precedent, own bump); **B2 seat elimination** once ruled; the **tech draft** (§9), which the spec calls
the highest identity-per-effort item and which needs only `raceId`.

**Medium-term:** W1-C (the castle produces — gated on B1) · W1-D (Layer 1 upgrades) · Wave 2's six
tier-3 towers in **one** batch (each `GodlyId` is a serialized literal, so six landings = six bumps).

## CHANGED FILES
41 files changed, 4450 insertions(+), 731 deletions(-) across `eb1958f..b854173`.
New: `src/state/races.ts`, `src/state/races.test.ts`, `src/state/castleGuns.ts`,
`src/state/castleGuns.test.ts`, `src/net/raceWire.test.ts`, `SPARK_RACES_SPEC.md` (merged from the
owner's branch), `.claude/plans/2026-09-02_PDR_RACES_W1_PREAPPROVED.md`.

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: **5/5 complete** | 698K/1000K (69.8 % YELLOW)
- P1 TURN proof + corrected false deploy instruction — completed — `93f30ab`
- P2 both tower dials ruled + 3 own-paperwork corrections — completed — `8b041be`
- P3 four numbers ruled + 4 statement corrections — completed — `551bc34`
- P4 races W1-A + castle guns, PROTOCOL 38→39 — completed — `3f3737c`
- P5 drone AoE, R77's list emptied — completed — `afc63de`
- Deliberation: **Council WAIVED on P4** by explicit owner instruction (their pre-approved PDR says
  "do not re-council"); the line-number verification it *does* ask for was run as a 32-agent
  read-only workflow. P1/P2/P3/P5 Micro-tier, auto-waived on the user path.

## REFLEXION ENTRIES (this session)
14 entries appended to `.claude/reflexion_log.md` (the live log; the root copy is a dead artifact
frozen at Session 54). Highest-signal:
- An assertion that greps a bare word reads the DOCUMENTATION, not the code — **five times** in one
  session, because good docblocks name what they forbid.
- Shipping a mechanic can silently **delete a different one**, and nothing is designed to notice: Q3's
  castle cadence killed the castle-kill win condition; ten unrelated red tests were the only signal.
- When a new mechanic reddens old tests, fix the fixture or the design, **never the assertion** — and
  my first repair (zeroing castles) was worse, because it trips the B2 win gate.
- The owner was right twice: I had not read all of their spec, and I stated a **fixture reading as a
  law of the game**. Report the ordering a fixture establishes, not the integer it produces.
- I nearly reported a green gate from a **leftover `/tmp` log belonging to session S153**.

## CARRY-FORWARD PRIORITIES
None incomplete. All five shipped, and the pre-defined list is closed at the owner's explicit
instruction that nothing be left buried behind the multi-session race expansion. The 14-item
carry-forward in `session-state.json` (`carry_forward_s160`) is **forward work + owner decisions**,
not unfinished session work.

═══════════════════════════════════════════════════════════
