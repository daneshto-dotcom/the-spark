# Boot Snapshot (auto-generated at handoff)
Generated: 2026-06-28 | Session: S112

## Next Steps
1. ✅ DONE this session — HELGA veo character is LIVE on https://spark-online.space/ (idle/walk/slap atlas replaces the procedural puppet; theme on walk/attack, base music when idle; HHWAPAH+clap slap). Owner will PLAYTEST her next.
2. PLAYTEST DIAL (owner to confirm): `PRINCESS_SPRITE_BASE_SCALE = 0.34` (her in-world size) — one-number tune + redeploy if too big/small. Build her: Triangle hub + 3 Spirals + 3 Circles.
3. DEPLOY going forward stays MANUAL: `npm run deploy` (GitHub Actions still dead — account billing-lock). A plain `git push` does NOT deploy.
4. Batch C (lightning-drone building) — front of the line: own PDR + 3-way Council + 9 owner design Qs; PROTOCOL_VERSION 13→14.
5. OWNER (non-blocking): clear the GitHub account billing lock (Settings → Billing) → can then revert to private + Pages workflow-mode auto-deploy.
6. Resume ROADMAP: Tier-1 G-series (G1b MOTION / G2 family traits / G3b silhouettes / G4 crown) → Tier-3 host-migration.

## Blockers
- GitHub ACCOUNT billing-lock kills all Actions (deploy.yml + e2e). Worked around (not fixed) via PUBLIC repo + classic gh-pages Pages builder. NOT blocking play or new deploys (`npm run deploy`). Owner clears when convenient.

## Pending Backlog
- Batch C lightning-drone building (PDR + Council + 9 owner design Qs)
- ROADMAP Tier-1 G-series → Tier-3 host-migration
- Owner-gated: anti-coast structure-loss CLAWBACK; worker-sim ?worker=1 cutover

## Recent Reflexion (last 2 sessions)
## 2026-06-28 — Session 112: Shipped HELGA as a real veo-animated character (idle/walk/slap tick-synced atlas) replacing the procedural puppet — owner loved the walk clip. State-driven audio (theme on walk/attack, base music when idle; HHWAPAH+clap slap). No wire/protocol change; tsc 0, vitest 1716/1716, deployed live (eeffb5e).
- S112 #veo-img2vid-is-strong-on-ambient-motion-weak-on-fast-specific-actions: veo holds a character on-model for walk/idle but can't produce a crisp slap from a target-less seed — generate veo for ambient states, sell the HIT with SFX+VFX+lunge; reframe violent prompts non-violently to dodge the content filter.
- S112 #matte-a-veo-clip-needs-component-keep-plus-shared-foot-canvas: for an animation, extend the border-flood still-matte with largest-component speck-removal + a SHARED foot-anchored canvas (per-frame crop jitters); diagnose alpha empirically before assuming the matte is broken.

## 2026-06-27/28 — Session 110/111: Shipped + then deployed the 5-priority owner-playtest batch (win 1500, uniform spark speed, codex avatar, Helga walk v13, matted Voltkin/Helga art). Root-caused the 2-day deploy block to a GitHub ACCOUNT billing-lock (kills ALL Actions); bypassed via PUBLIC repo + classic gh-pages builder; added `npm run deploy`.
