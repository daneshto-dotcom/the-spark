═══════════════════════════════════════════════════════════
HANDOFF SUMMARY — The Spark
Generated: 2026-05-31
Session: S59 — Memory-fog batch: P1 SHIPPED (remembered areas); P2 + P3 carried forward
═══════════════════════════════════════════════════════════

## PROJECT
- Name: spark (Phase 1+2) — 2-player real-time WebRTC game (Pixi.js 8.18 + Trystero/Nostr)
- Working dir: C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark
- Git: branch master — standalone `the-spark.git` — synced (pushed 17772b4)
- Tech: TypeScript 5.9 / Pixi.js 8.18 / @trystero-p2p (Nostr) / vite 6.4 / vitest 3.2.4 / Playwright 1.60

## CURRENT STATE
- Build: `tsc -b` clean. Bundle NOT re-measured this session (P1 is a small render addition; ~503–505 KB est, <550 cap — re-measure next session).
- Tests: **908 unit pass / 0 fail** (was 899; +9 from exploredMemory). **fog e2e 2/2**. Full smoke/sim-rate e2e NOT re-run this session (see VERIFY-NEXT-SESSION).
- Deployment: P1 pushed (`17772b4`); CI Deploy + E2E triggered — **confirm conclusion next session** (the tool channel was degraded this session, see ENVIRONMENT NOTE).

## THIS SESSION (S59) — 1 of 3 priorities shipped; P2/P3 designed + carried
Full pipeline ran: A.0 empirical state-discovery → Full-tier batch PDR → **2-round 3-way Council** (Grok grok-4.20-reasoning + Gemini 2.5-pro) → PRIME-AUDIT → P1 execute → per-priority CHECK.

### P1 — Memory-fog "remembered areas"  (SHIPPED · commit `17772b4`)
The StarCraft dimmed last-observed tier: terrain you've scouted stays a **dim OPAQUE shade** after your live vision leaves; never-seen stays near-black; live stays clear — **3 tiers in the single existing fog overlay, NO live-board leak.**
- NEW `src/state/exploredMemory.ts` — pure coarse explored grid (48×27), circle-rect **OVERLAP** marking, `isExplored`, `resetExploredGrid`. 9 unit tests. LOCAL per-peer, off determinism/netcode.
- `src/render/fogRenderer.ts` — grid rasterized into a tiny canvas-backed texture → **OPAQUE `MEMORY_FOG_COLOR` overlay (normal blend)** added to `maskScene` between the dark base and the live `'erase'` brushes; change-gated texture re-upload (`markVisible` returns `changed`); match-start (PLAYING-edge) memory reset.
- `src/constants.ts` — `EXPLORED_GRID_COLS=48`, `EXPLORED_GRID_ROWS=27`, `MEMORY_FOG_COLOR=0x161b2e`.
- CHECK: tsc clean · 908/908 unit · fog e2e 2/2 · GROK-ANALYST (fixed center-only coverage → circle-rect overlap; refuted a "bilinear leak" via blend math).

### Council + PRIME-AUDIT (covers P1/P2/P3)
- **ADOPT HYBRID**: pure unit-testable explored **grid** for explored STATE; proven half-res **RT** kept for LIVE fog. **M1 (dim-the-fog) REJECTED** (live-movement leak). Memory **permanent**; **structures-only** (mobile excluded).
- PRIME-AUDIT 3 deltas: (1) refuted Gemini's **inverted layer-order** recipe → memory layer must be ABOVE the fog, masked by `liveMaskRT` (Pixi v8 `sprite.mask` = alpha — confirmed) — **critical for P2**; (2) **kept terrain-dim** vs the R2 "structures-only" consensus (matches the literal ask + is the zero-ghost-risk piece; Gemini self-contradicted R1↔R2); (3) refined the "ghost structure" hazard as actually StarCraft-correct.
- Self-caught pre-commit: a partial-**ERASE** memory overlay would have **leaked** the live board (translucent fog reveals the board); switched to an **opaque dim colour** (source-over on the opaque base keeps mask alpha = 1).

## CARRY-FORWARD — P2 + P3 (re-confirm the PDR per Rule 11, then execute)
Both are DESIGNED + 2-round-Council + PRIME-AUDITed this session (draft batch; needs a fresh `go`). They live in `.claude/session-state.json` (status=pending, pdr_approved=false) + the batch PDR in this session's transcript.

### P2 — Last-seen STRUCTURE memory  (the RISKIEST piece — give it fresh focus + a memory e2e)
- A CPU `Map<PrimitiveId,{pos,type,color}>` of ENEMY structures, updated each tick a structure is currently in live vision.
- A dim `memoryLayer` Container of structure-silhouette sprites placed **ABOVE** `fogRenderer.container`, `.mask` = a Sprite of `liveMaskRT` (alpha mask → paints ONLY in non-live areas), gated by `isExplored && !live`.
- Ghost-resolution state machine: a remembered structure stays a stale silhouette until you re-scout its cell and confirm it's gone (StarCraft-correct). **Exhaustive UNIT tests** for the Map state machine (seen-on-live; drop-on-confirmed-destroyed-while-live; never drop on mere fog) — this is the Council #1 hazard.
- Renderer facts (read this session): structures = `world.primitives` (a Sprite per prim, `tint=ownerColor`, at `prim.pos`) in `StructureRenderer.primitiveLayer`; shape textures via `makeShapeTextures(app)` (`src/render/shapes.ts`). `fogRenderer.container` is added above avatarRenderer, below HUD (`main.ts:272-278`). Memory layer must sit ABOVE `fogRenderer.container`.
- Corrected composite (Council-ratified): board → liveFog (opaque, live holes) → memoryLayer (dim structures, masked by `liveMaskRT`).

### P3 — Fog polish (the new layer needs a/c/d)
- (a) WIN-fade freeze: when `fogTargetAlpha==0`, freeze the mask recompose so the win-lift is a uniform dissolve (no cutout drift). `fogRenderer.sync` recomposes every 3rd frame regardless of fade today.
- (c) `fogRenderer.reset()` on RETURN_TO_TITLE (`main.ts:340/344/378`): clears explored grid + (P2) last-seen Map + hides brush pool. (P1 already auto-resets on the PLAYING-edge; this is the explicit teardown hook.)
- (d) Wire the existing `fogRenderer.destroy()` into a teardown path + free `gridTex` (and P2's memory textures). `main.ts` has no app teardown (static SPA) → dev/hot-reload only, low urgency.
- (b) world→screen transform: **DEFERRED (YAGNI** — no camera; world==screen).

## VERIFY NEXT SESSION (carried checks)
- Confirm CI conclusion on `17772b4` (`gh run list`) — Deploy + E2E. (Couldn't confirm this session — channel degraded.)
- Run the FULL smoke e2e (sim-rate canary) once. P1's new work is inside `fogRenderer.sync`, which early-returns under `__FOG_DISABLE__` (the gameplay smoke specs), so the canary should be unaffected — confirm green.
- Re-measure bundle (<550 KB cap) after P1.
- **USER live 2-peer smoke** (carried from S58): all 4 S58 fixes + a lossy-network run for the P4 host-TTL edge; now also eyeball the remembered-areas dim tier + tune `MEMORY_FOG_COLOR` / grid resolution if it reads blocky or too dark/light.
- P4 host-TTL backstop (S58 carry) — only if the lossy smoke shows a stuck-carry.

## ENVIRONMENT NOTE (S59)
The tool-output channel returned results in **heavily-delayed bursts** this session (many empty probe round-trips per delivered result). It was **latency, not data loss** — every Write/commit/state-update landed correctly — but it slowed execution and drove the decision to checkpoint after P1 rather than grind the riskiest piece (P2) through degraded tooling. If it recurs: prefer **fewer, larger verification batches** + node file-writes (reliable) over many small probes; lean on JSON test reporters parsed in node for ground truth.

## REFLEXION (S59) — see .claude/reflexion_log.md
- P1 #memory-fog-opaque-not-translucent #grok-refute-and-fix
- SESSION #method-opaque-not-translucent-fog (dim tier = opaque colour change, never alpha reduction, or it leaks)
- SESSION #method-prime-audit-refute-not-rubberstamp (independently refuted 2 reviewer claims + 1 false consensus; caught 1 real bug)
- SESSION #method-checkpoint-under-degraded-tooling (checkpoint at clean boundary, carry the risky piece)

## SESSION PIPELINE REPORT
Pipeline: Session PDCA v2 | Priorities: 1/3 shipped (P2/P3 carried) | Full tier (2-round Council) | P1 GREEN
P1 `17772b4` · ANALYZE + Rule-22 audit + reflexions flushed to session-state

═══════════════════════════════════════════════════════════
S59 close: Memory-fog P1 (remembered areas) shipped + deployed — a pure
unit-tested explored grid + an OPAQUE dim overlay giving 3 fog tiers in one
overlay with NO live-board leak; 908/908 unit + fog e2e 2/2; pushed 17772b4.
P2 (last-seen structure memory) + P3 (polish) are Council-designed + PRIME-
AUDITed and carried forward — re-confirm + execute next session on a fresh
channel, with exhaustive unit tests for P2's ghost state machine. 🌫️⚡
═══════════════════════════════════════════════════════════
