## S139 (2026-08-11) — damage switched ON + the first free unit (the goblin) + PROTOCOL 17→18

3 of 4 priorities shipped and deployed. The session's defining discovery was that S138's damage
substrate had ZERO production call sites — perfect, tested, dead code for a whole session — and the
Council's defining contribution was catching that the Stink Tower shipped first would have been an
inert prop, since nothing in the game could attack a defender. Cut line re-planned; the Stink Tower
defers with its spec intact and every dependency now in place.

- #a-subsystem-can-be-perfect-tested-and-never-called: S139 P1: S138 shipped damageEntity as 'the ONE way anything in the world takes damage' - typed, serialized, hashed, 22 passing tests, a docblock stating the contract, and a handoff recording the blocker as GONE. A.0 found it had ZERO production call sites. The only importer in the entire repo was its own test file, while three live damage paths went on calling damageCreature directly. It was correct, tested, dead code for a full session. The reason no test caught it is the important part: all 22 unit tests called damageEntity THEMSELVES, so every one passed while the game never invoked it once. The defect was not in the function's behaviour, it was the ABSENCE OF AN EDGE from the game to the function - a property of the import graph, which no behavioural test can see. Two durable lessons. (a) 'Shipped' means called, not merely present and green; a new dispatcher's acceptance criterion must include a named production caller. (b) When a claim is about reachability, the guard has to read the import graph - damage.wired.test.ts is a filesystem scan for exactly that reason, and it strips comments first so a docblock mentioning the symbol cannot masquerade as a call site.

- #pin-the-relationship-not-the-value: S139 P1: the obvious lock test was PRIMITIVE_MAX_HP === 1000. That would have been nearly worthless. The number is not load-bearing as a number - it is load-bearing because 1%, 2.5% and 5% of it are INTEGERS, which is what makes damageEntity's integer guard unfireable at runtime. Pinning the value still permits someone to 'improve' it to 1024 and silently destroy that property. So the tests assert the RELATIONSHIPS: the percentages land on integers, DOT_CADENCE_TICKS === 0.5*PHYSICS_HZ, and attackFireTick === chewHits * CHEW_INTERVAL_TICKS for every chewing config. That last one existed ONLY as a comment ('chewHits: 5, // = constants.ts CHEW_HITS') beside two hardcoded literals, and A.0b measured that breaking it makes the sever dispatch never fire - the chew caps forever with the bond intact. Generalisation: when you lock a constant, first ask what PROPERTY makes it correct, and lock that. Also add a non-vacuity assertion (chewers.length > 0), or a future refactor that empties the collection turns the guard green and silent.

- #the-real-physics-test-caught-what-i-had-just-written: S139 P2: I fixed two spawn bugs that A.0b had FOUND FOR ME, then wrote a goblin that closed on its target, entered ATTACKING, and never hit anything. Every unit test passed. The FSM state was right, the target was right, the range test was right - and the damage never landed, because the Voltkin ATTACKING bounce aborts when `!bondValid && !creatureValid` and both are false for a structure attacker, so it bounced out before its fire tick on every single cycle. What caught it was the real-physics acceptance test asserting the VICTIM'S HP ACTUALLY DECREASED. A state assertion (is it ATTACKING? does it have a target?) would have passed happily. What made it diagnosable in two minutes was a throwaway scratch test that printed state/ticksInState/distance/hp every 80 ticks: `tis 0` recurring at ticks 160/240/320 with hp pinned at 1000 named the mechanism immediately. Two lessons. (a) The acceptance criterion for a new attacker is that the TARGET changed, never that the attacker reached a state. (b) This is the same defect class as P1's dead dispatcher - correct code that is never actually reached - and I reproduced it in the very session I spent fixing it. Assume you will too, and write the test that observes the EFFECT.

- #three-hand-synced-numbers-is-not-an-invariant: S139 P4: e2e/smoke.spec.ts asserted 'v15' while PROTOCOL_VERSION was 17, and set a peer override of 16 for a test titled 'Newer-version joiner' - so a test whose entire purpose was the newer-peer branch had silently inverted into the older-peer branch. S133 P2 had ALREADY FIXED this exact defect once, and left a ⛔ comment saying a future bump must update this line and the two below. It recurred anyway, and stayed invisible because the describe is @quarantine-flaky and the gating lane grep-inverts that tag, so nothing could go red. The lesson is not 'read the warning'. A comment asking a human to keep three numbers in sync across one file is not an invariant, it is a wish; and the second recurrence is the evidence. The fix is structural: ONE `LOCAL_PROTO_V` with `NEWER_PEER_V = LOCAL_PROTO_V + 1` DERIVED, so the inversion cannot be expressed. When you find a stale constant that a prior session already fixed once, do not fix the value - delete the opportunity.

- #a-hidden-pane-cannot-screenshot-but-the-scene-graph-can-be-interrogated: S139 P2: my own PRIME-AUDIT made 'LOOK AT THE RENDER' mandatory, because a 4th CreatureType is invisible across 8 non-exhaustive branches with zero switches and no state assertion can see 'nothing was drawn'. Then the screenshot failed: the Browser pane is hidden, so rAF is paused, the Pixi ticker never advances and the canvas composites no frames (S129 hit this and recorded 'visual unverified'). Rather than accept that again, I drove `app.ticker.update()` manually and interrogated the scene graph: aboveFogLayer.children.length === 15 (matching the fog roll-call), children[4] is the goblin's _Graphics with 19 draw instructions and a 25x27 px bounds. That is STRONGER evidence than a screenshot - it proves the draw path executed and emitted geometry, and a picture of a paused canvas would have proven nothing. The same probe incidentally confirmed the grant fires live and that creatureTypes was ['goblinMelee'] rather than ['voltkin'], verifying a fixed trap in the running app instead of only in a unit test. Generalisation: when a visual check is blocked, ask what OBSERVABLE the render would produce and assert that instead of downgrading to 'unverified'.

## S138 (2026-08-11) — damage substrate + 3 owner playtest fixes + PROTOCOL 16→17
- #a-council-fed-a-partial-fact-set-agrees-confidently-and-wrongly: S138 P1: my Council prompt stated that structuralSignature is SIZE-ONLY (true) but never mentioned that the differential rig actually compares hashWorldStateFull. Both Grok and Gemini then reasoned correctly from that incomplete set and CONVERGED on the same wrong conclusion — abandon per-primitive hp, the rig will be blind to non-lethal damage. Gemini even prescribed a remedy (add an XOR checksum of primitive hps to the worker signature) that was pure unnecessary work. The failure mode is the dangerous one: a partial fact set does not make a Council say "uncertain", it makes them AGREE, and agreement reads as confirmation. Two independent seats converging is only evidence if the premises were complete. Fix: when briefing external reviewers, state not just what a mechanism IS but which consumers actually call it — and treat unanimous agreement with my own framing as the weakest possible signal, not the strongest.
- #prove-the-guard-dont-read-it: S138 P1: the whole design rested on a docblock claim — that stateHashFull has a FIELD-LEVEL guard which fails tsc by name if you add a field to an already-hashed entity. That is a claim ABOUT a compile-time behaviour, exactly the class PRIME-AUDIT's runtime-verifiability question exists for. So step 1 of execution was to add hp?: number to Primitive, touch nothing else, and run tsc. It failed with ERROR_UNCOVERED_FIELD: "hp" at stateHashFull.ts:246. Two minutes, and it converted a cited assumption into a fact — and simultaneously refuted a reviewer who had asserted the opposite. When a docblock makes a falsifiable claim you are about to depend on, run it.
- #the-obvious-suspect-can-be-refuted-by-a-docblock: S138 P2: the owner reported "each primitive pushes another primitive away as anti-magnetism". The obvious suspect was the collision solver, and I could have spent the session tuning resolveCollisions. anchorStabilize.ts:9-11 refuted it outright, in prose someone had already written down: placed primitives are NOT free-integrated, verletStepAll steps only freeSparks, resolveCollisions is spark-only, a structure is held purely by solveBonds constraints. The real culprit was STRUCTURE_GROW_IMPULSE — a cosmetic "puff" from S13 that was in fact an unconditional outward physics impulse written into settled geometry on EVERY placement. Before chasing the plausible subsystem, grep for whether someone already documented that it CANNOT be the cause. The codebase often knows.
- #a-hardcoded-copy-of-a-formula-is-a-time-bomb-with-someone-elses-name-on-it: S138 P2: moving the keep ring 275→420 broke 4 e2e tests that had nothing to do with keeps. Cause: castle-panel.spec.ts:24 contained "CANVAS_WIDTH / 2 - (125 + 150)" — a hand-copy of SPAWNER_RADIUS + 150 — under a comment explaining the derivation. The comment made it look careful; it WAS the bug. e2e/helpers.ts had ALREADY learned this in S137 and installed the __SPARK__.keepCenter getter with a note that "the harness now cannot disagree with the game about where a castle is" — but only one file adopted it. When you change a constant, grep for every OTHER transcription of the same formula in the same commit; it is a search-and-adopt task, not an edit.
- #required-beats-optional-because-tsc-becomes-the-auditor: S138 P1: I made Primitive.hp REQUIRED rather than hp?: number, arguing that an optional field forces "hp ?? MAX" at every read and the forgotten one is the bug. It paid for itself within one tsc run: 57 errors, of which THREE were PRODUCTION sites (botSpawnerSeed.ts:92,159,180) that construct Primitive literals directly and would have minted shapes with hp: undefined — turning every later "prim.hp -= amount" into NaN, a silent un-killable primitive no test would have caught. The other 54 were test fixtures, i.e. cheap. Paying a mechanical fixture cost to convert a class of silent runtime bug into a compile error is almost always the right trade.
- #file_lacks-needles-match-my-own-explanatory-comments: S138 close: MCV hard-failed on two file_lacks assertions — lacks('SPAWNER_RADIUS + 150') and lacks('CANVAS_WIDTH / 2 - (125 + 150)') — because the very commits that REMOVED those expressions added comments QUOTING them to explain what changed and why. S137 recorded this exact trap and I walked into it again, because good commenting practice actively creates the collision: the better you document a removal, the more likely the needle survives. Then it happened a THIRD time in the same session — .gitleaksignore tripped its own generic-api-key rule by quoting a worked example of the pattern it suppresses. Durable rule: never assert file_lacks on a bare expression; assert the CODE form prose cannot plausibly reproduce ("const r = SPAWNER_RADIUS + 150", "const KEEP0"), and describe suppressed patterns in prose rather than writing them out.
- #a-test-can-pass-while-running-on-NaN: S138 P1: my real-physics acceptance test imported PHYSICS_DT from constants.ts, where it does not exist (it lives in physicsLoop.ts). vitest does not typecheck, so SUBSTEP_DT was NaN and the test PASSED — because solveBonds is purely positional and the fixture had no free sparks for verletStepAll to ruin. The assertions were valid but the test was not exercising what its name claimed. Only tsc caught it. Two habits: run tsc before believing a new test, and add a cheap guard (expect(Number.isFinite(SUBSTEP_DT))) so a degenerate input can never masquerade as a pass again.
- #invert-the-tests-that-pinned-the-behaviour-you-just-deleted: S138 P2: removing STRUCTURE_GROW_IMPULSE broke 5 tests in session13.test.ts that existed specifically to assert the outward puff, with careful comments deriving the expected 0.8px offsets. Deleting them would have erased the record that the behaviour was ever deliberate and left no guard against reintroduction. I inverted them instead — same fixtures, now asserting prevPos === pos — with a header explaining the owner reclassified it as a defect, plus one NEW test proving only the OUTWARD half went (the inward MERGE_IMPULSE still fires on a merge). A test that pinned a removed behaviour is not dead weight; inverted, it is the strongest guard against the removal being quietly undone.

## S137 (2026-08-10)
- #my-cost-estimate-was-an-order-of-magnitude-wrong: S137: I told the owner options A and B differed by "roughly an order of magnitude" in cost, and they said they leaned B anyway. Measuring instead of estimating showed B was ~2-3x, not 10x: castleBanks — added ONE session earlier — is already a serialized per-seat Map<PlayerId,Spark[]> whose entire production footprint is ~25 lines across 10 files, including a ONE-LINE hash coverage entry, and even the subtle trap (structural signature must count CONTENTS not seats) was already solved in a docblock. My estimate came from imagining the work rather than counting an existing instance of it. When a codebase already contains a near-identical feature, the cost of the next one is a COUNT, not a guess — and the owner s instinct beat my analysis.
- #the-owners-number-may-not-survive-the-tick-rate: S137: the owner specified a DoT as "5% hp per tick" and a "2% dot". At PHYSICS_HZ 60 that is death in 20 ticks (0.33s) and 0.83s — certainly not what was meant. The failure mode is that the number SOUNDS reasonable and only becomes absurd once multiplied by the engine rate. Fix adopted as a rule: author every DoT as a TOTAL ("X% of max HP over N seconds") on a damage cadence decoupled from the physics rate, and derive the per-application value. Also: percent-of-CURRENT hp can never kill (Zeno), and refresh-not-stack, or camping an aura is instant death.
- #prompting-cannot-fix-model-inconsistency: S137: the first veo attack loop had the goblin LET GO of his cleaver mid-swing. I wrote an explicit, emphatic "the cleaver stays GRIPPED, never thrown" instruction and regenerated. It improved most frames but STILL dropped the blade in one — and newly hallucinated a tail the source art does not have. The lesson is not "prompt harder": generative video does not hold prop/character consistency across a whole clip. The real fix was reframing the requirement — an atlas needs ~6-10 GOOD FRAMES, not a flawless 4s clip — so the answer is 2-3 takes plus frame-level curation. When a model fails a constraint twice, stop tuning the prompt and change what you need from the output.
- #a-claim-is-not-verified-until-a-machine-rechecks-it: S137 close: I wrote every verification[] entry as {file, assertion:"free text"} and it read as thorough - each one named a real file and described a real change. The MCV gate rejected all 12: verify-session-claims dispatches on a typed `type` field, so free prose is literally 'unknown assertion type: None' and every completed priority scored WEAK. Prose ABOUT verification is not verification. Then my first replacement was ALSO wrong - file_lacks 'toBe(13)' failed because that string survives in the comment explaining what the contract used to be, so the needle matched my own documentation rather than code. Two rules: author bindings in the typed vocabulary (file_contains/file_lacks/grep_count/json_field, absolute paths), and pick needles that cannot match prose - bind the symbol that actually disappeared, not a phrase about it.
- #instrument-before-you-infer: S137 P0b: the handoff, and my own static chain, both blamed the hunter bench for a swallowed click -- but the failure snapshot showed tick 261 against a ~400-tick catch, so the inference looked WRONG and I nearly went hunting for a second cause. Adding one diagnostic to the throw (all three isInputLocked clauses, printed individually) settled it in a single run: lockedByBench=true, benchedUntilTick=1925 at tick 330, hunters already empty. The estimate was stale, not the theory. When a failure message names a SYMPTOM ("panel did not open") rather than a CAUSE, the cheapest next move is almost never more reading -- it is making the failure say why. Cost: ~30 lines. Saved: an open-ended hunt.
- #a-count-is-a-contract-without-a-name: S137 P0a: fog.spec asserted aboveFogLayer.children.length === 13. When it became 14 the failure said "Expected 13 / Received 14" and nothing else, and attributing the extra child cost an entire prior session. Replacing the count with an ORDERED CONSTRUCTOR-NAME list (each line commented with its owning renderer) makes the diff point at the index that moved, and catches what a count structurally cannot -- one renderer leaking a second child while another adds none still sums to 14. Rule: if a test pins a NUMBER that stands for a LIST, pin the list. Also: I MEASURED the expected list (deliberate failing assertion printed the received array) rather than writing what I believed, and the measured Container positions then independently confirmed the static attribution.
- #dont-parse-typescript-with-a-regex: S137 A.0: a one-line regex count of PLAYER_COLORS returned 9 because it split on commas inside the block COMMENTS. I was one step from reporting "e2e/helpers.ts hardcodes /7 but there are 9 seats -- the keep geometry is broken", a confident and completely wrong finding. Reading the declaration directly showed 7. A measurement that is cheap to take is not automatically safe to trust; for anything that will become a CLAIM, read the source of truth rather than a derived count.
- #an-underpowered-window-reads-as-a-null-result: S137 P3: my first bank measurement ran a 22s window and reported 0.0% hauler WAITING at every cap -- a clean, confident table that said there is no bottleneck. It was wrong. The cap-5 bank did not saturate until 20.2s, so the run simply ended before the quantity I was measuring could exist. The tell was that time-to-full sat right at the window edge. At 60s the same experiment reports 88.7-94.1%. A measurement whose window is shorter than the phenomenon does not return no signal, it returns a CONFIDENT FALSE NEGATIVE, and it is indistinguishable from good news. Fix applied: the spec now splits pre/post-saturation and FAILS if any condition never saturates, so the error cannot recur silently.
- #a-passing-visual-test-that-produced-no-image: S137 P2: the rainbow-castle spec passed on its first run and wrote its screenshots via testInfo.attach. There were no images on disk -- attachments live in the HTML report bundle and are discarded under --reporter=list on a PASSING test. The entire point of the priority was that a human could look at the thing, and it had silently produced nothing to look at. Green does not mean the artifact exists. When a deliverable is a FILE, assert the file, or write it to an explicit path.

## S136 — 2026-08-10 (owner playtest batch: 4/6 shipped)

- **[P0] #false-repro-from-harness-flag #empirical-refutes-plausible-criticals**
  My first repro of the owner's "buttons not clickable" showed the click DEAD — and it was WRONG. The cause was ?debug=1, a DOM overlay covering the button column, which I had added myself by copying the harness convention. Two lessons: (a) reproduce in the configuration the USER actually ran, not the one the test suite uses; (b) when a bug report and a repro agree, that is when to look hardest for a shared confound, because agreement feels like confirmation. The tell was that the canvas received NO pointerdown at all while document did — a DOM-level fact that no amount of Pixi-side reasoning would have explained.

- **[P0] #look-at-the-render #assertions-cannot-see-layout**
  28/28 runtime assertions passed while the disabled button label visibly overflowed its own box. State assertions can prove a control is enabled, priced and clickable and still say nothing about whether the text fits. I only caught it by opening the screenshot. Any UI priority must include LOOKING at the render as a distinct step, plus a cheap numeric fit guard (label length x advance <= inner width) so the next one fails in vitest instead of on the owner's screen.

- **[P0] #zero-coverage-is-a-finding #policy-not-instance**
  Deleting an EXPORTED function (isOverFooterControl) and an entire render method (drawFooter) left the suite at exactly 2069/2069 green. That is not reassurance, it is a measurement: the controls had zero coverage, which is the structural reason the owner had to find this by playing. When a deletion breaks nothing, treat the untouched test count as the finding and write the missing test before the feature.

- **[P1] #runtime-finds-what-units-cannot #physics-invariants-invisible-to-unit-tests**
  Eighteen unit tests and an 8/8 mutation matrix all passed on a PULL that was fundamentally broken. I cleared escrow so the pulled shape would be "an ordinary free spark", and enforceSpawnerBounds promptly rim-snapped it off the porch back into the quarry — a 194px teleport, i.e. a worse version of the very fling I was fixing. No test in the suite runs the physics loop, so none of them COULD catch it. Rule: when a change puts an entity into the world at a new position, the acceptance test must run real physics for several frames and assert the entity is STILL THERE. Position is not established by the reducer that wrote it.

- **[P1] #comments-claiming-completeness-need-a-grep-first #policy-not-instance**
  I wrote a comment asserting that clearing the bank inside teardownGatherers meant no teardown path could forget it. That was false the moment it was written: five sites clear world.gatherers and three do it inline. A test dispatching RETURN_TO_TITLE caught it. Two lessons: any comment claiming "this covers every site" is a claim that needs a grep BEFORE it is written, and a teardown test must exercise the PUBLIC action — calling the helper directly would have passed and hidden all three gaps.

- **[P1] #delete-the-surface-not-the-symptom**
  The stacking and fling bugs had two different mechanisms (count-derived slot indices; EPSILON-guarded co-location turning into velocity via Verlet) and each could have been patched on its own. Taking the owner literally instead — store it INSIDE the castle — removed the shared precondition (the shape being a positioned physics entity) and both defects stopped existing rather than being handled. The change that deletes the surface is usually also the smaller diff.

- **[P4] #measure-then-set #dead-constant-is-a-finding**
  FREE_SPARK_SOFT_CAP had been 50 while the pool peaked at 4 — provably dead code for months. Rather than pick a new number by arithmetic I ran the game with the cap still at its dead value so the natural distribution was unmasked, then set the cap from the measured p95/peak. The measurements are recorded IN the constant’s docblock, so the next person to touch it inherits the evidence instead of re-deriving it. Rule: when re-tuning a constant that was never binding, measure it unbound FIRST — the arithmetic prediction (11.25) and the observed mean (8.92) differed by exactly the consumption the model omits.

- **[P4] #fixing-a-bottleneck-moves-it #report-the-new-constraint**
  The x6 faucet drove gatherer idling from chronic to 1.2% — and immediately made the 5-slot bank the binding constraint instead (247 of 338 samples WAITING with nobody spending). Both facts come from the same 90s run. Fixing a throughput bottleneck does not remove the bottleneck, it relocates it, so the acceptance measurement should report where it went. Saying only "idle fraction 1.2%, done" would have been true and misleading.

- **[P3] #verify-the-bug-exists-before-fixing-it #empirical-refutes-plausible-criticals**
  Owner item 6 was "the rainbow should change the castle colour too" — and reading the code showed it already did (the derangement remaps player.color; the keep tints from the live value). The real gap was that the castle did not join the CELEBRATION. Had I implemented the literal request I would have written a redundant recolour and reported a fix for a working feature. Rule: for any "X does not do Y" report, locate the code path that would do Y and confirm it is absent BEFORE writing a replacement — and when the mechanism already exists, say so explicitly in the commit so the finding is not lost.

- **[CLOSE] #green-by-luck-is-not-green #a-perf-change-exposes-stale-test-contracts**
  The gating e2e lane was green for two slots while encoding a rule the game had already deleted: the helpers grabbed sparks from the quarry, which V6-1.2 made un-pickable. It only passed because the old faucet left ~2 sparks and the one available was usually the escrowed (pickable) one. A 6x throughput change turned that luck off and five specs went red at once. Two lessons: (a) a suite can be green because the state space is too small to expose it, so a capacity/rate change is a legitimate way to DISCOVER stale contracts rather than a thing that broke them; (b) when a spec depends on a game rule, the helper should assert the SAME predicate the production code does — my fix was to mirror controls.pickSpark, and the deeper finding (pickable != draggable, because a hauled spark is re-pinned every tick) only appeared once the predicate was explicit.

- **[CLOSE] #do-not-encode-a-number-you-cannot-attribute**
  fog.spec asserts aboveFogLayer has exactly 13 children and it is stably 14, with the test comment inviting me to bump it. I did not: I could not identify WHICH renderer the 14th child is, and a contract whose number is updated without attribution stops being a contract — it becomes a rubber stamp that will absorb the next real leak silently. Left red with the diagnosis and the prime suspect named. Preferring an honest red to a green I cannot justify is the right trade when the test exists precisely to catch unnoticed additions.

# Reflexion Log — The Spark

Cross-session learnings, newest first. Each `## DATE — Session N` block holds that session's reflexion entries. Pruned to ≤50 entries; older entries live in the archived handoff docs.

## 2026-08-09 — Session 135: 3/3 priorities. P0 closed the hunter-lifetime serialization residual that gated the sim-worker flip (despawnAtTick/spawnedAtTick/prevPos now travel; 4 stale docblocks + the Blueprint + BACKLOG reconciled). P1 shipped the first player-VISIBLE feature since the v0.6 pivot — a placeholder keep, a 105-point buy button, the gatherer entity, and score becoming a spendable currency. The owner then PLAYED it and reported the decisive defect: the gatherers do not move. P2 (Rule 16 scope amendment, written before any code) made them work: walk to the quarry, take a shape into escrow, carry it home, BANK it beside the keep — and moved the cruiser grab from the spawn zone to the keep, so the loop is gatherer-fetches / player-builds. Plus a 50-point speed upgrade, start-at-1 + 100 opening points, a half-size spawn zone, and per-gatherer shape preference. A 5-lens adversarial audit of the P0/P1 diff produced 31 findings; the load-bearing ones (seat-6 keep collision, static-palette tint vs the rainbow derangement, nextGathererId hashed-but-unprojected, the full-width footer blackout, buttons ignoring input locks) were fixed in P2. tsc 0 · vitest 2020→2069 (137 files) · bundle 657.2/750 KiB · mutation matrices 15/15 accounted · verified in a real headless browser (4 shapes banked, zero console errors) · deploy verified live 4/4.

- #the-mutation-matrix-lied-a-third-time-but-a-second-detector-caught-it: S135 P0. My hunter mutation matrix reported a FALSE all-green on its first run — every mutation showed failCount 0 while staying GREEN, the exact S134 disease one session later. This time the harness had TWO independent detectors: a per-test x-scan and a summary regex. They DISAGREED (x-scan found TEST A/B/C failing while failCount read 0), and that disagreement is what exposed the real fault: ANSI colour codes broke the summary regex, not the mutations. Fix was to key RED off execSync non-zero exit. LESSON: a single parser of tool output is untrustworthy; build a second, differently-shaped detector and treat any disagreement between them as "my harness is lying", not "the code passed". The redundancy caught it, not my judgement.

- #the-adversarial-review-caught-a-false-red-trap-i-would-have-shipped: S135 P0. My draft test plan said "seed a hunter via makeHunter and assert hashWorldStateFull equality". The disk-grounded TEST agent found that restore reconstructs nextHunterId = maxId+1 while a raw-seeded source stays at 0, and nextHunterId IS hashed — so the equality would have been a FALSE-RED no correct fix could turn green, pressuring a later session to weaken the strongest assertion in the file. Seeding via applySpawnHunter (which advances the same cursor) fixes it. LESSON: before writing any host-vs-successor hash-equality assertion, enumerate EVERY hashed world scalar the rehydrate path RECONSTRUCTS (nextXId cursors, tick, guard flags) and make the source produce them the same way — otherwise the best guard becomes an un-passable trap.

- #my-own-test-was-wrong-not-the-code: S135 P2. The haul tests failed with "expected SEEKING to be HAULING" and my first instinct was a bug in the FSM. I traced it instead of patching: pickup lands at tick ~154 and delivery completes by ~310, so at my hard-coded 420-tick assertion the gatherer had already banked its shape and gone back to work. The CODE was right; the TEST encoded my arithmetic. Rewrote it to wait on the observable event (tickUntil) rather than a tick count — which is also immune to the speed, distance and radius tuning this very slot changed. LESSON: when a new test fails, first ask whether the assertion or the code is wrong, and prefer waiting on events over counting ticks in any test of a timed process.

- #a-surviving-mutation-is-not-automatically-a-weak-test: S135 P2. Mutation M5 (delete the owns-nothing guard in the speed upgrade) stayed GREEN. The reflex reading is "blind test, strengthen it". I checked instead and found [].every(...) is vacuously TRUE, so the level-cap guard below already returns early for a player owning nothing — the deleted line was REDUNDANT, not uncovered. Proved it by deleting BOTH guards, which went RED. Documented the redundancy in place so a later reader does not chase a phantom gap. LESSON: a mutation that survives means the line did not change behaviour — that is either weak coverage OR a redundant line, and the two demand opposite responses. Distinguish them before acting.

- #the-forcing-functions-did-the-work-i-would-have-forgotten: S135 P1/P2. Adding a new entity family touches ~17 sites. I did not remember them: tsc named them. FIELD_COVERAGE (keyed on keyof World), KNOWN_GAME_ACTION_TYPES_RECORD (Record over GameAction type), the HelloMsg protoVersion literal and the benchGate two-way set-equality test each failed BY NAME until registered. The two sites with NO forcing function were exactly the two the audit later caught: structuralSignature (a size-only line) and the positions buffer — and nextGathererId, which I marked hashed but never projected, slipping through because the test list of non-family scalars silently absorbed it. LESSON: when adding to a family, list the tsc-forced sites and the UNFORCED ones separately, and treat the unforced list as the real review surface.

- #ship-the-loop-not-the-substrate: S135. P1 shipped exactly the approved slice — a bought gatherer that stands at its keep — and the owner played it and said the decisive thing: they do not move. The slice was correct by the plan and useless by the game. The plan had even stated the consequence in writing ("a bought-but-idle gatherer is expected, not a defect"), which made it a KNOWN dead end rather than an accident. LESSON: when a slice deliberately stops short of a working loop, say so in the owner-facing summary in plain words BEFORE building it, and offer the smallest extension that closes the loop — a feature whose payoff is entirely in the next slot is a feature the owner cannot evaluate.

## 2026-08-07 — Session 134: 1/1 priority, but the code was the smaller half. P1 fixed creature lifetime serialization — `serializeCreature` coupled the `despawnAtTick` emit to `sourceSpawnerId !== null` and `trimMirrorCreature` stripped it anyway, so a rehydrated 0 deleted a promoted host's whole creature population AND mass-detonated its drones (hostTick Step 1.5 fires on `tick >= despawnAtTick - 1`, i.e. `>= -1`) — up to 36 irreversible bond severs per handoff. The Council MEASURED a third consumer nobody named: the worker-INIT path, live on today's build with no peer and no disconnect. `sourceSpawnerId` shipped in the same commit because the deletion was MASKING two defects it causes. CHECK returned SHIP-WITH-FIXES with 5 adopted items, four of them mine, including my own commit message overclaiming that the stale docblocks were fixed. THE LARGER OUTCOME: all six v0.6 economy decisions plus the whole of V6-1.1 were ruled by the owner, so Phase 1 is unblocked and specified after six sessions of waiting — and score became a spendable currency, the widest-reaching decision of the arc. tsc 0 · vitest 2020→2028 · bundle 645.3 KiB (−88 B) · wire +492 B measured · MCV exit 0 · 9 commits pushed · deploy verified live 4/4.

- #a-logged-fix-location-can-be-the-defect: The handoff AND three `save.ts` docblocks all said the fix belonged in `serializeCreature`'s emit condition. All four were wrong, and wrong in a self-concealing way: `netSnapshot` post-trims, so an emit-only change leaves the wire byte-identical AND leaves the suite fully green, because the existing characterization asserts through `netSnapshot`. A carried instruction is not just potentially incomplete — it can be the thing that hides the bug. Re-derive a logged fix LOCATION from source before trusting it, exactly as you would re-derive a logged claim.

- #the-bug-was-not-where-the-title-said: Everyone framed this as host-migration. The Council synthesis measured `main.ts` → `workerSim.restore()` + `isHost = true` and found a Voltkin's lifetime dying on the ORIGINAL host under `?worker=1` — no peer, no disconnect, live on today's build, and about to become universal when V6-1.1 flips the worker default on. The reframe came from MEASURING a shared serializer's consumers, not from reasoning about the reported symptom. Ask "who else calls this serializer" before scoping any serialization fix.

- #my-mutation-matrix-lied-twice-before-it-told-the-truth: First run: all three mutations reported ANCHOR MATCHED 0 TIMES because `save.ts` is pure CRLF and my anchors used bare LF — caught only because I had built an anchor-count guard that refuses to report an unapplied mutation. Second run: M2/M3 passed 8/8 against deliberately broken code because no fixture creature had a `targetCreatureId`, so every creature took `trimMirrorCreature`'s early return and the mutation sat in dead code. A mutation that PASSES is not evidence the guard works until you have proven the mutated line EXECUTED.

- #my-first-fix-for-a-vacuous-test-was-also-vacuous: CHECK found TEST D2 passed with and without the fix. I moved the P1-owned creature to the CHEWER — still wrong, because a chewer has no zap target, takes the early return, and is therefore immune to the very mutation the test exists to catch. Only the DRONE carries both a spawner and a zap target. Re-running the matrix caught it; review would not have. When fixing a vacuity bug, re-run the mutation that exposed it — do not reason your way to "that should do it".

- #fixing-one-bug-can-unmask-two: The creature deletion was MASKING the `sourceSpawnerId` strip: while creatures are being deleted you cannot observe that per-spawner caps are disabled or that a rehydrated chewer blocks its owner's Voltkin summon. Shipping the lifetime fix alone would have traded one loud bug for two silent, ungatable ones. When a fix makes previously-dead state reachable, enumerate what starts RUNNING, not just what starts working.

- #i-repeated-the-exact-failure-i-had-just-diagnosed: I wrote a commit message blaming stale docblocks for the bug surviving an extra session — while leaving seven more stale sites in the same file, including `serializeCreature`'s OWN header 44 lines above the emit I changed, and while claiming "all four rewritten". Diagnosing a failure mode does not inoculate you against it. When you name stale documentation as a root cause, grep the WHOLE file before claiming it is corrected.

- #three-assertions-from-memory-again-one-session-after-logging-it: S133 logged "#i-wrote-three-assertions-from-memory-and-all-three-were-wrong". S134 did it three more times — an MCV needle, then a `json_field` binding using `field`/`equals` when the schema is `key`/`op`/`value`, then an `op: 'contains'` that does not exist (`_OPS` is only `== != > >= < <=`). Reading the reflexion entry did not change the behaviour; the guard did. MCV caught all three because they were expressed as machine-checkable bindings rather than prose.

- #the-user-could-not-understand-my-output: Six turns of dense project shorthand and the owner said outright he had no idea what I was doing — then pointed out the game looks identical to two weeks ago, which was true and which I should have led with. The work was sound and the communication made it unusable. Plain English, lead with what changed for the player, and put decisions in a widget instead of prose. The single highest-value turn of the session was the one where I stopped using jargon.

- #the-owner-gave-a-better-answer-than-any-option-i-offered: Asked what happens when the bank is full, I offered stall / overflow / auto-cancel. He said gatherers walk home and WAIT at the castle holding the item, depositing the moment a slot frees. Better than all three — nothing is lost, and a visible cluster of loaded units is a far clearer "you are capped" signal than units stalling out of sight. Offer options to speed a decision, never to bound it, and read a custom answer as a design contribution rather than an off-menu selection.
