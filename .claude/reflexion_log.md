# Reflexion Log — The Spark

Cross-session learnings, newest first. Each `## DATE — Session N` block holds that session's reflexion entries. Pruned to ≤50 entries; older entries live in the archived handoff docs.

## 2026-06-13 — Session 87: USER-QUEUED mode batch — VS-BOTS (1-6 bots, NOOB/MID/HARD/IMBA, play via real dispatch) + Multiplayer rename + Quick Match (discovery election + all-ready gate) — Full tier Council 1-round, 4 commits, CI-GREEN E2E+Deploy, live-preview bot-play verified

- P1 #canary-tests-pay-rent #unit-conventions-bite: the S63 palette canary fired EXACTLY as designed when the 7th seat color landed — it pointed at the duplicated e2e literal that had to move in the same commit. Separately the bundle charter was nearly mis-enforced twice in one session because prior sessions recorded KiB while vite prints kB (548.3 KiB == 561.45 kB): ALWAYS name the unit when recording a gate number, and when a gate reads >2% off baseline, rebuild the BASELINE commit in a worktree before assuming regression.

- P2 #bots-are-remote-players #choke-point-dividends: implementing bots as ordinary seated players that may only dispatch CLIENT_INTENT-shaped actions meant every S84-S86 exploit fix (bench gate, poop arrival gates, reach validation, territory blocks) bound the bots ON DAY ONE with zero bot-specific enforcement code — the S86 dispatch-entry choke point paid its first structural dividend. The one crash class was the OPPOSITE direction: reducers that THROW on caller bugs (fsmPickup CarryViolation) are lethal to an autonomous actor, so the controller needs claim-outcome confirmation + self-heal, mirroring what controls.ts does for humans.

- P3 #reuse-the-human-path-helpers: the bot redundancy weaving is the human LMB-up path verbatim (componentOf + pickRedundantBondTargets + same constants) — when an AI actor needs a capability a human has, importing the human path pure helpers is both cheaper AND provably consistent (same geometry, same caps), vs re-deriving bot-flavored variants that drift.

- P4 #split-the-eager-from-the-lazy-at-the-import-boundary: a new feature whose UI is eager (the lobby quickmatch buttons) breached the bundle charter by ~4 KiB. The fix was NOT to trim the feature but to find pre-existing eager weight that should have been lazy all along — CodexOverlay was eager ONLY because the always-eager godlyOrchestration imported a tiny localStorage helper (unlockGodly) from the same module as the heavy Pixi class. Extracting the helper to codexStore.ts let the 12 kB overlay become a lazy chunk, recovering 3x the headroom needed. LESSON: when an eager addition breaches charter, audit the import graph for heavy modules pulled eager by a TINY shared symbol — splitting that symbol is cheaper + cleaner than shrinking the new feature. Also: the Trystero-importing discovery had to be split from the ready-gate helpers (quickmatch.ts vs quickmatchGate.ts) so hostHandlers could use the gate eagerly without dragging Trystero into index.

- P5 #a-version-bump-ripples-into-every-hardcoded-literal: PROTOCOL_VERSION 7->8 + a 7th PLAYER_COLOR broke 5 tests across unit (2 protocol pins, 1 palette canary, 1 netHardening hello-builder) AND e2e (2 protocol-mismatch version literals, 1 nplayer 6-color assertion) — none caught by tsc, all by running the suites. The palette canary FIRED EXACTLY as the S63 author intended (it exists to force the e2e literal to move in lockstep). LESSON: a wire-version or palette-cardinality change is a cross-cutting edit — grep ALL test files (unit AND e2e) for the old literal in the same commit, and expect the e2e lane (not just vitest) to surface the rest. The e2e protocol-mismatch tests specifically encode current/older/newer RELATIVE to PROTOCOL_VERSION, so a bump always moves the newer-peer literal to PROTOCOL_VERSION+1.

- P5 #verify-attribution-in-vivo-not-just-in-vitro #harness-pointer-artifacts: the live preview briefly showed the human seat (0) accumulating prims it never built — alarming (the S84 non-builder-win class). Root cause was NOT a bug: synthesized button-clicks + a cursor parked in the play area + the preview harness generating pointer events drove real human builds. A clean-room reload (start match, then ONLY read-only evals, cursor left off-board) proved byOwner={1:1} with the human at 0 — bots attribute to their own seats, the human builds only on real input. LESSON: when a live-preview anomaly looks like a known bug class, do a clean-room re-test that eliminates harness-generated input BEFORE concluding it is real; manual preview pokes (injected entities, synthesized clicks, parked cursor) contaminate attribution.

## 2026-06-12 — Session 86: playtest round-6 REGRESSION batch (fog black + LOCK, hazard-ring origin lines, central bench gate + claim-gated drag, spark-IS-the-pointer) + BACKLOG→ROADMAP rewrite on user mandate — Standard tier Council 1-round, 8 commits, CI-GREEN + deployed, CHECK Triumvirate SHIP (Grok 4/4 refuted)

- P1 #user-tuning-outranks-design-notes #lock-with-a-test-not-a-comment: S85 restored the 'designed' dim fog tier over the explicit S63 user tuning because the old design note read as a carry-forward while the user decision read as a mere knob — and the round-6 playtest caught it as a regression ('the stupid blue fog is back'). A documented decision provably failed to protect itself within 22 sessions; the fix layers LOCKED_DECISIONS §14 (rule: old design notes NEVER outrank newer user tuning; restores need a fresh same-session user ask) with constants.lock.test.ts, a CI tripwire whose failure message says 'you are overriding a USER decision'. LESSON: user-tuned values get a test-enforced lock at the moment of tuning, not a comment.

- P2 #unit-test-the-drawing-not-just-the-math: the S85 hazard ring shipped with green unit tests (pure segment/alpha helpers) while the actual Graphics path drew a line from the WORLD ORIGIN to every ringed hazard — canvas-path semantics connect the pen to arc() start, and a freshly-cleared Graphics pen sits at (0,0). The user's screenshots were the detection. LESSON: when a feature's output surface is a Graphics path, pure-math helper tests are necessary but NOT sufficient — verify at the path/screenshot level (moveTo per dash; live screenshot now in the loop).

- P3 #enforce-at-the-choke-point #the-gesture-is-a-verb-too: two consecutive sessions gated VERBS (S84 claim, S85 build) while the exploit lived in (a) the continuous DRAG gesture — a gate-rejected claim left the spark Free and the ungated AttractDrag lerp hauled it at full cursor speed forever — and (b) the bench dimension, which had NO reducer enforcement at all (input-layer onDown only; the catch force-drop even handed the eaten player their spark back into the live drag). Fixes: bench policy enforced ONCE at dispatch() entry via an explicit per-intent allow/deny map test-locked against CLIENT_INTENT_TYPES (a new verb cannot ship unclassified), and gesture ENTRY keyed on the CLAIM OUTCOME (dispatch is synchronous locally on host AND joiner via PREDICTABLE_ACTIONS — no local radius mirror to desync). LESSON: enumerate ALL consumers of a resource INCLUDING continuous gestures, and enforce actor-state locks at the dispatch choke point, not per-verb.

- P4 #the-framework-owns-the-canvas-style #pipe-masking-recursed: Pixi v8's EventSystem re-applies cursorStyles[mode] on every pointer interaction, so the naive canvas.style.cursor='none' was silently clobbered by a button-hover 'pointer' — the durable switch is Pixi's OWN cursorStyles.default, and ONLY the live preview pass could reveal it. Separately the S85 pipe lesson RECURSED: 'tsc | head' returned the pipe's exit code, the P3 commit claimed 'tsc clean' for a tree whose NEW test file had 3 type errors (esbuild-vitest is type-blind), and CI's deploy build went RED until the P4 commit fixed them. LESSON: gates run unpiped with explicit exit capture — violated twice now, so check_method MUST quote the captured exit code, not assert cleanliness.

- P5 #ground-roadmaps-in-code-audits #label-idea-origins: the user's frustration ('the whole point is being a geometric builder game and we have least focused on that') converted into an unarguable roadmap with three greps: 24 of 36 combos are literal 'Functional placeholder' entries (incl. the user-named Dot→Square and Line→Circle), isMagical has ZERO production consumers (magic bonds score the same as placeholders), areaMultiplier is dead data. A roadmap argued from measured gaps beats one argued from vibes; labeling every item USER-ASK vs CLAUDE/COUNCIL-SUGGESTED with a PARKED-until-sign-off rule structurally prevents the backlog from refilling with self-generated infra ahead of the user's game.

## 2026-06-11 — Session 85: playtest round-6 fixes (silent yell asset, flyover 10x, pooped BUILD-verb gate) + BACKLOG batch (host-migration design, EYES carries, lobby D1 + e2e geometry getters) — Micro tier user-pre-approved autonomous run, 6 commits, all CI-GREEN + deployed, $0 generative

- P1 #verify-the-asset-not-just-the-pipeline #uncommitted-generation-chains-are-unauditable: S84 shipped a SILENT rainbow yell (entire 2.67s under -45dB) because the ffmpeg mastering chain ran as ad-hoc shell commands — CHECK verified duration+size+wiring but nobody MEASURED the content, and with the chain uncommitted the failure could not even be diagnosed from git. The diagnosis discipline that worked: instrument the runtime FIRST (prototype-patch AudioBufferSourceNode.start + network watch proved the pipeline healthy in 3 probes), THEN measure the asset (volumedetect mean -52.8 vs the -13.2 audible reference = the smoking gun in one command). Fix encoded the lesson: scripts/make-rainbow-yell.py carries a BUILT-IN audibility gate (fail-loud floor on mean/peak, output deleted on miss) so a silent render can never ship again. LESSON: for generated assets, commit the generation pipeline WITH a content-level acceptance gate — duration/size/format checks are shape, not content.

- P3 #the-same-bug-class-recurs-on-the-adjacent-verb: S84 P1 fixed pooped-grab on applyPickupSpark and its OWN reflexion said the bug lives where the check is MISSING — yet the fix only covered the LMB-down claim verb while the atomic LMB-up PLACE_FROM_FREE (the verb every real build runs through) stayed ungated, so the debuff provably never bit the build loop. One file-read settled it (placeFromFree.ts has a numbered validation ladder; pooped appeared nowhere). LESSON: after gating a verb, enumerate the SIBLING verbs that consume the same resource (claim/build/drop for sparks) and assert the gate on each or document why not — a validation-ladder reducer makes the audit a 30-second grep.

- P4a #separate-authority-from-identity-in-handover-designs: host-migration looked intractable for 3 sessions because authority handover (who simulates, from what state) and identity handover (why survivors should TRUST the successor under a room code that commits to the dead host pubkey) were conflated. Probing save.ts:564 dissolved half the problem empirically — netSnapshot is the FULL WorldSnapshot minus exactly 5 reconstructible fields, so every client already holds 95 percent of takeover state and cold-standby adoption needs no new wire traffic. The identity half then has exactly one S82-trust-model-preserving shape: a succession warrant signed by the original host key at Begin + epoch counter for zombie demotion. LESSON: in P2P trust designs, write down WHAT the existing cryptographic commitment binds (room code = host pubkey) before designing — the constraint dictates the solution space.

- P4b/c #live-getters-kill-coordinate-drift #cosmetic-anim-needs-a-silent-baseline #never-pipe-a-gate: the e2e suite hardcoded title/lobby click coords with comments duplicating renderer math — the exact class behind the S50 P5 wrong-button regression — replaced with geometry getters that read the LIVE Pixi containers (drift impossible by construction; DEV-gated like __SPARK__). D1 seat animations needed a first-update SILENT baseline so entering a populated room fires no pop-in storm — transition animations key on OBSERVED state CHANGES, never initial projection. PROCESS: a gate run piped through tail reported exit 0 over 8 real e2e failures (pipeline exit = tail's), and the import-patcher's 'symbol present?' check was defeated by its own body-replacement having inserted the symbol — only the suspicious 26-vs-34 pass-count exposed it. LESSON: capture gate exit codes explicitly (never pipe), and verify imports in the IMPORT region, not the whole file. Bundle charter: re-measure the BUILD at every render-code commit boundary — the EYES commit silently crossed 550KiB and was caught one commit late; remediated by code-splitting the ?debug=1-only debugOverlay (546.5KiB, more headroom than pre-session).

## 2026-06-10/11 — Session 84: pooped pickup gate + rainbow flyover celebration + scoring overhaul + match length (user-queued batch + Rule 16 amendment, Standard tier, Council R1 + amendment R1, CHECK Triumvirate x3 remediation rounds, all CI-GREEN)

- P1 #recon-agents-map-the-wrong-verb #users-speak-in-avatars-not-actions: the recon agent read 'grab the primitives' as PLACE (the action that consumes a carried spark) because placement is where the existing validation blocks live — but the user's sentence structure ('until spark doesnt get to them he shouldnt be able to grab THEM') named the TARGET of the grab, and free sparks are what you grab. Reading applyPickupSpark directly settled it in one file: the only reach check was remote-only and cursor-keyed, so the debuff provably never bit for collecting. LESSON: when a user phrase maps to two candidate actions, diff the CANDIDATE VALIDATION PATHS — the bug lives where the check is MISSING, not where checks already exist. Second: the gate had to key on synced-field purity (poopedUntilTick/avatarPos/spark.pos) so optimistic and authoritative dispatch agree by construction — same construction S82 used for the cursor-target write.

- P2 #probe-the-channel-before-riding-it #a-wrong-reviewer-claim-can-point-at-a-real-gap: Grok's HIGH finding ('the latch violates the effectsRenderer contract') was factually backwards — the drain-into-internal-state latch IS the established idiom — but auditing it empirically exposed the REAL adjacent flaw neither reviewer articulated: buildNetSnapshot samples world.effects live at the 10Hz emit while effectsRenderer wipes the array at 60Hz, so a rare one-shot effect reaches the client ~1/6 of the time (frequent effects mask this statistically; a once-per-match celebration would have been silently absent for the joiner most games). The synced-field design (additive-optional, overwrite=restart, freshness-windowed audio) was strictly better than ALL R1 positions. LESSON: when a reviewer's mechanism claim dies under triage, autopsy the NEIGHBORHOOD the claim pointed at before discarding it — S83's lesson was 'a FAIL verdict is a list of claims'; S84's refinement is 'a refuted claim still marks a map location worth probing'. Also: Imagen 4 nailed a comedic character brief in ONE $0.04 roll when the prompt specified the COMEDY MECHANICS (mismatched googly eyes, exactly one crooked buck tooth, stubby arms thrown up) rather than 'make it funny'.

- P3 #co-tune-pacing-with-formula-changes #knob-version-history-is-the-tuning-memory: '+20% longer' is NOT 'raise WIN 20%' when the same batch changes income — duration = WIN/income exactly, so the lever math must compose BOTH changes (210/150 budget / ~1.15 typical bond income ≈ +22%); Grok's 'your +25% is pulled from air' challenge forced the estimate down to a defensible per-archetype range (trees gain bond income, blobs don't — so blob builders now get LONGER games AND an incentive to connect, both intended). The constants.ts version-history comment culture (S76 0.15 -> S78 0.05 -> S79 150 -> S84 210/70) is what made the co-tuning derivable at all — every pacing knob carries its own tuning memory.

- P4 #the-display-layer-is-part-of-the-scoring-system #honest-unreproduced-is-better-than-fake-fixed: the 'non-builder won' field report survived 6 in-vitro refutation probes — core attribution, win-pick, wire mirror, reconnect guards all CORRECT — and the REAL verified defects were perceptual: ONE anonymous leader bar shown identically to all peers, color-NAME row labels that lie after every rainbow shuffle, and S76 functional-bond neutrality flattening legitimate skill differences. LESSON 1: when players misread who is winning, the scoring SYSTEM includes the HUD — fix legibility WITH the math (seat-stable P{n} everywhere + leader marker + WIN-time per-seat forensics = the instrumentation Grok demanded). LESSON 2: re-weighting a deliberately-zeroed term needs a degenerate-strategy cap designed BEFORE shipping (0.25/bond capped 1.5x prims — Grok's 'you just invert the exploit' was right about the uncapped version). LESSON 3 (CHECK r3): when an e2e fails on TIMEOUT twice with growing budgets, stop stretching and measure WHAT is slow — the flyover's 4th full-canvas fill made CI software-GL seconds-per-frame; killing one rect fixed CI AND real low-end GPUs (sentinel poll strings made slow-sim vs stuck-state distinguishable from logs alone).

## 2026-06-10 — Session 83: Voltkin true-alpha + real animation (checkerboard root-cause kill, Veo clip set → atlas, intro plate-matte, CHECK Triumvirate) — Full tier, Council R1+R2 CONVERGED, user-approved batch

- P1 #measure-the-defect-before-modeling-it #verify-expectations-can-be-wrong: the 'white squares' bug was not a keying bug but a DATA bug — the generator drew a literal picture of a transparency checkerboard (0% real alpha even in RGBA files; the integration notes' 'PNG/RGBA' claim was true of the MODE and false of the CONTENT). The matte needed no AI: measured checker tones (240/254, chroma≤5) vs lightest body yellow (chroma ~148) gave a clean achromatic+bright+border-connected separation, and per-pixel unmix against the NEAREST bg pixel beat any global tone. Second lesson: when a verify probe fails, probe the FAILURE before loosening — charge/zap 'border ring opaque' px turned out chroma 39-191 = legitimate burst rays reaching the edge; the check's expectation was wrong, not the matte. Fix the assertion to forbid checker-tone survivors specifically, never blanket-relax.

- P2 #probe-the-hard-case-not-the-easy-case #seed-prep-matters: Grok's challenge ('an idle clip proves nothing') was right about WHICH probe to run, wrong about whether to probe — the synthesized adversarial walk-cycle probe (motion + loop closure + in-place constraint, the three things most likely to fail) converted the whole P3 bet into ground truth for $0.50, and the probe clip itself became the production walk asset. Two craft details that made it work: flatten the transparent seed onto the TARGET background color before image-to-video (alpha flattens to black otherwise, merging with the outlines), and prompt with animation-PRINCIPLE vocabulary (weight shift, squash/stretch, follow-through, anticipation) + the locked character spec verbatim — the model honors craft language better than 'make it look good'.

- P3 #background-color-is-a-matte-decision #live-probe-found-the-fsm-guarding-itself: choosing each Veo clip's background from the matte's FAILURE MODES (magenta for clips with achromatic-white decorations, white otherwise) plus a despill pass justified by the LOCKED palette ('no native pink' makes min(R,B)>G→lift-G provably safe) beat any single-background pipeline. Mid-priority A.0 discovery: serialized prevPos rehydrates equal to pos on the client mirror, so the S36 facing flip NEVER worked in 1v1 — render-side velocity estimation (own lastSeenPos + hold window) fixed walk/idle selection AND the legacy bug with zero wire change. And when the live ATTACKING probe came back 'wrong' (walk cells), the probe was actually catching the FSM bouncing a hand-injected ATTACKING state with no target bond — give the creature a REAL bond and the locked mechanics sever it under the new render: the best proof the swap is render-only.

- P4 #median-plate-plus-morphology #verify-metrics-need-the-same-rigor-as-code: the temporal-median plate captured MORE than the checkerboard — static lightning-arc cores persisted across >half the frames and landed in the plate; keying them would have hollowed the arcs. A 13px morphological opening separated the two BY SHAPE (45px checker blocks vs ≤10px streaks) — when tone can't discriminate, structure can. Second: the first --verify metric false-failed at 6.82% by counting the white explosion flash legitimately kept over the ring; the corrected metric (near-plate AND checker-tone) read 0.06%. A verification metric is code too — when it fails, autopsy the FAILING PIXELS before trusting either the metric or the pipeline.

- P5 #check-triage-earns-its-keep #reviewers-without-the-diff-hallucinate: GROK-ANALYST CHECK returned FAIL with a CRITICAL 'readiness latch race' citing files and fields that don't exist (VoltkinRenderer.ts, manifest.cells[].rect) plus a claimed repro he cannot have run — reviewers fed a SUMMARY instead of the diff attack the summary's prose, not the code. The inline Opus triage (S154 protocol) rejected 4/5 findings WITH EVIDENCE (assignment-order proof, despill arithmetic, constant imports, unchanged legacy path) and still extracted real value: the 5th finding (estimator state across snapshot restore) was a genuine edge worth 12 lines of hardening, and Gemini's single observation (unison loops) shipped as a 1-line aliveness win. LESSON: an external FAIL verdict is a list of CLAIMS to verify, not a stop sign — but never skip the triage, because rejecting 80% still leaves the 20% that improves the ship.

## 2026-06-10 — Session 82: user-queued full batch (cruiser-poopy-slow, spawner-save wiring, fog fuzzy-edge + CVD, netcode crypto-identity/reconnect/allowlist, lobby true-delta) — Full tier, Council R1+R2 CONVERGED, autonomous run

- P1 #target-chase-beats-message-cap #council-redesign: the per-MESSAGE displacement cap drafted for the slowed cruiser had two holes both externals found independently (spam bypass + pause-then-teleport via unclamped tick-delta). The fix that dissolved BOTH was a model change, not a parameter: store the cursor as a TARGET and chase it per-tick in the sim (hunter precedent) — spam now only moves the target, no dispatch-gate hack needed, and exact-snap-on-convergence (≤1 step ⇒ snap+clear) terminates without float-equality. LESSON: when a rate limit needs defending, move the authority from the message to the tick. Also: preview-tab rAF is throttled when occluded — pump app.ticker.update() manually to drive the REAL loop deterministically from eval.

- P2 #param-injection-beats-field-addition: the Spawner is a main.ts-owned class, unreachable from snapshot(world) — instead of putting spawner state ON World (which would ride every 10Hz NetSnapshot unless remembered in the omission list), inject it as an optional snapshot() PARAMETER. Wire-safety then holds BY CONSTRUCTION (netSnapshot passes no opts) instead of by discipline, with Omit+destructure+test as backstops. LESSON: for host-only persistence data, prefer call-site injection over world-state expansion — the narrowest data path is the safest serializer.

- P3 #inward-only-fuzz #verify-by-pixel-sampling: the fuzzy fog edge shipped INSIDE the existing brush footprint (wobble pulls the edge inward only, plateau untouched) so every fog.spec exact-RGB probe stayed valid BY GEOMETRY, not by re-tuning tests. Verified end-to-end by extracting the live maskRT and ring-sampling alpha at 24-48 angles (plateau 0, far 255, mid-band spread 75/255) — when a WebGL screenshot times out in an occluded preview window, extract.pixels IS the screenshot.

- P4 #room-code-as-key-commitment #empirical-probe-before-dependent-code: the TOFU race died not by adding a handshake but by making the EXISTING shared secret (the room code) a pubkey fingerprint — zero UX change, zero protocol bump, zero deps (WebCrypto). Buffering+replaying the once-only Begin signal during async verify preserved the no-drop property the sync latch had. The Trystero same-selfId rejoin assumption (R3) was PROBED with a real-WebRTC e2e BEFORE the UI depended on it — it passed in 10.3s, converting a design bet into ground truth. Also: e2e spec bugs (missing canvasToCss, wrong waitForWorld arg order) masquerade as product failures — read the helper signatures before blaming the feature.

- P5 #archaeology-before-execution: the CARRIED archive banner claimed the S69 P2 lobby refactor was 'never executed' — direct code reads + handoff archaeology proved it FULLY SHIPPED across four sessions. Executing the queued plan as-written would have redone shipped work and regressed the newer roster-based path the draft predates. LESSON: a plan file's own status header is a CLAIM, not ground truth — A.0-probe the code before re-running any carried plan, and when the banner is wrong, fix the banner as part of the close.

## 2026-06-10 — Session 81: round-3 playtest fixes (owner-only wipe, 3s hot-potato, random poop cadence, smooth remote cruisers, HUD above fog, hunter +20%, slower poop fall)

- P1 #ownership-predicate-extraction: owner-only splat wipe shipped as a PURE predicate (canAvatarCleanSplat) absorbing the S80 bench gate + new ownership gate + radius — main.ts sweep shrank to one call and the whole eligibility contract became unit-testable (the S80 bench fix had lived untested inside main.ts). Ownership via ownerColor (not placedBy) so the rainbow shuffle (lockstep remap) and a future Steal both keep working — parity test locks it.

- P2 #per-grab-timer #seam-audit-before-constants: 3s in-hand cook-off via carriedAtTick stamped on pickup / cleared on place+drop — per-grab semantics gives pass-it-on gameplay for free (re-grab restarts). Pre-checked every potato e2e carry window BEFORE landing the constant (S78 lesson): all carry legs sub-3s sim; the S75 bench spec (fuse 180 = hold 180) has an identical observable either way. Additive-optional serialization emitted only while CARRIED keeps FREE/ARMED snapshot bytes unchanged.

- P3 #stateless-randomness #hash-not-rng: random poop intervals WITHOUT touching any RNG stream — pure mix32(seagullId, lastPoopTick) picks each gap; both inputs already rode save/load, so replay/reload determinism came for free with ZERO new state. LESSON: when a deterministic system needs 'randomness', hash state you already serialize before reaching for a new seeded stream (no serialization work, no byte-drift risk to the 5 locked spawner streams). Sized the e2e smoke wait to the new worst-case gap (48 ticks at degraded CI fps → 1.5s→2.5s) in the same commit.

- P4 #render-side-smoothing #pure-helper-export: 'pixelated' enemy cruisers root-caused to raw 10Hz avatarPos rendering (vs the local cursor's per-frame glide) — fixed render-only with an exponential chase (k=1−e^(−dt/τ), τ=60ms, 300px snap guard, materialize-at-target) so sim/wire/determinism untouched. The S10 test-via-pure-helper-export pattern again: smoothTowards extracted, 6 tests incl. frame-rate independence (2×8ms ≡ 1×16ms).

- P5 #construction-order-is-z-order #verify-in-live-browser: the fogged HUD was pure Pixi child-add order — the row was staged before FogRenderer existed. Fix = move addChild, keep creation (handlers bind early). Verified the REAL stage indices in a live preview (fog=7 < aboveFogLayer=9 < legend/beta/♪/⚙=10-13) instead of trusting the diff — for z-order bugs the browser IS the test.

- P6 #ratio-preserving-tune: hunter +20% = both MAX_SPEED and ACCEL ×1.2 (0.0857 ratio + terminal-under-cap headroom preserved → juke character unchanged, only absolute speed). Confirmed the e2e catch wait is an UPPER bound (stationary target) so faster-only-catches-sooner — speed tunes are e2e-safe in that direction only.

- P7 #scope-honesty-over-silent-feature: poop fall −25% shipped as asked; the user's real question ('does poop slow MY spark?') answered truthfully in the report — the debuff targets FREE sparks, the cruiser is cursor-bound (teleport-to-pointer) so slowing it is a movement-model feature needing its own design + netcode thought. Rule 16: flag the gap, don't silently expand scope into the input model.

## 2026-06-10 — Session 79/80: round-2 playtest fixes (WIN 150 + seagull visible + pooped-building visuals) + FULL audit-backlog clearance (2 HIGH, 4 MEDIUM, LOW sweep) + in-context sharpening pass

- P1 #balance-lever #seam-protected-tuning: WIN 50->150 + TIER 15->50 (audit-rec lever after the S78 income cut still left games short). Pre-verified EVERY e2e injects __TEST_WIN_SCORE__/score seams before touching the constant — the S78 hunter lesson applied PROACTIVELY this time. Zero test fallout (1149 green).

- P2 #root-cause-the-feel-report #reuse-shipped-wire-state: user never saw the seagull -> root cause was ARITHMETIC not rendering (15-24 sparks at the LOCKED 0.15/s = 100-160s first gull vs ~2min S78 games); cadence 7/12 puts it in the opening minute. Pooped-building visual: A.0 discovered fouledPrimitives ALREADY rides NetSnapshot (worldTypes doc claimed host-only — stale), so the whole foul tint (prims+bonds) shipped with ZERO wire change/protocol bump. LESSON: probe the serializer before designing wire plumbing — the field you need may already ship.

- P3 #derive-dont-patch #adjacent-bug-found-in-A0: instead of the audit-prescribed per-site fouledPrimitives.delete patches, ONE reconcile primitive (foul set := union of live splat-anchor components) at the two destroy choke points fixed the leak AND the un-cleanable income-0 sever case. A.0 line-by-line read also surfaced an UNREPORTED bug: orphan CLEAN_POOP no-oped forever (empty BFS deleted nothing incl. the poop -> infinite per-tick dispatch + immortal splat). LESSON: re-derive state from first principles at mutation boundaries beats patching every delete site; read the consumer loop (main.ts sweep) not just the reducer.

- P4 #tofu-auth #one-primitive-two-fixes #quarantine-lane-blindspot: hostPeerId TOFU latch (roster seat-0-consistency anchor + NETSNAPSHOT fallback, fail-closed) closed backlog #2 client half; the SAME latch closed the S62 3+player host-loss limbo (hostLost overlay). Running the nplayer e2e to validate the gate exposed that the quarantined 4p-FFA test had been FAILING since the S78 income cut (natural-accrual WIN wait ~20s vs 20s timeout) — Rule 22 only watched the GATING lane. Fixed via score-injection (S78 hunter idiom). LESSON: after a balance-constant change, sweep the QUARANTINED lane too — a non-gating red is still a broken test.

- P5 #serializable-rng #non-vacuous-roundtrip: mulberry32 -> StatefulRng (state = one uint32; sequence byte-identical, save.replay guards prove no perturbation). Spawner.getState/restoreState round-trips all 5 streams; the test seeds the donor spawner DIFFERENTLY so restore provably overrides seeds, and asserts every hazard actually fired in-window (non-vacuous). CREATURE_CHARGE joined the audio-only drain skip (S37 omission inflating activeCount).

- P6 #doc-rot-sweep: 70-session-stale gameState header (primitive-count win), pre-S76 addScore docstring, as-7 cast made version-tracking via typeof, softReset hazard-teardown parity. Doc rot accumulates where the code outlives its header — sweep headers when the mechanic they describe is replaced.

- P7 #vacuous-empty-result #in-context-beats-fleet-when-broke #audit-your-own-diff: the external 5-lens audit workflow burned 443K subagent tokens into the org spend limit — 5/5 reviewers ERRORED and the workflow returned {confirmed:[]}, which READS like a clean bill but is VACUOUS (zero reviewers ran). Re-ran the audit in-context by re-reading my own S79 diff adversarially: found 1 real semantic inconsistency I had introduced (destroy-only reconcile = foul set timing-dependent on placement; fixed at the two placement dispatch sites), 1 pre-existing gameplay bug (benched players silently wiping splats), 1 hot-path optimization (per-tick per-poop sort -> zero-alloc lowest-id, differential-tested), 1 idiom-consistency alloc fix. LESSONS: (a) an empty result from a FAILED pipeline is not a green result — check the failure channel before trusting the payload; (b) when fixing X-on-destroy, ask whether X-on-CREATE has the mirror problem (reconcile belonged at ALL topology mutations, not just deletes); (c) optimization claims of equivalence need DIFFERENTIAL tests that scramble the incidental order (Map insertion) the old code happened to neutralize.

## 2026-06-09 — Session 78: playtest bug-fix batch — game length (income 0.15→0.05) + random explosions (FREE-potato harmless dissipate) + seagull −30%

- P1 #playtest-fixes #root-cause-before-fix #minimal-risk-lever #user-time-pressure: SHIPPED baa33f2. Three user-reported playtest bugs from the un-playtested S76/S77 batch. DEFINING MOVES: (1) ROOT-CAUSED each before touching code -- 'random explosions' = the FREE-potato auto-detonate (the Fork-E from-spawn fuse fires for ANY potato in the main.ts poll, so a never-picked-up potato blows up in the spawn-zone centre ~every cycle, deleting central structures); 'game over in 2min/ticks too fast' = the S76 complexity-income model accrues DURING the build-up so WIN=50 is crossed mid-ramp (and I VERIFIED tickScoring runs once-per-tick not per-substep + complexity has no double-count -> it's balance, not a bug). (2) chose the MINIMAL-RISK lever for each: rate-only cut 0.15->0.05 keeping WIN=50 (zero HUD/test/trigger ripple) over the audit's bigger win-score+tier rework (deferred as the next lever if 0.05 still feels short); a FREE potato DISSIPATES harmlessly (mirror applyDissipateBomb) rather than re-architecting the fuse -> kills the random blast while keeping hot-potato (CARRIED/ARMED still detonate); seagull one-constant -30%. (3) caught that GameAction's KNOWN_GAME_ACTION_TYPES_RECORD is an EXHAUSTIVE Record<GameAction['type']> so adding the union member REQUIRED the DISSIPATE_POTATO key or tsc fails -- verified every wiring site before building (tsc 0 / vitest 1149 / build 543.36KB first try). PROCESS LESSON: the 3-agent parallel deep-read audit was thorough (found the fouledPrimitives-leak + client sender-auth HIGHs) but cost ~48min/3x100K+ tokens and the user pushed back hard on the time -- for a 'find + fix these bugs' ask under time pressure, lead with direct root-cause reads of the NAMED symptoms (I had both headline bugs nailed from my own reads BEFORE the agents returned) and TIME-BOX or skip the broad fan-out unless the user asks for exhaustiveness; the user's 'run all fixes' is an explicit unlock -> execute, don't over-ceremony. Carried the audit's other findings (2 HIGH, 4 MEDIUM) to next session per the user's 'continue next session'. Gates: tsc0 / vitest1149 / build543.36KB; CI in-flight at close (e2e-low-risk; confirm next boot).

## 2026-06-09 — Session 77: rainbow 2x more common (P1) + global-reach fog-exempt (P2) + NEW seagull hazard (P3, FULL, PROTOCOL 6->7)

- P1 #rainbow-rarity #micro #playtest-gated-executed-autonomously: shipped dd05c85. RAINBOW_SPAWN_MIN/MAX_SPARKS 15/28->8/14 (S76 had just moved 35/60->15/28). DEFINING MOVE: the handoff gated this on a live playtest ('if still ~1/game, halve to 8/14'), but under an autonomous mandate I CANNOT playtest a P2P multiplayer game -- so I executed the user's PRE-SANCTIONED fallback value (8/14) rather than stalling the priority, reasoning from the spawn math (0.15 spark/s => 8-14 sparks = ~53-93s to first rainbow = ~2x the S76 cadence = the literal '2x more common' ask). Logged the no-playtest decision explicitly. LESSON: when a user-listed priority is gated on an action only THEY can take (playtest) but they've also handed you autonomy + a pre-approved fallback, take the fallback and flag it -- do not leave the priority unfinished waiting on an impossible-for-the-agent step.

- P2 #fog-exempt #render-zorder #standard #council-prime-audit #audit-corrected-the-ask: shipped b986831. Made global-reach hazards (potato/rainbow/hunter/Voltkin) render THROUGH the fog to all players. DEFINING MOVES: (1) the reach AUDIT corrected the user's literal candidate list -- the pickup-bomb severs only the PICKER's own bonds (bombLifecycle.ts:104) so by the user's OWN rule ('visible-to-all iff can-affect-all') it is EXCLUDED, not included; logged as 1-line-reversible. (2) Discovered the fog is a single full-screen OVERLAY that hides by PAINTING OVER (not culling) + the netSnapshot ALREADY replicates every entity to every peer => the whole feature is a CLIENT-SIDE Z-ORDER change, NO protocol bump -- this de-risked it from Full to Standard. (3) Reused the EXISTING memoryLayer precedent (a plain Container addChild'd after the fog) rather than Grok's zIndex/sortableChildren alt (which adds a per-frame full-stage sort the SW-WebGL perf doctrine avoids). (4) PRIME-AUDIT caught that the council's 'layer-membership UNIT test' fights the codebase doctrine (renderers are DOM-gated, browser-smoke-only) -> moved the proof to an e2e dual-pixel assertion (entity brown shows on the composited stage; board beside it stays fog-black; mask opaque at the entity = visible WITHOUT a board reveal). (5) kept vision.ts untouched so the scouting asymmetry (own units reveal the board; enemies show as bare sprites) is preserved. LESSON: when a user hands you a literal list AND the governing rule, AUDIT each item against the rule and correct the list with evidence (flagging the deviation) rather than rubber-stamping the enumeration; and verify the actual ARCHITECTURE (overlay-paints-over + snapshot-replicates-all) before sizing -- it collapsed a 'Full render+net' fear into a 6-file z-order change. Gates: tsc0 / vitest1142 / fog-e2e 6 pass.

- P3 #seagull #new-hazard #full #council-scoped-the-ask #verlet-decay-trap #mirror-the-pattern: shipped b9aff10. A whole NEW recurring hazard (seagull flies across the top dropping poop; poop-on-structure HALTS that structure's income until cleaned; poop-on-spark = 2x-slow 15s). DEFINING MOVES: (1) MIRRORED the 4 existing hazards end-to-end (hunter/potato/rainbow) — entity+lifecycle leaf modules, a dedicated 5th seeded rng stream so the prior 4 hazards stay byte-identical, additive snapshot fields, teardown in all 3 exit paths, the spawner-cadence->out-array->physicsLoop-dispatch + the main.ts per-tick fan-out. An integration-map subagent + verifying every wiring site myself (Rule 21) made a 16-file Full feature land with tsc green on the 2nd try (only the intended protocol-version tripwire fired). (2) the income-halt: the user said 'the WHOLE structure stops' — Council (both models) flagged whole-PLAYER halt as too harsh; SYNTHESIS = foul the hit prim's connected COMPONENT (a 'structure' IS a component) via computeComplexity skip-fouled-prims -> literal 'whole structure' + local/deterministic + tunable, honoring the ask without the whole-economy halt. (3) the poopy-slow: recognised the Verlet implicit-velocity DECAY TRAP (per-substep scaling exponentially freezes a spark) -> applied a one-time impulse-halve + a carry-follow rate scale (controls.ts), verlet CORE untouched; refuted Grok's 'substep-variation' risk with the LOCKED-8-substeps invariant. (4) PROTOCOL 6->7 even with NO new client intent (cleaning is host-detected) — the global income-affecting foul would confuse a stale peer (rainbow precedent), so gate at HELLO. (5) the gating lane caught 3 SELF-INFLICTED test-drifts (aboveFogLayer 4->6 children from adding the gull/poop renderers; the v6->v7 + v7->v8 protocol-mismatch literals) — fixed as routine bump-maintenance. LESSON: for a big new feature, MIRROR the closest proven pattern exhaustively (map every integration site first); when the user's literal ask and the Council's balance concern conflict, find the scoping that satisfies BOTH (component, not whole-player); and know the engine's traps (Verlet velocity is implicit -> never scale it per-substep). Gates: tsc0 / vitest1149 / build543.18KB / seagull-e2e + full gating lane green. PROTOCOL 6->7.
