# Reflexion Log — The Spark

Cross-session learnings, newest first. Each `## DATE — Session N` block holds that session's reflexion entries. Pruned to ≤50 entries; older entries live in the archived handoff docs.

## 2026-07-12 — Session 123: worker default-on prereqs batch + owner bot-intelligence amendment, 4/4 shipped. P1 VS-BOTS worker support fresh-from-seed via factory seam, bots differential HARD gate + e2e green first run (9f48d50). P2 networked worker-duel e2e — merged cross-mode matrix, remote-INTENT round-trip, wire-cadence bound (a8e073a). P3 dual-isolate GC audit, stabilized floors + raw-CDP worker-heap reads — NO LEAK either isolate, ~20k-tick oracle soak clean (c0eca11). P4 BOT_INTELLIGENCE_DESIGN.md Council-hardened, 7 owner questions (3ba5cf3). tsc 0, vitest 1884/1884, bundle 635.5/750, worker default-on now gated ONLY by the owner weak-device playtest. Ran on Fable 5 Ultracode.

- S123-P1 #verify-the-gates-mechanism-before-trusting-consensus: BOTH external Council seats simultaneously voted bit-exact serialization on the identical claim '(A) breaks the byte-identical differential gate' — a 2-seat consensus that collapsed on a 10-line read of the gate itself (fresh-vs-fresh from a pristine world, not a runtime handoff). Consensus across models is NOT independent evidence when both reason from the same unverified premise about how YOUR OWN test works; read the test before the vote, and weight seat-agreement at ~1 seat when their arguments share a load-bearing factual claim.

- S123-P2 #probe-the-pipeline-before-blaming-the-new-layer: the worker-duel red (joiner sparks=0) LOOKED like a worker-wire bug; a 10-line scratch probe showed host mirror + joiner in PERFECT tick lockstep with 0 hash mismatches — the wire was innocent, the e2e ENVIRONMENT starves the fps-capped sim (drain clamp 3xfps -> ~21 ticks/s -> production 0.15/s spawn rate stretches to ~1 spark/18s wall). The fix was the established spawn-rate seam every sibling spec already used. When a NEW layer sits atop a slow substrate, measure the substrate's rate FIRST — and check what conventions sibling tests adopted, they encode prior sessions' fights with the same environment.

- S123-P3 #a-suspicious-pass-is-a-finding: the v1 heap audit PASSED with -39MB 'growth' — a green that actually indicted the instrument (unsettled floor + wrong isolate). Both CHECK seats caught it because the recorded actuals made the anomaly visible; had the spec only asserted growth<limit without logging numbers, the meaningless pass would have shipped silently. Always RECORD the measured values a threshold consumes, treat a too-good/anomalous pass as a finding to explain, and when a platform API is absent (performance.memory in workers) probe what the platform DOES expose before writing off the measurement (Chromium /json/list turned out to list dedicated workers with their own debugger URLs — a 25-line raw-CDP read, no plumbing).

- S123-P4 #fix-degeneracy-by-sequencing-not-by-knobs: two Council-confirmed design degeneracies (sacrifice thrash loops, raid dogpiles) both had knob fixes on offer (cooldown timers, raider quotas needing shared state) — and both dissolved under a SEQUENCING constraint instead: sacrifice only while already Carrying the replacement piece (no revert window can exist), raider = a pure deterministic function of world state every bot computes identically (no negotiation state). In deterministic lockstep systems, prefer making the bad interleaving UNREPRESENTABLE over damping it with tuned state; knobs need maintenance, sequencing constraints are proofs.

## 2026-07-11 — Session 122: the S121-handoff top batch, 4/4 shipped + live. P1 B2 phase (d) worker cutover — TD-heavy re-measure FAILED the pre-registered ROI rule (structuredClone 10.1ms p95 @6x) → 60Hz transferable positions buffer + structural-batch snapshots; ?worker=1 live opt-in, 300-frame differential HARD gate, mirror-vs-worker hash oracle (82ea2c3). P2 host-migration D3 MIGRATION_CLAIM behind __TEST_MIGRATION__ — kill-host e2e GREEN first run over REAL 3-peer WebRTC, no PROTOCOL bump (5f53b3a). P3 green pulse capped at the income cap in scoring scan order (999e530). P4 deploy live (index-KQaaBM--.js). tsc 0, vitest 1882/1882, bundle 635.4/750, MCV 0. Full 2-round Council + 2 Triumvirate CHECKs: 1 CRITICAL adopted-fixed (allocator repair), 7 refuted with code cites. Actions ALIVE again — both deploy paths ran (owner: pick one). Ran on Fable 5 Ultracode.

- S122-P1 #cross-check-oracle-catches-your-own-layer: the mirror-vs-worker hashWorldState oracle's first live catch was MY OWN prediction layer (drag-preserve restore ran before the hash -> 6 mismatches), not the apply path. Order integrity checks to validate EXACTLY the layer under test (apply), before deliberate divergences (prediction/UX) re-enter. An oracle that fires on designed divergence trains people to ignore it.

- S122-P1 #measure-first-changed-the-design: the pre-registered ROI rule v2 FAILED on the TD-heavy re-measure (structuredClone p95 10.1ms @6x — 5x the stringify cost, refuting the 'simple clone' default BOTH Council seats and I expected to pass) -> the positions-buffer + structural-snapshot format came FROM the measurement, not the deliberation. Pre-registering the rule + the contingency ladder BEFORE measuring made the escalation mechanical instead of a debate.

- S122-P2 #suppress-competing-recovery-paths: the S82 auto-reconnect (transport teardown+rejoin @1s) and the D3 migration grace COMPETE for the same transport — a survivor mid-reconnect-cycle misses the claim broadcast. Seam-gated suppression + idempotent re-broadcast x5 made the e2e deterministic. When two recovery protocols share a resource, serialize them explicitly; do not let timers interleave.

- S122 #both-deploy-paths-ran: with Actions back alive, the master-push auto-deploy AND the manual gh-pages force-push both deployed the same session — they converged harmlessly this time, but dual-writer deploy paths are a race waiting for a divergence. Surface the owner decision at every close until one path is retired.

- S122 #feed-platform-constraints-into-council-prompts: BOTH Council seats bet R1 architecture on SharedArrayBuffer without knowing the deploy target (GH Pages) cannot set COOP/COEP headers. A 5-line PLATFORM CONSTRAINTS block in the R1 prompt (hosting, headers, worker/SAB availability, bundle charter) would have redirected both seats' effort to viable alternatives. Adopted for the S123 PDR template.

## 2026-07-10 — Session 121: B3 symbiotic batch (4 priorities, all live). P1 keystone rigidity telegraph — gold/green pulse renderer, cross-peer by DERIVATION from synced graph + world.tick, zero determinism cost (1ba7cdc). P2 Income Keystone — a Filament confers +0.25 income to up to 3 branched magic neighbors (per-Filament cap = Council anti-starburst); replay-self-consistent, differential-oracle mirrored, save.replay 24/24 (c77a817). P3 deploy live. P4 codex+title coherence — NEW codexPresentation.ts (characters wear art, geometry wears its BUILD CONSTELLATION → retired the voltkin-everywhere bug), textFit guards + tested copy budgets kill box-overflow, epic copy (87cdc35 + redeploy). tsc 0, vitest 1865/1865, bundle 625.3/750, MCV exit 0, live-verified on spark-online.space. Standard 3-way Council: 1 real balance finding ADOPTED, Q3 determinism criticals REFUTED (JS FP is deterministic; existing ×0.05 non-dyadic already passes byte-identical replay). CONST-4 close-gate id-matcher incident: `SNNN-PN-` ids invisible to the `^PN-` regex → guaranteed-block loop until ids renamed. Ran on Fable 5 Ultracode.

- S121-P1 #telegraph-cross-peer-by-derivation: the physics conferral (bond.stiffnessMultiplier) is host-only + un-synced, so a joiner cannot read it. Deriving the STRUCTURAL relation (un-fouled hub + magic neighbor sharing an endpoint) render-side from synced graph state + world.tick gives a bit-identical cross-peer telegraph with ZERO wire bytes. General pattern for visualizing an ephemeral host quantity: recompute from synced inputs, never sync the derived value.

- S121-P2 #income-keystone-replay-self-consistent-not-byte-identical: unlike the S118 rigidity Keystone (ephemeral stiffnessMultiplier → byte-identical), the income keystone changes SCORE values BY DESIGN. It is still safe because save.replay asserts run-A==run-B (self-consistency), NOT golden fixtures → both runs use the new formula and match. The gate is (a) mirror the term into the differential-test INDEPENDENT oracle (else the equivalence test fails) + (b) update any pre-existing test that hardcoded a now-changed value. Check whether replay tests are self-consistency vs golden BEFORE assuming a scoring change needs fixture regen.

- S121 #council-plan-value-check-value-refute: PLAN-phase Council caught a REAL balance finding (magic-bond term is uncapped scoring.ts:189 → income keystone amplifies clustering) → adopted per-Filament cap. But BOTH seats' Q3 determinism criticals (non-dyadic 0.3 → cross-arch FP desync; sort-by-id tiebreaker) were REFUTED on merits (JS doubles are spec-deterministic; existing ×0.05 non-dyadic passes byte-identical replay; a pure count has no tiebreaker). 10th #empirical-refutes-plausible-criticals: verify determinism criticals against language semantics + the existing passing suite before conceding.

- S121-P4 #copy-budgets-beat-fit-guards: the text-overflow class dies at AUTHORING time, not render time — put char budgets on the copy source (tested: name≤16/power≤34/recipe≤150) and keep fitText guards only as the safety net. The 34-char power budget came from a LIVE bounds measurement (42 chars escaped a 240px tile even at the fontSize floor) — measure the real zone before writing the budget, then encode it as a test so future copy fails in vitest instead of on screen.

- S121-P4 #placeholder-art-is-a-liability: three towers shipped sessions ago with a voltkin-zap placeholder sprite and it silently became the owner-visible "voltkin everywhere" bug. When a real asset is missing, prefer a PROCEDURAL representation derived from game truth (the recipe constellation in the board's own glyph language) over borrowing another entry's art — it can never be wrong, never drifts, costs zero assets. Encode the coherence rule as a test (sprite XOR emblem + anti-placeholder regression).

## 2026-07-10 — Session 120: Closed worker-sim phases (b)+(c) (3 priorities). P1 phase-(b) MEASURE — e2e/perf-snapshot.spec.ts (SPARK_PERF=1): REAL 2-peer Trystero duel, 3 probe windows incl. REAL 6× CDP CPU throttle; build 0.056–0.345 ms avg, send 3–6× build → pre-registered GO rule fired NO clause → NO-GO (eaca05b). P2 phase-(b) CLOSED-BY-MEASUREMENT docs close + BACKLOG S120 banner (543a3aa). P3 phase-(c) grid rebuild 64→8/tick (insertAll hoisted per-substep, post-integration) + 8-bit cellKey ctor guard + dense-pile hardening test (3fc6688). tsc 0, vitest 1841/1841 (+4), MCV exit 0, bundle 624.4/750, v14 held, live smoke clean. CHECK: 11 external criticals across P1/P3 → 2 adopted, 9 refuted (3 on FABRICATED symbols/files + invented runtime stats). Phase (d) worker cutover is next. Ran on Fable 5 Ultracode.

- S120-P1 #measure-first-kills-phantom-work: the 15-spread-dominates claim survived 15 sessions as an assumed bottleneck; ONE real instrument (2-peer Playwright duel + REAL 6× CDP Emulation.setCPUThrottlingRate — measured, not analytically multiplied) closed it in one session. Pre-register the GO/NO-GO rule BEFORE measuring: it makes the negative result undebatable and a shippable deliverable (CLOSED-BY-MEASUREMENT), not a failure.

- S120-P1 #repo-bounds-beat-speculation: when CHECK attacked the load-extrapolation ("could be 8-12×!"), the repo already held the answer — the 16KB wire-budget HARD gate (save.replay.test.ts:715) bounded worst-case at 2×. Search the existing test suite for empirical bounds before conceding any speculative multiplier.

- S120-P3 #reviewer-execution-claims-are-fabrications: a CHECK reviewer emitted I-ran-it claims (18%-of-runs, byte-184, tick-47) plus citations of NONEXISTENT files/symbols (spatial.test.ts, get3x3Neighbors, a ===0 guard). External reviewers have NO execution environment — auto-distrust every runtime claim; grep-verify every cited symbol EXISTS before spending triage effort. 9th #empirical-refutes-plausible-criticals (promotion-flagged at boot).

- SESSION #method #worked: batch PDR with an explicit NO-GO branch let the measurement decide P2 scope mid-session with zero re-approval friction; the docs-only close + truth-maintenance sweep (phase-d prereq row) is real work, not bookkeeping.

- SESSION #method #improve: OS session should harden council CHECK prompts with BOTH self-endorsed fixes: Grok [STATIC]/[PREDICTED] tagging + no-execution-claims rule; Gemini constraint-binding pre-computation + 4/5 baseline score cap. Also: pile-test fixtures — derive thresholds from an OBSERVED baseline run, never from analytic guesses (2 retunes wasted).

## 2026-07-09 — Session 119: Shipped the worker-sim seam (3 priorities). P1 B2 phase (a) runHostTick extraction — the host's ENTIRE per-tick sim body (~500 LOC) moved verbatim from main.ts's drain loop into DOM/Pixi-free state/hostTick.ts (the future Web Worker boundary), + NEW replay HARD gate (2× same-seed 1000-tick bot runs byte-identical) + NEW frozen-reference differential (verbatim pre-refactor code in-test, per-tick hash equality, 8 forced-state scenarios); 235653f. P2 dev-only snapshot-cost probe (performance.mark/measure build-vs-send split + __SPARK__.snapshotProbe; prod bundle byte-identical; 09f4bf2). P3 truth-maintenance (BACKLOG front-of-line refresh + root-fixed the every-boot ACTIVE-PLAN WARN; cdfdfd8). tsc 0, vitest 1837/1837 (+11), save.replay 24/24, bundle 624.2/750, PROTOCOL_VERSION 14 held, live browser smoke clean. CHECK: 8 external criticals → 1 adopted-modified, 7 refuted with file:line evidence. Gemini 2.5-pro retired upstream mid-session → gemini-3.1-pro-preview. Ran on Fable 5 Ultracode.

- S119-P1 #verbatim-move-plus-frozen-reference-differential: for a hot-loop extraction, transcribe the pre-refactor code INTO the test as a frozen reference and assert per-tick hash equality across forced-state scenarios — it catches transcription drift a two-run replay gate cannot (same bug on both runs), and it empirically re-verifies every deliberated substitution (per-frame alive-set) for free. PRIME-AUDIT R2 also killed an R1 events-seam design by tracing the CLIENT path: the edge watchers were shared, not host-only — trace the OTHER role's path before moving "host" code.

- S119-P2 #instrumented-twin-plus-throw-audit: when instrumenting a hot path, write the DEV branch as an exact call-twin of the prod line and prove parity by bundle-hash byte-identity; then audit what a mid-instrumentation THROW would skip (the send-gate stamp) — measurement code must be best-effort (try/catch) so the probe can never break the thing it measures.

- S119-P3 #fix-the-warn-at-its-root-not-the-scanner: the every-boot ACTIVE-PLAN WARN was neither hook-bug nor stray files — archived plan BODIES still said STATUS: IN-PROGRESS; correcting the two stale lines (not touching the hook, not moving files) silenced it with the hook's own predicate as the verifier. Check what a scanner actually greps before assuming it misfires.

- SESSION #method #worked: anchor-asserted scripted splices + frozen-reference differential moved 500 hot-loop lines with zero shipped defects; template the differential harness for phases c/d (~80 lines next time).

- SESSION #method #improve: 7x plausible-critical refutations say CHECK reviewer prompts should demand "verified semantics at file:line" before CRITICAL; also probe external-model availability at session start (gemini-2.5-pro retired mid-arc, 2 calls wasted; use gemini-3.1-pro-preview).

## 2026-07-06 — Session 118: Shipped the Tier-B strategic batch (4 priorities) LIVE + deployed. P1 B1 host-migration D2 — instrument-only detection layer (client PoP + NETSNAPSHOT epoch envelope + snapshot-starvation detector w/ visibility forensics + pure computeSuccessorSeat + live HELLO/warrant wiring; console-only, ZERO takeover; PROTOCOL_VERSION 14 held; 637ed84). P2 B3 Keystone Anchor — first SYMBIOTIC-CHAINING combo (an un-fouled Anchor confers partial territorial rigidity to bonded MAGIC structures → build topology becomes tactical, the North Star; replay-safe by construction; 5df5d4b). P3 F1b/F2 territory perf — ONE global union-find labeling pass replaces the per-player per-tick componentOf BFS re-walk, radius map reused (byte-identical; 06de193). P4 deploy — spark-online.space serves index-DrvI9WXb.js. tsc 0, vitest 1826/1826 (+44 new), F1b differential 400/400 bit-exact, save.replay 24/24 byte-identical, bundle 624.0/750 KiB. B1 carried its parked S116 3-way; focused NEW-surface 3-way Council this session (Grok+Gemini). Ran under Opus 4.8 (owner override). B2 worker-sim deferred to its own session.

- S118-P1 #execute-a-deliberated-parked-pdr-verbatim-but-re-run-A.0: the hostmig-D2 PDR was fully 3-way-deliberated in S116 then PARKED; S117 landed in between. Rather than re-deliberate, re-ran ONLY the A.0 state-discovery gate (net/ untouched by S117: D1 primitives present, succession.ts absent, no epoch, v14) -> the PDR's CURRENT-STATE block still held, so it executed verbatim. LESSON: a parked deliberated PDR carries its deliberation; the cheap, mandatory re-check is A.0 (has external state drifted?), not the whole Council.

- S118-P1 #ralph-patrol-caught-async-refactor-blocking-the-happy-path: signing the warrant BEFORE Begin (Grok ordering-by-construction) made beginMatch async with an await before the START_GAME dispatch -> a signWarrant throw would have silently prevented the match from starting. RALPH:PATROL flagged it; wrapped fail-open (log + proceed sans warrant). LESSON: when a correctness-ordering fix turns a sync happy-path async, audit what a mid-path reject/throw now blocks.

- S118-P2 #reuse-the-proven-replay-safe-template-for-the-cheap-proof: B3 (first symbiotic combo) deliberately extended the EXISTING applyAnchorStabilize pattern (ephemeral per-tick stiffnessMultiplier, idempotent constant-floor max) rather than inventing an income mechanic that would touch the byte-identity-sensitive scoring path. Result: replay-safe BY CONSTRUCTION, zero wire/save bytes, PROTOCOL_VERSION untouched, save.replay 24/24 green on the first run. LESSON: for a cheap proof-of-vision, pick the insertion seam whose determinism/replay-safety is already proven by a shipped sibling — novelty in the DESIGN (symbiotic conferral), not in the RISK surface.

- S118-P3 #the-caller-audit-was-the-real-gate-not-the-differential: the Council's convergent REJECT of F1b's naive gate resolved not by a fancier test but by a 2-minute componentOf-caller audit that found F1b does NOT modify componentOf (only adds a labeling internal to territory's count) + no caller consumes territory's labels -> Gemini's downstream-ID-key hazard was structurally impossible. The 400-world differential (canonical partition + complexity + radius + full stiffness map) then CONFIRMED byte-identity empirically. LESSON: when a reviewer flags a relabeling hazard, first check whether the shared primitive is even touched + who consumes its identity; a scoping audit can dissolve a hazard that a test would only sample.

- S118-P4 #deploy-last-makes-the-deploy-meaningful: sequenced deploy as P4 (after B1+B3+F1b/F2) rather than deploying the S117 build first, so ONE deploy carries real player-facing change (reliability forensics + a new combo) instead of a no-op S117 push. The deploy script's own live-hash verify + a post-deploy HTTP smoke (index 200 + bundle 200 exact-byte-match + apex 200) confirmed it without needing a browser. LESSON: when several priorities will ship together, defer the outward-facing deploy to last so it is a single meaningful release, and lean on the deploy script's built-in hash-verify + a cheap HTTP smoke for confirmation.

## 2026-07-05 — Session 117: Shipped the Tier-A audit-fix batch (3 priorities) from the S116 audit's recommended queue. P1 F1a single-pass complexity (byte-identical perf refactor — one prim pass + one bond pass replace the per-player re-walk in tickScoring; 471105d). P2 F3/F4/F5 consistency & hygiene (areaMultiplier documented RESERVED, LOCKED Magic-12→Magic-14, 23 stray root handoffs untracked; f3123e3). P3 F6 dep patch-bumps (trystero 0.25.2 + pixi 8.19; b9d77d3). tsc 0, vitest 1782/1782 (+3 F1a differential), bundle 611.0→617.2/750 KiB, PROTOCOL_VERSION 14 held. Ran under Opus 4.8 Ultracode (user override of the Fable-5 pin). Strategic Tier-B fork (hostmig-D2 parked / worker-sim / combo-depth) awaits owner pick.

- S117-P1 #prime-audit-the-council-not-just-the-code: Grok REJECTed F1a on an IEEE-754 float-summation-order kill-shot (N*0.6 vs incremental += in Map order) — real IN GENERAL, but PRIME-AUDIT against the actual code refuted it: computeComplexity counts INTEGERS then does the float math ONCE in a single final expression; no incremental float accumulation to reorder. Keeping that expression VERBATIM + feeding identical integer buckets makes the single-pass version byte-identical BY CONSTRUCTION. LESSON: a Council kill-shot is a hypothesis to verify against the code, not a verdict to obey — the strongest adversary can still misread the implementation; the code is the arbiter.
- S117-P1 #ship-the-byte-identical-half-defer-the-subtle-half: audit F1 had two collapses — F1a (scoring, integer-count-then-multiply-once → byte-identical) and F1b (territory, needs global component-labeling replicating the exact placerColor-relative connectivity). Council split on which to ship; resolution = ship the provably-safe half (F1a) now, defer the subtle half (F1b) to its own increment with union-find + differential test. LESSON: when a perf refactor has a trivially-equivalent part and a partition-equivalence part, split them — the differential harness built for the safe half de-risks the subtle half later.
- S117-P2 #resolve-the-real-defect-not-the-literal-instruction: F3 said "remove dead areaMultiplier", but it is woven through the LOCKED §6 Magic-14 seed catalog + 2 combos.test assertions — a literal removal would churn a governance doc and trip the no-scope-creep guard. The ACTUAL defect was "dead data masquerading as live schema", resolved by documenting it RESERVED (future area-mechanic slot) instead of deleting. LESSON: an audit finding names a symptom; fix the underlying defect in the way that respects surrounding invariants, not the literal verb.
- S117-P3 #a-dep-bump-is-a-behavioral-change-gate-it-like-one: F6 was "just" patch/minor version bumps, but pixi is the entire render layer and trystero is the transport — so it ran the FULL gate (tsc + 1782 vitest + build + bundle) like a code change, not a rubber-stamp. Bundle grew +6.2 KiB (pixi minor) but stayed under charter; no test moved. LESSON: dependency bumps on load-bearing libs get the same verification rigor as source edits — the diff being in package.json doesn't make it low-risk.

## 2026-06-30 — Session 115: Shipped a 3-priority Tier-1+infra batch — P1 ANCHOR planted-joint (Dot↔Square magic combo floors its territorial stiffnessMultiplier to resist the S49 enemy-territory sag) + P2 SPINDLE bounded-swirl (Line↔Circle orbits nearby free sparks; Council-mandated tangential-SPEED cap = provably no escape velocity) + P3 host-migration D1 (succession-warrant + client-identity + additive HELLO pubkey, DORMANT/feature-flagged off). PROTOCOL_VERSION held 14, save.replay byte-identical. 2-way Council (Grok ANALYST + Opus; Gemini 429) + PRIME-AUDIT, RALPH:PATROL PASS ×3. tsc 0, vitest 1779/1779 (+35), build 611.0/750 KiB, deployed live (commits 04ced0c / e80854f / 707c973).

- S115-P1 #scope-the-mechanism-against-the-engine-not-the-pdrs-mental-model: the PDR's "damp the prim drift velocity" Anchor design was a NO-OP against the real engine — placed primitives are NEVER Verlet-integrated (verletStepAll steps only world.freeSparks; resolveCollisions is spark-only; placement DELETEs the spark; hazards erase/sever a structure, never shove it), so there is no free momentum to damp. A.0-during-execution (read verlet.ts + physicsLoop + how potato/territory actually move a structure) revealed the ONLY real structural instability is the S49 territorial stiffnessMultiplier sag; reframed Anchor to FLOOR that sag (same owner-approved "planted joint" verb, engine-correct mechanism). LESSON: verify the PDR's IMPLEMENTATION mechanism against the integrator BEFORE coding, not just the feature's intent — the verb survived, the mechanism didn't.
- S115-P1 #a-stale-source-comment-is-a-claim-grep-the-construction-not-the-comment: the S91 "forward-key only — Square→Dot stays a placeholder" combos comment was SUPERSEDED by the S98 order-symmetry mirror loop (which mirrors every non-self magic pair, Anchor+Spindle included); my new isAnchorCombo docstring AND a unit test repeated the stale claim until the test caught it (isAnchorCombo(Square,Dot)===true). LESSON: trust the table-CONSTRUCTION code over an adjacent prose comment; reconcile the stale comment as part of the work; the symmetric reverse earns the BEHAVIOR, not just the income (Vortex S98 precedent).
- S115-P2 #bound-the-accumulated-STATE-not-the-per-tick-DELTA: Grok's REJECT of the naive Spindle (constant tangential impulse → escape velocity) was a real kill-shot; capping the per-tick IMPULSE does NOT fix it (it still accumulates across ticks since the spark stays in range). The fix bounds the RESULT: project the spark's current velocity onto the swirl axis and only add up to the headroom to SPINDLE_MAX_TANGENTIAL_SPEED. Provably non-accumulating; a 500-tick unit test asserts the bound holds every tick. LESSON: when a behavior adds to a quantity each tick, bound the STATE (resulting speed), not the DELTA (per-tick impulse).
- S115-P2 #a-hand-hacked-PLAYING-world-cannot-runtime-verify-a-behavior: driving the hidden preview by setting world.gameState='PLAYING' on a TITLE world (a) ran tick++ but SKIPPED stepPhysics until I cleared the forceNonet sudoku-FREEZE branch, then (b) NaN'd within ~14 ticks — a no-Spindle CONTROL spark ALSO NaN'd, proving the uninitialized-match state (territory/scoring/collision on a title world) is the NaN source, NOT the new code. LESSON: the app gates stepPhysics on its SCREEN state, not world.gameState; a hand-forced PLAYING world is not a valid runtime fixture. Use physicsLoop.test.ts (real stepPhysics) for wiring + the browser load-smoke for "no console error"; to truly drive live, start a real match via the title buttons (S113 method).
- S115-P3 #ship-the-PRIMITIVES-dormant-defer-the-LIVE-WIRING: host-migration D1 is the textbook "land the tested foundation, activate later" phase — the warrant build/sign/verify + client identity + the additive-optional HELLO pubkey field all ship now (14 tests, real WebCrypto), but NOTHING is wired into a live decision path (no boot keygen, no live HELLO populates the field, no MIGRATION_CLAIM). This keeps a security-sensitive netcode feature at ZERO behavior-change before the owner playtests, while D2–D4 build on a proven base. KEY trust property: verifyWarrant chains to the room-code COMMITMENT (host pubkey fingerprint MUST equal the room code) so a successor can't forge under a different key; reused the buildAttestPayload injective length-prefixed encoding + WebCrypto raw P-1363 sigs. LESSON: for risky infra, separate "capability exists + is tested" (D1) from "capability is active in the live path" (D2+); additive-optional fields with fail-closed parsers make the dormant phase truly inert.

## 2026-06-29 — Session 114: Shipped G4 in-world leader crown — a STATIC gold crown over the score-leader's avatar (networked/bots, PLAYING, not benched) via pure `scoring.leaderPlayerId` + `avatarRenderer.shouldShowCrown`. Render-only, PROTOCOL_VERSION held 14, replay byte-identical. Also reconciled the stale BACKLOG roadmap (G3b + G4 bond-juice were already shipped). Micro tier (user-path delib waived), RALPH:PATROL PASS. tsc 0, vitest 1744/1744 (+10), build 609.6/750 KiB, in-browser verified, deployed live (commit c2c1e80).

- S114 #scope-roadmap-next-items-against-the-code-not-the-roadmap-text: the BACKLOG listed G3b (Codex silhouettes) AND the G4 bond-formation juice as "next", but both were already shipped (G3b S97 in comboCodexStore/codexOverlay; bond-juice in render/effects/bondCommit.ts). Only grepping the code for the feature's expected symbols revealed the genuinely-open item (the G4 in-world leader crown). LESSON: a roadmap's "next" label is a CLAIM, not ground truth — run State-Discovery (grep the expected exports/files) on a candidate BEFORE picking it, or you rebuild something already done. Reconcile the roadmap text as part of the work.
- S114 #walking-a-pixi8-stage-for-render-verify-must-match-the-minified-class-name-and-assert-on-getBounds: the in-browser crown verify first found 0 graphics because the stage walker matched `constructor.name==='Graphics'` but Pixi 8 minifies the class to `_Graphics`; switching to `/Graphics/.test(name)` + asserting on `getBounds()` geometry (cx=700,cy=516,16×12.9 over the leader; moved to cx=1200 on lead-swap; null in solo) gave a deterministic runtime proof. The screenshot timed out (hidden preview can't paint a capturable frame, S113 artifact) — geometry assertion beats a pixel grab here. LESSON: when verifying render output by walking the Pixi tree, regex-match the (minified) class name and assert on `getBounds()`, not a screenshot.

## 2026-06-29 — Session 113: Shipped Batch C — the lightning-drone building. A new `lightningHub` recipe (1 Dot + 5 Circles) emits 3 suicide drones (procedural Voltkin rig @0.5) that home on + sever enemy connectors, then the structure self-destructs in a 240px owner-agnostic AoE. Full tier, PROTOCOL_VERSION 13→14, host-authoritative + replay byte-safe. Council 2-way (Grok-4.20 ANALYST + Opus; Gemini 429-down) SHIP-WITH-FIXES + PRIME-AUDIT. tsc 0, vitest 1734/1734 (+18), build 608.7/750 KiB, in-browser end-to-end verified, deployed live (commit 759fe80).

- S113 #a-new-spawner-emitted-type-silently-joins-any-cap-keyed-on-sourceSpawnerId: underChewerCaps + applySpawnCreature discriminated chewer-vs-Voltkin on `sourceSpawnerId === null` (a 2-value proxy that meant "spawner-emitted"); the new lightning-DRONE is ALSO spawner-emitted (sourceSpawnerId != null), so it silently counted toward the chewer GLOBAL cap AND a drone SPAWN_CREATURE routed through the chewer-cap re-check — violating the owner's INDEPENDENT-caps decision. RALPH:PATROL caught it (unit tests were green because none mixed the populations). FIX: discriminate on `creature.type` ('chewer' / 'lightningDrone'), not the provenance field, + added a cap-independence test. LESSON: when a boolean discriminant was a proxy for a type and you add a THIRD value into the proxied set, grep every reader of that proxy — each one silently mis-buckets the new type.
- S113 #drive-a-hidden-preview-sim-via-app.ticker.update-not-rAF: the preview page runs document.hidden=true so requestAnimationFrame is throttled and world.tick stayed 0 (no sim, no render) — but app.ticker.started is true and `app.ticker.update(growingTimestamp)` steps the loop deterministically. Driving 400+ manual updates ran the WHOLE time-based feature end-to-end in-browser: inject a hub via window.__SPARK__.world → recipe registers a spawner → 3 drones emit → a drone flies to + severs the enemy connector (enemy bonds 1→0) → the structure self-destructs wiping its own prims, 0 console errors. LESSON: a hidden/headless Pixi preview can still be driven frame-by-frame via app.ticker.update(ts) + state injected through the __SPARK__ DEV accessor — a REAL runtime verification of time-based sim, not a static parse, when rAF is asleep. Also: to start the canvas-only title screen (no DOM buttons), emit the Pixi federated 'pointertap' directly on the button object found by walking titleScreen.container.
- S113 #dont-bind-verification-to-a-moving-version-literal: a PRIOR priority's verification[] needle `PROTOCOL_VERSION = 13` (true when S110 shipped) hard-FAILED the global MCV gate (verify-session-claims.py, Stop hook) the moment THIS session legitimately bumped it to 14 — the gate re-checks EVERY completed priority against current disk, so a moving-target literal in any past priority breaks on the next change to that value. FIX: re-bind the stale claim to a DURABLE artifact of what that session actually did (S110's permanent `12->13: HELGA's walk-to-target rework` comment), not the live value. LESSON: when writing a verification[] needle for a value a LATER session will change (PROTOCOL_VERSION, a count, a hash), bind to the immutable evidence of the change (a dated comment / the bump-history line), never the current literal — else you fabricate a future MCV hard-fail for an innocent successor.

## 2026-06-28 — Session 112: Shipped HELGA as a real veo-animated character (idle/walk/slap tick-synced atlas) replacing the in-world procedural puppet — owner loved the veo walk clip ("genuinely good"). State-driven audio: her theme loops only on walk/attack, base game music when idle; slap fires a recorded HHWAPAH+clap. Render+asset+audio only, NO wire/protocol change (v13 held). tsc 0, vitest 1716/1716 (+6), build 604/750 KiB, in-browser render verified, deployed live (commit eeffb5e).

- S112 #veo-img2vid-is-strong-on-ambient-motion-weak-on-fast-specific-actions: veo image-to-video seeded from a static character holds it ON-MODEL for walk/idle (generic ambient motion) but CANNOT produce a crisp wind-up→impact→recover slap from a seed with no target (2 attempts, fast + standard model; the violent-worded prompt was content-filtered to 0 samples, the non-violent reframe returned a usable gesture). LESSON: generate veo for ambient states + sell the HIT with SFX + a VFX burst + a tick-synced lunge; don't burn re-rolls fighting img2vid's weak axis; reframe violent prompts non-violently to dodge the safety filter.
- S112 #matte-a-veo-clip-needs-component-keep-plus-shared-foot-canvas: the S110 border-flood still-matte must be EXTENDED for an animation: (a) drop stray opaque specks via largest-connected-component keep (AI-video margin noise isn't achromatic-bright so the bg-flood spares it and blows the alpha bbox edge-to-edge) and (b) place every frame on ONE SHARED foot-anchored canvas (per-frame bbox crop makes the character jitter/teleport in world space). Diagnose the alpha empirically (dump border-column opacity on one frame) before assuming the matte is broken — the bbox alarm was her widest gesture frame, not a bug.

## 2026-06-28 — Session 111: Got S110 LIVE — it had been built+pushed but never deployed for ~2 days. Diagnosed the deploy block to its true root cause, bypassed it, shipped, verified by content-hash, and left a one-command deploy script (`npm run deploy`). spark-online.space now serves the S110 build (index-D9JyMcs9.js + all art assets 200). Repo flipped PUBLIC (gitleaks: 0 leaks/729 commits). 2 artifacts: gh-pages deploy branch + ops(s111) 46eeff3.

- S111 #account-billing-lock-kills-ALL-actions-not-just-minutes: the S110 "private-repo out of free minutes" diagnosis was INCOMPLETE. The real cause — read verbatim from the Actions annotation ("The job was not started because your account is locked due to a billing issue") — is an ACCOUNT-level billing LOCK that fails EVERY Actions run (deploy + e2e + Pages-via-workflow) regardless of repo public/private; GitHub's generic "workflow file issue" hint is a red herring. Distinguish: a 0s startup_failure on a private repo *can* be exhausted minutes, but if it persists AFTER going public, read the per-run annotation (`gh run view <id>` → ANNOTATIONS) — it names the true cause. LESSON: don't stop at the first plausible deploy-block theory; get the verbatim annotation before prescribing the fix — raising a spending limit would NOT have fixed an account lock.
- S111 #classic-pages-builder-bypasses-the-actions-lock: GitHub's legacy/branch Pages builder runs on SEPARATE infra NOT gated by the account billing lock — it only needs the repo PUBLIC to run free. The fix that actually shipped S110 live: make repo public (gitleaks full-history clean first), `npm run build` locally, force-push dist/ to a gh-pages branch, set Pages build_type=legacy/source=gh-pages, POST .../pages/builds, then VERIFY by curling the live entry-asset content-hash (not by trusting status="built"). Do NOT switch Pages to "GitHub Actions" mode — that path IS the locked one (fails instantly). LESSON: when the CI runner is unavailable, the classic Pages builder is a runner-free deploy path — raise the charter, never get stuck.
- S111 #MSYS_NO_PATHCONV-global-export-breaks-git-tmp-paths: a deploy script that `export`s MSYS_NO_PATHCONV=1 (to stop Git Bash rewriting `gh api repos/...` endpoints) ALSO stops it translating `/tmp/tmp.XXX` for the native git.exe, so `git -C /tmp/...` dies "cannot change to '/tmp/...': No such file or directory". FIX: scope the flag to each `gh api` call inline (`MSYS_NO_PATHCONV=1 gh api ...`), never global-export it, so git keeps MSYS path translation. Caught ONLY because I ran the script end-to-end instead of trusting `bash -n` — runtime-verify beats static-check.

