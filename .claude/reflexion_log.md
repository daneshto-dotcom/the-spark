# Reflexion Log — The Spark

Cross-session learnings, newest first. Each `## DATE — Session N` block holds that session's reflexion entries. Pruned to ≤50 entries; older entries live in the archived handoff docs.

## 2026-06-22 — Session 95 (cont.): NONET realm visual buildout — IP rework (Totoro look-alike → original moss-spirit, after the user flagged it LIVE), then a guardian council of 4 original spirits, then a per-frame "living realm" animation pass (breathe/blink/hop/float + a 16-firefly swarm). commits 2f0fa89 / 34c545e / d556dc3, pushed → deploy live; tsc 0, vitest 1488/1488, entry 553.6/560 KiB, generative ~$0.24 cumulative.

- #shipped-an-IP-look-alike-then-got-flagged-live — self-check generated art FIRST: P1 shipped an Imagen kami that was an unmistakable Totoro (Studio Ghibli); the user caught it only AFTER it deployed. The vector 404-fallback AND the little white "kodama" were also Ghibli echoes. LESSON: AI image-gen drifts toward famous characters by default — before shipping ANY generated character, self-review for franchise resemblance and prompt explicitly for an ORIGINAL silhouette/features (saved as a standing project memory). Reworked to an original mossy seedpod spirit via the same magenta + hue/min-channel key.
- #procedural-life-on-static-sprites-is-cheap-and-high-impact: the realm felt alive with NO new art — per-frame sin-driven transforms give each static webp its own idle (breathe = belly scale.y pulse; blink = quick scale.y squint via a pure tested blinkPulse; hop = abs(sin) bounce + squash-on-land + L↔R patrol; float = drifting loop), phase-desynced so nothing moves in lockstep, plus a swarm of pre-drawn firefly Graphics that only animate position + alpha. LESSON: drive DECORATIVE animation on wall-clock (performance.now), NOT world.tick (which jumps on client snapshots) — keep cosmetic motion off the sim clock; extract the one non-trivial envelope (blink) as a pure unit-tested helper.
- SESSION #verify-animation-by-state-sampling-not-one-screenshot: rapid preview reloads wedged the headless tab's rAF (screenshots time out, world.tick stuck at 0). A FRESH preview server screenshots fine, and motion is provable without a screenshot by pumping app.ticker.update() + sampling each sprite's x/y/scale/rotation at t and t+Δ. LESSON: for animation work, restart the preview server (don't reload-churn one), and verify motion by sampling transforms over time rather than trusting a single frame.

## 2026-06-21 — Session 95: P0 NONET overlay never appeared in live 1v1 (catchless latching lazy dynamic-import → 180s silent hang, no board) FIXED via robust load (.catch+throttled-retry) + preload + global vite:preloadError reload + the missing 2-peer snapshot round-trip test; P1 illustrated kami sprite (Imagen + Pillow hue-key) auto-upgrading the Codex tile + overlay, bundle charter raised 550→560 KiB AND mechanically enforced in the build; P2 anime SFX + winner-colour resolve flood + resolve screen-shake, all kept in the lazy chunk. commits e23b8a8/92b7ea8/d5c54de, pushed → deploy live; tsc 0, vitest 1484/1484, entry 553.6/560 KiB, generative $0.12.

- P0 #a-silent-catchless-lazy-import-is-a-time-bomb-for-trigger-critical-ui: the NONET overlay 'never appeared in 1v1' was not a render bug (it renders + solves fine in solo) — it was an unguarded dynamic import() (no .catch) whose loading-latch flag stayed true forever, so one chunk-fetch failure (transient, or a stale chunk-hash 404 right after a deploy) froze the whole duel for the 180s timeout with NO board and NO error surfaced. LESSON: any lazy chunk that gates a critical, time-boxed interaction MUST (a) .catch + un-latch to retry, (b) preload ahead of the trigger, and (c) be covered by a vite:preloadError reload for the stale-deploy case. Verify the LOAD path, not just the happy-path render.
- P1 #key-a-stubborn-chroma-bg-by-its-minimum-channel-not-its-brightness: Imagen ignored the #ff00ff chroma request and returned a vignetted rose-pink bg. A brightness/distance key fights the vignette and risks holes; but the CHARACTER's palette (slate/cream/amber) never has GREEN as its minimum channel while the pink bg always does — so keying on 'G is the min channel AND R,G,B make a pink' cleanly separated them regardless of the gradient (corner diag: bg pinkness 67 vs char -7), then spill-suppress + 1px erode killed the fringe. LESSON: when a generated chroma bg is imperfect, key on a HUE/channel-relationship invariant the subject can't satisfy, not on absolute colour distance.
- P2 #a-soft-prose-charter-with-no-CI-guard-will-always-drift: the '550 KiB bundle' lived only in handoff prose for ~40 sessions with no LOCKED entry + no mechanical check, so it silently crept over. Raising the number alone would just restart the drift; the fix was raise + ENFORCE — a check script in the build that the deploy CI runs, plus keeping new feature code (NONET SFX/flood) in lazy chunks so the entry chunk barely moved. LESSON: a budget that isn't mechanically checked is a wish; gate it in the build and code-split to defend it.

## 2026-06-21 — Session 94: P1 colour bug (rainbow/poop left a human stuck Silver/white -> derange only the 6 human colours; Silver bot keeps Silver) + P2 NONET tweaks (trigger = a connected 9 of ANY one SparkType via a per-tick host SWEEP, fires on build-up OR erase-down; timeout 120->180s; synthetic Codex super-combo entry). commits 672cddd + 9333bbc, pushed -> deploy.yml; tsc 0, vitest 1467/1467, build OK, bundle 553.0KiB (+3 over 550 charter, flagged)

- P0 #one-root-cause-can-explain-two-symptoms-trace-to-the-shared-state: 'stuck white after rainbow OR poop' had ONE root — buildShuffleColorMap deranged over all 7 PLAYER_COLORS incl. the bots-only Silver (near-white), so a human could map INTO Silver (and a poop-foul reverts to that same Silver). The render tints were all render-only + self-reverting, which RULED THEM OUT and pointed upstream at a STATE corruption (player.color) that BOTH triggers flow through. LESSON: when two different triggers yield the same wrong end-state, find the shared state they both pass through (one fix), not two; a 7th 'special' palette entry leaking into a 6-human shuffle is an off-by-one-set bug hiding behind PLAYER_COLORS.length.

- P1 #a-per-tick-host-sweep-beats-a-per-event-hook-for-emergent-triggers: NONET must fire whether a 9-same-type structure is BUILT up or ERASED down. A per-placement hook catches only build-up (and would need a second hook on every erase/sever/bomb path). A single per-tick host sweep (detectNonet over all components, gated by the once-per-match guard so it's free after firing) catches ALL topology changes from ONE integration point at ~the cost of tickScoring's existing per-tick walk. LESSON: for a trigger reachable by many state mutations, sweep the resulting state once per tick (guarded) instead of hooking every mutation path.

- P1 #a-synthetic-codex-entry-with-a-forward-compatible-asset-path: NONET isn't a godly recipe so it can't go through listRecipes()->entryFromRecipe. A synthetic CodexEntry (id cast to GodlyId, unlocked via unlockGodly on the rising edge) slots into the same grid. makeTile already .catch()es a missing sprite (tile stays image-less, name+hint show), so pointing characterSprite at the FUTURE Phase-2 path renders cleanly now AND auto-upgrades when the asset lands — zero rework. LESSON: when a UI slot wants an asset you don't have yet, point at the eventual path + lean on the existing missing-asset fallback for a forward-compatible stub.

- SESSION #after-a-context-reset-mid-edit-run-the-failing-gate-first: a reset landed mid-P2 (sudokuEvent.ts fully changed incl. mintNonetSeed signature, but callers in placePrimitive/main.ts/tests NOT updated -> tsc broken). On resume I ran tsc FIRST to get the authoritative list of the half-done surface (2-arg call sites + renamed const) rather than reconstruct it from memory. LESSON: after a reset mid-implementation, run tsc/the failing gate immediately to diff what's actually half-done.

## 2026-06-20 — Session 93: NONET 9-square Sudoku-lock shipped (ALL modes) — gen+event+trigger+freeze+overlay+netcode(PROTOCOL 8->9, snapshot sudoku field seed-only + SUDOKU_SOLVED intent)+realm-audio(Cloudstep Caravan, off-bundle ogg); commit 31a8aa6 pushed->deploy.yml building spark-online.space; tsc 0, vitest 1463/1463 (incl. 3 snapshot-roundtrip), build OK, bundle 552.7KiB (+2.7 over 550 charter, flagged); preview-verified overlay 3-state, 0 console errors; user playtesting + combo order-symmetry bug flagged

- P1 #netcode-sync-a-host-event-by-mirroring-the-additive-optional-snapshot-pattern: to make a new host-authoritative event work in 1v1, (a) add an additive-optional field to WorldSnapshot (emit while active, omit -> client clears), (b) serialize ONLY the seed and REGENERATE derived state (the 6x6 puzzle) client-side from the shared mulberry32 RNG -- never send the grid, and (c) add ONE client INTENT for the single player-originated action. (c) forces matching edits across 5 tsc/test-enforced surfaces: GameAction union + dispatch case, the exhaustive KNOWN_GAME_ACTION_TYPES Record (also the parseNetMessage gate), the CLIENT_INTENT_TYPES allowlist, BENCH_INTENT_POLICY (a test asserts set-equality with the allowlist -> a new intent FORCES an allow/deny decision), and the PROTOCOL_VERSION literal-type tripwire on HelloMsg.protoVersion. Bump the version for a new gameplay intent so cross-version matches hard-reject at HELLO instead of silently desyncing.

- P1 #freeze-a-sim-loop-without-starving-the-network-or-the-clock: an early `continue` that pauses the whole fixed-step loop ALSO skips the snapshot-send at the loop bottom -> 1v1 clients stall on their last frame. Fix: inside the freeze branch, (1) still advance world.tick so timeout/resume windows elapse, (2) run the event's own per-tick driver, and (3) REPLICATE the host snapshot-send (same cadence guard) before continuing. Also: a whole minigame can cost only ~+4KiB of CORE bundle if the heavy Pixi VIEW is a lazy dynamic-import (mirror the codex/bot/debug overlay pattern) while the generator+event logic stays core (reducer+snapshot need it synchronously). Landed +2.7KiB over the 550 charter -> flag the residual, don't hide it.

## 2026-06-17 — Session 92: HYGIENE Micro — renamed MAGIC_12_KEYS→MAGIC_COMBO_KEYS (the const maps 14; 5 refs/3 files, RHS unchanged→semantically inert) + decoupled session15.test.ts solo-win loop from PHASE_1_WIN_SCORE/SCORE_ANCHOR (→ fixed WINNING_STRUCTURE_SIZE=12, dropped now-unused SCORE_ANCHOR import) — the two S91-CHECK LOW items, zero runtime behavior change; commits 3244ed3/c64caa8; 3-lens adversarial CHECK (wf_31455c37-1ef) ALL CLEAN high-confidence; tsc 0, vitest 1433/1433, bundle 548.3KiB<550 (unchanged)

- P1 #tsc-proves-rename-completeness-grep-proves-the-rest: a pure identifier rename (MAGIC_12_KEYS→MAGIC_COMBO_KEYS) is exactly what tsc fully validates FOR THE CODE — any missed/typo'd reference fails the build, so green typecheck IS the code-completeness proof. But tsc is blind to two non-type surfaces: (a) string-literal / dynamic access (obj['MAGIC_12_KEYS'], a test asserting on the NAME, a serialized key) which type-checks fine while silently breaking, and (b) live-doc staleness. So the CHECK grepped the LITERAL OLD name across the whole repo: 0 dynamic/string hits in src, 11 doc hits — each classified current-state (would-be-false → fix) vs dated-history (handoff/archive/reflexion/session-state task label → LEAVE, editing is revisionist). All 11 were history. LESSON: for an identifier rename let tsc carry code-completeness, but STILL grep the literal old name across src+docs for the two things tsc cannot see — dynamic/string access and present-tense doc claims — and split doc hits current-vs-dated before touching any.
- P1 #prove-a-test-loops-contribution-before-rescaling-it: session15's solo-win loop ran PHASE_1_WIN_SCORE/SCORE_ANCHOR (=630) iterations and LOOKED load-bearing. Before pinning it to a fixed 12, I verified the loop is INERT to the assertions: the win is driven by the direct w.scoreProgress=PHASE_1_WIN_SCORE set on the very next line, and the CHECK confirmed via gameState.ts:59 that WIN gates ONLY on floor(scoreProgress)>=threshold while placePrimitive never writes scoreProgress/scoreByPlayer — so 12 vs 630 changes nothing the test asserts (the sibling 1v1 test wins with ZERO placements). LESSON: before rescaling/removing a test loop, trace whether any ASSERTION depends on its iteration count; a loop that only mutates state the assertions never read is safe to pin to a small intentional constant — and decoupling it from a balance constant structurally prevents a future rebalance from silently changing the dispatch count (closes the S91 #a-relative-test-still-breaks-on-a-fixed-iteration-budget lesson at this site).

## 2026-06-16 — Session 91: G2-PROMO Phase 1 — Dot→Square (Anchor) + Line→Circle (Spindle) promoted to magic (visual-only) + Option A win-score rebalance (210→630 / 70→210) — commits 1cd7e3b/59df666/7fa2017; PDR recovered from the S90-resume-killed design-workflow journal (zero loss), audit READY; E2E 2-browser GREEN (37/1skip/0fail, Sym I win-condition + Sym F territorial both green); 3-agent adversarial CHECK completed (Balance/Desync CLEAN, Test-net LOW/MED-cosmetic → 4 doc-fixes); vitest 1423→1433, tsc 0, bundle 548.3KiB<550

- P1 #recover-an-orphaned-workflow-from-its-journal-dont-rerun: the G2-PROMO design workflow (5 map + 3 design + synthesis + audit = 10 agents) ran to COMPLETION but a SessionStart:resume tore down the detached process at the finish line, so the completion notification never delivered and the task fell off the list — it LOOKED like a failure. Every agent result was journaled to disk (journal.jsonl, 10 'result' records). I recovered the full PDR (the synthesis string) + the READY audit (the dict) directly from the journal with a tiny python extractor — zero re-run, zero token waste. LESSON: a background workflow that 'failed' on a session-resume almost certainly finished its agents; read journal.jsonl in the workflow transcript dir and pull the cached results before spending tokens to re-run. (Bonus trap: the PDR string had a Unicode arrow that crashed a naive cp1252 python print — write to a utf-8 file, don't print.)
- P1 #a-relative-test-still-breaks-on-a-fixed-iteration-budget: the PDR + the design-phase audit both asserted only scoring.test.ts:330-331 needed care for the win-score rebalance. WRONG — tripling SCORE_TIER_STEP (70→210) silently broke 5 tier-pulse tests (session10 x3 / session13 x2). The tests are RELATIVE (accrue until scoreProgress >= SCORE_TIER_STEP) so semantically correct, BUT cap iteration at a FIXED 20000 ticks; at complexity 13 the 2-band target (420) needs ~38.8k ticks, blowing the guard. Caught during the pre-edit grep sweep by MODELING ticks-to-band (income = 0.05*complexity/PHYSICS_HZ). LESSON: when a rebalance scales a constant N×, grep every FIXED-iteration accrual loop keyed to that constant and re-derive whether the loop budget still reaches the (now N× larger) target — a relative assertion does not protect a fixed iteration cap. Bumped to 120000.
- P1 #verify-the-serialization-SHAPE-not-just-the-no-bump-claim: the desync-safe claim hinged on whether discoveredCombos serializes by VALUE (key strings) or by INDEX into a 12-element MAGIC_12_KEYS array — the latter would make +2 combos shift indices and silently corrupt cross-version saves/mirrors. Rather than trust the PDR's 'additive-optional, no bump', I READ save.ts:525-526 and confirmed it emits [...world.discoveredCombos].sort() — key STRINGS by value, rebuilt into a Set on rehydrate (save.ts:722). So 2 new keys are purely additive; schemaVersion stays 1, no PROTOCOL_VERSION bump. LESSON: for a 'wire/save-safe' claim, verify the actual SERIALIZATION SHAPE (by-value vs by-index/by-count) in the serializer, not just the no-bump assertion — an index/count encoding is the failure mode a count change trips.
- AUDIT #passing-green-gates-still-warrant-the-doc-drift-sweep: tsc 0 + vitest green + bundle said nothing about the stale 'magic-12'/'Combos N/12'/'12 magical + 24 functional' CURRENT-STATE comments + the BACKLOG ROADMAP still listing PROMO as DEFERRED — exactly the S90 #ship-doesnt-mean-skip-the-doc-drift-sweep lesson recurring. Swept all current-state count comments 12→14 (left the HISTORICAL session-tagged S7/S19/S20 ones — editing those would be revisionist) + marked PROMO shipped in the ROADMAP + added the S91 SESSION HISTORY entry. LESSON: after a count/identity change, grep the literal old count across src + docs and fix every PRESENT-TENSE claim the same session; distinguish current-state drift (fix) from dated history (leave).
- CHECK #a-doc-sweep-can-MANUFACTURE-a-false-claim: my own 12→14 sweep blindly swapped bondCommit.ts:5 from '12 magic + 24 functional' to '14 magic + 22 functional' — but the BOND_COMMIT commit-pop switch still dispatches only 12 magic combos (Anchor/Spindle correctly fall to the default ring pop; bespoke commit-flair is Phase-2). So the sweep turned a stale-but-harmless comment into an actively FALSE one. The 3-agent adversarial CHECK's test-net lens caught it (MED) by reading the SWITCH, not the comment. LESSON: a count-swap doc sweep is only safe where the number describes DATA that actually changed — when the number describes CODE (a dispatch arity, a hardcoded test list), verify the code changed too, or you manufacture a false claim worse than the stale one. Sweep by reading the referent, not by find-replacing the digit.

## 2026-06-16 — Session 90: G1b combo behaviors — ECONOMY Filament income trickle (a448fd6) + DEFENSE Diamond/Lattice anti-sabotage (f8adc57) + post-audit fixes (save.replay scoring coverage 7797b01, BACKLOG drift + stray-file hygiene 9586c72) — Full-tier 2-round Council Option 1 (Grok-4.20+Gemini-2.5-pro); MOTION + all G2 deferred; adversarial Triumvirate CHECK + ultracode 6-dim final audit both = SHIP; vitest 1407->1423, tsc 0, bundle 547.0KiB<550, E2E 2-browser GREEN on tip

- P1 #the-cue-already-half-existed-ground-truth-the-renderer-first: the Council (Gemini, 1.75 on UX) was emphatic a silent Filament income bonus is 'a tree falling in an empty forest' — but reading the render layer FIRST showed each magic combo ALREADY draws a distinct persistent silhouette (drawFilament etc.) and the filament starburst ALREADY pulses on tick. So the 'render-derived, both-peers-visible' cue requirement (AUDIT-2) was largely MET by existing code; the gap was only 'show it's EARNING', not 'show it exists'. Added a minimal pulsing income-node ring rather than a speculative particle-flow-to-HUD (logged that as G4 juice, playtest-gated). LESSON: before building a 'we need a cue' feature, read the renderer — the visual identity may already exist; scope the addition to the actual gap, and don't gold-plate VFX before the mechanic is playtested.
- P1 #a-special-combo-breaks-the-generic-combo-test: promoting Dot->Line's scoring (Filament trickle) broke exactly one existing test (session9 'a magic bond adds the magic premium') that had innocently used Dot->Line as a GENERIC magic example expecting complexity 4 — now 4.6. It went through the real placement pipeline, so rather than swap it to a non-Filament pair I updated it to 4.6 and re-purposed it as an INTEGRATION-level lock for the Filament behavior (proving the trickle survives placePrimitive, not just the unit-level addBond). LESSON: when you give one combo special behavior, grep for tests that used that specific pair as a stand-in for the generic case; a forced test update is often a chance to upgrade the test into a real regression lock.
- P2 #derive-the-gate-from-the-cost-fn-so-they-cant-disagree: the DEFENSE premium needed BOTH the entry gate (canSeverBond) and the decrement (computeBaseCharge in severBond.ts) to know a Diamond costs 2. Rather than duplicate the isDefensiveCombo check in both (which invites the classic check-then-charge mismatch → negative charges), I made canSeverBond gate on `charges < computeBaseCharge(...)`. They now agree BY CONSTRUCTION — and the adversarial CHECK's TOCTOU finding was refuted precisely because the gate and the cost are the same pure fn over the same fetched-once refs in a synchronous reducer. LESSON: when a check and a mutation must use the same derived quantity, have the check CALL the quantity fn, don't re-derive it; single-source kills the TOCTOU class.
- P2 #the-armor-cue-fought-three-geometry-tests-so-i-cut-it: I added a static 'armored' inner-plate to the Diamond/Lattice silhouettes for DEFENSE legibility (consistent with Filament's income-node), but it broke THREE precise geometry-lock tests (diamond closed-quad 1-moveTo/4-lineTo, diamond single-color 1-stroke, lattice 3-strokes). That churn was the signal: the addition fought the established visual contract, was speculative VFX I couldn't visually verify pre-playtest, and the real 'it resisted' moment needs a transient clang (blocked by world.effects being host-local-only) not a static plate. I reverted it cleanly (git diff vs the P1 commit confirmed byte-identical) and logged armored-glint + resist-clang as DEFENSE-v2/G4. LESSON: when a small render 'tell' breaks multiple exact-count geometry tests, treat the churn as a smell — rely on the existing distinct silhouette for identity and defer the juice to a playtest-gated pass; the Filament node was worth it (1 test added, 0 broken), the diamond armor was not (3 broken).
- P2 #refute-first-CHECK-needs-supervisor-triage-against-the-execution-model: GROK-ANALYST (refute-first) returned a CRITICAL + a HIGH determinism/charge finding that LOOKED serious (order-dependent scoring desync; TOCTOU negative charges). All four were refuted by checking the ACTUAL execution model, not the abstract code: scoring is HOST-ONLY (clients mirror, never recompute), bond direction is SERIALIZED (so lookupCombo is identical everywhere), and the reducer is synchronous single-threaded with readonly placerColor/type (no interleaving, no mid-tick mutation). LESSON: an adversarial refute-first reviewer WILL produce plausible-but-wrong CRITICALs; the Supervisor must triage each against where-the-code-runs (host vs client vs replay) + the concurrency model + which fields are readonly, and FILTER with a concrete refutation — but the same pass also gave positive confirmation (Grok's attacks on the real risks, no-indestructible + float-order, both failed).
- AUDIT #the-final-audit-earned-its-keep-on-the-test-NET-not-the-code: the ultracode 6-dim final audit confirmed the shipped code CLEAN (SHIP) but found a real MED gap one layer out — the byte-equal replay-determinism harness (save.replay.test.ts runStress) never called tickScoring and only ever did a cause:'physics' sever, so a Date.now/Math.random crept into the new S90 scoring path would have stayed GREEN (the auditor proved it with an injection). The bespoke run-twice scoring test I'd added was a near-tautology (pure fn over commutative integer counts) that gave false confidence. FIX: wire tickScoring into runStress so the scoring float-multiply lands in the compared snapshot. LESSON: a determinism GUARD only guards the reducer paths its stress script actually exercises — when you add a new per-tick reducer (scoring/physics force/charge), extend the shared replay harness to call it, or the guard silently doesn't cover the new code; and a 'deterministic' unit test over a pure integer fn is near-tautological — the real determinism net is the serialize-replay-compare harness.
- AUDIT #ship-doesnt-mean-skip-the-doc-drift-sweep: verdict was SHIP, but the audit's anti-hallucination + Rule-22 dims surfaced that the BACKLOG roadmap still described 4 SHIPPED behaviors as forward work (incl. a false 'Vortex never built') and pinned scoring.ts:90 (P1 shifted it to :94). None touched runtime, but left unfixed they mislead the next session into re-building shipped work. LESSON: a passing audit verdict still warrants the doc-drift sweep — mark shipped behaviors shipped in the roadmap THIS session (strike+commit-tag), de-pin brittle file:line cites to symbol anchors, and keep SESSION HISTORY current; doc-drift compounds silently across sessions.

## 2026-06-16 — Session 89: 5 playtest regression fixes (lobby READY overlap+tick, HUD badge plate, poop-foul auto-expiry, hunter +25%, client render-delay netcode buffer) + G1b Vortex magic-combo behavior + ultracode 8-reviewer final adversarial audit — Full tier 2-round Council (Grok-4.20+Gemini-2.5-pro), 7 commits (de2f05d/9eeac55/4df76b1/43b4c0c/de1d1fd/f425167/6432391), vitest 1385→1407, tsc 0, bundle 546.7KiB<550, P5 E2E 2-browser GREEN; adversarial CHECK on the 2 novel mechanisms + final audit caught+fixed 3 real issues pre-ship

- P1 #surface-state-that-already-exists-dont-rebuild-it: the per-seat READY tick the user asked for needed ZERO new wire/sync — SeatPresence.ready was synced since S87 P4 but never rendered. One optional SeatView field + a lobbyView map + a seatRack tick. LESSON: before adding a feature, grep the data model for a field that already carries it; the gap may be render-only, not protocol.
- P3 #a-bug-report-can-be-a-design-decision-surface-it: the user-reported "stayed white" was partly a DELIBERATE mechanic (poop-foul = income-zero until you fly onto the splat to wipe). I did NOT blindly rip it out — ground-truthed it as intended, ran the Council (split: keep+cue vs auto-expiry), synthesized C (30s auto-expiry + manual-wipe-instant + fade cue) honoring user intent AND preserving the tempo depth. LESSON: when a reported bug is intended behavior, surface the tension + let deliberation + user-intent resolve it; never unilaterally gut a mechanic, never dismiss a user report as "working as intended."
- P3 #reuse-the-existing-expiry-rail-no-new-field: foul auto-expiry needed ZERO new synced field — Poop.landedAtTick already existed AND round-tripped (SPLAT_GROUND already used landedAtTick+TTL). One else-if mirroring that pattern + a call to the already-audited applyCleanPoop (which even handles the gone-anchor orphan). LESSON: before adding a timer field, check whether an existing landed/created tick + a new constant already expresses the window.
- P5 #zero-jitter-buffer-was-the-bug-not-missing-interpolation: the joiner choppiness wasn't "no interpolation" — it existed, but the lerp window EQUALLED the 100ms snapshot interval = zero buffer, so any late packet saturated t=1 → freeze-then-jump. Council proved that just WIDENING the window doesn't help (a late snapshot resets currentSnapTime, t=0, only shifting the freeze). Fix = render-delay clock + multi-snapshot buffer + bracket-lerp (render the past, where a newer snapshot reliably exists). LESSON: snapshot interpolation needs a render clock lagging real-time by >1 interval interpolating the bracketing pair; a single prev/current pair with window==interval is a structural freeze machine.
- P5 #adversarial-check-caught-a-real-ship-blocker: the Triumvirate CHECK (Grok break-it) found that extending interpolation to ALL moving entities SMEARS the ones with discontinuous transitions — a poop FALLING→SPLAT snaps its pos onto the struck structure (lerp slides the splat mid-air); a grabbed potato teleports to the carrier. Narrowed coverage to continuous movers. Unit tests passed but did NOT model the FALLING→SPLAT transition. LESSON: interpolation is only safe for CONTINUOUS-position entities; teleport/snap-mid-life ones must be excluded or state-gated — adversarial CHECK on high-risk netcode pays for itself.
- P6 #host-only-is-a-desync-guard-by-construction + #float-sums-need-a-canonical-order: the Vortex pull mutates spark positions, but because stepPhysics is host/solo-gated (clients mirror via snapshot, never simulate), the client CANNOT double-apply → the desync risk is closed structurally by WHERE it runs. The residual risk is replay-reproducibility: the multi-anchor float SUM depended on Map insertion order (IEEE-754 non-associativity) — hardened by sorting anchors by bond id (canonical order, the seagull lowest-id convention). LESSON: any float accumulation over a collection in a deterministic sim must iterate id-sorted, and determinism tests MUST include the multi-element case (my first tests only did single-anchor; the CHECK exposed it).
- P7 #ultracode-final-audit-catches-the-over-correction: the reactive P5 narrowing (drop creatures+hazards to kill the smear) OVER-dropped creatures (voltkin), which DO move continuously while SEEKING and DO ride the client mirror — the S27 "client creatures stay empty" comment was stale (NetSnapshot v2 made it false). The 8-reviewer audit caught it as a Council-fidelity MED; fix = state-gated creature interp (SEEKING-only). LESSON: a reactive narrowing fix can swing too far — re-audit the binding coverage list after any scope-narrowing, and verify stale "X is empty/disabled" comments against the CURRENT wire.
- SESSION #ultracode-audit-the-claims-and-the-docs-not-just-the-code: the final audit independently verified every check_method claim + every verification[] needle + ran tsc/vitest/bundle, AND separately diffed DOCS (LOCKED_DECISIONS §13.3/§13.8, neighboring invariant comments) against the new reality — catching 3 doc-drift items (stale netcode design, stale hunter-speed comments) that code-only review misses. LESSON: a shipped change must update the locked-decision doc + neighboring comments referencing the changed numbers; a final audit should explicitly diff docs/comments, not just code.

## 2026-06-15 — Session 88: G3a in-match discovery toast (magic-12) + per-match counter + roadmap audit-error correction — Standard Council 1-round UNANIMOUS, PRIME-AUDIT DROPPED G1a (magic already +2.0/8x since S76), 4 commits, vitest 1385/1385, bundle 544.4KiB<550; also fixed the recurring MCV false-UNBOUND advisory (project-local verify-watch-roots.json)

- P1 #verify-the-roadmap-against-the-code-before-building (Rule 21/20): the user-mandated roadmap's G1a premise ("isMagical is dead / magic earns no premium") was FALSE — scoring.ts:90 reads isMagical and a magic bond has earned +2.0 vs functional +0.25 (8x, uncapped) since S76, with a passing test. Coding the literal roadmap item (+0.75) would have been a -62% NERF. A state-discovery probe (read the actual scoring + combos source) BEFORE drafting caught it; the gap table had conflated "magic combos have no BEHAVIORS" (true, = G1b) with "magic earns no SCORING premium" (false). LESSON: an audit/roadmap claim about code is a hypothesis — ground-truth it with Read+grep before designing on it, even when it's user-authored.

- P1 #hook-at-the-confluence-not-per-path: combo-discovery detection needed to cover THREE bond-creation paths in placePrimitive (primary + redundancy + merge-sweep) PLUS the PLACE_FROM_FREE reducer — but placeFromFree delegates to placePrimitive, and all three run before the single BOND_FORMED emit. So ONE placement-level hook keyed off a captured firstNewBondId watermark (bonds id>=watermark) covers everything, vs N fragile per-path hooks. Reused the rainbowSwitchTick (synced-tick + pure-pose, additive-optional, overwrite=restart) AND fouledPrimitives (Set field: init in makeWorld, sorted-serialize, clear on START_GAME/RETURN_TO_TITLE) precedents verbatim — no protocol bump.

- P1 #preview-cannot-always-drive-pixi-input (process): the live-preview VISUAL of the toast/counter was not capturable — Pixi v8's federated event system did not react to synthetic DOM PointerEvents on the canvas (gameState stayed TITLE), and force-mutating world.gameState='PLAYING' on an uninitialized TITLE world WEDGED the render loop (screenshot timed out; recovered by reload). The S87 harness-contamination lesson recurred. LESSON: when the live visual isn't reachable, lean on (a) pure-pose unit tests for render math + (b) clean-boot + console-error checks for render WIRING, and REPORT the gap honestly rather than claim a screenshot that wasn't taken. Don't force engine state the real flow initializes.

- P2 #correct-the-source-of-truth-when-it-lies: when a PRIME-AUDIT overturns a roadmap/audit claim, fixing the code is not enough — strike the wrong claim FROM the source of truth (BACKLOG.md roadmap) the same session, with the empirical correction + evidence, so the next session doesn't rebuild on the false premise. Struck the gap-table row, marked G1a DROPPED with the nerf rationale, marked G3a SHIPPED.

## 2026-06-13 — Session 87: USER-QUEUED mode batch — VS-BOTS (1-6 bots, NOOB/MID/HARD/IMBA, play via real dispatch) + Multiplayer rename + Quick Match (discovery election + all-ready gate) — Full tier Council 1-round, 4 commits, CI-GREEN E2E+Deploy, live-preview bot-play verified

- P1 #canary-tests-pay-rent #unit-conventions-bite: the S63 palette canary fired EXACTLY as designed when the 7th seat color landed — it pointed at the duplicated e2e literal that had to move in the same commit. Separately the bundle charter was nearly mis-enforced twice in one session because prior sessions recorded KiB while vite prints kB (548.3 KiB == 561.45 kB): ALWAYS name the unit when recording a gate number, and when a gate reads >2% off baseline, rebuild the BASELINE commit in a worktree before assuming regression.

- P2 #bots-are-remote-players #choke-point-dividends: implementing bots as ordinary seated players that may only dispatch CLIENT_INTENT-shaped actions meant every S84-S86 exploit fix (bench gate, poop arrival gates, reach validation, territory blocks) bound the bots ON DAY ONE with zero bot-specific enforcement code — the S86 dispatch-entry choke point paid its first structural dividend. The one crash class was the OPPOSITE direction: reducers that THROW on caller bugs (fsmPickup CarryViolation) are lethal to an autonomous actor, so the controller needs claim-outcome confirmation + self-heal, mirroring what controls.ts does for humans.

- P3 #reuse-the-human-path-helpers: the bot redundancy weaving is the human LMB-up path verbatim (componentOf + pickRedundantBondTargets + same constants) — when an AI actor needs a capability a human has, importing the human path pure helpers is both cheaper AND provably consistent (same geometry, same caps), vs re-deriving bot-flavored variants that drift.

- P4 #split-the-eager-from-the-lazy-at-the-import-boundary: a new feature whose UI is eager (the lobby quickmatch buttons) breached the bundle charter by ~4 KiB. The fix was NOT to trim the feature but to find pre-existing eager weight that should have been lazy all along — CodexOverlay was eager ONLY because the always-eager godlyOrchestration imported a tiny localStorage helper (unlockGodly) from the same module as the heavy Pixi class. Extracting the helper to codexStore.ts let the 12 kB overlay become a lazy chunk, recovering 3x the headroom needed. LESSON: when an eager addition breaches charter, audit the import graph for heavy modules pulled eager by a TINY shared symbol — splitting that symbol is cheaper + cleaner than shrinking the new feature. Also: the Trystero-importing discovery had to be split from the ready-gate helpers (quickmatch.ts vs quickmatchGate.ts) so hostHandlers could use the gate eagerly without dragging Trystero into index.

- P5 #a-version-bump-ripples-into-every-hardcoded-literal: PROTOCOL_VERSION 7->8 + a 7th PLAYER_COLOR broke 5 tests across unit (2 protocol pins, 1 palette canary, 1 netHardening hello-builder) AND e2e (2 protocol-mismatch version literals, 1 nplayer 6-color assertion) — none caught by tsc, all by running the suites. The palette canary FIRED EXACTLY as the S63 author intended (it exists to force the e2e literal to move in lockstep). LESSON: a wire-version or palette-cardinality change is a cross-cutting edit — grep ALL test files (unit AND e2e) for the old literal in the same commit, and expect the e2e lane (not just vitest) to surface the rest. The e2e protocol-mismatch tests specifically encode current/older/newer RELATIVE to PROTOCOL_VERSION, so a bump always moves the newer-peer literal to PROTOCOL_VERSION+1.

- P5 #verify-attribution-in-vivo-not-just-in-vitro #harness-pointer-artifacts: the live preview briefly showed the human seat (0) accumulating prims it never built — alarming (the S84 non-builder-win class). Root cause was NOT a bug: synthesized button-clicks + a cursor parked in the play area + the preview harness generating pointer events drove real human builds. A clean-room reload (start match, then ONLY read-only evals, cursor left off-board) proved byOwner={1:1} with the human at 0 — bots attribute to their own seats, the human builds only on real input. LESSON: when a live-preview anomaly looks like a known bug class, do a clean-room re-test that eliminates harness-generated input BEFORE concluding it is real; manual preview pokes (injected entities, synthesized clicks, parked cursor) contaminate attribution.

## 2026-06-12 — Session 86: playtest round-6 REGRESSION batch (fog black + LOCK, hazard-ring origin lines, central bench gate + claim-gated drag, spark-IS-the-pointer) + BACKLOG→ROADMAP rewrite on user mandate — Standard tier Council 1-round, 8 commits, CI-GREEN + deployed, CHECK Triumvirate SHIP (Grok 4/4 refuted)

- P1 #user-tuning-outranks-design-notes #lock-with-a-test-not-a-comment: S85 restored the 'designed' dim fog tier over the explicit S63 user tuning because the old design note read as a carry-forward while the user decision read as a mere knob — and the round-6 playtest caught it as a regression ('the stupid blue fog is back'). A documented decision provably failed to protect itself within 22 sessions; the fix layers LOCKED_DECISIONS §14 (rule: old design notes NEVER outrank newer user tuning; restores need a fresh same-session user ask) with constants.lock.test.ts, a CI tripwire whose failure message says 'you are overriding a USER decision'. LESSON: user-tuned values get a test-enforced lock at the moment of tuning, not a comment.

- P2 #unit-test-the-drawing-not-just-the-math: the S85 hazard ring shipped with green unit tests (pure segment/alpha helpers) while the actual Graphics path drew a line from the WORLD ORIGIN to every ringed hazard — canvas-path semantics connect the pen to arc() start, and a freshly-cleared Graphics pen sits at (0,0). The user's screenshots were the detection. LESSON: when a feature's output surface is a Graphics path, pure-math helper tests are necessary but NOT sufficient — verify at the path/screenshot level (moveTo per dash; live screenshot now in the loop).

- P3 #enforce-at-the-choke-point #the-gesture-is-a-verb-too: two consecutive sessions gated VERBS (S84 claim, S85 build) while the exploit lived in (a) the continuous DRAG gesture — a gate-rejected claim left the spark Free and the ungated AttractDrag lerp hauled it at full cursor speed forever — and (b) the bench dimension, which had NO reducer enforcement at all (input-layer onDown only; the catch force-drop even handed the eaten player their spark back into the live drag). Fixes: bench policy enforced ONCE at dispatch() entry via an explicit per-intent allow/deny map test-locked against CLIENT_INTENT_TYPES (a new verb cannot ship unclassified), and gesture ENTRY keyed on the CLAIM OUTCOME (dispatch is synchronous locally on host AND joiner via PREDICTABLE_ACTIONS — no local radius mirror to desync). LESSON: enumerate ALL consumers of a resource INCLUDING continuous gestures, and enforce actor-state locks at the dispatch choke point, not per-verb.

- P4 #the-framework-owns-the-canvas-style #pipe-masking-recursed: Pixi v8's EventSystem re-applies cursorStyles[mode] on every pointer interaction, so the naive canvas.style.cursor='none' was silently clobbered by a button-hover 'pointer' — the durable switch is Pixi's OWN cursorStyles.default, and ONLY the live preview pass could reveal it. Separately the S85 pipe lesson RECURSED: 'tsc | head' returned the pipe's exit code, the P3 commit claimed 'tsc clean' for a tree whose NEW test file had 3 type errors (esbuild-vitest is type-blind), and CI's deploy build went RED until the P4 commit fixed them. LESSON: gates run unpiped with explicit exit capture — violated twice now, so check_method MUST quote the captured exit code, not assert cleanliness.

- P5 #ground-roadmaps-in-code-audits #label-idea-origins: the user's frustration ('the whole point is being a geometric builder game and we have least focused on that') converted into an unarguable roadmap with three greps: 24 of 36 combos are literal 'Functional placeholder' entries (incl. the user-named Dot→Square and Line→Circle), isMagical has ZERO production consumers (magic bonds score the same as placeholders), areaMultiplier is dead data. A roadmap argued from measured gaps beats one argued from vibes; labeling every item USER-ASK vs CLAUDE/COUNCIL-SUGGESTED with a PARKED-until-sign-off rule structurally prevents the backlog from refilling with self-generated infra ahead of the user's game.
