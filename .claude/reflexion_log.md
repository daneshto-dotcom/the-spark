# Reflexion Log — The Spark

Cross-session learnings, newest first. Each `## DATE — Session N` block holds that session's reflexion entries. Pruned to ≤50 entries; older entries live in the archived handoff docs.

## 2026-06-01 - Session 66: CI GitHub Actions node20->node24 major bump (user-directed, Micro)

- P1 #ci-node24-bump #grok-caught-hidden-file-default #boot-then-smoke-prod-deploy #a0-gh-release-authoritative: bumped all 5 GitHub Actions off deprecated node20 to node24-native majors (checkout v4->v6, setup-node v4->v6, upload-artifact v4->v7, upload-pages-artifact v3->v5, deploy-pages v4->v5) across deploy.yml + e2e.yml. KEY (Grok earned its keep again, cf S65 x2): A.0 via `gh release list` was authoritative over the stale handoff - it named only 3 actions, UNDER-counted by missing the upload-pages-artifact/deploy-pages coupled pair, and understated the gaps (implied ~v4->v5, reality up to v4->v7). Per-major changelog review cleared every breaking change vs our usage. GROK-ANALYST FIX-THEN-SHIP: confirmed 4/5 claims but flagged upload-pages-artifact v4+ SILENTLY excludes hidden files by default (v3 included them) -> added include-hidden-files:true (behavior-preserving). PRIME-AUDIT of Grok (don't rubber-stamp the reviewer): its 'Pages-via-Actions runs Jekyll' claim was WRONG (conflates legacy branch path with the Actions artifact path), and dist/ empirically has 0 dotfiles (NOT a live regression) - but the fix is still right as behavior-preservation + future-proofing; its SHA-pin suggestion was real but OUT-OF-SCOPE (Rule16) -> logged, not silently adopted. LESSONS: (a) A.0 empirical version-probe (gh release list) beats handoff claims - the handoff was both incomplete AND understated; (b) for a version BUMP, neutralize the action's CHANGED DEFAULTS (include-hidden-files), don't just swap the number; (c) staged-verify a prod-touching CI change - a PR exercises the pull_request lane with ZERO prod impact, then merge runs deploy + curl-smoke the LIVE site (200 + fresh Last-Modified + exact asset hash); (d) the [DELIBERATION GATE] 'ptrue' advisory is a COSMETIC non-numeric-id artifact (hooks expect numeric ids per json_helpers.sh:376); the real blocking gate keys on deliberation_completed+unlock_source which were set (confirmed by edits succeeding). EMPIRICAL: e2e PR SUCCESS (23/1 gating + upload@v7), deploy SUCCESS, e2e-master SUCCESS, live site HTTP 200 serving index-BQNJ5y21.js, build 509.37KB. checkpoint b643c42.

- SESSION #s66-method #grok-earned-keep-4th #boot-then-smoke-prod #a0-beats-handoff #cosmetic-gate-reconciled: single Micro priority (user-directed, approved x2) - bumped all 5 GitHub Actions node20->node24 across deploy.yml + e2e.yml + a Grok-caught include-hidden-files:true behavior-preservation fix. DEFINING: (1) A.0 via `gh release list` beat the stale handoff which UNDER-counted (3 named actions, missed the pages coupled pair) AND understated gaps (+3 majors on upload-artifact). (2) GROK-ANALYST FIX-THEN-SHIP for the 4th session running - caught the upload-pages-artifact v4+ hidden-file-exclusion default-change; PRIME-AUDIT rejected Grok's wrong Jekyll claim + deferred its out-of-scope SHA-pin suggestion. (3) staged prod-safe verify: PR exercises the e2e pull_request lane (zero prod), then merge runs deploy + curl-smoke the LIVE site (200, fresh Last-Modified, exact asset hash index-BQNJ5y21.js). (4) reconciled a cosmetic [DELIBERATION GATE] 'ptrue' advisory to the non-numeric-id hook quirk (json_helpers.sh:376) - non-blocking, write-gate keys on deliberation_completed+unlock_source; silenced via a self-documenting deliberation-record file. FINAL: e2e PR + deploy + e2e-master ALL SUCCESS, live site verified, build 509.37KB, PR #1 merged + branch pruned (local+remote). Grok 1 call (~$0.10), Gemini 0. GREEN throughout (226K/1M, 22.6%). checkpoint b643c42.

## 2026-06-01 - Session 65: lobby colour landmines + JOIN mode-guard + CI flake-harden quarantine (user-directed batch)

- P1 #lobby-colour-landmine-fix #fresh-attempt-resets-all-session-state #grok-check-caught-incomplete-fix: fixed the 2 S64-logged lobby status-colour landmines in the pure reducer - (#1) invalid room code now renders error-red not neutral grey (JOIN_ATTEMPT invalid -> statusColor=ERROR); (#2) HOST_START + valid JOIN_ATTEMPT now reset ALL session state (grey + unlatch + beginVisible/hostConnected false) so a stale error-red cannot bleed onto a neutral status and a stale errorLatched cannot block Begin. KEY (Grok earned its keep AGAIN, cf S64 colour divergence): my first fix reset only colour+latch; the GROK-ANALYST break-it lens found HOST_START still spread stale beginVisible/hostConnected=true after a host+peer+ERROR retry -> a Begin button on a fresh 0-peer room; my own errorLatched-clear UNMASKED this pre-existing latent state (previously frozen behind the latch). FIXED by resetting all session-scoped fields on a fresh attempt (incoherent tuple unrepresentable); Grok round-2 verified clean. LESSON: a PARTIAL fresh-start reset is itself a divergence - clear ALL prior-state fields or the unmasked remainder is worse than the original landmine. A.0 reachability probe (input display:none outside select) proved the residual cross-pane invalid-JOIN glitch degenerate+pre-existing -> logged as carry-forward (mode-guard), NOT papered over in the PEER_STATUS hot path. EMPIRICAL: tsc, 988 unit (+3), full Playwright 24/1, build 509.31KB. checkpoint d6b00ae.

- P2 #join-attempt-mode-guard #defense-in-depth-reducer+shell #close-p1-check-residual: closed the P1 CHECK residual (invalid JOIN_ATTEMPT while mode!=select painting a wrong-mode red status) with a mode-guard. A.0 confirmed reachability: the dimmed joinButton stays eventMode=static (clickable) while hosting/joining though the HTML input is display:none outside select, so a button-click was the only non-select JOIN vector. FIX: reducer guard (mode!==select -> return state, same-ref no-op) + shell attemptJoin early-return. KEY: behaviour-exact for ALL reachable flows since the UI only dispatches JOIN from select - a pure safety addition (47 ins/0 del). Grok break-it found CLEAN (3 attacks held); the dual-guard is redundant but harmless defense-in-depth (the reducer guard self-protects against any FUTURE direct dispatch e.g. network/auto-rejoin). LESSON: when a UI affordance is visually disabled (dimmed) but still event-live, guard at the STATE layer (reducer), not just the visual layer. EMPIRICAL: tsc, 992 unit (+4), Playwright 24/1, build 509.37KB. checkpoint 3f5c33b.

- P3 #ci-flake-harden #non-gating-quarantine-lane #grok-caught-report-collision+fail-unsafe-grep: quarantined the recurring 4-peer real-WebRTC nplayer CI-timeout flake to a non-gating lane so gating-CI-red == real regression. A.0 grounded it: retries=2 already existed (insufficient), deterministic-transport rejected by prior Council (config C9 - real P2P IS the surface), e2e.yml already deploy-independent -> the lever is a non-gating split. FIX: e2e.yml Run-E2E split into gating (--grep-invert @quarantine-flaky) + non-gating (continue-on-error+if:always --grep @quarantine-flaky). KEY (Grok earned its keep a 3rd time): r1 found TWO issues my YAML-only first cut missed - (1) both lanes wrote the same report dir, so the non-gating lane (runs 2nd via if:always) OVERWROTE the gating artifact -> on a real regression the uploaded report would be the flaky test's; fixed via --output=test-results-quarantine + --reporter=list. (3) grepping the SEMANTIC title was fail-UNSAFE (a future test sharing the phrase would silently non-gate); fixed via an explicit @quarantine-flaky tag (fail-SAFE: new tests gate by default). r2 + 5 attacks held. The FIRST real-CI run PROVED it: 4-peer flaked in the non-gating lane (exit 1) but the JOB stayed GREEN (gating 23/1). LESSONS: (a) run-but-non-gating REQUIRES splitting CI invocations (Playwright fails the run on any failed test regardless of tags); (b) boot-then-smoke a CI change - run the exact commands + watch the real run, static parse != runtime; (c) prefer fail-SAFE opt-in tags; (d) isolate per-lane output so a non-gating lane cannot corrupt the gating artifact. checkpoint cb0393c.

- SESSION #s65-method #grok-earned-keep-x2 #boot-then-smoke-ci #fail-safe-defaults: user-directed batch (P1 lobby colour landmines, P2 JOIN mode-guard closing the P1 residual, P3 CI flake-harden) - 3/3 shipped + pushed (d6b00ae/3f5c33b/cb0393c), all Micro user-path, RALPH+GROK-ANALYST CHECK each. DEFINING: (1) the adversarial GROK-ANALYST CHECK earned its keep TWICE - P1 (incomplete fresh-reset: beginVisible/hostConnected left stale) + P3 (report-collision + fail-unsafe semantic grep); both FIX-THEN-SHIP with an r2 confirmation round. A partial fix (reset SOME fields, grep a SEMANTIC name) is itself a divergence. (2) BOOT-THEN-SMOKE for the CI change: ran the EXACT npm-passthrough commands AND watched the first real-CI run - it PROVED the design (4-peer flaked non-gating, exit 1, JOB still GREEN, gating 23/1). (3) A.0 grounded every approach in real state (retries=2 already, C9 rejects mock, deploy-independent, joinButton event-live-while-dimmed). FINAL: tsc clean, 992 unit, full Playwright 24/1, build 509.37KB, P1+P2+P3 E2E+Deploy all green in real CI, gating-CI-red == real regression. Grok 5 calls (~$0.15), Gemini 0. GREEN throughout (202K->317K, 31.7%).

## 2026-06-01 - Session 64: LobbyStateMachine extraction + lobby behavioral net-extension + knip de-export (autonomous full-batch, user-AFK)

- P1 #lobby-state-machine-extract #pure-reducer-same-ref-churn-guard #adversarial-check-caught-color-divergence: extracted the lobby mode/Begin-gating/status/latch logic out of the LobbyScreen Pixi class into a PURE reducer (initialLobbyState/lobbyReduce/lobbyView) under the S63 P3 e2e net - closing the S61-flagged zero-unit-coverage gap (the SM had NO unit tests; now 28 deterministic, no Pixi/WebGL). KEY DESIGN (Council DP4 synthesis): the reducer returns the SAME state reference on a no-op so the shell if(next!==state) gate preserves the original per-frame churn guard AND kills hot-path allocation - one functional pattern solving churn + GC + diff-strategy. KEY CATCH: the adversarial Triumvirate CHECK earned its keep AGAIN (cf S63 N=6 false-pass) - GROK-ANALYST's break-it lens found a real behavior DIVERGENCE that RALPH (spec) and GEMINI (quality 5/5) both missed: the reducer reset statusColor to grey on HOST_START/valid-JOIN, but the original only ever wrote statusText.style.fill in setErrorMessage/reset, so a stale error-red CARRIED onto the next neutral status - unreachable by the tested e2e orders. FIXED by removing the color reset (carry the prior fill, behavior-exact) + 3 witness tests. LESSON: in a behavior-PRESERVING refactor a 'cleaner' value-reset the original did NOT do IS a divergence - preserve exactly + log the latent UX bug as a carry-forward, do not silently improve.

- P2 #lobby-net-extension #gamestate-driven-visibility #a0-killed-stale-carryforwards: closed the S63 P3 CHECK net-extension carry-forward with 3 e2e cases (pane-visibility via Back-to-Title, joinPane-tap-focus, Enter-submit) + a destroy() DOM-cleanup case (end-of-session CHECK gap). KEY: my first pane-visibility test manually called setVisible(false) and FAILED - main.ts:746 re-asserts lobbyScreen.setVisible(gameState==='LOBBY') EVERY FRAME, reverting it next rAF; fixed by driving the REAL gameState transition (RETURN_TO_TITLE). Lesson: a value the render loop re-derives per-frame cannot be poked from outside - test via the upstream cause (gameState), not the symptom. KEY (A.0 as hero): the State-Discovery gate killed 3 stale carry-forwards before code - vite/vitest CVE (npm audit=0 vulns), countdown-mode (nonexistent), audio-SFX-bug (wired via drainAudioEffects). Lesson: empirically re-probe carry-forwards each session; stale handoff claims accumulate.

- P3 #knip-de-export-conservative #noUnusedLocals-as-safety-oracle #council-deferred-risky-deletes: reduced knip unused-exports 33->28 by de-exporting 5 module-internal-only fns (audio SFX + duckMusic via drainAudioEffects; readNetDiagnostics via waitForRejected). KEY: the S64 Council (Grok, risk authority) CHALLENGED the broader knip purge for an unattended flawless run - save/protocol/serialization exports have dynamic usage tsc+tests+e2e silently miss, so DELETING risks a runtime break no guard catches; resolved de-export-only + DEFER deletes. KEY (elegant): tsc noUnusedLocals is the de-export SAFETY ORACLE - a de-export of a truly-dead fn ERRORS, so green tsc PROVES each de-exported fn is still used internally. Lesson: autonomous cleanup prefers signature-only de-exports (compiler-guarded) over deletes (un-guarded vs dynamic usage). A 'dead export' = wired-but-over-exported (de-export) | genuinely-dead (defer delete) | public-API (keep) - triage the category first.

- SESSION #s64-method #a0-retires-stale-carryforwards #diverse-lens-check #compiler-as-safety-oracle: autonomous full-batch (P1 LobbyStateMachine extract + P2 lobby behavioral e2e + P3 knip de-export + 1 CHECK-fix), all shipped + pushed (2c77b74/12f598e/f58c640/fa0283c). FINAL: tsc clean, unit 956->985, full Playwright 20/1->24/1, build 509.16KB, knip 33->28. DEFINING: (1) the A.0 State-Discovery gate RETIRED 3 stale carry-forwards before any code (CVE=npm-audit-0, countdown=nonexistent, audio-bug=wired) - stale handoff carry-forwards accumulate, re-probe empirically. (2) diverse-lens adversarial CHECK caught a P1 color divergence the spec+quality lenses missed (cf S63 N=6 false-pass) - assign DISTINCT refutation lenses. (3) tsc noUnusedLocals = de-export safety oracle. (4) Council risk-authority de-scoped a 46-item knip purge to 5 safe de-exports (deletes = dynamic-usage traps). (5) Rule 22 CI audit caught the P1 E2E 4-peer-nplayer flake + proved it transient (P2 green on same code) not a regression - recurring flake logged for attended hardening. Grok end-of-session sweep NONE/SHIP; Gemini completeness 4/5 -> destroy() gap FIXED.

## 2026-05-31 - Session 63: fog->black + N-player 4/5/6 verification + lobby-construction net (autonomous, user-AFK batch)

- P1 #fog-feel-tuning-live-user-eye #e2e-literal-sync-on-render-constant: the long-carried fog-feel tuning (waiting since S57 for the live user eye) resolved in one Micro pass once the user gave concrete direction during the 3-tab smoke: FOG_COLOR + MEMORY_FOG_COLOR to 0x000000 (pure black = bg, kill the blue tint) + R_PERSONAL 150->75 (halve the spark reveal). KEY GOTCHA (the standing e2e-outside-tsconfig warning bit exactly here): e2e/fog.spec.ts HARDCODES the fog color literal {r:5,g:7,b:13} and pixel-asserts it; tsc stayed green but Playwright would have silently failed; had to sweep + sync the literal to {0,0,0} and RUN playwright. The e2e became the runtime arbiter: test 1 extracts the mask pixel and now asserts concealed=(0,0,0), proving fog renders pure black AND stays opaque (concealment intact), stronger than a screenshot for a color question. Lesson: a render-constant change must sweep e2e specs for hardcoded literals of that constant; when an e2e already pixel-extracts what you changed, updating its literal makes it self-prove the new value. Also: preview_screenshot hangs on a continuously-rendering Pixi/WebGL canvas (eval stays instant), so rely on pixel-sample eval + e2e for objective proof and the user live look for aesthetic.

- P2 #nplayer-verify-two-layer #extract-pixels-render-arbiter #adversarial-check-extracts-real-flaw: verified FFA to 6 in two layers - deterministic unit (applyStartGame N=4/5/6 + palette guard, the seating LOGIC) + runtime e2e (4-peer real-WebRTC netcode incl green@seat3, + N=6 single-page deterministic RENDER proof). KEY: Council DROPPED flaky 6-peer real (15 channels/swiftshader) for a deterministic N=6 render proof; the adversarial CHECK (Grok FAIL) then caught that my N=6 proof was a FALSE PASS (world-state + no-pageerror does not prove the renderer DREW the 6 colors) - FIXED by adding app.renderer.extract.pixels(stage) asserting all 6 palette colors appear on the canvas (same arbiter e2e/fog.spec uses). Lesson: for un-unit-testable Pixi, a screenshot is human-only; the CI-grade render proof is a pixel-extract color-presence assertion. Also: hardcoded e2e color literals (e2e cannot import src) need a src-pinning unit canary so a palette change fails loud.

- P3 #lobby-construction-net #behavior-net-over-internals #cover-before-refactor: built the Playwright net the S61 Council required BEFORE the deferred 548-LOC lobbyScreen refactor - single-page deterministic coverage of the high-risk state machine (DOM input + sanitiser wiring, host->room-code, Begin-Match gating via updatePeerStatus, reset clears). KEY: a BEHAVIOR net (assert observable surfaces via __SPARK__ accessors + DOM) is the correct refactor guard - you refactor internals, the net catches behavior breakage - so Grok-ANALYST CONDITIONAL constructor-not-exercised was REJECTED (the constructor IS exercised via its outputs; reaching LOBBY + clickable HOST proves wiring). The real multi-peer Begin trigger is covered by the 3/4-peer e2e, so driving updatePeerStatus directly here is correct layering not tautology. Lesson: cover-then-refactor for an un-unit-testable Pixi/DOM component = a deterministic single-page e2e over the public accessors; log the uncovered modes (pane-switching, joining/error, listener cleanup) as the refactor session net-extension scope.

- SESSION #method #worked #autonomy-pivot: on AFK+autonomy, defer EYE-DEPENDENT items (visual tweaks/refactor, CVD aesthetics) as logged carry-forwards and execute ONLY the verification-safe core; the absent-user adversarial Council/CHECK stands in for the missing eye (S61 precedent re-validated; Grok ANALYZE 8.7/10 - better to ship zero eye-dependent than low-confidence).

- SESSION #method #worked #pixel-arbiter-not-screenshot: for un-unit-testable Pixi render, the CI-grade proof = app.renderer.extract.pixels color-presence assertion + a src-pinning unit canary for the duplicated e2e literals + N-run flake-stability; a human-only screenshot is NOT a CI proof (the adversarial CHECK caught a screenshot-only N=6 render as a FALSE PASS). Institutionalize.

- SESSION #method #improve #council-cadence: full Council R1 on pure deterministic TEST-hardening added serial latency for mechanical work (Grok ANALYZE); reserve full Council for behavioral/architectural shifts, use a lighter PRIME-AUDIT + adversarial-CHECK lane for test-only batches. Tier rules are global so log as observation, not a unilateral change.

## 2026-05-31 — Session 62: 2→N-player multiplayer (1v1v1 FFA shipped + runtime-verified)

- P1 #nplayer-seat-assignment-host-authoritative #determinism-ordered-roster-not-map: extended 2->N players by un-hardcoding plumbing (mechanics were already FFA-safe/owner-agnostic - 5 Explore agents found ZERO binary-opponent code). KEY: cross-client determinism comes from the host shipping an ORDERED seat->color roster ARRAY (not a Map - iteration order can diverge) in START_GAME_SIGNAL + every client INSERTING players in seat order. Host freezes peerId<->seat BEFORE broadcasting START so any post-signal INTENT is already stamp-able (no pre-ack buffer needed - ordering resolves Grok's race). Anti-spoof: host stamps INTENT.playerId from sender peerId->seat (intentStamp.ts), so a client can only drive its own seat. Trystero selfId (re-exported via transport) lets each client find its own roster entry. Lesson: generalizing N from 2, the win-condition (max-scan) + per-player Maps usually ALREADY generalize; the real work is seat IDENTITY + the count-hardcoded gates.

- P2 #anti-bloat-isNetworked-predicate #radial-spawn-pure-fn-reproduces-binary: replaced ~22 scattered gameMode==='1v1' with isNetworked(world)=gameMode!=='solo' - behavior-identical today but explicit + future-proof, and the per-site AUDIT caught the ONE site that must NOT blanket-swap (connection-lost: peerCount===0 is host-correct but a 3+-player client losing only the host needs explicit host-presence detection - logged P5 gap). radialSpawnPos(seat,total) pure fn REPRODUCES the historical 2-player left/right EXACTLY at N=2 (seat 0 always angle pi=left) so zero regression + all seats equidistant from the contested spawner = FFA-fair. Lesson: a generalized formula that provably reduces to the special case at N=2 beats a branch; Math.round kills cross-client float drift.

- P3 #minimal-cosmetic-over-refactor #coverage-by-e2e-not-unit: the N-seat lobby was 90% met by P1 (Trystero room accepts N peers; begin-button latches on first peer) - the only real gap was the one-shot 'Player 2 connected' TEXT. Did the MINIMAL fix (live count text, no constructor refactor) not the full 548-LOC lobbyScreen refactor the Council deferred to P5. The touched method (updatePeerStatus) is untestable in jsdom (needs Pixi) but the multi-peer begin flow it drives is covered by the P4 3-peer e2e at runtime. Lesson: when a 'task' is mostly already-working + the rest is cosmetic, do the cosmetic minimum + let the integration test (not a unit test of an un-unit-testable Pixi method) be the net.

- P4 #3-peer-e2e-real-webrtc-arbiter #protocol-bump-breaks-version-e2e: the 3-peer FFA test (3 real Trystero/Nostr contexts) is the definitive proof 1v1v1 works - distinct seats, cross-client color determinism (seat 2=yellow), FFA win. Built trivially on the existing N-page helpers (hostNewRoom/joinRoom/readWorldState/waitForWorld) - the harness was already N-ready. KEY GOTCHA: bumping PROTOCOL_VERSION 3->4 SILENTLY breaks the version-mismatch e2e (they hardcode 'v3' local + a 'v4 newer peer' that's no longer newer); these live in e2e/ which is OUTSIDE tsc -b, so tsc stayed clean while the tests would fail - caught only by RUNNING playwright (the standing 'e2e not in tsconfig -> run Playwright' gotcha). Lesson: any PROTOCOL_VERSION bump MUST sweep e2e/*.spec for hardcoded version literals AND run the version-mismatch specs.

- P5 #nplayer-leaderboard-hud-pool #screenshot-as-pixi-arbiter: replaced the hardcoded 2-row RED/BLUE HUD with a pooled MAX_PLAYERS Text leaderboard ranked by score, per-player color, local marked '<YOU' (Gemini quality lift: answers who's-winning + who-am-I in one glance). Pool pattern (create N hidden Text in ctor; show/position/sort the live subset per frame) is the clean way to do dynamic-count Pixi text w/o per-frame alloc. KEY: ui.ts (Pixi) has NO jsdom unit test, so the RUNTIME ARBITER for a HUD change is a SCREENSHOT - added one line to the existing 3-peer e2e (await page.screenshot) + Read the PNG to visually confirm 3 colored rows. Lesson: for un-unit-testable render code, the integration e2e + a screenshot artifact IS the verification; tsc+build only prove it compiles, not that it looks right.

- SESSION #s62-2-to-n-player-recon-first #council-hardened-determinism: shipped 2->N-player FFA (verified 1v1v1) in 5 priorities. The whole effort hinged on RECON FIRST - 5 parallel Explore agents found the game mechanics were ALREADY FFA-safe (owner-agnostic, ZERO binary-opponent code) + colors centralized + win=max-scan, so the 'big feature' was un-hardcoding seat IDENTITY + ~22 count-baked gates. Full-tier Council caught the load-bearing determinism fix (ordered-roster ARRAY not Map) + the ONE isNetworked site that must NOT blanket-swap (connection-lost peerCount===0, host-correct w/ a logged 3+p client-host-loss gap). Lesson: before scoping an N-from-2 generalization as a big rewrite, send parallel readers to find what already generalizes - per-player Maps + max-scan win usually do; the work is identity + the literal-2 gates. Every priority empirically gated (tsc + 946 unit + build + 16 e2e incl a NEW 3-peer real-WebRTC arbiter) before commit.

## 2026-05-31 — Session 61: autonomous §XV batch (severBond extract + reducer→render guard + worldTypes split); pivoted from non-autonomous fog PDR

- P1 #anti-bloat-orchestrator-extract-typeonly-no-cycle #prime-audit-refute-emitter-myth: extracted world.ts's last inline dispatch orchestrator (SEVER_BOND, 43 LOC) into severBond.ts; dispatch() now uniform 1-line delegations. Cycle Grok feared avoided by importing World/GameAction TYPE-ONLY (mirroring disruptionManager) + replacing the lone runtime symbol requirePlayer with an inline guarded lookup that is PROVABLY EQUIVALENT (computeBaseCharge is the existence-guarantor: returns >0 only after a successful world.players.get, so requirePlayer's throw was unreachable when chargeToConsume>0). PRIME-AUDIT refuted Grok's DEFER via a code probe: world.effects is a plain GameEffect[] so push(BOND_SEVERED) is an append NOT a listener call (no re-entrant dispatch), and computeBaseCharge + the charge line are adjacent synchronous reads (no interleave). Lesson: when a reviewer asserts a closure/emission/timing hazard in a reducer, verify whether the "event" is actually a data-array append (no side effect) before deferring; a 1-line non-verbatim change is safe IF you can name the invariant guaranteeing the dropped branch is unreachable AND lock it with a test. +6 charge-consume lock tests cover exactly where save.replay (final-state-only) is blind.

- P2 #import-boundary-guard-with-teeth #council-harden-transitive-not-shallow: added a determinism guard walking world.ts's transitive RUNTIME import graph asserting src/render/ unreachable. Both Council models independently flagged the FIRST design (shallow file-text-mentions-render + filename allowlist) as false-confidence: misses transitive (state→util→render) + dynamic import() coupling, and a rename breaks the allowlist. HARDENED: full-graph DFS following static+re-export+dynamic+side-effect edges, type-only ignored (erased), NO allowlist — the one known state→render file (godlyOrchestration) is structurally out of scope (it CALLS dispatch, downstream, not imported by it). KEY LESSON: a guard test that always passes is WORSE than none (false confidence), so every static-analysis guard MUST ship a POSITIVE CONTROL proving it detects the thing it guards against — here, scanning godlyOrchestration DOES flag render. Otherwise a broken parser returns [] and every assertion goes green. Transferable to any assert-X-never-happens lint/test.

- P3 #anti-bloat-type-logic-split-facade #programmatic-block-move-zero-transcription: split world.ts's 143-LOC World interface (+GameState/GameMode) into a pure-type worldTypes.ts, keeping the dispatch seam + GameAction in world.ts, with a type-only re-export facade so ~20 consumers stayed unchanged. world.ts 445→286. Two transferable techniques: (1) for a LARGE contiguous block MOVE, do it PROGRAMMATICALLY (Node marker-based byte-exact slice + sanity asserts + write both files) instead of a hand-typed giant Edit old_string — eliminates transcription error on 150 lines and self-verifies (World-def removed from src, present in dst, re-export added). (2) a type-only relocation is provably runtime-safe under verbatimModuleSyntax (tsc enforces no-emit for type-only import/export), and the prod-bundle byte-size is the empirical arbiter that nothing leaked into runtime JS (here unchanged: 507.12 KB). Grok-analyst tie-broke the boundary: keep GameAction in world.ts (moving it entangles worldTypes with every reducer sub-action type); use the DIRECT export-type-from re-export (canonical verbatimModuleSyntax idiom). Lesson: when an interface references only LEAF domain types it's a clean cut to a types module; when it would pull in sibling-module types, leave it.

- SESSION #s61-pivot-non-autonomous-pdr-to-safe-batch: the user approved a fog-feel-tuning PDR then left ~4h saying "work autonomously, highest quality, I approve the batch." But that PDR's OWN arbiter was "your live 2-peer eye" — executing it autonomously = low-quality guessing at aesthetic constants, AGAINST the highest-quality mandate. Correct call (S60 precedent): pivot to the handoff's autonomous-SAFE §XV set, preserve the fog tuning as a LOGGED carry-forward (never silently dropped), and run a real Council as the absent-user adversarial check. Lesson: "I approve the batch + autonomous run" authorizes high-quality autonomous WORK, not the literal execution of a PDR whose arbiter is the absent human; when the approved scope is non-autonomous, substitute the nearest autonomous-safe high-value work and log the deferral with its reason — don't execute the un-executable badly, and don't idle. 3/3 priorities shipped CI-green with empirical gates (tsc+unit+e2e+build) before every commit; world.ts 488→286 across P1+P3.

## 2026-05-31 — Session 60: fog P2/P3 (structure-memory + polish) + autonomous §XV P4/P5 (lobbyGeometry + godlyActions)

- P2 #memory-fog-cpu-gate-not-gpu-mask #pixel-e2e-caught-mask-attenuation: the S59-designed 'memoryLayer masked by Sprite(maskRT)' was empirically WRONG -- a Pixi v8 sprite mask attenuates the masked content by the mask's BRIGHTNESS, and the fog mask is near-black (MEMORY_FOG_COLOR 0x161b2e), so it crushed the ghost silhouettes to ~5% alpha (measured g=35 vs expected ~121 in the pixel e2e). Pivoted to a CPU per-sprite visibility gate (sprite.visible = !isPointVisible(sources, ghost.pos)), reusing the SAME vision predicate as the memory update -- simpler, no brightness coupling, and the GPU mask wasn't viable anyway (a fog shader is Council-vetoed in this codebase). Lesson: a render-compositing assumption from static design -- even a Council+PRIME-AUDIT-ratified 'sprite.mask=alpha' -- must be runtime-pixel-verified; 'sprite.mask=alpha' is FALSE for a dark mask (it's brightness-weighted). RALPH:PATROL separately caught a missing match-start ghost reset (match 2 would inherit match 1 ghosts). PRIME-AUDIT empirically REFUTED both GROK REALs (placedBy-immutable grep) + downgraded a GEMINI design-pref. Transferable: when a reviewer OR your own design asserts GPU-compositing behavior, the pixel e2e is the arbiter, not the reasoning.

- P3 #fog-polish-reuse-existing-state-edge: the win-lift FREEZE is a 1-line guard (if target===0 return) placed AFTER the alpha/visibility assignment but BEFORE the throttled recompose -> fog+ghosts fade via alpha over a FROZEN mask composition (uniform dissolve), and it self-releases because target>0 on the next PLAYING edge (no stuck-frozen risk; the win test still completes to alpha 0). reset() reused the EXISTING main.ts:680 *->TITLE teardown edge (already abort-cutscene/reset-shake/clear-creatures) rather than instrumenting all 3 RETURN_TO_TITLE dispatch sites -> one idempotent call covers lobby-Back + POSTGAME->title + peer-drop. Lesson: when adding teardown/reset for a NEW subsystem, find the existing state-transition edge that already orchestrates sibling teardown and co-locate there (single idempotent site) instead of touching every dispatch caller. Each polish item earned a dedicated e2e proof (freeze: a vision hole doesn't relocate mid-lift; reset: rememberedCount->0) -- runtime-verifying GPU/state behavior, not just tsc.

- P4 #anti-bloat-extract-with-reexport-facade: extracted lobbyScreen.ts's pure geometry/validation helpers (+ their module-local layout constants, which Council R1 Grok correctly flagged as coupled) into lobbyGeometry.ts, and RE-EXPORTED the public API from lobbyScreen.ts so external consumers (controls.ts) + the 29-test spec stayed UNCHANGED -- minimal blast radius (2 files), tsc as the exhaustive consumer-check (it caught INPUT_CANVAS_* beyond my grep window). Lesson: for an anti-bloat extraction with external consumers, a re-export facade beats hunting every import site; move module-local constants WITH the functions they feed (one-directional import) to avoid a reverse cycle. 712->548 LOC, behavior verbatim-preserved (tsc + 920 unit + lobby smoke canary).

- P5 #anti-bloat-reducer-extract-cycle-refuted: extracted world.ts's inline GODLY cinematic-state cluster into godlyActions.ts (gameMode.ts pattern). Council R1 Grok raised a BLOCKER (godlyActions<->world cycle via setCooldown); PRIME-AUDIT empirically REFUTED it pre-lock with a 1-grep check -- setCooldown is DEFINED in godlyCooldown.ts:25 and merely IMPORTED by world.ts:55, so godlyActions imports it directly (+ World type-only) with no runtime cycle, as Gemini predicted. Lesson: a reviewer's cycle/coupling BLOCKER that rests on WHERE a symbol lives is a 10-second import-graph check, not a reason to defer scope. Determinism preserved by VERBATIM bodies + exact mutation order; save.replay.test.ts is the determinism arbiter for any reducer touch.

## 2026-05-30 — Session 58: 4 user fog/netcode fixes + E2E gate repair (5 priorities, all shipped)

- **S58 #manual-context-tests-bypass-shared-seam** — The S57 fog `__FOG_DISABLE__` e2e seam was added inside `open2Peers`, but the 3 tests that build Playwright contexts by hand (Sym I + 2 protocol-mismatch — they need per-context initscripts BEFORE `newPage`, which `open2Peers` cannot inject) silently bypassed it → fog stayed ON under swiftshader → sim ~halved → Sym I red on master for the whole S57→S58 gap, while the S57 handoff/CHECK called it a "pre-existing flake, not fog." Lesson: a guard added to ONE setup path does not cover sibling paths that opt out of that path; when a seam exists but the symptom persists, enumerate ALL construction sites (grep `browser.newContext`), not just the shared helper. Same bug-class as S56 (a preserve mechanism wrapped around a value that was never produced). Two meta-lessons: (1) trust the CI signal over handoff prose — `gh run list` showed E2E went red EXACTLY at the fog commit (green S51-S56); (2) the OneDrive working-dir path intermittently garbled `cat`/`tail`/`grep` output (showed 9 passed when 11 ran) → use a JSON reporter parsed in node for garble-proof ground truth before committing a CHECK.

- **S58 #constant-tuning-from-playtest** — halved `R_PERSONAL` 300→150 on user playtest feedback ("visibility around the spark too large, twice smaller"). The S57 vision unit tests assert via the `R_PERSONAL` symbol rather than a literal, so the tuning produced zero test churn — a small payoff of the S57 decision to push vision math into a pure unit-tested module with symbolic constants.

- **S58 #dormant-netcode-already-wired-prime-audit-win** — P4 (spawner pickup claim) Council R1: Grok proposed a NEW 30Hz carrier→host cursor stream for relaying the held-spark position; Gemini proposed reusing the synced cursor. PRIME-AUDIT (read `gameMode.ts` + an Explore A.0 of the net send path) REFUTED both as incomplete: the mechanism ALREADY EXISTS — `UPDATE_AVATAR_POS` ships joiner→host @10Hz and `applyUpdateAvatarPos` (gameMode.ts:191, "S45 Sym A") already couples a Carried spark.pos to the carrier avatarPos, which rides the existing 10Hz snapshot. So P4 needed ZERO new position netcode. Lesson: when models propose new infra for a netcode gap, A.0-probe the existing send/apply path FIRST — this codebase has a strong "build it, then leave it dormant" pattern. Also: a Council "determinism trap" warning for an additive wire field was empirically refuted by the byte-exact `save.replay` pass (the determinism test compares two HOST snapshots that both carry the real field; the client-side `??0` rehydrate is never on that path).

- **S58 #re-activate-dont-rebuild-the-riskiest-path** — P4 reversed the S52 atomic-place decision in THE most regression-prone area (S52 glued-spark + S56 frozen-drag both bled here). The winning move was ZERO reducer changes: `PICKUP_SPARK`/`DROP_SPARK`/the Carrying FSM/carry-follow/carrier-color render/auto-cancel were ALL still present and tested — S52 only stopped CALLING `PICKUP_SPARK` from input. So the fix re-orchestrated existing actions from `controls.ts` (claim on LMB-down, DROP-before-PLACE on LMB-up as the guaranteed release, DROP on lost-capture) and left the proven atomic `PLACE_FROM_FREE` pristine. Lesson: when re-introducing a previously-removed mechanic, prefer re-activating dormant-but-tested machinery over rebuilding; the regression risk is in the ORCHESTRATION (every exit path must release), not the reducers. Two test traps: (1) the S56 dragLock-preserve test used `carrierId=0` to mean "grabbed by someone else" but 0 == the client localPlayerId, so broadening preserve to Carried-by-me flipped it — corrected to `carrierId=1` + added a Carried-by-me test; (2) `e2e/` is NOT in tsconfig (`include:[src]`), so a wrong-arity helper call is invisible to `tsc -b` — only caught by actually running playwright.

- **S58 #carry-forward-host-ttl-release-backstop** — P4 spawner-claim shipped WITHOUT the Council-D3-recommended 5s host-side TTL auto-release for a Carried spark. Deliberate scope cut: the existing reconciliation (spark persists in freeSparks; host re-validates on next place/grab; joiner mine-guard ends the drag when a snapshot shows the spark non-Free-and-not-mine) covered the full unit+e2e+Sym J matrix, and adding a host tick-loop timer was extra surface in the codebase area that regressed in BOTH S52 and S56. GROK-ANALYST post-ship retrospective flagged the lossy-release edge (lost `DROP_SPARK` under heavy WebRTC packet loss) as the one residual risk. Implement a host-tick TTL (despawn-style poll: if spark Carried and now-claimTick > TTL_TICKS → dispatch DROP) IF the user lossy 2-peer smoke shows any stuck-carry. Until then it is a logged, bounded, non-blocking gap.

- **SESSION #s58-four-user-fixes-plus-red-gate-repair** — Shipped all 4 user asks + repaired a silently-red E2E gate in one batch (user "Approved"). P0 red-gate (the bypassed fog seam). P1 vision radius 300→150. P2 Voltkin 480→1200 ticks (2.5×). P3 own-creature fog vision (new `ownerPlayerId` wire field, first client consumer of creature ownership). P4 (capstone) spawner pickup claim via re-activating the dormant Carried path with zero reducer changes + guaranteed DROP-before-PLACE release. 3-way Council on P3/P4; PRIME-AUDIT refuted both the "need new cursor netcode" and "determinism trap" claims. Final: 899 unit/0 fail, e2e 10 pass/1 skip incl new Sym J, bundle 503.57KB<550, CI green, deployed live. Survived a mid-session PC reboot (recovered cleanly). Git boundary verified: The Spark is a standalone repo, zero foreign paths tracked, no shared-repo collision with siblings. Tooling notes: OneDrive path garbles terminal text output + a state-rewrite hook races the Edit tool → used Node JSON reporters + atomic read-modify-write scripts as ground truth.

## 2026-05-30 — Session 57: Fog of War MVP shipped (P1) + S56 git block resolved + deployed live

- **S57 #fog-render-pass-throttle-the-low-frequency-mask** — Fog of War MVP shipped (commit `b09ac25`): a client-side RenderTexture `'erase'`-mask — opaque dark base minus soft radial-gradient cutouts at the local player's vision sources (personal radius @ LIVE cursor + per-own-primitive beacons + always-visible spawner), composed offscreen each frame and shown over the world layer; WIN lifts it via an alpha tween. Two non-obvious lessons: (1) RUNTIME-VERIFY the GPU path, not just `tsc` — the two Council models CONFLICTED on the Pixi v8 blend-mode id (`BlendMode.ERASE` vs `BLEND_MODES.DST_OUT`); the truth (string `'erase'`) came only from grepping `node_modules/pixi.js`, and the mask only proved correct via maskRT pixel-extraction (cursor=transparent, enemy=opaque FOG_COLOR `[5,7,13]`). Static parse ≠ runtime. (2) A per-frame full-screen render-to-texture pass nearly HALVED the sim under software WebGL (swiftshader CI) → the spawn-timing 2-peer smokes failed (Sym A: 0 sparks/10s). A/B (fog on→off flips it green) root-caused it. Fix = compose the LOW-FREQUENCY mask cheaply: half-res (0.5) + 20Hz throttle (every 3rd frame; the alpha tween stays per-frame so the win-lift is smooth) = ~12× less cost, a real win on mobile too; plus a `__FOG_DISABLE__` e2e seam so gameplay smokes (which test gameplay, not fog) are isolated from render-layer cost, fog itself covered by `e2e/fog.spec.ts`. Lesson: a new always-on render layer must be (a) runtime-pixel-verified and (b) frequency/resolution-budgeted — the swiftshader e2e harness is the canary for per-frame GPU cost that real-GPU dev never feels.

- **S57 #check-verify-audit-claims-against-source** — The post-ship Triumvirate CHECK returned a Gemini BLOCKER ("unbounded pool leak across matches") and a Grok HIGH ("world-vs-screen coord mismatch") — both DISSOLVED on PRIME-AUDIT against the real source: the pool is a correct bounded/reused object pool (plateaus ~≤52, surplus `visible=false`), and there is no camera (world==screen, already runtime-pixel-proven). Two more findings were artifacts of MY prompt summary (a "for..in loop" that's actually indexed; "no fog e2e" when `fog.spec` tests it enabled). Lesson: auditors reason from the brief you give them — a loose summary manufactures phantom findings, and the scariest severities are the ones most worth verifying line-by-line before acting. A CHECK that finds 0 confirmed bugs on already-runtime-verified work is a calibration signal that in-implementation verification was sufficient, not a failure of the CHECK.

- **SESSION #s57-fog-of-war-mvp-shipped** — Resolved the S56 git block first (billing fixed by user → pushed 7 commits, S56 P1 attract-drag deployed live, Deploy GREEN 47s). Then built Fog of War MVP (user's #1 ask): client-side cosmetic fog for 1v1; vision = personal radius @ live cursor + per-own-primitive beacons + always-visible spawner; enemy concealed until scouted; fog lifts on WIN. A.0 state-discovery via 4 parallel Explore agents de-risked it (no camera = world==screen coords; clients already hold full `world.primitives` → zero net messages; WIN wired via ENDGAME; vision constants pre-stubbed). 3-way Council (Grok+Gemini t0.3) CONVERGED Option A (RenderTexture mask), both VETO Option B GLSL (CinematicLumaKeyFilter silent-compile history). PRIME-AUDIT resolved 5 deltas incl. empirical Pixi v8 `'erase'` verification + refuted Gemini's v7 Graphics-gradient API. Shipped `src/state/vision.ts` (+19 tests) + `src/render/fogRenderer.ts` + `e2e/fog.spec.ts`; 875→894 unit + full e2e (10 pass/1 skip/1 flaky-recovered) green; bundle 499.99→502.69 KB (cap raised 500→550, closing S56 P2 carry-forward). Perf deviation (swiftshader) handled with half-res + 20Hz throttle + `__FOG_DISABLE__` test seam. Commit `b09ac25`. Context 36.6% GREEN. API ~$0.04.

## 2026-05-30 — Session 56: client AttractDrag parity fix (P1 shipped) + anti-bloat deferred (P2)

- **S56 #client-prediction-was-never-run-not-just-clobbered** — the Player-2 "dragged shape frozen at spawn then teleports" bug was a TWO-gap half-wired mechanism, mirroring S55's `errorLatched`. The S52 dragLock (skip the dragged spark in snapshot interpolation) was built to PRESERVE a client-side AttractDrag prediction — but the prediction was never PRODUCED: `applyPerSubstep` (stepAttractLerp) lived only inside `stepPhysics`, which is host/solo-gated, so the client never ran it (GAP1). And even once produced, `applySnapshotCore`'s clear+rebuild resets the dragged spark every 10Hz because the dragLock only guarded `interpolatePositions`, not the full apply (GAP2). Lesson: when a guard/preserve mechanism exists but the symptom persists, check whether the value it guards is actually being PRODUCED end-to-end — a preserve around a never-generated value is invisible to unit+tsc+Council; only the real-peer E2E proves the produce+preserve chain. Fix reused the host's exact `applyPerSubstep` (identical feel, zero dup) + completed the dragLock with a preserve/restore around `applyNetSnapshot`. Verified by a new 2-peer e2e (Sym G) asserting the held-drag spark tracks the cursor + 4 unit guards.

- **S56 #anti-bloat-extraction-breaches-the-cap-it-protects** — P2 (anti-bloat trim) DEFERRED after empirical discovery: the build sits at 499.99/500 KB and extraction ADDS ~0.3 KB module overhead (S19 precedent), so the planned trim would push the bundle OVER the soft cap it exists to protect. "Anti-bloat trim" conflates two metrics — per-file LOC (§XV, extraction helps) vs shipped bundle KB (only deletion helps). At-cap, extraction is counterproductive. Real headroom = a dedicated deletion/dependency pass when a feature needs it, or raise the soft cap. Lesson: verify which metric a "trim" actually moves before executing — the handoff's "trim main.ts for headroom" framing was a category error (LOC vs KB), surfaced at session start and confirmed at-cap here.

- **SESSION #s56-shipped-p1-attract-drag-parity-deferred-p2** — P1 (URGENT playtest bug) client AttractDrag self-view prediction SHIPPED + verified (commit `e51124e`; unit 871→875; new 2-peer e2e Sym G joiner-follow + full smoke 9pass/1skip; tsc clean; bundle 499.61→499.99 KB). Two-gap half-wired fix: the client never ran `applyPerSubstep` (stepPhysics host-gated) AND the S52 dragLock didn't shield `applySnapshotCore`'s 10Hz rebuild. 3-way Council (Grok+Gemini both REVISE→addressed); PRIME-AUDIT refuted 2 Grok claims (cadence-overshoot, prevPos-pop) with evidence. P2 (anti-bloat) DEFERRED — extraction would breach the at-cap bundle. NOT pushed: GitHub Actions billing still blocks CI/Deploy (user action pending) → P1 verified locally, not yet live. Context ~33% GREEN. API ~$0.04 (Grok 1 + Gemini 1).

- P1 #memory-fog-opaque-not-translucent #grok-refute-and-fix: remembered-areas = an OPAQUE dim colour overlay over the dark base (source-over keeps mask alpha=1 -> NO live-board leak); a partial-ERASE approach (my first draft) WOULD have leaked (translucent fog reveals the live board) -- caught in self-review pre-commit. Council ADOPT HYBRID (pure unit-testable grid for explored state, proven RT for live fog). GROK-ANALYST refuted a phantom bilinear-leak (opaque base) and caught a real centre-only coverage gap (-> circle-rect overlap). Lesson: in screen-space fog, 'dim' must be an opaque colour change, never an alpha reduction, or it leaks the board.

- SESSION S59 #method-opaque-not-translucent-fog: screen-space fog 'dim' tier MUST be an OPAQUE colour change over the dark base, never an alpha reduction (alpha reduction = translucent overlay = live-board leak, the rejected M1). source-over on opaque base keeps out.a=1 for any src alpha -> opaque dim. Caught my partial-ERASE draft pre-commit via blend math.
- SESSION S59 #method-prime-audit-refute-not-rubberstamp: PRIME-AUDIT independently refuted Gemini's inverted-layer recipe + Grok's phantom bilinear-leak (both dissolved on blend math), kept terrain-dim vs a false R2 consensus (literal ask + zero-ghost-risk), and a real centre-only grid-coverage bug got caught+fixed. State what falsifies each adopted/flagged item independently.
- SESSION S59 #method-checkpoint-under-degraded-tooling: tool-output channel degraded to delayed bursts mid-batch -> checkpoint at the nearest clean priority boundary + carry the riskiest remaining work (P2 ghost state machine) rather than grind through broken tooling. P1 shipped clean; P2/P3 carried with full Council design.
