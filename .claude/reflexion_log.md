# Reflexion Log — The Spark

Cross-session learnings, newest first. Each `## DATE — Session N` block holds that session's reflexion entries. Pruned to ≤50 entries; older entries live in the archived handoff docs.

## 2026-06-26 — Session 107: Batch A — the four S105/S106 carry-forwards. P0 stress.test perf-flake hardened (p95+catastrophic canary, restored 1664 baseline); P3 Voltkin dead-asset cleanup (13 files + dead consts, import-graph-verified); P4 bot self-breaks own pentagram FIXED at the auto-bond layer (collectSpawnerLockedPrimitiveIds excludes live-spawner rings; also protects human-built pentagrams); P1 anti-coast LEADER SCORE-DECAY (proportional, self-limiting, solo-exempt, amber HUD cue); P2 worker-sim FOUNDATION (stepPhysics replay-determinism HARD gate + hashWorldState oracle + WORKER_SIM_FOUNDATION.md audit/plan). vitest 1684/1684; entry 597.1/750; MCV exit 0 (14 assertions); P0-P1 deployed LIVE. Scoped via wf_d37331f2-37a (Haiku scope + Opus PRIME-AUDIT caught a 7x LOC undercount + a refuted worker-safe claim).

- P0 #a-flaky-perf-gate-erodes-the-suite-and-must-be-hardened-not-tolerated: the handoff said vitest 1664/1664 but a fresh run was 1663/1664 - stress.test.ts asserted the WORST single tick over 3600 ticks be <50ms, a machine-variant wall-clock gate that a lone 51.01ms GC/OS-scheduler spike fails. Fix = assert the p95 (immune to transient outliers, still catches a 5%+ systemic slowdown) + a worst<1000ms catastrophic-pathology canary (a true O(n^2)/NaN blowup is seconds/tick + also trips the 60s test timeout). LESSON: a perf assertion on a single MAX is inherently flaky; gate on a percentile + a far-looser catastrophic ceiling. Establish a trustworthy green baseline BEFORE editing sim code, or you can't tell a regression from pre-existing noise.
- P3 #dead-asset-cleanup-must-trace-the-WHOLE-import-graph-not-trust-the-scope: the Haiku scope said 'KEEP voltkinFrames.ts - 8 sim/test importers' but a full consumer grep showed it is ENTIRELY test-only post-S106 - currentSpriteUrl + currentAnimCell have ZERO production callers (the cited 'prod refs' in creature.ts/save.ts are doc-comments), and currentFrameKey/VoltkinFrameKey survive only because sync.test.ts uses them as a WIRE-DETERMINISM cross-check (host+client reconstruct the same frame key from synced fields). Kept the module for that test value, but deleted 13 dead asset files + the dead URL exports. TRAP avoided: deleting the 5 frame PNGs while VOLTKIN_FRAME_URLS still names them is safe ONLY because no test does readFileSync on them (they inspect the URL STRINGS) and no runtime path loads them - verified before deleting. Converted the on-disk anim.json drift-guard (which validated a now-deleted artifact) to assert the inline canonical manifest instead, preserving coverage without the disk dep. LESSON: 'dead-asset cleanup' = trace every exported symbol's real consumers (prod vs test, import vs comment) before deleting the asset OR pruning the code; a scope's KEEP/DELETE list is a hypothesis to verify, not a fact.
- P4 #fix-the-mechanism-not-the-distance-and-derive-state-dont-store-it: the bot self-broke its seeded pentagram because the host AUTO-MERGE sweep bonded its frontier into its own ring (degree 2->3 fails the recipe). The scope's first instinct (relocate the ring +100px) was REFUTED by the adversarial verifier as a band-aid (still reachable in ~4 hops). The robust fix attacks the MECHANISM: exclude any live spawner's locked-ring component from auto-bond candidacy. Chose to DERIVE the locked set from existing world.creatureSpawners + the bond graph (collectSpawnerLockedPrimitiveIds) rather than add a noAutoBond flag to Primitive -> zero new wire/save field, survives host-migration for free (re-derived per placement), and incidentally fixes the SAME latent self-break for a HUMAN building beside their own pentagram (applied at the merge-sweep chokepoint covering both remote + local paths). LESSON: when a 'just move it / just bump the constant' fix is offered, check whether it addresses the mechanism or only lowers the probability; and prefer deriving protective state from authoritative existing state over storing a new serialized flag.
- P1 #proportional-rubber-band-beats-flat-AND-the-existing-tests-pin-the-rate: anti-coast leader-decay shipped as PROPORTIONAL bleed = rate*(score-threshold) rather than the scoped FLAT rate, because proportional is self-limiting (zero at the threshold, floored there) so it can never hard-cap a deserved win - a builder whose income exceeds the decay at their score still climbs. The KEY constraint that set the rate: the existing WIN-driving tests accrue a structure to 786 THROUGH the decay zone, so any rate strong enough to be a real anti-coast (equilibrium complexity > the test's structure) STALLS them. Resolved by (a) decay is gated gameMode!='solo' and makeWorld defaults to solo, so MOST unit tests are decay-exempt by construction, and (b) the one non-solo WIN test (distributed-pipeline, complexity 6) was rebuilt to complexity 80 (> the ~39 equilibrium) so a committed builder still wins - which is exactly the intended new semantic. Visible cue per the S106 invisible-bug lesson: the own-bar already recedes but the gentle bleed is sub-threshold for the red drop-flash, so added an AMBER tint via a new progressBarFractions.ownDecaying flag. LESSON: when adding a score-mutating mechanic, the existing accrual/win tests ARE the calibration harness - run them to discover the rate's real-world constraint, and prefer a self-limiting (proportional, floored) shape so you never accidentally make the game unwinnable.
- P2 #the-honest-milestone-increment-is-the-verifiable-foundation-not-a-risky-half-cutover: 'smooth regardless of host' is a multi-session milestone; the 3-lens scope panel UNANIMOUSLY said the ?worker=1 cutover is not ship-quality landable in a 4-item batch (render-coupling + unmeasured pooling ROI + cross-context Math). Best-interests call = ship the SAFE critical-path foundation (a stepPhysics replay-determinism HARD gate that LOCKS the physics loop + a pure hashWorldState cross-check oracle + a determinism AUDIT proving worker-safety within a browser + a sequenced cutover plan) and carry the risky refactor forward with DOCUMENTED blockers - rather than ship a half-working cutover that could silently desync. KEY: the existing determinism gates all drove the reducer path; the part a worker actually runs (Verlet/bond/collision via stepPhysics) was UNGUARDED - so the highest-value safe deliverable was the test that proves IT deterministic. Also kept the state hash OFF the wire (no consumer yet = premature serialization). LESSON: for a big architectural milestone, the first session's deliverable is proof + plan (lock current behaviour with a test, audit the risk, sequence the phases), not a rushed cutover; and 'profile before optimizing' (S105) means SHIP THE PROBE/measurement before the optimization it justifies.

## 2026-06-25 — Session 106: make-the-game-better batch — +25% match length (630->786/tier 262 lockstep), chewer funny fangs (rect->poly triangles, original pencil), HELGA whole-screen range + slap-shockwave VFX, NONET point-loss VISIBLE (own-score bar + leader ghost + drop-flash) + harsher (loser ×0.4), Voltkin procedural Graphics rig replacing the bitmap looping-gif/square (new voltkinPose + creatureRenderer rewrite, death-watcher ported verbatim). vitest 1664/1664; entry 596.5/750; MCV exit 0 (10 assertions). Scoped via wf_3c8db072 (adversarial-verified).
- P1 #balance-anchors-bump-in-lockstep-or-the-invariant-test-catches-you: +25% match length = PHASE_1_WIN_SCORE 630->786, but SCORE_TIER_STEP (210) is NOT auto-derived — it's a sibling hardcoded const that MUST move in lockstep (->262) or scoring.test's exact-thirds invariant (786=3×262) fails. HUNTER_TRIGGER_SCORE IS auto-derived (floor(WIN×0.75)) so it rescaled free. Tests pin the SYMBOL not the literal, so they auto-track — except a fixed-iteration accrual loop must still reach the now-larger target (the post-S91 120k-tick cap had headroom). Pick a win value divisible by 3 to keep the tier thirds clean.
- P2 #match-the-shape-not-the-color (rainbow teeth): the chewer teeth 'looked bad' because they were g.rect BOXES while the rainbow's funny tooth is a downward g.poly TRIANGLE — funny = pointed, not the fill color. Restyled to 2 splayed triangular fangs but KEPT the chewer's ivory+graphite pencil palette (NOT the rainbow's flat white) per the no-franchise/original-style memory. TRAP: the test's GraphicsMock stubbed rect/circle/etc but not poly -> 'g.poly is not a function' on 3 tests; add poly to the mock + flip the teeth-count assertion rect->poly. When you swap a Pixi draw primitive, the render-test mock + its draw-call-count assertions move with it.
- P3 #a-screen-wide-melee-needs-a-reach-VFX-or-it-flails: giving HELGA whole-screen range was a 1-line sim-config change (PRINCESS_SLAP_RANGE -> ceil(hypot(CANVAS_W,CANVAS_H)), integer for determinism; targeting already picks nearest enemy so no targeting change) — but a melee slap across the map renders as a hand flailing in empty air while a star-burst pops on a distant victim. Added a render-only graphite slap-shockwave streak (hand->lastStrikePos, no-op when adjacent) mirroring the turret's drawBeam. The range test 'NO enemy in range stays IDLE' had its chewer at (900,900)=1131px which is now IN a 2203 range -> moved it beyond the diagonal. Range is sim -> integer + replay (fresh-run gate stays byte-identical).
- P4 #a-correct-reducer-can-still-be-an-invisible-bug (the display layer): the owner's 'losing NONET didn't cut my points in half' was NOT a reducer bug (winner ×2/loser ÷2 is correct + tested) — the HUD progress bar read world.scoreProgress=MAX-of-all-players=the LEADER, so when the friend's ×2 kept the shared bar near-full the owner's own ÷2 was invisible ON THAT BAR (the leaderboard + audio did surface it). Fix = bind the primary bar to the LOCAL player's own score (pure progressBarFractions helper, unit-pinned) + a leader ghost-tick + a red drop-flash; WIN gate + HUNTER still read max. Also harsher (loser 0.5->0.4). LESSON: when a 'broken' value is provably-correct in the reducer + tests, suspect the DISPLAY surface (which aggregate is it actually showing?).
- P5 #a-procedural-rig-replaces-a-bitmap-by-keeping-every-pure-helper-+-porting-the-watcher-verbatim: killing the Voltkin 'looping gif + square' (a Pixi.Sprite with per-tick texture swaps) = a full creatureRenderer rewrite to a Graphics rig + a new pure voltkinPose (mirrors helgaPose; boltCharge peaks at FIRE). The rewrite stayed SAFE by (a) keeping EVERY exported pure helper (computeCreatureScale/Tint/Alpha/Rotation, computeFacing, computeSpriteDelta, lerpHex) verbatim so their 55 tests passed untouched, (b) porting the S103#8 lightning-cloud death-watcher + zap-burst + velocity-estimator facing VERBATIM, (c) NOT deleting voltkin-zap.png/voltkinFrames.ts (Codex placeholder + 8 sim/test importers — verify the import graph BEFORE deleting an asset/module). arcFlash FIRE emit unchanged -> replay intact. Runtime-verify a can't-screenshot WebGL rig by INJECTING entities via the dev seam + checking console-error-free across frames (the rAF-await times out — that's the game loop saturating rAF, not a fault).
- META #scope-the-batch-with-an-adversarial-workflow-then-bank-trivial-wins-first: a 5-item batch (teeth/length/point-loss/HELGA/Voltkin) scoped via a profile->investigate->adversarial-verify workflow caught the NONET framing correction (the leaderboard+audio DO surface the halving — 'invisible' was only true for the shared bar) before I built on a wrong premise. Executing P1->P5 banked the trivial/micro wins (length, teeth) + deployed them mid-session so the owner could playtest while the large Voltkin rewrite proceeded. Held the riskiest economy lever (structure-loss clawback, overturns 'banked score is safe') as a flagged owner decision rather than rushing it into a big batch.
## 2026-06-25 — Session 105: quickmatch playtest fixes — fully-random+20%-faster spawns (host-only reseed, replay-safe), codex EXACT recipes + reveal-when-locked (the turret discoverability fix), host-smoothness step-1 (send-cadence decouple out of the physics drain + chewer render trim; worker-sim chosen as the scalable milestone), gnaw reliability (fire on ATTACKING entry + louder). Deploy 3b58366 SUCCESS LIVE; gating E2E green; vitest 1653/1653; entry 598/750 KiB. MCV exit 0 (8 binding assertions).
- P1 #the-randomness-bug-was-a-fixed-seed-not-a-bad-picker: 'spawns always come square/triangle/square' was NOT a broken picker — rngPick over ALL_SPARK_TYPES (6 shapes) was already random. The real cause was a hardcoded boot seed (main.ts SEED=0xc0ffee) that's never reseeded, so EVERY match replays the identical seeded sequence. Fix = reseed per match. KEY determinism insight: the spawner is HOST-ONLY (client mirrors sparks via NetSnapshot, never ticks the spawner), so a per-match random reseed needs NO wire change / PROTOCOL bump / desync — always check WHERE a system runs (host vs both) before assuming a P2P change must touch the wire. Replay stays byte-identical because tests pin their own seeds and save captures rng WORDS via getState.
- P2 #reveal-the-recipe-not-the-reward: the 'turret didn't work with 4 spirals' report was pure discoverability — the recipe (1 Line + 7 Spirals) was already EXACT in the hint, but the codex hid it behind the build-it-once unlock gate ('??? / build it to reveal'), so you couldn't check requirements BEFORE building. Fix = show the recipeHint on LOCKED tiles too; keep only the name + character art as the build-once reveal. When a feature is 'undiscoverable', the gate is often the bug, not the content.
- P3 #profile-before-optimizing-the-host-gap: a 5-agent architecture judge-panel profiled the host per-frame cost and found the DOMINANT host-only cost is the 10Hz full-world serialize+JSON.stringify (no delta-encode), and it ran INSIDE the physics catch-up while-loop; the 0.05s accumulator clamp means an overloaded host DROPS sim time (no death-spiral). KEY: render cost is SHARED by both peers so optimizing it raises the floor but does NOT close the host/client GAP — the gap is host-only sim+serialize. Shipped determinism-NEUTRAL wins (move send to a wall-clock cadence out of the drain loop; trim chewer draw). The real 'smooth regardless of host' fix is the WORKER-SIM cutover (host becomes render-only == client; worker boundary == future dedicated-server boundary) — chosen as the scalable milestone, too risky to land big-bang alongside 4 other items on a live game. Sim-hot-path opts (collision 64x grid rebuild, Voltkin bond rescan) are determinism-sensitive → need new stepPhysics replay tests first (current replay tests dispatch reducers, not stepPhysics).
- P4 #not-broken-just-quiet: the 'chewers make no gnaw' report was NOT a broken synth or wire — playGnawSFX is a correct procedural rasp, the chewer holds ATTACKING 300 ticks and the gate fired ~5x/chew on the host, AND state+ticksInState ARE wired to the client mirror (chewProgress correctly stripped). The gnaw was just MISSABLE: started 1s into the chew (bucket>=1) at 0.2 gain, lost under music + positional distance attenuation (listener at canvas centre). Fix = fire on ATTACKING entry (bucket>=0) + raise gain. Verify the WHOLE chain (synth -> FSM -> wire -> mixing/volume) before assuming the logic is broken; sometimes correct code is simply inaudible.
- META #the-judge-panel-earned-its-keep + supervisor-cross-read: running the architecture design as a profile->propose->judge workflow corrected my prior (I'd assumed serialization-allocation; the profiler showed the synchronous JSON.stringify is the dominant half + the clone-alloc is secondary) and made the worker-vs-incremental tradeoff explicit with scores. I still cross-read the dominant-cost claim against the actual send site + accumulator clamp before acting. Pattern: delegate the broad profile, verify the load-bearing claim yourself.

## 2026-06-25 — Session 104: TD Playability + Unified In-Game Codex — shipped P1-P4 LIVE (Deploy SUCCESS): chewer continuous-production + audible chewing, vs-bots host-seeded enemy spawner (the turret now WORKS vs bots), unified 3-tab CODEX (G+C chord) + unlock-on-build + pentagram registration. commits 6f9f763/aabd4d6/b95cb9b, pushed → Deploy to GitHub Pages SUCCESS; tsc 0, vitest 1650/1650, entry 597.4/750 KiB. FULL-tier 3-lens Opus Council ADOPT-WITH-FIXES (M1-M13) + boot-smoke clean (G+C opens the codex, zero console errors). P5 G4 juice + the ghost build-hint deferred (YELLOW after the core landed).

- S104 P1 #finite-lifetime-is-an-FSM-change-not-a-config-tweak: the chewer despawn gate is `if (!config.persistent)`, so lowering lifetimeTicks alone is a NO-OP (despawnAtTick is never read for a persistent creature) — flip persistent:false to REUSE the proven Voltkin DESPAWNING→auto-delete FSM. Key render SFX off WIRED fields (state==='ATTACKING' + ticksInState) — chewProgress/sourceSpawnerId are STRIPPED from the client mirror, so a chewProgress-keyed gnaw is silent on the 1v1 joiner. Two tests pinned the OLD contract (towerDefense + voltkin-config) — find + invert them. The Council caught this no-op + both tests; I'd under-specified it as a config tweak.
- S104 P2 #host-seed-beats-a-scripted-bot-builder: to give a bot a named multi-primitive structure WITHOUT RNG-draw-order surgery on its single shared mulberry32 (any new draw shifts EVERY later draw + invalidates all vs-bots baselines + changes recorded-seed behavior), have the HOST deterministically PLACE it at START_GAME via pure seat-angle math (zero RNG) and reuse the real recipe-ignition path. Place the FULL shape (re-validation tears down a partial) — which doubles as the player's raid counterplay. Cleared the seed in pure-bot-decision test fixtures.
- S104 P3 #merge-overlays-but-prove-it-via-the-build: verify a lazy-overlay merge by the BUILD output (ONE ~15.6 KiB codexOverlay chunk, the old comboCodexOverlay chunk GONE, entry stayed lazy) not just tsc. unlock-on-build is a render-loop localStorage mirror of world.creatureSpawners/defenders — uniform host+client, idempotent; the determinism invariant is unlock is WRITE-ONLY from the sim's view (no reducer READS unlock state). A G+C chord is pure UI → main.ts (overlay owner), NEVER controls.ts (the sim-input FSM whose lock-guards would swallow it); edge-trigger + clear the pressed-set on blur/visibilitychange (stuck-key guard).
- S104 P4 #verify-the-registry-consumer-before-registering: registering the orphaned PENTAGRAM_RECIPE was safe ONLY because findSpawnerMatch (the registry consumer) is DEAD CODE and ignition uses findAllPentagramAnchors directly — so no double-fire. Always grep the consumer before assuming a registration is inert vs a double-ignition.
- S104 META #the-council-caught-a-blocker-i-missed: the Opus 3-lens deliberation (determinism/gameplay/architecture) found a real BLOCKER (the persistent-gate no-op + the two old-contract tests) PLUS the chewProgress-wire-strip correctness bug that would have shipped a client-silent chewing SFX. LESSON: run the adversarial determinism/wire lens even when confident — on a lockstep game it earns its keep.

## 2026-06-25 — Session 103: TD Defenders Bundle — shipped #8 Voltkin-vs-chewer + a generic Defender substrate + #9 laser turret + #10 HELGA articulated princess LIVE. commits c768549/3650e64/fe6ab29/712c2e9/56adeda/a29b272/eecbdef, pushed → deploys all SUCCESS; tsc 0, vitest 1645/1645 (was 1594), entry 594.3/750 KiB. FULL-tier batch PDR + 3-way Council ADOPT-WITH-FIXES (8 must-fixes) + 2 Triumvirate CHECKs (3 findings accepted, rest PRIME-AUDIT-rejected with evidence). P5 (Tier-1 G4) deferred under YELLOW.

- S103 P1 #the-opportunistic-override-stays-byte-identical-when-the-population-is-empty: #8 (Voltkin attacks chewers) looked like it'd disturb Voltkin's locked replay, but framing it as an OPPORTUNISTIC override (Council MF3 — bonds drive navigation, a creature is zapped only when ALREADY in attackRange) made every new branch reduce to the pre-existing expression when no enemy creatures exist: findNearestEnemyCreature returns null → targetCreatureId stays null → the FSM SEEKING→ATTACKING add, the wind-up-abort change, and the attack-fire guard all collapse to identical behavior. ALL 1594 prior tests stayed green with NO re-baseline (resolving Grok's 're-baseline violates the HARD gate' objection). LESSON: to add a behavior to a determinism-locked entity, gate the new code on a condition provably absent in the existing test corpus, then the old replays are byte-identical BY CONSTRUCTION + you add fresh coverage for the new path. The lightning-cloud shipped as a render-driven prev/curr-diff death-watcher (proven S102 goo pattern) — dormant until P3/P4 kill Voltkins — and I overruled BOTH reviewers' 10Hz-unreliability flag with evidence (DESPAWNING lasts ~60 ticks = ~10 snapshots, so kill-from-a-live-state is a reliable discriminator).
- S103 P2 #mirror-the-spawner-substrate-then-the-recipe-registry-already-has-the-seam: the generic Defender substrate (new synced entity + protocol bump) was far lower-risk than it looked because S100's spawner is a complete template — entity leaf + lifecycle reducers + Map<Id,Entity>+nextId + teardown sites + additive-optional sorted-id save + host-internal-not-client-intent protocol rows. Cloning that SHAPE got determinism/wire/save/teardown right on the first typecheck-green pass; the registry already had a 'cinematic'|'spawner' kind discriminator so a 3rd 'defender' kind slotted in. KEY WIRE PATTERN (MF1): a one-shot strike VFX on a PERSISTING entity can't be a render-diff (it doesn't vanish) NOR a raw-cadence derivation (10Hz misses the tick) — hold the FSM FIRE state ~12 ticks (≥2 snapshots) + SYNC it = the state IS the event bus; + a synced lastStrikePos so the beam keeps its endpoint after a one-shot kill. CHECK PRIME-AUDIT lesson: 4 of 5 auditor findings were FALSE-POSITIVES from not knowing two durations (Voltkin ATTACKING=60 ticks; FIRE/RECOVER exit on ticksInState not nextFireTick) — PRIME-AUDIT against actual FSM-state DURATIONS separates real from hallucinated. MF5 insta-fire-on-load: always re-phase any saved absolute-tick cadence field on load.
- S103 P3 #a-consumer-on-a-good-substrate-is-just-a-recipe-plus-a-renderer: with the substrate built+tested, #9 (laser turret) was ONLY a recipe predicate + a render — zero new sim, zero new determinism risk. The recipe reuses the pentagram componentOf-isolation idiom; it MUST registerRecipe at module tail (pentagram is the counter-example: imported as a side-effect but never registers, so it relies on a hardcoded scan — the defender path goes through the generic findDefenderMatches/getDefenderRecipe registry, which only works if registered). All wire-split thinking was paid down in P2, so the renderer just READS synced state (charge/rings from nextFireTick, beam from FIRE→lastStrikePos, SFX on the FIRE edge). LESSON: invest determinism/wire/protocol thinking ONCE in the substrate; each consumer is then a cheap recipe + renderer.
- S103 P4 #a-puppet-rig-IS-the-real-character-and-beats-veo-for-an-action-character: HELGA (the owner-critical 'real state-driven character, NOT a gif/twitch') shipped as a PURE Pixi puppet rig — helgaPose(state,ticksInState,phaseTick,offset) returns per-part transforms for a multi-limb body with hand-authored per-FSM-state keyframes. The S96 'PowerPoint spin' rejection was about twitching ONE flat sprite; an articulated rig rotating INDEPENDENT limbs through authored windup→impact→recover angles is a categorically different, OC3-sanctioned thing. Choosing the rig over veo frames was right for an ACTION character + an autonomous run: deterministic by construction (pose=f(...) → replay-safe + unit-testable: I asserted the slap arc + a no-NaN sweep), zero-bundle, zero-API. KEY CHECK CATCH (accepted): a pure pose fn on the GLOBAL phaseTick makes N instances animate in robotic UNISON — add a per-entity offset (the synced defenderId) to desync the idle ambient. RECIPE lesson: match the owner's combo SEMANTICS not lookupCombo direction — 'Star' = the unordered {Triangle,Circle} type-set (check leaf TYPE, not the direction-sensitive lookupCombo). And (both reviewers): a 'pure leaf = degree-1' gate silently fails to ignite under dense AUTO_BOND — gate on hub-degree + component-size + leaf-type-COUNTS (force the star by pigeonhole) and TOLERATE inter-leaf bonds. Rule 22 END-OF-SESSION audit caught the E2E fog aboveFogLayer child-count contract stale (10→13) from 3 new renderers while deploys were all green (deploy doesn't gate on E2E).

## 2026-06-24 — Session 102: TD combat overhaul — shipped 7 of 10 owner playtest items LIVE (P1 #4 buck-teeth/#5 owl-mask/#6 NONET sounds/#7 rebuild · P2 #2 gnaw/#3 melee + unified-HP foundation · P3 #1 RAID-pops-chewer + green-goo). commits 2fea955/e0d5abf/f228532, pushed → deploys 28120442098/28122820403/28124978894 all SUCCESS; tsc 0, vitest 1594/1594, entry 574.6/750 KiB. FULL-tier batch PDR + 3-way Council ADOPT-WITH-FIXES (6 must-fixes) + PRIME-AUDIT + owner corrections. Items 8/9/10 deferred (shared creature-targets-creature foundation).

- S102 P1 #verify-the-mask-on-the-real-frame-not-just-the-keyframe: the owner circled the bottom-left NONET owl (owl-b) as "too faded, missing his wings". Both owl KEYFRAMES looked fine — the fade was in the COMPOSITING MASK (owl-b's bespoke matte had a 39%-wide opaque core vs the shared spirit-mask's 69%, so the raised wing fell into the transparent rim). To fix a video-mask fade you must look at the ACTUAL VIDEO under the ACTUAL mask, not the still keyframe — a max-brightness PROJECTION across the loop revealed the "stray flame" it crops is an orange streak sweeping HORIZONTALLY across the lower body and OUT past the owl (invisible in any single early frame), so the fix is an ASYMMETRIC matte (shifted up, wide for wings, cropping the lower-edge extensions) — a symmetric widen would re-expose the flame. Verified without a screenshot: a composite preview (mask-alpha × real-frame = what Pixi renders) + a live served-asset alpha probe (right-wing pixel 0→255, corner 0). General: the WebGL screenshot tool TIMES OUT on this Pixi rAF game (harness limit, console clean) — eval pixel-probes + offline composites are the reliable masked-video verification path; a faded-sprite bug usually lives in the asset/mask generator, with bespoke per-asset masks (owl-b skipped in S99's shared fix) exactly where the regression hides.
- S102 P2 #touch-the-shared-reducer-once-and-let-suppression-cascade: the chewer "gnaw not lightning" (#2) + "move close" (#3) fixes were far smaller/lower-risk than the PDR feared. (a) ONE branch in the shared applyCreatureAttack (type==='chewer' → cause:'chewer' + suppress ARC_FLASH) — and suppressing ARC_FLASH AUTOMATICALLY drops the screen-shake (main.ts gates the shake on an ARC_FLASH emission this tick — no separate edit). (b) the "shoots from afar" bug was a HARDCODE: isWithinAttackRange used VOLTKIN_ATTACK_RANGE_SQ for ALL creatures, ignoring per-type config.attackRange, so the chewer's 120 never applied; routing through getCreatureConfig(type).attackRange + lowering chewer to 35 + skipping the legacy canvas-centre repulse fixed melee. LESSON: before adding fields/systems, grep for hardcoded constants the per-type config was SUPPOSED to drive — the seam is often already there, half-wired. De-scoped a separate Bond.hp field to the existing chewProgress counter (CONNECTOR_HP=CHEW_HITS) — identical single-chewer feel, far less risk; visible-damage + stacking logged as carry-forward. Determinism held: runChewerStress + the locked Voltkin replay stayed byte-identical.
- S102 P3 #drive-kill-VFX-from-the-snapshot-removal-not-a-wire-effect: the PDR planned a wire-synced CREATURE_SPLAT effect for the raid's green goo. The leaner+robust shipped design: damageCreature just removes the creature, and the chewer RENDERER's death-watcher diffs prev/curr world.creatures and splats goo + plays the fly-splat for ANY chewer that vanished from the SYNCED snapshot. Wins: reliable on host AND the 1v1 client (no transient-effect 1/6 sampling loss — the rainbow-yell trap); ZERO new wire/effect/save surface (only the RAID_CREATURE intent bumps protocol); fires for EVERY chewer death — raid, potato, next session's laser — free. LESSON: for "show something died" VFX in a host-authoritative snapshot game, derive it from the entity's REMOVAL at the render layer, not a per-kill wire event (guard on PLAYING). Also: the owner's "raid" was a VOCABULARY match, not a missing feature — "raid" = the existing right-click charge-sever; item 1 just extended its target set (pickCreature-before-pickBond) to enemy spawn.

## 2026-06-24 — Session 101: RECOVERY — shipped the S100 tower-defense slice LIVE (its deploy had silently hard-failed on the bundle gate) + verified end-to-end. commits 6169c2b (fix) / 6065c76 (close), pushed → deploy run 28109794663 SUCCESS; bundle charter 560→750, vitest 1584/1584 (incl 7 new real-pipeline buildability tests), entry 570.9/750 KiB, gating E2E green.

- S101 #shipped-pushed-but-NOT-live-was-a-failed-deploy-not-a-code-bug: owner said "I built two pentagrams in multiplayer and it didn't work — sure you pushed it and it works in all versions?" The feature WAS committed + pushed (git clean, HEAD==origin/master) — but the GitHub Pages DEPLOY had FAILED, so the LIVE site was a stale build with NO tower-defense at all. Root cause: check-bundle-size.mjs is a HARD gate (exit 1) that runs as the LAST step of `npm run build` — the exact command deploy.yml runs; S100's TD code put the entry chunk 10.9 KiB over the 560 charter → build exit 1 → deploy aborted → nothing published. The S100 session even SAW the breach but mislabeled it "soft budget, no hard CI gate" (it is a HARD gate) and still marked the priority done. Lessons: (a) "committed+pushed" ≠ "LIVE" — for a deploy-gated project a priority isn't done until `gh run list --workflow=deploy.yml` shows SUCCESS; (b) a CI gate's FAILURE MODE matters as much as what it guards — a cap that blocks the whole deploy costs more than the ~3 KiB-gzip it saved, and it's self-imposed, so RAISE it (560→750), don't get stuck debugging (owner: "who cares why confine ourselves"); (c) added an early-warning band (<60 KiB headroom) to the build + a memory so the next session catches it locally first; (d) never trust a "soft" label on a mechanically-enforced gate — read the script; (e) verification that exercises the REAL pipeline (placement+autobond+matcher) beats synthetic fixtures — a new integration test proved a real hand-built pentagram ignites at circumradius ~32-51, and surfaced a LOW UX carry-forward (spacing-sensitive + unguided, so "it didn't work" could recur post-deploy without a build hint).

## 2026-06-24 — Session 99: 2 playtest bug-fixes (user-approved + screenshots) — P1 NONET spirit sprites fading into the bg · P2 Voltkin not firing when a structure is REDUCED by deletion. commits 7db1ee1/807bbb6, pushed → deploy auto; tsc 0, vitest 1522/1522, entry 557.2/560. 2-agent investigation + 1-round 3-way Council ADOPT-WITH-CHANGES (accepted).

- P1 #nonet-sprite-fade-was-a-mask-peaking-at-231-not-255: "sprites fade into the bg" = the shared alpha matte never reached full opacity (peak 231/255) + an 81px feather blur eating the WHOLE subject (not just the rim). Fix lives in the MASK GENERATOR, not runtime code: a plateau-clamp (multiply-then-min-255 after blur) for a flat-255 core + soft rim — you can't add back opacity a mask removed (sprite.alpha is unset; the fade is in the matte's alpha channel + a baked webm vignette). Sweep params EMPIRICALLY and measure (peak / opaque-core% / corner-transparency / wing-band alpha): too-high plateau (1.7) made it 100% opaque = hard rectangle; 2.0@feather0.20 was the sweet spot. Keep a bespoke per-asset mask (owl-b) untouched — it was cropping a flame. Verify with BOTH the numeric alpha profile AND a live screenshot (the WebGL screenshot worked this time after forceNonet + ticker.update pump).
- P2 #godly-matcher-only-ran-on-bond-create-not-sever: "X didn't fire after I DELETED down to a valid pattern" = the matcher was triggered only by BOND_FORMED effects, never by a sever, so reducing TO a valid state never re-evaluated (build-up worked, delete-down didn't). Fix = also act on the sever effect, but cause-FILTER to player-initiated only (bomb/creature severs must not random-fire a godly in combat) and REUSE the existing single-loop+break (already caps to one-trigger/frame, so no double-fire from a form+sever in the same frame — dissolved the Council's double-fire concern without a bigger refactor). Keep the strict topology gate (relaxing reopens a prior false-positive); the player's structure must end as a clean isolated chain. Wrote the FIRST test to drive runGodlyMatcher by stubbing only the ctx fields it reads (the Pixi-heavy fields are for the cinematic fn). General lesson: an event-driven matcher must listen to ALL topology mutations (create AND destroy), not just creation.

## 2026-06-24 — Session 98: user-approved Full batch — P1 E2E lane quarantine fix · P2 combo order-symmetry (Option B) · P3 Connection Preview. commits fc21bd6/6e62d29/65978c6, pushed → deploy GREEN; tsc 0, vitest 1517/1517, entry 557.1/560. 2-round Council (Grok+Gemini) ADOPT, PRIME-AUDIT clean (2 deltas landed), Rule 22 audit clean.

- P1 #ci-flake-quarantine-is-a-CLASS-not-a-named-list: the handoff named 4 flaky tests but the FIRST real run proved the WHOLE real-WebRTC (open2Peers) suite is CI-flaky — a different subset (this run: the 2-peer Sym A/G/J joiner tests, waitForWorld timeout: sparks on joiner, peerCount:1) fails each run. Static `playwright --list`/grep said "tag S62 → green" but the gating lane stayed red. LESSONS: (a) verify a CI fix on a REAL run (D3 boot-then-smoke), not static parse; (b) the actual email-stopper is JOB-LEVEL `continue-on-error: true` — a non-required job still reds the run conclusion + emails; job-level continue-on-error makes the RUN conclusion success regardless (confirmed: overall=success, e2e=success, e2e-quarantine=failure-absorbed → no email); (c) quarantine by the shared characteristic (real P2P) not by names — tag the whole class (all 10 smoke.spec.ts describes) so a different flake next run can't re-red.
- P2 #order-symmetry-mirror-the-table-not-sort-the-key: achieve commutativity by ADDING reverse rows that reuse the forward outcome object (byte-identical) + canonicalize DISCOVERY by resultName/forward-key — NOT by sorting comboKey (which would collapse the intentional Wheel/Star dual AND break persistence/replay keys). REBALANCE done empirically, not by reflex: an ADDITIVE table change never touches a forward key, so the optimal/canonical-build income (the ~157.5s match-length anchor) is PROVABLY unchanged → ship win-score unchanged (over-correcting like S91's forward-additive case would punish skill); symmetry only raises the floor for sloppy ordering, with a playtest-gated micro-rebalance held. PRIME-AUDIT caught my own PDR error (Square→Dot/Circle→Line are reverses of the 8 pairs → flip to magic, not "stay functional"). Read the actual tests before claiming which break.
- P3 #preview-equals-release-by-reusing-the-reducers-own-pure-pickers: a "show exactly what will happen" preview must call the SAME pure functions the authoritative path uses (here the reducer's pickHostTargetPrimitive/collectHostMergeCandidates/pickRedundantBondTargets) + mirror its merge-sweep dedup, not re-implement picking — and prove equivalence with a unit test against those pickers. Kept it PURELY ADDITIVE (new resolver + renderer + 1 sync line) rather than refactoring the critical onUp input path — a reasoned deviation from the Council's literal "shared computePotentialBonds called by onUp", chosen because refactoring a critical path is riskier once its e2e safety net is quarantined (logged). refPos = isClient?cursor:spark.pos (the exact onUp expr) is the fidelity crux; verify the shipped module at runtime via Vite dynamic-import when the WebGL screenshot tool times out.

## 2026-06-23 — Session 97: S96-deploy playtest batch (user full-batch approval) — P1 owl-b flame crop · P2 G3b Combo Codex · P3 NONET spirits re-placed asymmetric · P4 NONET winner jackpot · P5 multi-godly (1 of each type). commits 77b7dbd/e1e7b6b/4b9447d/bbca3d9/de5643a, pushed → deploy live; tsc 0, vitest 1505/1505, entry 555.0/560. (E2E lane red on a PRE-EXISTING CI-sandbox WebRTC flake + 15m-cap cancel, not S97 code.)

- P1 #tighter-render-mask-beats-baked-darken-or-reshoot-for-a-matte-edge-artifact: a localized generative artifact (a flame veo baked into owl-b) leaking through the shared matte → fixed with a per-ASSET tighter render mask (crop), not a baked darken (dimmed the whole subject) nor a veo re-shoot ($/gamble). Measure the mask alpha where the artifact sits (19-57) vs the subject (100-231) to size the crop; verify with an ffmpeg masked-composite (pixel-equivalent to Pixi's alpha mask), not a flaky WebGL screenshot.
- P2 #verify-a-pixi-feature-without-a-screenshot-and-mind-bg-rAF-throttling: verified the Combo Codex end-to-end via the REAL UI path (emit the title button's pointertap → introspect the live Pixi stage + localStorage) when preview_screenshot timed out on the 1920×1080 WebGL canvas. GOTCHA: a background preview throttles requestAnimationFrame → world.tick freezes → render-loop side-effects never fire; pump app.ticker.update() manually before concluding a hook is broken. A 5-dimension adversarial review (each finding independently verified) returned 0 confirmed findings, corroborating the live check.
- P3 #asymmetric-placement-is-data-not-code-verify-by-introspecting-coords: the "spirits look symmetrical/unnatural" fix was a pure DATA change (GUARDIANS x/y/vw); keep each spirit's x-RANGE outside the centred panel bbox [630,1290] (an x-interval test suffices, y irrelevant); verify by reading the live masked-sprite coords + a 0-overlap check since the WebGL screenshot tool times out. Visual taste stays the user's call on deploy.
- P4 #winner-only-celebration-is-naturally-netcode-safe-when-gated-on-local-vs-synced: a per-player celebration (winner jackpot, losers nothing) is desync-proof when render-only — the host writes ONE authoritative ev.solvedBy in the snapshot and each peer compares it to its OWN localPlayerId, so the winner's client celebrates and others don't with zero wire/protocol change. Extract the animation to a pure tested module; keep the photosensitivity charter (gentle ~1Hz full-screen glow capped <0.30, put the flash energy in small-area fireworks).
- P5 #find-the-real-gate-before-coding-the-users-theory-then-mirror-a-proven-pattern: the user's "one godly per game" theory was wrong — reading the code showed NONET (sudokuFiredThisMatch) and godlies (a per-player 60s cooldown) are independent; the real friction was the cooldown cross-blocking DIFFERENT types for 60s. Delivered "1 of each type per match" by MIRRORING the sudokuFiredThisMatch once-per-match guard (world field + init + 2 reset sites + additive-optional sorted save/restore + matcher gate + trigger mark). A fresh dynamic import gets EMPTY module-level state (the recipe REGISTRY) → live matcher probes are vacuous; cover registry-gated logic with unit tests. Be honest if the recipe predicate (not the gate) was the user's real blocker.

## 2026-06-22 — Session 96: NONET realm brought to LIFE with real veo video loops (replacing the procedural "PowerPoint-spin" twitch); combo order-symmetry surfaced as a LOCKED design decision (decision-ready PDR, NOT auto-changed); E2E protocol version tests unblocked after the S93 8→9 bump. Autonomous overnight build on explicit full-batch pre-approval. commits 639b140 / dcf88ae / b8f2a47 / a706554, pushed → deploy live; tsc 0, vitest 1488/1488, entry flat 553.6/560 KiB, veo spend $2.10/$10.

- P1 #image-to-video-beats-procedural-twitch-for-character-life: the user rejected the S95 procedural breathe/blink/hop/float sprite idle as "an early-2000s PowerPoint shape spinning". The fix was NOT better math — it was a real pipeline: Pillow composites each approved sprite onto a dark dusk keyframe → veo-3.1 image-to-video → ffmpeg seamless xfade-loop + opaque VP9 → Pixi feathered-mask composite, video-primary over the S95 static+procedural fallback. LESSONS: (a) when libvpx can't encode an alpha plane (this build dropped it on BOTH VP8/VP9 though alphamerge verified varying alpha pre-encode), pivot to render-time masking, don't fight the codec; (b) a perfect loop = xfade-against-self at offset=(D-CF) keeping segment [CF,D] (first frame == last; seam diff 1.4/255); (c) calibrate ONE veo clip before fanning out in parallel ($2.10 for 6); (d) verify motion empirically (readyState 4 + currentTime advancing/looping + live screenshot), not by trusting the prompt.
- P2 #a-flagged-bug-can-be-a-locked-design-decision-in-disguise: the "combo order-symmetry bug" is INTENTIONALLY order-dependent (LOCKED § V.1 + 3 invariant tests) and Triangle→Circle=Wheel vs Circle→Triangle=Star is a DELIBERATE dual-magic a naive commutative fix would delete — plus it would ~double magic income and break the S91-tuned match length. LESSON: in an autonomous session, when "the fix" amends a LOCKED decision / breaks invariant tests / changes balance with ambiguous intent, surface a decision-ready PDR (docs/combo-order-symmetry-PDR.md) and let the awake user choose. "Be pedantic" includes refusing to quietly gut a feature.
- P3 #a-version-bump-breaks-more-tests-than-the-one-flagged: the handoff named only smoke.spec.ts:625 (assert 'v8') as stale after the S93 8→9 bump, but a SECOND test ("newer-version joiner v9") was also broken — it assumed host=v8 so a v9 peer was "newer", but the host is now v9 so a v9 joiner MATCHES (no rejection → waitForRejected hangs). LESSON: when a constant bumps, fix ALL hardcoded refs AND reason about tests whose PREMISE depends on the old value; bumped the joiner to v10. Verified by actually running the 2 E2E tests (2 passed, real WebRTC).

## 2026-06-22 — Session 95 (cont.): NONET realm visual buildout — IP rework (Totoro look-alike → original moss-spirit, after the user flagged it LIVE), then a guardian council of 4 original spirits, then a per-frame "living realm" animation pass (breathe/blink/hop/float + a 16-firefly swarm). commits 2f0fa89 / 34c545e / d556dc3, pushed → deploy live; tsc 0, vitest 1488/1488, entry 553.6/560 KiB, generative ~$0.24 cumulative.

- #shipped-an-IP-look-alike-then-got-flagged-live — self-check generated art FIRST: P1 shipped an Imagen kami that was an unmistakable Totoro (Studio Ghibli); the user caught it only AFTER it deployed. The vector 404-fallback AND the little white "kodama" were also Ghibli echoes. LESSON: AI image-gen drifts toward famous characters by default — before shipping ANY generated character, self-review for franchise resemblance and prompt explicitly for an ORIGINAL silhouette/features (saved as a standing project memory). Reworked to an original mossy seedpod spirit via the same magenta + hue/min-channel key.
- #procedural-life-on-static-sprites-is-cheap-and-high-impact: the realm felt alive with NO new art — per-frame sin-driven transforms give each static webp its own idle (breathe = belly scale.y pulse; blink = quick scale.y squint via a pure tested blinkPulse; hop = abs(sin) bounce + squash-on-land + L↔R patrol; float = drifting loop), phase-desynced so nothing moves in lockstep, plus a swarm of pre-drawn firefly Graphics that only animate position + alpha. LESSON: drive DECORATIVE animation on wall-clock (performance.now), NOT world.tick (which jumps on client snapshots) — keep cosmetic motion off the sim clock; extract the one non-trivial envelope (blink) as a pure unit-tested helper.
- SESSION #verify-animation-by-state-sampling-not-one-screenshot: rapid preview reloads wedged the headless tab's rAF (screenshots time out, world.tick stuck at 0). A FRESH preview server screenshots fine, and motion is provable without a screenshot by pumping app.ticker.update() + sampling each sprite's x/y/scale/rotation at t and t+Δ. LESSON: for animation work, restart the preview server (don't reload-churn one), and verify motion by sampling transforms over time rather than trusting a single frame.

## 2026-06-21 — Session 95: P0 NONET overlay never appeared in live 1v1 (catchless latching lazy dynamic-import → 180s silent hang, no board) FIXED via robust load (.catch+throttled-retry) + preload + global vite:preloadError reload + the missing 2-peer snapshot round-trip test; P1 illustrated kami sprite (Imagen + Pillow hue-key) auto-upgrading the Codex tile + overlay, bundle charter raised 550→560 KiB AND mechanically enforced in the build; P2 anime SFX + winner-colour resolve flood + resolve screen-shake, all kept in the lazy chunk. commits e23b8a8/92b7ea8/d5c54de, pushed → deploy live; tsc 0, vitest 1484/1484, entry 553.6/560 KiB, generative $0.12.

- P0 #a-silent-catchless-lazy-import-is-a-time-bomb-for-trigger-critical-ui: the NONET overlay 'never appeared in 1v1' was not a render bug (it renders + solves fine in solo) — it was an unguarded dynamic import() (no .catch) whose loading-latch flag stayed true forever, so one chunk-fetch failure (transient, or a stale chunk-hash 404 right after a deploy) froze the whole duel for the 180s timeout with NO board and NO error surfaced. LESSON: any lazy chunk that gates a critical, time-boxed interaction MUST (a) .catch + un-latch to retry, (b) preload ahead of the trigger, and (c) be covered by a vite:preloadError reload for the stale-deploy case. Verify the LOAD path, not just the happy-path render.
- P1 #key-a-stubborn-chroma-bg-by-its-minimum-channel-not-its-brightness: Imagen ignored the #ff00ff chroma request and returned a vignetted rose-pink bg. A brightness/distance key fights the vignette and risks holes; but the CHARACTER's palette (slate/cream/amber) never has GREEN as its minimum channel while the pink bg always does — so keying on 'G is the min channel AND R,G,B make a pink' cleanly separated them regardless of the gradient (corner diag: bg pinkness 67 vs char -7), then spill-suppress + 1px erode killed the fringe. LESSON: when a generated chroma bg is imperfect, key on a HUE/channel-relationship invariant the subject can't satisfy, not on absolute colour distance.
- P2 #a-soft-prose-charter-with-no-CI-guard-will-always-drift: the '550 KiB bundle' lived only in handoff prose for ~40 sessions with no LOCKED entry + no mechanical check, so it silently crept over. Raising the number alone would just restart the drift; the fix was raise + ENFORCE — a check script in the build that the deploy CI runs, plus keeping new feature code (NONET SFX/flood) in lazy chunks so the entry chunk barely moved. LESSON: a budget that isn't mechanically checked is a wish; gate it in the build and code-split to defend it.
