## S173/S174 (2026-09-12/13) - the playtest session: nine live bugs from a 2-player internet game, all triaged and most shipped; tower/castle health bars made honest; the codex cut back and unlocked; $6.00 of veo clips that taught a protocol; and THREE self-inflicted faults worth more than the features - a deploy I broke with a staged deletion, a lesson I bought that was already in the repo, and a hypothesis that contradicted my own A.0.

- P1 #the-function-with-no-callers-was-the-spec: structureDefenceFifths already existed in stats.ts with ZERO production callers, and its docblock said outright that it existed "so the HUD and the tests can speak their language". The tower health bar the owner asked for had already been half-written by a past session and never wired - the same shape as S167's t3TowerAtlasBase, where the art shipped with no renderer. Grepping for an unused function that names your feature is cheaper than designing the feature.

- P1b #two-durability-models-and-the-bar-can-only-tell-one-truth: a tower dies EITHER by connector severance (Bond.damageFifths vs connectorCapacityFifths) OR by primitive death (prim.hp -> razePrimitives), and towerRenderer already derives its damaged/destroyed FRAME from the second while the owner's words name the first. I shipped his model and documented the other AT THE SITE rather than picking silently - because a future session finding the bar "under-reporting" will otherwise re-derive the whole thing before discovering there were two.

- P1c #vitest-green-and-tsc-red-on-the-same-commit: the full suite passed 4303/4303 while typecheck exited 1 with three errors in the very test file that was passing. Bond.a is a PhysicsBody, not a Primitive, so my adjacency detach was a no-op the assertions could not see. Reading ONE gate's exit code would have shipped it. The project rule is "read every gate's exit code from a captured $?" - every, not the slowest one.

- A0 #i-rode-six-lanes-on-one-invocation-and-lost-five: the state-discovery workflow put all six probe lanes in a single Workflow call, an org spend limit hit mid-run, and 6 of 8 agents died - the exact S161 failure this project's CLAUDE.md warns about in a section written after it happened before. I had READ that section during boot. Re-running the four dead lanes by hand cost more than dispatching them separately would have. When the owner later asked for parallel work I used three SEPARATE Agent invocations for that reason.

- P4 #the-agent-refused-my-brief-and-was-right: I briefed the tower-shortfall work at castlePanel's caption from my own grep. CASTLE_BUILD_GRID_ENABLED = false - that panel has been dead code since S149 P5 moved tower-building to the footer band. The agent verified the premise before building on it and redirected to footerBand.ts. I had grepped for the STRING ("NEED n MORE") and found a real site, but never asked whether that site was still REACHABLE. A live-looking code path is not the same as a rendered one.

- P4b #two-agents-one-working-tree-sweep-each-other: staging is global, so when two subagents commit into the same checkout, whoever commits second sweeps up the first's staged files - and the first then reports success for a commit that does not contain its work. The P4 agent even NAMED this hazard in its report and still concluded, wrongly, that its work was safe in HEAD. It was uncommitted. VERIFYING THE AGENT'S CLAIM AGAINST THE TREE is what saved it: three greps, and the symbols were absent from every commit. Next time: give parallel agents separate worktrees, or let exactly one of them commit.

- B3 #a-noted-defect-is-still-a-defect: healthBar.ts carried the line "The castle bar (gathererRenderer.drawKeep) still carries fault 1 - noted, not touched here" from S171 onward. Fault 1 is hide-while-undamaged, which the owner had ALREADY rejected once for creatures. He then found it on the castle by playing, and reported it a second time. Writing a defect down bought nothing except the ability to say it was known. If it is worth a comment that names it as a fault, it is worth either fixing or a line in the backlog he can see.

- P1/B3 #shipping-half-a-ruling-reads-as-not-shipping-it: P1 gave every tower a health bar and he still said "the other buildings don't have HP bars" - because the bar was RED and the castle's is GREEN, so it did not read as the same feature. The mechanic was right and the signal was wrong. Colour was carrying meaning (building vs unit) that nobody had written down, and I had matched the creature convention without asking which family a tower belongs to.

- B5 #my-brief-named-the-wrong-layer-and-the-agent-refused-it: I briefed a wave-indexed ladder falling through to a default creature type, because the owner said "after wave three the CASTLE starts SPAWNING goblins" and that sentence sounds like sim. There is no wave->creature table in the tree at all; world.waveNumber has one consumer and it scales a spawn RATE. The real cause was a RENDERER fall-through - direwolf in GOBLIN_KINDS with no atlas, drawn as the pre-veo puppet. TWICE IN ONE SESSION an agent has corrected the layer I aimed it at (P4 was the dead castle panel). The owner describes what he SEES; the word he uses for the mechanism is a guess I should not inherit.

- B5b #a-deliberate-exemption-is-still-a-defect-and-it-was-defended-in-prose: S168 wrote at the constant that the direwolf had NO ATLAS "deliberately... visible and readable until the art lands", while the coverage test one paragraph below stated the opposite principle correctly for the locust cloud. The exemption survived because it was ARGUED rather than tested. Same shape as B3 this session, where "the castle bar still carries fault 1 - noted, not touched here" sat in a docblock for five sessions until he found it by playing. A defect with a paragraph defending it outlives a defect with nothing.

- P2 #i-had-both-halves-of-the-contradiction-and-shipped-anyway: A.0 established that normaliseStateScale equalises HEIGHT only (_subject_h, no _subject_w exists) AND that 5 of 6 mismatched atlases predated the packer fix. Every failure the guard prints is a WIDTH divergence ("1.21w(WIDE)"), so the repack could never have worked - the two findings were in the same write-up and contradicted each other. I built a priority on the mtime half because it was NEW and felt like the stronger signal. The experiment cost one repack to disprove, which is cheap; the lesson is to cross-check a new finding against the mechanism finding BEFORE writing the PDR.

- P2b #a-negative-result-is-a-deliverable: the repack produced no improvement and was reverted, and the session is BETTER for it - the corrected verdict is that ALL SIX size-mismatched atlases need regenerated art, not just the scarab. Recorded with the measurement so the next session does not re-run the same experiment on the other four.

- S174 #i-fell-into-the-shared-tree-hazard-i-warned-four-agents-about: the codex agent had STAGED the deletion of codexStore.ts without finishing its importers. My unrelated clips commit ran git commit, the staged deletion rode along, and the Pages build died on four "Cannot find module ./codexStore.ts" errors - the live site sat stale for an hour. I had written the warning about global staging into every one of those four agent briefs. Writing a hazard down does not exempt you from it; `git status` before `git commit` does.

- S174 #a-dead-agent-is-not-an-empty-lane: all four agents died on a spend limit and every one of them had left real work uncommitted - a finished discovery removal, a new structureComponents module, the entire /veo-generate protocol. Their last messages read like they had barely started. Reading the TREE rather than the transcripts recovered ~1,500 lines that would otherwise have been re-paid for. Check what landed before re-running anything.

- S174 #i-bought-the-lesson-that-was-already-in-the-repo: generated 12 veo clips at $0.50 each and 7 were condemned, because I wrote my own prompts and my own seed geometry instead of first reading assets-source/race-tier9-bosses/clip-spec.json - the spec that produced the one boss the owner says looks right. The working recipe was sitting in the tree the whole time. That is now a protocol with STEP ZERO being "read the last known-good spec".

## S172 (2026-09-10) — made the fight readable: the health bar actually moves (two separate causes), floating damage + healing numbers in Kanit 900 Italic, bosses doubled, and two GUARDS found broken while using them.

- P1 #the-fix-was-right-and-the-bug-was-somewhere-else: healthBar's fill was frozen at 100% on six unit types because span()'s 9px floor was applied to the FILL as well as the TRACK (the floor binds below 7.01 fifths; goblinMelee/Hound/Archer, raceUnit, locustCloud and chewer all sit under it). I fixed that, shipped it, and the owner STILL saw no movement - because there was a second, unrelated cause in a different file. Fixing the first cause is not evidence there is only one.

- P1b #a-missing-wire-cannot-be-caught-by-a-unit-test: drawHealthBars is called from GoblinRenderer and was handed a sprite map populated behind `if (!GOBLIN_KINDS.has(c.type)) continue`, so every boss, tier-3 unit, Voltkin and chewer measured null, fell back to FALLBACK_SPRITE_H=26, and had its bar drawn INSIDE the sprite. No unit test of healthBar.ts could ever see that - the defect lived BETWEEN two renderers. The pin has to be a source-text assertion on the wire itself.

- P1c #believe-the-owner-over-your-own-model: I explained the missing bar as one-shot combat. He said flatly 'he doesnt die in one hit, it took him a good thirty seconds' - and that one sentence relocated the bug from the stat ladder to renderer wiring. His observation was the evidence; my model was the thing that had to give.

- P2 #arithmetically-right-and-design-wrong: the first stat derivation took HP from each unit's OWN hit. It wanted to cut the locust from ATK 10 to 1 (straight through R142) and flatten the drone and the suicide bomber. ownHits is the WRONG target because role IS ownHits variation. Re-deriving against a tier REFERENCE hit with a role factor moved only HP and touched no owner ruling.

- P2b #ship-what-he-ruled-then-measure-it-out-loud: he ruled 'double the boss HP' expecting it to fix boss-v-boss. It moved 1.0 to 1.2-3.7 hits, not the 4 he wanted, because the glass-cannon bosses hit for 150. I implemented the ruling exactly and reported what it actually bought, rather than quietly over-delivering or quietly under-delivering.

- P3 #no-library-is-a-finding-not-a-shrug: he assumed a floating-damage-number package existed. Eight npm searches, the awesome-pixijs list and all 40 PixiJS org repos returned zero, for any engine. But the RECIPE was recoverable from two real clients (MapleStory's timings, an MIT WoW addon's pop and fling), so the answer was 'nothing to install, here are the measured constants' rather than empty.

- P3b #the-census-earned-its-keep-twice: the S171 acquisition census failed the build the moment damageNumbers.ts scanned enemy creatures - correctly, and the answer was a recorded VERDICT, not an exemption. Then it FAILED TO FIRE when my rewrite wrote `(o.ownerPlayerId as number) === mine`, because its regex allowed only whitespace before the operator. A guard reporting a clean board because it cannot SEE the file - the S171 atlas shape exactly. Fixed the cast AND the detector.

- P5 #the-last-hit-is-a-disappearance-not-a-delta: a creature leaves world.creatures the same tick its pool reaches zero, so the killing blow can never be observed as an ehp drop - only as an absence. And to place and AIM that number you must have remembered the victim's position AND owner, because both are gone by then. Without the owner the lookup returns undefined, every creature counts as an enemy, and the final number flies at the victim's own ally.

- MCV #author-the-bindings-before-announcing-done: the Stop hook hard-failed with UNCOVERED - both completed priorities carried verification: []. I created the entries with an empty array at PDR time and never filled them at close. The work was real; the CLAIM was unbacked. Authoring them proactively is step 4 of the completion protocol, and I did it reactively instead.

- MCV-b #grep-the-needle-before-you-assert-it: checking every verification needle against disk before writing it caught my first file_lacks candidate immediately - `span(ehp)` was STILL PRESENT at healthBar.ts:200, inside the docblock that explains the old bug, where it belongs. Asserted from memory it would have failed, and the 'fix' would have been deleting a comment that should stay.

- SESSION #comments-that-lie-are-a-defect-class-not-untidiness: three in one session. stats.ts claimed 'every SHIPPED unit sits inside the range' after six bosses left it; damageNumbers.ts still said the killing blow COULD NOT be shown after I made it show; and a pre-existing docblock had the Pharaoh at 130 fifths when he had drifted to 143. That same stats.ts paragraph already lied for sixteen sessions and caused the S167 accident. A number that lives only in prose has nothing checking it.

- SESSION #newline-detection-can-corrupt-what-it-protects: my own patch helper chose CRLF whenever a file contained ANY CRLF, so it wrote CRLF blocks into otherwise-LF files and left goblinRenderer.ts and main.ts mixed (8 and 20 lines). The project rule 'patch with newline=""' preserves what exists; deriving a newline from a sample of the file does not.

## S171 (2026-09-10) — the Pharaoh completed (locusts + Ra ritual), health bars, the atlas guard repaired, and three bugs the owner found by playing it. ⚠ APPENDED LATE, BY S172: these were written at S171 close but never flushed to this log; recovered intact from session-state.json at commit 9bd6236.

- P2A #a-completeness-claim-in-prose-cannot-stay-true: Two separate docblocks in this repo asserted that the untargetable enumeration was complete — creatureAI.ts's 'THIS ONE LINE COVERS EVERY CREATURE-TARGETING PATH IN THE GAME' and untargetableGates.test.ts's 'THE BYPASSES WERE AUDITED RATHER THAN ASSUMED'. Both were written by sessions that had genuinely looked, and both were wrong: six paths of thirteen, and three files of fifteen. The carried handoff then repeated a SIX-item list whose most important member (world.ts's authoritative RAID_TARGET reducer) was never on it, and three of the six had wrong paths or line numbers. The fix is not a better audit; it is that the enumeration must be re-counted by a machine on every run. damage.wired.test.ts already had the pattern for exactly this reason and I reused it. ⭐ AND ITS ANTI-ROT ARM CAUGHT A STALE ENTRY ON ITS FIRST RUN — mine, a verdict about a file the census never reaches.

- P2A #locate-by-content-never-by-the-recorded-line: Every one of the six carried bypass sites was found by grepping for the CLAUSE, not by opening the recorded file:line. Three were wrong: src/controls.ts is src/input/controls.ts, voltkinChain is under creatures/ and the loop is at 106 not 92, seagullLifecycle is under seagulls/. Had I trusted the numbers I would have patched nothing and reported six fixes.

- P2A #the-owners-reframing-was-a-better-design-than-mine: I planned an 'unkillable creature' — a guard at eight removal sites. The owner reframed it instead: 'he's in a different dimension, between realities... he's not really in the game.' That is not a softer version of my design, it is a cleaner one: a creature that has LEFT the world is not a candidate anywhere, where an unkillable one must be special-cased at every site that can acquire, strike or remove it. It also turned out to be the exact mechanism voltkin-config.ts:995 had predicted in writing for R121's submerged naga. ⇒ When the owner restates a mechanic in his own terms, read it as a design input, not as a looser spec.

- P2A #a-control-arm-that-fails-is-the-fixture-not-the-gate: Two of the new tests failed on their CONTROL arm first: a raid KILLS a chewer (so the landed raid is a removal, not a reduced pool) and resolveProjectileShot returns null unless the archer is ATTACKING inside the flight window. Both were fixture errors. A control that fails for the same reason the assertion would have is precisely how a vacuous test gets written and shipped green — so both diagnoses are recorded IN the tests rather than silently patched.

- P2A #delegated-lanes-die-and-must-be-named: The A.0 probe returned 14/14, but the design panel lost 2 of 4 proposal lanes to the StructuredOutput retry cap (one on unescaped Windows backslashes in its JSON). Per the S161 rule those lanes are NOT DONE and are named as such in the PDR's PRIME-AUDIT delta; their angles were covered by hand. The panel that DID return found four things I had missed, including a ninth removal class (world.creatures.clear() at three sites) and the population latch that would have made a multi-cloud cone into N-1 silent no-ops.

- P2B #the-guards-fired-four-times-and-were-right-four-times: Adding one CreatureType tripped four independent repo guards, and not one was a false alarm: the renderer-invisibility coverage test (the cloud drew nothing), the hand-maintained type list (the one site tsc cannot reach), the S169 'nothing is untargetable' assertion (correctly obsolete the moment the flag got its first consumer), and my OWN P2A acquisition census, which flagged bossSkillsPharaoh.ts on the very first file written after it shipped. ⭐ THE TEMPTING FIX WAS THE WRONG ONE TWICE: I could have satisfied the invisibility guard by dropping 'locustCloud' into GOBLIN_KINDS, which would have given a swarm of insects a HUMANOID puppet - satisfying the test while defeating its purpose; and I could have granted the Pharaoh a census verdict instead of recognising nearestEnemyFor as a genuinely guarded scanner. A guard is a question, not an obstacle: the right response is the answer, not the shortest thing that turns it green.

- P2B #never-bind-a-claim-to-a-number-that-is-supposed-to-move: P2A's bindings pinned 'PROTOCOL_VERSION = 45 as const' as evidence for its no-bump claim. P2B bumped it to 46 - correctly - and the Stop hook hard-failed P2A as the S149 fabrication class. The claim was never 'the version is 45'; it was 'raRitualUntilTick is additive-optional and so costs no bump'. Rebound onto the MECHANISM that makes it true (the conditional emit/rehydrate pair in save.ts), which survives every future bump. verify-session-claims.py already warns about this exact class for json_field version keys - I walked into the file_contains version of the same trap.

- P2B #a-compression-in-the-handoff-becomes-a-wrong-mechanic: Three separate carriers - boot-snapshot, session-state and the S170 PDR - had compressed R142's 'a cone of locusts THAT FLY AROUND IN LOCUST CLOUDS' to 'the locust cone', and the PDR hardened it further to 'wedge'. All describe a persistent cone VOLUME, which is a different mechanic from a fan of autonomous hunters. Reading the owner's verbatim words rather than the carried summary changed the template from the Kraken's sonar to the Warlord's direwolf summon. ⭐ AND MY OWN PDR CARRIED A VERSION OF THE SAME ERROR: it called the launch 'a reuse of inCone'. inCone is a MEMBERSHIP test; the launch PLACES. It became the test ORACLE instead, which is a real check where building the launch on it would have been circular.

- P2C #count-the-outcome-not-the-call: The ritual's five columns are timed so the last one lands on the exact tick the channel ends. My runner checked death first, removed the boss, and silently dropped that column - the ultimate would have shipped as four columns and a corpse, and it would have LOOKED fine. What caught it was that the test counts columns by standing a real victim on each landing spot and asserting all five die. A spy on applyRadialDamage, a call-count, or a vaguer 'columns landed' assertion would every one of them have been green on four of five. ⇒ When a mechanic's value is N of something, assert N OUTCOMES, not N invocations.

- P5 #measure-the-population-before-choosing-a-threshold: The atlas guard reported 30 sheets clean while the owner could see two of them were dirty. The instinct is to loosen a threshold; the truth was that neither defect was a threshold problem. It measured HEIGHT on a creature whose scale lives in WIDTH (scarab: width dev 0.27, height 0.01), and it measured the bbox of ALL opaque pixels so one stray sliver redefined the animal (188px against a 136px beetle). Both were found by dumping the metric across all 48 atlases and looking at the distribution, and the two sheets he named came out ranks 1 and 2. ⇒ When a guard disagrees with the owner's eyes, print its raw numbers for the whole population before touching a constant - the ranking tells you whether the metric or the threshold is wrong.

- P5 #engage-the-docblock-that-already-rejected-your-fix: My first instinct for the white fringe was 'threshold on TOTAL instead of largest blob'. The existing docblock had explicitly considered and rejected exactly that, with data: eye glints give healthy sheets 66-768px of total white while their largest pocket is 2-52px, so summing calls the healthy sheets worse than the broken ones. It was right. Reading it turned a wrong fix into the correct one - the discriminator is not how much white but WHERE: a glint is interior, a matte fringe hugs the cut-out edge. ⇒ A comment that pre-refutes your change is the cheapest review you will ever get; the fix that survives it is a better fix.

- P3 #run-typecheck-after-test-edits-not-only-source-edits: Twice in one session vitest went fully green while `tsc -b` inside `npm run build` failed on a type error in a TEST file - once a missing property on a return type, once an unused binding. vitest strips types, so a test suite passing says nothing about the test file compiling, and the failure surfaces two gates later where it reads as a build break rather than as the test edit that caused it.

- P6 #report-in-the-owners-units-not-the-engines: I quoted RAW FIFTHS at the owner all session - in a PDR, a stats table and four commit messages ('150 damage against a 143 pool'). The sim stores combat as atk*(5+pen) to stay integral; 150 fifths IS 30 points, which is exactly his own 10*(1+0.2*10). Every RATIO I gave was right and every NUMBER was in a unit he does not think in, so it read as nonsense and he had to challenge it to find out. ⇒ An internal representation is never the reporting unit. Built scripts/stat-table.mjs so the whole roster prints in points and this cannot recur - and that table then became the evidence for his much larger point, that the stat system has no ladder at all.

- P6 #the-owners-words-can-name-the-mechanism-exactly: He described shapes flying off the porch as 'it seems like there's MAGNETISM or ANTIMAGNETISM between the shapes'. Vortex (Dot+Spiral) is a radial suck and Spindle (Line+Circle) is a tangential shove - he had named both mechanics precisely without knowing the code. Treating that phrase as a literal hypothesis rather than as vague colour is what pointed the investigation at two files with no escrow guard. ⇒ When a non-engineer reaches for a physical metaphor, test it as a hypothesis before translating it.

- P6 #a-guard-and-its-builder-can-share-one-blind-spot: check-atlas-scenery measured row size from the bbox of ALL opaque pixels, so a stray sliver redefined the creature - and build-sprite-atlas's normaliseStateScale measured the SAME way, where the number is the DIVISOR in a rescale. So the speck did not merely mismeasure the row, it SHRANK it, and the checker that should have caught that was blind for the identical reason. Both fixed to use the largest connected component. ⇒ When a guard misses something, check whether the thing it guards computes the same quantity the same wrong way.
## S170 (2026-09-09) — audited S169's compacted handoff (12 agents, 7 false claims found), shipped 6 priorities, and rebuilt fog of war FOUR times before getting it right

- #a-green-test-can-be-defending-the-bug: the owner reported the same two-pharaoh defect twice and S169 'fixed' it by removing the gate that was PROVABLE in a unit test (a direct-dispatch latch) while leaving the gate he actually hit. t9BossTower.test.ts asserted the waiting behaviour as CORRECT, so the suite was green ON the defect and would have blocked the real fix. LESSON: when a reported bug 'does not reproduce' in tests but the owner keeps reporting it, grep for a test that ASSERTS the reported behaviour - a defect with a passing test defending it is invisible to every gate. Also: the docblock defending that gate justified itself by citing the latch S169 had just deleted, so a stale rationale outlived its own premise.

- #gate-the-effect-on-the-outcome-not-on-the-trigger: the drone's blast VFX was pushed unconditionally, BEFORE the enemy-only candidate scan and the owner-sparing damage - so a fuse-expiry detonation at home showed a full shock ring for an event that could not damage anything. The tempting fix (silence blasts on fuse expiry) would have silenced real hits too, since explode-in-place next to an enemy base is deliberate. LESSON: gate a visual on what actually HAPPENED (candidates.length), never on the branch that led there.

- #one-populations-ceiling-silently-gating-another: GOBLIN_MAX_PER_SPAWNER=10 was shared by the goblin tower and the tier-3 race towers, so a tier-3 tower went permanently silent at ten units - the owner's original 'the Piranha didn't produce at all' returning, months later, from a constant named after a different unit. Three prior exclusions in the same function existed for exactly this failure and a fourth was still owed. LESSON: fix it as an EXEMPTION, never by raising or deleting the shared number, and assert BOTH sides - a test proving 'tier 3 is uncapped' cannot tell that from 'the cap is gone' and would have re-tuned an economy the owner chose.

- #the-unit-of-a-ruling-is-part-of-the-ruling: the sudoku trial counted SHAPES with an exact test; the owner had said 'twelve same-shape-connectors' and the code's own comment flagged that one word would flip it - and nobody asked him for two sessions while he reported 'there is no sudoku, it doesn't work'. Twelve connectors is not a fixed shape count (chain of 13 = ring of 12 = 12 bonds), so no shape-count test could express his rule. LESSON: when a comment says 'one word flips this if the intent was X', that is an OWNER QUESTION, not a note. And an exact-N test on a value that can jump by two is skippable by construction.

- #a-private-renderer-method-scopes-a-condition-to-one-renderer: the stun stars lived as a private method of goblinRenderer, which silently limited them to GOBLIN_KINDS - while applyStun's only caller, the Kraken sonar, has NO type filter and stuns voltkin/chewer/drones drawn by two other renderers. Those froze with no stars, reading as a hang. The same method also used flat geometry, so on the scaled-up bosses (the units stun exists to counter) the ring sat inside the sprite. LESSON: a visual for a property of an ENTITY belongs in a module, not in whichever renderer happened to add it. Extraction is also what made it testable - it went from an untestable private method to a pure function with a determinism assertion.

- #research-the-standard-solution-before-inventing-a-mechanism: I spent most of a session and THREE shipped-and-wrong attempts hiding enemy units, by re-layering a backdrop twice and then building an inverse Pixi mask - without ever checking how fog of war is done. The owner had to ask twice ("did you look into them or no? I said, look at Red Alert") before I looked. The classic RTS answer is per-entity culling (dont draw it), the repo ALREADY contained two of the three pieces (explored grid + last-seen ghosts), and the fix was one predicate plus one line per renderer. LESSON: when a problem is old and solved everywhere, the first move is to find the standard mechanism, not to reach for a rendering trick. SECOND, SHARPER LESSON: prefer the mechanism the SUITE CAN SEE. All three failures passed every gate, because masks and compositing are invisible to a test harness that cannot read composed frames - renderer.extract does not apply filter effects. A pure predicate was both the correct answer and the testable one, and "can I assert this?" would have picked it on day one.
