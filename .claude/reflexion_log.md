# Reflexion Log — The Spark

Cross-session learnings, newest first. Each `## DATE — Session N` block holds that session's reflexion entries. Pruned to ≤50 entries; older entries live in the archived handoff docs.

## 2026-08-04 — Session 131: S131: discharged S130's owner playtest gate EMPIRICALLY (headless Chromium via the project's own playwright — the blocked layer was the PANE, not the browser) and shipped V6-0.3 Commit B (sever attribution), P2 (a CI gate so a red test blocks a production deploy) and P3 (backing plates). Both HUD surfaces verified in REAL PIXELS through genuine emitters, never synthetic payloads. A 3-way Triumvirate CHECK then found 6 real problems — 4 of them vacuous guards I had written this same session, each broken by deleting 1-2 lines with the full suite staying green. tsc 0, vitest 1990/1990 (+49), bundle 645.5/750 KiB, MCV exit 0, gitleaks clean. 30 commits unpushed: write auth absent, read auth healthy.

- #a-blocked-verification-path-usually-has-a-side-door-one-layer-over: Two sessions recorded the banner's visual as unverifiable because the Browser pane will not composite. True, and not the whole board: the project already ships playwright, and headless Chromium composites fine. The blocked layer was the PANE, not the BROWSER. Before recording something as unverifiable, name the exact layer that is blocked and ask what else supplies it.

- #drive-the-real-emitter-not-a-lookalike-payload: It was tempting to push a hand-built SCORE_TIER into world.effects to make the banner appear. Setting the leader's score to STEP-1 and letting scoring.ts emit costs the same and proves strictly more: the emitter's own gates (cinematicsEnabled, leaderId, newTier>oldTier) become part of the evidence. Same for the toast, driven through the app's live dispatchFn. A synthetic payload verifies the consumer while quietly assuming the producer.

- #a-detector-can-fail-on-a-property-you-forgot-varies: My banner-presence check matched yellow pixels and reported ZERO hits across an entire hold sweep - because that run's leader was seat 1 (cyan) and the fill is the leader's colour. I nearly filed 'the banner does not render' off a detector measuring the wrong invariant. When an oracle returns all-negative, suspect the oracle before the subject.

- #a-fully-specced-plan-still-needs-its-anchors-probed: The handoff said FULLY SPECCED, DO NOT RE-DERIVE, and that was right about the DESIGN. It was not a reason to skip Rule 21 on the plan's factual anchors: three did not hold - two renderers the PDR wanted in a 'reads world.effects' allowlist read no effects at all, a cited comment did not exist at the cited line, and a seven-arm table collapsed to one rule once the six dispatch sites were actually read. Do not re-derive the decision; do verify the state it rests on.

- #a-frame-count-is-not-a-duration: TIER_BANNER_FRAMES=120 reads like 2 seconds and is not: nothing caps the Pixi ticker, so it is 2.0s at 60Hz and ~0.83s at 144Hz. The owner was being asked to judge legibility 'in the ~2s hold' on hardware where it may be under a second. The new toast holds in SIM TICKS instead. When a window exists for a human to READ something, measure it in time, not frames.

- #a-grep-proves-a-string-is-absent-not-a-behaviour: The claim 'NO CI job runs vitest OR typecheck - zero hits across .github/workflows/' was carried by several handoffs, drove an owner ruling, and I repeated it in LOCKED_DECISIONS.md earlier in this same session. The grep was correct; the inference was half wrong, because npm run build IS 'tsc -b && vite build && check-bundle-size'. A literal-string search cannot see through a script indirection. When a grep is load-bearing for a decision, follow the scripts it names.

- #measure-inside-the-hold-not-on-the-edge-of-the-ramp: My first plate check compared with-plate against after-expiry and got a 22-pixel difference, which would have read as 'the plate never appeared'. The shot had landed inside the 0.3s fade-in. When a thing animates in, any measurement taken at t=0 measures the animation, not the thing. Sample in the steady state.

- #a-plate-must-die-with-its-label: Adding a backing plate creates a new way to fail that the text alone did not have: the plate can outlive its label and sit on the board as a black rectangle. I wired visibility in the same statements on every path and then PROVED absence (0 -> 3759 -> 0 warm px) rather than only proving presence. Verifying that a feature appears is half a test.

- #four-of-my-own-guards-were-vacuous-and-only-mutation-testing-found-it: I mutation-tested the guards I THOUGHT were load-bearing and shipped four others untested. A CHECK panel then broke all four by deleting one or two lines each, with the full suite staying green every time. Writing a test after fixing a bug is not the same as proving the test would have caught the bug. The rule that actually works: for every new assertion, delete the code it names and watch it fail — no exceptions for the ones that look obvious.

- #indexOf-over-source-text-matches-your-own-comments: The drain-order guard — written specifically because this defect already shipped once — could be blinded by adding one token to a COMMENT, because String.indexOf returns the first match and the file reads main.ts as raw text. I had already fixed this exact class in the CI guard the same hour (asserting on extracted `run:` commands rather than file text) and did not carry the lesson one file across. When you solve a class of problem, sweep for other instances immediately.

- #a-refutation-that-fails-can-still-correct-you: A reviewer tried to prove my bomb-suppression clause redundant, failed, and in failing showed my stated REASON for the clause was wrong — the rainbow shuffle preserves the correspondence I claimed it broke; the real path needs the next match's re-seating. I would have kept a true conclusion resting on a false mechanism, which is how the next session gets misled. Read the reasoning of findings that were REFUTED, not only the ones confirmed.

- #split-a-review-panel-by-what-each-leg-can-actually-verify: Giving all three legs the same diff would have had two of them guessing about code they cannot read. Internal agents that can open files took code-level review and found four surviving mutations by running them; the external models took design-level questions and returned a genuine logic bug and an independent protocol-skew confirmation. Match the lens to the leg's actual access instead of triplicating one review.

## 2026-08-03 — Session 130: V6-0.3 A.0 (12 agents) + 3-way Council found that S129's tier banner SHIPPED DEAD (its world.effects scan ran 29 lines after the array is wiped) — repaired, and VERIFIED RENDERING by pumping app.ticker directly, which sidesteps the rAF blocker two sessions had accepted. tsc 0, vitest 1941/1941 (+9), bundle 642.6/750 KiB (+218 B measured), MCV exit 0. Commit B (sever attribution) NOT started — held at the owner playtest checkpoint. 17 commits unpushed: write auth absent (read works).

- P1a #test-the-call-site-not-only-the-arithmetic: V6-0.2 shipped a tier banner that could never render, and its test file was GREEN with ten passing tests. The tests pinned formatTierBanner, tierBannerAlpha and resetWatermarkIfRegressed — every pure helper AROUND the defect — while the defect itself was a call-site ORDERING fact: the scan ran 29 lines after the array it reads is emptied. The file's own docblock even recorded the reason for the gap ('the draw path cannot be driven headlessly'), so the blind spot was known and accepted rather than hidden. The lesson is that "I extracted the logic into pure functions and pinned it" is NOT a substitute for verifying the wiring; it is a substitute for verifying the LOGIC, and those fail differently. When the untestable part is the part that decides whether the feature runs at all, extract a seam that makes the WIRING assertable — here a source-order lock over main.ts plus a pure capture function whose order-sensitivity can be demonstrated. Both new guards target the call site; neither adds arithmetic coverage.

- P1a #a-guard-that-only-passes-has-not-been-tested: I wrote a source-order lock asserting that every world.effects consumer precedes the wipe in main.ts, and it passed on the first run. That proves nothing on its own — an assertion that can never fail is indistinguishable from one that always passes. So I mutated the source, moving hud.drainTierBanner below effectsRenderer.sync to reproduce the exact S129 arrangement, and confirmed the guard FAILED (1 failed / 8 passed), then reverted and confirmed 9/9. Two minutes, and it converted "the guard exists" into "the guard bites". Same principle as S128's lesson that when an artifact's job is to MEASURE you must audit the measurement path — generalised: when an artifact's job is to CATCH, you must show it catching. Every regression lock should ship with evidence of a deliberate failure.

- P1a #a-blocked-verification-path-may-have-a-side-door: Two sessions running recorded "the visual cannot be verified because a hidden Browser pane pauses requestAnimationFrame, so the Pixi ticker never advances" and substituted pure-function tests. The premise is true but the CONCLUSION was too broad: rAF is only the DRIVER of the ticker, not the ticker itself. app.ticker.update(now) pumps the real frame callback directly, in production order, with no rAF involved — so "is it wired correctly" was answerable all along, and it was exactly the question that mattered. Pushing a genuine SCORE_TIER into world.effects and pumping one frame showed the banner arm, showed world.effects go 1 -> 0 in that SAME frame (proving the read beat the wipe rather than merely sitting earlier in the file), showed the hold, the hide at TITLE, and the re-arm next match. What genuinely remains blocked is far narrower than the recorded blocker: PIXEL output, because a hidden pane's GL context produces no frames. When a capability is recorded as unavailable, re-derive WHICH layer is actually blocked before inheriting the conclusion — the blocked layer is often one level away from the layer you need.

- P1a #a-gate-that-lies-about-unlocking-is-worse-than-one-that-fails: Getting the approved PDR past the write gate exposed THREE independent bugs in the approval handshake, none previously visible. (1) validate_priority_id allows only [A-Za-z0-9_-], so a dotted id like P1-v06-0.3-... is rejected as malformed and glue_pdr_unlock returns without minting unlock_source; S129 used a dotted id too, so its unlock_source=user cannot have come from the hook and must have been hand-written. (2) The mint uses awk sub() on an EXISTING unlock_source line and cannot INSERT the key, so a priority entry lacking it makes the mint a silent no-op — while the hook still prints "Cleared ... Edits permitted" UNCONDITIONALLY, so success and failure are indistinguishable from the output. (3) glue_pdr_unlock writes priority_state='unlocked' but pdca-final-gate.sh accepts only approved|in_progress|completed, i.e. the unlock hook writes a state its own gate rejects. Bug 2 is the dangerous one: I initially believed the mint had succeeded because the hook said so, and only caught it by reading the file back. Trust the STATE, not the announcement — when a control claims to have acted, verify the artifact it was supposed to change.

- P1a #do-not-satisfy-a-checker-by-making-a-false-claim: MCV hard-failed because BACKLOG.md was modified with no absolute-path binding, and reading the verifier showed why my binding was ignored: line 289 collects verification[] ONLY from priorities whose status is 'completed'. The fastest fix would have been to mark the whole V6-0.3 priority completed — which would have been a lie, since the attribution work had not started, and precisely the fabrication class MCV exists to catch. The honest fix was to notice that the priority as RECORDED did not match reality: the banner repair genuinely WAS a complete, committed, independently revertable unit, and the attribution work genuinely was a separate not-started one. Splitting the record to match what actually happened made the checker pass as a side effect. When a gate blocks, ask first whether it has found a real misstatement in the bookkeeping — not how to phrase the claim so it slips through.

## 2026-08-02 — Session 129: V6-0.2 learnability I (rescoped Standard->Micro by A.0) + V6-0.3 pre-scoped + a 23-boot MODEL DRIFT false alarm root-caused. tsc 0, vitest 1932/1932 (+10), bundle 642.3/750 KiB (+1,365 B measured). NOT pushed — GitHub token invalid.

- P1 #probe-the-slot-before-believing-the-roadmap-s-estimate-of-it: V6-0.2 was planned as a Standard-tier 'make the score readable' slot. Ten minutes of A.0 probing showed ~80% already ships - progress bar in all modes, numeric N/1500 + rank + crown in the leaderboard, combos counter, energy gauge, and the tier pulse as a 48-tick ring. The genuine gap was two narrow things, so the slot dropped to Micro. The probe also refuted a claim from my OWN prior audit (that vs-bots loses the HUD). Lesson: a roadmap slot's SIZE is a claim about external state like any other, and Rule 21 applies to it - probe before scoping, and let the scope shrink when the evidence says so rather than padding work to match the plan.

- P1 #verify-the-reviewer-s-MECHANISM-not-just-its-conclusion: GROK-ANALYST correctly found that the tier-banner dedupe watermark was broken, and attributed it to 'world.tick resets on a new match'. That mechanism is false - neither applyStartGame nor applyReturnToTitle touches world.tick. Had I accepted the conclusion and fixed the stated cause, I would have added a match-restart reset and left the ACTUAL path broken: applySnapshotCore's `world.tick = snap.tick` (save.ts:830), which makes a client adopt a lower tick when joining a fresh host after a long local session. Grep-verifying the mechanism produced a two-path fix instead of a one-path one. Third session running that a reviewer was directionally right and mechanically wrong; the conclusion is a hypothesis, the mechanism is the thing to check.

- P1 #read-why-a-prior-decision-was-made-before-reversing-it: The obvious fix for 'the tier pulse is easy to miss' was to move it back to a fixed HUD corner. The SCORE_TIER docblock records that S13 P4 moved it OFF the corner to the placement position deliberately, 'so the pulse lands where the player's eyes already are' - sound reasoning I would have silently undone. Instead the world pulse is untouched and a banner was ADDED to complement it. One grep of the docblock was the difference between reverting a playtested decision and building on it.

- P1 #an-early-return-in-a-draw-method-can-skip-unconditional-cleanup: My first solo-score block ended with `return`, which read as harmless because everything below it was networked-only. It was not: connectionDot.clear() runs UNCONDITIONALLY before its own isNetworked guard, so the early return would have left a stale connection dot on screen after returning from a networked match to solo. Caught by reading what came after the insertion point rather than only what I was inserting. In a per-frame draw method, an early return is a claim that NOTHING below it matters - check the whole tail, especially unconditional clear/reset calls.

## 2026-07-30 — Session 128: v0.6 pivot landed on master (slot V6-0.1) + dev-only economy probe harness. RECOVERED AT S129 HANDOFF — these entries were written to session-state at S128 close but never appended here, because that close was done by hand instead of via /handoff STEP 2.8.A.

- P2 #the-same-blocker-was-nearly-missed-twice-at-two-different-layers: B4 (does a capped exact-type bank destroy the carve-down tactic?) nearly escaped twice. First at DESIGN — the original probe gave free material under carry-1, which forces the old behaviour and can only illustrate starvation; both Council legs caught that independently. Then at IMPLEMENTATION — the metric compared owned-primitive COUNTS, so a placement and a sever in the same window cancelled to zero and carving was under-reported; GEMINI-AUDITOR caught it, GROK missed it. Same question, two layers, both caught by an adversarial reviewer rather than by me. When an artifact's PURPOSE is to measure something, audit the measurement path as a first-class deliverable: "does it run?" and "can it tell the difference?" are different questions.

- P1 #never-run-python--c-through-bash-when-the-payload-has-backticks: applying doc corrections via `python -c "..."` inside a bash command let the shell command-substitute every backticked identifier out of the markdown payload, deleting ~20 symbols from 3 lines while REPORTING "4 of 5 applied" — a true success count over corrupt content. Write a script FILE and execute that. Recurred at S129 in a `git commit -m` string, so the habit is not sticking; promotion candidate for a standing rule.

- P2 #a-pipeline-verification-can-false-pass-because-the-last-command-decides-the-exit-status: `if grep -rl NEEDLE dist/ | grep -v map | head -3; then` ALWAYS takes the then-branch because `head` exits 0 — the check could not have reported a pass for any input, including a clean bundle. Make the check emit a NUMBER and assert on the number. Recurred at S129 reading a review-gate exit code through `tail`.

- P2 #measure-the-delta-by-building-both-sides-rather-than-trusting-a-remembered-baseline: rebuilt the true baseline by moving src/dev aside and git-checkout-ing main.ts; it reproduced EXACTLY at 656186 B, making the delta a precise +114 B rather than "unchanged" or "noise". Matters because the PDR's headline claim was "zero production change" and the honest number is "logic stripped; a 114-byte empty module record remains".

- P2 #a-hidden-browser-pane-freezes-requestAnimationFrame-so-gameplay-cannot-be-driven: synthetic pointer events did nothing and an rAF-awaiting script timed out at 30s, because a non-displayed pane does not composite, so rAF never fires and the Pixi ticker — which drives the whole sim loop — never advances. Static DOM reads and window globals still work, which is what misled me into trying clicks. When a FRAME is required and the pane is unavailable, drop to the reducer level.

- P1 #relabel-a-numbering-scheme-before-introducing-new-references-to-the-old-numbers: the S126-S150 -> V6-x.y relabel had to run BEFORE the factual corrections, because several corrections legitimately ADD references to the real S126/S127; landing them first would have rewritten true historical refs into V6 slot labels — a corruption that greps clean. Range-restrict the rename regex so refs outside the range survive.

## 2026-07-29 — Session 127: soak-lane CI viability — fix the INSTRUMENTS, not the budget. CI-VALIDATED (run 30395046615 SUCCESS, e2e-soak 22m41s, -48%). RECOVERED AT S129 HANDOFF (never appended at the time).

- A0 #probe-external-state-even-when-a-code-comment-states-it: copied "CI minutes are a hard constraint" out of e2e.yml's own header into the A.0 packet as fact. The repo is PUBLIC (`gh repo view --json isPrivate` -> false), so Actions minutes are free — and the stale premise had already anchored a Council recommendation to DELETE the lane. Probe every external-state noun in a premise, including the ones a repo file appears to answer.

- P1 #a-threshold-that-moves-three-times-is-the-wrong-SHAPE-not-the-wrong-number: the census limit went 1500 -> 75 -> 30 -> 40 (the last passing by ONE object) before the dimensional model appeared. Both signal (t/30) and noise (~t/220) scale with the window, so a FRACTION of the signal was correct all along. A value repeatedly overtaken by the next sample is dimensionally wrong, not merely mis-sized.

- CHECK #i-rejected-the-right-shape-using-too-little-data: rejected GROK-ANALYST's tick-scaled census limit citing n=4-plus-an-outlier, in the same session whose PDR said n=3 cannot support a threshold, then adopted the same shape after rediscovering it the long way. When rejecting a reviewer's SHAPE, the bar is a mechanism or a power-adequate sample.

- CHECK #an-early-expect-in-a-shared-helper-voids-every-assertion-below-it: the determinism oracle had NEVER executed in CI for worker-heap:333 because a tick-floor assertion threw first. When a test fails, assertions after the failing line produce NO evidence — never cite them as passing. A continue-on-error lane makes this invisible.

- P1 #cheap-module-evaluation-beats-a-16-minute-feedback-loop: `e2e/**` is not type-checked, so a temporal-dead-zone ReferenceError cost a full 16-minute soak run. `npx playwright test <spec> --list` evaluates the module in ~2s and catches load-time errors.

## 2026-07-28 — Session 126: revive the CI E2E gating lane (3-lane split) + consolidate to ONE deploy path. RECOVERED AT S129 HANDOFF (never appended at the time).

- BOOT #audit-run-conclusions-not-just-that-the-workflow-exists: the S125 handoff's "e2e GREEN" was true of a LOCAL run and masked 3 weeks of dead CI. A job killed by `timeout-minutes` concludes `cancelled`, not `failure` — no email, reads as a benign concurrency cancel, and the SIGKILL destroys the artifacts that would explain it. Fix pattern: set the test runner's own global timeout BELOW the job's timeout-minutes so the TOOL lands the kill and flushes artifacts.

- P1 #measure-the-composition-before-you-tune-the-budget: never extrapolate a per-unit average across a heterogeneous population. S127 then repeated this exact error (linear tick extrapolation) one session after it was written down, which is itself evidence that a written lesson without a mechanical carrier does not hold.

## 2026-07-20 — Session 125: host-migration v2 (zombie auto-rejoin) + F9 INTENT token-bucket, 2/2 shipped + live (51d8b2f feature / 433793a close, Deploy-to-Pages run 29699637227 SUCCESS → production). Full-tier Council R1+R2 + PRIME-AUDIT + CHECK (2-way, Gemini timed out); zero residual HIGH/CRITICAL. tsc 0, vitest 1914/1914 (+13), bundle 640.8/750; both e2e GREEN over real WebRTC (freeze-thaw rejoin 25.5s + D3 takeover 20.2s). Owner-directed batch of input-free carry-forwards. NO protocol bump either priority.

- S125-P1 #reuse-a-shipped-proven-path-beats-new-migration-machinery: Both Council seats filed high-severity criticals on the zombie-rejoin (transport-tear race, permanent demotion, buffer contamination); every one dissolved once the design was recognized as a REUSE of the already-shipped, already-e2e-proven S82 auto-reconnect path rather than new machinery. The deposed original host is just a seat-0 (warrant-excluded) peer that re-runs connectAsClient — so it inherits reconnect's proven robustness and its correctness reduces to two existing tested invariants (fresh-ClientSync epoch fence + seat-0 can't-ladder). The freeze-thaw e2e passed first run because the mechanism was 90% shipped code. When a 'new' feature is re-pointing an existing wired path, the review surface collapses to the seam, not the whole subsystem.

- S125-P2 #size-the-guard-to-the-measured-legit-ceiling: Gemini's priority-inversion finding (a burst could drop a real placement while spending tokens on idempotent avatar-pos) was legitimate, but the proportionate fix for a Med/Small hardening item was to SIZE a single bucket above the measured worst legit burst (avatar-pos is 10Hz SENDER-throttled → a 3s unfreeze buffers ~38 intents « capacity 90) rather than build a two-bucket QoS subsystem. Measuring the legit ceiling first turned a 'needs QoS architecture' finding into a one-constant decision + a documented v2 escalation lever. Reach for structure only after the number says the simple guard can't hold.

- S125-CLOSE #bind-completion-claims-with-verification-not-just-prose: The close INITIALLY hard-failed MCV (verify-session-claims.py exit 1) — both completed priorities UNCOVERED (0 assertions) + the BACKLOG.md watch-root UNBOUND. Every claim was TRUE (gates green, e2e passed, deployed); the failure was a process omission — a rich prose check_method but no machine-checkable verification[] bindings the S149 anti-fabrication gate requires. Prose isn't disk-verifiable; the gate only trusts file_contains/file_exists needles it can re-read. LESSON: at completion, ALWAYS author verification[] binding each modified watch-root + representative per-priority files; treat it as part of 'completed', not an afterthought. A too-rich check_method can mask a missing binding.

## 2026-07-19 — Session 124: host-migration D4 PRODUCTION-ON batch, 3/3 shipped + live-verified. P1 D4 (80f1058): PROTOCOL 14→15, claim ladder 1500ms, monotonic epoch + lowest-seat-wins, zombie demotion w/ partition-evidence anti-grief gate + claim echo, roster-complete hostSeats, fail-closed stamping both paths, pause-only window, LOCKED §13.21; vitest 1901/1901 (+17), e2e 2/2 incl NEW no-seam production test; D4 markers content-verified in the LIVE spark-online.space bundle. P2 (0d1385a): B2(c) BACKLOG drift reconciled (shipped S120 P3 3fc6688). P3 (5756060): F10 render-side heap/census audit — NO LEAK (Δ3.08MB organic, census tracks entities) — F10 closed both halves. DEPLOY DISCOVERY: Actions ALIVE, every master push auto-deploys (billing-lock memory superseded). Full Council R1+R2 + PRIME-AUDIT + CHECK Triumvirate. Ran on Fable 5.

- S124-P1 #triage-external-criticals-against-the-exact-arithmetic: both CHECK seats filed CRITs on the D4 diff; the only real one (post-sign yield window) was found by checking WHERE the await yields, while two others died on exact arithmetic (a rival fires at the SAME epoch, not higher — routing to a different, safe code path) and one was fabricated (invented setTimeout(grace+null) when the code has an explicit ladderDelayMs!==null gate). Triage = re-derive the failing interleaving's numbers yourself against the shipped lines; severity labels from external reviewers carry zero evidential weight.

- S124-P2 #probe-the-backlog-against-git-before-planning: the S123 handoff and BACKLOG both listed B2(c) as open work worth a session slot; a 2-minute A.0 grep found it shipped 4 sessions earlier (S120 P3, 3fc6688) with its own lock test. Roadmap prose drifts; git+code never do. Every priority candidate gets a state probe before it enters a PDR — second instance (after S114 G3b), which makes it a pattern, not an accident.

- S124-P3 #census-decoupling-beats-heap-noise: a heap delta alone cannot distinguish a render leak from organic world growth (Δ3.08MB looked ambiguous until entity counts explained it). The display-object census made the verdict decisive for free: renderer leaks show as census growth DECOUPLED from entity counts — a structural signal immune to GC noise. When auditing a subsystem for leaks, instrument the subsystem's OWN object population, not just bytes.

## 2026-07-12 — Session 123: worker default-on prereqs batch + owner bot-intelligence amendment, 4/4 shipped. P1 VS-BOTS worker support fresh-from-seed via factory seam, bots differential HARD gate + e2e green first run (9f48d50). P2 networked worker-duel e2e — merged cross-mode matrix, remote-INTENT round-trip, wire-cadence bound (a8e073a). P3 dual-isolate GC audit, stabilized floors + raw-CDP worker-heap reads — NO LEAK either isolate, ~20k-tick oracle soak clean (c0eca11). P4 BOT_INTELLIGENCE_DESIGN.md Council-hardened, 7 owner questions (3ba5cf3). tsc 0, vitest 1884/1884, bundle 635.5/750, worker default-on now gated ONLY by the owner weak-device playtest. Ran on Fable 5 Ultracode.

- S123-P1 #verify-the-gates-mechanism-before-trusting-consensus: BOTH external Council seats simultaneously voted bit-exact serialization on the identical claim '(A) breaks the byte-identical differential gate' — a 2-seat consensus that collapsed on a 10-line read of the gate itself (fresh-vs-fresh from a pristine world, not a runtime handoff). Consensus across models is NOT independent evidence when both reason from the same unverified premise about how YOUR OWN test works; read the test before the vote, and weight seat-agreement at ~1 seat when their arguments share a load-bearing factual claim.

- S123-P2 #probe-the-pipeline-before-blaming-the-new-layer: the worker-duel red (joiner sparks=0) LOOKED like a worker-wire bug; a 10-line scratch probe showed host mirror + joiner in PERFECT tick lockstep with 0 hash mismatches — the wire was innocent, the e2e ENVIRONMENT starves the fps-capped sim (drain clamp 3xfps -> ~21 ticks/s -> production 0.15/s spawn rate stretches to ~1 spark/18s wall). The fix was the established spawn-rate seam every sibling spec already used. When a NEW layer sits atop a slow substrate, measure the substrate's rate FIRST — and check what conventions sibling tests adopted, they encode prior sessions' fights with the same environment.

- S123-P3 #a-suspicious-pass-is-a-finding: the v1 heap audit PASSED with -39MB 'growth' — a green that actually indicted the instrument (unsettled floor + wrong isolate). Both CHECK seats caught it because the recorded actuals made the anomaly visible; had the spec only asserted growth<limit without logging numbers, the meaningless pass would have shipped silently. Always RECORD the measured values a threshold consumes, treat a too-good/anomalous pass as a finding to explain, and when a platform API is absent (performance.memory in workers) probe what the platform DOES expose before writing off the measurement (Chromium /json/list turned out to list dedicated workers with their own debugger URLs — a 25-line raw-CDP read, no plumbing).

- S123-P4 #fix-degeneracy-by-sequencing-not-by-knobs: two Council-confirmed design degeneracies (sacrifice thrash loops, raid dogpiles) both had knob fixes on offer (cooldown timers, raider quotas needing shared state) — and both dissolved under a SEQUENCING constraint instead: sacrifice only while already Carrying the replacement piece (no revert window can exist), raider = a pure deterministic function of world state every bot computes identically (no negotiation state). In deterministic lockstep systems, prefer making the bad interleaving UNREPRESENTABLE over damping it with tuned state; knobs need maintenance, sequencing constraints are proofs.

## 2026-07-11 — Session 122: the S121-handoff top batch, 4/4 shipped + live. P1 B2 phase (d) worker cutover — TD-heavy re-measure FAILED the pre-registered ROI rule (structuredClone 10.1ms p95 @6x) → 60Hz transferable positions buffer + structural-batch snapshots; ?worker=1 live opt-in, 300-frame differential HARD gate, mirror-vs-worker hash oracle (82ea2c3). P2 host-migration D3 MIGRATION_CLAIM behind __TEST_MIGRATION__ — kill-host e2e GREEN first run over REAL 3-peer WebRTC, no PROTOCOL bump (5f53b3a). P3 green pulse capped at the income cap in scoring scan order (999e530). P4 deploy live (index-KQaaBM--.js). tsc 0, vitest 1882/1882, bundle 635.4/750, MCV 0. Full 2-round Council + 2 Triumvirate CHECKs: 1 CRITICAL adopted-fixed (allocator repair), 7 refuted with code cites. Actions ALIVE again — both deploy paths ran (owner: pick one). Ran on Fable 5 Ultracode.

- S122-P1 #cross-check-oracle-catches-your-own-layer: the mirror-vs-worker hashWorldState oracle's first live catch was MY OWN prediction layer (drag-preserve restore ran before the hash -> 6 mismatches), not the apply path. Order integrity checks to validate EXACTLY the layer under test (apply), before deliberate divergences (prediction/UX) re-enter. An oracle that fires on designed divergence trains people to ignore it.

- S122-P1 #measure-first-changed-the-design: the pre-registered ROI rule v2 FAILED on the TD-heavy re-measure (structuredClone p95 10.1ms @6x — 5x the stringify cost, refuting the 'simple clone' default BOTH Council seats and I expected to pass) -> the positions-buffer + structural-snapshot format came FROM the measurement, not the deliberation. Pre-registering the rule + the contingency ladder BEFORE measuring made the escalation mechanical instead of a debate.

- S122-P2 #suppress-competing-recovery-paths: the S82 auto-reconnect (transport teardown+rejoin @1s) and the D3 migration grace COMPETE for the same transport — a survivor mid-reconnect-cycle misses the claim broadcast. Seam-gated suppression + idempotent re-broadcast x5 made the e2e deterministic. When two recovery protocols share a resource, serialize them explicitly; do not let timers interleave.

- S122 #both-deploy-paths-ran: with Actions back alive, the master-push auto-deploy AND the manual gh-pages force-push both deployed the same session — they converged harmlessly this time, but dual-writer deploy paths are a race waiting for a divergence. Surface the owner decision at every close until one path is retired.

- S122 #feed-platform-constraints-into-council-prompts: BOTH Council seats bet R1 architecture on SharedArrayBuffer without knowing the deploy target (GH Pages) cannot set COOP/COEP headers. A 5-line PLATFORM CONSTRAINTS block in the R1 prompt (hosting, headers, worker/SAB availability, bundle charter) would have redirected both seats' effort to viable alternatives. Adopted for the S123 PDR template.
