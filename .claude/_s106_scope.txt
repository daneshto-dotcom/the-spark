========================================================================
### TEETH :: SPARK render — chewer creature front buck-teeth restyle (chewerRenderer.ts) to match the rainbow's goofy tooth (rainbowRenderer.ts)
ROOT/GAP: The chewer teeth are RECTANGLES (g.rect) while the rainbow's funny tooth is a downward-pointing TRIANGLE/wedge (g.poly with 3 points, apex down). Boxes read as stiff/ugly; the triangular fang reads as goofy. To make the chewer 'funny like the rainbow's teeth', the two front incisors must become two downward-pointing triangular fangs (keep the pair + comedic gap), instead of two flat boxes.
PROPOSE: In chewerRenderer.ts replace the two-rectangle for-loop body (lines 391-405) with TWO downward-pointing triangular fangs, mirroring the rainbow's g.poly wedge but kept in the chewer's ORIGINAL pencil-sketch style (ivory fill + graphite outline, not the rainbow's flat-white). Concretely, per tooth (s=-1 then +1): compute fang centre `cx = pairCx + s*(toothW*0.5 + gap*0.5)` and draw a 3-point poly with the apex pointing DOWN — top-left, top-right, bottom-apex: `g.poly([cx - toothW*0.5, toothTopY, cx + toothW*0.5, toothTopY, cx, toothTopY + toothH]).fill({ color: TOOTH_COLOR, alpha: fa(1) }).stroke({ width: 2, color: GRAPHITE, alpha: fa(0.95) });`. Keep the existing toothTopY (mouthY - gape*0.35) anchor and reuse toothH/toothW but shrink toothW a touch (e.g. toothW = mw*0.55) so the fangs are chunky-but-pointed like the rainbow's band*1.1 wedge. Replace the rectangle-specific decorations: drop the rounded 'chewed bottom arc' (a triangle already tapers to a point) and replace the full-height vertical seam with a SHORT soft graphite hatch near the top inner edge so it still reads pencil-shaded: `g.moveTo(cx, toothTopY + 2).lineTo(cx, toothTopY + toothH*0.45).stroke({ width: 1, color: GRAPHITE_SOFT, alpha: fa(0.35) });`. Optionally make one fang slightly taller/skewed (multiply that fang's apex x by a small s*offset) to echo the rainbow's deliberately 'crooked tooth' goofiness. Net: two pointed ivory beaver-fangs with a comedic gap, outlined in graphite — funny like the rainbow, still original pencil-sketch, render-only.
FILES: C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark\src\render\chewerRenderer.ts
TESTIMPACT: Render-only cosmetic change inside ChewerRenderer.sync drawing code — no sim state, no NetSnapshot field, no constants exported elsewhere. No deterministic sim values touched (purely visual Graphics calls driven by existing render-only `arc`/`nowSec` interpolation), so replay/determinism is unaffected and no host/client desync. No unit test pins the tooth geometry (tests cover sim/network, not Pixi vector shapes); grep for any snapshot/golden-image test of chewer teeth before shipping — none expected. Visual verification only (run the app, spawn a chewer, eyeball the fangs vs the rainbow pickup).
DETERMINISM: No determinism impact — change is confined to ChewerRenderer's per-frame Graphics drawing (visual layer). It reads only already-computed render values (arc, frontX, mouthY, gape, mw) and emits Pixi draw calls; it writes no sim/world/NetSnapshot state, so host and client renders stay independent of the deterministic simulation and replay is unaffected.
RISK: Low. Self-contained cosmetic edit in one draw block; worst case the fangs render mis-positioned or clip the mouth interior — caught instantly on visual check and tunable via toothW/toothH/toothTopY. No gameplay, networking, or determinism impact. Keep TOOTH_COLOR+GRAPHITE (not the rainbow's flat white) to avoid the ORIGINAL-style regression flagged in memory. | EFFORT: Micro (<10K, single file, ~15-line draw-block swap)
OPENQ: Should the gap between the two fangs stay (current gap=toothW*0.14) or widen for a more comedic 'gap-toothed' look? | Does the owner want the rainbow's literal 'crooked/asymmetric' tooth vibe (one fang skewed/taller) or two symmetric matching fangs? | Keep the chewer's ivory TOOTH_COLOR + graphite outline (recommended, stays original pencil-sketch) vs the rainbow's flat outline-less white — confirm the pencil-sketch styling is preferred so it doesn't read as a rainbow clone.
========================================================================
### WINSCORE :: Match-length scoring tunables (PHASE_1_WIN_SCORE + SCORE_TIER_STEP) in src/constants.ts
ROOT/GAP: Not a bug — a deliberate balance knob. To make a match last ~25% longer the win budget must rise. The tier thresholds are DERIVED-by-convention, not auto-derived: SCORE_TIER_STEP is its own hardcoded constant (210), NOT computed from PHASE_1_WIN_SCORE, so it MUST be bumped in lockstep to preserve the exact-thirds invariant. HUNTER_TRIGGER_SCORE, by contrast, IS auto-derived (constants.ts:539-540: floor(PHASE_1_WIN_SCORE × 0.75)) and rescales for free. 630 × 1.25 = 787.5 is not divisible by 3, so a clean exact-thirds value must be chosen: 786 = 3×262 (−0.19% vs 787.5) or 789 = 3×263 (+0.19%). Recommend 786 / step 262 (closer to exact +25%, even step).
PROPOSE: Two-line lockstep edit in src/constants.ts (RECOMMENDED 786/262 for the closest clean +25%; 789/263 is an equally-valid alternate):\n\n1. Line 300: `export const PHASE_1_WIN_SCORE = readTestWinScore() ?? 630;` → `... ?? 786;`\n2. Line 375: `export const SCORE_TIER_STEP = 210;` → `export const SCORE_TIER_STEP = 262;`\n\nValidation: 786 % 262 === 0 and 786 / 262 === 3 (exact thirds preserved → scoring.test.ts:360-362 stays green; pulses at 262/524, WIN at 786). 786 = 630 × 1.2476 ≈ +24.8% win budget. HUNTER_TRIGGER_SCORE auto-rescales to floor(786×0.75) = 589 (no edit). Add brief 'S105/S106 — 630→786 (×1.248), SCORE_TIER_STEP 210→262 in lockstep, exact thirds preserved; user: +25% match length' notes to the comment blocks at lines 293-299 and 372-374, and refresh the two comment-only mentions of '630' (scoring.ts:121, constants.ts:836) to 786 for accuracy (optional, non-functional). NO other source edits — HUNTER_TRIGGER_SCORE and all tests track the symbol automatically.\n\nNote on '+25% longer': raising the win score raises the SCORE BUDGET ~25%, which maps ~1:1 to duration ONLY for a constant-income (blob) builder. For combo/connected builders whose income ramps, real duration gain is somewhat less than 25% (the S91 note documents this asymmetry: blobs run proportionally longer than builders). If the owner wants +25% wall-clock for the canonical combo build specifically, that needs a playtest pass, not just a constant bump — flag as an open question.
FILES: C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark\src\constants.ts
TESTIMPACT: NO test assertions pin the literal 630, 210, or 472 — every test references PHASE_1_WIN_SCORE / SCORE_TIER_STEP / HUNTER_TRIGGER_SCORE via import (symbolic), so they auto-track. The KEY guard is scoring.test.ts:360-362 (exact-thirds invariant): it passes iff the chosen pair divides evenly by 3 — 786/262 and 789/263 both pass; 787/788 etc. would FAIL (that test is precisely the lockstep enforcer). constants.lock.test.ts does NOT pin the score (only MEMORY_FOG_COLOR), so no lock edit. Win-condition / determinism / replay tests (gameState.test.ts:52-58 income→WIN, scoring.test.ts:307-356 host+client-mirror WIN attribution, session15.test.ts WIN) assert WIN at the symbol and run income-accrual loops — they grow ~25% more ticks but stay under their 100000/120000/200 guards, so they remain green; determinism/replay is unaffected because the change is a single host-side authoritative threshold constant (no RNG, no per-tick branching change) — replays recorded under 630 would diverge at the win boundary, but no committed replay fixtures pin a 630-win were found. e2e specs use __TEST_WIN_SCORE__ override seam (999/3), fully decoupled from production value.
DETERMINISM: Host-authoritative single-threshold change; deterministic and replay-safe within a build. No committed replay fixtures pin a 630-based win were found, so no replay-asset regen needed; cross-build replays spanning the change would diverge only at the win boundary (expected for any win-threshold tuning).
RISK: Low | EFFORT: Trivial — 2 functional line edits + comment refresh, no logic changes, no test rewrites.
OPENQ: Confirm the target metric: '+25% longer' as score budget (this 2-line bump) vs '+25% wall-clock for the canonical combo build' (needs a playtest pass because combo income ramps, per the S91 asymmetry note at constants.ts:288-299). | 786/262 (recommended, −0.19% vs exact +25%) vs 789/263 (+0.19%) — owner preference; both satisfy the exact-thirds invariant identically. | Per global CLAUDE.md this is a user-facing balance change to a 'protected anchor' (scoring.ts:121 / constants.ts:836 call PHASE_1_WIN_SCORE=630 the 'protected anchor') — needs an approved PDR + user 'go' before the edit lands; treat as Micro-tier.
========================================================================
### NONET :: NONET halving is invisible — the HUD progress bar reads the LEADER (max-of-all scoreByPlayer), not the local player's own score. The reducer math is correct; the on-screen "victory points" surface shows the wrong player.
ROOT/GAP: world.scoreProgress is defined as max(scoreByPlayer) — the LEADER's score — and is used for THREE different jobs: (a) the WIN gate (correct — first to PHASE_1_WIN_SCORE=630 wins), (b) the HUNTER_TRIGGER at 75% (correct — should track the leader), and (c) the main on-screen progress bar (WRONG for a per-player 'your victory points' read). drawProgress (ui.ts:205-215) renders scoreProgress/PHASE_1_WIN_SCORE as one shared white bar for everyone, so in a 1v1 it ALWAYS shows whoever is ahead. There is no bar bound to world.scoreByPlayer.get(world.localPlayerId). When the NONET doubles the friend and halves the owner, the leader-max bar can even go UP at the moment the owner is halved — the exact opposite of the felt outcome. The unit test 'winner ×2, others ÷2' passing is a red herring: it asserts scoreProgress===200 (the winner's doubled max), confirming the bar shows the leader, not the loser (sudokuEvent.test.ts:97-103).
PROPOSE: DISPLAY FIX (root cause, ~10 lines, the priority): make the primary progress bar show the LOCAL player's own score, and show the leader as a secondary marker so 'who's winning' is still legible. In src/render/ui.ts drawProgress(world), replace the leader-max read with the local player's own banked score:\n  const localScore = world.scoreByPlayer.get(world.localPlayerId) ?? world.scoreProgress;\n  const target = Math.min(1, localScore / PHASE_1_WIN_SCORE);\n(keep the world.scoreProgress fallback for solo/early-frame before scoreByPlayer is populated — in solo localPlayerId=0 IS the only entry so it equals scoreProgress anyway). Then add a thin leader 'ghost' tick on the same track at world.scoreProgress/PHASE_1_WIN_SCORE (a 2px vertical mark) so the owner still sees the friend pulling ahead — and critically, when the NONET halves the owner the OWN bar visibly drops by half AND the leader ghost jumps up, making the swing unmistakable. Leave the WIN gate and HUNTER trigger reading world.scoreProgress (max) untouched — those SHOULD track the leader. Optionally flash the own-bar red on any frame where localScore decreases (catches every future point-loss vector, not just NONET).\n\nPOINT-LOSS / ANTI-COAST MECHANICS (the owner's competitiveness ask — design proposals, each its own PDR):\n  (1) STEEPER NONET PENALTY: lower NONET_LOSER_MULT from 0.5 to e.g. 0.35-0.4 (loser keeps ~⅓ instead of ½) so losing a NONET is a real gut-punch. One-line constant change (sudokuEvent.ts:23); updates the pinned test. Lowest-risk lever, ships with the display fix.\n  (2) LEADER SCORE-DECAY (the direct 'runaway leader can't coast' answer): in tickScoring, after computing the leader, apply a small per-tick decay ONLY to the current leader's banked score once they cross a threshold (e.g. > HUNTER_TRIGGER_SCORE = 75% of win): scoreByPlayer[leader] -= LEADER_DECAY_PER_SEC × leaderScore / PHYSICS_HZ. This makes a passive lead bleed unless actively defended/extended, and pairs thematically with the existing HUNTER that already targets the leader at 75%. Tunable so it never fully blocks a win (decay < max achievable income).\n  (3) LOSE-A-STRUCTURE-DOCKS-SCORE (turn destruction into real clawback): today destruction only lowers the income RATE (banked score is safe — scoring.ts:13-17). Add a discrete one-shot CLAWBACK when an enemy chewer/raid severs a connector or kills a spawner: dock the victim a fraction of the destroyed component's complexity from banked score (mirror awardSpawnerKillReward's structure but as a negative addScore on the owner, gated to not go below 0). This is the highest-impact competitiveness change and directly rewards aggression against the leader. Bigger PDR (touches the SEVER_BOND / REMOVE_SPAWNER host paths).\nRecommendation: ship the DISPLAY FIX + lever (1) together first (small, decisive, makes the halving felt). Then evaluate (2) leader-decay as the dedicated anti-coast mechanic; hold (3) clawback for a follow-up PDR since it changes the core 'banked score is safe' invariant the S76 economy was built on.
FILES: src/render/ui.ts (drawProgress: bind primary bar to world.scoreByPlayer.get(world.localPlayerId), add leader ghost-tick + optional drop-flash) — THE fix, src/state/sudokuEvent.ts (optional lever 1: NONET_LOSER_MULT 0.5 -> ~0.35-0.4), src/state/scoring.ts (optional lever 2: leader score-decay in tickScoring), src/state/gameMode.ts (optional lever 3: negative-clawback helper on structure/spawner loss), src/render/ui.test.ts or new test (pin: own-bar tracks localPlayerId and DROPS after a NONET loss)
TESTIMPACT: sudokuEvent.test.ts:97-103 stays GREEN under the display fix (scoreProgress untouched). Lever (1) requires editing the loser expectations in that file (lines 101-132). The new regression test should assert that after a client receives a snapshot whose own scoreByPlayer entry was halved, drawProgress' target halves too — locking this exact bug. Levers (2)/(3) need replay-equivalence tests since they add host-side score mutation.
DETERMINISM: All scoring mutations are host-only and serialized: scoreByPlayer rides both the host save and the NetSnapshot (save.ts:623, rehydrated 833-836), and resolveSudoku's float math runs host-only with the result shipped to clients (sudokuEvent.ts docblock lines 99-105). The display fix reads this already-synced state and is render-only — no determinism/replay impact. Any new subtractive mechanic (levers 2/3) must remain in the host tick + serialized scoreByPlayer to preserve replay equivalence.
RISK: Display fix LOW (render-only, solo unchanged); needs a leader ghost-marker so the 'who's winning' read isn't lost. Lever 1 LOW. Lever 2 MEDIUM (tuning/stall risk). Lever 3 HIGH (overturns the S76 'banked score is safe' invariant; needs owner sign-off + floor-at-0). | EFFORT: Display fix: Micro. Lever 1: Micro. Lever 2: Standard. Lever 3: Standard/Full.
OPENQ: Confirm with the owner: should the primary bar mean 'YOUR victory points' (recommended — matches the felt expectation) with the leader as a secondary ghost-tick, or keep the shared leader bar and instead add a distinct second bar for 'you'? | Which anti-coast lever does the owner want first: steeper NONET penalty (cheap), leader score-decay (the dedicated coast-breaker), or structure-loss clawback (most aggressive, breaks the banked-score-is-safe rule)? | If clawback (lever 3) is desired, what fraction of the destroyed component's complexity should be docked, and should it floor at 0 or allow a deficit?
========================================================================
### HELGA :: SPARK tower-defense — Princess HELGA (defender kind 'princess') attack range + slap VFX
ROOT/GAP: (a) Range is just a config constant — raise PRINCESS_SLAP_RANGE to >= the canvas diagonal. (b) Targeting ALREADY picks nearest enemy creature, so a large range alone = she slaps anything on screen, deterministically (no targeting change needed). (c) A screen-wide melee 'slap' does NOT read right: at distance the renderer pops a star-burst on a far enemy while HELGA's hand flails in empty air ~34px from her body, with nothing connecting them. She needs a reach/projectile/arc VFX that visually connects her to the far victim. The repo already has the exact precedent — the turret's drawBeam connects d.pos → lastStrikePos across the whole map (turretRenderer.ts:84-86,131-147).
PROPOSE: Two coupled changes. (1) RANGE (sim/config): in constants.ts change PRINCESS_SLAP_RANGE from 160 to a whole-screen value. Recommended: a named derived constant `export const PRINCESS_SLAP_RANGE = Math.ceil(Math.hypot(CANVAS_WIDTH, CANVAS_HEIGHT)) = 2203;` (or round up to 2400 for margin against verlet drift / off-canvas spawns). Using hypot keeps it self-documenting as 'the whole-screen diagonal' and auto-tracks any future canvas resize. CANVAS_WIDTH/HEIGHT are defined at constants.ts:74-75, well before line 870, so the reference is in scope. No change to defender.ts / defenderLifecycle.ts — acquisition already reads attackRange and already picks the nearest enemy. (2) VFX (render-only, non-deterministic-safe): in princessRenderer.ts add a screen-spanning connector so a far slap reads. Cleanest is to mirror the turret: draw an energy 'slap reach' from HELGA's slap-hand to lastStrikePos during FIRE — e.g. a thick painterly graphite arc / streak (CtCD-consistent) or a lightning-arc, capped with the existing IMPACT star-burst at the far end. Compute the hand world-pos exactly as drawHelga does (sSho + face*sin(sa)*reach for x, sSho + cos(sa)*reach for y) and stroke a quadraticCurveTo to lastStrikePos. This keeps the close-up melee slap unchanged (arc is near-zero length when the enemy is adjacent) and makes a cross-map slap read as 'HELGA flings a slap-shockwave/whip-crack across the screen'. Keep it render-only (wall-clock jitter is fine — the turret does the same) so determinism is untouched.
FILES: C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark\src\constants.ts, C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark\src\render\princessRenderer.ts, C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark\src\state\defenders\defenderLifecycle.test.ts, C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark\src\state\save.replay.test.ts
TESTIMPACT: PRINCESS_SLAP_RANGE is a SIM config constant, so the change is replay/determinism-relevant (the FSM stays a pure fn of world.tick; two same-seed runs remain byte-identical — but the BASELINE snapshot shifts because more chewers now die in range). Tests that pin the old behavior: (1) defenderLifecycle.test.ts:96-106 'with NO enemy in range stays IDLE' — chewer at (900,900) is 1131px away, INSIDE a whole-screen range, so this test WILL FAIL (she'd kill it). Fix: move the chewer beyond the diagonal (>2300px, e.g. (1700,1700) is 2263px out, or use an off-canvas coord) OR re-purpose it to assert a strike. (2) save.replay.test.ts:497-503 'princess defender actually kills chewers' — comment says 'range 160'; assertion is liveChewers<20 which still holds (likely MORE die), but the byte-identical determinism gate at 485-495 will produce a DIFFERENT (still stable) snapshot — fine since it compares two fresh runs, not a stored golden, but update the inline '160' comment. (3) defenderLifecycle.test.ts:86 comment '(160)' is cosmetic — update for accuracy. (4) The VFX change is render-only (PrincessRenderer.sync, wall-clock jitter) — NOT part of the sim, so it has ZERO replay/determinism impact and no sim-test exposure (helgaPose.test.ts only asserts slapReach>0 in FIRE / ==0 in IDLE, which the pose fn keeps unchanged).
DETERMINISM: PRINCESS_SLAP_RANGE lives in the SIM (config feeding the host-authoritative FSM), so it IS determinism-load-bearing: the FSM remains a pure fn of world.tick (no wall-clock, no Math.random; lowest-CreatureId tie-break), and same-seed replays stay byte-identical, but the realized snapshot baseline changes because acquisition now covers the whole screen. Use an INTEGER value (Math.ceil(hypot(1920,1080))=2203 or 2400) to avoid any float drift if the range ever rides a serialized path. The VFX/reach change must be confined to PrincessRenderer (render-only, may use performance.now jitter exactly like turretRenderer) so it has NO effect on the sim or replay; never let the far-reach connector mutate world state or feed back into targeting.
RISK: Low-medium — see body. | EFFORT: Micro (sim+tests) plus a small-to-Standard render VFX pass.
OPENQ: Is whole-screen range a deliberate balance buff for HELGA, or should it be capped (e.g. a large-but-not-infinite radius) so she isn't a strictly-dominant map-wide auto-killer? Owner decision (memory: HELGA is item 10, awaiting owner spec details). | VFX style for the far slap: (a) a graphite slap-shockwave/whip-crack streak (CtCD-consistent, original), (b) a lightning-arc (but Voltkin already owns lightning — risk of visual collision), or (c) a thrown projectile (a flying stein/slap-hand). Owner art call — must stay ORIGINAL per the no-franchise-copying memory. | Exact constant: 2203 (tight diagonal) vs a rounded 2400 (margin for verlet-mobile anchors near a corner + slightly off-canvas spawns). Recommend hypot-derived + ceil, optionally +margin.
========================================================================
### VOLTKIN :: Voltkin in-game render — replace sprite/atlas Pixi.Sprite with a procedural Pixi.Graphics rig (precedent: chewerRenderer + helgaPose/princessRenderer)
ROOT/GAP: The Voltkin is a bitmap sprite (atlas cell / PNG) drawn with `new Sprite(...)`. Any bitmap carries a rectangular texture frame with a hard alpha edge / baked matte, so it reads as a low-quality looping-gif image in a box. The fix the reflexion lesson already validated for the chewer and HELGA — 'a puppet rig IS the real character, beats veo for an action character' — is to draw the creature entirely with Pixi.Graphics (vector strokes/fills, no texture, no rectangle), posed every frame by a pure function of the synced FSM state + ticksInState. Voltkin has not yet been converted; chewerRenderer (one shared Graphics, physics-keyed hop, death-watcher goo) and princessRenderer + helgaPose (authored windup/FIRE/recover keyframe rig) are the two in-repo templates to copy.
PROPOSE: Replace the bitmap Voltkin with an ORIGINAL procedural electric-being rig drawn entirely in Pixi.Graphics, keyed off the synced creature FSM state + ticksInState (NO image, NO texture, NO rectangle). Concretely:

1) NEW pure module `src/render/voltkinPose.ts` (mirrors helgaPose.ts): export `interface VoltkinPose` (e.g. coreScale, coreGlowAlpha, armAngleL/R radians, headTilt, boltCharge 0..1, bodyBobY, jitterAmp) and `export function voltkinPose(state, ticksInState, worldTick, offset=0): VoltkinPose`. Author keyframes off the existing voltkinFrames schedule + LOCKED FSM constants (VOLTKIN_ATTACK_CHARGE_ENGAGE_TICK=15, VOLTKIN_ATTACK_FIRE_TICK=30, ATTACKING_IDLE_RELEASE_TICK=45, CREATURE_SPAWN_TICKS=60, CREATURE_DESPAWNING_TICKS=60, CREATURE_FADE_TICKS=30, VOLTKIN_ATTACK_CADENCE_TICKS=60): SPAWNING = coalesce/grow pulse; SEEKING = idle hover + subtle 2Hz bob + slow body sparks; ATTACKING windup(0-14)=idle, charge(15-29)=boltCharge ramp 0→1 + arms raise + warm tint, FIRE(30)=boltCharge=1 peak/flash punch (emit arcFlash this tick as today), recover(30-44)=ease boltCharge→0, cooldown(45-59)=idle; DESPAWNING = killCount>0 victory crackle else hurt droop, alpha-fade over last CREATURE_FADE_TICKS. Use a smooth() helper like helgaPose. Pure + unit-testable (new voltkinPose.test.ts walking all 4 states × tick range).

2) REWRITE `src/render/creatureRenderer.ts` CreatureRenderer to the chewerRenderer shape: replace the per-creature `Sprite` Map + 6-PNG loader + atlas loader + animCells/spriteFrames/spriteFacings bookkeeping with ONE shared `Graphics` (plus the existing cloudGfx) cleared + redrawn each frame; keep the `creature.type === 'voltkin'` filter, the velocity estimator (lastSeenPos), the facing flip (computeFacing), and the S103 #8 death-watcher → lightningClouds (already pure Graphics — keep verbatim). Each live voltkin: compute pose=voltkinPose(...), then drawVoltkin(g, pos.x, pos.y, face, alpha, pose, nowSec) draws the electric being — e.g. a jagged graphite/cyan body core, a couple of crackling limb-bolts at pose.armAngle, an electric 'mane' of short jittered Graphics polylines whose count/length scale with pose.boltCharge, eye glints, all with the project's stroke/fill style (cyan core 0xeaffff / glow 0x66dddd matching arcFlash). Cosmetic per-frame jitter may use performance.now() (render-only, like chewer wobble); the POSE/timing stays tick-deterministic so host + 1v1 client agree.

3) DELETE the now-dead `voltkinFrames.ts` bitmap layer (VOLTKIN_FRAME_URLS, currentFrameKey/currentSpriteUrl, currentAnimCell, atlas manifest types, FLASH_TINT/FLASH_SCALE_AMPLITUDE if unused) — keep only constants the FSM/audio still import (verify imports first). Remove `voltkin-atlas.png`, `voltkin-anim.json`, and the 6 legacy `voltkin-*.png` from public/ + their build scripts (scripts/build-voltkin-atlas.py, compress-voltkin-frames.py) once nothing references them. Keep arcFlash.ts UNCHANGED.

4) Preserve the render contract exactly: constructor(app, parent=aboveFogLayer), sync(world) reads world & NEVER mutates, clear()/destroy() drain Graphics + maps. main.ts wiring stays as-is.
FILES: src/render/voltkinPose.ts (NEW — pure pose fn + VoltkinPose interface, mirrors helgaPose.ts), src/render/voltkinPose.test.ts (NEW — pure-fn unit tests across all 4 FSM states), src/render/creatureRenderer.ts (REWRITE draw path: Sprite/atlas/PNG layer → one Graphics + drawVoltkin; keep velocity estimator, facing, death-watcher/lightningClouds, clear/destroy contract), src/render/creatureRenderer.test.ts (UPDATE — drop sprite-texture-swap assertions; assert Graphics-based lifecycle + computeSpriteDelta still drives add/remove), src/render/voltkinFrames.ts (DELETE bitmap selectors/atlas layer; trim to only still-imported constants, or remove entirely if none), src/render/voltkinFrames.test.ts + voltkinFrames.anim.test.ts (DELETE/trim with the module), public/godly/voltkin/anim/voltkin-atlas.png + voltkin-anim.json (DELETE — dead Veo atlas), public/godly/voltkin/sprites/voltkin-{idle-1,idle-2,charge,zap,hurt,victory}.png (DELETE — dead legacy frames; verify recipe characterSprite codex-gallery references first), scripts/build-voltkin-atlas.py, scripts/compress-voltkin-frames.py (DELETE — dead pipelines)
TESTIMPACT: creatureRenderer.test.ts currently pins the bitmap path (texture loading / per-tick sprite.texture swaps / spriteFrames/spriteAnimCells) — those assertions must be rewritten for the Graphics path (keep the computeSpriteDelta + computeCreatureAlpha/Scale/Tint/Rotation/computeFacing pure-helper tests if those helpers survive). voltkinFrames.test.ts and voltkinFrames.anim.test.ts pin currentFrameKey/currentAnimCell against the manifest — DELETE with the module. ADD voltkinPose.test.ts for the new pure pose fn (walk every state × representative ticks, assert boltCharge peaks exactly at VOLTKIN_ATTACK_FIRE_TICK=30 and DESPAWNING victory vs hurt branch on killCount, mirroring the helgaPose test pattern). DETERMINISM/REPLAY: arcFlash emit on CREATURE_ATTACK at FIRE_TICK is unchanged, so save.replay.test.ts byte-equivalence is unaffected (render-only change, no world mutation). The pose fn is a pure function of synced state+ticksInState+worldTick (+ id offset) so host and 1v1 client animate identically; ONLY cosmetic performance.now() jitter differs frame-to-frame (invisible as desync, same license chewerRenderer already holds). arcFlash.test.ts unchanged.
DETERMINISM: Render-only change. The pose function is pure: pose = f(state, ticksInState, worldTick, idOffset), all serialized/synced inputs (same pattern as helgaPose + the atlas-cell selector it replaces), so host and the 1v1 client animate identically. No world mutation, so save.replay byte-equivalence and 1v1 host-determinism are untouched. Cosmetic per-frame Graphics jitter may use performance.now() — the same render-only wall-clock license chewerRenderer/princessRenderer already hold (only the sketch 'wobble' differs frame-to-frame, invisible as desync; positions/timing are tick-keyed). arcFlash emit timing on FIRE_TICK is unchanged.
RISK: Medium. Largest item in scope. Risks: (a) creatureRenderer is load-bearing — it also owns the S103 #8 lightning-cloud death-watcher + zap-burst SFX which must be carried over verbatim or kill-VFX regresses; (b) deleting voltkinFrames.ts may break non-render importers (FSM/audio import VOLTKIN_ATTACK_* constants from creature.ts, not voltkinFrames — verify exact import graph before deletion, esp. VOLTKIN_ATTACK_CADENCE_TICKS re-export and FLASH_TINT/isLionClip); (c) art quality — a procedural electric being must look BETTER than the boxed sprite or the change isn't worth it (HELGA/chewer set the bar; expect 1-2 visual tuning iterations); (d) z-order/parent must stay aboveFogLayer so an enemy's Voltkin shows through fog; (e) facing/lean composition (negative scale.x semantics) handled inside Graphics draw, not sprite.scale. Net: cosmetic/render-only, no sim or wire surface touched, fully reversible via git. | EFFORT: L — multi-priority. ~1 day: new pure voltkinPose.ts + tests (mirrors helgaPose, ~1-2h), creatureRenderer rewrite to one-Graphics drawVoltkin while preserving velocity estimator + facing + death-watcher/lightningClouds + clear/destroy (~half day, the bulk), test rewrites + voltkinFrames/asset/script deletion + import-graph verification (~2-3h), plus visual tuning iterations on the electric rig to clear the HELGA/chewer quality bar.
OPENQ: Visual direction for the ORIGINAL electric being: match the CtCD thick-outline/flat-fill palette HELGA uses, or a brighter pure-electric cyan/white crackle (arcFlash palette 0xeaffff/0x66dddd)? Needs an owner art call like the HELGA spec. | Keep the chibi<->lion 'transformation' conceit (idle vs combat form) as a pose-morph, or retire it (it was a bitmap-era frame-set construct)? Recommend retire — fold the menace into the FIRE pose + boltCharge. | Confirm nothing outside the render layer imports voltkinFrames.ts before deleting it (grep: FLASH_TINT, isLionClip, currentFrameKey, VOLTKIN_ANIM_*); the FSM/audio CHARGE/FIRE constants live in creature.ts/voltkin-config.ts and should be unaffected. | Do any Codex/gallery views still reference the voltkin sprite PNGs (recipe.characterSprite = voltkin-zap.png is used by laserTurret/pentagram/princessHelga as a placeholder)? If so, keep voltkin-zap.png until those get their own art, or generate a static still from the rig.
========================================================================
### VERDICTS
[NONET halving is invisible — the primary HUD progress bar reads the leader (max-of-all scoreByPlayer), not the local player's own score. (VOLTKIN finding text was not supplied in the task; only its render-path ground truth could be checked.)] holds=True conf=high :: 
[VOLTKIN in-game render (creatureRenderer.ts Pixi.Sprite/atlas → procedural Pixi.Graphics rig) + NONET score-display verification (ui.ts progress bar vs leaderboard; sudokuEvent.resolveSudoku)] holds=True conf=high :: VOLTKIN finding HOLDS — every load-bearing claim verified against the real code, tried hard to refute and could not:
- creatureRenderer.ts:421 `sprite = new Sprite(initialTexture)`, then per-tick `sprite.texture = this.animCells![animCellIdx]` (430, atlas) or `= desiredTexture` (440, legacy PNG). In-game Voltkin IS a bitmap Sprite whose texture is swapped each tick — the "looping gif." CONFIRMED.
- Both bitmap loaders exist: 6 legacy PNGs via Assets.load(VOLTKIN_FRAME_URLS) (292-306) + atlas/manifest sliced into Rectangle cells (314-345). CONFIRMED.
- voltkinFrames.ts FSM schedule + tick constants (CHARGE_ENGAGE=15, FIRE=30, IDLE_RELEASE=45, SPAWNING_MORPH=30) and currentAnimCell/currentFrameKey selectors are exactly as cited. CONFIRMED.
- arcFlash.ts is already pure Graphics (jittered polyline + corona/halo/core + spark burst, deterministic seed) — NOT the square; keep as-is. CONFIRMED.
- cinematic mp4 (voltkin.ts:264, lumaKey.enabled:false) is a SEPARATE offline-matted intro overlay, not the in-game render. CONFIRMED. Independent corroboration of the "visible square": voltkin.ts:283-291 documents the mp4's baked "transparency checkerboard / user-visible squares" matte problem — strong evidence the Veo bitmaps carry box/matte artifacts.
- Templates real: chewerRenderer.ts (one shared Graphics, velocity estimator, death-watcher goo, facing) and helgaPose.ts (pure pose=f(state,ticksInState,phaseTick,offset) smoothstep keyframes) + princessRenderer.ts (articulated Graphics rig consuming the pose). CONFIRMED. S103 #8 lightningClouds death-watcher already pure Graphics inside creatureRenderer (511-591) — the risk note correctly flags it must be ported verbatim.
- main.ts wiring (constructor + sync(world) + z-order under aboveFogLayer) CONFIRMED.
- DELETION-RISK caveats are real and material (the finding already flags them, so the plan still stands): voltkin-zap.png is `characterSprite` for FOUR recipes — laserTurret.ts:109, pentagram.ts:166, princessHelga.ts:107, voltkin.ts:266 (Codex gallery placeholders) — and sync.test.ts:20 imports currentFrameKey from voltkinFrames. So a naive asset/module deletion WOULD break the Codex + a net test; the finding's own openQuestions/risk say verify-imports-first and keep voltkin-zap.png — heed those. Assets all exist on disk (atlas 553KB, 6 PNGs ~1.2MB, both build scripts present).

NONET VERIFICATION — core claim CONFIRMED with one framing correction. The on-screen PROGRESS BAR (ui.ts drawProgress 205-215) reads world.scoreProgress = max(scoreByPlayer) (scoring.ts:157, sudokuEvent.ts:125) — it is max-of-all-players, NOT the local player's own score. resolveSudoku (sudokuEvent.ts:106-128) does winner ×2 (NONET_WINNER_MULT=2), every OTHER player ÷2 (NONET_LOSER_MULT=0.5), then scoreProgress=max. So a NON-leader's halving does NOT move the white progress bar. BUT the premise "the owner's halving would be invisible" OVERSTATES it: (1) the per-player LEADERBOARD (drawMultiplayerHUD 241-280) DOES render each player's own halved/doubled scoreByPlayer value — it is shown whenever isNetworked && PLAYING (i.e. exactly the multi-player case where a halving-vs-other-player is meaningful; solo has no other player to halve); (2) nonetJuice.ts:146 plays a "your score halved — descending aww" AUDIO cue; (3) since the winner is ×2 and ALL others ÷2, the player holding max almost always changes after a resolved trial, so the bar generally DOES shift. Net: the bar is max-based (true), but the halving is surfaced to the affected networked player via the leaderboard row + audio, so "invisible" is only accurate for the solo progress bar in isolation, not for the actual multiplayer NONET scenario.