# PDR — S135 BATCH (Full tier) — 2026-08-09

**Status:** ✅ COMPLETED — all three priorities shipped, pushed, and verified live (S135). P2 scope amendment executed in full.
**Grounding:** every claim below is anchored to disk (file:line) via the S135 mapping workflow
(`wf_f41487a0-ec8`) and the hunter deliberation (`wf_9b715b7d-3cd`). Nothing here is from memory.
**Authoritative sources:** BACKLOG.md §OWNER RULINGS S134 (:433), §V6-1.1 RULINGS (:484),
§SCORE-IS-CURRENCY (:497), §B6/Naming (:445); SPARK_v0.6_DESIGN.md §5 (castle/cosmos, :237);
owner reinforcement 2026-08-09 (price=105; castle=identical placeholder for every player;
full "build-from-leftover-shapes-in-a-separate-screen" castle = later, its own spec).

---

## P0 — HUNTER LIFETIME SERIALIZATION FIX (folded in; small, decided, fully analyzed)

**OBJECTIVE.** A live SEEKING hunter that survives a worker-INIT restore or host migration
must keep chasing, not instantly give up. Today `serializeHunter` (save.ts:1675) omits the
lifetime and `deserializeHunter` (save.ts:1691) hardcodes `despawnAtTick:0`; the SEEKING gate
`if (world.tick >= hunter.despawnAtTick)` (hunterLifecycle.ts:148) is then always true, so the
rehydrated hunter flips to DESPAWNING on its next tick and `hunterSpawned` blocks any respawn.

**FIX (single serializer path — there is NO trimMirrorHunter; verified).**
- `SerializedHunter` (save.ts:566): add `readonly despawnAtTick?: number`, `readonly spawnedAtTick?: number`,
  `readonly prevPos?: Vec2` — all OPTIONAL (mirrors `SerializedCreature.despawnAtTick?` at :476;
  keeps schemaVersion at 1; pre-S135 saves with a live hunter still parse).
- `serializeHunter`: emit all three.
- `deserializeHunter`: `despawnAtTick: s.despawnAtTick ?? 0`, `spawnedAtTick: s.spawnedAtTick ?? 0`,
  `prevPos: s.prevPos ?? {…pos}`.
- **DECISION — travel all three (full fidelity), per DESIGN-agent rec.** Traveling `prevPos` too
  makes the hunter the first full-fidelity entity, restores its momentum on migration
  (hunterAI.ts:41 reads pos−prevPos), and — the load-bearing reason — is what lets the
  workerSim.differential INIT guard use a real `hashWorldStateFull` equality assertion. Cost is
  ~16 B for the single capped hunter (`hunterSpawned` once-per-game guard, hunterLifecycle.ts:92).
  `?? 0` is safe here because serializeHunter now ALWAYS emits; for a hunter `0` is a benign
  EARLY-ESCAPE, NOT the creature DETONATION — do not parrot the creature "neutral default" framing.

**DOCBLOCKS — grep the WHOLE file; FOUR are stale, not three:**
1. SerializedHunter interface (save.ts:559-565), 2. serializeHunter (1670-1673),
3. deserializeHunter (1685-1689), **4. `workerSim.ts:207-216` "KNOWN VIOLATION, logged not fixed"**
(DESIGN delta — the fourth, on the authority-assuming worker path; false after the fix).
Delete the "host owns the despawn" sentence explicitly — that is the untrue claim that let this survive.

**RECONCILE (INTEGRITY-WARNING — a closed carry-forward must not be left asserting it is open):**
- Strike SPARK_Blueprint.md:725-729 — BUT its three paths are all CREATURE paths S134 already
  fixed; strike it as "the hazard CLASS is now fully closed / last accurate warning of the class,"
  NOT "the hunter fix closed those creature paths." Commit message must not claim the latter.
- Mark BACKLOG carry-forward (:686-712) resolved; update BACKLOG:494 ("close the hunter residual
  FIRST") and :526 ("the hunter case is not [fixed]").

**TEST — `save.hunterLifetime.test.ts` (mirrors save.creatureLifetime.test.ts). Traps the review caught:**
- **Seed via `applySpawnHunter`, NOT raw makeHunter+set** (TEST blocking issue): restore
  reconstructs `nextHunterId = maxId+1` (save.ts:986) and `nextHunterId` IS hashed
  (stateHashFull.ts:291) — a raw-seeded source stays at 0 ⇒ hash-equality is a FALSE-RED even with
  a correct fix. Fixture: makeWorld(0) (seats P0), gameState='PLAYING', tick=500,
  `applySpawnHunter`, then tick=700 ⇒ spawnedAtTick=500, despawnAtTick=2300 (HUNTER_HUNT_TICKS=1800).
- **Seat the target player with a FAR avatarPos; assert `state==='SEEKING'`, NOT `hunters.has(id)`**
  (a buggy hunter lingers 24 fade ticks, so presence FALSE-GREENs; a missing player deletes the
  hunter via the crash-guard hunterLifecycle.ts:130).
- **Do NOT tick before the hash/field assertions** (ticking moves pos ⇒ prevPos≠pos). Order:
  round-trip → assert `hashWorldStateFull(restored)===hashWorldStateFull(source)` + despawnAtTick===2300
  + spawnedAtTick===500 → THEN run 5× `applyHunterTick` and assert still SEEKING.
- Cover BOTH disk (snapshot→restore, the ?worker=1 seam) and wire (netSnapshot→applyNetSnapshot,
  the migration seam) — belt-and-suspenders (they share deserializeHunter; not independent discriminators).
- **Mutation matrix M1-M4, re-run after every fixture change:** M1 deserialize despawnAtTick:0,
  M2 deserialize spawnedAtTick:0, M3 serialize omit despawnAtTick, M4 serialize omit spawnedAtTick.
  serializeHunter/deserializeHunter are unconditional object literals (no branch), so the S133/S134
  "fixture routed around the mutated line" vacuity structurally cannot occur — still run it empirically.
- **Seed workerSim.differential.test.ts** (boot-snapshot step 4): NEW focused `it()` only —
  `buildSoloWorld()`, tick=500, applySpawnHunter, tick=700, snapshot→`makeWorkerSim(INIT)`,
  assert `hashWorldStateFull(sim.world)===hashWorldStateFull(w)`. Do NOT touch the shared
  buildSoloWorld/buildBotsWorld (high blast radius on the 300-frame HARD GATE).

---

## P1 — V6-1.1: GATHERERS + PLACEHOLDER CASTLE + BUY BUTTON + SCORE-AS-CURRENCY

**OBJECTIVE.** The first player-visible feature since the v0.6 pivot: a placeholder keep on the
field that you buy gatherers from with victory points. Delivers the buy-transaction slice; the
bank/deposit productivity (V6-1.3), the ordered build queue + full footer (V6-1.4), and the full
castle entity (V6-1.2) are explicitly OUT.

**R3 — GATHERER IDENTITY = (b) a brand-new entity Map** `gatherers: Map<GathererId, Gatherer>` +
`nextGathererId`. Rejected: (a) freeSparks entry — reaped at 10 s (physicsLoop.ts:221) + rim-snapped
inside the 250 px spawn disc (spawner.ts:456); (c) seated Player — collides with MAX_PLAYERS=6
(constants.ts:68) + iterated by scoring/leaderboard. The "new map is invisible to R1/R2" objection
is mitigated by FIELD_COVERAGE (tsc-forced, keyof World) + one `.size` line in structuralSignature.

**CASTLE = render-only placeholder, NOT a world entity.** A `CastleRenderer` box drawn at each
player's fixed start anchor (avatarRenderer.ts:33-34, P1 spawner-left / P2 spawner-right), identical
for every player (owner ruling 2026-08-09). NO World field, NO save/protocol/hash surface. The full
serialized castle entity is V6-1.2; the build-from-leftover-trophies fortress is SPARK_v0.6_DESIGN §5
(V6-3.x). Resolves the B6 "additive snapshot field" tension: only the GATHERER is serialized state;
the castle is a buy affordance.

**SCORE-AS-CURRENCY (the widest-blast-radius ruling).**
- New `BUY_GATHERER` GameAction (world.ts:158 union + dispatch case near RAID_CREATURE :512), a
  CLIENT intent (register in protocol.ts KNOWN_ + CLIENT_INTENT_TYPES, PROTOCOL_VERSION bump,
  benchGate BENCH_INTENT_POLICY entry — the two-way set-equality test hard-fails otherwise).
- New `spendScore(world, playerId, cost)` helper (NOT raw addScore): guard affordability
  `scoreByPlayer[p] >= cost`, then `addScore(world, p, -cost)` (reuses the exact max-recompute the
  leader-decay/Nonet paths already exercise). `GATHERER_PRICE = 105` in constants.ts near
  PHASE_1_WIN_SCORE (flat, tune-later comment). ONE POOL (BACKLOG:491) — no lifetime-earned total.
- **Monotonicity audit (scoped INTO this slot):** score is ALREADY non-monotonic (leader-decay
  scoring.ts:273-286; Nonet ×0.4 sudokuEvent.ts:113-127) so every read site already tolerates a
  drop. The two GENUINE new deltas: (1) **tier-pulse re-crossing** — SCORE_TIER fires on upward
  500/1000 crossings (scoring.ts:291); a spend that drops below then re-climbs replays the milestone
  banner → add a persistent max-tier watermark in world state. (2) **HUD drop-flash** (ui.ts:529)
  fires red on any own-score decrease → suppress on the spend tick. Confirm the win check
  (gameState.ts:61, reads scoreProgress=max) tracks a leader's spend downward; affordability guard
  makes negative score unreachable (no clamp downstream — ui.ts:206/543).

**GATHERER ENTITY — R15 17-site registration (defenders hit 12/17; template = hunter/defender):**
worldTypes.ts field+nextId · types.ts GathererId/asGathererId · world.ts makeWorld init + dispatch ·
save.ts SerializedGatherer + serialize/deserialize + snapshot emit(size>0) + applySnapshotCore
clear-rehydrate-advance-nextId · stateHashFull.ts FIELD_COVERAGE 'hashed' + GathererHashed +
NoUncovered guard + determinismParts projection · **NOT** stateHash.ts NARROW (R1: keep narrow) ·
workerSim.ts structuralSignature `.size` + buildPositions/applyPositions section (POSITION_SECTIONS
8→9) if it moves · protocol.ts KNOWN_ record · 5 teardown sites (world.ts WIN_TRIGGER, gameState
softReset, gameMode applyStartGame + applyReturnToTitle, godlyActions applyGodlyAbort) ·
gathererRenderer.ts + main.ts construct/clear/sync.

**Gatherer art:** procedural shapeshifting spark, pure fn of (tick, gathererId) at render time,
renderer-only, NEVER world state (BACKLOG:493) — no serialized morph field. Slow cycle, not 60 Hz.
**Gatherer motion RNG:** stateless `mix32(tick,id,salt)` (R4 — no 6th RNG stream, no serialization).
**Movement (if in scope — see OPEN DECISION):** reuse `hunterPursue` (pure, replay-accepted) +
`pickTargetSpark` verbatim; tick in runHostTick fan-out near creatures (hostTick.ts:345), NOT in
workerSim (it re-runs runHostTick), NOT as a bot. A hauled spark needs a new SparkState variant
(don't overload Carried.carrierId:PlayerId) and exclusion from the reap.

**UI:** canvas footer (Pixi, NOT DOM — matches every existing HUD element), reuse the
botSetupOverlay button factory. V6-1.1 ships ONE control (Buy Gatherer 105) but reserve a full-width
footer Container. **NON-NEGOTIABLE: footer-rect early-return at the top of controls.ts onDown (+onUp)**
— the raw pointerdown hit-tests world objects with no HUD guard, so a buy click would also
grab a spark / sever a bond. Relocate the bottom-left progress bar off the footer.

**Naming:** `gatherer` in code AND doc prose, same change. NEVER `Worker` (the Web Worker owns the World).

**R5 DECISION (bind at add-time, not V6-3.1):** WIN_TRIGGER (world.ts:436) destroys entity families
at t=0 of a win. Decide whether gatherers survive into the WIN/POSTGAME ceremony or get a
teardownGatherers. Default: teardown (no ceremony role yet).

**OUT OF SCOPE (fence — do NOT pull forward):** sim-worker default-on flip (SPLIT OUT, gated behind
P0); 6× spawn rate / FREE_SPARK_SOFT_CAP (B3); bank 5 slots + deposit + build-from-bank (V6-1.3);
ordered build queue + full footer bar (V6-1.4); full castle entity + spawner shrink R9/R10 (V6-1.2);
CarryingPlayer deletion (V6-4.3); PHASE_1_WIN_SCORE / SCORE_INCOME retune (B5/V6-4.3). ⚠ The BACKLOG
roadmap-row TITLE (:584) + §523 + SPARK_v0.6_DESIGN §13 still say "+ sim-worker flip" — STALE;
honor the :494 RULINGS table (flip OUT).

**⚠ SCOPING TRUTH to state plainly:** with the bank at V6-1.3, a bought gatherer's haul→deposit loop
does NOT fully close in V6-1.1. A gatherer can exist/be visible/shapeshift/roam, but "productive"
depositing into a bank is V6-1.3. "I bought a gatherer and it doesn't bank anything yet" is expected, not a bug.

**OPEN DECISION (owner) — how much the gatherer DOES in slice 1:** (A) spawns + drifts at the castle,
fetching behavior next slot [strict plan, smallest]; (B) also roams out, grabs a spark, carries it home
[fuller visible loop, pulls movement forward from V6-1.2].

**TESTING.** tsc 0; full vitest green; new gatherer family exercised by the wide differential
harness (FIELD_COVERAGE forces it); BUY_GATHERER reducer unit test (affordability guard, spend,
one gatherer minted); monotonicity: a downward tier crossing renders sanely (no replayed banner, no
stuck/negative tier); bundle + wire delta reported; boot-then-smoke in the browser preview (castle
box visible, buy button spends 105 and spawns a shapeshifting gatherer).

---

## PROCESS
Full tier. Deliberation substrate = the two S135 workflows (6-agent map + 3-agent adversarial hunter
review), disk-grounded (chosen over external Grok/Gemini Council for this disk-heavy task per the
documented reviewer-fabrication risk). PRIME-AUDIT + END-OF-SESSION runtime audit before /handoff.
Gate: write `pdr_approved:true` + `deliberation_completed:true` + `unlock_source:user` at BOTH
top-level AND each priority entry after owner `go`. Commit P0 and P1 separately.

---

# 🔄 SCOPE AMENDMENT — P2 (Rule 16), owner-directed 2026-08-09 after playtesting P1

**Trigger.** The owner played the shipped build and reported the gatherers do not move. Correct —
P1 shipped the buy transaction with a static unit (the approved slice). The owner then specified the
working loop plus four additions. Rule 16: this is NEW scope and gets its own amendment.

## OWNER SPECIFICATION (verbatim intent)
1. **Gatherers MOVE**: go to the spawn zone, take the shape they need, carry it to the castle.
2. **The grab MOVES, it does not disappear** — *"your cruiser spark can grab the shapes that are in
   the castle to build structures and combinations, but only gatherers can grab shapes from the
   spawn zone."* ⇒ the player harvests from their KEEP; the spawn zone becomes gatherer-only.
3. **Spawn zone 50% smaller.**
4. **Gatherer speed upgrade**, ~50 victory points.
5. **Start with 100 victory points.**
6. **Fetch = nearest shape unless filtered**; a per-gatherer preference selector, reachable from the
   castle or by clicking the gatherer.

## RECONCILIATION WITH EXISTING RULINGS (surfaced, not silently resolved)
- **B6 additive-only / `CarryingPlayer` RETAINED — HONOURED.** The owner explicitly chose "not yet"
  on deleting the player grab. `CarryingPlayer` stays fully functional; only its SOURCE narrows
  (spawn zone → keep). No branch, no reducer fork.
- **"START AT 1" (V6-1.1 ruling) — NOW HONOURED.** P1 deviated by making the player buy their first
  gatherer. 100 starting points against a 105 price would otherwise deadlock the economy. Fixed:
  every player starts owning ONE gatherer, plus 100 points (= two speed upgrades, or savings toward
  a second unit). This also removes the P1 deviation logged in the audit.
- **B4 build queue (V6-1.4) — NOT pulled forward.** The per-gatherer preference is the "empty queue
  ⇒ collect nearest" fall-through the B4 ruling already specifies, expressed as a single-type
  filter. It is NOT the ordered/consumed queue and must not become one here.
- **B4b bank = 5 slots — NOT implemented as a cap yet.** Deposited shapes park at the keep as real,
  grabbable sparks. The 5-slot cap + the waiting-gatherer rule stay V6-1.3.

## DESIGN
- **`Spark.escrow?: 'hauled' | 'banked'`** (one additive-optional field). `hauled` = a gatherer is
  carrying it (pos slaved to the gatherer); `banked` = parked at a keep. Sparks with `escrow` set are
  exempt from `reapExpiredFreeSparks` (10 s TTL), `enforceFreeSparkCap` and `enforceSpawnerBounds`
  (the rim-snap that would otherwise yank a hauled spark back into the disc). ⚠ Without this
  exemption the whole feature is impossible — the reap deletes the haul mid-flight and the bounds
  check teleports it home. Banked sparks stay `Free`, so the player grabs them with the EXISTING
  mechanic — no parallel pickup path, no bank entity, B6-compliant.
- **Gatherer FSM**: `SEEKING` (walk to the chosen spark) → `HAULING` (carry to the owner's keep) →
  deposit → `SEEKING`. Deterministic per-tick movement toward the target (pure float math, no RNG,
  no Verlet), ticked in `runHostTick` alongside the other entity fan-outs.
- **Player grab gate**: `controls.pickSpark` rejects any spark INSIDE the spawn disc. Banked sparks
  sit outside it, so they remain grabbable — this is the whole of change (2).
- **Speed**: `speedLevel` per gatherer; `UPGRADE_GATHERER_SPEED` costs 50 and steps every gatherer
  the player owns (one visible button, no unit micro-management in this slot).
- **Preference**: `preferredType: SparkType | null` per gatherer; clicking a gatherer CYCLES it
  (Any → the six primitives → Any) with the current choice rendered as a badge. Minimal, usable, and
  a strict subset of the V6-1.4 surface.

## SCOPE FENCE (unchanged)
Still OUT: the 5-slot bank cap + waiting-gatherer rule (V6-1.3), the ordered build queue and the
full footer control set (V6-1.4), the 6× spawn RATE (B3 — only the RADIUS changes here, which is a
separate owner instruction), the real castle entity (V6-1.2), the sim-worker flip, and any
`PHASE_1_WIN_SCORE` / `SCORE_INCOME` retune (B5/V6-4.3).

## TESTING
Reducer/FSM unit tests (seek → pick up → haul → deposit); escrow exemption proven against BOTH the
TTL reap and the spawner rim-snap; the player-grab gate proven in both directions (spawn-zone spark
refused, banked spark accepted); upgrade affordability + effect; start-state (1 gatherer + 100 pts);
serialization round-trip of every new field on both paths; mutation matrix on each new guard;
boot-then-smoke in a real browser showing a gatherer complete a full haul cycle.
