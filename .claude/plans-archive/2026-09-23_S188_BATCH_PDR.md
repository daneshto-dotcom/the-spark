**STATUS: COMPLETED — P1-P7 shipped (deploy #1, 5c6615f); P8-P11 carried INTO `.claude/plans/2026-09-24_S189_BATCH_PDR.md` (its P1-P6), which is the live plan.**

# S188 — BATCH PDR · THE RACIALS GO LIVE: 12 MECHANICS, THE 17 CARDS, AND THE CASTLE UPGRADE BUTTONS

**Tier:** Full · **Owner approval:** given in advance, verbatim below · **Protocol:** 49 → 50 (substrate, once)
**Merge owner:** the main S188 session. Branches commit LOCALLY to their own branch and never push.

> *"Now what you need to do is to actually wire them, implement them into the game where they need to
> be implemented. Make sure the racial mechanics work. Build everything from the racial mechanics, from
> the upgrades. Don't worry about number four [hub blast / health bar] … the level 10 to level 20 racial
> slots, don't worry about them … let's do zero and five, okay? Because all of those are designed and
> spec'd. You just need to build and wire them."* — owner, S188
>
> *"castle upgrades … wire that in and implement it … if you want to spec it too and build it at the
> same time, then do it."*
>
> *"I pre-approve the full session batch. Do the PDR, consider the then approved and get to work."*
>
> *"I definitely want you to send all the priorities to autonomous independent work trees … you will
> just be the orchestrator that will merge them all … when it lands, they will commit it locally in
> their own branches and then you will go through them and see how it landed, if they need to fix
> anything, you fix it. And then in the end, you will merge everything into main and push it to deploy."*

---

## 0 · READ THIS FIRST — WHERE THE SPEC ALREADY LIVES (nothing below is re-derived)

| source | what it holds |
|---|---|
| `SPARK_CANON.md` §2, §3, §3d, §4, §6, §9b | the stat ladder, the castle, the draft, what is attackable, the wire, retaliation |
| `assets-source/upgrade-cards/MANIFEST.md` | the 17 cards, their names, the title-collision rule, `drawAxisGlyph` deletion |
| `C:\Users\onesh\OneDrive\Desktop\Claude\Founder DNA\Extension Projects\The Spark\.tmp-gates\research\W1.json` | S187's VERIFIED discovery: lanes `lifesteal` (D), `zombie` (E), `wire` (B), `ui` (H), `phase-edge` (A) — each claim with `file:line` and an adversarial verdict |
| `…\.tmp-gates\research\W2.json` → `internal[reuse-mechanics]` | LANE K — teleport, castle summon, rage multiplier, DoT aura, zone membership, the Pharaoh's Ra columns, the per-seat modifier template, the one-live-per-(owner,type) latch |
| §2 of THIS file | the owner's verbatim ruling for every one of the 12 mechanics |

⚠ Line numbers in W1/W2 are from BEFORE S187's five commits. The FACTS hold; re-find the line.

---

## 1 · OBJECTIVE

Every level-0 and level-5 racial upgrade becomes a real, choosable, working mechanic; every draft tile
shows its card; the castle HP/ATK/DEF/PEN upgrades get the buttons the owner can press. Levels 10+
stay COMING SOON (vampires L10 THE SWARM is designed but out of scope by the owner's word).

## 2 · THE TWELVE MECHANICS — HIS WORDS, AND WHAT IS MINE

"Level 0" = the pre-wave-1 draft (draft index **0**, wave 1). "Level 5" = the draft after wave 5's
fight (draft index **1**, **wave 6** — `isDraftWave`, canon §3d). A seat holds a racial perk iff
`player.draftPicks[index] === 'racial'` and `player.raceId` is that race — see `state/racialPerks.ts`.

⭐ **STANDING RULINGS THAT APPLY TO ALL TWELVE:** the floor-at-one rule (*"anything that doesn't ship as
at least a whole number you just give him the lowest amount possible, which is one"*) · the ONE stat
ladder (never invent a scale) · *"if something doesn't work when I play it, I'll just change it … don't
argue if it's too OP"* · R104: the draft never touches a CASTLE stat.

| perk id | name | his words (verbatim, S187) | my calls, flagged at the constant |
|---|---|---|---|
| `vampires.l0` | BLOOD DEBT | *"all spawn have lifesteal. So every spawned unit will get lifesteal. They will heal 20% of each damage output. So let's say if a spawn has 20 a hit, then they would be healed by 4 HP every time they hit someone."* | Every creature the seat owns. Heal = floor-at-one(20 % of the hit's amount), capped at its own `maxEhp`. Counts hits on creatures, connectors (buildings) and castles. |
| `vampires.l5` | CRIMSON TIDE | *"50% life steal for vampires at level five for all units it might be op but we'll see"* | The rate becomes 50 %. It REPLACES 20, it does not add (50 + 20 = 70 is not what he said). Holding L5 without L0 still gives 50. |
| `zombies.l0` | THE RISEN | *"any character that your … zombie characters kill … any racial characters kill. So not like Voltkin or Helga or Pencil Chewers … Every unit you kill is spawned like a one, one, one, one zombie from the castle."* | Killer = one of the seat's RACIAL creatures: `raceUnit`, the zombie tier-3 creature, the zombie tier-9 boss. Victim = any enemy CREATURE. The spawn is one `raceUnit` at the seat's castle (1/1/1/1 = R125, the castle's own zombie). |
| `zombies.l5` | CORPSE EATER | *"once he reaches 20% HP, he starts eating everyone around him … he does the same damage as he would by attacking, but he has 100% life steal on his attack … for like eight seconds … he shouldn't be moving a lot. He moves only in a tiny radius … enemy units first … if there's no enemy units, he eats his own units and heals."* | The zombie tier-9 boss of that seat gains a THIRD skill. Fires ONCE per boss life at ≤ 20 % pool. 8 s = 480 ticks. Leash radius and "own units" = the seat's non-boss creatures are MINE. Art: `assets-source/race-tier9-bosses/zombie-corpse-eater/` + `TILE_AND_REROLL_PROMPTS.md` §3 (PING-PONG the crouch-in descent frames). |
| `mummies.l0` | POWER OF RA | *"once per fight, you can use the power of Ra. So it adds you a skill button … maybe to the left of the tier three tower because there's nothing there … It gives you a skill to call Ra that hits like the lightning beams from the sky, kind of like Pharaoh has. But you get to choose where it lands … you click on it and then you have to click on the area of the map … it will hit and damage buildings or creatures in that area."* | Reuses the Pharaoh's Ra columns (`RA_COLUMN_*`, `raColumnPos`, `drawRaRitual`) centred on the aimed point. Castable during FIGHT only, once per FIGHT. Spares the caster's own shapes and units (the S157 hub precedent) — MINE. |
| `mummies.l5` | ENDLESS DYNASTY | *"every time a castle loses 1,000 points, it spawns a pharaoh. So 1,000 points of health. So from now on and until the end of the game."* | Counts castle HP actually LOST (after castle DEF) from the moment the perk is taken; each whole 1,000 spawns one mummy tier-9 boss (the Pharaoh) at the castle, owned by the seat. Regen does not un-count a loss — MINE. |
| `orcs.l0` | BLOOD FRENZY | *"Every time your orc warlord does rage … all your orc creatures, so any orc spawn on the screen that is currently playing also goes into rage … they move two times faster and they attack two times faster."* + ⛔ *"goblins do not enrage … they don't change their colour and enrage like the orcs would."* | While ≥ 1 of the seat's warlords is enraged, every ORC RACIAL creature of that seat (`raceUnit`, the orc tier-3 creature, other warlords) is enraged too. ⛔ Predicate = ownership **AND** race-creature type. A goblin passes ownership and must FAIL the type test. |
| `orcs.l5` | THE HORDE GROWS | *"the goblin towers allow 20 instead of 10. And also your castle generates the base unit twice as fast."* | That seat's goblin towers: per-spawner cap 20. That seat's castle emitter: interval halved. ⚠ The goblin ceiling is LOAD-BEARING (`persistent`), raise it, never remove it. |
| `demons.l0` | SCORCHED GROUND | *"their quadrant … if it's a four player, then it's a quarter of the map. If it's a two player game, it's half … does damage over time. Anyone who goes into their lands gets a debuff … burning hell … the same mechanic as our zombie boss, two percent of their total HP per second that they're there."* | `zoneOf(pos) === zoneOwner(seat)` (NOT the retired radius `isInsideEnemyTerritory`). The zombie-aura mechanic (`damageOverTime.ts`, 1 fifth per tick, the RATE carries the %) at **20 per-mille** (his 2 %, not the boss's 2.5 %). Enemy creatures only. |
| `demons.l5` | HELLSPAWN | *"the pencil chewers and the pentagram become demonic … when a pencil chewer dies, it spawns two more pencil chewers with half the stats in each. So 50% and 50% of the main one. And when those die, each one of those spawn two more with 25% stats each."* | The seat's chewers only. Generation 0 → 2 × gen 1 at 50 % → 2 × gen 2 at 25 % → nothing. Pool and damage floor-at-one of the PARENT's numbers. "Demonic" look = a red/black tint until he supplies art. |
| `nagas.l0` | DEEP CURRENT | *"allows their gatherers to teleport back to base. So they will go to get a shape and then they will teleport back to base rather than having to walk all the way back … we actually have teleportation already."* | The HAULING leg becomes an instant snap home with the cargo, then the existing deposit. The outbound walk is unchanged. |
| `nagas.l5` | APEX PREDATOR | *"upgrade the tier three piranha into a big one … the stats will be like three times stronger than a regular piranha unit. So all the stats you take and you just triple them."* + (earlier) *"two times bigger than the current piranha"* | A new `CreatureType` `'t3PiranhaElite'`: HP/DEF/ATK/PEN each ×3 of `t3Piranha`, drawn 2× from its own atlas (`assets-source/race-tier3-units/piranha-elite/`). The seat's piranha tower emits it from then on; piranhas already on the board are unchanged ("from now on", the draft's own rule). |

## 3 · SCOPE — ONE SUBSTRATE, THEN SIX BRANCHES WITH DISJOINT-AS-POSSIBLE FILE SETS

### P1 · THE SUBSTRATE — main session, on local `master`, NOT pushed until the final merge

S182 lesson 4: shared infrastructure is handed out BEFORE the split, or every branch reinvents it.

- `DraftPick = GeneralPick | 'racial'`; `DRAFT_PICKS` includes it; `GENERAL_TRACK: GeneralPick[]`.
- `state/racialPerks.ts` (leaf): `RacialPerkId` ×12, `RACIAL_PERKS_BY_RACE`, `RACIAL_PERK_COPY` (title /
  line / detail / card file), **`RACIAL_PERK_BUILT` — all `false`** (each branch flips ONLY its own),
  `racialPerkFor(race, draftIndex)`, `seatHoldsPerk(player, perk)`.
- `draftEvent.ts`: `draftOptionsFor(wave, race)` returns the racial perk or null; `autoPickFor` takes the
  racial when offered (his S187 reversal of R106 — finally live); **`applyDraftChoice` now REJECTS a pick
  that was not offered** (it accepted any axis — a latent bug a modified client could use);
  `playerHoldsPerk(world, playerId, perk)`.
- `state/racial/racialTick.ts`: ONE host-tick hook with one named slot per branch, called from
  `hostTick` after the boss skills and before the death sweep. Each branch replaces its own slot line.
- `PROTOCOL_VERSION` 49 → 50, all six sites. **Branches never touch it**; they report their wire changes
  and the merge owner writes them into the one docblock.

### The six branches

| branch | priority | owns (primary files) | flips `RACIAL_PERK_BUILT` for |
|---|---|---|---|
| `s188/cards` | P2 · wire the 17 cards + make the racial tile live | `render/draftOverlay.ts` (+test), `public/art/upgrade-cards/`, `assets-source/upgrade-cards/MANIFEST.md` | — |
| `s188/castle` | P3 · the castle HP/ATK/DEF/PEN buttons | `render/castlePanel.ts`, `render/characterSheetModel.ts`, `main.ts` (dispatch only) | — |
| `s188/racial-a` | P4 · lifesteal, frenzy, horde, scorched, deep current | `state/damage.ts` (attacker threading + heal), `state/racial/*` (new files), `bossSkillsWarlord.ts`, goblin cap, `raceUnitEmit.ts` cadence, `gathererLifecycle.ts` HAULING | vampires.l0, vampires.l5, orcs.l0, orcs.l5, demons.l0, nagas.l0 |
| `s188/racial-b` | P5 · the risen, hellspawn, endless dynasty | `creatureLifecycle.ts` death arm, `state/racial/*` (new files), `Creature.hellspawnGen`, `Player.dynasty*` (four sites each) | zombies.l0, demons.l5, mummies.l5 |
| `s188/racial-c` | P6 · power of Ra | new `CAST_POWER_OF_RA` intent (protocol records, bench, elimination), `Player.ra*` (four sites), `footerBand.ts`, `input/controls.ts`, `bossAuras.ts` reuse | mummies.l0 |
| `s188/racial-d` | P7 · corpse eater, apex predator | zombie boss skill + `Creature.corpseEater*` (four sites), `CreatureType 't3PiranhaElite'` + configs + atlases + renderer | zombies.l5, nagas.l5 |

**OUT:** L10+ racials (incl. THE SWARM), the hub blast, the health bar (owner: "we'll talk about that
later"), the randomised pool (recorded future direction, canon §3d).

## 4 · A.0 STATE DISCOVERY (Rule 21) — measured, not assumed

Taken from S187's verified lanes (W1/W2 — see §0) plus S188 hand-checks. **The owner ruled that the
handoff and boot ARE the discovery; no discovery fan-out was run.** Hand-checked this session:

| claim | check | result |
|---|---|---|
| castle ATK/DEF/PEN are in the SIM but have NO UI | `grep UPGRADE_CASTLE_STAT src --include=*.ts` minus tests | reducer + allowlists + damage/gun reads exist; **no dispatch anywhere** — `main.ts:980` dispatches regen only. His "we just have regen" is exactly right. |
| tier-3 race towers exist | `state/raceTowerIds.ts`, `CreatureType` has `'t3Piranha'` | ✅ exist — APEX PREDATOR is buildable |
| the draft accepts any pick | `draftEvent.ts:138 applyDraftChoice` pushes `pick` unvalidated | ❗ latent bug — fixed in P1 |
| every racial is dead | `draftOptionsFor` returns `racial: null`; `draftHitTest` returns `'general'` only | ✅ as designed in S187 |
| bundle headroom | S187 handoff: 919 of 1000 KiB | **~81 KiB, SHARED by six branches** — target ≤ 12 KiB each |

## 5 · DETERMINISM & THE FOUR SITES — every branch

- No `Math.random`, no wall clock, no float in the sim. Integer fifths only; `damageEntity` throws on a
  non-integer.
- Every scan that picks something is a TOTAL ORDER — squared distance, then explicit id. Never `Map` order.
- A new wide field = `…Hashed` union + string projection + per-field contribution test + worker. A new
  `CreatureType` = every exhaustive `Record` tsc forces, AND every consumer with a tolerant `default:`
  (S182 lesson 7 — grep for them).
- A spawn of >1 creature of one type for one owner on one tick MUST be exempted from the
  one-live-per-(owner,type) latch in `creatureLifecycle.ts` (it has silently eaten a feature 4 times).
- Nothing fires inside `pendingCreatureDeaths`' deferral window on the wrong side of the sweep
  (`SPARK_RACES_SPEC.md` §9.6).
- Per-strike visuals are DERIVED from synced state each frame, never a one-shot `world.effects` push.

## 6 · PROTOCOL 49 → 50 — one bump, for the RULE

A new discriminant value (`'racial'`) on `CHOOSE_DRAFT.pick`, a new client intent (`CAST_POWER_OF_RA`),
new serialized fields, a new `CreatureType`, and twelve sim rules both peers compute. Any ONE of these
earns it. Taken once in P1; the merged docblock lists every branch's reason (S182 lesson 6).

## 7 · TESTING — owed by every branch, run by every branch, RE-RUN by the merge owner

- `npm run typecheck`, `npx vitest run`, `npm run build` — exit codes read from a captured `$?`.
- Per mechanic, THREE tests (S187's rule): the arithmetic, the wire/hash round-trip where state is new,
  and that the mechanic **REACHES** what it changes through the real host tick (not by calling the helper).
- A negative test per mechanic: a seat WITHOUT the perk, and a seat of ANOTHER race with the pick, are
  unchanged. For BLOOD FRENZY: a goblin owned by the orc seat does NOT rage.
- Mutation-test at least one guard per branch (restore the broken form, watch it go red).
- **Branches do NOT run e2e** (six parallel Playwright lanes on one machine measure each other's load);
  the merge owner runs `e2e:gating` + `e2e:races` on the merged tree.
- The merge owner opens the game after the merge and LOOKS (S187: two bugs were visible only on screen).

## 7b · COUNCIL (Claude + Grok `grok-4.20-0309-reasoning` + Gemini `gemini-3.1-pro-preview`) — BATTLE LEDGER

| # | from | challenge | verdict | why |
|---|---|---|---|---|
| G1 | Grok HIGH | JS key order in save/hash unstable across host/worker | **REJECTED** | the wide hash is a HAND-WRITTEN string projection and save emits explicit fields — no enumeration order is involved |
| G2 | Grok HIGH | `main.ts` dispatch arms merge in nondeterministic order | **REJECTED** | `main.ts` dispatch is client INPUT → intent; it is not sim code. The `damage.callSites` count conflict is expected and is the merge owner's job |
| G3 | Grok MED | THE RISEN's racial-killer reading narrows his intent | **REJECTED** | his words: *"any racial characters kill. So not like Voltkin or Helga or Pencil Chewers"* |
| G4 | Grok MED | per-mille DoT math drifts in floats | **REJECTED** | `dotDueThisTick` is integer by construction and already ships for the zombie aura — use it UNCHANGED |
| M1 | Gemini HIGH | HELLSPAWN at 0 HP loops forever | **ACCEPTED → A2** | the brief already had a generation counter + floor-at-one; A2 adds the tests that PROVE termination |
| M2 | Gemini HIGH | Ra aim coordinates as raw floats | **ACCEPTED → A1** | the host reducer rounds x/y to integers and clamps to the canvas before storing |
| M3 | Gemini HIGH | ENDLESS DYNASTY farmed through regen | **MODIFIED → A3** | his literal words are *"every time a castle loses 1,000 points"* — cumulative loss stays. A per-seat LIVE-pharaoh sentinel is added as a performance backstop (the §9B/chewer-cap philosophy), flagged MINE |
| M4 | Gemini MED | registry flips / protocol bump conflict across branches | **ACCEPTED → A4** | protocol was already substrate-only; A4 isolates each branch's BUILT entries and tick slot in its own comment-separated block |
| P1 | PRIME-AUDIT | a spawn during the strike batch inserts into `world.creatures` WHILE it is being iterated (JS `Map` visits new entries) | **ACCEPTED → A5** | every spawn-on-event mechanic defers its spawns to a list drained after the strike batch |

### The amendments, binding on the briefs

- **A1 (P6)** `CAST_POWER_OF_RA`: the reducer stores `Math.round(x)`, `Math.round(y)` clamped to the
  canvas. A client that sends NaN / Infinity / off-canvas is a no-op.
- **A2 (P5)** HELLSPAWN tests: a gen-2 death spawns nothing; one gen-0 chewer produces at most 6
  descendants in total; every child pool ≥ 1 and every child damage ≥ 1.
- **A3 (P5)** `DYNASTY_LIVE_PHARAOH_SENTINEL = 40` per seat — a pharaoh that would exceed it is not spawned
  and its 1,000 is still consumed. `⚠ MINE, a performance sentinel, never a gameplay cap` at the constant.
- **A4 (P1)** `RACIAL_PERK_BUILT` and `racialTick.ts` are laid out one BLOCK per branch, each separated by
  a comment line, so two branches never edit adjacent lines.
- **A5 (P5, P7, P4 where relevant)** a creature born because of an event mid-strike (THE RISEN, HELLSPAWN,
  ENDLESS DYNASTY) is QUEUED and born after the strike batch, never inserted into `world.creatures` while
  the host tick is iterating it. Prove it with a host-vs-worker differential over a full cycle.

## 8 · ROLLBACK & DEPENDENCIES

Nothing ships until the merged tree is green; `master` is pushed once. Any branch that is not clean at
merge time leaves its perks `RACIAL_PERK_BUILT: false` — the tile stays COMING SOON, which is exactly
today's behaviour, so a failed branch costs its own perks and nothing else. P2–P7 depend only on P1.

---

# THE BRIEFS

Every branch reads §0–§8 above AND its own brief below. Common rules for every brief:

- **You are in your own git worktree on branch `s188/<name>`. Work ONLY there.** `cd` into it for every
  Bash command and use its absolute paths for every Read/Edit/Write. Never touch the main checkout.
- `npm install` first (your worktree has no `node_modules`; do NOT junction another one — the Vite cache).
- **Commit locally to your branch. NEVER push. NEVER touch `master`.** Identity `daneshto@gmail.com`.
- **Do ONLY your brief.** No refactors, no tidying, no drive-by fixes outside your file set (S182 lesson
  5: defects tracked scope of change). If you find a real bug outside your scope, REPORT it; do not fix it.
- **Do NOT edit** `PROTOCOL_VERSION` or its docblocks, `SPARK_CANON.md`, `src/canon.test.ts`, or
  another branch's `RACIAL_PERK_BUILT` entries. Put the canon text you would add, with its constants, in
  `.claude/plans/S188_CANON_NOTES_<name>.md` — the merge owner lands it with its assertions.
- Detect a file's EOL before patching (`file` / byte count); preserve it. Never round-trip text through
  `unicode_escape`. Python writes need `newline=''`.
- Numbers that are yours say so at the constant: `⚠ MINE, not the owner's` + the reason.
- Finish with a report: commits, files touched, every constant you introduced, every wire/hash change,
  gate results with the captured exit codes, what you could NOT do, and anything you suspect.

## BRIEF P2 · `s188/cards` — the 17 cards, and the racial tile goes live

1. Read `assets-source/upgrade-cards/MANIFEST.md` IN FULL. Its "one wiring decision" section is the spec.
2. Ship the cards as runtime art: downscale each 1254² / 784×756 source to a tile-appropriate size (the
   tile is 251 × 242 — ship ≈ 2× for high-DPI, cover-fit, keep the baked title in the top band) into
   `public/art/upgrade-cards/`, lazy-loaded (Pixi `Assets`) so a missing or slow card never blocks the
   panel. Static assets are NOT in the bundle cap, but keep the payload sane (report KB per card).
3. Every tile draws its card: general → `general-<axis>`; racial → `l<level>-<race>`
   (`RACIAL_PERK_COPY[perk].card`). **A tile that HAS a card does not draw the overlay's own title**
   (they collide); a tile WITHOUT one keeps the text title. The effect line and the hover detail stay in
   both states. **Delete `drawAxisGlyph`** (owner: *"just a hand drawn heart that looks gay"*) — deleted,
   not dormant.
4. **The racial tile becomes CHOOSABLE when `draftOptionsFor(wave, race).racial !== null`**: it joins
   the hit-test (`draftHitTest` returns `'general' | 'racial' | null`), gets hover + a detail panel from
   `RACIAL_PERK_COPY`, and `onPick('racial')`. When `racial === null` it stays today's dimmed COMING
   SOON tile, absent from the hit-test. Both states must work and be tested.
5. Replace the hardcoded `/ 5` "owed" arithmetic in `render()` with `seatMustStillPick`.
6. `draftOverlay.test.ts`'s S182 opaque-fill enumeration: a card sprite is not a `.fill({`; a new opaque
   plate still must be hit-tested or declared decorative.
7. Update MANIFEST.md's stale lines (it still lists orcs L5 as UNRULED and the L5 cards as "owner
   generating now") and record what was wired.
8. `l10-vampires.png` is out of scope (L10 is not built) — do not wire it.

## BRIEF P3 · `s188/castle` — the castle HP / ATK / DEF / PEN buttons

1. Read `state/castleUpgrades.ts` (S187 P4) and `state/castleRegen.ts` + `render/castlePanel.ts`'s regen
   button — the regen button is the template, line for line.
2. Four buttons beside the regen upgrade in the castle panel: HP, ATK, DEF, PEN. Each shows its level
   (`/10`), its price (100 VP), what the next point buys (`castleUpgradePreview` — for HP that is the
   CURRENT band's gain, 250…650), and a disabled state that SAYS WHY (the gatherer/regen contract: cap,
   can't afford, benched, eliminated, not your castle).
3. `main.ts` injects the `UPGRADE_CASTLE_STAT` dispatch for the local seat exactly as it does regen
   (`main.ts:980`). Nothing else in `main.ts`.
4. The character sheet / castle readout shows the upgraded HP / ATK / DEF / PEN (`characterSheetModel.ts`
   — find the castle's lines).
5. REACH tests: a click on each button → the intent → the reducer → VP spent, level up, and the effect
   observable through the real host tick (HP pool up by the band gain, shot damage up, incoming damage
   down, PEN on the ladder). A negative test per disabled reason.
6. The S182 fill enumeration applies to every new opaque plate in the castle panel.

## BRIEF P4 · `s188/racial-a` — BLOOD DEBT, CRIMSON TIDE, BLOOD FRENZY, THE HORDE GROWS, SCORCHED GROUND, DEEP CURRENT

Read W1 lane `lifesteal` and W2 `reuse-mechanics` items 1c, 3, 4, 4b IN FULL before writing a line.

- **Lifesteal (V0/V5).** `damageEntity` already carries a required `attacker`. `damageConnector` does NOT
  — thread the attacker through its call sites so a hit on a building heals too. Heal =
  floor-at-one(pct × amount / 100) with pct 20 (L0) or 50 (L5, replacing), capped at the attacker's own
  `creatureMaxEhp`. A dead or absent attacker heals nothing. The green heal number is FREE
  (`damageNumbers.ts` emits on any `ehp` rise) — verify it, do not rebuild it.
- **BLOOD FRENZY (O0).** Rage is `Creature.enraged` + `rageMultiplier(c)` (move + attack cadence). Each
  host tick (your `racialTick` slot), for each orc seat holding the perk: frenzy = any own warlord
  enraged. Set/clear `enraged` on that seat's ORC RACIAL creatures accordingly (no new field — `enraged`
  is already serialized + hashed). ⛔ Goblins: ownership passes, TYPE must fail. The rage tint follows
  the predicate. ⚠ A warlord's OWN rage latch must be untouched by the frenzy clear.
- **THE HORDE GROWS (O5).** Per-spawner goblin cap 20 for that seat's goblin towers (find
  `GOBLIN_MAX_PER_SPAWNER`'s consumer); that seat's castle `raceUnit` emit interval halved (keep it
  tick-derived and phase-spread by seat — `castleEmitsOnTick`).
- **SCORCHED GROUND (D0).** In your `racialTick` slot: every ENEMY creature with
  `zoneOf(pos) === zoneOwner(demonSeat)` takes 1 fifth when `dotDueThisTick(tick, id, type, 20)`.
  Sorted by id. Attacker `null` (area damage) — decide and state what `cause` it uses. A render-only
  ember tint over the zone, derived from picks (no wire).
- **DEEP CURRENT (N0).** Replace ONLY the HAULING `stepToward` walk with a snap home: gatherer
  pos + prevPos, cargo pos + prevPos (the verlet trap — LANE K 1/1c), then the existing deposit path.
  A render-only vortex at both ends, derived from the position jump (no one-shot effect).

## BRIEF P5 · `s188/racial-b` — THE RISEN, HELLSPAWN, ENDLESS DYNASTY

Read W1 lane `zombie` IN FULL (it maps the one death path, `pendingCreatureDeaths`, and killer
attribution) and W2 items 2, 2b, 2c.

- **THE RISEN (Z0).** At the lethality decision in `damageCreature`, when the victim is an enemy
  creature and the attacker is a RACIAL creature of a zombie seat holding the perk: spawn ONE `raceUnit`
  at that seat's castle through the same path the castle emitter uses (`raceUnitEmit.ts`), so it gets the
  seat's draft buffs and shelters/releases like every castle soldier. ⛔ Where the spawn happens relative
  to the death sweep must be proven deterministic (host vs `?worker=1` differential over a cycle).
- **HELLSPAWN (D5).** `Creature.hellspawnGen?: 1 | 2` (four sites). A demon seat's chewer dying at gen 0
  → two gen-1 chewers at the death spot (deterministic spread by id), pool = floor-at-one(50 % of the
  parent's `maxEhp`), damage derived from gen at strike time (floor-at-one 50 % / 25 % of the parent's
  drafted attack). Gen 1 → two gen 2 at 25 %. Gen 2 → nothing. Exempt the latch. A demonic tint on the
  seat's chewers and pentagram (render-only, derived).
- **ENDLESS DYNASTY (M5).** `Player.dynastyHpLost` (four sites; required field, the `castleRegenLevel`
  template). Accumulate castle HP actually lost (after castle DEF) at the castle-damage site while the
  perk is held; each time it crosses a multiple of 1000, spawn one mummy tier-9 boss (the Pharaoh) at the
  seat's castle, owned by the seat, through the tier-9 boss spawn path. Several in one tick (a huge hit)
  is legal — exempt the latch. Reset per match.

## BRIEF P6 · `s188/racial-c` — POWER OF RA

Read W2 items 5 and 5b IN FULL (the Pharaoh ritual, and why there is no point-carrying intent yet).

- New client intent `CAST_POWER_OF_RA { playerId, x, y }` — both allowlist records in `protocol.ts`,
  `benchGate.ts` and `elimination.ts` policies (state and justify each), `world.ts` reducer.
  Host-authoritative, no-op-never-throw: legal only for a seat holding `mummies.l0`, during FIGHT, not
  already cast this wave, a point on the canvas.
- `Player.raCastWave` (+ whatever the strike needs to render and resolve — e.g. the aimed point and the
  cast tick), four sites. The strike resolves in your `racialTick` slot using the Pharaoh's own column
  functions/constants re-centred on the aimed point: `RA_COLUMN_COUNT` columns, `RA_COLUMN_TICKS` each,
  `attackFifths(RA_COLUMN_ATK, RA_COLUMN_PEN)`, `RA_COLUMN_RADIUS`. Hits enemy creatures AND enemy
  buildings (connectors); spares the caster's own.
- The footer skill button, left of the tier-3 chip (his words). Click → targeting mode (cursor telegraph
  using the SAME predicate the reducer uses) → click the board → intent. Esc / right-click cancels.
  Disabled (and saying why) outside FIGHT and after the cast. The S182 footer fill enumeration applies.
- The telegraph + columns render through `drawRaRitual`'s code, derived from the synced state.

## BRIEF P7 · `s188/racial-d` — CORPSE EATER, APEX PREDATOR

Read `ART_PIPELINE.md` BEFORE any art work (memory: the packer needs 12 frames, run `check-clip.mjs`
before packing), `assets-source/race-tier9-bosses/zombie-corpse-eater/PROMPTS.md`, and
`TILE_AND_REROLL_PROMPTS.md` §3.

- **CORPSE EATER (Z5).** The zombie tier-9 boss of a seat holding `zombies.l5`: once per life, at
  ≤ 20 % pool, stamp `corpseEaterUntilTick = tick + 480` (+ whatever anchor the leash needs; four sites).
  While feeding: movement leashed to a tiny radius (MINE), normal attack damage and cadence, target =
  nearest ENEMY creature in reach, else nearest OWN non-boss creature, and every hit heals 100 % of its
  amount (capped at max). Stun gates it like every other action (`stunGates.test.ts`). Render: the eat
  loop (ping-pong the descent frames) while feeding, the stand-and-burp one-shot at the end.
- **APEX PREDATOR (N5).** `CreatureType 't3PiranhaElite'`: config = `t3Piranha` with HP/DEF/ATK/PEN ×3,
  2× draw scale. The naga seat's piranha tower emits it once the perk is held. Every exhaustive record
  tsc forces, plus every tolerant `default:` (grep `'t3Piranha'` and handle each). Atlas from
  `assets-source/race-tier3-units/piranha-elite/` (swim / attack / death) through the art pipeline.
- If the art pipeline blocks on something you cannot resolve, ship the mechanic with a clearly-marked
  fallback (the base sprite, scaled / tinted) and REPORT it — never block the mechanic on art.
