**STATUS: IN-PROGRESS — PLANNED IN S189, EXECUTES NEXT SESSION (owner pre-approved). P1 (deploy #2) DONE.**

# S189 → NEXT SESSION — BATCH PDR · LAND EVERYTHING S188 CARRIED + THE OWNER'S TEN PLAYTEST CORRECTIONS

**Tier:** Full · **Merge owner:** the main session (never a worktree) · **Council:** 2 rounds, ledgers §8
**Owner approval (S189, verbatim):** *"record it as a PDR, and then run a full and complete handoff. And
then next session, you already have the plan ready to be deployed … make sure you know the plan, you've
researched it, you know what to do, you know how to split between the different work trees … I approve it
and I pre-approve it."* — and earlier the same session: *"launch all of those … in different work trees
under different agents … every priority that lands already and that you checked and works feel free to
deploy it and like merge it in. Don't wait till everything has landed … anything that didn't land that
you check that isn't perfect, then you send back to the work trees agent … maybe like five this session,
six … Sandworm will move to the next session."*

⛔ **Why it did not run in S189:** the account hit 96 % of its weekly limit. The owner ordered: plan +
Council + PDR + full handoff here, execute on a fresh account next session. **The next session's job is
to DISPATCH this plan after the boot, not to re-plan it.**

---

## 0 · STATE AT HANDOFF (measured S189, not carried)

| | |
|---|---|
| live | **deploy #2 = master `15035b9`** — `verify-deploy` **4/4** (`VERIFY_DEPLOY_EXIT=0`; LIVE ≠ local by 155 B = build-time TURN injection, the documented pass) |
| gates on that tree | typecheck 0 · vitest 0 (**5996 / 367**) · build 0 (**944.2 / 1100 KiB, headroom 155.8**) · e2e:gating 0 (**69/69**) · e2e:races 0 (**5/5**) — all captured `$?` |
| protocol | **50** (deploy #2 added only `Creature.attackCycleRaged`, additive-optional, emitted when true — no bump) |
| S188's 3 @visual reds | **not real** — load; 69/69 on a quiet machine |
| CI to read at boot | E2E run `35972498981` (15035b9) was in progress at handoff; the E2E run for **5c6615f concluded `cancelled`** — read its reason (memory: *CI can die silently as cancelled*) |
| worktrees ready | `s189-net`, `s189-units`, `s189-weld`, `s189-render` — branch `s189/<name>` at `15035b9`, **`npm ci` done** (exit 0 each). Carried: `s188-input-layer`, `s188-wrath`, `s188-swarm`, `s188-ra-vfx`, `s188-draft-atk`, `s188-canon` |
| read-only research | **NOT DONE** — both S189 workflows were stopped unfinished on the quota order with zero agents complete. Scripts saved: `.claude/plans/s189-workflows/s189-disconnect-hunt.js`, `s189-branch-audits.js` |

## 1 · THE OWNER'S TEN CORRECTIONS (S189 playtest with his brother, verbatim excerpts)

| # | his words | branch |
|---|---|---|
| C1 | *"the upgrades that you have in level zero, level five, level 10, the spark should be one layer above … it gets highlighted when you mouse over it, but the mouse is under it"* | `s189/render` |
| C2 | *"If you connect shapes … to existing towers, like to a laser tower, my brother connected like two triangles … it got his tower disappeared … that's like a regression … we changed it that you can connect towers together … to get them have more HP … as long as the existing tower, the shape is there … it still has a pentagram, but you can connect to it"* (+ *"if it's too difficult for this session, then keep that in mind for next session"*) | `s189/weld` |
| C3 | *"Vulcan attacks his own buildings … instead of going to the right to my zone and starting to attack my buildings, and I had lots of them, he just started attacking the buildings around him"* — he ACCEPTS own-building attacks only once every enemy building is gone | `s189/units` |
| C4 | *"connection was lost at like wave five or something … Need to make sure connection is established and always on"* | `s189/net` |
| C5 | *"it was lagging at about wave five. I thought we fixed the lags"* | `s189/net` |
| C6 | *"Quick match … I'm player one … my brother connects … it switches me to player two puts him as player one … only happens to this workstation … if my brother goes in as player one and then I join him then I'm player two"* | `s189/net` |
| C7 | *"the Naga's gatherer teleport … a big line every time they teleport all over the screen … He should just teleport back to the castle … giving the shape back … then go back to the center … without that weird like laser beam"* | `s189/render` |
| C8 | *"Helga moves behind the map … I built the Helga tower near the castle, and she now moves behind when she does her little patrol … I thought that you added invisible walls around the whole map"* | `s189/units` |
| C9 | *"the down arrow … click on four or like five … then I click in the middle of that tower, the down arrow just brings that whole footer down"* | `s188/input-layer` — **already built in S188, never merged**; merging it is the fix |
| C10 | *"the Kraken sonar sends units flying … outside the map … knock them back a little bit … and stun them"* | `s189/units` |

## 2 · PRIORITIES

| id | what | who | status |
|---|---|---|---|
| P0 | disconnect (C4) + lag (C5) + quickmatch seat (C6) | `s189/net` agent | brief after the hunt |
| P1 | deploy #2 | merge owner | ✅ **DONE S189** — 15035b9, verify-deploy 4/4 |
| P2 | `s188/input-layer` — C9 + draft click-through | merge owner (audit → fix → merge) | carried |
| P3 | `s188/wrath` — WRATH OF RA (mummies.l10), square Ra icon, bots cast Ra, racial-c F1/F4 | merge owner | carried |
| P4 | `s188/swarm` — THE SWARM (vampires.l10) | merge owner | carried |
| P5 | `s188/ra-vfx` — owed tests + gates | agent in its worktree | carried |
| P6 | `s188/draft-atk` — drafted ATK/PEN reach strikes; HELLSPAWN card numbers | agent in its worktree | carried |
| P7 | C3, C8, C10 + four sim LOWs | `s189/units` agent | new |
| P8 | C2 | `s189/weld` agent | new |
| P9 | C1, C7 + two render LOWs | `s189/render` agent | new |
| P10 | canon (from `s188/canon`, re-derived vs the merged tree, with `canon.test.ts` assertions) · stale CLAUDE.md lines (bundle cap says 1000 → **1100**; protocol says 46 → the merged value) · close `S173_NONET_STAGES.md` as SUPERSEDED (canon §9: R182-G withdrew the fixed-seed ladder, R182-H defers adaptive difficulty) · `pdca-context.sh` string-id parse (`~/.claude`, the `pin_progress` warning) | merge owner, LAST | carried |

**OUT:** SANDWORM (owner: next session after this one), levels 15/20. **Owner questions (report, never block):**
APEX PREDATOR bites ×4 and THE SWARM ×11 — both ATK and PEN are multiplied on `ATK × (5 + PEN)`; built as
ruled (*"all the stats you take and you just triple them"*).

---

## 3 · EXECUTION ORDER FOR THE NEXT SESSION (do exactly this)

**Step 0 — boot (gate-enforced), then read CI:** `gh run view 35972498981` and the cancelled 5c6615f E2E run.
Record a verdict on each. Confirm `git worktree list` shows the ten worktrees in §0.

**Step 1 — in ONE message, launch in the background:**
1. `Workflow({scriptPath: ".claude/plans/s189-workflows/s189-disconnect-hunt.js"})` — read-only, 5 lanes + verify + critic.
2. `Workflow({scriptPath: ".claude/plans/s189-workflows/s189-branch-audits.js"})` — read-only audits.
3. The four worktree agents that do NOT wait on research — **units, weld, render, ra-vfx** — each an
   `Agent` (background, NAMED `s189-units` / `s189-weld` / `s189-render` / `s188-ra-vfx`, model
   omitted so it inherits) with its brief from §5 pasted in full. Named Agents, not Workflow agents,
   so a fix round goes back by `SendMessage` with the agent's context intact (the owner's
   *"send back to the work trees agent"*).
   ⚠ The saved scripts hardcode `MAIN` = this repo and read master; the hunt reads master's src, which
   is deploy #2 = the owner's playtest build — correct target.

**Step 2 — when the audits return:** triage per S182 lesson 5 — behavioural defects go back to a branch
agent (dispatch `s188-input-layer` / `s188-wrath` / `s188-swarm` fix agents in their worktrees with ONLY
the confirmed findings, "fix only these"); stale comments / docs / one-line contract fixes the merge owner
does itself. Dispatch the `s188-draft-atk` agent (§5.6) with its triage result.

**Step 3 — when the hunt returns:** write the net brief's DIAGNOSIS section from the VERIFIED findings
only, and dispatch `s189-net` (§5.1). A lane that returned nothing is re-run BY HAND before the brief is
written (S161 rule). If the owner is present, ask him the one discriminating question the critic names
(e.g. whether a tab was backgrounded / reloaded).

**Step 4 — merge trains (one branch at a time; typecheck + full vitest after EVERY merge; e2e:gating +
e2e:races on local master before EVERY push; `verify-deploy` after every push):**

| train | branches, in order | protocol | deploy |
|---|---|---|---|
| A | `s188/input-layer` | none (input only) — confirm from its audit | **#3** |
| B | `s188/wrath` → `s188/swarm` | **→ 51 once** (raStrike→raStrikes reshape; new CreatureType `t3BatSwarm`); ONE docblock listing both reasons (S182 lesson 6) | **#4** |
| C | `s189/render` → `s189/units` → `s189/weld` → `s188/ra-vfx` | bump only if one of them changes the wire or a rule the client computes — decide from each branch's report + the S186 test (*"can two builds that shake hands disagree about anything either computes?"*) | **#5** (split if one branch lags — never hold a green branch for a red one) |
| D | `s188/draft-atk` (after `s189/units`) → `s189/net` | draft-atk adds `Creature.atkFifths?` (changes strike damage both sides compute) → bump; net per its report — **one bump for the train** | **#6** |
| E | canon + docs (P10) | — | **#7** |

⛔ Batch bumps per train (Council M1) — never bump twice for one push. ⛔ A branch that is not clean does
not ride its train; the train leaves without it.

**Step 5 — after each deploy:** open spark-online.space and LOOK (S187: two bugs were visible only on
screen); the preview pane proves static truths only (memory: it never advances `world.tick`).

---

## 4 · RULES EVERY BRIEF INHERITS (paste into each)

- You are in YOUR worktree `…\The Spark\.claude\worktrees\<name>` on branch `<branch>`. `cd` there for every
  Bash; absolute paths for every Read/Edit/Write. Never touch the main checkout, `master`, or another
  worktree. Commit LOCALLY; NEVER push. Identity `daneshto@gmail.com` (`git -c user.email=…`).
- ⭐ **SAVE EVERY STEP** (owner): commit after EVERY completed step, and every commit updates
  `.claude/plans/S189_PROGRESS_<name>.md` (done / in-flight / next / decisions / numbers that are MINE).
  If you are cut off, the merge owner resumes from that file.
- **Do ONLY your brief** (S182 lesson 5). A real bug outside scope is REPORTED, not fixed.
- Never edit `PROTOCOL_VERSION` or its docblocks, `SPARK_CANON.md`, `src/canon.test.ts`. Put canon text +
  constants in `.claude/plans/S189_CANON_NOTES_<name>.md`. Report every wire / hash / shared-rule change.
- **Hotspot files** (`src/state/save.ts`, `src/state/stateHashFull.ts`, `src/state/worldTypes.ts`,
  `src/main.ts`) — Council G1/M4: edit them only with a SELF-CONTAINED block (a new field with its four
  sites; one construction line moved) and list every hotspot hunk in your progress file. No refactors there.
- Determinism: no `Math.random`, no wall clock, no float accumulators in the sim; every target scan is a
  total order (squared distance, then explicit id); never Map-iteration order. New wide field = `…Hashed`
  union + string projection + per-field contribution test + worker. Tolerant `default:` arms are where a
  new value hides (S182 lesson 7) — grep for them.
- The ONE stat ladder (`unitPoolFifths`, `attackFifths`); never a bespoke damage/HP number. A number that is
  yours says `⚠ MINE, not the owner's` + the reason at the constant.
- EOL: detect before patching; many src files are CRLF; Python writes need `newline=''`; never
  `unicode_escape`.
- Tests owed per fix: the arithmetic, a REACH test through the real host tick / real Controls / real
  renderer model (not the helper alone), a negative test, and ONE mutation-tested guard (restore the bug,
  watch it go red, restore). A source-text guard proves a line EXISTS, not that it is REACHED (S182 lesson 2).
- Gates, each exit code from a captured `$?` (never a pipe, never the wrapper): `npm run typecheck`,
  `npx vitest run`, `npm run build`. **Do NOT run e2e** (the merge owner runs it on the merged tree).
  Bundle: ≤ 10 KiB of the shared 155.8 KiB headroom.
- Finish with a report: commits, files, constants introduced, wire/hash changes, gate exit codes, what you
  could NOT do, what you suspect.

## 5 · THE BRIEFS

### 5.1 · `s189/net` — C4 disconnect, C5 lag at wave 5, C6 quickmatch seat  (worktree `s189-net`)

**Established (S189, verified by hand):**
- The overlay (`src/render/connectionLostOverlay.ts:45/53`) is driven by `src/main.ts` ~3627-3747 and
  needs `isNetworked && gameState === 'PLAYING' && (peerCount() === 0 || hostLost)`; terminal after
  `RECONNECT_GRACE_MS = 15_000` (`main.ts:2460`) or the migration deadline (`CLAIM_LADDER_MS` 1500 ×
  `MAX_PLAYERS` + 5000; `HOST_STARVATION_MS` 6000, `src/net/succession.ts:39/78`).
- **A protocol mismatch cannot cause it** — HELLO is refused before the lobby. One tab reloaded/closed
  mid-match DOES cause it (after 15 s) on the other tab.
- A peer leaves the peer set only in `transport.ts:500` `onPeerLeave` (all strategies must lose it); the
  transport is torn down at `main.ts:1779, 1853, 2284` (`teardownNet`), `2586` (migration demotion
  rejoin), `3714` (the client's reconnect retry).
- Quickmatch (`src/net/quickmatch.ts`): seekers join the SMALLEST advertised code; a PEERLESS host that
  hears a smaller code DEMOTES and joins it; a host with peers never demotes. So "first in the lobby"
  does not decide seat 0 — the smaller code does. "Only this workstation" means a per-machine bias —
  find it (code generation, relay set / beacon reach, a stable identity key).
- `playwright.config.ts`: e2e port = FNV hash of the worktree path + vite `strictPort: false`
  (`vite.config.ts:61`) — an orphan vite on that port shifts the new one to +1 while Playwright polls the
  old URL for 60 s. That is S188's unexplained webServer timeout. **In scope:** make the webServer fail
  fast instead (`--strictPort` on the e2e command) — one line, test it.

**DIAGNOSIS:** ⛔ written by the merge owner from the hunt's VERIFIED findings before dispatch.
**Work:** fix the diagnosed disconnect cause(s) with a test that REPRODUCES the mechanism (Council G2);
measure and fix the wave-5 lag with numbers before/after (the S182 wire table, canon §6, is the baseline:
84.0 KiB / 6.88 Mbit/s at 120 creatures — re-measure at a wave-5 racial board with the draft buffs, and
a host frame-time measurement through the real host tick); C6 — **the seat that was in the lobby FIRST
stays player one** (the owner's expectation), deterministic on both sides, with a two-seeker test in
both arrival orders. **File boundary:** `src/net/**`, `main.ts` net/session/reconnect sections,
`playwright.config.ts` webServer line; `save.ts` serializer trims only if the diagnosis names them.
Do not touch `src/net/lerp.ts` (render branch may). Protocol: report; any wire change rides train D.

### 5.2 · `s189/units` — C3 Voltkin, C8 Helga, C10 Kraken + four sim LOWs  (worktree `s189-units`)

Order: C3 → C8 → C10 → LOWs, **one commit each**.
- **C3.** Voltkin (`src/state/creatures/voltkin-config.ts`, targeting in `creatureAI.ts`) must prefer
  ENEMY structures anywhere on the board over its own owner's; own buildings only when no enemy building
  exists (his words). Find why it picks near own structures (a nearest-structure scan without an owner
  filter, a search radius that falls back to own, or `targetsStructures` reuse). Test: brother's layout —
  Voltkin spawned by seat 0 beside seat 0's buildings with many seat-1 buildings across the field walks to
  seat 1's; with zero enemy buildings, it may take its own. Retaliation rules (canon §9b) untouched.
- **C8.** Helga's patrol (WALK inside her hub leash, canon §9b R183-C) must stay on the board: clamp to the
  same bounds every creature uses (`CANVAS_HEIGHT − WORLD_EDGE_MARGIN` etc. — find the creature clamp in
  `physics/creatureVerlet.ts` and why Helga's path bypasses it). Test: a hall stamped at the castle edge,
  a full patrol cycle, every sampled position inside the bounds.
- **C10.** Kraken sonar (`src/state/bossSkillsKraken.ts`, knockback in `physics/creatureVerlet.ts`) flings
  ~1000 px (S188 LOW; the docblock says otherwise). Make it a SHORT knockback clamped to the board + a stun
  through the existing stun system (`stunGates.test.ts` census). Distance and stun duration are MINE —
  flag them; derive from existing constants where one fits.
- **LOWs:** (a) CORPSE EATER's bite timer reads LIVE rage — latch it per cycle the way deploy #2's F3
  `attackCycleRaged` does; (b) castle regen is a % of the flat base `CASTLE_MAX_HP`, not of the upgraded
  pool — R128 is "% of max", and max now includes the bought `hpBonus`; make it read the effective max,
  flagged as a balance consequence in the canon notes; (c) `restore()` re-derives `nextCreatureId` from
  LIVE ids, so a dead creature's id is reused after a save/restore or host migration — **Council M2 fix
  shape: serialize the monotonic counter** (additive-optional with the old derivation as the fallback for
  an old snapshot; four sites); (d) spawn-queue work dispatched outside `runHostTick` is dropped if a
  save lands in that gap — make the queue survive or drain at the boundary; prove with a save mid-gap.
- **File boundary:** creature targeting, Kraken skill, verlet knockback/clamp, Helga movement, castle
  regen, the corpse-eater skill, `save.ts` restore (hotspot rule), the spawn queue.

### 5.3 · `s189/weld` — C2 welding onto a tower must not dissolve it  (worktree `s189-weld`)

Canon §7b **R185-A** (a welded shape draws at FULL opacity), **R185-B** (welding buys pool and makes the
structure UNREPAIRABLE — he endorsed it, *"those two bat towers are a lot harder to destroy because now
they're welded"*), and §8 (`blueprintGroupOf`, `src/state/structureRepair.ts:213`, returns null if any
member has `origin === null`). Find the path that makes a tower VANISH when foreign shapes bond on —
likely recipe recognition demanding the component equal the blueprint rather than CONTAIN it (compare
`isStarAt`, `src/state/godlyRecipes/starShape.ts:98`, which S158 B2b already fixed to walk the hub's own
bonds). A tower exists while its own recipe members and bonds are intact, whatever else is welded on.
Tests (all owed): two triangles welded onto a laser turret → turret stays, fires, keeps its sprite; a
pentagram with an extra triangle → still a pentagram; **two towers welded together → BOTH survive**
(Council M3); cutting one of the tower's OWN connectors still levels it; the welded shape is alpha 1
(R185-A); FIX refused on the welded structure (R185-B); host-vs-worker hash differential over a cycle.
⚠ Owner: *"if it's too difficult for this session, then keep that in mind for next session"* — if the fix
needs a recognition rewrite beyond an afternoon, STOP after the diagnosis + failing tests and report the
real cost (file by file) rather than shipping half. **File boundary:** recognition / structure lifecycle /
tower cover (`render/towerRenderer.ts`, `structureRamp.ts`) only.

### 5.4 · `s189/render` — C1 spark above the draft panel, C7 teleport line + two LOWs  (worktree `s189-render`)

- **C1.** Pixi z-order is `addChild` order (canon §7b R183-G). `main.ts:619` builds `SparkRenderer(app,
  fogHiddenLayer)`, `:803` `DraftOverlay`, `:896` `AvatarRenderer` — find which one draws the thing that
  follows his mouse and why it sits under the panel (a container / layer, not only construction order).
  Make it draw ABOVE the draft panel (and state whether it should also be above the castle panel / footer
  cards — do not change those unasked). `main.ts` edits = moving existing construction lines only (Council
  M4); a source-text guard pins the order and states its own limit (no renderer runs under vitest).
- **C7.** DEEP CURRENT (`src/state/racial/deepCurrent.ts`, `render/gathererRenderer.ts`) — a line across the
  screen on every teleport. Find it: the S188 "render-only vortex at both ends derived from the position
  jump" drawn as a segment between the ends, a tether gatherer↔cargo, or client interpolation (`src/net/lerp.ts`)
  sliding across the jump. Target, his words: vanish → appear at the castle → deposit → walk back to the
  centre. No line, no beam. Test on the render model: a teleport tick produces no segment spanning the jump.
- **LOWs:** (a) a same-tick heal hidden inside a NET damage floater (`damageNumbers.ts` emits on the net
  `ehp` delta — show both, or state why not); (b) the elite piranha has no fallback sheet — a missing
  atlas must fall back to the base piranha at 2× like the swarm does, zero errors.
- **File boundary:** the renderers named, `main.ts` construction lines, `src/net/lerp.ts` only for a
  teleport snap.

### 5.5 · `s188/ra-vfx` — the owed tests  (existing worktree `s188-ra-vfx`)

Its `.claude/plans/S188_PROGRESS_ra-vfx.md` "NEXT SESSION MUST DO" lists the `raStrikeArt.test.ts` cases
exactly (timeline sums, manifest pins, sprite path via `setRaStrikeArtForTests`, missing-atlas fallback,
`afterEach` reset). Do those, then gates. Then merge `master` into the branch and re-run gates (it is 32+
behind). Mechanics must stay unchanged — `git diff b5c9fc9 -- src/state src/net src/bots` empty.
⚠ It and `s188/wrath` both touch the Ra drawing path and the `l10-mummies` card — the merge owner merges
wrath first (train B) and resolves.

### 5.6 · `s188/draft-atk` — finish  (existing worktree `s188-draft-atk`, dispatched in Step 2)

Its progress file has the 7-step plan, all `pending`; its last commit is an UNVERIFIED salvage. First
`git merge master` into it (base 2703365 is behind), then: step 1 confirm the bug on the code (a drafted
ATK/PEN pick never reaches a creature strike; HELLSPAWN card 7/5 vs sim 3/2) with a failing REACH test;
then the plan. Enumerate EVERY production strike site (`grep -rn "attackFifths(\|damageEntity(" src
--include=*.ts | grep -v test`) — the plan lists ten; find the rest. Merge after `s189/units` (train D).

### 5.7 · fix rounds for `s188/input-layer`, `s188/wrath`, `s188/swarm` (Step 2)

Only the audit's VERIFIED behavioural findings, one agent per branch in its own worktree, "fix ONLY these,
do not refactor". Known before any audit: wrath's `raStrike → raStrikes` and swarm's `t3BatSwarm` owe
**51** (the merge owner writes it, once, for train B); swarm's progress file lists merge-owner
reconciliations (`build-upgrade-cards.py`, the 16-card test, `RACIAL_PERK_BUILT` blocks); input-layer's
progress file records its `draftOverlay.ts` conflict resolution.

## 6 · A.0 STATE DISCOVERY (Rule 21, measured S189)

| claim | check | result |
|---|---|---|
| the @visual reds are real | e2e:gating on a quiet machine | ❌ load — 69/69 |
| the disconnect is a protocol mismatch | `main.ts:3640`, HELLO path | ❌ impossible mid-match |
| deploy #2 changes the wire | diff of save/hash/worldTypes | one additive-optional field — no bump |
| the webServer timeout is a mystery | playwright + vite config | hashed port + `strictPort:false` + an orphan vite |
| `S173_NONET_STAGES.md` is live work | canon §9 | ❌ SUPERSEDED (R182-G, R182-H) |
| verify-deploy failure after push | re-run after a main-checkout rebuild | stale `dist/`, not the deploy — 4/4 |

## 7 · ROLLBACK

Each branch merges alone; each deploy is one push; `git revert -m 1 <merge>` + push reverts it. A branch
that is not clean stays unmerged and costs only its own items.

## 8 · COUNCIL — BATTLE LEDGERS (Claude + Grok `grok-4.20-0309-reasoning` + Gemini `gemini-3.1-pro-preview`)

### Round 1 — the scope
| # | from | challenge | verdict | why |
|---|---|---|---|---|
| G1 | Grok HIGH | freeze branches; one integration branch | **REJECTED** | owner ordered parallel + piece-by-piece; one-at-a-time merge with gates IS the integration (S182 lesson 3) |
| G2 | Grok HIGH | block deploy #2 on the hunt | **REJECTED for #2 / ACCEPTED for net** | #2 touches no `src/net`; the net fix must ship a test reproducing its mechanism |
| G3 | Grok MED | tests before sign-off; canon before the bump | **ACCEPTED / REJECTED** | canon describes the MERGED tree → last |
| G4 | Grok HIGH | no deploys until the owner returns | **REJECTED** | he authorized piece-by-piece deploys; its "vanish-then-recreate hack" cites a patch that does not exist |
| M1 | Gemini HIGH | batch protocol bumps | **ACCEPTED** | one bump per train, §3 |
| M2 | Gemini HIGH | `nextCreatureId` a serialized monotonic counter | **ACCEPTED (fix shape)** | it misread the LOW; the amendment is the fix → §5.2(c) |
| M3 | Gemini HIGH | refuse welds that join two towers | **REJECTED / test ACCEPTED** | reverses R185-B; two welded towers must both survive → §5.3 |

### Round 2 — the split
| # | from | challenge | verdict | why |
|---|---|---|---|---|
| G1 | Grok CRIT | merge owner owns all hotspot-file edits; agents send diffs | **MODIFIED** | agents edit hotspots only as self-contained blocks listed in the progress file (§4); full ownership would bottleneck six branches on one session |
| G2 | Grok HIGH | a linear dependency chain across all branches | **PARTIAL** | render/weld/units have no code dependency; ACCEPTED only `units → draft-atk` (both near creature birth) as merge order |
| G3 | Grok HIGH | a 7th integration worktree; drop progress files | **REJECTED** | the main checkout IS the integration tree, gated after every merge; progress files are the owner's *"save every part"* order |
| M1 | Gemini CRIT | bump thrash; bundle wire changes | **ACCEPTED** | the train table, §3 |
| M2 | Gemini HIGH | extract the spawn-queue fix before draft-atk | **REJECTED as a blocker / order ACCEPTED** | ATK baking happens in `makeCreature` whoever calls it; the LOW is about queued work lost across a save. Order units → draft-atk kept |
| M3 | Gemini MED | no net dispatch before the hunt | **ACCEPTED** | Step 3: the net brief's diagnosis is written from verified findings first |
| M4 | Gemini MED | render must not restructure `main.ts` | **ACCEPTED** | construction-line moves only (§5.4) |
