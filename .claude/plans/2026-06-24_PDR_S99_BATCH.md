# PDR — S99 Batch (Standard tier, user-approved 2026-06-24)

> Gate: `unlock_source = user` ("I approve full session priority batch ... add a few other small fixes").
> Two playtest bug-fixes. Deliberation: 1-round 3-way Council (Standard) + PRIME-AUDIT.

## P1 — NONET spirit sprites fade into the background (Micro)
**OBJECTIVE.** The 3 shared-mask spirits (kami, owl-a, owl-b excluded, moss-b) show their FULL outline/wings during NONET, naturally integrated, without re-exposing owl-b's cropped flame.
**ROOT CAUSE.** `public/art/nonet/spirit-mask.png` (the default for kami/owl-a/moss-b — their GUARDIANS entries have no `mask` field, `sudokuOverlay.ts:394-417` falls back to `spirit-mask`) peaks at **alpha 231/255 (never opaque)** and feathers ~93% of the frame (make_mask.py defaults inset=0.06/feather=0.30 → 81px blur). The ≥50% core is only ~57% of half-width → wings melt into the dark bg. owl-b looks correct because `owl-b-mask.png` (S97 P1) hits full 255 + a hard cutoff that ALSO crops its stray veo flame.
**APPROACH.** Regenerate ONLY `spirit-mask.png` with a high plateau (peak 255, large opaque core, feather just the outer rim) — via `assets-source/nonet-video/make_mask.py` (add a plateau clamp `min(255, v*~1.7)` after blur, or tighter `--inset/--feather`). **Leave `owl-b-mask.png` byte-identical.** Optional complement: a soft warm backing glow behind each spirit (sudokuOverlay buildGuardian) if contrast still reads low — lead with the mask.
**TESTING.** ffmpeg masked-composite of each of the 3 spirits through the new mask over dusk → full owl/spirit body+wings opaque, only true corners feather, no hard rectangle edge; owl-b unchanged (flame still cropped). tsc 0 (no code change unless glow added); build (PNG is off-bundle); LIVE preview: 3 spirits read fully, owl-b clean, 0 console errors.
**OUT.** Re-encoding the .webm vignette (heavier; mask fix is sufficient); touching owl-b.
**ROLLBACK.** `git checkout public/art/nonet/spirit-mask.png`.

## P2 — Voltkin doesn't fire when a structure is REDUCED (by deletion) to a valid chain (Standard)
**OBJECTIVE.** Severing/deleting bonds down to a valid Voltkin chain (clean isolated SQ×4→TR×4) re-evaluates the recipe and fires — not just building up to it.
**ROOT CAUSE.** The godly matcher runs ONLY on bond creation: `runGodlyMatcher` (`godlyOrchestration.ts:73-90`) hard-filters `eff.kind === 'BOND_FORMED'` before calling `findGodlyMatch`. The sever path (`severBond.ts:79-86`) emits only `BOND_SEVERED`/`SEVER_ERASE` → deleting down to a valid chain never re-runs the matcher. The `godlyFiredThisMatch` gate is NOT the cause (first-time Voltkin passes; NONET is a different id). SECONDARY: the recipe needs a STRICT isolated linear chain (S48 P4 anti-false-positive gate, `voltkin.ts:147-200`) — endpoints degree 1, middles degree 2, no off-chain bonds — so a reduction leaving stubs/junctions also fails even if re-evaluated.
**APPROACH.** In `runGodlyMatcher`, ALSO act on `BOND_SEVERED` effects → run `findGodlyMatch` against the post-sever world at the sever pos. De-dupe to **one matcher scan per frame on sever** (a sever emits multiple SEVER_ERASE + one BOND_SEVERED; the existing `break` caps triggers, add an early-exit so the O(prims) DFS scan runs once). KEEP the strict topology (relaxing reopens the S47 5-square-blob false-positive). Host-authoritative path unchanged (stays inside `runGodlyMatcher`, `world.isHost`-gated, broadcasts GODLY_TRIGGER). Runs on the already-mutated world (BOND_SEVERED pushed after `applySeverTopology` — safe).
**USER COMMUNICATION (not code).** For Voltkin the reduced structure must end as a CLEAN isolated line: 4 squares then 4 triangles, endpoints degree-1, middles degree-2, NO leftover stub bonds/junctions. The `?debug=1` overlay's `findLongestVoltkinPartial` shows how close a chain is. With the trigger fix, a clean reduction WILL now fire.
**TESTING.** New tests in `voltkin.test.ts` / `godlyReducer.test.ts`: (1) build 4Sq-4Tr-plus-extra-bond, dispatch SEVER_BOND to leave a clean isolated chain, run `runGodlyMatcher` (host) → asserts GODLY_TRIGGER 'voltkin' fires; (2) negative: sever leaving a non-isolated/junction residue → does NOT fire; (3) the `godlyFiredThisMatch` gate still permits the first post-sever fire. tsc 0; full vitest green.
**OUT.** Relaxing the strict-isolation topology (would reopen S48 false-positive — surface as a SEPARATE design decision if the user wants "any 4S+4T blob"). Client-side sever-trigger (would desync).
**ROLLBACK.** Revert the `runGodlyMatcher` filter change + tests.

## BATCH RISKS
- R1 (P1): tightening the mask too far could re-show a rectangle edge on the 3 spirits' own framing → verify with masked composite on all 3; the baked vignette mitigates.
- R2 (P1): owl-b MUST keep `mask:'owl-b-mask'` + byte-identical mask → flame stays cropped.
- R3 (P2): multiple sever effects/frame → cap to one DFS scan/frame.
- R4 (P2): host-only matcher — fix stays inside runGodlyMatcher (no client path).
- R5 (P2): topology unchanged → the user's exact structure may still need to be a clean line (communicated).

## DELIBERATION
1-round 3-way Council (Claude + Grok-ANALYST + Gemini-AUDITOR) + PRIME-AUDIT.
