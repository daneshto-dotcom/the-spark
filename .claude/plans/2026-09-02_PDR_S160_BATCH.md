═══════════════════════════════════════════════════════════
    PDR — S160 BATCH
    Boot-snapshot §1–§3, then §4 (CASTLE GUNS) via the owner's races roadmap
═══════════════════════════════════════════════════════════

Tier: **Full** (4 priorities, one PROTOCOL_VERSION bump 38 → 39)
Session: S160 · 2026-09-02 · master @ `eb1958f` · PROTOCOL 38
Owner instruction (verbatim, mid-turn): *"Lets run the first three priorities in order this batch. and
then once we are ready to do priority 4 'CASTLE GUNS' i have prepared with a different account while
you were working previous session a full roadmap … castle races is fully designed, owner-ruled
(R93–R126), and already audited by three review agents, so it is pre-approved: execute, do not
re-council the design or re-derive the site lists."*

Authority for P4: **`SPARK_RACES_SPEC.md`** + **`.claude/plans/2026-09-02_PDR_RACES_W1_PREAPPROVED.md`**,
both on `origin/claude/castle-races-unique-towers-xb9uzi`. Their §14/§15 are treated as **given**.
Numbering for P1–P3 is `boot-snapshot.md`'s Next Steps list, which is where the owner's "priority 4
CASTLE GUNS" comes from.

---

## 0. THE DELIBERATION POSITION, STATED ONCE

- **P4 (races W1-A): Council WAIVED — by explicit owner instruction.** The pre-approved PDR says
  *"You are not being asked to investigate. You are being asked to build … Do NOT re-run a Council on
  the design questions."* Recorded as a deviation from the Full-tier Council rule, on the owner's
  authority, per Rule 0.
- **What the owner's own PDR still asks for, and what I therefore DID run:** *"verify the spec's line
  numbers against your local checkout before editing (this plan was written against commit `1e5b261`
  + docs; a `file:line` can drift), run `npm ci` first, and run the gates. Verification is welcome.
  Re-deliberation is not."* → §2 below is that verification, and nothing more.
- **P1–P3: Micro tier each**, auto-waived deliberation on the user path (`pdr_approved` +
  `unlock_source: user`).

---

## 1. ⚠ THE ONE STATE DELTA THAT CHANGES HOW P4 IS EXECUTED

`origin/claude/castle-races-unique-towers-xb9uzi` is branched from **`1e5b261`** (S158 close) and is
**21 commits behind master**. It is docs-only — three files, 1523 insertions, zero `src/` changes — so
there is no merge risk. But its measured baseline is stale:

| | the branch's PDR says | master measures now |
|---|---|---|
| unit tests | 3353 / 212 files | **3399 / 218 files** |
| protocol | 38 | 38 *(unchanged — good)* |
| base commit | `1e5b261` | `eb1958f` (S159 shipped 9 priorities of `src/`) |

⇒ **Every `file:line` in §15 is drifted-until-proven.** §2 is the proof pass.

---

## 2. A.0 STATE-DISCOVERY GATE (Rule 21) — PRIME-AUDIT ROWS

*Filled from the S160 probe workflow before this PDR is presented.*

---

## 3. THE PRIORITIES

### P1 — TURN: close the code side with PROOF, hand over the minimal action · Micro

**Objective.** The handoff has said "code side complete and verified" for three sessions. S158's
lesson is that a TURN runbook once pointed the owner at a gitignored `.env` that CI never reads. So:
trace `VITE_TURN_URLS` / `VITE_TURN_USERNAME` / `VITE_TURN_CREDENTIAL` from the Actions workflow yaml,
through the Vite build, into `RTCConfiguration.iceServers`, and prove each hop with a `file:line`.
Correct `TURN_SETUP.md` wherever the runbook and the code disagree.

**Scope.** `.github/workflows/*.yml` (read), `src/net/*` (read), `TURN_SETUP.md` (edit if wrong).
No behaviour change unless the trace finds a break — if it does, that break is the priority.

**Testing.** The trace itself is the evidence; a `file:line` per hop. If a code fix is needed, a unit
test that asserts the three vars reach `iceServers`.

---

### P2 — Rule on the two tower dials that are MINE · Micro

The handoff flags both as *"a decision that is MINE, not the owner's"*. "Run" them = decide them,
record the ruling at the constant with the measurement behind it, and leave the owner a number to
*feel* rather than a question to answer.

- **(a) the stink tower's reload cadence** — currently a full 5-bag refill on every BUILD edge.
  Alternatives already costed in the handoff: a slow BUILD-spread reload, or a FEED gesture.
- **(b) the drone hub's death blast** — the lightning storm MOVED to hub death rather than deleted.
  ⚠ **And the P9 uplift**: 9 drones per 45 s fight where it used to be 3. That is a large buff to a
  structure the owner already called strong. `DRONE_MAX_PER_SPAWNER` is the first dial, the 5 s
  cadence the second.

**Scope.** Constants + their docblocks; a reload/cadence change only if the measurement says so.
Any behaviour change earns a phase-crossing test — S159 P8's lesson was that no single-fight test
could see the bug.

---

### P3 — Rule on the four remaining numbers + record the overlap finding · Micro

(a) drone cadence 5 s · (b) stink-bag HP 1 · (c) **chain-lightning hop range 120 px and NO falloff**
· (d) the stink aura's 12× nerf since S158 A1. Plus the measured **recipe-OVERLAP** result.

**(c) is the one to watch** and the only one likely to move: a full bolt taking six connectors off a
base in one strike. R77 says *"max6"* and mentions no decay. The arithmetic behind "≤ 29 connectors"
gets **re-derived, not trusted** — see §2.

**Scope.** Constants + docblocks; a per-hop multiplier only if the re-derivation supports it.
A falloff multiplier is a sim change ⇒ check whether it earns a protocol bump (it should not: no new
wire field, no new discriminant).

---

### P4 — CASTLE: the race token + the wire (races W1-A), PROTOCOL 38 → 39 · Full

**Exactly the owner's pre-approved P1**, executed from §15 verbatim, with the line numbers corrected
by §2. *"P1 is first and almost alone. Every later surface reads `raceId`."*

**Work — §15.1 … §15.9, in order:** new `src/state/races.ts` (shipped verbatim) · `Player.raceId` ·
save round-trip (additive-optional) · `RosterEntry.raceId` + `isValidRoster` · the six bump sites ·
`lobbyRoster` (⛔ B6: the race follows the **peer**, not the dense seat) · `START_GAME` (⛔ B7: the
`else` arm, or the host's own race is dropped) · the 4-edit `PLAYER_COLORS` audit · the three new test
files + five extended ones.

**⛔ NOT in scope:** the selection UI (owner's P2 — gated on B3's transport decision), the castle art
(owner's P3 — 57 art pieces), the emitter (owner's P4 — gated on B1).

**The one design decision P4 must make and record — B5.** `raceId` selects the emitted `CreatureType`,
so it is a sim input and *should* be hashed — but `FIELD_COVERAGE` is keyed on `keyof World` and marks
`players: 'acknowledged'`, so putting it on `Player` **compiles clean and escapes the wide oracle with
no tripwire**. The owner's PDR says *"weigh both options explicitly and record the choice"* and
forbids defaulting to the `raidPoints` precedent. **My recommendation, to be written at the field:**
`raceId` on `Player`, NOT hashed, because it is *immutable after Begin* — a value that never changes
during simulation cannot diverge, so hashing it buys nothing while breaking the `players` family's
documented exclusion. ⚠ That argument does **not** extend to the tech perks (they change mid-match),
so the perks decision stays open for the draft's own PDR and is recorded as such.

**Testing / exit gate.** `typecheck` 0 · `vitest` green off the **3399/218** baseline · `build` under
900 KiB · `e2e:gating` exit 0 — **every exit code read from a redirect, never through a pipe** ·
`protocolVersionSync.test.ts` left free to fail · a race survives the wire, a save/load **and a host
migration**.

---

### ⭐ P4b — CASTLE GUNS, offered not assumed

The owner named priority 4 **"CASTLE GUNS"**. That mechanic is owner-ruled
(`SPARK_TD_SESSION_SPECS.md:59` Q4 — retaliation-only, 300-tick window; costed at `:208-230`) and
**grep confirms it does not exist**: no `CASTLE_ATTACK|CASTLE_DAMAGE|CASTLE_FIRE|CASTLE_RANGE`
constant, no `retaliat*` anywhere in `src/`.

**⛔ NEW FINDING — B15, and it is exactly the sweep the owner's PDR admits was never run.**
The races spec **presupposes a castle weapon that does not exist**, in three places:
- `§W1-B` item 4 — *"Per-race castle **attack VFX** — cosmetic only"* (VFX for a gun that cannot fire);
- `§7.3` — *"every acquisition path in the game has to learn the word 'untargetable' … **the castle
  guns**"* (listed as an existing acquisition path);
- `§11` item 7 — *"whether castle attack shapes genuinely diverge"* (deferred as a balance question,
  not flagged as unbuilt).

The races PDR §5 states honestly: *"a fourth agent was to check this spec against `BACKLOG.md` and the
older roadmaps for cross-document contradictions. It died on an account spend limit and did not
report."* **This is that finding.** The two documents dovetail rather than compete: castle guns is the
missing foundation W1-B paints on top of, and R94/R117 make it stat-identical for every race, so it is
orthogonal to `raceId` and can land either side of P4.

**Not started without a ruling** — it is a new Standard-tier priority, not covered by the races PDR,
so Rule 16 applies. Two ways to authorise it, owner's choice: run a Council on it, or waive.

---

## 4. WHAT I AM NOT DOING, AND WHY

- **No re-Council on the races design.** Owner instruction. §2 is verification, not deliberation.
- **No re-derivation of §15's site lists** — only correction of drifted line numbers.
- **No seat elimination (B2).** It is a real gap (`gameState.ts:76-88`: the first castle to fall ends
  the match for everyone, victory to `survivors[0]` = Map iteration order). It gates the races spec's
  4-player reasoning, **not** P4. Carried forward explicitly, never silently dropped.
- **No merge of the docs branch to master until this PDR is approved**, then merged as-is (docs-only,
  no conflicts) so `SPARK_RACES_SPEC.md` sits at repo root where its own PDR says it lives.

---

## 5. CARRY-FORWARD LEDGER (nothing dropped)

| item | why it is not in this batch |
|---|---|
| B2 seat elimination | own priority; owner ruling needed on whether races ship onto a first-castle-ends-it match |
| B1 emitter `null`-vs-sentinel | gates the owner's P4, not W1-A |
| B3 `CLAIM_RACE` transport | gates the owner's P2 (selection UI) |
| B5 tech-perk hashing | gates the tech draft; P4 records only the `raceId` half |
| B8–B14 | bound to the waves that use them |
| DRONE AoE (`DRONE_ATK`/`PEN` dead) | boot-snapshot §5; pinned by `pinnedDeadStats.test.ts` |
| N2 raid parity | needs an owner observation, not a fix |
| 4 parallel nullable target fields on `Creature` | held debt, `creature.ts:237` records the rejection |
| the OS-level hook test (1 of 12) | needs a RULE decision this session lacks authority for |
