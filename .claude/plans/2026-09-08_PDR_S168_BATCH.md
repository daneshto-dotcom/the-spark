# PDR — S168 BATCH (Full tier)

STATUS: IN-PROGRESS
Approved: owner, verbatim — *"i approve full session priority batch"* (+ *"keep working iil see what
you have done when i get back"*). Owner is AWAY; delivery expected on his return.

## OBJECTIVE
Close the owner's reported gameplay bugs, put boss stats on his newly-ruled scale, make multiplayer
actually work, clear three UI complaints, and build + illustrate the boss skills he has now ruled
for four of six bosses.

## OWNER RULINGS CAPTURED THIS SESSION
- **R141 — BOSS STATS.** *"HP 10–12, DEF 4–8, ATK 6-10 ... PEN which will be 8-10. with varried speed"*
  Supersedes S167's Claude-authored HP 9-12 / DEF 3-8 / ATK 4-7 / PEN 0-5.
- **R142 — PHARAOH SKILLS.** Locust attack: a CONE of locusts, flying in clouds, targeting units AND
  buildings, for 15 s, launched when the first enemy enters range; locusts hit at **10 ATK / 10 PEN**
  and **cannot be targeted**. Ultimate: summons **RA** to bring down **columns of burning light**,
  **up to 15 ATK / 15 PEN per column**, each column a **2 s cinematic**, **~5 columns** in sequence.
  Trigger: **right before he dies** — at 1 HP / about to die he stops, takes a ritual stance, and
  **cannot be killed while channelling**; when the ultimate finishes, he dies.
- **R143 — ABILITY STANCES ARE THEIR OWN ART.** *"if you generate another form for those characters
  (when they do their abilities) then make sure to generate another stance (how they attack) that has
  to be logical to the way they attack"* — kraken opens his mouth to send the sonar wave; pharaoh puts
  his hands towards an enemy and releases the locusts. All need generated video.
- **R144 — DEF IS A MULTIPLIER ON HP.** *"def affects the total defensive stat (hp x def) so if
  voltkin has 8 hp and 3def (thats 1.6) then he has a total defensive stat of 8 x 1.6 = 12.8"*
  ✅ ALREADY EXACTLY WHAT THE CODE IMPLEMENTS — `unitPoolFifths(hp,def) = hp × (5+def)` fifths,
  `multiplierFifths(n) = 5+n`, a LINEAR `1+0.2n` ladder (`src/state/stats.ts:119,129`).
  8 × (5+3) = 64 fifths = **12.8 points**. His arithmetic and the sim agree.
- **R145 — CASTLE SPAWN UNITS.** Missing from the S167 stat list. Ruled: **weaker by 1 point, taken
  off their strongest stat, than the tier-3 units.**
- **R146 — MULTIPLAYER MUST WORK.** *"by the end of the session we need to make sure that the
  multiplayer actually works"* — live TEST CONNECTION returns *"The relay server refused us — the
  credentials are wrong or expired"*.

## SCOPE — priorities in execution order
- **P1 ⛔ RAID ONE-SHOTS CONNECTORS.** Bot raid destroys a connector/tower in one action while the
  player's attack only lands ATK damage. Owner: *"same bug we had before - we need to fix it one and
  for all and make it consistent!"* Deliver: root cause, ONE enforced invariant, a regression guard.
- **P2 ⛔ TIER-3 TOWER DOES NOT SPAWN.** Should emit at a cadence similar to the castle.
- **P3 BOSS STATS → R141**, with the ladder guard extended and speed varied.
- **P4 CASTLE SPAWN UNIT STATS → R145**, plus the full stat table and the "180" reconciliation.
- **P5 UI (three owner complaints).** (a) BACK TO MAIN moves to the top; (b) badge text → `BETA`
  only; (c) delete the bottom-left help line (`LMB drag spark → …`).
- **P6 MULTIPLAYER / TURN.** Determine empirically whether the credentials are rejected, mangled, or
  the panel is mis-reporting; fix what is fixable in code; state precisely any owner action left.
- **P7 BOSS SKILLS** — zombie (rot aura + death explosion), kraken (3 ground tentacles + sonar cone),
  vlad (on-kill conversion + life sap), pharaoh (locusts + Ra columns + unkillable channel).
- **P8 ART** — skill VFX + R143 ability stances, generated (AI Studio credits restored by owner).

## TESTING
Every priority: `npm run typecheck`, `npx vitest run`, and a targeted new test. Batch close adds
`npm run build`, `npm run check:atlas`, `npm run e2e:gating`, `verify-deploy`, `probe-relays`.
⛔ Every gate's exit code read from a captured `$?` — never a pipe, never the wrapper's trailing line.

## DELIBERATION / A.0 (Rule 21)
Two adversarial read-only discovery workflows were run BEFORE execution, each lane refuted by an
independent verifier and synthesised by a critic:
- `s168-a0-state-discovery` — death path, explosion machinery, boss model, heal/charges, stun/cone,
  four-sites tax, gate landscape.
- `s168-owner-reported-probe` — raid one-shot, tier-3 spawn, TURN, stat list + DEF math.
DEVIATION (recorded per Rule 0): no live 3-way Council round — the owner approved the batch and left,
expecting delivery. The adversarial refute+critic passes substitute; residual risk is the absence of
an independent Gemini quality lens.

## RISKS
- P7 conversion mutates `ownerPlayerId` (serialized AND hashed) → protocol bump + four-sites tax.
- The 3%/s aura and 20% life sap are non-integer against a `damageEntity` that throws on fractions;
  float accumulators are banned. Needs a tick-quantised integer rule.
- ⚠ "3% of WHOSE health" (max vs current) remains genuinely owner-open — percent-of-current never
  kills. Will implement percent-of-MAX and flag it for his confirmation rather than block.
