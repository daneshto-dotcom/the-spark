# S171 — RESEARCH FOR NEXT SESSION (R171-O / P / Q)

3/3 lanes returned, 0 errors (`wf_9f166340-102`). Every claim carries file:line in the raw output:
`~/AppData/Local/Temp/claude/…/739c07de…/tasks/w0b8pjm38.output` (65 KB).

---

## R171-O · SPAWNER TOWER CONTENTS — **much cheaper than it looks**

⭐ **THE CRUX, ANSWERED: nothing needs to be stored.** Units are never queued — `applyFeedTower`
mints them synchronously — and every fed unit carries `sourceSpawnerId`, which **rides the wire
un-stripped** (`trimMirrorCreature` strips `targetCreatureId` and nothing else). So the per-kind
census is a pure read over `world.creatures`, identical on host and peer:

```
for (const c of world.creatures.values()) if (c.sourceSpawnerId === id) tally[c.type]++
```

That is the SAME loop `underGoblinCaps` already runs to enforce the ten. ⇒ **No new synced
structure, no PROTOCOL bump, no four-sites tax.** Estimated Standard tier, ~12–18K, one sitting.

⛔ **THE FIELD THAT LOOKS USABLE IS A TRAP.** `spawner.spawnedCount` is (a) LIFETIME-cumulative, not
live, and (b) **stripped from the wire by `trimMirrorSpawner`**. Any design reading it is correct on
the host and blank on the joiner — the silent-divergence class.

**RECOMMENDATION: a census row on the existing structure popover, in the footer-chip idiom.** Not a
footer widget. He asked for it *on the click*, the popover already resolves the spawner
(`feedSpawnerId`), and **a footer census could not say WHICH tower** — the ten-cap is per-tower and
he is meant to build several. His footer reference is about the LOOK (*"kind of like the queue of the
primitives"*), which is `shapeStrip.ts` + `footerBand.ts:246-283`: a 38×30 `roundRect` radius 6 with
an `xN` badge — literally *"a tiny little square picture with rounded edges"*.

⚠ **THE "/10" DENOMINATOR IS NOT UNIVERSAL.** Tier-3 race towers are exempt from the per-spawner cap
(R159, *"truly limitless"*), the pentagram's is 10 000, drones' is 3 and inert. A census must not
print "/10" where there is no denominator.

⭐ Chip art already exists and is already in memory: all six goblin atlases are eagerly loaded and
Pixi-cached, so slicing frame 0 of row 0 costs one `Texture` and no second fetch. They live in
`GoblinRenderer`'s private `atlases` map — needs a small public accessor.

**OWNER DECISION:** popover-only, or popover + a footer total? (A footer readout can only ever show a
total across all his towers, and the cap is per-tower.)

---

## R171-P · THE RA RITUAL AS A GENERATED LOOP

**Shape: one new atlas ROW on the existing mummies sheet, generated by me (R171-M), with the
telegraph and columns left as CODE.** ~15 lines of packer, ~10 of renderer, 7 deleted from
`bossAuras`, 2 JSON entries, ~120 of tests. One veo run, ~$3–6 of his ~$50 polish budget.

⛔ **THE CRUX IS 12 FRAMES VS 24.** A 12-frame row is two JSON keys plus a one-line renderer arm and
touches the packer not at all. Anything longer forces `framesPerState` from a scalar into a per-state
value (5 sites). 24 frames at `ticksPerFrame: 5` = 120 ticks = **exactly one Ra column**, five times
across the 10-second channel — which is the reason to pay for it.

⛔⛔ **THE TRAP THAT WOULD DAMAGE THE WHOLE CHARACTER.** The packer takes **ONE union bbox across
every frame of every state** and one height-fit scale. So a ritual pose TALLER than the ready stance
shrinks the Pharaoh in idle, walk, attack AND die — permanently, on a sheet that is currently fine.
⇒ The prompt must constrain the silhouette (*"hands never above the headdress, same overall height
and framing as the reference"*).

**OWNER DECISION:** does *"generate that as a loop"* include the light BEAM? R171-M puts anything NEW
on him, and a Ra column VFX is a new effect asset. It also **cannot ride inside the character clip** —
the matte deletes border-connected near-white, so a beam drawn in the clip would be destroyed or
welded in.

---

## R171-Q · THE LOCUST-RELEASE STANCE — ⭐ the best finding of the three

⭐⭐ **THE RELEASE NEEDS NO NEW FIELD, because a cloud's BIRTH TICK is exactly recoverable on both
peers.** `despawnAtTick` is emitted **unconditionally** for every creature (S134) and the locust
config deliberately omits `lifetimeClock`, so:

```
launchTick   = cloud.despawnAtTick - PHARAOH_LOCUST_LIFETIME_TICKS
isReleasing  ⟺ ∃ cloud of this owner with (launchTick + bossId) % LAUNCH_INTERVAL === 0
                and 0 <= world.tick - launchTick < CAST_STANCE_TICKS
```

**A cloud's existence is proof the launch fired** — which is the whole difficulty ("can the renderer
know he actually fired, given the launch is conditional?"), dissolved. `world.tick - launchTick`
doubles as the cast row's frame clock and as the cloud's age for a hand-birth beat. The `% INTERVAL`
term attributes a cloud to the right Pharaoh when a seat fields two.

Alternative (b), an additive-optional `locustCastUntilTick` on the `sapFlashUntilTick` shape, is also
correct and also costs no protocol bump — keep it as the fallback if the derivation proves fiddly.

⛔ **NO LOCUSTS IN THE CLIP — this one matters.** The matte keeps anything not border-connected, so
insects drawn near his palms would be **welded into the cut-out and drawn on him forever**, doubling
the procedural swarm he has already approved (*"Locust is fine. It doesn't look bad."*). The stance
carries only his own ochre sand from the palms; the swarm stays 100 % `render/locustCloud.ts`.

**ORDERING:** do R171-Q and R171-P in one sitting — one generation run, one atlas repack, and one
shared renderer change (both are "select a non-FSM row from a derived condition").
