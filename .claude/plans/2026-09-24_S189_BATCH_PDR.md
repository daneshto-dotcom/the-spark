**STATUS: IN-PROGRESS**

# S189 — BATCH PDR · LAND EVERYTHING S188 CARRIED + THE OWNER'S PLAYTEST CORRECTIONS

**Tier:** Full · **Merge owner:** the main S189 session · **Owner approval:** given, verbatim below ·
**Protocol:** 50 live; every deploy that changes a shared rule or a required/new wire value takes its
own bump (the merge owner writes it — branches never touch `PROTOCOL_VERSION`).

> *"this session, you need to continue working everything that didn't land last session. So including
> all the carry forwards, um, all the approved priorities, and all the stuff that I have corrections from
> yesterday night … I want you to launch all of those, everything I just mentioned in different work
> trees under different agents so they can work it all in parallel. And again, you will just check that
> everything has landed in the end and merge it, uh, to main uh, and deploy it … make sure the different
> work trees and the session, the agents, save every part of the way … every priority that lands already
> and that you checked and works feel free to to deploy it and like merge it in. Don't wait till
> everything has landed … anything that didn't land that you check that isn't perfect, then you send back
> to the to the work trees agent … maybe like five this session, six … Sandworm will move to the next
> session."* — owner, S189

## 0 · WHERE THE SPEC LIVES

| source | holds |
|---|---|
| `.claude/plans-archive/2026-09-23_S188_BATCH_PDR.md` | the S188 rulings for all twelve racials, the brief rules every branch still follows |
| `SPARK_CANON.md` | the stat ladder (§2), castle (§3), draft (§3d, ⚠ STALE: racials ARE live), wire (§6), welding R185-A/B (§7b), retaliation (§9b), R182-G/H (§9) |
| `.claude/session-archive/session-state_S188_2026-09-24.json` | S188's carry-forwards and scope amendments SA1-SA3 verbatim |
| each `s188/*` worktree's `.claude/plans/S188_PROGRESS_<name>.md` | where that branch stopped |
| the S189 read-only workflows `s189-disconnect-hunt`, `s189-branch-audits` | verified findings with file:line |

## 1 · THE OWNER'S PLAYTEST CORRECTIONS (S189, verbatim excerpts)

| # | his words | owner of the fix |
|---|---|---|
| C1 | *"the upgrades that you have in level zero, level five, level 10, the spark should be one layer above … it gets highlighted when you mouse over it, but the mouse is under it"* | `s189/render` |
| C2 | *"If you connect shapes … to existing towers, like to a laser tower, my brother connected like two triangles … it got his tower disappeared … that's like a regression … we changed it that you can connect towers together … to get them have more HP … as long as the existing tower, the shape is there … it still has a pentagram, but you can connect to it"* (+ *"if it's too difficult for this session, then keep that in mind for next session"*) | `s189/weld` |
| C3 | *"Vulcan [Voltkin] attacks his own buildings … instead of going to the right to my zone and starting to attack my buildings, and I had lots of them, he just started attacking the buildings around him"* (he accepts own-building attacks only once every enemy building is gone) | `s189/units` |
| C4 | *"connection was lost at like wave five or something … Need to make sure connection is established and always on"* | `s189/net` |
| C5 | *"it was lagging at about wave five. I thought we fixed the lags"* | `s189/net` |
| C6 | *"Quick match … I'm player one … my brother connects … it switches me to player two puts him as player one … only happens to this workstation … if my brother goes in as player one and then I join him then I'm player two"* | `s189/net` |
| C7 | *"the Naga's gatherer teleport … there's like a line … a big line every time they teleport all over the screen … He should just teleport back to the castle … giving the shape back … then go back to the center … without that weird like laser beam"* | `s189/render` |
| C8 | *"Helga moves behind the map … I built the Helga tower near the castle, and she now moves behind when she does her little patrol … I thought that you added invisible walls around the whole map"* | `s189/units` |
| C9 | *"the down arrow … if I do like click on four or like five … and then I click in the middle of that tower, the down arrow just brings that whole footer down"* | `s188/input-layer` (built S188, never merged) |
| C10 | *"the Kraken sonar sends units flying … outside the map … it should like move them … knock them back a little bit … and stun them"* | `s189/units` |

## 2 · SCOPE — PRIORITIES

| id | what | where |
|---|---|---|
| P0 | CONNECTION LOST hunt → fix (C4) + lag at wave 5 (C5) + quickmatch seat (C6) | `s189/net` |
| P1 | Deploy #2 = `s188/deploy2-candidate` (racial-a + racial-d fix rounds) | merge owner — gates re-run S189: typecheck 0 · vitest 0 (5996/367) · build 0 (944.2/1100 KiB) · e2e:gating 0 (69/69) · e2e:races 0 (5/5) |
| P2 | `s188/input-layer` (C9 + draft click-through) — audit, fix, merge | merge owner (+ branch fix round) |
| P3 | `s188/wrath` — audit, fix, merge (+ protocol bump) | merge owner (+ fix round) |
| P4 | `s188/swarm` — audit, fix, merge (+ protocol bump) | merge owner (+ fix round) |
| P5 | `s188/ra-vfx` — the owed tests + gates, then merge | worktree agent |
| P6 | `s188/draft-atk` — finish (the dead ATK/PEN draft picks, the HELLSPAWN card numbers), then merge | worktree agent |
| P7 | units: C3 Voltkin, C8 Helga, C10 Kraken + LOWs (CORPSE EATER bite timer reads live rage; castle regen is a % of the flat base pool; restore() re-derives nextCreatureId; the spawn-queue gap outside runHostTick) | `s189/units` |
| P8 | weld: C2 | `s189/weld` |
| P9 | render: C1, C7 + LOWs (a same-tick heal hides inside a net damage floater; the elite piranha has no fallback sheet) | `s189/render` |
| P10 | canon (from `s188/canon`, re-derived against the merged tree, with its assertions) + the stale CLAUDE.md lines + S173 NONET plan disposition | merge owner, last |

**OUT (owner):** SANDWORM → next session. Levels 15/20 design. **OWNER QUESTIONS carried, not blocking:**
APEX PREDATOR bites ×4 and THE SWARM ×11 (both ATK and PEN multiplied on `ATK × (5 + PEN)`) — built
as ruled (*"all the stats you take and you just triple them"*), reported for his confirmation.

## 3 · A.0 STATE DISCOVERY (Rule 21) — measured this session

| claim | check | result |
|---|---|---|
| deploy #2's 3 @visual reds are real | `e2e:gating` on a quiet machine, own worktree | ❌ not real — `E2E_GATING_EXIT=0`, 69/69; the S188 reds were load |
| the webServer timeout is a config bug | `playwright.config.ts` + `tasklist` | port = FNV hash of the worktree path + `strictPort:false`; an orphan vite on that port pushes the new one to +1 while Playwright polls the old URL. No `node.exe` alive now → cannot recur until an orphan exists. Noted for the net/infra brief, not fixed blind |
| the disconnect is a protocol mismatch | `main.ts:3627-3747`, HELLO path | ❌ cannot be — the overlay needs `gameState === 'PLAYING'`; a mismatched HELLO never reaches PLAYING. It is a mid-match transport loss (or one tab reloaded/closed mid-match) |
| deploy #2 changes the wire | `git diff master s188/deploy2-candidate -- src/state/save.ts …` | one additive-optional field, `Creature.attackCycleRaged` (emitted only when true) — no bump |
| NONET stages plan (S173) is live work | canon §9 | ⚠ SUPERSEDED — R182-G withdrew the fixed-seed ladder; R182-H rules adaptive difficulty DEFERRED on persistent identity. Close the plan with that reference; build nothing |

## 4 · THE PARALLEL SPLIT — the S182/S188 rules, restated as binding

- One self-contained brief per branch; branch off **deploy #2's tip `9f3efe5`**; commit locally,
  NEVER push, NEVER touch master; `daneshto@gmail.com`; own `npm ci`; **commit after every step** and
  keep `.claude/plans/S189_PROGRESS_<name>.md` current in every commit (the spend limit killed S188 twice).
- Do ONLY the brief. Real bugs outside scope are REPORTED, not fixed. Never edit `PROTOCOL_VERSION`,
  its docblocks, `SPARK_CANON.md` or `src/canon.test.ts` — canon text goes to
  `.claude/plans/S189_CANON_NOTES_<name>.md`.
- Every mechanic: the arithmetic test, the wire/hash round-trip where state is new, a REACH test
  through the real host tick, a negative test, one mutation-tested guard.
- Gates per branch: `npm run typecheck`, `npx vitest run`, `npm run build`, exit codes from a captured
  `$?`. Branches do NOT run e2e — the merge owner runs it on the merged tree.
- Each branch is audited by an agent that did not write it before the merge owner sees it; the merge
  owner re-audits, re-runs the gates after EVERY merge, and deploys piece by piece.

## 5 · ROLLBACK
Each branch merges alone. A branch that is not clean stays unmerged; nothing already live regresses.
`git revert -m 1 <merge>` + push rolls back any single deploy.

## 6 · COUNCIL (Claude + Grok `grok-4.20-0309-reasoning` + Gemini `gemini-3.1-pro-preview`) — BATTLE LEDGER

| # | from | challenge | verdict | why |
|---|---|---|---|---|
| G1 | Grok HIGH | freeze s189 branches, one integration branch re-audited by all | **REJECTED** | the owner ordered parallel branches merged piece by piece; one-at-a-time merge + full gates between IS the integration discipline (S182 lesson 3), on disjoint file sets |
| G2 | Grok HIGH | block deploy #2 until the hunt ends | **REJECTED for #2 / ACCEPTED for net** | #2 touches no `src/net` (diff verified); blocking it keeps the owner's buggy build live. The net fix MUST ship a test reproducing its diagnosed mechanism |
| G3 | Grok MED | owed tests + LOWs before sign-off; canon before the bump | **ACCEPTED (1st) / REJECTED (2nd)** | tests-owed already binding; the canon must describe the MERGED tree, so it lands last |
| G4 | Grok HIGH | suspend deploys until the owner returns | **REJECTED** | owner: *"feel free to deploy it … Don't wait till everything has landed"*. Its "vanish then recreate hack" cites a patch that does not exist |
| M1 | Gemini HIGH | batch protocol-changing branches into one deploy | **ACCEPTED** | wrath + swarm merge one at a time (gates between), ONE bump, ONE push. e2e runs on LOCAL master before any push — only a push deploys, so the trunk is never live-broken |
| M2 | Gemini HIGH | `nextCreatureId` must be a monotonic serialized counter | **ACCEPTED as the fix shape** | it misread the LOW (the re-derivation IS the defect) but its amendment is exactly the fix → units brief |
| M3 | Gemini HIGH | reject welds that merge two towers | **REJECTED / ACCEPTED as a test** | reverses R185-B (he endorsed welding two bat towers). The weld branch must prove two welded towers BOTH survive |
