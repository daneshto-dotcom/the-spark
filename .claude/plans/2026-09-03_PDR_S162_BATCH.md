# PDR — S162 batch (FULL tier) — STATUS: AWAITING OWNER `go`

Owner directive (verbatim, 2026-09-03): *"boot the session correctly and completely according to the
full boot sequence/procedures. then read this handoff... then look at everything that was unfinished
and undeployed. then look at what i just said about the connection and races. and start working
methodically to close of all the gaps and complete all the work from previous session and to make
sure everything we have worked today actually landed and is deployed correctly"*

Owner bug reports: (1) race/colour cannot be changed — picker opens, pick does nothing;
(2) owner + friend + a second workstation ON THE SAME LAN all cannot connect.

## 1. OBJECTIVE

End the live multiplayer outage, make the race picker actually change the race, then close every
open S161 finding and the two hunt lanes that never ran — and prove the result is deployed.

## 2. A.0 STATE-DISCOVERY (Rule 21) — RUN BEFORE THIS PDR WAS WRITTEN

| # | Check | Verifier | Expected | ACTUAL |
|---|---|---|---|---|
| A0-1 | live bundle TURN config | `curl` live asset + grep | valid `turn:` URL | ⛔ `urls: "turn:global.relay.metered.ca:80"` — JS-literal fragment, all 3 keys wrapped |
| A0-2 | when did TURN secrets appear | `gh secret list` | — | **2026-09-03T06:10Z — today.** Fresh regression |
| A0-3 | `turnFromEnv` validates URLs | Read `iceConfig.ts:178` | scheme-checked | ⛔ trim + drop-empty ONLY. No scheme validation |
| A0-4 | quickmatch sets `session.netTransport` | grep `src/net/quickmatch.ts` | yes | ⛔ **zero `session.` refs** — never set |
| A0-5 | picker closes on pick | Read `racePicker.ts:174` | yes | ✅ `hide()` is called — so the defect is the REPAINT |
| A0-6 | `raceIsFree` counts default races | Read `hostHandlers.ts:432` | yes | ⛔ CLAIMS only (OF-1 confirmed) |
| A0-7 | `setVisible(false)` closes picker | Read `lobbyScreen.ts:542` | yes | ⛔ no `close()` (MED-3 confirmed) |
| A0-8 | offline seat leaves `livingSeats` | grep `elimination.ts:138` | — | ⛔ only `players.delete` is RETURN_TO_TITLE (OF-2 confirmed) |
| A0-9 | baseline typecheck | `npm run typecheck`, direct exit | 0 | ✅ 0 |

## 3. SCOPE — sequenced by owner-visible value

**P0 — the live outage.** `turnFromEnv()` sanitises + VALIDATES: strip an accidental `key: "value"`
wrapper, require `^(stun|stuns|turn|turns):`, drop invalid entries with a console warning, and fall
back to STUN-only rather than shipping a config that THROWS. Wrap `RTCPeerConnection` construction
(`iceProbe.ts`, `transport.ts`) so no ICE config can ever kill connectivity outright. Correct the
lobby's mis-diagnosis ("WebRTC is disabled by an extension or a policy" — it was our own config).
`TURN_SETUP.md`: document the dashboard-paste trap that caused this.

**P1 — race pick does not repaint.** Let the host's presence rebuild run with NO transport (compute
roster + `onPresence` locally, skip only the wire send), so a solo/quickmatch host sees its own pick.
Keeps the "ONE presence path" invariant.

**P2 — OF-1 (owner's top item).** Two seats can hold the same race+colour; seven recipe resolvers
identify owners BY COLOUR, so this mis-assigns towers. Both the lobby taken-set and `raceIsFree`
operate on `claimed ?? defaultRaceForSeat(seat)`. S161 wrote and reverted this; `raceClaim.test.ts`
reddened — finish it properly.

**P3 — MED-3.** `close()` the picker in `setVisible(false)` (both lobby + bot overlay).

**P4 — OF-2.** A dropped peer's intact castle blocks the last-one-standing win.

**P5 — the two lanes that never ran, BY HAND** (per CLAUDE.md: a hunt that returns nothing is not a
completed hunt). Lane 2 dead-ends; lane 4 doc/claim drift — including the known drift in
`boot-snapshot.md` + `SPARK_RACES_SPEC.md` (W1-B / B2 / selection UI still described as UNBUILT).

**P6 — remaining findings** OF-3…OF-11 as triaged, + CF-S161-a once ruled.

**P7 — landing proof.** All five gates read by DIRECT exit code, Rule 22 runtime audit, push,
`verify-deploy` 4/4, and a real browser check of the live site. Then `/handoff` as a skill.

## 4. OUT OF SCOPE

New gameplay features; the bundle charter; host-migration D4; bot-intelligence Phase A; any TNAS
path (R23). ⛔ **I will not set the GitHub secrets** — credentials are the owner's to enter.

## 5. RISKS

- The sanitiser could mask a genuinely bad config → it WARNS loudly and the report step prints the
  parsed result, so a wrapper paste is visible rather than silently repaired.
- P2 touches the colour→owner identity used by 7 resolvers → per-resolver test before/after.
- P1 changes a documented invariant ("both wait for LOBBY_PRESENCE") → invariant is preserved: the
  repaint still goes through `onPresence`, only the wire send is skipped when there is no wire.

## 6. TESTING

Unit: malformed-TURN matrix (wrapper form, bad scheme, empty, valid, mixed) asserting STUN-only and
NO throw; solo-host repaint; effective-race collision refusal; picker-closes-with-lobby; offline seat
excluded from `livingSeats`. Gates: typecheck, `vitest run`, `e2e:gating`, `build`, `verify-deploy`,
`probe-relays` — each exit code read DIRECTLY, never through a pipe.

## 7. VERIFICATION

A real browser against the LIVE site after deploy: construct `RTCPeerConnection` with the shipped
`ICE_SERVERS` and require **no throw** + `relay > 0` once secrets are clean; open the lobby, pick a
race, and confirm the tile changes.

## 8. ROLLBACK

Every priority is its own commit; `git revert <sha>` per priority. P0 is additive validation — the
worst case reverts to today's behaviour, which is already broken.
