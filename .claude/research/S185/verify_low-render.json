{
  "lane": "ADVERSARIAL VERIFY of the \"low-render\" probe (damage-number anchor on a hidden connector; rampAnchorAtPoint vs fog + atlas state) at 7d1514b",
  "verdicts": [
    {
      "claim": "F1 — damageNumbers.ts anchors the connector floater at the raw bond midpoint (:691) and imports neither concealment.ts nor towerCover.ts.",
      "verdict": "CONFIRMED",
      "note": "src/render/damageNumbers.ts:691 `        (a.pos.x + b.pos.x) / 2, (a.pos.y + b.pos.y) / 2,` inside the `track(` at :689. Import block is exactly lines 58,59,60,61,65,66,67,68,69 — pixi.js, world.ts, types.ts, gatherer.ts, constants.ts, castleGuns.ts, voltkin-config.ts, defender.ts, stats.ts. `grep -n \"concealment\\|towerCover\\|isConcealed\\|coverAlphaForBond\" src/render/damageNumbers.ts` returns NO MATCHES."
    },
    {
      "claim": "F1 — the same raw midpoint is used for the connector-BREAK number at :660.",
      "verdict": "CONFIRMED",
      "note": "src/render/damageNumbers.ts:660 `      this.emitAt(world, (a.pos.x + b.pos.x) / 2, (a.pos.y + b.pos.y) / 2, hit.amount, 'damage', a.placedBy);`"
    },
    {
      "claim": "F1 — structureRenderer.ts:226 `continue`s on any bond whose cover alpha has ramped below epsilon, so the bond the floater points at is genuinely not drawn.",
      "verdict": "CONFIRMED",
      "note": "src/render/structureRenderer.ts:225-226 `      const coverAlpha = coverAlphaForBond(bond.id);` / `      if (coverAlpha <= TOWER_COVER_DRAW_EPSILON) continue;`, preceded at :220-223 by the S183 note. The retirement docblock at :39-62 is real and names the floater."
    },
    {
      "claim": "F1 — geometry: LIFT_PX 30 at damageNumbers.ts:130 and RISE_PX_TOTAL 23 at :114 put upper-arm connectors at cy-52, clear of the art.",
      "verdict": "PARTIAL",
      "note": "VALUES right, BOTH LINE NUMBERS WRONG. Actual: `const RISE_PX_TOTAL = 23; // 31.25 px/s × 0.75 s` is at :94 (report said 114) and `const LIFT_PX = 30;` is at :115 (report said 130). STAR_R=44 (blueprints.ts:138), GOBLIN_TOWER_ART_PX=99 (:282), HUB_ART_PX=84 (:243) all confirmed. The arithmetic also silently drops the TOWARD_ATTACKER term the same file calls load-bearing: damageAnchor returns `y: vy + (bestY - vy) * TOWARD_ATTACKER - LIFT_PX` (:382). cy-52 only holds when the nearest enemy is level with the bond — true for a chewer standing on it, false otherwise."
    },
    {
      "claim": "F2 — rampAnchorAtPoint (structureRamp.ts:545) is pure geometry over creatureSpawners + defenders and never consults fog or atlas state.",
      "verdict": "CONFIRMED",
      "note": "src/render/structureRamp.ts:545 `export function rampAnchorAtPoint(world: World, x: number, y: number): PrimitiveId | null {`; the only test is `const half = spec.artPx * 0.5;` / `if (Math.abs(x - at.cx) > half) return;` / `if (Math.abs(y - at.cy) > half) return;`, then `for (const sp of world.creatureSpawners.values()) consider(...)` / `for (const def of world.defenders.values()) consider(...)`. Its imports (lines 87-92) carry no render-state module, and its docblock at :527 opens `PURE — the anchor of the ramp building whose SPRITE BOX contains (x, y)` — so the report's \"the docblock opens with PURE\" warning is real."
    },
    {
      "claim": "F2 — the renderer that draws these towers clears exactly the two gates the hit test skips, at structureRampRenderer.ts:193/194.",
      "verdict": "CONFIRMED",
      "note": "src/render/structureRampRenderer.ts:192-194 `    // Fog: the same test, on the same field, that every other structure renderer applies.` / `    if (isConcealed(anchor.pos.x, anchor.pos.y, anchor.placedBy)) return false;` / `    if (!this.manifests.has(spec.atlasBase)) return false; // loading, or failed — shapes stay bare`. Cover publishes only after both, at :222-223 `    // Declared HERE, after the sprite is committed — never above the fog skip or the atlas bail.` / `    markTowerCover(members, bonds, newestTick);`"
    },
    {
      "claim": "F2 — clicking fogged ground opens a full ENEMY BUILDING card with a LIVE health value under a LAST SEEN label.",
      "verdict": "CONFIRMED",
      "note": "characterSheetModel.ts:1227 `  const frozen = isConcealed(prim.pos.x, prim.pos.y, owner);`, :1280 `    health: { cur: Math.max(0, pool - banked), max: pool, frozen },` with `subtitle: mine ? 'YOUR BUILDING' : 'ENEMY BUILDING'` and stats CONNECTORS/SHAPES above it. characterSheet.ts:403 `    if (frozen) this.text('LAST SEEN', rx, by + BAR_H + 20, 10, DIM);` — frozen only picks `barColor`/DIM. LIVENESS VERIFIED, which the report asserted: characterSheet.ts:289 `const view = this.selected === null ? null : characterSheetModel(world, seat, this.selected);` inside `sync(world, seat)`, whose own docblock says \"`sync` runs every frame\", called from main.ts:4008."
    },
    {
      "claim": "F2 — the fix is safe for the owner's repair gesture because isConcealed short-circuits on your own things before any geometry.",
      "verdict": "CONFIRMED",
      "note": "concealment.ts:106-109 `export function isConcealed(x: number, y: number, owner: PlayerId | null): boolean {` / `  if (!ctx.active) return false;` / `  if (owner !== null && owner === ctx.localPlayerId) return false;` / `  return !isPointVisible(ctx.sources, x, y);`. (The owner short-circuit is :108, not :107 as the report's prose says.)"
    },
    {
      "claim": "F2 — it is a CLASS: towerAnchorAtPoint has the identical hole, controls.ts:832-840's fallback scan has none either, and `grep -rn \"isConcealed\" src/input/` returns zero lines.",
      "verdict": "PARTIAL",
      "note": "Every stated part CONFIRMED — towerFrames.ts:410 has no isConcealed; controls.ts:832-840 is exactly the `for (const prim of this.world.primitives.values())` scan with `const r = prim.radius + 6;`; `grep -rn \"isConcealed\" src/input/` → ZERO MATCHES. But the enumeration is INCOMPLETE and stops two arms short — see corrections. handleSheetSelect has FIVE fog-blind arms, and the two the report missed are the worst ones."
    },
    {
      "claim": "F2 — the atlas half is real but mild; `manifests` is private and a failed load is never retried.",
      "verdict": "CONFIRMED",
      "note": "structureRampRenderer.ts:103-107 `      } catch {` / `        // Left absent and never retried — see the load-failure contract in the file docblock.` / `        this.sheets.delete(base);` / `        this.manifests.delete(base);` / `      }`, with `private readonly manifests = new Map<string, Manifest>();` at :86 and the `loadStarted` guard at :95."
    },
    {
      "claim": "ADJACENT — damage numbers themselves have no fog gate; an enemy creature skipped by creatureRenderer's concealment `continue` still prints its damage numbers.",
      "verdict": "PARTIAL",
      "note": "Code shape CONFIRMED: damageNumbers.ts:491 `      if (delta > 0) this.emit(world, c.id, c.pos.x, c.pos.y, delta, 'damage', owner);` vs creatureRenderer.ts:497 `      if (isConcealed(creature.pos.x, creature.pos.y, creature.ownerPlayerId)) continue;`. But the report under-states the breadth and over-states the live window. BREADTH: the hole is the WHOLE file — shapes, bonds, defenders, stink clouds and castles all `track(...)` with no fog test, so the very lines F1 is about (:660, :691) are themselves the structure-side fog leak and the report analysed them for cover-alpha only. WINDOW: fog needs `matchPhase === 'BUILD'` (vision.ts:175), and at the BUILD edge hostTick.ts runs `standDownDefenders(world)` under the comment `// Walls up, guns cold, doors open.` with walls.ts:111 `return world.matchPhase === 'BUILD';` — so cross-seat damage during fog is narrow. The obvious escalation I tested is CLOSED BY DESIGN: repair zeroes damageFifths (structureRepair.ts:509 `if (b !== undefined) b.damageFifths = 0;`) but poolDelta:304 `if (delta < 0 && !rising) return {...kind: 'heal'};` returns null for bonds, which carry `rising: true` — so an enemy repairing under fog prints nothing."
    },
    {
      "claim": "fix_shape — the re-anchor arm must land a 4-arg markTowerCover at all FOUR publish sites (structureRampRenderer:223, towerRenderer:313, voltkinTowerRenderer:576, towerCover.ts:203).",
      "verdict": "REFUTED",
      "note": "There are THREE publish sites; the report counted the declaration as a fourth. towerCover.test.ts:275 `  it('⛔ EXACTLY three renderers publish cover, and each commits it exactly once', () => {` over `PUBLISHERS = [['towerRenderer.ts', …], ['structureRampRenderer.ts', …], ['voltkinTowerRenderer.ts', …]]`, and :281 `it('⛔ and no OTHER renderer publishes — a fourth publisher must be a deliberate act')`. `towerCover.ts:203` is `export function markTowerCover(` — the signature. Direction is safe (overestimate), but it is a fabricated site in a project that counts sites for a living."
    },
    {
      "claim": "tests_owed — towerCover.test.ts pins both the publish-site total and the cover-alpha CONSUMER total, so the SUPPRESS arm's new consumer re-pins them.",
      "verdict": "PARTIAL",
      "note": "Both censuses exist — :267 'the cover PUBLISH sites, counted and named' and :304 'the cover CONSUME sites, counted and pinned'. But the CONSUME census does NOT fire on this fix and the report's inference that it constrains the work is wrong: :305 `const CONSUMERS = ['structureRenderer.ts', 'spawnerZoneRenderer.ts'];` and the forbidden list at :311-312 is `['towerRenderer.ts', 'voltkinTowerRenderer.ts', 'structureRampRenderer.ts', 'turretRenderer.ts', 'princessRenderer.ts']`. `damageNumbers.ts` is in neither, so importing coverAlphaForBond there stays green. Net: the report's SUPPRESS price (~6 lines, 1 file, Micro) is CORRECT — but for the opposite reason it gave, and the census hole is itself a finding."
    },
    {
      "claim": "tests_owed — structureRamp.test.ts's structureWorld() helper builds a bare cast with no localPlayerId/gameState/matchPhase, so existing cases stay green.",
      "verdict": "CONFIRMED",
      "note": "structureRamp.test.ts:561 `function structureWorld(opts: {` returning `  } as unknown as World;` over only `{ primitives, bonds, creatureSpawners, defenders }`. Existing cases stay green for a stronger reason than stated: isConcealed never reads `world` at all, it reads module-level `ctx.active`, which is false until beginConcealmentFrame runs. towerFrames.test.ts exists. `resetConcealmentForTest` is concealment.ts:118 (as cited); `__resetTowerCoverForTests` is towerCover.ts:257, not :258."
    },
    {
      "claim": "four_sites_risk — NONE; nothing touches factory/serialize/hash/worker and PROTOCOL_VERSION 47 stands.",
      "verdict": "CONFIRMED",
      "note": "Verified independently. rampAnchorAtPoint takes `world` and returns `PrimitiveId | null`; structureSheet/characterSheetModel read only; damageNumbers.ts docblock ~line 40 states \"This is a renderer-local visual over synced state: it cannot desync the sim, and `world` is never written to.\" No serialized field is added by either arm. The viewer-dependence hazard the report raises for isConcealed-inside-a-pick is real and correctly characterised."
    },
    {
      "claim": "owner_questions #1 — SUPPRESS vs RE-ANCHOR is a genuine unresolved conflict between S175 P9 and R183-E that only the owner can settle.",
      "verdict": "PARTIAL",
      "note": "The conflict is real and the two quotes are accurate (SPARK_CANON.md:351 and the R183-E block at :344-352). But the reconciliation the report says is missing is already written down, three lines above the text it quoted: structureRenderer.ts:54-57 `⭐ **THE TWO RULINGS ONLY LOOK OPPOSED, AND WHAT RESOLVED THEM IS THE DAMAGE RAMP.** … Damage is still visible everywhere; it is just visible on the thing that is taking it.` S183 knew the floater was the thing DAMAGED_BOND_MIN_ALPHA existed for and retired it anyway. So the current behaviour is a KNOWN consequence of R183-E, and the canon's own logic already points away from the status quo and toward the building. Taking this to him as a blank conflict is the exact thing SPARK_CANON.md's opening page exists to stop."
    }
  ],
  "corrections": [
    "⛔ THE BIGGEST MISS — `handleSheetSelect` has FIVE fog-blind arms, not three, and the two the report skipped are the worst. In precedence order: controls.ts:766 `for (const bag of this.world.stinkClouds.values())`; controls.ts:776 `for (const c of this.world.creatures.values())`; :823 towerAnchorAtPoint; :824 rampAnchorAtPoint; :832-840 the primitive scan. The CREATURE arm is strictly worse than the structure headline: characterSheetModel.ts:1143 `const frozen = isConcealed(c.pos.x, c.pos.y, c.ownerPlayerId);` and :1162 `health: { cur: Math.max(0, c.ehp), max, frozen },` — a card rebuilt every frame over a unit that MOVES, so sweeping the cursor across fog pops cards that track an invisible enemy's live position and live pool. The stink-bag arm leaks the same way via :1422. A fix that gates 545 and 410 closes the two smallest arms and reads as complete — this project's named failure mode (S158: 'announced it had fixed four, and left the fourth live').",
    "⛔ THE PRECEDENT THE REPORT NEVER FOUND, AND IT IS DIRECTLY ON POINT FOR OWNER QUESTION #2. spawnerZoneRenderer.ts:137-142 already states this divergence and records it as put to the owner and accepted: `⚠ **A CONSEQUENCE HE WAS TOLD ABOUT AND ACCEPTED: THE RAID AIMS BLIND.** … Verified safe rather than assumed: the raid pick in `controls.ts` never consults `coverAlphaForBond`, so an invisible connector is still clickable and still raidable. The mechanic works; the player simply cannot see which arm he is cutting on an enemy tower.` That is a SIXTH call site of the class the report did not enumerate — `pickBond()` at controls.ts:1793, reached from the RAID_TARGET dispatch at :1233-1251 — and it means 'an invisible thing stays clickable' has already been ruled acceptable once. The distinction that still justifies asking is that a raid reveals nothing while the card prints recipe name, CONNECTORS, SHAPES and live health; but the question must be put against this quote, not as if it were new. Project rule: 'Before asking for a ruling, grep the archive for it.'",
    "A CHEAPER AND MORE COMPLETE FIX SHAPE THAN THE ONE PROPOSED. characterSheetModel already computes `isConcealed` at every one of its six subjects — :1143 creature, :1187 defender, :1227 structure, :1325, :1422 bag, :1469 — so the fog policy is one decision in one file rather than a gate bolted onto five picks in two files. ⚠ AND IT CARRIES A TRAP NEITHER THE REPORT NOR THAT SHAPE ACCOUNTS FOR: characterSheet.ts:295 `if (view === null) this.selected = null;` under the docblock 'THE ONE LINE THAT MAKES A DEAD SUBJECT'S CARD VANISH WITH NO DEATH LISTENER'. Returning null for a concealed subject would slam an already-open card shut the instant its subject entered fog, destroying the entire `frozen` / 'LAST SEEN' feature. The model arm must return a frozen view, never null; gating the PICK is the only shape that gets 'keep the card you opened, refuse the new pick' for free — which is exactly the split the report's own owner question #2 names.",
    "A TEST THAT SHOULD BE RED AND IS NOT — the consumer census is a CLOSED LIST, not an enumeration. towerCover.test.ts:305 names two consumers and :311-312 names five forbidden files; every other file in src/render/ is unconstrained, which is why adding coverAlphaForBond to damageNumbers.ts stays green. Its sibling at :324 shows the pattern that has teeth — it regex-extracts all six `alpha:` values out of spawnerZoneRenderer and requires each to end in a cover multiplier. That is S182 lesson 2 verbatim ('a source-text guard proves a line EXISTS, it cannot prove the line is REACHED'), and the consume census is the half that never got the mechanical treatment. Whichever F1 arm ships, re-pin :305 by globbing src/render/*.ts instead of extending a hand-written list.",
    "LINE-NUMBER ERRORS, all verified by reading: RISE_PX_TOTAL is damageNumbers.ts:94 not :114; LIFT_PX is :115 not :130; __resetTowerCoverForTests is towerCover.ts:257 not :258; the isConcealed owner short-circuit is concealment.ts:108 not :107; `cx, cy` is destructured at structureRampRenderer.ts:203, twenty lines before the markTowerCover at :223, not 'two lines earlier'. Every VALUE quoted is correct and every substantive quote is verbatim — these are citation drift, not fabrication, but this lane exists because of S120.",
    "THE ESCALATION I TESTED AND IT IS CLOSED — recording it so the next session does not re-derive it. FIX is BUILD-only (structureRepair.ts:140 `if (world.matchPhase !== 'BUILD') return false;`) and repair zeroes the bond (:509), so 'an enemy repairing under fog prints a heal floater over blank ground' looks like the strongest live case. It cannot fire: damageNumbers.ts:304 `if (delta < 0 && !rising) return { amount: Math.round(-delta), kind: 'heal' };` and connectors are tracked with `rising: true`, so a decrease returns null. Benign, by construction, not by luck."
  ],
  "net_assessment": "SUBSTANTIALLY SOUND AND WORTH ACTING ON — every load-bearing line in it is really there and really means what it says, and its four best moves (measuring the geometry instead of repeating 'blank ground', proving the fix cannot make your own tower unrepairable via the isConcealed owner short-circuit, catching that rampAnchorAtPoint's docblock opens with PURE, and refusing to accept a source-text guard as the test) are the work of something that read the tree rather than the handoff. Both findings are STILL LIVE at 7d1514b. But it fails this lane in the two ways that matter most here. FIRST, ITS ENUMERATION IS INCOMPLETE IN THE DIRECTION THAT SHIPS A PARTIAL FIX READING AS COMPLETE: it found three fog-blind picks and there are five in handleSheetSelect plus the raid's pickBond, and the arm it missed — creatures, controls.ts:776 into characterSheetModel.ts:1143/:1162 — leaks a MOVING enemy's live position and live pool, which is worse than the static building it headlined. Take its fix_shape as written and F2 ships closed-looking and open. SECOND, IT ASKS THE OWNER TWO QUESTIONS THE REPO HAS ALREADY ANSWERED IN PART: the S175-vs-R183-E conflict is reconciled at structureRenderer.ts:54-57 three lines above the text it quoted, and 'should an invisible thing be clickable' was put to him and accepted for the raid at spawnerZoneRenderer.ts:137-142. Both are precisely the enervating re-litigation SPARK_CANON.md's first page was written to end. SIZING: four_sites_risk NONE is CORRECT and I verified it independently — nothing reaches factory/serialize/hash/worker, no wire field, 47 stands, and the two determinism hazards it names (damageAnchor's total order, towerCover's tick-driven ramp) are the right two. size_estimate Standard (10-30K) SURVIVES but for redistributed reasons: its publish-site count is one too many (three, not four — towerCover.test.ts:275 says so in as many words), its SUPPRESS arm really is a 1-file Micro though not for the reason given, and its F2 arm is UNDER-scoped by two pick sites plus the consume-census re-pin. Those roughly cancel. I would NOT split it into two Micros as suggested — F2 done properly is one policy decision touching characterSheetModel plus the picks plus three test files, and the creature arm is the part the owner would actually notice."
}