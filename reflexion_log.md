# SPARK — Reflexion Log

Cross-session learnings. Most recent at top. Entries pruned when total >50.

## 2026-05-24 — Session 42 (BUG-CRITICAL-1: Real-time 1v1 restoration; S15 P2 design-drift fix; commit 6e3bfaf; run 26360989574; deploy 34s)

- S42 #design-drift-can-ship-13-sessions-without-blueprint-amendment-catching-it: S15 P2 (commit add497f, 2026-05-12) added END_TURN action + currentPlayerId field + requireActivePlayer gate to 1v1 mode, implementing it as turn-based hotseat. Blueprint SPARK_Blueprint.md:3 reads `"A Real-Time Multiplayer Game of Geometric Emergence"`, line 36 `"real-time, six-player canvas"`, lines 48-56 specify a real-time core loop with simultaneous Travel/Grab/Combine/Defend tradeoffs (Speed/Safety/Information/Pressure). The implementation literally contradicted the LOCKED title sentence — yet 759 tests passed, 26 sessions accumulated work on top of it (S15→S41), and the bug shipped to production. User caught it visually post-S41 deploy ("turn based while it is supposed to be real time!"). Tests were valid for the broken implementation (asserted the very `silently rejects in 1v1 when playerId !== currentPlayerId` predicate the spec forbids). **Lesson: green tests verify the IMPLEMENTATION against itself, not the implementation against the SPEC. Any PDR that adds a mechanic to a §LOCKED area MUST cite the blueprint line being implemented; if no citation is possible, the PDR is amending the spec and needs explicit user blessing before code lands. Constitutional add to Rule 21 (State-Discovery Gate): when adding to a §LOCKED system, blueprint-citation gate fires BEFORE empirical state discovery — code lacking a cite is a spec change in disguise. Pattern carry-forward: future PDRs touching real-time / FSM / spawner / win-condition / fog-of-war systems must lead with §X.Y line quote from SPARK_Blueprint.md. Missing-cite = invalid PDR.**

- S42 #council-r2-sharpened-shared-vs-owned-error-semantics: R1 Gemini-#3 flagged "race vulnerability beyond PICKUP" — both Grok and I initially proposed blanket silent-return-with-warn on all "target already claimed" paths. R2 Gemini sharpened the audit semantics: **silent-return applies ONLY to SHARED-RESOURCE races (spark, primitive, bond) — NOT to player-owned violations (carry slot, energy, charges)**. The distinction: a player can race for a spark another player grabs first (shared); a player CANNOT race for their own carry slot (they own it; if reducer sees them not-Carrying when they expected to be, it's a desync bug). Applied: `applyPickupSpark` line 90 `state !== Free` → silent + counter (shared); `applyPickupSpark` line 89 missing-spark stays throw (invariant); `placePrimitive` target-missing → silent + counter (shared, race-prone after P1 severs P2's target between cursor pick and dispatch); `applyDropSpark` CarryViolation stays throw (own slot); `placePrimitive` CarryViolation/insufficient-energy/missing-carried-spark stay throw (own state); `canSeverBond` charge<1 already silent (shared by being checked at runtime); cause='physics'/'creature' already bypass everything. **Lesson: when reviewing concurrency safety, classify each error path by RESOURCE OWNERSHIP — shared (race-prone, should silent-return) vs player-owned (invariant, should throw). Blanket silent-return masks real bugs; blanket throw crashes under legitimate races. Council R2 produced a material refinement over R1 here — exactly what 2-round deliberation is for. Pattern carry-forward: codify this taxonomy as a checklist in CHECK Triumvirate's quality gate; for any reducer touching multi-player state, classify each non-happy-path explicitly.**

- S42 #race-counter-as-observability-not-just-debugging: Council R2 CONVERGENT preferred `world.diagnostics.raceRejects++` counter over `console.warn` alone. The counter is a non-serialized World field that increments on every silent-return-from-race path. Three concrete benefits: (a) tests assert exact race-count semantics deterministically (PICKUP race test expects `expect(w.diagnostics.raceRejects).toBe(1)` — testable invariant); (b) future debug overlay could show live count to detect prod race storms; (c) reset at START_GAME + RETURN_TO_TITLE so the counter is per-match, not cumulative cross-session (prevents stale state from previous game polluting observability). Console.warn would have been observable in dev but invisible in prod and untestable. **Lesson: when introducing silent-failure paths (legitimate or otherwise), pair them with a structural observability mechanism — counter / event log / status flag — rather than relying on console output. Counter is more testable; structured event-log is more diagnostic. For SPARK's 2-player scale, counter is sufficient. Pattern carry-forward: future PRs that change throw→silent-return MUST add a counter or event-log entry as part of the same change; pure silencing without observability is auditing-debt.**

- S42 #boot-then-smoke-runtime-verifiability-gate-applied: Rule 20 PRIME-AUDIT amendment (S105) demanded a "boot-then-smoke" question on runtime infrastructure PRs. Applied here: would the change actually work under real host dispatch path with mocked client? Confirmed via the new PICKUP-race + PLACE-target-vanish tests which call `dispatch(world, intent)` against a live World (not mocked) and assert state mutations. The applyPickupSpark "spark not Free" path was previously caught only by integration tests of the auth gate; under S42's silent-return semantics it now has explicit unit coverage. The deploy verification handshake's bundle-grep step (positive: raceRejects, localPlayerId, START_GAME_SIGNAL present; negative: END_TURN, currentPlayerId, requireActivePlayer, turnBadge, "PLAYER N'S TURN", "SPACE end turn" absent — 10/10 PASS) is the runtime-pass complement to the static-pass typecheck. **Lesson: for PDRs that change reducer semantics, write the test that calls real dispatch with real World — not a mocked dispatch with mocked World — and assert post-conditions on the World fields. Static typecheck + green unit tests can miss runtime behavior; "boot-then-smoke" tests close that gap. Pattern carry-forward: when amending a `case 'X':` body in a reducer or changing throw-semantics in an apply* helper, ALWAYS add a `dispatch(realWorld, action)` integration test alongside the unit test.**

- S42 #first-Intent-wins-is-the-natural-resolution-for-trystero-p2p: Three race resolution strategies surfaced in the Council direction-questions: (a) first-Intent received by host wins; (b) closer-player wins via distance tiebreaker; (c) defer to Council R1. User picked (a). Implementation: host's network handler at main.ts:275 `netTransport.on((msg) => { if (msg.kind === 'INTENT') dispatch(world, msg.action); })` is already first-Intent-wins by construction — each incoming Intent dispatches immediately on arrival; whichever arrives at host's main-thread first wins because the reducer mutates the World monotonically. Trystero's WebRTC channels are per-peer ordered (Data Channel guaranteed); intents from a single peer are FIFO. Cross-peer ordering is host-receive-order. No additional code required — the existing dispatch path already enforces first-wins; what S42 changed was the LOSER'S handling (was throw → now silent+counter). **Lesson: when adopting a race-resolution strategy, audit whether the existing code path already implements it (the win path) and only the loser-path needs adjustment. Often the "implementation" is just permitting an existing arrival to be silently ignored rather than building new resolver logic. Pattern carry-forward: for future multi-player resource conflicts (bond contention, structure ownership transfer, win-trigger race), check if host's existing FIFO dispatch already provides first-wins, then only add loser-side observability.**

- SESSION #s42-bug-critical-1-stats: **18 files changed (1 deleted: src/state/authGate.ts), +393/-490 LOC (-97 net).** Single commit 6e3bfaf "[S42 BUG-CRITICAL-1] Restore real-time 1v1". Run 26360989574, build 26s + deploy 8s = 34s SUCCESS first attempt. 4-layer verification handshake 10/10 PASS (positive shibboleths raceRejects + localPlayerId + START_GAME_SIGNAL all present in bundle; negative shibboleths END_TURN + currentPlayerId + requireActivePlayer + turnBadge + "PLAYER N'S TURN" + "SPACE end turn" all 0). Last-Modified advanced 2026-05-24T09:48:42Z (S41 deploy) → 2026-05-24T12:17:23Z (S42 deploy), ~2.5h delta. Bundle 474.26 KB → 472.87 KB (-1.39 KB from deleted code). Tests 759 → 754 (-5 net: -11 deleted hotseat/END_TURN/active-player tests + 6 added race+real-time+back-compat tests). Council Full tier R1+R2 deliberation: ~5K tokens R1 challenges + ~3K R2 refinement = ~8K Council overhead. Execution ~25K tokens. Total session ~35-40K tokens. **Highest-impact session by user-visible value since S35 lobby fix: gameplay went from broken-turn-based-spectator-mode → blueprint-conforming-real-time-race. 2-peer 1v1 smoke (S35-P11 carry, now 6+ sessions overdue) gating only on user calendar — covers ALL fixes through S42 INCLUDING real-time restoration.**

## 2026-05-24 — Session 41 (S40 P1 verification gate completed; production caught up 8 days; deploy 26357977462; no source commits)

- S41 #billing-block-root-cause-was-pro-quota-exhaustion-not-failed-payment: The S40 check-run annotation read *"recent account payments have failed OR your spending limit needs to be increased"* — verbatim ambiguous OR. S40 reflexion #actions-billing-failure-presents-as-2-second-job-failure-with-empty-runner correctly documented the signature but didn't disambiguate. S41 user-screenshot evidence resolved it definitively: GitHub Pro subscription was active + paid ($48/year, next payment "—"), no failed-payment banner; the 3000-min Pro quota was at 100% consumed (red bar) with `$0` Actions spending limit (Pro default). Root cause was **quota exhaustion + $0 ceiling**, not payment failure. Chateau-guardian alone consumed $10.29 (53%) of the $19.30 Pro discount cycle; the-spark consumed only $1.24 (6%). Fix was raising the Actions budget on daneshto-dotcom account to $5/mo — instant unblock, ~$1-3/mo actual cost given Spark's tiny share. **Lesson: when GitHub annotates "payment failed OR spending limit", treat the OR seriously — check both sides empirically via the billing dashboard before recommending a fix. Failed-payment fix (replace card) is destructive; spending-limit fix (raise ceiling) is non-destructive and works for either case. Future deploy-block diagnosis: present both options to user and ask them to check which it is from the dashboard screenshot — don't guess. Constitutional note: the annotation message is intentionally OR-phrased because GitHub's check-run system can't disambiguate the two underlying states from inside the runner-allocation rejection path.**

- S41 #deploy-verification-handshake-as-4-step-ladder: After billing unblock, full verification of "did production actually catch up?" runs as 4 sequential probes, total ~2 minutes: (1) `gh workflow run` + `gh run list --limit 1` — capture new run ID + confirm `status: in_progress` (NOT `completed/failure` in <5s = billing-block fingerprint); (2) `gh run watch <id> --exit-status` — block until success or failure, surfaces build/deploy job timings (~33s build + ~9s deploy = ~42s typical); (3) `curl -sI <live-url>` — verify `Last-Modified` advanced past prior frozen-state timestamp; (4) `curl -s <bundle-url> | grep -c <fix-signature>` — verify bundle actually contains the known-feature string from current master HEAD. Step (1) alone is enough to confirm billing resolved; (2-4) confirm production actually has fresh code. The grep in step (4) is the critical gotcha: a CDN cache + new ETag could fool step (3) if Cloudflare/Fastly serves stale-revalidated content, but `grep -c` of a known fix-signature against the live bundle proves the actual JS file has the fix. **Lesson: deploy verification needs all 4 layers — pipeline finished, headers advanced, AND bundle content verified. Skipping any layer leaves room for "looks deployed but isn't." Pattern carry-forward: bake this 4-step ladder into a verify-prod skill or post-deploy hook; the bundle-grep step in particular catches CDN-cache anomalies that header-only checks miss.**

- S41 #path-filter-empirically-validated-by-no-redeploy-on-state-edits: After the deploy success, this session updated `.claude/session-state.json` (+15 lines of S41 gate fields) and `reflexion_log.md` (+~30 lines of S41 entries) — both are session-bookkeeping files, both pushed to master, neither triggered a deploy. Confirms the S40 P2 path filter is working: only changes to `src/**`, `public/**`, `index.html`, `vite.config.ts`, `tsconfig.json`, `package*.json`, `.github/workflows/deploy.yml` trigger deploys. The cascade-cancel chronic pattern from S31-S39 is structurally eliminated. **Lesson: structural filters (path-based trigger restrictions) outperform conventional ones (`[skip ci]` commit-message flags) because they don't require author discipline and can't be forgotten. The validation that matters is "did the bookkeeping push trigger a run?" — answered by `gh run list --limit 1` after the push. If conclusion is empty and createdAt is older than the push, filter is working. Pattern carry-forward: any future workflow trigger redesign should verify post-deploy by observing what does and doesn't trigger over the next 5+ pushes.**

- S41 #chateau-guardian-actions-bloat-is-separate-medium-term-issue: Billing dashboard showed chateau-guardian consuming $10.29 (53%) of monthly $19.30 metered usage — by far the largest Actions consumer across the 5 active repos. At current burn rate, chateau-guardian alone will exhaust Pro's 3000-min quota by mid-month every cycle going forward. The-spark deploy this session was unblocked by raising the spending limit ceiling, but the underlying CI heaviness in chateau-guardian remains. Worth a dedicated session to audit chateau-guardian's workflow runtime — likely candidates: test suite parallelization, build caching gaps, redundant matrix dimensions, slow Docker steps. **Lesson: when one repo dominates Actions spend in a multi-repo account, that repo's CI is the leverage point. Fixing it benefits ALL other repos sharing the quota. Pattern carry-forward: add chateau-guardian CI audit to medium-term backlog as a cross-project sync item; until then, the-spark and others depend on the $5/mo Actions ceiling holding under chateau-guardian's monthly burn.**

- SESSION #s41-p1-verification-batch-stats: **ZERO source commits this session.** Single workflow_dispatch (run 26357977462, build 33s + deploy 9s = 42s total, success on first attempt post-billing-resolution). 8-day production catch-up shipped: Last-Modified 2026-05-16T22:00:06Z → 2026-05-24T09:48:42Z. S37+S38+S39+S40 fixes all landed live in single deploy (no incremental waves needed because all commits were already in master HEAD from S40 close). Bundle 474.26 KB local → 474343 bytes live (parity within rounding). START_GAME_SIGNAL grep count: 2. Total gh API ops: ~6 (dispatch + 2 list + 1 watch + 2 curl). Session cost: ~0 deliberation API calls (Micro-tier carry-forward, no Council), ~5-7K tokens execution + verification + bookkeeping. **Highest-leverage 2-minute session in recent history: 8 days of unshipped user-value fixes landed live by raising a $5/mo ceiling. 2-peer 1v1 smoke (S35-P11) now unblocked + ready for user. Carry-forward: vite/vitest CVE bump (regression risk → dedicated session), main.ts hypertrophy refactor (Standard batch), chateau-guardian CI audit (cross-project, separate session), Node.js 20 deprecation warning (deploy.yml uses actions/checkout@v4 etc — actions will auto-upgrade by 2026-06-02 per workflow annotation, no action required).**

## 2026-05-24 — Session 40 (Deploy pipeline unblock + 1v1 production verification; commit f6cf27c; user-side billing block uncovered)

- S40 #deploy-claims-need-live-url-head-verifies-not-just-tests-green: S39 PDR closed with 759/759 tests green + "user verifies at live URL" acceptance step. User reported "1v1 lobby still doesn't work" because the live URL was serving **S36-era code** — every deploy attempt since S37 close had been silently failing. S39 State-Discovery (Rule 21 Phase A.0) verified code-side claims (parseNetMessage wired, schemaVersion strict, applyNetSnapshot throws, lobby visibility gate, avatar render path, AttractDrag clamp, S35 P0 fix intact) but did **NOT** verify the production-deploy claim. The fix's CSS-px-perfect cursor math could be valid in unit tests + live in master HEAD + tested by user against stale production code that never had it. **Lesson: any PDR whose acceptance includes "verify at live URL" or "user playtests in prod" MUST include a State-Discovery probe that the live URL is currently serving post-fix code. `curl -sI <live-url>` gives `Last-Modified` as the cheap probe (compare to expected push date); strict version is `curl -s <bundle-url> | grep -c <fix-signature>` for the fix's identifying string. S39 would have caught this in ~10s of state-discovery had this probe been on the checklist. Constitutional add to Rule 21: production-deploy claim is a state-discovery item.**

- S40 #phantom-deployment-state-waiting-as-multi-day-concurrency-lock: Deployment record `4716837489` (github-pages env, sha `1be5fce`, 2026-05-17) was created but its only status was `state: waiting` — never advanced to `queued → in_progress → success` like normal deployments (which take ~45s end-to-end). It held the github-pages environment's deployment concurrency lock for **7 days**. Every subsequent workflow run for the deploy.yml workflow sat in `pending` with `updated_at` 2 seconds after `created_at` and 0 jobs ever picked up — waiting for the phantom to finalize. Diagnosis path: `gh api .../deployments?environment=github-pages` → check the most-recent deployment's `updated_at == created_at` AND `gh api .../deployments/{id}/statuses` returns only non-terminal states (`waiting`/`queued`/`in_progress`). Fix path: POST `state: inactive` to deployment_statuses, then DELETE the deployment. The github-pages env uses **deployment-level** concurrency (separate from workflow-level `concurrency:` block) that `actions/deploy-pages@v4` enforces — it overrides `cancel-in-progress: false` at the workflow layer. **Lesson: when prod has been stale beyond what cancel-cascade explains, check for stuck deployments BEFORE assuming workflow-level concurrency or runner issues. Constitutional add to /handoff verification: if the last successful github-pages deployment timestamp is >24h ago AND new deploys aren't completing, run the phantom-deployment probe before declaring done. Add to integrity-check skill: assert most-recent deployment's `state` is terminal (success/failure/inactive) before /handoff.**

- S40 #actions-billing-failure-presents-as-2-second-job-failure-with-empty-runner: The signature for "GitHub Actions runner not allocated due to billing" is precise and easy to misdiagnose: job `started_at` is set, `runner_name: ""` (empty string), `steps: []` (no steps ever ran), `completed_at` is ~2s after `started_at`, `conclusion: failure`. Looks like a real build failure but `gh run view --log-failed` returns `log not found` because no logs exist. The actual message is in the check-run annotation: `gh api repos/{owner}/{repo}/check-runs/{job_id}/annotations` returns `"The job was not started because recent account payments have failed or your spending limit needs to be increased."` Each subsequent rerun reproduces in 2s — not transient. **Lesson: never trust the run-level `conclusion: failure` alone for jobs with empty `runner_name` and `steps: []`. Always fetch the check-run annotation. The annotation is the ground-truth diagnosis. Pattern carry-forward: codify the diagnosis-ladder: (1) check job-level `started_at`/`runner_name`/`steps`; (2) if runner empty + steps empty + duration ~2s → query check-runs annotations endpoint; (3) annotation text is the actionable signal. The 2-second failure pattern with empty runner is ALSO documented by GH support as the exact signature for billing/payment failures.**

- S40 #path-filter-beats-skip-commit-flag-for-doc-deploy-suppression: Pre-S40, deploy.yml triggered on every push to master with no filter. Session-close + handoff + state-autocommit fired 3+ pushes within minutes (`[Sxx close]` reflexion archive → `[Sxx handoff]` boot snapshot → `[state-autocommit]` hook commit) — each cancelled the prior in-flight deploy via the github-pages env's built-in deployment concurrency. Two design options for the chronic fix: (a) `[skip deploy]` commit-message convention requires author discipline, hook compliance, and grep-style matching — high failure mode rate; (b) `on.push.paths:` filter at workflow level is structural — only pushes touching build inputs trigger, every other push is silently ignored at the trigger layer with zero author cognitive overhead. Chose (b). Filter list = exact set of files the build step reads: `src/`, `public/`, `index.html`, `vite.config.ts`, `tsconfig.json`, `package.json`, `package-lock.json`, `.github/workflows/deploy.yml`. Verified by inspection: bookkeeping files (`reflexion_log.md`, `HANDOFF_*.md`, `boot-snapshot.md`, `.claude/`, `BACKLOG.md`, `AUDIT.md`, `LOCKED_DECISIONS.md`, `findings.*.json`) are NOT in the list and cannot trigger deploys. **Lesson: when designing trigger filters, prefer structural (path/branch/file) over conventional (commit-message tags, author email) — structural filters can't be forgotten and don't require team discipline. Pattern carry-forward: any future "should this CI run?" decision should be a path/branch question first, a commit-message question only if path is insufficient. The path filter list itself is a SPEC of what the build consumes — keep it accurate as the build inputs evolve.**

- S40 #compound-deploy-failures-need-empirical-layer-strip-not-conjecture-of-single-cause: S40 found **3 distinct compounding failure modes**, each independently sufficient to block deploys: (a) phantom github-pages deployment locking env concurrency, (b) cascade-cancel via rapid session-bookkeeping pushes, (c) billing-blocked runner allocation. S40 PDR conjectured (b) as the sole cause — would have shipped only the path filter and STILL found prod broken because (a) was holding the lock AND (c) was rejecting all runner requests. Only by stripping layers empirically — `gh api .../deployments` → `gh api .../deployments/{id}/statuses` → `gh api .../actions/runs` → `gh api .../actions/runs/{id}/attempts/{n}/jobs` → `gh api .../check-runs/{job_id}/annotations` — did each layer's separate failure surface. Layers don't compose by inference: a workflow_run can sit pending forever for reasons that have nothing to do with the deploy.yml content. **Lesson: when production is unexpectedly stale, do NOT settle on the first plausible explanation. Strip the failure layers empirically using the API endpoint at each layer of the stack. The github-pages deploy pipeline has at least 4 separate state machines (deployments, workflow_runs, jobs, check-runs/annotations) and each can be stuck independently. Pattern carry-forward: codify a deploy-troubleshooting runbook: (1) last successful deployment age; (2) any non-terminal deployment records?; (3) any non-terminal workflow runs?; (4) jobs that started_at but never got a runner?; (5) annotations on check-runs of failed jobs. Each step a single `gh api` call; together they triangulate the failure layer in seconds.**

- SESSION #s40-deploy-pipeline-bug-stats: 1 commit `f6cf27c` ([S40 P1+P2] deploy.yml paths filter + fresh concurrency group). 1 file changed (.github/workflows/deploy.yml +27/-2). gh API operations: 1 deployment-status POST (inactive), 1 deployment DELETE, 2 workflow_dispatch firings, 2 run cancellations, 1 rerun, ~25 gh API probes across deployments/runs/jobs/check-runs/annotations. **Diagnosis-to-action ratio was high: ~75% of session value was the empirical layer-strip that found the billing-failure annotation — the actual code change is 27 lines. The billing block means P1 + P3 carry forward to S41 (user must restore billing OR change repo visibility to public before any deploy can land). P2 (path filter + concurrency group) shipped and will prevent the chronic cascade-cancel pattern once billing is restored. Bundle untouched (deploy.yml only). Tests untouched. Production still on S36-era code (Last-Modified: 2026-05-16T22:00:06Z) because billing prevents the queued deploy from executing. Cost: ~0 deliberation API calls (Micro tier per user APPROVED waiver); execution ~6-8K tokens spread across 4 tool-call turns; diagnostic discovery ~10-15K tokens.**

## 2026-05-23 — Session 39 (Live 1v1 BUG-A peer-lobby-stuck + BUG-B cursor↔avatar drift; commit f8d237a)

- S39 #runtime-only-bug-needs-runtime-verification-not-just-unit-tests: S38 closed with 745/745 tests passing + audit Pass 1 + Pass 2 + Pass-2 fixes all green. User playtest of the live deployed S38 build immediately surfaced BUG-A (peer never leaves "Waiting for host to begin" after host clicks Begin) — a runtime regression of the S35 P0 fix introduced by S38's strict schemaVersion gate + try/catch around applyNetSnapshot + parseNetMessage wired at recvFn. Three silent-drop points added in the audit hardening; any one of them silently strands the peer in LOBBY with no user-visible feedback. Unit tests passed because they exercised the path with constructed-in-memory snapshots, not the JSON-roundtrip-through-Trystero live wire. **Lesson: any Standard/Full-tier PDR that touches wire-protocol code MUST include a runtime-verifiability check before declaring done — boot the dev server, complete one round-trip end-to-end, OR explicitly note that live verification is gated by 2-peer-smoke and surface the gate as a BLOCKER. Tests on the in-process boundary are necessary but not sufficient for "the wire works." S38 audit caught 17 latent issues but introduced 3 NEW runtime failures because verification was test-only. Pattern carry-forward: bake "live 2-peer smoke or explicit deferral" into the audit workflow's Pass-2.5 gate.**

- S39 #dedicated-control-signals-decouple-state-transitions-from-snapshot-reliability: BUG-A fix introduced a `START_GAME_SIGNAL` wire envelope (independent of NETSNAPSHOT) so the peer's LOBBY→PLAYING transition no longer depends on snapshot delivery succeeding. Pre-S39 the peer transitioned only when `applyNetSnapshot(firstSnap, world)` ran cleanly — any of the 3 silent-drop modes (strict schemaVersion mismatch, applyNetSnapshot throw, broadcast race) left the peer indefinitely stuck. Post-S39 the signal is the primary lobby-exit trigger; snapshots still drive authoritative state but are no longer load-bearing for the FSM transition. Handler is idempotent via `if (gameState === 'LOBBY')` guard so late/duplicate signals (future reconnect path) can't clobber currentPlayerId / pendingCreatureSpawn. **Lesson: state-machine transitions should ride on dedicated control signals, not on whichever data-payload happens to carry the new state. The S38 hardening at the snapshot apply chain (defense-in-depth) was correct, but the LOBBY transition layer was still a single-failure-point because it had no separate signal. Pattern carry-forward: any future critical FSM transition (RECONNECT, MATCH_END, FORFEIT) should follow the same separate-signal pattern instead of being inferred from the next snapshot.**

- S39 #visible-to-user-diagnostics-over-debug-flag-required: Pre-S39 the only way to see "are snapshots arriving on the peer?" was to open `?debug=1` and read console logs across two browsers in real-time. Cumbersome and only available to the developer. Lobby now renders an unobtrusive 13px grey diagnostic strip below the "Waiting for host" status: `sync N/T seq=K kind=X applyErr=J gs=LOBBY` — surfaces the three S38 silent-drop modes in plain English. The N/T ratio shows wire health; applyErr=J>0 names the throw path; gs= shows the FSM state. If BUG-A regresses, the user sees `gs=LOBBY` while snapshots count up — diagnosable in seconds. **Lesson: critical wire-protocol failure modes that are silent in the network layer (rejected at validator + logged at warn level + dropped) MUST have a visible-to-end-user diagnostic surface for the failure-mode in question. Console logs are insufficient because the user playtesting will not open dev tools. Pattern carry-forward: any silent-drop path in production code should pair with a counter increment + UI-visible exposure. Cost is ~30 LOC for counters + ~20 LOC for the lobby text widget.**

- S39 #object-fit-contain-letterbox-coordinate-mapping-trap: BUG-B was caused by `controls.updateCursor` using non-uniform `sx = CANVAS_W/rect.width`, `sy = CANVAS_H/rect.height`. The canvas is rendered with `object-fit: contain` (Pixi default) — under any viewport aspect ≠ canvas aspect, content is letterboxed inside the CSS box and `getBoundingClientRect()` returns the full box including bars. The non-uniform formula was correct ONLY at matched aspect; at any other aspect the cursor mapping diverged from visible canvas content by up to the letterbox-bar size. Bug is invisible at visual center (where both formulas agree on canvas-center) and grows linearly to ±72 CSS-px at the visible canvas edges (verified empirically at 1280×900 viewport: pre-fix drifts -90/-72/-54/0/+50/+72/+90 across CSS-y axis; post-fix 0 everywhere). The same buggy math was ALSO present in `mapCanvasRectToPage` (latent in HTML input positioning). Fix: shared pure helpers `cssToCanvasCoords` + `fitCanvasIntoRect` in lobbyScreen.ts compute the letterbox-aware uniform scale + offset; both call sites switch to the helpers. **Lesson: any code that maps between CSS-px and canvas-units MUST use letterbox-aware math when the canvas is rendered with object-fit:contain (or its `cover` / `fill` cousins). The "simple ratio of widths" formula is a trap — it gives correct answers at the visual center (because center is invariant under any aspect transform) and wrong answers everywhere else. Pattern carry-forward: codify a single canonical helper for canvas↔CSS conversions in this codebase; any new call site MUST import from it instead of inlining the math.**

- S39 #wire-protocol-handler-idempotence-via-state-gate: The peer's START_GAME_SIGNAL handler at main.ts:onJoinAttempt is guarded by `world.gameState === 'LOBBY'`. Without the guard, a late or duplicate signal (host reconnects → re-broadcasts on rejoin, or future retry path) would re-dispatch START_GAME and clobber any state the in-between snapshots populated — currentPlayerId reset to P1, pendingCreatureSpawn cleared, etc. With the guard, the handler is a no-op once the peer has already transitioned. Cost: 6 chars (`world.gameState === 'LOBBY'`). Benefit: handler is idempotent against the entire class of "message-arrives-after-state-has-already-progressed" failure modes, without requiring sequence numbers or de-dup logic. **Lesson: every wire handler whose action would RE-INITIALIZE state (vs. INCREMENTALLY UPDATE state) should gate on the current state. Snapshot handlers update incrementally so they don't need this; control signals (START_GAME_SIGNAL, END_GAME, RETURN_TO_TITLE) reset blocks of state so they do. Pattern carry-forward: any future control signal that triggers an `apply*` reducer with reset semantics should follow the same state-gate pattern.**

- SESSION #s39-bugfix-batch-stats: 1 commit f8d237a (batched 2 bugs in one Micro PDR; both bugs in the 1v1 critical path and discovered simultaneously, so atomic). 9 files changed (+520 / -27). Tests 745 → 759 (+14: +3 protocol, +3 sync, +8 lobbyScreen). Bundle 472.47 → 474.26 KB (+1.79 KB; 25.74 KB headroom on 500 KB cap). tsc clean. Empirical verification of BUG-B at 1280×900 viewport (pre-fix drift ±72 CSS-px at edges → post-fix drift 0 everywhere). User `go` on the PDR waived opt-in deliberation (Micro tier). End-of-session audit caught 1 defensive improvement (idempotence guard on START_GAME_SIGNAL handler — added inline). Cost: ~0 deliberation API calls (Micro tier); execution + state-discovery + verification ~3-4K tokens. **Highest user-value session since S35 P0 (which fixed the original deadlock that this session's BUG-A regressed). The deploy queue was stuck on c9db329 (S38 handoff push) for ~45h — actual ship to users gated on `gh run rerun` or natural unblock.**

## 2026-05-21 — Session 38 (Cross-Project Audit campaign — Pass 1 → P1-P5 fixes → Pass 2 → Pass-2 fixes; commits 5750cd6, 04887e5, da23b51)

- S38 #cross-project-audit-prompt-as-high-leverage-protocol: Methodical Phase-1-2-3 audit (Council-Synthesized v1.0 prompt) with constitutional rules (empirical-before-assertion, design-never-apply, Chesterton's-fence, multi-source-verification, no-fake-metrics) surfaced findings that 15+ sessions of incremental development had missed. Centerpiece: `parseNetMessage` defined in S22 P3 (commit 318039f) for "defense against malformed peers" but NEVER wired at the recvFn call site in transport.ts — a SECURITY HIGH finding latent for ~6 weeks. Also caught: `applyNetSnapshot` throws uncaught into Pixi ticker (sync.ts:91), `resetAudioDrainCursor` unwired since S18 P1 (Δ4 carry-forward closed), 4 unused constants in constants.ts dating to initial commit, no README.md at repo root. **Lesson: scheduled adversarial audit catches what continuous incremental development misses. The K1+K3 pattern ("validator added but call site not wired") is invisible in normal review because each diff looks plausible; only the cross-cutting "is this fully wired?" question surfaces it. Pattern carry-forward: run this audit prompt every ~10-15 sessions, OR after any Standard/Full-tier batch that lands multi-file infrastructure (where the wire-up step is most likely to be missed).**

- S38 #audit-pass-2-self-check-catches-fix-induced-regressions: Pass 2 re-ran the same probes after the P1-P5 fix commit landed. Surfaced 3 NEW findings all traceable to the fix work itself: `ce51b032` (KNOWN_GAME_ACTION_TYPES allowlist as untyped Set<string> — maintenance trap), `d4541985` (parseNetMessage permissive schemaVersion=undefined carve-out for test back-compat — wire gap), `622a7c7f` (save.ts→render dep edge — architectural precedent). This is exactly Karpathy K4 (absent success-criteria) caught by re-checking. Without Pass 2, all three would have shipped as undocumented debt. **Lesson: every non-trivial fix batch deserves a Pass-2 re-audit before declaring done. The cost is ~10-15K tokens (re-run probes + diff against Pass 1); the benefit is catching fix-induced regressions while the implementation context is still warm. Pattern carry-forward: bake Pass-2 into the audit workflow — Pass 1 → fix batch → Pass 2 → optional Pass-3 → /handoff. Don't ship audit fixes without a verification pass.**

- S38 #record-of-union-literal-true-beats-set-of-string-for-closed-allowlists: Pass-1 introduced `KNOWN_GAME_ACTION_TYPES = new Set<string>([...19 literals...])` as the wire validator's source-of-truth for INTENT.action.type. Pass 2 caught this as TYPE_DEBT: untyped Set gives zero compile-time link to GameAction['type']. Pass-2 fix swapped to `Record<GameAction['type'], true> = { SPAWN_SPARK: true, ... }`, deriving the Set via `Object.keys(record)`. Now tsc enforces both directions: adding a kind to GameAction breaks the Record's exhaustiveness (missing key); removing a kind breaks the Record's key constraint (invalid literal). The "wire silently rejects valid INTENT" failure mode is impossible at typecheck. **Lesson: when implementing a closed-set runtime check against a structural union, prefer `Record<Union, T>` over `Set<string>([...literals])`. The Record gives bidirectional compile-time exhaustiveness for free; the Set gives nothing. Cost is identical (Object.keys traverse on import). Pattern carry-forward: any future closed-set allowlist (codex-recipe IDs, godly-effect kinds, godly-trigger types) should follow the Record pattern from the start.**

- S38 #publish-subscribe-seam-closes-layer-violation-with-zero-runtime-cost: Pass-1 fix 3c8630d7 added `import { resetAudioDrainCursor } from '../render/audioManager.ts'` at save.ts:57 — first state→render import in the codebase, breaking the conventional render→state direction. Pass 2 flagged as ARCH_DRIFT INFO. Pass-2 fix introduced `src/state/audioCursor.ts` as a single-slot publisher (`registerResetHandler` + `triggerReset`); audioManager registers itself at module-init via a side-effect import; save.ts and main.ts fire `triggerReset()` from inside the state lifecycle. State→render dep edge eliminated; grep `from '../render/'` under `src/state/` returns 0 real imports. Pattern: 60 LOC publisher + 80 LOC tests, single-handler (no Set), test-paths-that-bypass-audioManager get silent no-op via null check. **Lesson: when a fix forces a layer violation, a thin module-level pub/sub publisher in the lower layer restores the boundary at low cost. The handler-null-as-no-op pattern preserves test ergonomics (tests that don't load the render layer treat triggerReset as silent). Single-slot is correct when there's only ever one subscriber — generalizes to Set<() => void> additively. Pattern carry-forward: future state-layer events that need render-layer reactions (settings reset, world reset, audio-context-suspend) should use the same `src/state/<event>Cursor.ts` pattern.**

- S38 #defense-in-depth-at-the-peer-trust-boundary: parseNetMessage at wire (transport.ts:148) is layer 1; try/catch around applyNetSnapshot in sync.ts is layer 2. Either layer alone is insufficient: the validator can be bypassed if a future variant permits a too-loose check (Pass-2 d4541985 confirmed — undefined-carve-out was a real gap); applyNetSnapshot throws would crash the Pixi ticker without the catch. Both layers together bound the worst case to one dropped frame per malformed message. Pass 2 hardened layer 1 (strict schemaVersion check); layer 2 stays as belt-and-suspenders for the internally-inconsistent snapshot case (bond → missing primitive — schema-valid but referentially broken). **Lesson: at trust boundaries, layer the defenses. A single validator is a single point of failure; a validator + downstream guard means each layer can be hardened independently. The cost is small (try/catch + duplicate-ish checks); the benefit is no single-fix-broke-everything risk. Pattern carry-forward: any future peer-bound deserializer (save/load, IPC, etc.) gets the same two-layer treatment.**

- S38 #knip-unused-exports-need-per-symbol-chesterton-triage-not-blanket-deletion: Pass 1 P5 cleared 4 deletable constants (NET_CONNECTION_TIMEOUT_MS, BOND_LINE_WIDTH, BOND_GLOW_INTENSITY, AUDIO_MASTER_VOLUME_DB) out of 46 knip-flagged unused exports. Each one required git log -S '<symbol>' archaeology to confirm the Chesterton answer (initial-commit placeholder vs S15 P2 unwired plumbing). The remaining 42 are now individually triageable (loadFromLocalStorage = needs user product decision on Continue UI; audioManager test-only surface = move to test fixture; godlyRecipes types = public API for future recipes). Blanket deletion would have removed loadFromLocalStorage (latent feature) and audioManager.stopMusic (test infrastructure). **Lesson: knip's "unused export" signal is necessary-but-not-sufficient for deletion. Each symbol needs (a) git archaeology for the Chesterton answer, (b) cross-reference to design docs / BACKLOG, (c) classification as wire-up-missed vs speculative vs test-only-public. The audit downgraded all 46 from DELETE_CANDIDATE to DEPRECATE_CANDIDATE on this basis. Pattern carry-forward: per-symbol triage sessions, 5-10 deletions per session over multiple sessions — never one big sweep.**

- SESSION #s38-audit-campaign-batch-stats: 3 fix commits across 2 audit passes. Pass 1: 17 findings (0C / 2H / 4M / 8L / 3I); fix commit 5750cd6 closed 8 (47% same-session closure). Pass 2: 12 findings (9 carry + 3 new); fix commit da23b51 closed 3. Tests 729 → 740 → 745 (+16 total). Bundle 471.11 → 472.40 → 472.47 KB (+1.36 KB; 27.5 KB headroom). knip 46 → 42 unused exports. Deferred to dedicated sessions: vite/vitest CVE major bump (regression risk), main.ts hypertrophy refactor (multi-priority), Continue-UI product decision, per-symbol triage of remaining 42 exports. Session cost (Opus 4.7 1M per memory rule throughout): rough estimate ~150K-200K context window peak across audit + fixes + Pass 2 + fixes + handoff. **The audit prompt itself is the highest-ROI work this codebase has seen since the multi-frame animation Council deliberation in S25-S28 — single-session caught + closed a SECURITY HIGH finding that would have been one peer-malice incident away from a real bug.**

## 2026-05-18 — Session 37 Path B (Procedural Voltkin charge SFX + NetSnapshot frame-parity test — 2 priorities shipped of 20-priority S36+S37+S38 plan; commits f7f9f7c, fe2c0e2)

- S37 #council-success-after-S36-cancellation-skills-run-alone-pattern-validated: S36 reflexion #council-cancelled-mid-launch-via-parallel-tool-coupling codified "invoke Skill tools in their OWN parallel batch, never alongside environment probes that could fail." S37 applied this — A.0 state-discovery probes (Grep/Read/Bash) finished cleanly in their own batches, then Skill `council-of-models` invoked alone in its own batch, then `grok_chat` + `gemini_chat` fired in parallel as R1 Trident Strike. Council completed with 9-decision Battle Ledger, no vetoes, HIGH confidence. R1 surfaced material adopts (drain-parity test from Gemini; polyphony test + rollback ladder from Grok; click-free 5ms decay tail from synthesis). PRIME-AUDIT Δ1-Δ7 inline. **Lesson confirmed: the S36 cancellation pattern is fully avoidable with strict Skill-batch-isolation discipline. Pattern carry-forward: codify "Skills run alone" as a hard rule — any expensive deliberation invocation MUST be the only tool call in its batch. Environment probes go in batches BEFORE skill invocations, never alongside.**

- S37 #procedural-audio-synthesis-as-extensible-fartFreq-pattern: `playChargeSFX` mirrors the existing `playFartSFX` shape (S18 P1 originator) — sawtooth oscillator + biquad lowpass with exp-sweep cutoff + gain envelope (linear ramp + hold + exp decay tail) — with new constants tuned for "lightning charging up before it strikes" (sawtooth for electrical/buzzy harmonics, freq sweep 150→900 Hz rising for energy gathering, lowpass cutoff sweep 600→4000 Hz opening for muffled-buzz to piercing-arc feel, 5ms exp decay tail for click-free baton-pass into FIRE-tick `lightning-crackle.ogg`). Pure helpers `chargeFreq` + `chargeEnvelope` mirror `fartFreq` + `claveEnvelope` analytically, unit-testable without Web Audio. **Lesson: when adding new procedural SFX, find the closest existing template (fartFreq for sweep-based; claveEnvelope for impulse-based) and clone its shape. Cuts implementation time + makes the pure-helper test pattern obvious. The diagnostic-counter pattern (`xxxCallsTotal` / `xxxCallsSynthed`) is also worth cloning — jsdom-safe drain dispatch testing without AudioContext mocks. Pattern carry-forward: P8 FWOOSH form-swap SFX should follow the same shape (likely triangle or sine osc for warmer character vs sawtooth's electrical bite). Anvil + future creatures' procedural audio follow same pattern.**

- S37 #table-driven-it-each-for-fsm-walk-tests-was-right-call-over-individual-its: P10 test had 18 frame-derivation scenarios across 4 FSM states. Individual `it()` per scenario would be ~270 LOC + readability tax (same setup boilerplate 18×). `it.each(table)('description with $state $t', (row) => ...)` collapsed to ~70 LOC with the same coverage. Vitest's `$state` / `$t` substitution makes the test names descriptive at failure time. **Caveat: it.each requires a homogeneous helper signature — when killCount needed a 3rd parameter, I split into two separate it.each blocks (state×ticksInState walk vs DESPAWNING killCount sweep) rather than passing an `unknown?` field. Lesson: use it.each when 5+ scenarios share an assertion pattern with table-shaped parameters. Don't force heterogeneous params into one it.each table — split into homogeneous sub-tables. Pattern carry-forward: any future "walk every value of a discriminated enum × every tick boundary" pattern reaches for it.each first. Visible test count goes UP (729 individual rows for 18 scenarios) which is honest reporting of coverage breadth.**

- S37 #drain-parity-as-multiplayer-audio-guarantee-not-just-frame-parity: Gemini's R1 quality-ceiling note pushed P10 from "frame-derivation parity only" to "frame-derivation + drain-parity for CREATURE_CHARGE." Without drain-parity, the test would prove the joiner SEES the same sprite frame but not whether the joiner HEARS the same audio cue. Both halves are needed for "the multiplayer experience matches between peers." Drain-parity test asserts: host CREATURE_CHARGE in world.effects → netSnapshot serializes → applyNetSnapshot rehydrates → client.world.effects contains identical entry → client's `drainAudioEffects` would fire `playChargeSFX` at the same tick with the same pos. The actual SFX playback isn't testable in jsdom but the wire-integrity check covers everything up to the function call. **Lesson: when adding new wire-mirrored game-event-as-effect (here CREATURE_CHARGE; previously ARC_FLASH/BOND_FORMED/BOND_SEVERED), the parity test must cover BOTH visual AND audio surface end-to-end. Wire shape verified by save.test.ts round-trip; multiplayer parity verified by sync.test.ts host→snapshot→client→derive. The two together prove deterministic peer-mirroring of the new event class. Pattern carry-forward: P8 FWOOSH form-swap effect should also wire-mirror + parity-test if it's emitted host-authoritatively (likely render-side trigger which would mean no wire). P9 crown sprite is render-only (no GameEffect needed).**

- S37 #signal-rubric-external-user-facing-fired-for-audio-quality-pdr: Council §2.5 signal rubric assessment fired `external_user_facing` because user-perceptible audio quality matters — mandated Gemini participation in Council (no Grok-only 2-way fallback). Gemini's quality-ceiling push for drain-parity inclusion + the QUALITY/EFFICIENCY/COMPLETENESS scorecard surfaced ROI that pure-implementation lens (Claude+Grok) would have missed. Net result: P10 became GREATER scope (drain-parity added) AND the post-playtest fallback ladder got explicit (waveform swap → recorded sample → gain reduction). **Lesson: the signal rubric was load-bearing here. `external_user_facing` signal correctly identified that "user hears this code" warrants the quality lens. Without the rubric → Standard tier could have devolved to 2-way (cheaper) and missed drain-parity entirely. Pattern carry-forward: always run the signal rubric assessment BEFORE invoking Council, and let it shape the participant list. S37 batch was the first session to use the §2.5 rubric explicitly.**

- S37 #counter-hook-on-session-state-prevents-direct-edit-write-pattern: session-state.json is touched by a `state-autocommit` hook between every tool call (increments `tool_calls_session_total`). Result: Read + Edit/Write pattern hits "File has been modified since read" because the file genuinely changes between Read and Edit. Multiple Edit attempts during S37 P7+P10 work failed. Workarounds attempted: Write overwrite (also fails on subsequent reverts), parallel Read+Edit batching (still races against hook), direct content drafting (file content gets restored to S36 form). **Resolution for S37: deferred session-state.json full update to /handoff; reflexion_log.md is the canonical S37 record (no hook conflict). Lesson: file-touched-by-background-hook files can't be reliably Edit'd from a Claude session via the normal tool pattern. If a session-state update is critical (e.g., for the PDR gate), use surgical single-Edit operations with quick parallel Read+Edit — accept some retry latency. For routine progress tracking, document in reflexion_log.md and propagate via /handoff. Pattern carry-forward: investigate whether the hook can be paused during long sessions, or whether session-state.json should be split into a hook-owned counter file + a Claude-owned progress file.**

- SESSION #s37-pathb-batch-stats: 2 priorities shipped (P7 procedural audio + P10 NetSnapshot parity tests of 20-priority S36+S37+S38 plan). 2 commits (f7f9f7c P7, fe2c0e2 P10) pushed to master. Tests 680 → 729 (+49: +24 P7, +25 P10). Bundle 469.66 → 471.11 KB (+1.45 KB; 28.89 KB headroom on 500 KB cap; P10 test-only zero bundle delta). tsc clean throughout. Council R1 cost ~$0.05 (default models grok-4-1-fast-non-reasoning + gemini-2.5-flash). Real context 339K / 1M = 33.96% GREEN at session close. **Cost-benefit: ~5K Council R1 input + ~3K PRIME-AUDIT + ~30K execution (P7 + P10 combined, including emit-site debugging when initial test assertions failed because of targetGoneEarly FSM abort) + ~3K test-fix iterations = ~41K Standard-tier batch. Caught 9 deliberation findings → 2 ADDED-Grok + 1 ADDED-Gemini + 5 SYNTHESIS-or-CONCEDED + 0 BLOCKED, then PRIME-AUDIT raised 7 deltas resolved as Δ1-Δ7. Net: P7+P10 ship with stronger design than the v0 PDR (drain-parity + polyphony + click-free decay + fallback ladder were post-deliberation adds).**

## 2026-05-17 — Session 36 (Voltkin multi-frame animation + Transformation Arc — 6 priorities shipped of 20-priority S36+S37+S38 plan; commits 0c8700a..af76b76)

- S36 #council-cancelled-mid-launch-via-parallel-tool-coupling: Full-tier PDR launched Council deliberation via Skill tool in the SAME parallel batch as a Bash environment probe. The Bash chain used PowerShell `&&` which Win-PS-5.1 doesn't support → command failed → entire parallel batch errored → Council got cancelled with the error message "Cancelled: parallel tool call Bash(...) errored". 20K+ tokens of intended deliberation lost. **Lesson: any expensive Skill invocation should be in its OWN tool batch, never alongside environment probes or commands that could fail. The cancellation is silent at the protocol-flow level (PDCA pipeline doesn't know Council was cancelled, just that it never ran). Mitigation: (a) probe environment first in a small batch, (b) invoke Council in a separate turn with a clean batch, (c) if Council fails to launch, retry it explicitly instead of falling back to PRIME-AUDIT-only. S36 fell back to PRIME-AUDIT self-critique (Δ1-Δ7) under user-explicit-go which authorized rigor — documented but should not be the new norm. Pattern carry-forward: codify "Skills run alone" as a parallel-batch heuristic.**

- S36 #transformation-arc-from-style-mismatch-as-strongest-creative-interpretation: User-generated WINNER sprites came in TWO distinct art styles — boxy/chibi for idle/hurt/victory and lion/fierce for charge/zap. Initial reading was "style continuity bug — pick one and normalize." Stronger reading: deliberate Pokemon-style transformation mechanic where the creature morphs between forms at FSM boundaries (chibi at rest, lion in combat). Confirmed by user → mapped to existing FSM with NO new states: SPAWNING tail (chibi after lion-form cinematic-carry), SEEKING (chibi idle alternation), ATTACKING wind-up (chibi → lion), ATTACKING fire (lion zap), ATTACKING recovery (lion → chibi), DESPAWNING (chibi victory/hurt). Visual transformation flash (2-tick scale+cyan-tint punch) at form-swap boundaries sells the morph. **Lesson: when user-generated assets carry visible style/category contrast, the strongest creative interpretation is usually "the contrast is meaningful and deliberate" rather than "normalize and pick one." Map the contrast to a meaningful axis (rest vs combat, before vs after, etc.) and design around it. The contrast IS the design. Pattern carry-forward: Anvil + future creatures may also have multi-form sprites if user generates them deliberately — read the contrast before normalizing.**

- S36 #pure-frame-selector-as-side-of-render-coin-not-inside-renderer-class: Voltkin frame selection logic lives in `src/render/voltkinFrames.ts` as a pure module: `currentFrameKey(state, ticksInState, killCount) → VoltkinFrameKey`, `isLionForm(key) → boolean`, `flashIntensity(state, ticksInState) → 0|0.5|1`. Zero Pixi/Creature dependency. Renderer (`creatureRenderer.ts`) consumes these as a thin shell: per-tick lookup + texture swap from a preloaded Map. **43 unit tests** cover all FSM states + sub-phases + form-swap detection + empirical sync between currentFrameKey and flashIntensity — without ANY Pixi mocks. Compare to the renderer class (`CreatureRenderer.sync()`) which is hard to unit-test (Pixi DOM coupling + Assets.load network coupling). **Lesson: when designing animation/rendering systems, separate the SELECTION logic (what to show) from the RENDERING logic (how to show it). Selection becomes pure-function unit-testable; rendering inherits from selection. The same pattern applies to: which audio plays when, which color a UI element gets, which cursor shape shows. Pattern carry-forward: Anvil + Pac-Predator should also have their own `*Frames.ts` modules (e.g. `anvilFrames.ts`) following the voltkinFrames shape — selector + form-classifier + flash-detector.**

- S36 #empirical-sync-test-between-two-deterministic-functions: `currentFrameKey` and `flashIntensity` independently encode form-swap moments using separate constants (currentFrameKey: SPAWNING_MORPH_TICK=30, ATTACKING_CHARGE_ENGAGE_TICK=15, ATTACKING_IDLE_RELEASE_TICK=45; flashIntensity: hardcoded 30/31, 15/16, 45/46). If anyone changes one set without the other, the renderer's flash will fire on non-swap ticks or miss real swap ticks. Defended via empirical sync test: walk every tick of SPAWNING+ATTACKING, assert `flashIntensity(state, t) === 1.0` iff `isLionForm(currentFrameKey(state, t-1, 0)) !== isLionForm(currentFrameKey(state, t, 0))`. **Lesson: when two pure functions must agree on a derived predicate (here "is this tick a form-swap?"), don't trust manual review — write the cross-function consistency check as a test. Cheap (~20 LOC), catches the drift class of bug forever, and the test BECOMES the spec. Pattern carry-forward: any future "this constant must match that constant" coupling deserves the same kind of walk-the-domain assertion test.**

- S36 #killCount-as-render-derived-state-not-display-flag: When wiring DESPAWNING victory/hurt frame selection, the temptation was to add a boolean field like `creature.didConnect` or `creature.showVictoryFrame`. Wrong shape — that bakes display intent into game state and prevents future uses. Instead: added `creature.killCount: number` (additive in serializer; tick-deterministic increment in `applyCreatureAttack` success guard). Renderer derives `killCount > 0 → victory else hurt`. **Future-proof: kill-streak achievements, post-game scoring derivatives, multi-kill UI feedback, balance-tune "if creature lands ≥3 kills give extra cooldown" — all become 1-line derivations. Lesson: when adding state for a visual feature, ask "what is the underlying countable/measurable, not the display flag." Store the measurable; derive the display. Always cheaper than two fields drifting apart. Pattern carry-forward: Anvil's hurt/victory frames should also derive from a measurable (e.g. `anvil.smashCount`), not a flag.**

## 2026-05-17 — Session 35 P0 (1v1 join bootstrap deadlock — bug existed since S15 add497f, ~20 sessions; commit 1be5fce)

- S35-P0 #integration-blind-spot-since-S15-because-unit-tests-bypass-main-ts-inline-gates: 1v1 join bootstrap deadlock at main.ts:765 client-interpolation gate existed in production since S15 commit add497f (introduction of 1v1 networking). 20 sessions of subsequent 1v1 work (S20 error plumbing, S22 P3 godly broadcast, S31 P0-3 effects mirror, S33 P1-11 creatureId additivity, S34 multiple) were validated only via vitest unit tests that exercise ClientSync.receive + interpolateInto directly — bypassing the main.ts inline render-loop gate that prevents interpolateInto from being called at all on joiner. The boot-snapshot's recurring "1v1 brother retest pending" carry item across S31/S32/S33/S34 is the symptom: cross-network 2-peer playtest kept being deferred, likely because it was IMPOSSIBLE — the joiner never left LOBBY, so post-PLAYING bug retests (effects mirror, creature additivity) couldn't even be attempted. **Lesson: any code that lives inside main.ts inline ticker callbacks (or other Pixi/DOM-gated code paths) has ZERO unit-test coverage by default. The vitest pattern of "construct a World, call reducer/sync/lerp directly" mocks AROUND the main.ts wire-up. For state-machine wiring like the join/host handshake, the gates BETWEEN reducers and renderers are the highest-leverage bug surface AND the hardest to test. Mitigation options: (a) extract main.ts inline gate logic into pure helpers, (b) Playwright/jsdom integration test for the LOBBY→PLAYING transition, (c) explicit playtest gate in PDR execution order. Option (c) is what S35 P0 used — cheap but human-loop-dependent.**

- S35-P0 #asymmetric-host-joiner-setup-needs-symmetry-check-on-every-1v1-relevant-world-field: Host gets world.gameMode='1v1' via applyStartGame reducer dispatch at onBeginMatch. Joiner is supposed to mirror via NETSNAPSHOT apply, BUT the apply path is gated on gameMode='1v1' — Catch-22. Root cause is that onJoinAttempt sets isHost=false but not gameMode='1v1', breaking the bootstrap-from-snapshot pattern. **Lesson: any new world.* field that host sets via reducer should be checked for joiner-side parity in onJoinAttempt. Specifically: any field that is later used as a GATE for the snapshot-apply path must be set on joiner BEFORE first snapshot. Pattern carry-forward for S35+ Anvil + future networked features: when adding gameMode-style flags, audit whether they appear in render-loop gates that themselves gate the snapshot-apply path. The bootstrap-from-snapshot pattern is only safe for fields that AREN'T gates on the path that applies the snapshot.**

- S35-P0 #prime-audit-all-gates-sweep-before-state-mutation-in-entry-point-handler: Before adding `world.gameMode = '1v1';` to onJoinAttempt, PRIME-AUDIT swept all 11 `gameMode === '1v1'` references across main.ts (6 sites) + state/ (4 sites) + render/ (1 site) for misfire risk in the post-fix interim state (gameMode='1v1' + gameState='LOBBY' on joiner). Zero misfires found: 10 of 11 sites either additionally gate on gameState='PLAYING' OR are host-only / unreachable from joiner-LOBBY; only main.ts:201 dispatchFn intent-wrap activates earlier (benign — controls in LOBBY don't fire gameplay actions; UPDATE_AVATAR_POS leak applies as host no-op). **Lesson: when changing a world.* field in an entry-point handler (onHostStart, onJoinAttempt, onBeginMatch, onBackToTitle, etc), grep ALL `world.<field> ===` references repo-wide and verify no side-paths misfire in the new interim state. The all-gates-sweep took ~3 min and gave high-confidence safety on a 1-line fix. Pattern carry-forward: codify as a PRIME-AUDIT step for any handler-side world-field mutation.**

- S35-P0 #bootstrap-gate-catch-22-pattern-codified: A "bootstrap gate Catch-22" occurs when a gate-controlled state mutation is itself gated on the result of that mutation. main.ts:765 gates clientSync.interpolateInto on gameMode='1v1', AND interpolateInto is the only path that sets gameMode='1v1' (via applyNetSnapshot via applySnapshotCore at save.ts:313). Without an external bootstrap (setting gameMode='1v1' OUTSIDE the gate's control), the system is permanently stuck. **Lesson: any pattern like `if (world.X === V) { ... mutates world.X to V ... }` is a deadlock unless world.X is set to V somewhere else first. Audit checklist for future gate-and-mutate paths: (1) does the gate condition appear as the OUTPUT of the gated mutation? (2) is there an INDEPENDENT path to set the gate-condition? If (1) is yes AND (2) is no, it's a deadlock. Naming: 'bootstrap gate Catch-22'.**

- S35-P0 #user-driven-playtest-as-final-validation-gate-when-unit-tests-cannot-reach-the-failure-site: Trystero/Nostr P2P needs 2 separate browser instances (different signal IDs) — single-browser preview workflow cannot validate. Vitest tests prove the apply-side machinery works but cannot exercise the main.ts:765 inline gate itself. PDR execution order explicitly named "2-peer manual smoke" as the final validation gate, deferred to user after commit. **Lesson: when a fix targets an integration boundary that no unit/integration test can reach in CI (cross-network, DOM-gated, hardware-gated), the PDR must name the human-loop validation as an explicit step BEFORE the next priority starts. The session can declare "fix landed, unit tests green, awaiting 2-peer smoke" — not "fix shipped" — to keep the verification chain honest. Pattern: 2-stage close — "[ZERO] P0 fix landed pending 2-peer manual smoke" then "[ZERO] P0 fully validated after user playtest confirms."**

## 2026-05-16 — Session 34 Phase B (Fresh audit + cleanup: 9 priorities shipped, 3 audit-agent false-positives rejected; commits 89cb543..eaaa9eb)

- S34-PB #audit-agent-false-positive-rate-9-percent-control-flow-skipped: Fresh 4-parallel-agent audit produced 16 findings; PRIME-AUDIT rejected 3 (~9%) on control-flow grounds. Pattern: agents flagged "scary" code (div-by-zero, NaN underflow, atan2 corner case) without reading the 5-10 lines above for the guard that prevents the hazard. (1) `computeCreatureTint` div-by-zero — guarded by `ticksInState < FIRE_TICK` which is unreachable when FIRE_TICK=0 since ticksInState is unsigned. (2) `computeCreatureRotation` leanFactor NaN — guarded by `dist < 1e-6` epsilon. (3) `redundantBondTargets` atan2(0,0) — deterministic outcome, no actual ambiguity. Same pattern as S33 P1-7 + S34 P2-18 (audit flagged "redundant code" but existing comment documented intent). **Cumulative S33+S34 false-positive rate ~9% (3 of 32 audit findings). Lesson: audit-agent prompts are tuned to "flag scary patterns" but skip the 5-line context grep that would distinguish real hazards from control-flow-guarded code. Codify into PRIME-AUDIT — for any audit claim of div-by-zero / NaN / underflow / "race condition," grep ±5 lines for `if (... < epsilon)`, `if (... === 0)`, early returns, or branch guards. ~30 second check; saved 3 dead-code commits across two batches.**

- S34-PB #test-invariant-vs-runtime-guard-choose-by-reachability: PB-8 (FIRE_TICK invariant test tighten) considered adding a runtime guard `if (FIRE_TICK <= 0) return defaultTint` in computeCreatureTint. Grok HIGH-risk classification claimed "potential NaN in prod under corrupt save." PRIME-AUDIT analysis: VOLTKIN_CONFIG is a module-level CONSTANT (not save data); corrupt save can't mutate it; only source-code edit could change FIRE_TICK to 0; even then, control flow `ticksInState < 0` is unreachable since counter is unsigned. Runtime guard would be DEAD CODE. Test invariant tightening (`> 0` instead of `>= 0`) is the right defense — surfaces a mis-configured Anvil at test time, not prod runtime. **Lesson: when a "defensive runtime guard" is proposed, ask: can the guarded condition ACTUALLY occur in production? If the answer requires "developer edits source code to a value that breaks invariant," the right fix is a test-invariant assertion (catches at CI), not a runtime guard (dead code that bloats bundle + reads like real defensive logic). Test invariants document the contract; runtime guards bloat the binary.**

- S34-PB #council-q-pre-resolve-saves-revision-round: Gemini AUDITOR initial verdict was REVISE because the PDR had 4 Q's left open. PRIME-AUDIT pre-resolved all 4 during synthesis (PB-4 delete-not-rename, PB-5 new-describe-block, PB-8 invariant-only-not-runtime-guard, Q3 calibration noted-and-logged). Council 2nd-round (Gemini re-prompt) would have just confirmed the resolutions. **Lesson: PDR Q's structured as "A vs B vs C with reasoning" often have a 1-of-3 answer that PRIME-AUDIT can lock via evidence before R2. Saves a re-prompt cycle. Pattern: structure deliberation Q's to expose the highest-evidence answer rather than asking Council to gestalt-pick. Then synthesize with the evidence path. Two-round Council is only needed when the answer is genuinely contested by multiple competing valid frames.**

- S34-PB #micro-batch-aggregate-doesnt-need-standard-tier-deliberation-rigor-but-protocol-mandates: Phase B was 9 priorities, all individually Micro (mostly 1-LOC test or doc edits). Aggregate ~8K execution tokens. Protocol mandates Standard tier Council for batches in 10-30K range; this borderline batch (8-12K estimate, actual ~8K) could have been waived to Micro per CLAUDE.md "Auto-waived when priority_state:approved + unlock_source:user." User pre-approved + explicit-go was clear. **Lesson: when batch aggregate hovers near the Micro/Standard boundary AND all individual priorities are Micro AND user pre-approval is explicit, the Council pipeline is still cheap (~$0.10) but adds calendar-time friction. Calibration data: S34-PB Council R1 cost ~$0.04 (lite mode w/ short prompts), found 2 valuable synthesis adopts (Grok PB-4 rename concern → resolved as delete, Grok PB-5 separate-describe → adopted). Net value: positive but marginal. Keep Council on batch-aggregates because the 1-or-2 catches across 9 priorities are still meaningful, but consider lite-mode (Grok 1500-token + Gemini 400-token prompts) as the right cost/value tradeoff for batch-aggregate Standard.**

- SESSION #s34-phaseb-batch-stats: 9 priorities shipped (1 P0 doc, 6 P1, 2 P2). 5 commits (89cb543..eaaa9eb) — natural grouping (3 docs in one, 3 micro-tests in another, 3 standalone). Tests 620 → 625 (+5: +3 PB-5 creature replay, +2 PB-6 avatarPos). Bundle 0 delta — Phase B was 100% doc+test+dead-code-delete. P1-12 replay + new PB-5 creature replay both green. **Cost-benefit: ~6K front-loaded audit-agent tokens (4 agents × ~1.5K result tokens) + ~3K Council + ~12K execution = ~21K Phase B total. Caught 9 real findings + 3 false-positive rejections. Phase A + Phase B combined: 17 priorities shipped, 14 commits, 0 regressions, 37 net new tests. Single session bandwidth on Opus 4.7 1M context — 40.6% GREEN at close, plenty of headroom for /handoff overhead.**

<!-- S34 Phase A block pruned at S41 handoff (5 entries: #audit-finding-false-positive-pattern-fires-twice-in-2-batches, #council-helper-extraction-tradeoffs-tested-vs-untested-dom-code, #voltkin-config-as-anvil-prereq-byte-exact-via-replay-determinism, #council-low-roi-suggestions-when-cleanup-batch-meets-architecture-pushback, SESSION #s34-batch-completion-stats; archived in .handoff-archive/HANDOFF_2026-05-16_S34close.md and prior handoffs) -->
<!-- S33 block pruned at S40 handoff (6 entries: #audit-finding-can-be-false-positive-when-existing-comment-documents-intent, #council-reasons-from-generic-best-practice-prime-audit-brings-domain-evidence, #replay-determinism-test-catches-its-own-first-run-nondeterminism, #replay-test-as-guard-for-byte-exact-refactor-claim, #a0-state-discovery-gate-catches-backlog-scope-deltas-before-user-go, SESSION #council-prime-audit-pipeline-cost-benefit-ratio-real-numbers; archived in .handoff-archive/HANDOFF_2026-05-16_S33close.md) -->
<!-- S32 block pruned at S40 handoff (3 entries: #browser-cache-first-look-for-multi-system-asset-loss, #empirical-controlled-test-before-scope-amendment, #read-backlog-before-scoping-emergency-pdr; archived in .handoff-archive/HANDOFF_2026-05-16_S32diagnostic.md) -->
<!-- S31 block pruned at S39 handoff (full 6 entries archived in .handoff-archive/HANDOFF_2026-05-16_S31close.md) -->

