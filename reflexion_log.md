# SPARK — Reflexion Log

Cross-session learnings. Most recent at top. Entries pruned when total >50.

## 2026-05-16 — Session 31 (P0 batch: 5 audit fixes — Standard-tier Council R1 + PRIME-AUDIT 2 overrides + 1 scope amendment; commits f234279..ab30261 shipped sequentially)

- S31 #post-ship-audit-as-CHECK-phase-with-4-parallel-agents: User playtest was pending on shipped Voltkin (S30); user requested codebase audit "in the meanwhile." Fired 4 parallel general-purpose agents across non-overlapping dimensions (code-quality / test-determinism / runtime-correctness / docs-drift), each ~150-200 word self-contained prompt, ~600 word report. Returned 24 distinct findings across P0/P1/P2 severity. Single-Claude exploration would have taken 4-6× longer + missed dimension-specialization. User split into 3-session plan (P0 now S31, P1 S32, P2 S33). **Lesson: post-ship audit at strategic moments (between feature ships + user-playtest windows) catches latent bugs that R1+CHECK pipelines miss because R1 audits design pre-ship and CHECK audits implementation at-ship; neither audits the integrated whole 3+ sessions later. Cost ~$0.10 for 4-agent parallel audit; benefit was discovering 5 user-visible bugs before user playtest could surface them. Pattern: codify audit-as-CHECK-phase as opt-in step at end-of-batch for any Standard/Full PDR on shipped multi-session features; trigger when user signals "audit" or "check codebase" OR proactively before high-risk user touch points (live playtest, multiplayer test, public release).**

- S31 #prime-audit-overrides-council-unanimous-when-math-disagrees-with-imagery: Council unanimously chose Q1=B (spawn at fade-START 270 ticks, "emerge through fade" imagery). PRIME-AUDIT computed actual visibility math: at tick 270 `bg.alpha=1` (fade hadn't started); first 18 of 60 SPAWNING-animation ticks render BEHIND fading-down overlay, average creature alpha ≈50% during entry pulse. Option A (288 ticks = fade-END) gets 100% of pulse fully visible. Council reasoned by analogy ("dramatic emergence"); didn't tick-by-tick the alpha curve. Override Option A landed. **Lesson: when Council convergent on a design driven by aesthetic imagery (drama / emergence / impact), PRIME-AUDIT MUST verify the imagery matches the user's actual bug target via arithmetic — render-curve math, visibility timeline, contrast math. Override is justified when the math contradicts the imagery. Pattern: every PRIME-AUDIT pass should ask "what's the arithmetic that backs this aesthetic claim?" for any timing / alpha / sizing / coloring decision. If no arithmetic, compute it before accepting.**

- S31 #yagni-override-on-protocol-surface-additions: Council convergent on explicit `SCREEN_SHAKE` NetMessage for client-side shake trigger (architectural-purity argument: host as single source of truth). PRIME-AUDIT overrode to implicit ARC_FLASH-in-world.effects detection: 5 LOC scan vs ~25 LOC + permanent protocol surface widening. Future-compat argument for explicit ("what if Anvil wants shake without ARC_FLASH?") was hypothetical; Anvil hasn't shipped. Refactor when concrete consumer arrives. **Lesson: when Council's architectural-purity defense rests on a hypothetical future consumer (not a concrete one in the next session's PDR), YAGNI override is valid. Implicit-via-shared-state is cheaper AND symmetric (host already triggers shake on ARC_FLASH locally; client mirrors); explicit message is decoupled BUT speculative. Pattern: any "decouple X for future Y" Council recommendation must name Y as a concrete next-session PDR, else override per YAGNI and add a 5-line code comment documenting where the refactor would land when Y materializes.**

- S31 #code-evidence-rebuts-grok-fabrication-now-pattern-3x-observed: Grok DISRUPTOR R1 cited 5 specific failure modes for cinematicTimer deletion (Q4 "unsafe" ruling): duplicate creature IDs, skipped spawns, runGodlyMatcher early-resume, lastCinematicOwner staleness, reconnection apply race. PRIME-AUDIT investigation against actual code refuted EACH: single-slot pendingCreatureSpawn (main.ts:508-521) prevents dup IDs; matcher gated on activeCinematicPlayerId (main.ts:418) prevents early-resume; lastCinematicOwner watcher (main.ts:455-457) tracks correct transition; connection-lost path dispatches GODLY_ABORT which clears owner immediately. THIRD observation of code-blind-Grok-fabrication pattern (S30: 2 of 5 fabricated; S29: similar; now S31: 5 of 5 specifics fabricated, but the categorical concern "audit timer changes carefully" was legitimate). **Lesson: Grok's BLOCKER-HIGH classifications carry weight; Grok's SPECIFIC line:file citations against unread code DON'T. Treat the CATEGORY (timer race / dispatch dup / state-window) as signal worth investigating; treat the SPECIFIC failure modes as starting hypotheses to verify against code, not as findings to accept. Pattern: codify into council-of-models §3.4 — "For code-blind R1 audits citing specific file:line failures, PRIME-AUDIT MUST verify each specific claim against actual code BEFORE adoption or rejection. Fabrication rate ~30-50% across S29-S31 measurements."**

- S31 #pre-flight-warn-source-must-be-read-before-dismissed-as-false-positive: At S31 boot, my pre-flight assessment dismissed the "5 ACTIVE_PLAN voltkin files" WARN as a false positive because `Glob ACTIVE_PLAN_voltkin*.md` returned zero hits. Audit Agent D caught my error: pre-flight hook reads `.claude/plans-archive/` (not `.claude/plans/`); 5 files DID exist there with stale `STATUS: IN-PROGRESS`. My glob mismatched the hook's scan path. **Lesson: when dismissing ANY pre-flight WARN as false positive, verify by reading the HOOK SOURCE (typically in `~/.claude/hooks/pre-flight.sh`) rather than by inferring scan paths from project layout. Hook authors may scan unexpected paths (archive dirs, hidden dirs, non-standard locations). The 30-second `cat ~/.claude/hooks/pre-flight.sh` saves the embarrassment of mis-calling a real warning. Pattern: codify into pre-flight protocol — "to dismiss a WARN as false positive, cite the hook line that emits it + show the actual path it scans + prove that path lacks the offending files."**

- S31 #parallel-agents-as-CHECK-multiplier-with-strict-scope-partitioning: 4 audit agents in parallel with non-overlapping scope partitions (code-quality vs determinism vs runtime vs docs-drift) returned ~400-800 word reports each. Total ~$0.10. Findings did NOT meaningfully overlap — each agent surfaced distinct issues in its dimension. Single-agent broader-scope exploration would have hit context-window cliff before reaching all dimensions. **Lesson: for read-only audit / CHECK tasks on large codebases (>10K LOC), parallelization with strict dimension-partitioning beats single-agent broad sweep. Each agent's prompt MUST be self-contained (they don't see each other) and MUST be scope-fenced ("audit ONLY X, skip Y") to prevent overlap. Pattern: codify in handoff/audit skill — "for audit tasks ≥3 dimensions or codebase ≥10K LOC, prefer 3-5 parallel general-purpose agents with explicit scope fences over single-Claude exploration. Each agent prompt under 200 words, expected report under 800 words, total cost ~$0.10-0.15."**

- S31 #tests-locking-math-relationships-not-just-values: P0-1 added 4 tests asserting `cinematicMsToTicks(cinematicMs + sustainedEffectMs + FADE_MS) === 288` for Voltkin. The math is trivial; the value is computable by hand. But these aren't arithmetic tests — they're locking the CONTRACT "spawn delay covers full overlay-active window" against future independent tuning of the 3 underlying constants. If S32 retunes sustainedEffectMs from 500 to 200, the test fails AND signals "spawn-delay math now diverges from overlay-clear time" — exactly the regression class the test exists to catch. **Lesson: when fixing a bug whose correctness depends on a math relationship spanning multiple files / constants / modules, write tests that LOCK the relationship even if the value is computable by hand. The test isn't asserting arithmetic; it's asserting that the relationship holds as constants drift. Pattern: any fix that uses formula `f(c1, c2, c3) === target` where c1/c2/c3 live in separate files MUST land with a test asserting the relationship, named clearly enough that a future tuner sees WHY the test exists. Comment the test with the relationship's natural-language meaning.**

## 2026-05-14 — Session 30 (P0 Voltkin regression repair + alive pipeline — Standard-tier Grok pre-mortem only, Gemini quota exhausted; 6 commits dbd0e51..5648681 shipped overnight per USER PRE-APPROVED Scope Amendment)

- S30 #real-root-cause-was-overlay-timing-not-shader-or-fsm: Initial regression diagnosis pointed at (a) Texture.from(video) race + autoplay block + CORS for the "static voltkin not mp4" complaint, and (b) FSM stuck-state + missing lightning code for the "doesn't move/shoot" complaint. Mid-session code-reading revealed the ACTUAL root cause: creature spawns at world.tick+cinematicMsToTicks(cinematicMs)=world.tick+240 (4 sec into cinematic) with fixed 480-tick (8 sec) lifetime, AND the cinematic overlay covered the screen for cinematicMs(4000)+sustainedEffectMs(8000)=12s wall-clock — EXACTLY the creature's lifetime window. User saw only the static crossfadeCharacterSprite mount; the gameplay creature lived its entire 8s life under the opaque-black bg overlay. ARC_FLASH lightning was firing the whole time, invisible underneath. Fix was 2 LOC + 1 method deletion (voltkin.ts sustainedEffectMs 8000→500 + cutsceneOverlay.ts remove crossfadeCharacterSprite). **Lesson: when user reports "X is broken on shipped feature," map the WALL-CLOCK timeline of (overlay duration + creature lifetime + asset render) BEFORE diving into shader/network/CORS/autoplay theories. If the timeline shows feature-visibility-window perfectly overlaps obstruction-window, that's the bug class. Lifecycle math is the cheapest debug pass (5 min of reading + spreadsheet-style timeline mapping) and catches "the feature works but is hidden" bugs that look identical to "the feature doesn't work" bugs from outside. Pattern: include "feature-visibility timeline diagram" as a required diagnostic step for ANY user-visible regression on time-bounded features (cinematics, animations, spawn windows, fade-outs).**

- S30 #lightning-was-already-shipped-claimed-not-built: Boot prompt + S28 reflexion + handoff doc all framed S30 P0c as "BUILD cyan ARC_FLASH lightning bolt VFX (~120 LOC new src/render/arcFlash.ts)." Mid-session discovery: lightning was ALREADY shipped in S27 P0 at src/render/effects/arcFlash.ts (different path), wired end-to-end through creatureAttack.ts → world.effects → effectsRenderer.ts → drawArcFlash (5-segment jittered polyline, halo+core passes, 18-tick lifetime). The "user doesn't see laser" complaint was downstream of the S30-P0b overlay-timing bug, NOT a missing feature. Scope auto-corrected to "TUNE existing drawer for visibility" (~30 LOC additions: corona pass + bumped widths + duration). **Lesson: before scoping "build new" for ANY claimed-missing feature, GLOB the codebase for the relevant filename pattern (e.g., `src/**/arcFlash*`, `src/**/lightning*`, `src/**/bolt*`). Boot prompts and reflexion logs can mis-frame state when prior session's "deliverables shipped" notes conflict with later session's "what's broken" complaints. Pattern: A.0 STATE-DISCOVERY GATE should always include a Glob for keywords from the proposed PDR (asset name, mechanic name, file pattern). If hits exist, READ them BEFORE proposing "build new."**

- S30 #grok-pre-mortem-3-useful-deltas-2-fabricated-rejected-pattern: Solo Grok pre-mortem (Gemini quota exhausted) on locked PDR surfaced 5 findings. 3 were USEFUL (DG1 video.load()+currentTime nudge for first-frame decode; DG2 verify-handoff-before-FSM-edits; DG4 renderer post-FSM ticker-ordering). 2 were FABRICATED (Grok-2 invented `cutsceneOverlay.on('complete')` event pattern that doesn't exist in code — flow uses ctx.onComplete callback instead; Grok-5 invented "old + new shader coexist" state with no such file/import duplication). Both fabrications had useful KERNEL ideas (verify-handoff, verify-no-stale-state) that I extracted, while rejecting the specific (false) code claims. **Lesson: when running adversarial pre-mortem with LLM that hasn't seen full source code, EXPECT ~30-50% hallucinated specifics. Treat the audit as a SEED for thinking, not a deliverable. Extract the IDEAS / CATEGORIES of risk the auditor surfaces, then verify each against actual code. Don't accept findings on specifics without grep'ing the cited file:line. Pattern: format pre-mortem prompt as "list bug CATEGORIES you'd worry about" not "cite specific buggy lines" — reduces hallucination temptation. Score 3/5 useful is normal for code-blind adversarial audit.**

- S30 #gemini-2-5-pro-daily-quota-1000-requests-rule-17-2way-fallback: Mid-session pre-mortem audit: Grok call succeeded but gemini-2.5-pro returned 429 RESOURCE_EXHAUSTED "daily quota generate_requests_per_model_per_day limit 1000 exceeded, retry in ~4h42m." Rule 17 fallback "Gemini err→2-way" applied automatically: continued with Grok-only audit + documented quota exhaustion as operational fact in session-state. Session-state council_invoked field records this for transparency. **Lesson: gemini-2.5-pro has a hard 1000/day cap; check usage budget BEFORE invoking expensive premium models. If quota likely exhausted from prior same-day sessions, default to gemini-2.5-flash (cheaper, separate quota) OR document quota-exhausted in PDR risk register before launching session. Pattern: if session involves multiple Council/pre-mortem invocations, monitor quota usage early; fall back to flash variants for non-critical passes. Operational reality > protocol purity when budget hits ceilings.**

- S30 #user-pre-approval-overnight-execution-mode-pattern: User said "i pre-approve it now as i am going to bed. be pedantic about this. work this session thoroughly to completion and then run /handoff after running self audit." This created a NEW execution mode: standalone autonomous session with no mid-session approvals available. Required (a) FULL deliberation upfront (Grok pre-mortem; would have been 3-way Council if Gemini available), (b) locked PDR with NO ambiguity for execution path, (c) preview-tool screenshot self-audit since no user available to confirm visual quality, (d) /handoff at end for next-session user review. All 6 priorities shipped + verified + handed off in 1 turn-batch from pre-approval. **Lesson: when user signals "going to bed / overnight execution / work to completion," treat the session as ATOMIC: the only deliverable is a fully verified, fully committed, fully handed-off state. No partial wins. No mid-session pivots that require user input. Plan ALL pivots upfront. Use preview-tool screenshots as the verification proxy for "user would approve this." Pattern: in pre-approval mode, double the diligence on PDR locking + risk register; treat any "in retrospect we should have asked" as a session-blocking error since the user is unavailable.**

- S30 #preview-tool-ticker-pump-needed-for-headless-simulation-advancement: Preview-tool spark-dev sessions don't auto-pump app.ticker fast enough for full lifecycle verification. Took screenshots showing tick=259 right after dispatching GODLY_TRIGGER, but creature wasn't spawning. Forced advancement via `for (i=0; i<1000; i++) app.ticker.update(performance.now() + i*16.67)` advanced world.tick to 600 in microseconds, allowing post-cinematic state to be observed. Without this, would have waited 4-5 seconds wall-clock per screenshot (preview tab may be backgrounded / throttled). **Lesson: when using preview-tool for tick-based simulation verification, expect rAF throttling and call app.ticker.update() manually with synthetic deltaMS to fast-forward. The Pixi v8 ticker's update() takes a `currentTime` arg in ms; pass synthetic increments to drive world.tick deterministically. Replay-safe code (no wall-clock dependencies) makes this reliable. Pattern: include a "forced-tick-advancement" recipe in preview-tool verification workflow for any tick-gated visual feature (cinematics, FSM lifecycle, scheduled events).**

- S30 #verify-handoff-before-fsm-edits-paid-off: Grok pre-mortem DG2 "verify cinematic→creature spawn handoff before editing FSM" forced me to read main.ts startCinematicIfNeeded + pendingCreatureSpawn poll + creatureLifecycle.applyCreatureTick + creatureAttack + creatureAI BEFORE writing any FSM code. The reading revealed: (a) FSM was already correct; (b) ARC_FLASH was already wired; (c) the real bug was 50 ticks downstream in cinematic overlay timing. If I had skipped this step and added "diagnostic logging + fix FSM bug" code per the original P0b scope, I would have shipped non-improving changes. **Lesson: when fixing a "feature regression on shipped code," ALWAYS read the full chain (state machine → orchestration → render → display) BEFORE editing ANY layer. The pre-mortem DG2 codifies this as "verify handoff before edits." Saved one wasted priority's worth of work. Pattern: for any regression fix that touches state machines, add a mandatory pre-step "read the full data-flow chain end-to-end" before scoping edits. Catches "the bug is elsewhere" cases that confirmation bias would miss.**

- S30 #standard-tier-check-degraded-to-screenshot-self-audit-when-gemini-unavailable: Rule 17 specifies Standard-tier CHECK uses Triumvirate (RALPH + GROK-ANALYST + GEMINI-AUDITOR). With Gemini quota exhausted, degraded to RALPH+GROK-ANALYST self-audit + preview-tool screenshot self-audit. Documented as `active_check_triumvirate: "inline:S30-CHECK-via-preview_screenshot-..."` in session-state for transparency. The screenshot evidence (mp4 played, creature visible, lightning bolt rendered) was independently verifiable from the artifacts (preview_console_logs + screenshots). **Lesson: in operational degraded modes (model quota / API outage / preview-env limitations), DOCUMENT THE DEGRADATION in session-state.active_* fields explicitly. Don't pretend Triumvirate ran; record what ACTUALLY happened. Future sessions reading this will know to revisit if budget allows. Pattern: every protocol step needs both an HAPPY-PATH execution recipe AND a DEGRADED-PATH recipe with documentation requirement. When degraded, prefer "documented partial verification" over "claimed full verification."**

## 2026-05-14 — Session 29 (P0a Cinematic shader fix — Standard-tier Council R1+R2 + user-requested PRE-MORTEM Triumvirate audit; P0b rig system DEFERRED to S30 pending GIMP slice)

- S29 #user-requested-pre-mortem-found-11-issues-r2-synthesis-missed: User explicitly requested adversarial PRE-MORTEM audit AFTER R2 Battle Ledger was already presented but BEFORE scope lock. Grok-pre-mortem + Gemini-pre-mortem (CHECK-mode adapted for pre-implementation) + RALPH:PATROL self-audit surfaced 11 CRITICAL/HIGH findings the R1+R2 deliberation missed. Top fatal: "rig pays back across godlies" payback claim was FALSE — Anvil + Pac-Predator are non-humanoid (smithing hammer + maze chomper), a Voltkin-tuned skeletal rig is Voltkin-specific cosplay disguised as infrastructure. BOTH Grok and Gemini independently raised this. Also: Imagen body-part reference-conditioning UNVERIFIED THEATER (6 prior iteration failures = species warning, not retry budget); no success metric (could build right system delivering wrong feeling); spring-jiggle non-deterministic in 1v1 (LUT only fixed easing curves, NOT spring integration over Verlet velocity); problem-definition risk (Council interpreted "alive" mechanically when user reference was MapleStory mob). **Lesson: R1+R2 Battle Ledger deliberation can rubber-stamp shared blind spots. Pre-mortem with adversarial framing ("find what we MISSED, not what we chose") is materially different from CHECK Triumvirate (post-impl). Cost ~3K Grok + Gemini API tokens + ~5K Claude synthesis (~$0.05) to find 11 issues that would have cost 25K+ tokens to discover during execution + rebuild. Pattern: for ANY Standard/Full-tier PDR touching asset pipelines, network determinism, OR cross-target payback claims, run a 1-round pre-mortem BEFORE locking scope. Frame the question as adversarial: "what could screw us that we DIDN'T plan?" + "better paths not in the Battle Ledger?" Don't ask "is this plan good?" — confirmation bias kills the audit value.**

- S29 #imagen-reference-conditioning-non-functional-with-api-key-auth: `gcp-vertex` MCP's `imagen_edit` tool description explicitly warns: "Currently non-functional with API key auth. Kept for future Vertex AI migration." Means reference-image-conditioned editing is NOT available in this auth setup — only text-only `imagen_generate` works. R2 Battle Ledger assumed reference-conditioning was viable for body-part generation. PRE-MORTEM Grok flagged "Imagen reference conditioning is unverified theater"; A.0 probe attempt to load the tool confirmed the description's warning. **Lesson: before proposing any plan that depends on "Imagen with reference image" workflows, READ the MCP tool description for capability limits. Don't assume reference-conditioning is available across all Imagen-related MCP tools. For style-locked characters this collapses Imagen-based asset prep paths to text-only generation, which has materially lower style fidelity. Pattern: when a tool has "non-functional with X auth" / "Vertex AI only" / similar caveats in its description, flag during PDR draft as a HARD constraint. Don't discover during execution.**

- S29 #imagen-text-only-cannot-hold-locked-canonical-style: A.0 STATE-DISCOVERY probe — $0.02 imagen-4.0-fast-generate-001 call with detailed text prompt (matching canonical Voltkin colors/proportions/features/style references). Produced a recognizably similar but visibly DIFFERENT voltkin — different fur texture, face proportions, crystal arrangement, "kawaii" vs "feral" expression balance. Same style-drift problem user explicitly rejected from 6 prior iterations. The 6 prior failures + this 7th attempt = 7 confirmations the path doesn't work for THIS specific character. **Lesson: when a user has locked a canonical character design, do NOT propose Imagen-generated body parts, pose variants, or animation frames as the asset strategy — even with detailed style-prompting. Text-only generation cannot hold a locked style; you get a same-archetype-different-character every time. Manual slice from the canonical (Photoshop/GIMP) is the only reliable path. Pattern: if a PDR depends on N≥2 visually-consistent generated assets from a style-locked canonical, mark it RED-RISK in the risk register. If reference-conditioning is also unavailable (see above), demote the PDR to "blocked on manual asset prep" and pivot scope.**

- S29 #alive-needs-concrete-reference-not-mechanical-interpretation: User said "I want him to be a living game entity that moves by actually moving his limbs." Council R1+R2 interpreted mechanically (= skeletal rig with bone articulation = 350 LOC infrastructure). User actual reference (revealed only when frustrated by repeated option-picking and ambiguity): "a MapleStory enemy other in game mobile enemy that moves his legs, his mouth, seems like he's breathing and talking. does some sort of complex motion, targets things." Concrete reference made the architecture obvious — keyframe pose interp at 8-12 fps perceived motion, no skeletal rig needed. **Lesson: when user uses abstract emotional words ("alive", "snappy", "polished", "professional"), PROBE FOR A CONCRETE REFERENCE before designing. "What game / scene / video clip should this feel like?" before "here are 4 architectural options." Saved one Council R3 + one architecture pivot. Pattern: in user-clarification turns, prefer "show me what alive means" over "pick A B C D" because users don't know architecture but they know references. Codify into Standard/Full PDR template: REFERENCE-ANCHOR field, mandatory for any UX/feel/aesthetic scope.**

- S29 #rig-pays-back-claim-must-be-roadmap-verified: R2 synthesis claimed "rig pays back across Anvil + Pac-Predator + future godlies — invest in a SYSTEM, not a one-off." Pre-mortem both Grok AND Gemini independently flagged FALSE — Anvil is a smithing-hammer creature, Pac-Predator is a maze-chomping entity. Neither shares Voltkin's humanoid topology. A skeletal rig tuned for Voltkin's body plan would be 530 LOC + JSON tuning hell that becomes legacy ballast the moment Anvil ships. **Lesson: "pays back across X" claims in deliberation MUST be verified against the actual roadmap entities + their topologies, not assumed from "this is a system" framing. If next 2 consumers don't share the topology / interface / abstraction shape, accept the system as per-character bespoke and scope accordingly. Pattern: when R1 synthesis includes "reusable / pays back / generalizes" claims, add a Q-row to Battle Ledger explicitly probing: "Name the next 2 consumers. Do their topology / interface / requirements actually match? If not, demote payback claim." Both Grok and Gemini caught this in S29 pre-mortem; pre-emptive Q in Council R1 next time.**

- S29 #glsl-es-300-vs-legacy-100-silent-shader-failure-class: cinematicLumaKey.ts fragment shader mixed GLSL ES 300 `in vec2 vTextureCoord` syntax with legacy GLSL 100 `gl_FragColor` (removed in 300 ES). Under Pixi v8's auto-300-version prelude this caused silent compile failure — filter never applied, video sprite never painted frames, surface symptom was "black screen instead of mp4 cinematic." Bug had been latent since the file was authored (S22 P3). User had been seeing it without diagnostic until S29 user-frustration-flag prompted code review. Pre-mortem Grok-#5 caught it from code review alone — independent verification. Fix: declare `out vec4 finalColor;` per 300 ES spec, write to `finalColor` (not `gl_FragColor`). **Lesson: when Pixi v8 / WebGL 2 sprites silently fail to render and console is clean, suspect SHADER-VERSION-SYNTAX-MISMATCH as a high-prior class. Check that fragment shaders declare `out vec4 ...;` (not `gl_FragColor`) and match the vertex shader's `in`/`out` GLSL 300 ES syntax. Pixi prepends `#version 300 es` automatically — DON'T add it manually in source. Pattern: any new Pixi v8 shader code MUST be reviewed for GLSL ES 300 syntax consistency; ban `gl_FragColor` in lint config if possible.**

- S29 #askuserquestion-tool-can-return-empty-answers: AskUserQuestion tool returned empty answer strings twice this session — once on architectural pick (Anvil/Pac-Predator/Bond UX/NET), once on animation depth (Pose-cycle/Articulated/Both/Different). Both times the picker fired without capturing the user's actual choice; inline ask-in-prose worked on retry. **Lesson: don't rely on AskUserQuestion for critical decision points. Embed the question inline in response text instead. AskUserQuestion is OK for low-stakes ordered preferences ("which file format do you want?") but unreliable for choose-1-of-N architectural decisions. Pattern: if a question is load-bearing for PDR scope, ask it inline in prose with numbered options, not via AskUserQuestion.**

- S29 #pre-mortem-cheaper-than-rebuild-codify-protocol: Pre-mortem audit consumed ~3K tokens of Grok + Gemini API calls (~$0.05) and ~5K Claude tokens for synthesis. Caught 11 issues that would have cost 25K+ Claude tokens to discover during execution + rebuild. Cost-benefit ~5× favors pre-mortem on any Standard/Full-tier PDR touching architecture, asset pipelines, or cross-target claims. THIRD observation total of pre-mortem-finds-rubber-stamp-issues pattern (S27 didn't run, S28 PRIME-AUDIT served similar role for code-execution refactors). **Lesson: codify pre-mortem as a Standard/Full-tier protocol addition between R2 synthesis and user-presentation. The Council's R1+R2 generates the plan; PRIME-AUDIT self-checks the synthesis (~2-3K cost); PRE-MORTEM checks what the Council collectively missed (~5K cost). Three distinct lenses, each catching different bug classes. Pattern: when seen ≥3 times productive in same project, codify into protocol. Two observations now (S29 explicit + S28-equivalent via PRIME-AUDIT extension); if S30 confirms third utility, propose Council-protocol amendment to add PRE-MORTEM Triumvirate as MANDATORY step before user-presentation on Full-tier (>30K).**

## 2026-05-14 — Session 28 (P0 Voltkin Phase 2D polish — Standard-tier Council CODE-EXECUTION; Phase 2 finale)

- S28 #a-0-state-discovery-should-enumerate-assets-not-just-code: A.0 STATE-DISCOVERY GATE pre-PDR ran 7 probes; the BIGGEST delta wasn't in the code — it was in the asset pack. Blueprint § S28 AC#1 assumed a 14-frame Imagen spritesheet was on disk; reality: side-session 2026-05-13 archived 5 character sprites as `off-model-v1/` (explicit "do not regress") with only `voltkin-zap.png` canonical, AND `lightning-crackle.ogg` (18 KB) existed in `assets-source/audio/` but was NOT deployed to `public/`. Surfacing these BEFORE PDR lock enabled a user-locked scope pivot to "procedural transforms on zap.png + deploy existing OGG" instead of running another v2 Imagen side-session ($5-10 + 1-2 hrs + off-model retry risk per S22 history). **Lesson: A.0 STATE-DISCOVERY GATE must enumerate AVAILABLE-ASSETS as a first-class probe target, not just code-state. Probes for "where does X live?" should grep both `src/` AND `assets-source/` + `public/` because a "missing asset" can mean (a) genuinely needs generation OR (b) exists upstream but isn't promoted. Pattern: for any PDR whose scope mentions "generate spritesheet" / "create asset" / "side-session needed," first probe `assets-source/<package>/*` AND the relevant `public/` mirror — the side-session may have already shipped the deliverable but the integration step never landed. Cost: 1 extra ls + glob; benefit: skip a redundant side-session.**

- S28 #council-q-compromise-game-feel-at-equal-loc-cost: Q3 wind-up tint curve: Grok-A (linear) + Claude default-A (linear) vs Gemini-B (ease-in `t²`). Grok's argument was "simpler"; Gemini's was "better game-feel for a charging-up moment." Battle Ledger took COMPROMISE B (Gemini minority overruling Claude default + Grok) because BOTH options are 1 LOC each — the complexity argument doesn't survive when costs are equal. `progress = ticksInState / 30; eased = progress * progress` is one line; `eased = progress` is also one line. **Lesson: when "simplicity" is invoked as a Council defense in R1, verify the cost asymmetry is REAL (LOC, branches, tests, mental load). If both options have equal cost, defer to the game-feel/UX/correctness argument from the minority. The "simpler is better" heuristic only applies when simpler is also CHEAPER; at equal LOC, simpler-but-worse-feel loses to ease-in-but-equal-cost. Pattern: in Battle Ledger triage, if Grok+Claude vote A and Gemini votes B, audit the LOC delta between A and B — if it's ≤2 LOC, the simplicity defense is moot.**

- S28 #trimmed-wire-shape-when-consumer-is-render-only: Q4 SerializedCreature: Claude default-A full mirror (80 B); Grok-B + Gemini-B trimmed 36 B. Client renderer reads only (pos, state, ticksInState); it doesn't simulate AI so targetBondId / targetPos / prevPos / ownerPlayerId / spawnedAtTick / despawnAtTick are all dead bytes on the wire. Council 2/3 B over Claude-A. Net savings: ~44 B/creature × 2 max creatures = ~88 B/snapshot × 10 Hz = ~880 B/sec. Small absolute but >2× wire efficiency. **Lesson: when serializing actor state for a RENDER-ONLY consumer, the trim is ALWAYS smaller than the actor's full struct. Project the consumer's needs forward — what fields does the consumer's hot path actually read? — and omit the rest. Default to mirror-the-full-object only when the consumer also simulates. Pattern: for any net-serialization Q, ask "what does the consumer use this for?" If render-only, trim. If sim-rebuild, mirror. Council heuristic: full-mirror is the lazy default; trimmed render-only is the principled choice when consumer scope is bounded.**

- S28 #cross-council-unanimous-check-finding-now-3x-observed: CHECK Triumvirate found 2 cross-Council UNANIMOUS issues — Grok-C1 + Gemini-G1 nextCreatureId collision (P0) AND Grok-C2 + Gemini-G4 recovery scale denominator off-by-one (P2). Both surfaced independently with identical proposed fixes. This is the THIRD observation of the cross-Council-UNANIMOUS CHECK pattern (S27 had Grok-C4 + Gemini-G5 ARC_FLASH determinism). **Lesson: cross-Council-source UNANIMOUS findings during CHECK Triumvirate are the highest-confidence accepts in the entire pipeline — higher than Council R1 unanimous (which votes on PRE-FORMULATED structured Q's; CHECK finds organic bugs in landed code). Pattern: when Grok-C{n} and Gemini-G{m} both surface the same issue with the same fix, accept immediately without further deliberation — that's wasted tokens. Three observations in three sessions = systematic, not noise. Codify: cross-Council UNANIMOUS CHECK = auto-accept; cross-Council 2/3 = audit-before-accept; single-source = weigh against R1 context.**

- S28 #gemini-check-redesign-self-contradiction-now-3x-observed: Gemini G2 proposed `Array<{ fireAtTick, event }>` queue redesign for pendingCreatureSpawn — directly relitigating Council Q2 UNANIMOUS A single-slot (which same Gemini voted A in R1 ~30 min earlier). Same self-contradiction pattern as S26 Gemini-Q7+G2 (helper-export redesign) and S27 Gemini-Q1+G1 (re-dispatch contradiction). THREE consecutive sessions, same model, same pattern. **Lesson: Gemini's CHECK pass evaluates landed code WITHOUT R1 deliberation context (stateless), so it sometimes proposes redesigns that R1-Gemini already rejected. The fix is procedural: when CHECK output reads like "this design is wrong, switch to X," compare against R1 record — if same model voted differently in R1, REJECT with R1 cite. Don't waste tokens debating; the deliberation context already won. Pattern: codify into Council protocol — "CHECK Gemini may propose redesigns previously rejected; tag with R1 cite + reject, no debate." Same logic applies to any model that votes contradictorily R1-vs-CHECK; the systemic Gemini case is now well-documented (S26/S27/S28 = 3 consecutive), but Grok could do this too.**

- S28 #wall-clock-state-mutation-refactor-pays-prime-audit-dividend: S25 carry-forward refactor (replace wall-clock setTimeout in cutsceneOverlay.ts:152 with tick-deterministic `world.pendingCreatureSpawn` + poll). PRIME-AUDIT pre-impl flagged Δ4 (cinematicMsToTicks Math.round not floor — for 4017 ms boundary) AND Δ5 (GODLY_ABORT must clear pendingCreatureSpawn — otherwise zombie spawn after peer-drop). Both deltas surfaced during the adversarial-audit pass BEFORE landing code — meaning the refactor would have shipped with two boundary bugs without PRIME-AUDIT. **Lesson: refactors that explicitly invoke "S25 reflexion: never mutate from wall-clock setTimeout" get PRIME-AUDIT bonus scrutiny on the new tick-math because that's exactly where the bugs hide. The replacement is a state-mutation seam → audit at the seam, not at the call site. Pattern: when a refactor moves logic FROM `setTimeout` callbacks INTO tick loop polls, PRIME-AUDIT explicit checks: (a) tick-math rounding direction at boundaries; (b) cancellation paths for the new state flag (abort, disconnect, save-load); (c) replay determinism (same inputs → same fireAtTick); (d) catch-up scenarios for skipped ticks (use `>=` not `===`). All four caught in S28 PRIME-AUDIT; would have leaked to CHECK or production otherwise.**

## 2026-05-14 — Session 27 (P0 Voltkin Phase 2C AI + attack — Standard-tier Council CODE-EXECUTION)

- S27 #council-unanimous-overrules-claude-default-when-future-state-architecture-cleaner: Q1 Council R1 was UNANIMOUS B (extend SEVER_BOND.cause + re-dispatch from applyCreatureAttack); my Claude default was A (inline severance mirror of GODLY_TRIGGER pattern, "consistent with current codebase"). Both Grok and Gemini independently chose B with the same rationale: post-S27 the GODLY_TRIGGER cascade is DELETED, so the "current codebase pattern" Claude was preserving WAS THE DELETED CODE. Council saw the future state ("only one place severs bonds, full stop") and chose architecture for that state, not for the transition. The cost: one re-dispatch from a reducer (CQS exception, well-documented as Council-sanctioned). The gain: zero severance code duplication post-S27. **Lesson: when Claude defaults to "consistent with current codebase" but Council unanimously picks "consistent with future state after planned migration," Council wins. Past-codebase-consistency is a weak signal when the past code is about to be deleted. Project the architecture forward by one milestone before voting on consistency. Pattern: in migration-flavored PDRs, ask "what does the codebase look like POST-migration?" and prefer designs consistent with that, not the transitional state.**

- S27 #council-q-compromise-tunable-constant-defers-the-fight-cheaply: Q2 had Grok-A (fire at tick 0 entry — instant zap then 60-tick recovery, "reactive feel") vs Gemini-B (fire at tick 30 middle — wind-up + zap + recovery, "telegraphed for animation"). At S27 there's NO animation yet (S28 ships sprites), so the "wind-up frames" are dead air. Argument for A was stronger IN ISOLATION. Compromise: VOLTKIN_ATTACK_FIRE_TICK=30 (Gemini's value) but exposed as a NAMED TUNABLE CONSTANT. S28 retunes via single-LOC change if animation timing wants entry-fire or different middle position. The compromise wasn't "split the difference at tick 15"; it was "lock Gemini's value now AND make the tuning point obvious for S28." **Lesson: when a Council split is about a numeric tuning value (timing, count, threshold) AND the right answer depends on future work (animation, asset, scale), don't pick the median — pick one position AND name the constant. The compromise is "I picked X, here's the constant; future work changes one line." This is cheaper than re-deliberating in S28 because the constant + docstring captures BOTH R1 arguments for future readers. Pattern: numeric-magnitude Q-disagreements → always become named constants, never inlined magic numbers.**

- S27 #state-discovery-probe-finds-blueprint-vs-actual-deltas-before-impl: Blueprint § "S27 migration notes" claimed "REMOVE: ~5-8 tests that assert GODLY_TRIGGER cascades SEVER_BOND on target component (these tests currently exist for the Phase-1 cascade)." A.0 STATE-DISCOVERY GATE grep through godlyReducer.test.ts + sever.test.ts + disruptionManager.test.ts found that NO test asserts the cascade behavior. The blueprint claim was based on a reasonable assumption (Phase-1 ships meant Phase-1 tested) that was empirically false. Surfacing this as a PDR-time Δ adjusted the PDR scope from "5-8 removed + 12-15 added" to "0 removed + 15-18 added." If this Δ had not been surfaced, S27 implementation would have hit it as a "where are the cascade tests?" 30-minute detour mid-impl. **Lesson: every blueprint claim that touches EXISTING files (not net-new code) needs a State-Discovery probe to verify the claim against current repo state. Architectural claims about new code can be deferred to impl; claims about existing code MUST be probed BEFORE the PDR locks scope. The blueprint is a one-time deliberation artifact; the repo evolves. Pattern: A.0 should grep every "DELETE / REMOVE / MIGRATE / RENAME" verb in the blueprint and verify the target exists with the asserted shape.**

- S27 #check-triumvirate-relitigation-of-r1-design-must-reject-with-cite: Gemini G1 in CHECK said "applyCreatureAttack reducer dispatches SEVER_BOND — architectural anti-pattern, violates Q6 A main.ts dispatch decision." Same Gemini voted Q1 B (re-dispatch SEVER_BOND from applyCreatureAttack) in R1 just ~20 minutes earlier. This is direct R1-vs-CHECK self-contradiction from the same model — IDENTICAL to S26's Gemini Q7-vs-G2 contradiction pattern. Resolution: REJECT G1, cite the R1 vote, refer to S26 reflexion #5 lesson "R1 deeper deliberation wins; CHECK should find IMPLEMENTATION bugs, not relitigate DESIGN." **Lesson: this contradiction pattern is now seen TWICE (S26 Gemini Q7+G2, S27 Gemini Q1+G1). It's not random — it's a systematic Gemini-behavior where CHECK pass evaluates landed code WITHOUT the R1 deliberation context (each call is stateless). CHECK Triumvirate's value is finding IMPLEMENTATION bugs (boundary off-by-ones, race windows, missed edge cases). When CHECK output reads like "this design is wrong," reject with R1 cite — the deliberation context was the value-add R1 provided. Pattern: log "CHECK G* relitigates Council Q* — REJECT, R1 deliberation context wins" as the canonical rejection rationale.**

- S27 #cross-council-unanimous-check-finding-multi-creature-determinism: Grok C4 (ARC_FLASH pseudoRand seed = effect.tick only → same-tick arcs from different creatures get identical jitter) + Gemini G5 (same finding, identical proposed fix). When BOTH CHECK members independently surface the same issue with the same fix, that's the strongest possible CHECK signal. Distinct from R1 unanimous (which is on a structured question); CHECK unanimous is independent discovery on free-form audit. **Lesson: track CHECK Triumvirate findings by cross-source pattern: (a) UNANIMOUS across Grok + Gemini + RALPH = highest-confidence accept; (b) 2/3 (any two) = strong signal, audit before accept; (c) 1/3 = single-source signal, weigh against R1 context. The ARC_FLASH determinism finding was (a) UNANIMOUS — Grok C4 + Gemini G5 — and got immediate accept with no R1 contradiction. Pattern: cross-Council-source UNANIMOUS findings are the most reliable CHECK output, more so than 3-concern lists where reviewers disagree.**

- S27 #boundary-off-by-one-check-catches-what-prime-audit-missed: Δ4 wind-up abort transition condition was `creature.ticksInState < VOLTKIN_ATTACK_FIRE_TICK` — at exact boundary ticksInState===30, the abort doesn't fire. main.ts step 3 would then dispatch CREATURE_ATTACK with a missing target, applyCreatureAttack would no-op (defense-in-depth), but the user would see a visible attack-fire moment with NO ARC_FLASH (benign visual gap). My R1 + PRIME-AUDIT + RALPH self-audit all missed this exact-boundary edge. Gemini G3 in CHECK caught it. Fix: `<` → `<=`. **Lesson: any "during window X" check where X is a discrete integer needs SPECIFIC test coverage of (a) X-1 inclusive, (b) X exact, (c) X+1 exclusive. PRIME-AUDIT's 5 questions should include "for every discrete boundary in the diff, what happens at exactly the boundary value?" CHECK pass IS the right place to find these — they're implementation details that R1's design-level review doesn't probe. Pattern: integer-boundary checks in the diff are highest-yield CHECK targets; always probe X-1 / X / X+1.**

## 2026-05-14 — Session 26 (P0 Voltkin Phase 2B physics + locomotion — Standard-tier Council CODE-EXECUTION)

- S26 #cross-resolve-delta-better-than-two-separate-mitigations: Council R1 produced TWO different Q7 failure modes — Gemini SPAWNING+repulse momentum trap (creature spawned inside spawner-zone with target on opposite side accelerates AWAY for 60 ticks) AND Grok DESPAWNING substep verlet drift (creature accelerating during fade window). My instinct was two separate mitigations (blend-factor for SPAWNING + state-gate on DESPAWNING). PRIME-AUDIT Δ4 collapsed both into a SINGLE rule: `computeSteeringAccel` returns ZERO_ACCEL when state !== 'SEEKING'. One rule, both failure modes prevented, simpler invariant. **Lesson: when 2 R1s flag related-but-different failure modes around state-machine transitions, look for the single rule that gates BOTH. The pattern is "force-active only during a specific positive state" rather than "force-disabled during each problematic state." More elegant + fewer code paths + easier to test. Generalizes to any FSM-driven entity where forces or effects should be state-gated.**

- S26 #council-q-compromise-better-than-either-2of3-option: Q4 had Gemini argue per-behavior pure functions (`seekForce` / `arriveForce` / `repulseForce`) for FSM-composability + testability vs Grok argue single combined `computeSteeringAccel` for simple public API + perf (avoiding call overhead at 480Hz). Both arguments had merit. Compromise: combined PUBLIC `computeSteeringAccel` (Grok's surface) + 3 EXPORTED per-behavior helpers (Gemini's atomic testability). Cost: ~0 LOC vs either pure option; tree-shaker may eliminate unused exports anyway. **Lesson: when a 2/3 Council split is mostly about surface area (exposed vs private vs composable), the right answer is often "expose MORE not less." Multiple functions exported from one module costs ~0 bundle bytes if unused (tree-shaking) and ~0 cognitive load if naming is clear. The original "private helpers in single file" framing was a false dichotomy — exporting them costs nothing while paying for both R1 positions.**

- S26 #prime-audit-explicit-carry-forward-vs-silent-drop: Battle Ledger went 2/3 majority for Q2 hardcoded `CREATURE_SPAWN_TICKS=60` over Gemini's per-creature-type config table. Easy to silently drop Gemini's position as the loser. Instead, PRIME-AUDIT Δ6 made the deferral EXPLICIT: "Gemini's per-type CreatureConfig table approach deferred to S29 when Anvil ships." When S29 actually adds Anvil + PacPredator, the carry-forward note ensures the config-table refactor is on the table, not forgotten. **Lesson: 2/3 majority overruling a minority position is fine for IMMEDIATE execution but the alternative MUST be logged as carry-forward so future sessions can revisit when conditions change. Otherwise the minority position is silently lost. Pattern: every PRIME-AUDIT pass should ask "what 2/3 disagreement did we resolve, and does the minority side need a future trigger to revisit?" The Δ6 (S29 config-table) + Δ7 (S28 NetSnapshot serialization for IEEE 754) patterns both followed this — explicit deferral, not silent dismissal.**

- S26 #check-triumvirate-zero-fixes-is-a-signal-not-a-failure: S25 CHECK Triumvirate found 3 actionable fixes (save.ts nextCreatureId, abort docstring, AC7 test gap). S26 CHECK found 0 actionable fixes despite 6 concerns raised (3 architecturally rejected, 2 accepted-as-covered, 1 accepted-as-documented-trade-off). My initial reaction was "did I CHECK rigorously enough?" — but on inspection, all 6 concerns were genuine analysis output and the rejection rationale was solid in each case. **Lesson: CHECK Triumvirate finding 0 actionable changes is a POSITIVE quality signal when the rejection rationale is detailed and architecturally grounded — it means R1 + PRIME-AUDIT did their job. The temptation to manufacture fixes "to justify the CHECK pass" is anti-pattern. The CHECK Triumvirate's value is in the analysis itself — even rejected concerns become the documentation record ("we considered X and chose Y because Z"). Pattern: track CHECK Triumvirate fix-count as a quality metric over sessions; 0 is fine if rationale is sound.**

- S26 #same-model-r1-vs-check-contradiction-r1-deeper-deliberation-wins: Gemini R1 Q7 said "no forces during SPAWNING to avoid momentum trap." Gemini CHECK G2 said "apply repulse during SPAWNING to avoid spawner-zone overlap." These positions are MUTUALLY EXCLUSIVE — same model contradicted itself across R1 and CHECK passes. Resolution: keep R1 position (Δ4 ZERO_ACCEL during SPAWNING) because (a) the momentum-trap failure mode is MORE SEVERE (60 ticks of wrong-direction motion vs 1 second of stationary overlap at spawn), and (b) R1 had deeper deliberation context (full 7-question deliberation) than CHECK (one-shot audit pass on landed code). Documented the trade-off in the code comment for future readers. **Lesson: when CHECK output contradicts the same model's R1, the older R1 position usually wins because deeper deliberation > one-shot pattern-matching on materialized code. CHECK's job is to find IMPLEMENTATION bugs that R1's DESIGN-level review missed, not to relitigate design decisions. If CHECK appears to relitigate, audit the trade-off explicitly (severity + frequency + scope) before flipping the design. Pattern: CHECK pass should NOT amend R1 decisions; it should find missed implementation details.**

- S26 #grok-api-timeout-retry-with-condensed-prompt: First Grok R1 call (grok-4.20-0309-reasoning, ~6KB prompt, 120s timeout) failed with "ERROR: xAI API request timed out after 120s." Retry with same model but condensed prompt (~4KB) succeeded in ~30s. Without the retry, Standard-tier 3-way Council would have fallen back to 2-way (degraded mode). **Lesson: Grok reasoning-model API timeouts are correlated with prompt size at the 120s boundary. When a Council R1 call times out, retry once with an aggressively condensed prompt (drop redundant context, shorter verbose framing, keep only the question + minimal facts) before falling back to 2-way Council. The cost is one retry (~$0.02); the benefit is preserving the 3-way Council's distinct-blind-spot value. Add to council-of-models protocol: "On Grok timeout, retry with condensed prompt before fallback." Document a "Council R1 prompt size budget" rule of thumb: ≤5KB per model.**

## 2026-05-14 — Session 25 (P0 Voltkin Phase 2A creature scaffold — Standard-tier Council CODE-EXECUTION)

- S25 #hallucinated-precedent-in-council-r1-fact-check-against-actual-codebase: Gemini R1 CH1 proposed renaming `creatureLifecycle.ts` to `creatureReducer.ts` citing "godlyReducer.ts precedent" in `src/state/`. Battle Ledger fact-check: `src/state/godlyReducer.ts` does NOT exist. Only `godlyReducer.test.ts` exists — the implementation is INLINE in `world.ts` dispatch case. The actual locked precedent is `sparkLifecycle.ts` (S20 P1 extraction pattern) — which my proposed `creatureLifecycle.ts` already followed. Gemini hallucinated the precedent + the rename. Battle Ledger correctly rejected the rename. **Lesson: Council R1 may invent "precedents" that don't exist in the codebase, especially around file naming conventions where the model defaults to the most common idiomatic name (e.g., "*Reducer.ts") rather than the project's actual idiom. Before accepting any rename or pattern-claim from R1, grep for the cited file. The 30-second `ls` or `Glob` saves committing to a fake convention. Pair-fact-check is the Council's job, but PRIME-AUDIT must explicitly verify any claimed precedent is real before adopting it.**

- S25 #pdr-explicit-out-of-scope-section-prevented-council-scope-creep: PDR §2 SCOPE listed "Out of scope (S25 does NOT touch)" with 9 explicit items (S26 Verlet physics, S26 FSM tables, S27 CREATURE_ATTACK, S27 cascade migration, S28 spritesheet, S28 NetSnapshot v2, S26 skip wiring, S28 1v1 net sync). When Council R1 surfaced concerns about 1v1 desync via NetSnapshot serialization, the explicit-out-of-scope text let me legitimately defer to S28 without it feeling like a punt. Gemini CH7 raised the wall-clock-vs-tick-deterministic concern; I deferred per scope — same justification. **Lesson: every PDR for a multi-session feature MUST include an explicit "out of scope (deferred to future session)" list. This serves two purposes: (1) signals to Council R1 that you're aware of the broader concerns; (2) provides a clear gate against scope creep when Council proposes "while you're in there, also fix X." The list itself becomes the carry-forward to future PDRs. Pattern: count the carry-forward bullets — if there are >5, your feature scope is right-sized for multi-session execution.**

- S25 #check-triumvirate-found-different-bugs-than-r1-not-redundant: After implementation, CHECK Triumvirate ran GROK-ANALYST + GEMINI-AUDITOR against the LANDED CODE (not the blueprint). Grok CH3 found that `applySnapshotCore` clears `world.creatures` but doesn't reset `world.nextCreatureId` — a defense-in-depth gap I missed (and that R1 didn't catch either because R1 was looking at the PDR, not the implementation). Gemini CH4 verified the cutsceneOverlay.abort() / GODLY_ABORT timer-race scenario was actually safe in the existing connection-lost path (already calls abort() before dispatching GODLY_ABORT). 3 actionable fixes from CHECK that R1 had not surfaced. **Lesson: CHECK Triumvirate is NOT just "did R1 deltas land correctly?" — it's a fresh audit pass on the materialized code that catches different bug classes than R1 did. R1 audits design; CHECK audits implementation. They're complementary, not redundant. Always run both for Standard/Full tier. Triumvirate finds an average of 1-3 fix-now items per audit; the cost is ~5K tokens; the ROI vs missed bugs is high.**

<!-- S25 entries (#standard-tier-council-finds-1v1-bug, #full-state-union-landed-in-s25-vs-subset, #vitest-dom-env-gap-pragmatic-skipif) pruned at S31 close to maintain ≤50 cap with 7 new S31 entries. Patterns now-canonical or superseded: council-finds-1v1-bug now well-evidenced across S25/S27/S28 (council-on-multiplayer-paths is constitutional Council protocol); full-state-union-subsetting absorbed into PDR-template "future-proof unions land in first scaffold"; vitest-dom-env-skipif is now-canonical test convention. Originals preserved in archived handoff docs under .handoff-archive/. -->

## 2026-05-14 — Session 24 (P0 Voltkin Phase 2 blueprint — Full-tier Council pure-deliberation)

- S24 #full-tier-council-on-pure-deliberation-session: S24 ran zero code priorities — P0 was Full-tier Council deliberation (3-way Claude+Grok+Gemini) producing `voltkin_phase2_blueprint_v1.md` covering 10 architecture questions for an autonomous-creature-actor system spanning S25-S28 (4 implementation sessions). R1 parallel (~14K tokens of model output across Grok/Gemini/Claude), Quality Gate identifying 5 disagreements + 5 gaps, R2 refinement (both Grok+Gemini ACCEPT ALL), PRIME-AUDIT producing 8 additive blueprint §s. session_id rotated S23→S24 with priorities=[P0 only], deliberation_completed=true at PDR approval, check_completed=true at blueprint write. **Lesson: pure-deliberation Full-tier sessions are valid PDCA priorities — the blueprint IS the deliverable. Echoes S21 lesson but with stronger evidence: when a 5-session implementation series needs locked architectural decisions, spending one session on deliberation alone is the highest-leverage move. The cost of a wrong-architecture S25 + then-rewrite is 10x the cost of a S24 Council. Pattern: any feature spanning >3 sessions should open with a pure-deliberation Council session.**

- S24 #cross-model-factcheck-caught-grok-blind-spot-on-per-player-cooldown: R1 disagreement #1 (Q10 multi-creature) had Grok saying "single global slot, cooldown prevents overlap" vs Gemini saying "cooldown is per-player, both can trigger simultaneously, need array." Quality Gate fact-check by Claude reading `godlyCooldown.ts:21` confirmed Gemini correct — `isOnCooldown(player, currentTick)` checks `player.godlyCooldownEndsAtTick` (per-player field). Grok's claim was factually wrong about the codebase. If single model had been used (no Council), this would have caused a 1v1 bug. **Lesson: the value of 3-way Council is NOT redundancy of correct answers — it's that each model has DIFFERENT blind spots, and Quality Gate's job is to fact-check disagreements against the codebase. Claim "cooldown prevents X" is checkable by reading the cooldown file. Always pull receipts on factual disagreements before accepting either side. The fact-check itself costs <500 tokens and can save a entire bug-fix cycle.**

- S24 #quality-gate-surfaces-gaps-no-individual-R1-can-see: All 3 R1s passed an internal "CONTRADICTION CHECK" against LOCKED contracts and reported "1-7 clean." Yet Quality Gate identified 5 gaps that NONE of the R1s caught — most critically Gap A: the existing `GODLY_TRIGGER` reducer cascades SEVER_BOND synchronously, which becomes redundant once creature does severance. Each R1 wrote about creature spawning + attack + SEVER_BOND emission but no R1 explicitly said "we should delete the existing cascade." This is a CROSS-CUTTING concern — visible only when reading all 3 R1s side-by-side AND the existing reducer code. **Lesson: an R1 is a feature design; the Quality Gate is an INTEGRATION audit. Gaps in R1s are often not contradictions but omissions where the new design overlaps existing surface area, and each model assumes "someone else will handle that." Schedule Quality Gate to explicitly ask "what existing code does this REPLACE?" not just "what does this ADD?" for every new system. Gap A would have caused S27 test surgery surprise.**

- S24 #prime-audit-additive-deltas-become-blueprint-sections: PRIME-AUDIT generated 8 audit deltas (S27 migration notes, performance budget worst-case, edge cases listing, creature type config interface, audio plan, S25 acceptance criteria specificity, S25 renderer fallback, S28 acceptance criterion). ALL 8 were additive specifications, NOT architecture changes. The synthesis stood; PRIME-AUDIT raised the floor without re-litigating. **Lesson: PRIME-AUDIT's most common output isn't "you got the decision wrong" — it's "the decision is right but you under-specified the implementation details, here are the gaps." Embracing this distinction makes PRIME-AUDIT cheaper (no re-deliberation) and more useful (adds operational rigor). Pattern: after every Council R2 synthesis, ask PRIME-AUDIT: (a) is any decision wrong? (b) is any decision under-specified? (c) what edge cases / migration concerns / performance budgets are missing? Almost always (b) and (c) yield deltas; (a) almost never does if R2 was properly synthesized.**

- S24 #council-r2-accept-all-can-be-genuine-or-rubber-stamp-prime-audit-checks: R2 from both Grok (temp 0.5) and Gemini (temp 0.5) returned literally "ACCEPT ALL" after Quality Gate findings. Suspicion: did they read carefully or just sign off? PRIME-AUDIT audit-question-1 explicitly examined this for D5 (despawn): Grok had been emotionally invested in "12-arc final burst 1.4s" in R1, yet R2 ACCEPT ALL signed off on quiet despawn. Re-evaluated: 8 seconds of creature attacks IS the spectacle; loud despawn after that is visual noise. Conclusion: rubber-stamp was acceptable, the decision is right. **Lesson: when R2 says "ACCEPT ALL," PRIME-AUDIT MUST examine the most-emotionally-charged R1 disagreement to verify the model didn't just defer to consensus. If the model had a strong R1 position and gives no pushback in R2, that's a signal to re-evaluate the decision independently. Sometimes consensus IS correct; sometimes the model lost a battle and is being a good loser. PRIME-AUDIT distinguishes them.**

- S24 #deliberation-output-format-decision-rationale-rejected-risk-sketch-scales-to-10-questions: R1 prompts demanded structured per-question output (DECISION/RATIONALE/ALTERNATIVES-REJECTED/RISK/IMPLEMENTATION-SKETCH). All 3 R1s complied; the rigid structure made the Quality Gate convergence-matrix trivial (one row per question, one column per model). The CONVENTION mattered more than the depth — having "RATIONALE" forced each model to cite its reasoning, having "REJECTED" forced each to consider alternatives, having "SKETCH" forced concreteness. **Lesson: for multi-question Council deliberations (>5 questions), enforce a STRICT per-question output template. Variable-format responses are unauditable — you can't make a convergence matrix if R1s wandered conversationally. The template cost (model has to write more boilerplate per Q) is paid back 10x in Quality Gate efficiency. Reuse this exact template for future multi-Q Councils.**

<!-- S23 entries (8 across 2 blocks: cursor-equality-bug-in-effect-drain, debug-overlay-as-
     root-cause-discovery-instrument, prime-audit-Δ1-preemptive-vitest-fixture-skipped-
     unnecessary-deploy, scope-amendment-rule-16-fired-twice-in-one-session, ship-
     cinematic-stamp-as-phase-1-defer-creature-actor-to-phase-2, continuous-threshold-
     predicates-are-undiscoverable-without-HUD, bond-graphs-are-bidirectional-the-spec-
     is-not, micro-tier-PDR-with-user-explicit-go-skips-Council-cleanly) pruned at S30
     close to maintain ≤50 cap with 8 new S30 entries. Patterns now-canonical or
     superseded: cursor-equality-bug-in-effect-drain → fix shipped (`<` not `<=`),
     specific bug class one-off; debug-overlay-as-root-cause-discovery-instrument →
     shipped as `?debug=1` overlay still in production; prime-audit-vitest-fixture-skip
     → codified into PRIME-AUDIT protocol; scope-amendment-rule-16 → now Rule 16 in
     CLAUDE.md; phase-1-defer-phase-2 → S30 finally shipped Phase 2 alive feel;
     continuous-thresholds-need-HUD → discrete-predicate pattern now canonical in all
     recipes; bond-graph-bidirectional → now caught by paired forward+reverse tests;
     micro-tier-user-go-skips-Council → now Rule 17 Micro auto-waive. Originals preserved
     in archived handoff docs under .handoff-archive/HANDOFF_2026-05-13_S23close.md. -->

<!-- S22 entries (5: full-tier-batch-PDR-with-mid-stream-user-amendments, prime-audit-delta-
     1-fallback-when-infra-claim-fails-A0, consistency-gate-before-shipping-AI-generated-
     asset-pack, custom-pixi-filter-for-mp4-bg-keying-beats-asset-re-roll, side-effect-
     import-for-recipe-registration) pruned at S29 close to maintain ≤50 cap with 8 new
     S29 entries. Patterns now-canonical or superseded: mid-stream-user-amendments codified
     into Rule 16 SCOPE AMENDMENT; A.0-existence-vs-capability codified into Rule 21 STATE-
     DISCOVERY; AI-asset-consistency-gate codified into DESIGN_BRIEF template + Imagen-text-
     only-cannot-hold-canonical-style (S29) is the stronger evidence; pixi-luma-key-shader
     lesson superseded by S29 glsl-es-300-vs-legacy-100-silent-shader-failure-class which
     captures the actual bug class the original shader hit; side-effect-import-for-recipe-
     registration is now-canonical pattern in godlyRecipes/index.ts. Originals preserved
     in archived handoff docs under .handoff-archive/HANDOFF_2026-05-13_S22close.md. -->

<!-- S21 entries (5: full-tier-deliberation-without-pdca-priority-execution, user-veto-on-
     domain-weighted-vote-is-supreme, parallel-sessions-with-disjoint-paths-and-single-
     writer-state, gen-ai-asset-pipeline-promoted-to-in-scope-tooling, ip-safe-character-
     design-needs-upfront-anti-checklist) pruned at S29 close to maintain ≤50 cap with 8
     new S29 entries. Patterns now-canonical or superseded: pure-deliberation-as-priority
     codified via S24 Full-tier blueprint session pattern; user-veto-supreme well-internalized;
     parallel-sessions disjoint-paths codified in assets-source/README.md; gen-ai-asset
     tooling now first-class in DESIGN_BRIEF templates; IP-safe upfront-checklist codified
     in DESIGN_BRIEF §3 pattern. Originals preserved in archived handoff docs under
     .handoff-archive/HANDOFF_2026-05-13_S21close.md. -->

<!-- S20 entries (9 across P0/P1/P3 — extraction archetype grouping, tree-shake bundle
     parity, DAG-safety fallback placement, A.0 level-2 pivot, shared-helper threshold,
     scope-mechanism vs scope-expansion, lib-installed type probing, observability-with-
     fix bundling, PRIME-AUDIT-2 protocol-field dedup, JSON-string channel for discriminated
     unions) pruned at S27 close to maintain ≤50 cap with 6 new S27 entries. Patterns
     now-canonical or codified into protocol docs: extraction-archetype is now session-pdca
     SKILL.md guidance; tree-shake bundle parity is well-internalized; DAG-safety in
     shared module is the standard pattern; A.0 level-2 superseded by Rule 21 A.0 STATE-
     DISCOVERY GATE constitutional; 3-call-site shared-helper threshold now-canonical;
     scope-mechanism vs scope-expansion folded into CLAUDE.md SCOPE AMENDMENT rule;
     lib-installed type probing is now-canonical for any external-dep blocker;
     observability-with-fix bundling is now-canonical for second-shot+ failures;
     PRIME-AUDIT-2 protocol-field dedup absorbed into council-of-models §3.4; JSON-string
     channel for discriminated unions is now-canonical for any Trystero-style API.
     Originals preserved in archived handoff docs under .handoff-archive/HANDOFF_2026-05-12*.md. -->

<!-- S19 entries pruned at S26 handoff to maintain ≤50 cap with 6 new S26 entries
     (S19 #per-channel-gain-as-children-of-master-pause now-canonical audio pattern
     in audioManager.ts; #node-test-env-lacks-localstorage subsumed by S25 #vitest-
     dom-env-gap-pragmatic-skipif-vs-config-scope-creep (same pattern: prefer skipIf
     gate to environment scope-creep); #council-convergent-effect-ordering-blocker
     codified into council-of-models §3.4 convergent-blocker rule; #a0-state-discovery-
     flags-handoff-loc-drift superseded by Rule 21 A.0 STATE-DISCOVERY GATE now-
     constitutional; #shared-helper-extraction now-canonical anti-bloat pattern in
     §XV charter; SESSION #refactor-before-feature absorbed into trip-wire-as-
     judgment-signal codification in session-pdca SKILL.md; S19 P4 #silent-npm-bump-
     trystero now LOCKED in package.json + protocol.ts; S19 P4-retro #fix-first-
     hypothesis-can-be-wrong subsumed by S20 P0 #observability-before-third-shot-fix).
     Originals preserved in archived handoff docs under .handoff-archive/. -->

<!-- S18 entries pruned at S25 handoff to maintain ≤50 cap with 6 new S25 entries
     (S18 #gh-api-binds-pages-custom-domain superseded by `gh api` shortcut now-canonical
     workflow; #effect-array-bridge-keeps-purity-in-audio absorbed into S20+ pure-helper
     extraction patterns; #replay-safe-audio-via-tick-cursor superseded by S23 #cursor-
     equality-bug `<=` → `<` rule which generalized the cursor pattern; #council-convergent-
     blockers-skip-debate codified into council-of-models §3.4; SESSION #state-discovery-
     finds-forward-compat-discriminator now canonical Rule 21 A.0 STATE-DISCOVERY GATE).
     Originals preserved in archived handoff docs under .handoff-archive/. -->**

<!-- S17 entries (#a0-state-discovery-charge-accumulator-already-wired,
     #council-flagged-pixi-v8-gradient-api-myth-stroke-decomp,
     #spec-faithful-rendering-sources-placerColor-not-ownerColor,
     #hostile-if-either-endpoint-differs-mixed-ownership-edge-resolved,
     #lobby-double-offset-found-via-position-math-not-tests)
     pruned at S24 handoff to maintain ≤50 cap with 6 new S24 entries
     (S17 a0-state-discovery superseded by S24 #cross-model-factcheck-caught-grok-blind-spot
     (same pattern of verifying claims against source code); S17 council-flagged-pixi-v8
     superseded by S24 #council-r2-accept-all-can-be-genuine-or-rubber-stamp-prime-audit-checks
     (same pattern of cross-model API verification); S17 spec-faithful-rendering subsumed by
     S22 #spec-driven-canonical-sprite-lock pattern; S17 hostile-if-either-endpoint
     LOCKED at §13.11 with rule documented; S17 lobby-double-offset now-canonical Pixi UI
     test pattern). Originals preserved in archived handoff docs under .handoff-archive/. -->

<!-- S16 entries pruned at S23 final handoff to maintain ≤50 cap with 5 new S23 entries
     (S16 #council-dual-dissent-overrides-authority codified into council-of-models §3.4;
     #prime-audit-catches-required-workflow-elements absorbed into Rule 20 PRIME-AUDIT spec;
     #scope-amendment-mid-execution generalized into Rule 16; #headless-preview-rAF-throttle
     now-canonical preview-fallback pattern; SESSION #gh-cli-as-pages-shortcut codified in
     deploy-toolkit notes). Originals preserved in archived handoff docs under .handoff-archive/. -->

<!-- S15 entries pruned at S23 handoff to maintain ≤50 cap with 3 new S23 entries
     (S15 #user-amendment-mid-session subsumed by S22 #full-tier-batch-PDR-with-mid-stream-
     user-amendments; #locked-decision-amendment now canonical user-authority pattern;
     #council-r2-converges generalized into council-of-models SKILL.md R2 framing;
     #test-contract-as-implementation-surface absorbed into S22 P4 test rewrite pattern;
     SESSION #trip-wire-as-judgment-signal codified as session-pdca SKILL.md trip-wire rule).
     Originals preserved in archived handoff docs under .handoff-archive/. -->

<!-- S14 entries pruned at S22 handoff to maintain ≤50 cap with 5 new S22 entries
     (S14 #council-led-restructuring-as-prerequisite superseded by S20+ "Council restructures
     fragmentation" + S22 PRIME-AUDIT Δ1 fallback patterns; #no-score-for-redundancy-clean-frame
     tactical balance now-shipped; #pure-function-extraction-for-class-method-testability now-
     canonical S10/S14/S22 pattern; #verify-council-claim-with-source-not-narrative subsumed
     by S20 #a0-of-library-types-finds-three-api-gaps and S22 #prime-audit-delta-1; SESSION
     #prime-audit-as-revision-gate-not-decoration codified into CLAUDE.md Rule 20).
     Originals preserved in archived handoff docs under .handoff-archive/. -->

<!-- S13 entries pruned at S21 handoff to maintain ≤50 cap with 5 new S21 entries
     (S13 #verify-council-claims-against-source superseded by S20 #a0-of-library-types
     finding API gaps via type files — same "verify against source not narrative" pattern;
     S13 #knob-splitting now-canonical refactoring practice; S13 #impulse-direction-ux-framing
     remains relevant but conceptually subsumed by S15+ "tests are part of implementation surface"
     and S17 #spec-faithful-rendering-sources placerColor patterns; S13 #short-bond-clamp-rejustify
     subsumed by S20 PRIME-AUDIT-2 pattern of revising Council adoption reasoning post-hoc;
     S13 SESSION #per-priority-commit-vs-thematic-batching subsumed by S20 batch PDR norms.)
     Originals preserved in archived handoff docs under .handoff-archive/. -->

<!-- S11 + S12 entries pruned at S20 handoff to maintain ≤50 cap with 4 new S20 entries
     (S11 buffer-session + design-doc-overage tactical, now-canonical via S15+ design-doc cycles;
     S12 per-kind-split superseded by S20 P3 archetype-grouping pattern (Council pushed 12→3 files;
     "split along growth axis" replaced by "right-size to archetype, not function");
     S12 dead-code-audit-before-move now-canonical refactoring practice (S14/S19/S20 all did it);
     S12 pure-fn-drawers-with-age tick-state pattern now-canonical render convention;
     S12 council-r1-vs-handoff-endorsement superseded by S20 #council-shrinks-over-fragmentation
     which is the same pattern with concrete example). Originals preserved in
     archived handoff docs under .handoff-archive/. -->

<!-- S10 entries (#attract-impulse-to-lerp, #bfs-hop-map-as-effect-payload, #verlet-impulse-via-prevpos-offset,
     #cinematic-physics-half-stays-unconditional, #test-via-pure-helper-export,
     #cross-cutting-gate-deferred-to-last-priority, #anchor-place-emits-grow-too,
     SESSION #playtest-confirmed-bug-fix-as-first-priority) pruned at S19 handoff
     to maintain ≤50 cap with 6 new S19 entries (S10 attract-lerp now canonical
     in controls.ts; effect-payload pattern repeated S17+ multi-color/§13.11/§13.14;
     verlet-impulse-via-prevpos canonical; cinematics toggle pattern superseded by
     S18 mute-glyph + S19 settingsOverlay UX-toggle; pure-helper-export pattern
     now-canonical S10-S19; cross-cutting-gate-last superseded by S19 batch order;
     anchor-place-emits-grow now-canonical; playtest-first ordering superseded by
     S15+ playtest-gated carry-forward pattern). Originals preserved in archived
     handoff docs under .handoff-archive/. -->

<!-- S7, S8, S9 entries pruned at S18 handoff to maintain ≤50 cap with 5 new S18 entries:
     S7 #cursor-vs-pos-as-source-of-truth superseded by S10 #attract-impulse-to-lerp
     (lerp eliminated the dual representation entirely — no more cursor-vs-pos divergence path).
     S8 #prime-audit-delta-as-pre-playtest-target + #sister-defect-via-pattern-search now-canonical
     (applied uniformly S9-S17; PRIME-AUDIT Rule 20 codified in CLAUDE.md).
     S9 #urgency-detected-as-pdr-gate now-canonical (codified in CLAUDE.md URGENCY PROTOCOL).
     S9 #single-bond-vs-multi-bond-action-shape + #score-threshold-as-balance-knob +
     #design-doc-as-decision-matrix-not-proposal + #pdr-renumbering-vs-execution-order
     tactical patterns now-canonical. SESSION #handoff-push-not-just-commit durable in
     feedback memory (~/.claude/projects/.../memory/feedback_handoff_push.md) + CLAUDE.md
     HANDOFF protocol. Originals preserved in archived handoff docs under .handoff-archive/. -->

<!-- S4 technical entries (spec-drift, color-as-ownership, pixi-particle-container,
     boundary-strict-vs-equal, stress-test-fixture-broke) pruned at S10 handoff.
     S5 detail entries (#max-speed-clamp-broke-attract, #attract-needs-momentum-or-strength,
     #spawner-bounds-blocks-pickup, #pointer-capture-auto-release) pruned at S11 handoff
     to keep total ≤50 entries — S6+ lessons supersede.
     S5 pixel-tech entries (#dpr-double-bug, #naive-fix-incomplete, #max-speed-clamp-location)
     + S6 #headless-tab-pauses-ticker pruned at S12 handoff (DPR fix is in code; ticker
     workaround superseded by S8 #headless-render-needs-game-loop).
     S4 SESSION block (#spec-correct-vs-playable) + S5 detail entries (#single-action-place,
     #headless-test-contamination) pruned at S13 P5 closeout (S5+ playability work superseded
     by S7-S10 cursor/place/cinematics rework).
     S5 SESSION block (#user-path-go-skip-deliberation) + S6 #git-init-late + S6
     #defensive-fix-for-non-bug pruned at S13 /handoff to bring total to 50 cap
     (handoff skill Step 2.8 enforced; S7+ user-path-approval entries supersede).
     S6 block (#effects-cap-belt-and-braces, #per-combo-effects-cheap,
     #endpoint-info-needed-for-line-effects, SESSION #user-path-batch-approval)
     pruned at S14 closeout to maintain ≤50 cap (S6 effects-renderer detail
     superseded by S8 animation-static-split + S12 per-kind effect-renderer split;
     batch-approval mechanic superseded by S13 explicit-go-token-permits-same-turn-action).
     S7 entries (#ephemeral-to-persistent-silhouette-rewrite,
     #pdca-deliberation-skip, #pixi-tab-pause-mutate-then-render,
     #zone-aware-test-coords, #structure-renderer-tick-plumbing) pruned
     at S15 closeout to maintain ≤50 cap with the 5 new S15 entries
     (silhouette geometry-re-imagining superseded by S12 per-kind split;
     skip-deliberation by S13 explicit-go pattern + S15 amendment-cycle;
     tab-pause by S8 #headless-render-needs-game-loop; zone-aware-tests
     well-internalized; tick-plumbing now-canonical).
     S8 entries (#headless-render-needs-game-loop-not-just-renderer,
     #pixel-hash-distinguishes-animation-classes,
     #mock-extension-safe-via-tick-independence,
     #animation-static-split-as-paired-regression-tests,
     #flag-for-veto-in-pdr, SESSION #continuation-discretion-with-pedantic-thoroughness)
     pruned at S16 closeout to maintain ≤50 cap with the 5 new S16 entries
     (headless-render-loop superseded by S16 #headless-preview-rAF-throttle-blocks-screenshot;
     pixel-hash + mock-extension + paired-regression-tests now-canonical testing patterns;
     flag-for-veto-in-pdr superseded by S15+ scope-amendment-mid-flight pattern;
     continuation-discretion superseded by S15+ user-amendment-cycle-re-entry pattern).
     S9 entries (#s7-snap-cursor-undone-by-physics-gate, #component-dedup-via-set-not-bfs-per-call,
     #post-place-sweep-emit-effects-too, and S7 SESSION #user-path-batch-approval-with-discretion)
     pruned at S17 closeout to maintain ≤50 cap with the 5 new S17 entries
     (snap-cursor superseded by S10 P1 position-lerp follow which eliminated the snap mechanic;
     component-dedup tactical and now-canonical; post-place-sweep emit-effects now-canonical;
     batch-approval-with-discretion superseded by S15+ user-amendment-cycle-re-entry pattern).
     well-internalized; tick-plumbing now-canonical render convention).
     S3 + S1-2 session blocks pruned at S9 handoff.
     Originals preserved in archived handoff docs under .handoff-archive/. -->
