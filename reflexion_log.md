# SPARK — Reflexion Log

Cross-session learnings. Most recent at top. Entries pruned when total >50.

## 2026-05-12 — Session 19 of 10+ (Audio controls UI + disruptionManager extraction + per-silhouette gradient)
- S19 #per-channel-gain-as-children-of-master-pause: Audio refactor preserved masterGain as the 'M' key global-pause target (0/1 toggle) and routed musicGain + sfxGain as children of master. Effective output of each channel = `muted ? 0 : clamp01(volume)`. 'M' toggles master only, preserving per-channel state — so when the user unmutes via 'M', their per-channel settings are exactly where they left them. Legacy `spark_audio_muted` localStorage key kept driving the master pause (back-compat for prior users); 4 new `audio.*` keys added for per-channel persistence. **Lesson: when extending a single-knob audio system to multi-channel, layer per-channel gains as CHILDREN of the original master rather than replacing master with a mixer. Preserves prior UX semantics (mute toggle still pauses everything), prior localStorage key (no migration), and gives per-channel state independent persistence. Cheaper than rewriting; backward-compatible by construction.**
- S19 #node-test-env-lacks-localstorage-skip-persistence-tests-trust-trycatch: Vitest default environment is `node` (no `window`, no `localStorage`) — confirmed by lobbyScreen.test.ts also having zero DOM use. Two new localStorage roundtrip + legacy-key tests written for the audio module failed because the try/catch in `loadSettingsFromStorage` correctly returns fallback defaults when `window` is undefined. Options: install jsdom dep (~30 KB + config), or drop the 2 tests and note manual playtest covers persistence. Chose drop + comment because (a) only 2 tests need it, (b) localStorage is browser behavior not pure logic, (c) jsdom adds runtime cost to all 24 other test files. **Lesson: when a test requires browser globals and the rest of the test suite doesn't, prefer dropping the test + noting "manual playtest verifies X" over adding a heavy environment dep. Pure-logic tests cover the algorithm; browser tests cover the integration; live-URL smoke covers the persistence. Each level pays for itself; don't pay for the second level when the first + third cover the gap.**
- S19 #council-convergent-effect-ordering-blocker-orchestrator-owns-effects: P2 disruptionManager extraction Council R1 — Grok DISRUPTOR #4 ("computeSeverEffects loops after mutation but uses deleted prims") + Gemini AUDITOR #1 BLOCKER ("effects emit upfront breaks SEVER_ERASE → mutation → BOND_SEVERED ordering") converged independently on the same risk: helpers cannot own effect emission because effect ORDER is semantically load-bearing for audio drain + visual sequencing. Adopted: split into `computeSeverEraseEffects` (pre-mutation only, returns array) + orchestrator owns final `BOND_SEVERED` push AFTER `applySeverTopology`. Same pattern as S18 #council-convergent-blockers — when DISRUPTOR + AUDITOR independently flag the same issue, skip the "is this real" debate. **Lesson: in pure-helper extractions of stateful code, the helper boundary should respect TEMPORAL ordering of side effects (when does the effect emit?), not just logical groupings (what does the effect describe?). When effects must interleave with mutation, the orchestrator owns the emission sequence; helpers compute payloads or apply state but don't push to shared effect queues mid-sequence.**
- S19 #a0-state-discovery-flags-handoff-loc-drift: Pre-PDR A.0 probe of `wc -l` + grep for `bondHover` surfaced 3 corrections to the S18 handoff: world.ts was 359 LOC (handoff said 311), `bondHover` symbol doesn't exist anywhere in the codebase (handoff included P7 bond-hover preview as ~30 LOC extension), lobbyScreen.ts 551 LOC over 500 charter (handoff said ~480 under). All 3 corrections moved into the PDR DELTA table BEFORE user `go`. Rule 21 A.0 is cheap (3-4 reads + 2 greps); catches scope-defining claims from a stale snapshot. **Lesson: handoff documents are session-close snapshots, not real-time queries. Verify all LOC + symbol existence claims against the current codebase before locking the PDR. Pre-flight A.0 turns stale-handoff scope errors into PDR-time corrections rather than mid-execution surprises.**
- S19 #shared-helper-extraction-when-refactor-pushes-file-over-charter: P3 per-silhouette gradient first pass pushed bondVisualRenderer.ts from 447 → 544 LOC, 8.8% over 500 charter. Extracted `strokePathLerp` (vortex + whip cross-color parametric path lerp — both had nearly-identical 8-segment loops) → 544 → 536 LOC. Still over but significantly narrower. **Lesson: when a refactor pushes a file across charter, look for shared structure in the NEW code before accepting the overage as carry-forward. Duplicate parametric-path loops, twin parallel-stroke helpers, repeated polygon-side stroke calls — these are the lowest-cost wins. If a shared helper doesn't fit, then accept the carry-forward; don't accept it until you've checked.**
- SESSION #refactor-before-feature-S14-lesson-replayed-for-anti-bloat-debt: world.ts entered S19 at 359 LOC (28% over 280 charter) — accumulating since S15 P2 added the 1v1 networked FSM. P2 extraction took ~70 LOC out, leaving 311 LOC (11% over). The previous "ship + log carry-forward" decision from S15 #trip-wire-as-judgment-signal compounded over S15-S18 into a hard charter violation that Council R1 explicitly cited. **Lesson: trip-wire deferrals have a half-life — a single 11% overage in S15 became a 28% overage by S18 because S17 P1 + S18 P1 each added ~10-20 LOC without triggering a fresh extraction. When a file is over-charter for ≥2 consecutive sessions and the carry-forward note hasn't been cleared, escalate the extraction to a Standard-tier priority instead of deferring again. Anti-bloat charter compliance is a back-pressure mechanism; ignoring it once is fine, ignoring it three times means the charter is being silently downgraded.**
- S19 P4 #silent-npm-bump-trystero-0.20-to-0.24-broke-relay-defaults: Post-handoff playtest BLOCKER — both peers stuck at "connecting" in 1v1 lobby. A.0 root-cause via node_modules inspection: LOCKED §13.1 pinned `trystero ^0.20` at S15 P2 wiring; package.json had silently drifted to `^0.24.0`; 0.24's `@trystero-p2p/nostr` module ships 55 default Nostr relays and picks 5 via `shuffle(defaults, strToNum(config.appId))`. Shuffle IS deterministic per appId (both peers land on the same 5) but the default list mixes well-known relays (`relay.damus.io`, `nos.lol`) with many personal/dead endpoints. Hypothesis: both peers picked the same DEAD set → no Nostr signaling. Fix: pin 6 known-reliable public relays via `relayConfig.urls` + `redundancy = NOSTR_RELAYS.length` (all 6 used, no sub-sampling). **Post-deploy retest: fix DID NOT resolve the BLOCKER — still stuck at "Connecting...". Hypothesis revised: dead relays were ONE possible cause but the actual failure is downstream (peer handshake / WebRTC ICE / Trystero 0.24 API wrapper drift). Lesson 1: LOCKED version pins must be enforced at the package-manager level not just the doc level — a `^x.y` semver caret drifts; pin BEHAVIOR (explicit resource config) when library defaults depend on external infrastructure. Lesson 2: A.0 state-discovery findings are HYPOTHESES, not verified causes — even when they look definitive (matching version drift + obvious-looking default-list problem). The post-deploy verification was the load-bearing step; without it I'd have closed S19 thinking P4 fixed the bug. Build empirical retest INTO the fix protocol, never just LGTM-merge a network-layer change.**
- S19 P4-retro #fix-first-hypothesis-can-be-wrong-even-when-it-fits-the-evidence: Urgency + plausibility + A.0 confirmation made the relay-pin look like the right fix — version drift was real, default list was visibly polluted, the math (5 random of 55 with deterministic shuffle) explained the symptom. Shipped + redeployed in ~10min. But the fix didn't work. **Lesson: in urgency mode the temptation is to ship the first plausible fix that survives A.0; resist it for network-layer changes. The cheaper move is to attach diagnostic console logging to the actual failure path BEFORE proposing a fix, so the post-deploy retest produces actionable signal whether the fix landed or not. Cost of adding `console.log` at peer-join / message-received / WebRTC state-change in transport.ts: ~5 min. Cost of "shipped relay pin, didn't work, no idea why": rebuilding hypotheses from zero with no observable difference between pre-fix and post-fix runtime. Diagnostic instrumentation is cheaper than a wrong-shot fix.**

## 2026-05-12 — Session 18 of 10+ (Custom-domain push closeout + P8 audio implementation)
- S18 #gh-api-binds-pages-custom-domain-faster-than-ui-toggle: After user did Squarespace DNS migration (Squarespace Defaults preset deleted + 5 custom records added: 4 A records @ 185.199.108-111.153 + CNAME www daneshto-dotcom.github.io.), the GH Pages binding was completed via `gh api -X PUT repos/.../pages -F cname=spark-online.space` (initial response: `"cname":null` → `"cname":"spark-online.space"`) + `-F https_enforced=true` after Let's Encrypt cert auto-issued ~30s later (`https_certificate.state=approved`, exp 2026-08-10). Total: 2 API calls + ~30s wait, vs documented UI path "Settings → Pages → Custom domain dropdown → Save → wait for verification → Enforce HTTPS toggle." Same one-call pattern as S16 #gh-cli-as-pages-enablement-shortcut. **Lesson: most "user must visit GitHub web UI" steps in Pages docs have a `gh api` equivalent that's faster + auditable + reproducible across repos. Default to `gh api` lookup before documenting "user must click X" — keeps deploy paths self-contained and scriptable.**
- S18 #effect-array-bridge-keeps-purity-in-audio-subsystem: Audio playback is impure (AudioContext, GainNode lifecycle, network fetch). World reducer must stay pure (state-replay correctness for save/load + NET reconciliation). The pattern from S17 P1 (SEVER_ERASE visual effects) extended cleanly: `world.ts` SEVER_BOND case pushes `BOND_SEVERED` effect; `placePrimitive.ts` end pushes `BOND_FORMED` effect; `audioManager.drainAudioEffects(world.effects, world.tick)` iterates and calls `play*SFX`. Reducer NEVER imports audioManager — purity preserved. Same `world.effects` queue serves BOTH visual renderer AND audio drain. **Lesson: the effects-array bridge isn't just for the visual renderer — it's the canonical bridge for ANY impure observer of reducer state. New audio = new consumer of the same queue, not a new architectural concept. Reusing the established pattern means no new mental model required, only the consumer changes. Same purity guarantee applies.**
- S18 #replay-safe-audio-via-tick-cursor-not-effect-id: Reducer-replay during save/load + NET reconciliation produces identical effects in identical ticks (deterministic). Audio must NOT re-fire on replay. Solution: monotonic `lastDrainedTick` cursor (initialized -1, advanced to `world.tick` each drain) — effects with `tick <= cursor` are skipped silently. No effect-ID tracking needed; tick monotonicity does the work. Replay restarts reducer with same effects in same ticks, but cursor preserved across replay → silent. **Lesson: for replay-safe observers of a deterministic reducer's effect stream, a monotonic tick cursor is simpler than effect-ID-set tracking. Cursor = O(1) memory; ID set = O(N) memory + needs ID assignment scheme. Works because `effect.tick` is the natural ordering key. Reset cursor on world reset (RETURN_TO_TITLE) only.**
- S18 #council-convergent-blockers-skip-debate: Council R1 returned 2 BLOCKERs from DIFFERENT lenses converging on the SAME issue: Grok #2 (DISRUPTOR — net-replay re-trigger) + Gemini #1 (AUDITOR — reducer-replay double-fire). Both identified `effects array drained blindly → SFX fires on every replay tick`. Different reasoning paths, same finding. Skip "is this real?" debate — convergence from independent reasoning is high-confidence signal. Went directly to mitigation design (lastDrainedTick cursor) without re-deliberating. **Lesson: when DISRUPTOR (risk lens) and AUDITOR (correctness lens) converge on the same BLOCKER from independent reasoning, the issue is empirically high-confidence. Allocate Council adjudication time to the DISAGREEMENTS, not the convergences. Convergent BLOCKERs are the strongest signal Council can produce — they cost ~$0 to adjudicate since both members already agreed.**
- SESSION #state-discovery-finds-forward-compat-discriminator-unused: STATE-DISCOVERY GATE A.0 probe of `src/physics/` for `SEVER_BOND` dispatch — ZERO matches in production. The `cause: 'player' | 'physics'` discriminator added in S17 P1 is FORWARD-COMPAT only: test fixture in `physics/stress.test.ts:94` exercises the 'physics' branch but no production path hits it (production only severs via player RMB-click). Audit re-sized BOND_SEVERED audio filter from "must distinguish carefully" to "physics branch is dead in v1; just trust the discriminator + filter accordingly." **Lesson: discriminators added in prior sessions can be "specified but not yet dispatched in production." A.0 probe BOTH (a) spec adoption AND (b) production usage before designing downstream filters. Designing for a case that doesn't yet exist wastes scope; trusting the discriminator is correct future-proofing. Rule 21 A.0 is cheap; catches "I'm reasoning about a code path that doesn't exist yet."**

## 2026-05-12 — Session 17 of 10+ (Phase-2 Tier-1 disruption + custom-domain + lobby BLOCKER fix)
- S17 #a0-state-discovery-found-charge-accumulator-already-wired: Pre-Council A.0 probe of `src/game/player.ts` revealed `disruptionCharges` field + `tickBuildAction` already plumbed AND already invoked from `placePrimitive.ts:468`. Constants `BUILD_ACTIONS_PER_CHARGE=5` + `MAX_DISRUPTION_CHARGES=2` LIVE since S2 (per archived S2 handoff). Initial PDR LOC estimate for P1 was ~80 LOC (per design-doc Phase-2 C anchor); after A.0, ~30 of that 80 was already-done, true P1 work was ~50 LOC (the auth gate + cause discriminator + ui dots). **Lesson: A.0 state-discovery isn't just defensive — it's a SIZE-AS-MOTH-OF-PRESUMPTION audit. Design docs from N-sessions-ago over-estimate work when prerequisite seams have landed in interim sessions. Doing A.0 before locking scope (Rule 21) right-sizes the priority AND prevents Council deliberating an inflated estimate that the user then approves at the wrong cost.**
- S17 #council-flagged-pixi-v8-gradient-api-myth-stroke-decomp-correct: PDR draft assumed Pixi v8 Graphics had a native A→B endpoint gradient stroke API (`stroke({ gradient: { from, to } })` style). Grok R1 #6 + Gemini R1 #5 both INDEPENDENTLY flagged: Pixi v8 has NO native endpoint-color gradient stroke. The Graphics API takes a single color OR a fillStyle gradient object suited for fills not strokes; SimpleRope/Mesh/shader are the fallback paths. Both Council members converged on stroke-decomposition (split bond into N sub-segments with interpolated colors). Adopted. **Lesson: when proposing API usage from a library Council has prior exposure to, ALWAYS verify the specific method actually exists in the version pinned. Pixi v8 changed a lot from v7; LOCKED §13.1 has `pixi.js ^8.5.x` so a memory of v7's API can be wrong. ~30 min library-doc check pre-Council would have caught this without spending two Council slots on the same finding.**
- S17 #spec-faithful-rendering-sources-placerColor-not-ownerColor-per-VI.4-X.2: PDR draft proposed sourcing bond colors from `Primitive.ownerColor`. Gemini R1 #1 BLOCKED with "§VI.4 / §X.2 says 'reveal contributions' — contributions = who placed each prim = placerColor (immutable historical record). ownerColor is the TRANSIENT mutable post-Steal field. Rendering ownerColor would visually overwrite contribution history when Phase-2 Steal eventually ships." Adopted placerColor. **Lesson: when two spec-mandated fields seem interchangeable for a render concern, pick the one that matches the spec's semantic justification, not the one with the matching shape. The spec text "reveal contributions" → contribution = action = placerColor (the historical fact of who placed it). ownerColor encodes "who controls it now" which is a different render concern entirely. PRIME-AUDIT for any render-from-field decision: read the spec line cited and check which field the prose actually describes.**
- S17 #hostile-if-either-endpoint-differs-mixed-ownership-edge-resolved: §VIII.3 row 1 LOCKED says "Sever costs 1 charge" — spec didn't explicitly address mixed-ownership bonds (where one endpoint is yours, one isn't). Auth rule could have been "actor.color matches ANY endpoint" (lenient, free for shared bonds) OR "actor.color matches ALL endpoints" (strict, hostile for any mixed). Gemini R1 #3 proposed strict rule: "hostile if EITHER endpoint differs." Adopted. Mixed bonds are hostile for BOTH players — first-to-charge wins. **Lesson: when a spec rule has an obvious binary auth check but the schema admits a more complex case (mixed-ownership), the auth rule needs explicit Council-time decision PLUS LOCKED amendment. Don't ship "inferred from spec" rules — write them down as §X.Y.Z LOCKED.** §13.11 codified this in P3 closeout.
- S17 #lobby-double-offset-found-via-position-math-not-tests: User mid-PDR-execution reported BLOCKER: "lets you put the code in that my friend generated and then cant click enter." Investigation found `lobbyScreen.ts:255` set Connect button position with ABSOLUTE canvas coords on a Container that was itself a child of a relative-positioned pane → double-offset rendering at stage (2090, 940), 170px past CANVAS_WIDTH=1920. S16 P1's 16 tests (sanitize / validate / mapCanvasRectToPage pure helpers) couldn't catch this — they test the HTML input contract, not Pixi Container child-positioning math. Fix: 3 absolute-coord `position.set` calls → pane-relative + extracted `attemptJoin` closure invoked from BOTH `joinButton.pointertap` AND new `inputEl.keydown(Enter)` UX fallback. Tests: added 5 NEW regression tests via pure-helper exports (`getConnectButtonCanvasBounds` etc.) asserting elements stay in canvas bounds + 1 explicit witness against the buggy x=2090 position. **Lesson: pure-helper unit tests don't catch Container-graph rendering bugs. For Pixi UI features, when a Container holds children with set positions, add a TEST that asserts the CHILD'S effective stage position (parent + local) stays inside `CANVAS_WIDTH × CANVAS_HEIGHT`. Cheap (~10 LOC pure helper + 3-5 assertions) and catches the class of bugs that ship invisible past test suites.**

## 2026-05-12 — Session 16 of 10+ (Cross-network playtest blockers: lobby UX + GH Pages deploy)
- S16 #council-dual-dissent-overrides-implementation-feasibility-domain: Original PDR proposed peaceiris/actions-gh-pages@v3 (a known-good GitHub Pages deploy action). Grok R1 #4 challenged "deprecated zombie, use actions/deploy-pages@v4"; Gemini R1 #2 challenged "lock action SHA, audit CI." Both are quality concerns NOT in their primary domain authority (peaceiris's actual maintenance status was Grok's strongest point but he's the risk/edge-case lens, not the deploy-tooling lens). Per Council §3.4 domain-weighted voting, the deploy action choice is "implementation feasibility" → Claude (1.75 authority) wins solo vs one dissenter. BUT: 2 dissenters override even an authority winner (2.0 > 1.75). Adopted GitHub's official path despite Claude's initial preference. **Lesson: §3.4 authority is a DEFAULT bias, not a veto. Two dissenters in non-primary domains still get aggregated past authority. Don't ratify your own preference just because the decision sits in your authority lane — count the dissenters.**
- S16 #prime-audit-catches-deploy-pages-v4-required-workflow-elements: Council R1 adopted "switch to actions/deploy-pages@v4" but neither Grok nor Gemini spelled out the THREE required workflow elements: `permissions: { pages: write, id-token: write }`, `environment: github-pages` on deploy job, `concurrency: group=pages`. PRIME-AUDIT caught all three before commit. Had I shipped the original Council synthesis verbatim, first deploy would have failed with permissions error (different from the actual "Pages not enabled" error that bit — that one was a one-time-user-step issue, not a workflow-spec issue). **Lesson: when Council recommends a tool/action switch, PRIME-AUDIT MUST verify the spec of the new tool, not just the swap. The action-replacing finding is "USE X" but the implementation has 3-5 sub-requirements that the audit catches. ~5 min audit, prevents a 2-min deploy-fail debug loop AND keeps the diff clean.**
- S16 #scope-amendment-mid-execution-when-original-plan-breaks-product-promise: Initially planned P2 Step 1 + Step 2 same-session per S15 handoff. Mid-execution realized: Step 2 commits base='/' which deploys assets at /assets/ instead of /the-spark/assets/, breaking the github.io fallback URL until user toggles Custom Domain in Pages Settings (asynchronous). User's stated goal: "play by end of day." Same-session Step 2 push breaks the playable URL for a DNS+cert wait window (~15 min to hours), in direct conflict with the stated goal. Wrote Scope Amendment #2 to PDR + deferred Step 2 to S17 ready-to-ship. **Lesson: scope amendments aren't only triggered by user requests — they're triggered ANY time mid-execution discovery shows the original plan contradicts the user's stated goal. Document the amendment (don't slip-stream-fix) so the gate flags + PDR-archive narrative match what shipped. Better: catch this in PRIME-AUDIT before commit; here it was caught at the deploy-strategy-review moment, still ahead of the broken push.**
- S16 #headless-preview-rAF-throttle-blocks-screenshot-not-implementation: Verified Lobby JOIN input attrs + sanitize logic + position math live in the Vite dev preview, but Pixi ticker.lastTime stuck at 378ms and screenshot timed out at 30s. Diagnostic: preview tab background-throttled → requestAnimationFrame paused → Pixi rAF loop paused → canvas never repainted → screenshot RPC blocked. The IMPLEMENTATION is correct (HTML input renders even without rAF since it's DOM, not WebGL); only the visual proof-of-life is blocked. Validated via eval-script alternative (input attrs + sanitize event + position math computation) instead of relying on the screenshot. **Lesson: when the preview verification path is blocked by environment (background-throttle, no WebGL, no audio), don't read it as "the change doesn't work." Switch to alternative proofs (eval, DOM inspection, network response, log assertion) that don't depend on the same blocked path. Implementation correctness and visual-proof-of-life are different claims; default to the cheaper proof when the expensive one is blocked.**
- SESSION #gh-cli-as-pages-enablement-shortcut: First deploy 25732612027 failed with "Pages not enabled" (the PRIME-AUDIT-documented one-time user-step). Web UI path: user visits Settings → Pages → Source dropdown → "GitHub Actions" → save. CLI path: `gh api -X POST /repos/<owner>/<repo>/pages -f build_type=workflow`. The CLI path completed in 1s, returned the full Pages config object, and the next push (P3 commit `9d9d9ee`) deployed green in 1m4s. **Lesson: many "user must visit GitHub web UI" steps in deploy specs have a `gh api` equivalent that's faster + auditable + reproducible across repos. Default to checking `gh api` first for any one-time setup step before documenting "user must click X" — keeps the deploy path self-contained.**

## 2026-05-12 — Session 15 of 10+ (S14 Charter Extraction + Phase-2 1v1 Networked Play)
- S15 #user-amendment-mid-session-as-2nd-council-cycle: User amended scope twice in one session (original "Hotseat + Fog Tier-0" → "drop fog + add lobby same-machine hotseat" → "friend in different country, networked Trystero"). Each amendment fired its own deliberation cycle, not a half-measure: R1 on original; R1 amend on hotseat+lobby (carry-forward Council findings preserved); R1+R2 on networked (Full tier escalation because LOCKED § 1 breach + cross-country UX surface area). Approval gate re-required per amendment; PRIME-AUDIT re-ran per synthesis. **Lesson: user amendments are LEGITIMATE PDR-cycle re-entry points, not "paper-over" moments. Run the pipeline (draft → Council → PRIME-AUDIT → user gate) fresh per amendment. Carry-forward Council findings from prior rounds compose with new findings without re-deliberating settled questions. Resist the temptation to "just adjust" mid-flight — adjustment without re-deliberation is how scope creep ships unaudited features.**
- S15 #locked-decision-amendment-via-user-authority: User explicitly authorized breaking LOCKED § 1 ("Phase-3 net: Colyseus or Geckos.io later") for Phase-2 1v1 ("friend in different country"). Amendment landed in same session: LOCKED § 1 row split into Phase-2 net (Trystero) + Phase-3 net (Colyseus, reserved for >2-player scalability); new § 13 "Phase-2 Networked Play v1" added with full transport/authority/sync spec; § 10.2 + § 10.4 amended with S15 P2 notes. **Lesson: LOCKED_DECISIONS sections are USER-AUTHORITY-amendable not implementation-frozen. When user authorizes amendment, document the amendment in the SAME session (don't defer the doc to a "later cleanup"). Future sessions read LOCKED at boot; stale lock = silent contradiction with the running code.**
- S15 #council-r2-converges-disagreements-not-restarts: R1 returned REVISE/REVISE with Trystero vs PeerJS as the major disagreement (Grok cited tracker rate-limit failure mode; Gemini cited Trystero zero-infra + multi-strategy fallback). R2 closed cleanly: Grok CONCEDED Trystero on Gemini's specific counter that BitTorrent is one of multiple strategies (Nostr/Firebase/Supabase available + auto-fallback). Host-migration disagreement persisted (Gemini won "Connection lost overlay" v1; Grok mandatory-stub deferred to S16 if playtest annoys). **Lesson: R2 is a CONVERGENCE round, not a fresh deliberation. Frame R2 prompts symmetrically: "defend or concede your R1 stance against the other member's counter; cite concrete data." AI peers handle disagreement productively when given the other's R1 plus a request for a decisive verdict. Disagreement EITHER resolves via concrete data OR persists with documented carry-forward (next-session signal) — both are productive R2 outcomes.**
- S15 #test-contract-as-implementation-surface: `addScore` helper (introduced for per-player scoring in 1v1) initially broke 3 tests because they DIRECTLY mutate `world.scoreProgress` as part of their fixture setup (session10 + session13 SCORE_TIER tests pre-bake scoreProgress=14 then expect deltas; gameState.test.ts L51 expects exact equality after N anchors). First fix attempt (set scoreProgress = scoreByPlayer.get(playerId)) preserved 1v1 semantics but broke solo (overwrote the test's manual mutation). Resolution: branch on `gameMode` — solo path stays ADDITIVE (`world.scoreProgress += delta`); 1v1 path uses max(scoreByPlayer.values()) for WIN gate. **Lesson: existing test contracts are PART of the implementation surface, not external to it. New features must preserve them (e.g., additive new path while keeping the old path's mutation semantics intact) OR explicitly amend the tests with rationale documented in the diff. Treating tests as collateral damage of a refactor turns a green CI signal into a noisy one.**
- SESSION #trip-wire-as-judgment-signal-not-hard-gate: world.ts grew 228 → 357 LOC vs 280 trip-wire set in the PDR. Decision: ship + log S16 carry-forward (extract new dispatch handlers + addScore to gameMode.ts), not split mid-implementation. Rationale: ~80 LOC of growth is documentation/comments + new typed fields are tightly coherent; splitting now would require two additional commits to half-finish the integration (gates + reducer cases need to live somewhere; spreading them across files mid-PDR adds churn). The work is coherent + complete + green-tested. **Lesson: trip-wires from the PDR are SIGNALS for "stop and reconsider whether to ship vs split," not hard gates. When the over-trip is mostly documentation + the integration is at a clean stopping point, ship + log carry-forward beats fragmenting. When the over-trip is genuine logic creep that signals a missing abstraction, split. Distinguishing the two cases requires reading the diff — a numeric trip-wire alone is insufficient.**

## 2026-05-12 — Session 14 of 10+ (Avatar Disambiguation + Multi-Endpoint Redundant Bonding)
- S14 #council-led-restructuring-as-prerequisite: Both Grok #7 and Gemini § 7.1 independently flagged "refactor first, feature second" for world.ts charter compliance. My original PDR proposed defer-extraction-post-feature with "safer post-feature" framing. Council inverted: adding new logic to a 587-LOC monolith is the *riskiest* path, not the safest. Restructured P2 into P2.0 (mechanical placePrimitive extraction, zero behavior change, validated by 216 existing tests) + P2.1 (redundancy bonds in the new file). world.ts dropped 587→228 LOC. **Lesson: when two Council members independently flag a sequencing issue, that's high-signal AGAINST deferring instinct. Restructure-before-feature lets the new feature land in a clean seam with full test validation of the seam before the feature changes its behavior. Cost is one extra mechanical-move commit; benefit is a code review surface that's bounded to one concern per commit.**
- S14 #no-score-for-redundancy-clean-frame: Original PDR proposed score-light (0.5×) OR full-weight scoring of redundancy bonds, plus a PHASE_1_WIN_SCORE bump 50→80 to compensate for expected ~1.5× bond growth. Council Grok G5/G8 challenged: counting redundancy bonds at full value contradicts the "raid-resistance" framing — optimal play becomes "spam redundancy for score" rather than "build interesting topology." Adopted: zero score for redundancy bonds, keep PHASE_1_WIN_SCORE at 50. Frames redundancy as defense (one concern, one knob), not score-velocity (a second concern that would need its own balance pass). Avoided fractional-score logic + HUD display format changes + a tuning compensation knob. **Lesson: when a feature has one job (defense), don't inflate it to also do a second job (faster wins). One concern per feature; one knob per concern. Threshold bumps that "compensate" for feature additions usually indicate the feature is doing too much.**
- S14 #pure-function-extraction-for-class-method-testability: `redundantBondTargetsInSameComponent` was originally a Controls class method using `this.world`. Class methods require a Pixi Application + world instantiation to test (controls.test.ts doesn't even exist for this reason — DOM mock cost > test value). Extracted `pickRedundantBondTargets({primary, componentIds, primitives, newPrimPos, radius, k, minAngleRad, angleEpsilon, maxCandidates})` as a top-level exported pure function; Controls method became a 5-line wrapper that computes `componentOf(primary)` and delegates. Enabled 10 direct unit tests covering K=0/1 boundary, empty/single-prim component, no-cand-in-range, K=3 well-spread vs sparse, AUTO_BOND_RADIUS=59-in/61-out boundary, colinear-degeneracy, MAX_CANDIDATES=17→16 truncation. Same S10 stepAttractLerp pattern, applied to a more complex geometric algorithm. **Lesson: when a class method's logic has nontrivial geometry or math, extract as a top-level pure function taking a parameter struct. Class method becomes a 5-line wrapper computing world-derived inputs. Test surface grows dramatically with minimal API impact; tests run without Pixi/DOM setup; tunables are explicit (no hidden globals).**
- S14 #verify-council-claim-with-source-not-narrative: Grok G4 claimed "STRUCTURE_GROW + parallel-constraint strain claim is mathematically false" using force-domain framing ("Σk effective stiffness"). Verified against the actual solver: bonds.ts:58 breaks on EXTENSION ONLY (`dist > rest * STRAIN_BREAK`); Verlet is POSITION-DOMAIN not force-domain; per-substep correction is independent per bond + clamped by POSITION_CORRECTION_CLAMP_RATIO=0.5. STRUCTURE_GROW pushes existing prims OUTWARD from centroid → bonds connecting new-prim-to-existing EXTEND RADIALLY → 4% strain on 20-px bond, K=3 asymmetric worst case ~8%, still 3× under HIGH-tier 25% break. Grok's "Σk" model would apply to a force-spring system, not a Verlet constraint solver. Adopted strain-cascade test (test #12) as belt-and-braces; rejected the framing. Same pattern as S13 #verify-council-claims-against-source. **Lesson: physics-domain framing matters — force-domain (F=Σk·x) vs position-domain (per-bond correction with clamp) produce wildly different safety analyses for the same impulse. Verify which solver model the Council is reasoning in before adopting OR rejecting the critique.**
- SESSION #prime-audit-as-revision-gate-not-decoration: PRIME-AUDIT (Rule 20) after Council synthesis caught (a) test #16 save/load round-trip with cycle as a missing test, (b) BOND_COMMIT visualEffectId assertion needed to be explicit not implicit, (c) anchor-placement S10 regression test needed re-verification. Each ~2 minutes to address; would have shipped as silent gaps without the audit step. Same value-density as S11 #cost-anchors-staleness-checked-via-live-loc. Cost: ~3K tokens. Catch rate: 3 material findings on a Standard PDR. **Lesson: PRIME-AUDIT is not a victory lap or paperwork — it's the last revision-gate where "did I miss anything specific" produces concrete diffs. Schedule 5-10 minutes after Council synthesis BEFORE writing gate flags; expect at least 1-2 material findings per Standard tier deliberation. Empty PRIME-AUDIT = audit was rubber-stamped, not run.**

## 2026-05-12 — Session 13 of 10+ (Playtest Feedback Batch)
- S13 #verify-council-claims-against-source-before-adoption: Grok DISRUPTOR returned 6 challenges; 3 of them (spatial index, constraint amplification, off-center dedup brittleness) were demonstrably wrong against the actual codebase. Reading `spatial.ts` (Sparks-only index, no primitive use), `bonds.ts:65` (strictly dissipative `error*stiffness*0.5/dist` solver), and the placePrimitive dedup logic (independent components are dedup-safe) refuted each in turn. Adopted only 2 of 6 Grok findings; the other 4 Gemini concerns were higher-signal. **Lesson: Council outputs are proposals, not verdicts. Verify physics/code claims against actual source before adopting OR rejecting. A REJECT documented with code-citation prevents later "we missed this" reopens — same value as an ADOPT.**
- S13 #knob-splitting-when-one-constant-doubles-as-two-semantic-concepts: AUTO_BOND_RADIUS=60 was doubling as both "primary target picking radius" AND "merge sweep radius." User reported merge inconsistency at 3 separate structures ~90 px from placement; root cause was the shared radius — they're semantically different concerns (primary precision vs merge reach), the use cases had diverged. Split into separate constants: AUTO_BOND_RADIUS (stays 60, controls.ts-local) for primary-pick precision; MERGE_REACH_RADIUS=100 (constants.ts, imported) for merge sweep. **Lesson: when a single constant serves two semantically distinct use cases AND playtest reveals a tuning conflict, the right fix is to split the constant, not to compromise on the shared value. Cheaper than one-knob optimization; clearer code.**
- S13 #impulse-direction-as-ux-framing: Original P2 design pushed STRUCTURE_GROW prims OUTWARD FROM NEW-PRIM ORIGIN — Grok #2 challenge: this reads as "structure recoils from new prim arrival," not "structure grows." Revised to CENTROID-OUTWARD (compute centroid of post-bond component including new prim; each pre-existing prim pushed away from centroid). Same magnitude, same physics, but UX framing is "the structure expands radially," matching user's mental model of "growth." **Lesson: physics-impulse direction is a UX framing choice as much as a math choice. Same per-frame motion, different mental model. When the user describes a desired feel ("grows physically"), check whether the impulse direction matches the conceptual frame they're using.**
- S13 #short-bond-clamp-prevents-teleport-not-strain-break: Gemini #1 flagged "short bonds risk strain break under MERGE_IMPULSE." But `bonds.ts:58` breaks on EXTENSION only and MERGE_IMPULSE is INWARD (compression-only) — the strain analysis was over-stated. Adopted the clamp anyway, because at `idist < MAG` the cand teleports PAST the new prim (bond direction flips, visual goes haywire). The right justification for the clamp is "prevent direction-flipping teleport," not "prevent strain break." **Lesson: a Council mitigation can be correct (adopt) AND its stated reason can be wrong (re-justify). Adopt the mitigation; correct the reasoning in the comment so the next reader doesn't propagate the wrong model.**
- SESSION #per-priority-commit-vs-thematic-batching: PDR originally had 4 work priorities (P1, P2, P3, P4) + closeout. P1 (merge reach refactor) and P3 (MERGE_IMPULSE tuning) shared the same code path (merge sweep in placePrimitive). Either commit them together (semantic coherence, fewer atoms) or split them via revert-and-re-add (per-priority hygiene, more churn). Chose: commit P1+P3 as one atom with both labelled in the message body; P2 + P4 as separate atoms since they touched separate code regions. **Lesson: per-priority commits is a hygiene guideline, not a hard rule — when two priorities share a code path tightly enough that splitting would require revert-and-re-add, commit as a thematic atom with both priority labels in the message. Don't trade hygiene for churn.**

## 2026-05-11 — Session 12 of 10+ (effectsRenderer Per-Kind Split)
- S12 #per-kind-split-when-charter-breached-via-monolith: § XV LOC breach on a file scheduled for additive growth (Phase 2 will add new effect kinds per spec § VIII / § X.4) is "refactor now along the additive axis." Per-kind split means each future kind plugs in as a new file under `src/render/effects/`. Counter-argument (Grok DISRUPTOR #1: defer to post-Phase 2) was rejected on charter authority — § XV is a current breach; deferral makes the monolith worse with each kind added. **Lesson: if a file is on the charter cliff AND will grow further, refactor along the growth axis before the growth lands. Don't wait for "Phase 2 informs the seam" — the seam IS the growth axis itself.**
- S12 #dead-code-audit-before-code-motion: Grok DISRUPTOR #2 challenged: don't transport unverified code; grep for dead silhouettes first. Audit ran (combos.ts visualEffectId values vs the 13 cases in drawBondCommit's switch) and yielded ZERO deletions — all 12 magic IDs + fx.bond.default actively emitted. Audit still load-bearing: proves moved code is all live, costs one grep. **Lesson: refactoring is the right time to grep for unreachable code. The audit is cheap (one grep); even a negative result is a positive — every byte moved is byte still used. Doing this *before* the move means dead code dies in its old home rather than getting copy-promoted.**
- S12 #pure-fn-drawers-with-age-not-tick-state: Gemini AUDITOR flagged "Graphics ownership semantics + world.tick state assumptions" as missing risk-register rows. Resolution by design (not by mitigation): parent class owns the Graphics instance (calls `g.clear()` once per sync), drawers receive `(g: Graphics, effect, age: number)` as pure-fn parameters — they never read `world.tick` directly. Pre-computing `age = world.tick - bornTick` at the orchestrator + passing it through is the seam that keeps cross-file drawers stateless. **Lesson: when extracting drawers from a stateful class, parameterize by the time-derived value (age, t, eased), not the time source (world.tick). The class owns time; the drawers own geometry. Same shape for shared `Graphics`: class owns lifecycle; drawers append.**
- SESSION #council-r1-vs-handoff-endorsement: Handoff explicitly endorsed per-kind split (S11 P3 PRIME-AUDIT carry-forward block named the 5 file targets verbatim). Council R1 still ran and surfaced 7 concerns I had not anticipated: separate silhouettes.ts from bondCommit.ts, CI smoke test (zero renderer unit coverage before), Graphics/tick risks, budget honesty (22K→12K), dead-code audit pre-move, SEVER_ERASE consistency call, defer-or-not. Adopted 6 of 7 (rejected #1 — defer — on charter authority). **Lesson: handoff endorsement is "WHAT to do" direction; Council R1 is "HOW to do it well" deliberation. Don't skip Council even when handoff is direct — the direction was endorsed but the implementation details, file layout, test plan, and risk envelope were not.**

## 2026-05-11 — Session 11 of 10+ (Buffer: Drift Cleanup + Phase 2 Design Matrix)
- S11 #council-r1-surfaces-spec-mechanic-pdr-omitted: PDR enumerated 6 Phase-2 mechanics (Fog/Local-MP/Inject Spiral/Steal/Multi-color/Mega-combos). Council R1 (Grok DISRUPTOR) challenged spec citations for each, surfacing that `§ VIII.3` lists THREE disruption actions (Sever / Inject Spiral / Steal) — the original PDR list silently dropped Sever-as-disruption because Phase 1's self-sever (`§ VIII.4`) already half-implements it. Added as 7th mechanic with explicit note in doc that Phase 1 only handles the SELF case; cross-player charge-gated attack is distinct. **Lesson: bound design-doc scope by SPEC enumeration, not by PDR enumeration. PDR is the proposal, spec is the authority. Council audit catches PDR-vs-spec gaps that pure synthesis can rubber-stamp.**
- S11 #cost-anchors-staleness-checked-via-live-loc: Initial design-doc cost-anchor table used HANDOFF numbers (world.ts 370, effectsRenderer.ts 470). PRIME-AUDIT step ran `wc -l` on each — actual was 481 and 569 respectively (S10 cinematics grew both). Updated table + flagged effectsRenderer's charter-breach (569 > 500-LOC § XV) as carry-forward. **Lesson: anchor design-doc cost estimates to LIVE LOC at PRIME-AUDIT time, not to handoff snapshot. Code grows between sessions; a 1-session-old anchor is already drifting. The `wc -l` check is free and catches charter breaches as a side effect.**
- S11 #design-doc-overage-justified-by-council-additions: Doc landed at 523 lines vs 280-400 target (matched S9 P4's 280-line precedent). Cause: Gemini AUDITOR's REVISE verdict added Risks + Playtest-Readiness blocks per option (~25 lines × 7 options = 175 added). Accepted overage with explicit justification in PRIME-AUDIT: "materially better than R1, not just longer." **Lesson: when Council deliberation adds quality dimensions to a deliverable, the line-count budget grows proportionally. Decide "better vs longer" by content density (does each added line carry a new dimension, or repeat prior content?), not by raw count. Budget targets are heuristics, not gates.**
- SESSION #buffer-session-doc-prep-when-everything-gated: S11 was a buffer session with all 3 actionable backlog items user-gated (cinematics tuning blocks on playtest; audio blocks on Suno track upload; Phase 2 implementation blocks on "ship Phase 2" sign-off). Only doable work was design-doc prep — same pattern as S9 P4 producing cinematics options doc 1 session before S10 implementation. **Lesson: in buffer sessions where all implementation gates are user-controlled, design-doc prep for the NEXT-session conversation is the highest-leverage non-gated work. When user later opens the gate, the doc-as-decision-matrix lets implementation start at "pick from matrix" rather than upstream brainstorm.**

<!-- S10 entries (#attract-impulse-to-lerp, #bfs-hop-map-as-effect-payload, #verlet-impulse-via-prevpos-offset,
     #cinematic-physics-half-stays-unconditional, #test-via-pure-helper-export,
     #cross-cutting-gate-deferred-to-last-priority, #anchor-place-emits-grow-too,
     SESSION #playtest-confirmed-bug-fix-as-first-priority) pruned at S19 handoff
     to maintain ≤50 cap with 6 new S19 entries (S10 attract-lerp now canonical
     in controls.ts; effect-payload pattern repeated S17+ multi-color/§13.11/§13.14;
     verlet-impulse-via-prevpos canonical; cinematics toggle pattern superseded by
     S18 mute-glyph + S19 settingsOverlay UX-toggle; pure-helper-export pattern
     now-canonical S10-S19; cross-cutting-gate-last superseded by S19 batch order;
     anchor-place-emits-grow now-canonical; playtest-first ordering superseded by
     S15+ playtest-gated carry-forward pattern). Originals preserved in archived
     handoff docs under .handoff-archive/. -->

<!-- S7, S8, S9 entries pruned at S18 handoff to maintain ≤50 cap with 5 new S18 entries:
     S7 #cursor-vs-pos-as-source-of-truth superseded by S10 #attract-impulse-to-lerp
     (lerp eliminated the dual representation entirely — no more cursor-vs-pos divergence path).
     S8 #prime-audit-delta-as-pre-playtest-target + #sister-defect-via-pattern-search now-canonical
     (applied uniformly S9-S17; PRIME-AUDIT Rule 20 codified in CLAUDE.md).
     S9 #urgency-detected-as-pdr-gate now-canonical (codified in CLAUDE.md URGENCY PROTOCOL).
     S9 #single-bond-vs-multi-bond-action-shape + #score-threshold-as-balance-knob +
     #design-doc-as-decision-matrix-not-proposal + #pdr-renumbering-vs-execution-order
     tactical patterns now-canonical. SESSION #handoff-push-not-just-commit durable in
     feedback memory (~/.claude/projects/.../memory/feedback_handoff_push.md) + CLAUDE.md
     HANDOFF protocol. Originals preserved in archived handoff docs under .handoff-archive/. -->

<!-- S4 technical entries (spec-drift, color-as-ownership, pixi-particle-container,
     boundary-strict-vs-equal, stress-test-fixture-broke) pruned at S10 handoff.
     S5 detail entries (#max-speed-clamp-broke-attract, #attract-needs-momentum-or-strength,
     #spawner-bounds-blocks-pickup, #pointer-capture-auto-release) pruned at S11 handoff
     to keep total ≤50 entries — S6+ lessons supersede.
     S5 pixel-tech entries (#dpr-double-bug, #naive-fix-incomplete, #max-speed-clamp-location)
     + S6 #headless-tab-pauses-ticker pruned at S12 handoff (DPR fix is in code; ticker
     workaround superseded by S8 #headless-render-needs-game-loop).
     S4 SESSION block (#spec-correct-vs-playable) + S5 detail entries (#single-action-place,
     #headless-test-contamination) pruned at S13 P5 closeout (S5+ playability work superseded
     by S7-S10 cursor/place/cinematics rework).
     S5 SESSION block (#user-path-go-skip-deliberation) + S6 #git-init-late + S6
     #defensive-fix-for-non-bug pruned at S13 /handoff to bring total to 50 cap
     (handoff skill Step 2.8 enforced; S7+ user-path-approval entries supersede).
     S6 block (#effects-cap-belt-and-braces, #per-combo-effects-cheap,
     #endpoint-info-needed-for-line-effects, SESSION #user-path-batch-approval)
     pruned at S14 closeout to maintain ≤50 cap (S6 effects-renderer detail
     superseded by S8 animation-static-split + S12 per-kind effect-renderer split;
     batch-approval mechanic superseded by S13 explicit-go-token-permits-same-turn-action).
     S7 entries (#ephemeral-to-persistent-silhouette-rewrite,
     #pdca-deliberation-skip, #pixi-tab-pause-mutate-then-render,
     #zone-aware-test-coords, #structure-renderer-tick-plumbing) pruned
     at S15 closeout to maintain ≤50 cap with the 5 new S15 entries
     (silhouette geometry-re-imagining superseded by S12 per-kind split;
     skip-deliberation by S13 explicit-go pattern + S15 amendment-cycle;
     tab-pause by S8 #headless-render-needs-game-loop; zone-aware-tests
     well-internalized; tick-plumbing now-canonical).
     S8 entries (#headless-render-needs-game-loop-not-just-renderer,
     #pixel-hash-distinguishes-animation-classes,
     #mock-extension-safe-via-tick-independence,
     #animation-static-split-as-paired-regression-tests,
     #flag-for-veto-in-pdr, SESSION #continuation-discretion-with-pedantic-thoroughness)
     pruned at S16 closeout to maintain ≤50 cap with the 5 new S16 entries
     (headless-render-loop superseded by S16 #headless-preview-rAF-throttle-blocks-screenshot;
     pixel-hash + mock-extension + paired-regression-tests now-canonical testing patterns;
     flag-for-veto-in-pdr superseded by S15+ scope-amendment-mid-flight pattern;
     continuation-discretion superseded by S15+ user-amendment-cycle-re-entry pattern).
     S9 entries (#s7-snap-cursor-undone-by-physics-gate, #component-dedup-via-set-not-bfs-per-call,
     #post-place-sweep-emit-effects-too, and S7 SESSION #user-path-batch-approval-with-discretion)
     pruned at S17 closeout to maintain ≤50 cap with the 5 new S17 entries
     (snap-cursor superseded by S10 P1 position-lerp follow which eliminated the snap mechanic;
     component-dedup tactical and now-canonical; post-place-sweep emit-effects now-canonical;
     batch-approval-with-discretion superseded by S15+ user-amendment-cycle-re-entry pattern).
     well-internalized; tick-plumbing now-canonical render convention).
     S3 + S1-2 session blocks pruned at S9 handoff.
     Originals preserved in archived handoff docs under .handoff-archive/. -->
