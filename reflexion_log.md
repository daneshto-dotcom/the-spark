# SPARK — Reflexion Log

Cross-session learnings. Most recent at top. Entries pruned when total >50.

## 2026-05-27 — Session 53 (S53 autonomous batch overnight: P0 CI verify pre-flight + P1 PROTOCOL_VERSION mismatch UX gloss + per-peer protocolMismatchPeers latch (S52 CHECK CONVERGENT BLOCKER resolution) + P2 RMB ConnectDrag dead-code removal -80 LOC + P3 LOCKED §13.11 strikethrough amendment block + CHECK Triumvirate onPeerLeave latch cleanup; 4 source commits bde5e41..f9ed115 + push, unit 828/828, bundle 498.51 KB / 500 KB charter +1.49 KB headroom, Deploy SUCCESS, PC reboot mid-session work resumed intact)

- S53 #protocol-mismatch-ux-latch-shipped-as-dormant-infrastructure: Implemented full Council-deliberated CONVERGENT BLOCKER fix (detectProtocolMismatch pure helper + onProtocolMismatch callback + per-peer protocolMismatchPeers Set<peerId> latch dropping ALL subsequent messages from a mismatched peer + onPeerLeave defensive cleanup per Gemini CHECK M2) — only to discover via PRIME-AUDIT ΔI grep at session close that NO PRODUCTION CODE in the entire codebase actually sends a HELLO envelope. `grep transport.send src/` returns ENDGAME / GODLY_TRIGGER / START_GAME_SIGNAL / INTENT / NETSNAPSHOT — but never HELLO. The HelloMsg type + parseNetMessage HELLO check at protocol.ts:193-198 + S22 P3 protoVersion bump comment all anticipate a HELLO-handshake that has NEVER been wired on the send side, since S15 P2 (2026-05-12) when the lobby was first built. So the entire P1 latch + UX infrastructure is correct, type-safe, fully unit-tested, but currently DORMANT — fires zero times in live P2P sessions. **Lesson: state-discovery (Rule 21 A.0 gate) should include "does any production code path actually PRODUCE the input my new feature consumes?" not just "does the consumer code I'm modifying exist?". Council R1 + Gemini AUDITOR both deliberated on the consumer (transport.onMessage) without questioning whether HELLO is sent. PRIME-AUDIT delta caught it only post-ship via grep audit. Pattern carry-forward: for any new wire-protocol feature, the A.0 gate MUST include a grep for the producer's CALL SITE, not just the type/interface definition. S54+ should wire `transport.send({kind:'HELLO', playerId, color, protoVersion})` at peer-join time on BOTH host (hostHandlers onPeerJoin) and joiner (clientHandlers post-connect) to activate the latch. The infrastructure is ready and waiting.**

- S53 #per-peer-protocolmismatchpeers-latch-pattern: Set<peerId> at the transport boundary drops ALL subsequent messages from a peer once protoVersion mismatch detected on HELLO. Closes the v2-peer-INTENT-bypass-after-failed-HELLO desync gap that Council R1 flagged as CONVERGENT BLOCKER (Grok #4 + Gemini #2): a stale-build peer whose HELLO null-rejects can still send INTENT(PICKUP_SPARK) — the action type is in KNOWN_GAME_ACTION_TYPES_RECORD allowlist, host applies, joiner gets stuck Carrying. The latch makes the HELLO-reject sticky per-peer. Cleared on `disconnect()` (full transport teardown) + on `onPeerLeave` after stillSeenElsewhere check (defensive hygiene per Gemini CHECK M2 — Trystero gives fresh peerIds on browser refresh, but unbounded set growth across long sessions is undesirable). emitProtocolMismatch is idempotent via early-return when peerId already in set — multi-strategy fan-out (Nostr + Torrent + MQTT) only fires the UI callback ONCE per peer. **Lesson: when a wire-protocol handshake fails, the failure must be sticky at the transport boundary, not just at the rejection point. Otherwise the protocol below the handshake remains permissive to the same peer. Pattern carry-forward: any future peer-validation feature (auth tokens, capability negotiation, version handshake) should adopt the per-peer Set<peerId> latch pattern at the transport.onMessage boundary — cheap (1 Set membership check per message), blast-radius-contained (single class field), defensive (idempotent emitter).**

- S53 #rmb-connectdrag-removal-confirms-dead-via-grep-and-typescript-narrowing: Post-S52 P1 atomic LMB-up made the entire RMB ConnectDrag carry-then-aim-then-place flow unreachable. Dead-code chain spanned 5 files: ConnectDrag variant in ControlState union → RMB-Carrying onDown branch + onMove ConnectDrag branch + onUp RMB-up branch → pickPrimitive wrapper → structureRenderer.drawPreview method → 4 helpers (drawTierGlyph, drawNoBuildGlyph, TIER_COLOR, isInsideSpawnerZone) → SPAWNER_* + ControlState imports. TypeScript type narrowing surfaced each dead branch as a compile error after the union variant was removed, so removal could proceed top-down with tsc as a guide. Removal verified via two PR-gate checks: (a) `grep ConnectDrag src/` returns ONLY explanatory comments (zero active code refs), (b) tsc -b --noEmit clean. Bundle delta -2.26 KB (P2 alone), net -1.12 KB vs S52 close. **Lesson: when a feature becomes architecturally dead, the cleanup must be ordered by TypeScript narrowing — remove the discriminated-union variant FIRST, then let tsc point to every dead consumer. Mechanical, low-risk, complete. Pattern carry-forward: any future dead-code sweep on a discriminated-union-shaped state machine should follow this order: (1) remove the variant, (2) compile, (3) tsc errors enumerate the dead consumers, (4) remove each, (5) compile clean, (6) grep-zero-references PR gate. The 4 dead helper functions (TIER_COLOR + drawTierGlyph + drawNoBuildGlyph + isInsideSpawnerZone) would have been missed by a pure name-grep — tsc unused-import warnings + manual lint caught them.**

- S53 #locked-13-11-strikethrough-amendment-convention-codified: Established convention for amending LOCKED_DECISIONS.md spec entries without losing historical context: `~~markdown strikethrough~~` + `_HISTORICAL (date-range)_` italic annotation wrapping the deprecated text + explicit `**Sxx Py AMENDMENT (date, authorization context)**` block below with the active rule + rationale + implementation pointers. Preserves spec-as-record (future sessions can audit why the rule changed without git-archaeology) while making the live rule unambiguous to the reader. Applied to §13.11 PRIME-AUDIT B (cycle-no-consume → cycle-consume) — covers the S52 P2 user-authorized rule inversion. Gemini CHECK #6 surfaced this as a process finding ("is this the project's convention?") but recommended REJECT since it's a process question not a code defect. Convention is now codified in §13.11. **Lesson: spec amendments need a project convention not just ad-hoc per-amendment style. Strikethrough + annotated historical block + amendment block + implementation pointers is the four-part pattern. Pattern carry-forward: every future LOCKED amendment should adopt this four-part shape. §13.x is the easiest place to start the convention since each numbered subsection is self-contained.**

- S53 #council-r1-and-check-grok-hallucinations-pattern-documented: Across Council R1 (pre-impl) + CHECK Triumvirate (post-impl), Grok DISRUPTOR/ANALYST surfaced 12 findings; 8 were FALSE-POSITIVES, of which 4 were outright HALLUCINATIONS of files or classes that don't exist in the codebase. Hallucinations: R1-G3 ("pickPrimitive callers in structureRenderer.test.ts:142 + hotkeys.test.ts:67") — both test files MISSING (verified via ls). CHECK-G5 ("structureRenderer.test.ts test doubles will break") — same file still missing. CHECK-G6 ("GameHost/GameClient classes may pass old signature") — no such classes exist in main.ts. Logic-inversion errors: R1-G1 ("race condition on parsed shape") — transport.ts:257-269 JSON.parses BEFORE my pre-check; CHECK-G4 ("unconditional SEVER_BOND removes safety guard") — pre-S53 else-branch always fired post-S52 P1 so no behavior change. Gemini AUDITOR also had ONE logic-inversion (CHECK-M1 "missing protoVersion bypasses detection" — code explicitly handles it via fall-through, test explicitly covers + PASSES) and was otherwise reliable. **Lesson: Council/CHECK adversarial agents need fact-checking against the actual filesystem + actual code before adopting BLOCKER/HIGH findings. Grok in particular hallucinates aggressively when prompted in adversarial mode. The PRIME-AUDIT pass at synthesis time MUST verify cited file paths via `ls` and cited code patterns via `grep` before adopting recommendations. Pattern carry-forward: a PRIME-AUDIT checklist line item — "for every Grok/Gemini finding citing a specific file:line, file:line — VERIFY via Read or Grep before adopting." Document the false-positive count per session in handoff so trend is visible. S52 close had 1 Grok hallucination (E2E brittleness); S53 had 4 (3 R1 + 4 CHECK = 7 ratio'd to 12 total findings = 33% hallucination rate). Project may benefit from a dedicated `verify-council-citations.sh` tool that auto-greps cited paths before PRIME-AUDIT.**

- S53 #onpeerleave-latch-cleanup-defensive-hygiene-not-functional-bug: Gemini CHECK M2 ADOPT — clear the protocolMismatchPeers Set entry when a peer fully leaves the Trystero room (gated by the existing stillSeenElsewhere check confirming peer gone across ALL strategies). No functional bug because Trystero assigns fresh peerIds on browser refresh (the typical "mismatched peer refreshes to fix" scenario uses a new peerId on re-connect, fresh HELLO re-check), but unbounded Set growth across a long-lived session with many transient mid-deploy v2 joiners is undesirable. ~5 LOC addition mirroring the existing peerSet.delete(peerId) cleanup pattern in the same block. **Lesson: bounded growth of per-session state structures (Set<peerId>, Map<peerId, X>, etc.) at peer-lifecycle boundaries is a defensive hygiene rule even when no functional bug is provable. Pattern carry-forward: any new per-peer state structure should be cleaned up in BOTH disconnect() AND onPeerLeave(after-stillSeenElsewhere-check) by default. The onPeerLeave cleanup is the long-session safety net; disconnect() is the full-teardown safety net.**

- SESSION #s53-shipped-3-priorities-plus-check-fix-5-commits-828-of-828-green-bundle-498-51-KB-1-49-KB-headroom: 5 commits (bde5e41 P1 / f0f629f P2 / 5c4883d P3 / f9ed115 CHECK) all pushed to origin/master. Tests 815 → 828 GREEN (+13 new from P1: 7 detectProtocolMismatch + 4 formatProtocolMismatchMessage + 2 NetTransport.onProtocolMismatch field). Bundle 499.60 → 500.74 (P1, briefly over) → 498.48 (P2 reverse) → 498.51 KB (CHECK +0.03) at S53 close. tsc -b --noEmit clean throughout. Council R1 Battle Ledger 5 ADOPT / 4 REJECT / 3 PARTIAL — CONVERGENT BLOCKER from S52 CHECK resolved (Grok#4+Gemini#2 v2-peer-INTENT-bypass). PRIME-AUDIT 9 deltas total (6 R1 pre-impl + 3 CHECK post-impl), including the critical ΔI discovery that the P1 infrastructure is correct but currently DORMANT — no production code sends HELLO. CHECK Triumvirate: 12 findings, 1 ADOPT (M2 onPeerLeave cleanup), 8 false-positives + 4 Grok hallucinations documented as PRIME-AUDIT learning. PC reboot mid-session — work resumed from intact uncommitted edits via git status verification. API spend: Grok ~$0.02 (2 calls Council R1+CHECK), Gemini ~$0.10 (2 calls). Cumulative S53: ~$0.12. User authorized autonomous overnight: "APPROVED! I will approve your full autonomous run because iil be heading to bed!" + "extremely methodical, pedantic and effective ... full scope of your tools." S53 P1 E2E failure (Sym F territorial-block test, joiner placed 1 of 3 expected prims) flagged as likely flake — unrelated to P1 changes (HELLO is never sent in any path). CHECK-Triumvirate E2E in flight at handoff write.

## 2026-05-26 — Session 52 (S52 autonomous batch overnight: P1 atomic PLACE_FROM_FREE replacing PICKUP+PLACE burst + dragLock interpolation skip + P2 LOCKED §13.11 cycle-no-consume amendment + P3 duckMusic Web Audio scheduled restore Gemini CHECK #1 carry; 4 commits 7c446a2..f4b516d + push, unit 815/815, bundle 499.60 KB / 500 KB charter, Deploy SUCCESS, E2E SUCCESS at e529c4e, user-reported joiner input asymmetry RESOLVED)

- S52 #atomic-place-from-free-vs-two-action-burst-fixes-stuck-carrying: The joiner (P2) LMB-drag-place had two compound bugs that 815 unit tests didn't catch but 2-peer playtest exposed: (1) the legacy LMB-up dispatched PICKUP_SPARK + PLACE_PRIMITIVE as TWO separate intents, and (2) if PLACE_PRIMITIVE silently rejected (spawner-zone / target-missing race / enemy-territory hard block — host saw `territoryBlock: 45` rejects from joiner in user's debug snapshot), the prior PICKUP had already mutated `player.kind='Carrying'` + `spark.state='Carried'` with NO rollback path. Joiner stuck in Carrying — user perceived as "click and you're glued to the spark; RMB to release." Fix: NEW atomic `PLACE_FROM_FREE` action (src/state/placeFromFree.ts) collapses the two intents into one. Validation FIRST (placementPos shape, spark.state==='Free' race, player.kind==='Idle' race, remote-origin reach plausibility, spawner-zone, territory, target-existence+color-demotion OR host re-pick for remote per Council C2 BLOCKER). Only after ALL validators pass: snap spark.pos to placementPos, fsmPickup Idle→Carrying, spark.state→Carried, delegate to existing placePrimitive (which cannot reject — preconditions pre-verified per Council C3 BLOCKER contract). **Lesson: any multi-action user gesture that splits into separate intents with non-atomic intermediate state is a bug waiting to bite. Refactor to ATOMIC validate-then-commit — pre-validate everything that any of the sub-actions check, then commit. Keep legacy action handlers intact for back-compat with alternative paths (RMB ConnectDrag here). Pattern carry-forward: every multi-intent gesture on the joiner side needs an atomic alternative.**

- S52 #dragLock-skips-snapshot-interpolation-for-local-cursor-spark-fixes-jitter: User reported second symptom — "you cant see that they are being dragged. they stay at spawn point and then teleport to supposed leave point." Root cause: `sync.ts:interpolatePositions` unconditionally clobbered `spark.pos.x/y` every render frame from the snapshot, fighting the joiner's local `controls.stepAttractLerp` 60Hz writes. Snapshot wins at 10Hz (NET_SNAPSHOT_HZ); local lerp at 60Hz; net effect = jittery toward-snapshot bias. Fix: `interpolatePositions(prev, curr, t, world, dragLockedSparkId?)` accepts optional sparkId that the lerp loop SKIPS. `controls.getDragLockedSparkId()` returns active AttractDrag sparkId OR pendingPlaceFromFree-within-300ms-TTL OR null. main.ts threads to `clientSync.interpolateInto`. Joiner now sees their own AttractDrag visual smoothly. **Lesson: when client-side prediction writes to authoritative state (e.g., spark.pos directly), the interpolation layer must have an OPT-OUT mechanism so locally-predicted positions survive the next snapshot frame. The "self-correcting" claim of snapshot-overrides-everything is wrong when the spark IS the one being interacted with — it produces visible jitter as the user drags. Pattern carry-forward: any future client-prediction features must implement a dragLock-style opt-out for the predicted state's render path.**

- S52 #pendingPlaceFromFree-300ms-TTL-closes-1-frame-blink: Gemini AUDITOR Council R1 #2 HIGH caught a subtle race: after LMB-up dispatches PLACE_FROM_FREE, `controls.state` transitions Idle→Idle (passes through AttractDrag). On the NEXT render frame, main.ts reads `controls.state.kind === 'AttractDrag'` which is now false → passes no dragLock → interpolation lerps spark.pos from local-cursor-pos toward stale-snapshot-spawn-pos → spark "blinks" away from cursor BEFORE the placement snapshot arrives. Fix: extend dragLock with `pendingPlaceFromFree { sparkId, sentAt }` 300ms TTL set on LMB-up dispatch; getDragLockedSparkId() returns this sparkId until TTL expires OR a fresh AttractDrag overrides. 300ms covers typical RTT/2 50-100ms with comfortable margin. TTL self-clears on read. **Lesson: state-transition races between local FSM and snapshot interpolation need explicit "transient lock" periods, not "active drag only" gates. The pendingPlaceFromFree pattern (sparkId + sentAt + TTL) is reusable for any client-prediction window. Pattern carry-forward: any client→host intent with optimistic local visual needs a TTL-based hand-off lock.**

- S52 #protocol-version-bump-fails-closed-via-HELLO-mismatch: PROTOCOL_VERSION bumped 2→3 to add PLACE_FROM_FREE to wire allowlist. Old peers (cached pre-S52 bundle) send HELLO with protoVersion=2 → host's parseNetMessage rejects → host never acknowledges → joiner sees Connection-lost overlay → user refreshes. Mirrors S22 P3 protocol bump precedent (same-deploy assumption: GH Pages atomically deploys, both peers reload, both get new bundle). Council R1 CONVERGENT BLOCKER (Grok #8 + Gemini #1) ADOPTED. UX trade-off: opaque "Connection lost" vs explicit "Protocol mismatch — please refresh" — deferred S53 polish (~20 LOC). **Lesson: protocol versioning via HELLO check is the simplest fail-closed mechanism, but the user-facing message is opaque. For client-side P2P games on auto-deploy infra, the trade-off favors fail-closed simplicity. Pattern carry-forward: any PROTOCOL_VERSION bump should include a S+1 carry-forward to add explicit version-mismatch UX in the lobby diagnostic strip.**

- S52 #cycle-no-consume-removal-strategic-balance-amendment: User-authorized LOCKED §13.11 PRIME-AUDIT B amendment. Pre-S52 rule granted 0-charge path for hostile severs that split into empty `del` set (closed cycle). User verbatim ask "each raid point = break 1 connection no 5" inverts the exception to uniform rule: every hostile sever costs 1 charge regardless of cycle topology. Self-sever (computeBaseCharge=0 for same-color endpoints) remains the only 0-cost path. Strategic balance shift: attackers can no longer chip cycle bonds for free; defenders' triangulated cells now cost actual raid charges to penetrate. Implementation: removed ternary `split.del.size === 0 ? 0 : computeBaseCharge(...)` → direct `computeBaseCharge(...)` in world.ts SEVER_BOND. LOCKED §13.11 amended with strikethrough-historical text preservation pattern. **Lesson: when amending a LOCKED PRIME-AUDIT rule via user override, preserve the historical text (mark as pre-S* historical) AND add the new amendment block with rationale. The strategic-balance trade-off must be explicit so future sessions don't accidentally revert thinking it's a bug. Pattern carry-forward: never replace LOCKED rule text — amend with both versions visible.**

- S52 #duck-music-webaudio-setTargetAtTime-survives-suspend: S51 Gemini CHECK #1 carry-forward. Pre-S52 `duckMusic` used `setTimeout(restoreFn, durationMs)` wall-clock scheduling; restoreFn read `audioContext.currentTime` at TIMEOUT-FIRE time. Tab-blur during duck → ctx.currentTime froze during suspend → setTimeout fired at wall-time → restoreFn landed setTargetAtTime at stale ctx-time → abrupt restore on tab refocus. Fix: replace setTimeout closure with `musicGainNode.gain.setTargetAtTime(restoreTarget, newEnd, 0.150)` queued at SCHEDULE time via Web Audio automation. Per W3C Web Audio API §4.3.2: ctx.currentTime freezes during suspend; queued automation events are ctx-time relative; on resume, automation continues from suspend-point. Net behavior: duck persists through suspend, restores at originally-scheduled relative ctx-time without abrupt cuts. **Lesson: for ANY audio scheduling that needs to "happen later", prefer Web Audio's setTargetAtTime/linearRampToValueAtTime over setTimeout. Web Audio automation is ctx-time relative (suspend-safe); setTimeout is wall-clock (suspend-broken). Pattern carry-forward: any future audio code using setTimeout for scheduling MUST be audited.**

- S52 #check-triumvirate-convergent-blocker-on-atomicity-defensive-reorder: CHECK Triumvirate (Grok-ANALYST + Gemini-AUDITOR) caught a convergent finding (Grok #1 + Gemini #1 BLOCKER) that both 815 unit tests and PRIME-AUDIT missed: placeFromFree.ts mutated `spark.pos.x/y` BEFORE `fsmPickup + placePrimitive` delegation. If either fallible op threw (defensive code paths now claimed unreachable post pre-validation but not statically proven), spark.pos leak. In single-threaded JS the throw is unreachable empirically — but the theoretical concern is valid for any future async refactor. Adopted in-flight: reorder so `fsmPickup` (fallible) runs FIRST, THEN spark.pos mutations (infallible), THEN Map writes (infallible), THEN placePrimitive delegation. **Lesson: atomicity contracts in single-threaded JS reducers are easy to claim but hard to PROVE without static analysis. Defensive ordering (fallible-first, mutations-last) is cheap insurance even when the throw path is unreachable today. Pattern carry-forward: any new atomic reducer MUST be ordered fallible-first / infallible-last by default; comment the invariant explicitly. Add try/catch only when an async boundary is introduced.**

- SESSION #s52-shipped-3-priorities-4-commits-815-of-815-green-deploy-success-e2e-success: 4 commits `7c446a2..f4b516d` + push to origin/master. P1 atomic PLACE_FROM_FREE (NEW src/state/placeFromFree.ts 210 LOC + world.ts dispatch + protocol.ts v3 + controls.ts onUp single-dispatch + pendingPlaceFromFree TTL + sync.ts dragLock + main.ts thread + 16 placeFromFree tests + 2 sync dragLock tests + protocol v3 tests). P2 cycle-no-consume removed. P3 duckMusic setTimeout → setTargetAtTime. CHECK Triumvirate defensive reorder. Bundle 497.67 → 499.60 KB (+1.93 KB, 0.40 KB headroom under 500 KB charter). Unit 796 → 815/815 GREEN (+19). tsc clean. Deploy SUCCESS + E2E SUCCESS at e529c4e. Council R1: Battle Ledger 7 ADOPT + 4 REJECT + 6 Δ. PRIME-AUDIT 6 deltas handled. CHECK Triumvirate: 1 CONVERGENT BLOCKER adopted; 3 HIGH/MED resolved false-positives; 2 LOW deferred S53. User upfront authorization. API spend ~$0.12.

## 2026-05-26 — Session 51 (S51 autonomous batch overnight: P1 e2e spawner-rate test-override seam + Sym D/I downstream fixes + P2 audio polish OGG/Opus + PannerNode + auto-duck; 2 commits a394612..0c277a4 + push, 6/6 e2e GREEN local in 1.3 min, unit 796/796, bundle 497.67 KB / 500 KB charter)

- S51 #deterministic-seed-x-low-rate-causes-test-timeout-systematic-not-statistical: 5 e2e tests in CI run 26440547587 timed out on `waitForWorld(sparks spawned)` with the EXACT same final state pattern — gameState=PLAYING, gameMode=1v1, peerCount=1, tick advancing past 1500, freeSparks=[] forever. Pre-S51 hypothesis menu: P2 refactor broke spawner / snapshot serialization / WebRTC flake / spawner config drift. The actual root cause came from a 3-step empirical chain: (1) Spawner constructor at `src/game/spawner.ts:58` calls `sampleInterarrival()` which is `-ln(rng())/ratePerSecond`. (2) `mulberry32(0xc0ffee)` is a deterministic Lehmer-class PRNG — its FIRST call returns exactly `0.0214…` always. (3) `LOCKED_DECISIONS.md` Item 3 documents the S5 amendment from 1.5/sec → 0.15/sec ("strategic-bet feel — wait for the type you need"); production rate intentionally low. Math: `-ln(0.0214) / 0.15 = 25.71s` first wait. Tests time out at 10-30s. P(0 sparks at tick=1604 | λ=0.15) ≈ 1.8%; over 5 tests × 3 retries = 15 trials, P(all 15 zero by chance) ≈ 10⁻²⁶. **Lesson: when "intermittent" tests fail with statistically-impossible all-zeroes patterns, treat the failure as systematic, not flake. The investigation should start with `node -e "function mulberry32... let sec = -ln(rng())/rate; console.log(sec)"` BEFORE adding logging or rerunning. Empirical seed-replay is faster than hypothesis-iteration when the random source is deterministic. Pattern carry-forward: any test that depends on a seed-driven random arrival process should compute its expected first-occurrence wall-time AT TEST-AUTHOR TIME and either (a) use a test-only rate override OR (b) document the seed-dependent minimum wait in a leading comment so future failure analysis takes seconds, not hours.**

- S51 #sym-d-test-contract-obsolete-by-sym-f-mechanic-test-only-disable-seam-is-the-fix: Sym D was authored at S46 P3 to verify cross-color bond rejection — place BLUE prim, then RED prim within AUTO_BOND_RADIUS=60, assert no cross-color bond. S49 P1 then shipped Sym F territorial repulsion (TERRITORY_BASE_RADIUS=60, min radius 60 + 12·log₂(2) = 72px > AUTO_BOND_RADIUS=60). The geometric consequence: it is impossible to be within bond range of an enemy primitive without already being inside enemy territory, so the RED placement is silently HARD-BLOCKED before the color-seg check ever runs. Sym D's test contract became unreachable in normal play. Fix: add `__TEST_TERRITORY_BASE_RADIUS__` window seam in constants.ts (mirror of PHASE_1_WIN_SCORE pattern); Sym D-only `addInitScript({content: 'window.__TEST_TERRITORY_BASE_RADIUS__=0'})`. With BASE=0, territory R for 1 prim = 12px, RED at (1520,410) vs BLUE at (1500,400) distance 22 > 12 → places. Color-seg invariant becomes observable as defense-in-depth. Production rate untouched — Sym F has its own dedicated test that does NOT disable. **Lesson: when a new mechanic geometrically excludes a prior test's contract (radius A ≥ radius B), don't delete the prior test — add a test-only override seam for the new mechanic so the prior invariant remains testable as defense-in-depth. The prior code path is dead code in normal play but a load-bearing belt-and-suspenders if anything ever bypasses the new mechanic (territory shrink debuff, future godly disables, mod hooks). Pattern carry-forward: every PR that ships a mechanic with a radius/threshold check MUST audit existing tests for "is this contract still reachable given the new mechanic?" — track findings in the PR description.**

- S51 #spawner-zone-placement-rejection-silently-eats-anchors-test-must-account-for-it: Sym I host anchor placements at (800,400) / (800,600) / (800,800) — 2 of 3 were INSIDE the spawner zone (canvas center 960,540, SPAWNER_RADIUS=250). Distance from (800,400) to center = √(160² + 140²) = 213 < 250. placePrimitive's spawner-zone exit check fires for placements inside the zone → silently rejects (no error, just no primitive). Only the (800,800) third anchor at dist 305 actually placed. Score reached 1, never the WIN_SCORE=3 trigger. Test design bug from S50 P4 — never observed because the earlier 5-test sparks-spawned timeout masked everything downstream. Fix: anchors moved to X=300 (distance 675+ from center, all 3 well outside zone). **Lesson: e2e tests that place primitives must compute distance-from-spawner-center BEFORE committing to coordinates. The silent-rejection-on-spawner-zone-placement rule is a load-bearing gameplay constraint that tests can't violate without becoming non-deterministic. Pattern carry-forward: factor a `safePlacementPos(direction: 'left'|'right'|'top'|'bottom', minDistance=300)` helper into e2e/helpers.ts so future tests don't recompute the geometry. Also: when authoring an e2e test, run it AT LEAST ONCE in isolation before relying on it — Sym I shipped without ever passing because the prior tests in the spec all failed first, swallowing this test's own design bug.**

- S51 #playwright-addinitscript-content-form-beats-function-form-when-toplevel-vars-suffice: First attempt at Sym D's territory disable seam used the function form: `const disableTerritory = (): void => { (window as {...}).__TEST_TERRITORY_BASE_RADIUS__ = 0; }; await ctx.addInitScript(disableTerritory);`. Mirrored the existing applyTestSpawnRate helper shape, should have worked. Did NOT — first re-run still saw Sym D fail at the 1v1 button click on the joiner page (gameState stayed TITLE for 30s despite the click registering elsewhere). The fix: switch to the string-content form `await ctx.addInitScript({ content: 'window.__TEST_TERRITORY_BASE_RADIUS__ = 0;' })`. Test then passed consistently across 2 confirming runs. Root cause hypothesis: Playwright serializes function-form via `.toString()` which has subtle closure / TS-compiled-arrow / source-map / IIFE-wrapper interactions; the string-content form is byte-stable and skips all of that. The applyTestSpawnRate function form works because it passes an ARGUMENT (the rate value) — the call-with-arg path takes a different serialization branch. **Lesson: for `addInitScript` calls that just set a top-level window property to a literal value, prefer `{ content: '...' }` over arrow-function-via-closure. Function form is appropriate when (a) the script needs to use a Playwright-provided argument, OR (b) the script is non-trivial. String form is byte-stable and dependency-free. Pattern carry-forward: any future test-override seam in this codebase using addInitScript should default to the `{ content: '...' }` form. Document the heuristic next to e2e/helpers.ts applyTestSpawnRate so the precedent is visible.**

- S51 #duck-music-max-end-time-semantics-prevents-overlap-shortening: Gemini-COUNCIL R1 Δ2 caught a subtle bug pattern in the proposed `duckMusic` implementation. Naive shape: `cancelScheduledValues + setTargetAtTime(ducked) ; clearTimeout(prevRestore) ; setTimeout(restore, durationMs)`. The bug: a SHORTER overlapping duck event arriving mid-flight through a LONGER one would call clearTimeout on the long event's restore timer and replace it with the short event's shorter timer — the LONGER active duck gets truncated. E.g., 700 ms BOND_SEVERED-creature duck at t=0, 300 ms CREATURE_CHARGE duck at t=0.5 would restore at t=0.8 instead of t=0.7 + correct, but a 700 ms duck at t=0 followed by a 100 ms duck at t=0.5 would (naively) restore at t=0.6 instead of t=0.7. Fix: track `duckEndCtxTime: number` in module state; on each call, compute `newEnd = max(currentEndCtxTime, now + durationMs/1000)`; ONLY reschedule the restore timer when `newEnd > currentEndCtxTime`. Otherwise the existing longer-running timer is left alone. Factored the arithmetic into a pure helper `nextDuckEndCtxTime(currentEnd, now, durMs): number` exported for unit testing — 6 tests added asserting overlap edge cases (shorter-mid-longer / longer-mid-shorter / exact-tie / zero-duration / zero-current). **Lesson: any "highlight/duck/flash for N ms" pattern with multiple async triggers has a hidden ordering bug if the implementation does naive `clearTimeout + new setTimeout` on each trigger. The fix is `max(currentEnd, newEnd)` semantics. Factor the arithmetic into a pure helper so unit tests can verify the overlap math without fake-timers / async dance. Pattern carry-forward: when writing async UX effects with multiple sources, always ask "what happens if a shorter event arrives mid-flight through a longer one?" before shipping.**

- SESSION #s51-shipped-2-priorities-2-commits-6-of-6-e2e-green-bundle-under-charter: 2 commits `a394612..0c277a4` + push. P1: src/constants.ts test-override seams for SPAWN_RATE_PER_SECOND (mirror PHASE_1_WIN_SCORE) + TERRITORY_BASE_RADIUS; e2e/smoke.spec.ts applyTestSpawnRate helper for Sym A/C/D/F/I (rate=1.5 per Council C1 over Grok B 0.5 which fails the test contract at the seeded sequence) + Sym D territory disable + Sym I anchor X 800→300 outside spawner zone. P2: ffmpeg libopus 64k re-encode blue-steppe-orbit.mp3 (10.0 MB) → .ogg (3.5 MB, -65%); MP3 retained as Safari pre-17 fallback per Council C5. PannerNode positional audio (optional pos? on 4 SFX entry points; mapPanningPosition + createPanner helpers); duckMusic + nextDuckEndCtxTime pure helper (Gemini Δ2 refinement). Wired into drainAudioEffects: BOND_FORMED/BOND_SEVERED/CREATURE_CHARGE get positional routing; CREATURE_CHARGE → 300 ms duck, BOND_SEVERED-creature → 700 ms duck. Bundle 496.28 → 497.67 KB (+1.39 KB, 2.33 KB headroom under 500 KB charter). Tests 783 → 796 GREEN (+13 panner/duck pure-helper tests). tsc -b --noEmit: clean. e2e LOCAL: 0/5 active fail → **6/6 GREEN in 1.3 min** (Sym E fixme skipped, unchanged). CI re-run (run 26444645703) IN_PROGRESS at handoff time post-push. Council R1 via parallel Grok grok-4-1-fast-non-reasoning + Gemini gemini-2.5-pro MCP calls — Battle Ledger 6 rows + PRIME-AUDIT 5 deltas handled in-flight + CHECK Triumvirate (RALPH self-audit + GROK-ANALYST + GEMINI-AUDITOR) — 1 real MED finding (Gemini #1: AudioContext suspend + setTimeout edge case for tab blur >> duck duration) noted for S52 follow-up, all other findings false-positive or LOW. Total tool calls 90+. User authorized autonomous overnight via "be extremely methodical, pedantic and effective... full scope of your tools... thorough /handoff... coherent prompt for next session." API spend: Grok ~$0.01 (2 calls Council R1+CHECK), Gemini ~$0.05 (2 calls). Cumulative: $0.06.

## 2026-05-26 — Session 50 (S50 autonomous batch overnight: P1 vite/vitest CVE bump + P2 main.ts hypertrophy refactor 1221→888 LOC + P3 Sym E score occlusion polish + P4 e2e Sym I/F coverage; 5 commits e392e96..8484e7e + push)

- S50 #npm-audit-fixavailable-can-overshoot-when-nested-deps-pin-old-major: npm audit's `fixAvailable: vitest 4.1.7 isSemVerMajor:true` was the registry's "latest fix" recommendation, but the actually-minimum-sufficient version was vitest 3.x — because vitest 2.x has `dependencies.vite: ^5.0.0` (NOT a peer dep) which conflicts with project's vite ^6.4.2 and causes npm to nest a vite 5.x copy under `node_modules/vitest/node_modules/vite/`. The naive minimal-bump (vite ^6.4.2 + vitest ^2.1.9 per Council R1) WORSENED the audit from 4→5 moderate CVEs because the nested vite 5.x retained the path-traversal CVE. vitest 3.2.4 widens the vite range to `^5||^6||^7-0`, allowing npm to hoist to a single vite 6.4.2 install. **Lesson: when an `npm audit` advisory is in a nested dep tree, check the parent's `dependencies` (NOT `peerDependencies`) `npm view <pkg>@<ver> dependencies` to determine the minimum parent version that DOESN'T pin the vulnerable range. PRIME-AUDIT did not catch this — discovered only via empirical retry. Pattern carry-forward: after every CVE bump, re-run `npm audit` and SCAN the `nodes` field for nested paths like `node_modules/X/node_modules/Y` — those indicate version mismatches that aren't fixed by the top-level bump.**

- S50 #closure-state-mutable-holder-pattern-preserves-identity-across-factory-boundary: main.ts pre-refactor had 4 lifecycle-coupled `let` mutables (netTransport, hostSync, clientSync, lastSnapshotTick) referenced from BOTH lobby callbacks (mutator side) AND per-tick snapshot loop (reader side). Council R1 R2 disagreed on extraction pattern — factory closures vs mutable record vs state holder. Picked state holder: `interface NetSession { netTransport: NetTransport | null; ... }; const session = makeNetSession();`. Per-invocation `session.X` reads guarantee freshness on reconnect (PRIME-AUDIT Δ1). Avoids both factory-closure stale-capture AND mutable-record type-safety loss. Empirical verification: all 783 tests stay GREEN, ctx rebuilt per-frame in main.ts:725-734 sees latest netTransport ref. **Lesson: when extracting closure-shared mutable state across module boundaries, an explicit typed holder object (single shape, single identity) beats both factory closures (proliferate) and `Record<string, unknown>` (type-unsafe). The 1.4 KB bundle overhead from extra module boundaries is acceptable for the architectural cleanliness gain. Pattern carry-forward: any time multiple call sites need to read/mutate a shared lifecycle resource (transport, websocket, sync state), declare a holder type at module level + pass-by-reference. Avoid `let` exports.**

- S50 #playwright-context-addinitscript-is-the-correct-test-only-feature-seam: For e2e Sym I (win-condition), Council ADOPTED a `__TEST_WIN_SCORE__` env-override read at PHASE_1_WIN_SCORE module load. Production module reads `window.__TEST_WIN_SCORE__` (falls through to 50 when window undefined OR override absent). Playwright's `context.addInitScript()` runs BEFORE bundled scripts so the assignment is observable at constants.ts load time. PRIME-AUDIT Δ2 mitigation: scope override to the specific Sym I test's BOTH contexts via inline `addInitScript` calls — Playwright contexts are isolated so other tests (Sym A/C/D/E/F) don't inherit the override. **Lesson: when an e2e test needs to shortcut a costly-to-produce production state (e.g. reaching a high score), the `addInitScript` + module-load read pattern beats both (a) direct world mutation via DEV-only accessor (snapshots overwrite) and (b) skipping the test. Production cost is ~15 LOC + 1 readonly module-load check. Pattern carry-forward: for any future e2e test that needs to bypass a configurable production gate (timer thresholds, cap values, retry counts), use the `window.__TEST_X__` + addInitScript pattern. Scope override per-test via context (NEVER beforeAll-fixture — leaks).**

- S50 #rule-21-empirical-state-discovery-caught-handoff-claim-vs-reality-delta-in-under-1-minute: Boot snapshot said P4 should "flip test.fixme() for Sym I + Sym F" but Read on e2e/smoke.spec.ts at boot showed only Sym A/C/D/E describes exist — NO Sym F or Sym I describes (and therefore no fixme() to flip). The handoff text was inaccurate. Rescoped P4 from "flip-fixme" to "author-new" in the PDR before Council deliberation. Cost: 1 file Read (≤2K tokens). Saved: not building a Council deliberation around a fictitious flip-task. **Lesson: Rule 21 STATE-DISCOVERY GATE is cheap enough to fire on EVERY priority that has external state assertions, not just SCOPE-locking. A 1-minute file Read upfront prevents 30 minutes of misdirected Council + execution. Pattern carry-forward: when a PDR scope mentions specific code patterns (fixme markers, dead-wired actions, env-rendered config), Read those files BEFORE composing the PDR body. Cite the empirical observation in the PDR's SCOPE field. Treat handoff text as a hint, not a contract.**

- SESSION #s50-batch-shipped-4-priorities-5-commits-783-tests-green-bundle-under-charter: 5 commits `e392e96..8484e7e` + push. P1 vite 5.2→6.4.2 + vitest 1.5→3.2.4 (4 moderate CVEs → 0). P2 4 extractions main.ts 1221→888 LOC (-27%): src/physics/physicsLoop.ts (146 LOC), src/state/godlyOrchestration.ts (204 LOC), src/net/session.ts (76 LOC), src/net/hostHandlers.ts (87 LOC), src/net/clientHandlers.ts (95 LOC). P3 Sym E chargeDots 210→260 + qHintText 240→290 (user-deferred 3 sessions). P4 e2e Sym I + Sym F describes added + __TEST_WIN_SCORE__ env-override seam in constants.ts. Bundle 494.45 → 496.28 KB (+1.83 KB total, 3.72 KB headroom under 500 KB charter). Tests 783/783 GREEN throughout. tsc -b --noEmit: clean. Context 15.15% GREEN at P2 close. Council Standard-tier (3-way agent deliberation via general-purpose subagent — Battle Ledger 5 rows + PRIME-AUDIT 2 deltas + state-discovery Rule 21 catch). API: 1 Council deliberation call (general-purpose Agent, ~38K tokens). User authorized autonomous overnight ("be extremely methodical, pedantic and effective ... highest level of output ... thorough /handoff ... coherent prompt for next session"). E2E CI run 26439874197 IN_PROGRESS at handoff write-time — new Sym F/I tests pending first CI verification.

## 2026-05-26 — Session 49 (S49 autonomous full-batch: P1 Sym F territorial repulsion NEW MECHANIC + P2 latent audits CLEAN + P3 housekeeping; 2 commits 463df39..ba54c3e + push)

- S49 #territory-base-radius-equals-auto-bond-radius-creates-geometric-Sym-D-defense-in-depth: The decision to set TERRITORY_BASE_RADIUS=60 equal to AUTO_BOND_RADIUS=60 means it is geometrically impossible to be within auto-bond range of an enemy primitive without already being inside enemy territory. Consequence: Sym D (cross-color color gate in placePrimitive.ts) is now unreachable in normal gameplay — territory rejection fires first. This also made the dead rendering code in shared.ts/axisAligned.ts auditable and deletable in P3. **Lesson: when two radius constants are equal, they create a layered-gate invariant where the larger-scoped check (territory) always fires before the narrower check (bond color). Document this relationship explicitly in LOCKED_DECISIONS.md (did so: §13.16) so future constant changes (raising AUTO_BOND_RADIUS above TERRITORY_BASE_RADIUS) are recognized as spec-weakening. Pattern carry-forward: any time two radius constants interact geometrically, add a comment in BOTH constant definitions noting the invariant and its implications.**

- S49 #dead-code-deletion-requires-test-audit-not-just-code-audit: When Sym D made the multi-color gradient rendering branches dead code, the test file had 8 tests specifically exercising those branches. Simply removing the production code without updating the tests would produce test failures. The right approach: (a) identify which tests specifically test the dead path (not just use the functions), (b) remove those tests, (c) verify surviving tests still cover the fast-path behavior. **Lesson: dead-code deletion = source deletion + test audit. The test file is the semantic contract — a test that "passes" by exercising dead code is a test that provides false assurance. Pattern carry-forward: before deleting a code branch, grep the test file for the branch-triggering inputs (e.g. colorA !== colorB) and remove those tests explicitly. Do NOT just delete source and watch tests pass — some tests may be green simply because the deleted code is no longer called, not because the remaining code is correct.**

- S49 #ephemeral-per-tick-physics-annotation-pattern-for-territory-engulf-warp: stiffnessMultiplier needed to be "applied this tick, reset next tick" without becoming persistent game state. Solution: non-readonly field on Bond interface, reset implicitly each tick by computeTerritorialInfluence (which only sets it when inside territory — bonds outside territory get no write, and solveBonds uses `?? 1.0` fallback). This keeps §10.2 compliance (stiffnessMultiplier NOT in WorldSnapshot/NetSnapshot) while allowing per-tick physics influence. **Lesson: the "ephemeral annotation" pattern (write-once-per-tick, optional-field with fallback) works well for physics properties that should influence simulation but not persist as game state. The `?? 1.0` default in solveBonds is the clean seam — callers don't need to reset the field, consumers don't crash on undefined. Pattern carry-forward: use this pattern for future per-tick physics overlays (e.g. damping zones, acceleration fields, collision multipliers).**

- S49 #latent-audit-clean-sweep-saves-scope: The S49 P2 latent-bug audit of 4 areas (audioCursor.ts replay safety, isLocal gating, stale bondId, UPDATE_AVATAR_POS idempotency) came back CLEAN on all 4. This saved the scope of P2 entirely — no code changes needed. The audioCursor.ts `lastDrainedTick` cursor pattern was specifically designed to prevent double-play on NET reconciliation re-drain. **Lesson: schedule latent audits BEFORE assuming they'll have fixes. A clean audit is a positive signal (the code is correct) and saves session scope for other priorities. Pattern carry-forward: when planning a session batch, always budget for the possibility that an audit is clean — if it were always fixing things, it would be called "fixing" not "auditing."**

- SESSION #s49-autonomous-sym-f-territorial-repulsion-shipped-783-tests-green: 2 commits `463df39..ba54c3e` + push. P1 Sym F: 13 files changed, territory.ts NEW (4 functions), territory.test.ts NEW (21 tests), bonds.ts stiffnessMultiplier, player.ts territorialShrinkUntilTick, world.ts SHRINK_TERRITORY, gameMode.ts resets, save.ts serialization, protocol.ts registration, controls.ts Q handler, debugOverlay.ts territory section, ui.ts Q=ZONE hint. P3: LOCKED_DECISIONS.md §13.16-13.19 (Sym D/G/I/F), dead-code deletion ~51 LOC (shared.ts + axisAligned.ts), 8 cross-color tests removed, e2e.yml continue-on-error removed. Bundle 492.22 → 494.45 KB (+2.23 KB, 5.55 KB headroom). Tests 770 → 783 (+13 net: +21 territory − 8 dead-code). Context 13.05% GREEN. Council Full-tier (pre-compaction). API: $0 additional in post-compaction phase.

## 2026-05-25 — Session 48 (S47/S48 regression triage SHIPPED autonomous overnight RALPH:HUNT mode; P1-P5 fixed all 6 confirmed bugs from S47 live smoke — Sym A/B/C/G/I + Sym E deferred; 5 commits daa750d..3c615a6 + deploy run 26416265601)

- S48 #wire-envelope-scaffolded-but-not-connected-is-now-3-instance-anti-pattern: Sym I CRITICAL — host POSTGAME never reached joiner because (a) main.ts snapshot-send was gated on gameState==='PLAYING' so transitions to WIN/POSTGAME stopped wire flow, AND (b) ENDGAME envelope existed in protocol.ts since at least S15 but NEVER had a `netTransport.send` site OR a recv handler. Joiner won the match (host debug `P1 cd=25076t`) and was never informed. Same anti-pattern as parseNetMessage pre-S38 audit (defined but not called) + KNOWN_GAME_ACTION_TYPES pre-Pass-2 (Set existed but not wired). Three instances of the same shape is a pattern. **Lesson: when adding a new NetMessage kind to protocol.ts, MUST verify BOTH halves exist before merging — (a) `netTransport.send({kind:'X',...})` call site, AND (b) `netTransport.on` recv-side dispatch. Pre-merge checklist now codified. Pattern carry-forward: a "wire shibboleth" verification step in CI that greps for each protocol.ts NetMessage kind and asserts ≥1 send + ≥1 recv site exists in main.ts. Static-only, zero runtime cost, catches scaffolding-without-wiring regressions automatically.**

- S48 #per-reason-diagnostic-counters-beat-aggregate-counters-for-silent-drop-localization: Sym A "joiner LMB-release doesn't place" had 3 plausible reject paths in `applyPickupSpark` (sparkLifecycle.ts:128/135/142): pos shape malformed, spark not Free under race, reach validation fail. All three incremented the SAME `world.diagnostics.raceRejects` counter — when live smoke showed `raceRejects:3`, no way to know WHICH path fired. Split into named buckets (`world.diagnostics.rejectReasons.{pickupPosShape, pickupSparkNotFree, pickupReachFail, placeTargetMissing}`). Cost: 4 fields + one line per reject site. Benefit: immediate root-cause localization in live smoke without source-level guesswork. **Lesson: silent-drop paths in distributed systems need per-reason counters, not aggregate ones. The "one bucket per failure mode" pattern beats "one bucket per category" because category-grain hides which of N possible failures actually fired. Pattern carry-forward: for SEVER_BOND validation, UPDATE_AVATAR_POS rate-limit (if added), snapshot apply errors — split each reject site into its own bucket. The debug overlay then surfaces N rows that map 1:1 to N failure paths.**

- S48 #autonomous-overnight-ralph-hunt-execution-viable-when-plan-is-pre-written-and-source-cited: User authorized overnight execution via "work autonomously now as i am going to bed. in the end of full priority batch, and having checked and analyzed your work thoroughly, committed pushed and updated everything run a thorough /handoff and finish off this session with a coherent prompt for next session!" Council was user-waived for S47/S48 ("no need for council, just need you and ralph hunt"). Pre-conditions for safe autonomy: (a) plan IS pre-written as `.claude/plans/IN-PROGRESS_*.md` with source citations (file:line) per Sym; (b) each priority has a narrow, tested fix path with vitest + tsc + bundle verify gates; (c) PC rebooted mid-S48 — work survived because every priority was committed before moving to the next (no in-memory state lost). Total cost ~$0 in external LLM (no Council/Auditor delegation). Rigor preserved via 5 vitest + tsc passes (one per priority) + final 4-layer prod verify. **Lesson: autonomous overnight execution is constitutional WHEN the plan is pre-written and source-cited, AND each priority has narrow vitest-verifiable scope, AND commits are per-priority (not batched). Without those preconditions (e.g. ambiguous scope, large multi-priority commits, no test coverage on touched paths), autonomy degrades to "hope it works." Pattern carry-forward: codify "autonomous mode prerequisites" in session-pdca/SKILL.md — at minimum (1) IN-PROGRESS plan with file:line citations, (2) per-priority test gate, (3) per-priority commit, (4) 4-layer prod verify before /handoff.**

- S48 #strict-spec-changes-INVERT-existing-tests-mark-them-explicitly: Sym G (Voltkin chain isolation) flipped an existing test from MATCH→REJECT on the new strict spec. Old test "matches a valid 8-chain embedded in a branched topology" → renamed to "S48 P4 (Sym G) — REJECTS a chain when any chain prim has an off-chain bond" with a multi-line comment block citing the spec change AND the old behavior being deleted. Future-self reading the test sees BOTH the new spec AND the historical context. **Lesson: when a spec change inverts test expectations, REWRITE the test rather than DELETE it — preserves the audit trail of "this used to pass, now it must fail" so a future regression that re-permits the old behavior is caught immediately. Pattern carry-forward: for any pre-S48 test that contradicts a new spec, the inversion edit MUST include (a) renamed `it()` block with new spec identifier (e.g. "S48 P4"), (b) comment block citing the spec change source (commit / handoff / cookbook), (c) inversion of the assertion (expect not toBeNull → expect toBeNull). Greppable across the codebase.**

- SESSION #s48-autonomous-overnight-ralph-hunt-shipped-5-priorities-770-tests-green: 10 files changed (constants.ts, render/structureRenderer.ts, render/debugOverlay.ts, input/controls.ts, state/world.ts, state/gameMode.ts, state/sparkLifecycle.ts, state/placePrimitive.ts, state/placePrimitive.test.ts NEW, state/godlyRecipes/voltkin.ts, state/godlyRecipes/voltkin.test.ts, src/main.ts), 5 commits `daa750d..3c615a6` + close commit `054bd57`, deploy run `26416265601` SUCCESS (4-layer prod verify L1 Last-Modified Mon 25 May 19:21:41 GMT, L2 ETag `6a14a145-782fe`, L3 bundle 492,286 bytes, L4 shibboleths ENDGAME + placementPos + pickupReachFail + off-chain all present). Bundle 490.06 → 492.22 KB (+2.16 KB, 7.71 KB headroom under 500 cap). Tests 768 → 770 GREEN (+2 net from voltkin isolation tests; 7 new placePrimitive.test.ts cases added). **Token cost ~250K with PC reboot mid-session (state recovered from committed work; no progress lost). API: $0 (Council user-waived). User-direct execution — 5 vitest passes + tsc + bundle + 4-layer prod verify per priority.** S47 plan archived to `.claude/plans-archive/2026-05-25_PDR_S47_S48_regression_triage_COMPLETED.md`. CLOSE GATED on user 2-peer smoke verification of Sym A/B/C/G/I PASS post-deploy.

## 2026-05-24 — Session 45 (BUG-CRITICAL-3 SHIPPED autonomous-mode Full tier; Sym A + Sym B + Sym C(a); 3 new layer-N+1 bugs surfaced post-S44 smoke now resolved; commit bf5090c + deploy run 26370843779)

- S45 #state-discovery-pre-empted-a-schema-bump-by-discovering-existing-wiring: PDR draft post-S44 estimated Sym B (multi-avatar render) at "snapshot schema delta + render refactor" because the assumption was that per-player cursor data needed a new NETSNAPSHOT field. State-Discovery turn 1 ran `grep UPDATE_AVATAR_POS` and discovered the ENTIRE infrastructure was already wired end-to-end (Player.avatarPos field at game/player.ts:27, action type at state/gameMode.ts:39, reducer at line 155, snapshot serialization at state/save.ts:483+547, net protocol whitelist at net/protocol.ts:146, tests at game/session15.test.ts:107) — but ZERO production dispatch sites. The action was "dead-wired" — fully plumbed but never fired. Sym B's fix collapsed from "schema bump + protoVersion + back-compat" to "wire the dispatch + iterate render", a one-third scope reduction. **Lesson: when scoping a fix, the first State-Discovery probe should be "does the infrastructure for this feature already exist but unused?" — particularly in codebases with carry-forward features that were partially shipped. SPARK's S15 P2 1v1 commit (add497f) added avatarPos + UPDATE_AVATAR_POS as preparation for future multi-player render but never wired the consumer. 8 sessions of work since then assumed greenfield. Pattern carry-forward: before writing PDR scope for a "new feature," `grep -r FOO_FIELD src/` and `grep -r FOO_ACTION src/` first — dead-wired features are free 30% scope reductions.**

- S45 #council-r2-c3-hybrid-sourcing-unanimous-instant-resolve-pattern: Council R1 challenge #3 (own-player avatar source: controls.cursor vs world.players[localId].avatarPos) had unanimous convergence — Claude (Prime, implementation lens), Grok (Disruptor, risk lens), Gemini (Auditor, quality lens) ALL independently arrived at the same answer: hybrid sourcing (local=cursor lag-free, remote=avatarPos snapshot-driven). No model dissent, no Council vote needed. Gemini called rendering own avatar at network-lagged position "a cardinal sin of game feel" — strongest assertion language in the audit. **Lesson: when all 3 Council voices converge on the first round with no dissent, that's the INSTANT-RESOLVE signal — skip the synthesis step for that decision and move on. Battle Ledger captures the convergence as audit trail. Pattern carry-forward: build "unanimity → instant-resolve, no R2 needed for this row" into the Council synthesis flow; reserves the R2 quality gate budget for actual disagreements. Especially valuable on Full tier where R2 is expensive (~5K tokens) and unanimity is a strong signal of design correctness.**

- S45 #prime-audit-delta-4-expansion-caught-deeper-coupling-than-PDR-scope: Pre-implementation, the PDR scoped Sym A as "bypass joiner-local reach gate" — a single 1-line change in controls.ts. PRIME-AUDIT Δ4 expansion mid-execution discovered the actual fix required THREE coupled changes: (1) controls.ts client-mode gate bypass, (2) sparkLifecycle.ts applyPickupSpark snap to avatarPos for REMOTE carriers, (3) gameMode.ts applyUpdateAvatarPos carry-coupling so spark.pos tracks avatarPos when player is Carrying. Reason: even with the reach gate bypassed, joiner's PICKUP_SPARK + PLACE_PRIMITIVE intents arrive at host in the SAME tick — between them, no UPDATE_AVATAR_POS dispatches (10Hz throttle). Without the pickup-time snap, host's spark.pos remains stale at pickup-time spawner-pulse value, so PLACE_PRIMITIVE lands the primitive at that stale pos, not where joiner clicked. The snap in applyPickupSpark captures the carrier intent at pickup-time, and the carry-coupling in applyUpdateAvatarPos maintains tracking thereafter. **Lesson: PRIME-AUDIT's role is not just adversarial review of synthesis — it's "what coupling does the proposed fix imply that the PDR didn't surface?" Multiplayer state corrections often have multi-file ripples because the same authoritative value (here: carrier intent) flows through pickup + update + render. PDR scope estimates should add a 30% multiplier for net-state changes to account for these couplings. Pattern carry-forward: when authoring PDRs for net-state bugs, explicitly trace "the value flows through file X → Y → Z" before estimating LOC.**

- S45 #remote-carrier-gating-on-spark-snap-preserved-754-test-suite: First implementation attempt did unconditional snap of spark.pos = player.avatarPos in applyPickupSpark. This broke the existing sparkLifecycle.test.ts "happy path" + would have broken session15.test.ts placeAnchor helper because tests create sparks at arbitrary positions and dispatch PICKUP_SPARK without setting avatarPos first — primitives would land at default spawn pos instead of test-intended coords. Solution: gate the snap on `world.gameMode === '1v1' && action.playerId !== world.localPlayerId` (remote-carrier-only). Solo + 1v1-host-own-pickups preserve pre-S45 behavior byte-identical (controls.applyPerSubstep already snaps spark.pos to cursor each substep, so the same-tick PICKUP+PLACE pair gets correct position from local cursor). Only the joiner case (remote from host's POV) takes the new snap path. **Lesson: when adding a behavioral change to a reducer that's called from multiple modes, gate the change on the mode that needs it rather than blanket-applying. The "fewest tests broken" heuristic is a proxy for "narrowest contract change." Pattern carry-forward: for any reducer change in shared code paths, run the test suite immediately after the first implementation attempt and INSPECT the failures to identify the right gating dimension (often: mode, role, or actor identity).**

- S45 #avatarrenderer-attractdrag-pulse-boost-zero-new-mechanism: Battle Ledger C10 ADOPT-LITE: leverage existing controls.state.kind === 'AttractDrag' as a local-only visual cue (double pulse depth on the local avatar while LMB-dragging). This bridges the perceptual lag for joiner between LMB-down and the host snapshot reflecting the pickup (~100ms RTT). The alternative Gemini originally proposed (cursor color change, animated borders, custom CSS) all required new state + new code paths. The AttractDrag-state read approach was 3 lines and reused the existing FSM that was already running locally on every browser. **Lesson: when looking for a quick visual feedback mechanism, scan the existing FSM states first — local input state often has all the signals needed without any net-roundtrip or new code path. Local FSM is the cheapest source of "intent immediately known to UI." Pattern carry-forward: before designing a new UI feedback channel, list every state field already mutated on the input layer and check if one of them already maps to "user did the thing that needs visual feedback."**

- SESSION #s45-bug-critical-3-shipped-autonomous-mode: 10 files changed, +507/-175 LOC, 1 commit `bf5090c`, deploy run `26370843779` SUCCESS (auto-triggered via push path filter). User autonomous-mode authorization 2026-05-24 ("run full session priority batch! ... try to get as much done ... project's best interests in mind ... thorough /handoff !") interpreted as Full-tier `go` with deferred-decision delegation. Locked Option B+ (Sym A interaction + Sym B multi-avatar with existing UPDATE_AVATAR_POS infra + Sym C(a) carry-spark tint); deferred Sym C(b/c) primitive/creature creator-tint to follow-up PDR per regression-compounding rule. Council R1 (Grok grok-4-1-fast-non-reasoning + Gemini gemini-2.5-pro parallel via MCP) returned substantive challenges + risk register + 8 new questions; Quality Gate PASS without re-prompt; R2 Synthesis 13-row Battle Ledger; PRIME-AUDIT 7 deltas. Bundle 486.91 → 488.16 KB (+1.25 KB, 11.84 KB headroom under 500 cap). Tests 754 → 757 (+3) all GREEN. tsc -b CLEAN. 4-layer verification 10/10 PASS (L1 timestamp 16:59→19:39, L2 ETag new 6a1353dd-488, L3 bundle size+filename exact match, L4 positive shibboleths UPDATE_AVATAR_POS + PICKUP_SPARK present in deployed bundle). Runtime probe via preview_eval confirmed code paths exist (controlsHasClientBypassMarker=true, p1AvatarPosNonZero=true, worldHasLocalPlayerId=true). **Token cost ~120-150K (State-Discovery 15K + Council R1 6K + Synthesis 3K + Implementation 35K + Tests+Build 10K + Verify 8K + Reflexion+Handoff 10K + Audit overhead). API: Grok 1 call (~$0.01), Gemini-2.5-pro 1 call (~$0.05). Total API spend ~$0.06.** P9 vite/vitest CVE bump DEFERRED per "project's best interest" judgment — 4 MODERATE dev-only CVEs require vite 5→6 + vitest 1→3 majors; major-version regression risk with user-asleep authorization is unwise. CLOSE GATED on user 2-peer smoke for BUG-CRITICAL-3 (joiner can interact + sees both avatars + carried spark colored by carrier).

## 2026-05-24 — Session 44 (BUG-CRITICAL-2 FIX SHIPPED + USER-SMOKE PASS for pairing; 3 NEW symptoms surfaced; BUG-CRITICAL-3 PDR drafted for S45; commit 6f412f3 + handoff dfd7f50)

- S44 #user-smoke-confirmed-bug-critical-2-pairing-restored-AND-exposed-deeper-bugs: User real 2-browser smoke on https://spark-online.space/?debug=1 PASSED for the original BUG-CRITICAL-2: peers paired, snapshot sync flowing (gameState=PLAYING, gameMode=1v1, scores update, BOND_FORMED currentBondsInWorld=1, VOLTKIN CHAIN progress visible to client). Multi-strategy + relay rotation worked exactly as designed. BUT smoke surfaced 3 NEW symptoms that were MASKED while signaling itself was broken: (A) joiner cannot interact with primitives at all, (B) joiner doesn't render host's avatar/spark ("primitives moving by invisible force"), (C) per-player color identity not enforced in render of carried state / constructions. Static-parse probe at session close confirmed Sym B root cause: `main.ts:228 new AvatarRenderer(app, P1)` — SINGLE avatar instance per browser, parameterized for local player only; remote player's avatar literally never renders. **Lesson: fixing the lower layer (signaling) unmasks bugs in higher layers (interaction/render) that depend on it. Each smoke iteration peels back one layer. The 8-session-overdue smoke from S35-P11 wasn't just hiding ONE bug (relay decay); it was hiding a STACK of bugs that needed the lower layer working to even be observable. Pattern carry-forward: when fixing a foundational bug, build the next-session handoff ASSUMING new bugs will surface on first user smoke — pre-draft a state-discovery checklist for layer N+1 (interaction, render, sync state). Never close a foundational PDR without surfacing "if pairing works, here are the next 3 things to verify" prophylactically.**

- S44 #council-r2-architectural-pivot-from-race-winner-to-multi-broadcast: Council C4 + R2 both flagged race-winner architecture (first-onPeerJoin wins, teardown losers) as under-specified. PRIME-AUDIT Δ2 alternative: keep ALL enabled strategies active simultaneously, broadcast NetMessages on every strategy, rely on app-layer dedup (NETSNAPSHOT.snapshotSeq monotonic / INTENT timestamp / HELLO idempotency). Obsoletes the race AND Δ3 mid-session zombie state in one stroke — no single transport failure can sever the session. Cost: 2-3x bandwidth on small messages (~5 KB/s aggregate at 10Hz NETSNAPSHOT) = negligible. **Lesson: when Council deliberation surfaces a design that's structurally race-prone, sometimes the right move is to ELIMINATE the structure (multi-broadcast) rather than refine the race resolution (Hello-handshake-receive-wins). Council voices both reached for the latter; the former wasn't on the menu until PRIME-AUDIT synthesis. Pattern: PRIME-AUDIT should explicitly ask "is there an architecture that makes this race condition not exist?" before accepting a race-resolution refinement.**

- S44 #trystero-0-25-api-breaking-change-makeaction-shape-and-event-properties: 0.24 → 0.25 made breaking changes in `@trystero-p2p/core`: `room.makeAction<T>(namespace)` now returns `MessageAction<T>` object `{ send, onMessage, onReceiveProgress }` instead of 3-tuple `[sender, receiver, progress]`. `room.onPeerJoin` and `room.onPeerLeave` are now `((peerId: string) => void) | null` PROPERTIES (assigned with `=`) instead of registration METHODS. `recvFn(handler)` callback shape changed from `(data, peerId)` to `(data, ctx)` where `ctx = { peerId, metadata }`. **Lesson: type-driven refactoring caught the breaks immediately (TS2488 "not iterable" on tuple destructure, TS7006 implicit-any on callback args) — would have been silent ship-then-runtime-error failures in plain JS. Pattern carry-forward: when migrating a critical infrastructure dep across minor versions, run `tsc -b --noEmit` BEFORE testing — type errors locate the breaks faster than runtime exceptions in browser console.**

- S44 #runtime-verifiability-via-preview-eval-beats-deploy-and-pray: Instead of shipping the multi-strategy code to prod and waiting for user 2-peer smoke to discover whether all 10 endpoints actually attach, used `preview_eval` to dynamic-import NetTransport in the dev browser + call `.connect('TESTROOM')` + wait 2.5s + read `getDiagnostics()`. Result: `nostr: ready, 7/7 relays connected; torrent: ready, 3/3 trackers connected; mqtt: disabled` — empirical confirmation BEFORE deploy that the multi-strategy code actually executes against the new 0.25 API. This is the Council R2 Δ5 Runtime-Verifiability gate satisfied EMPIRICALLY (not static-parse). Caught one surprise: torrent trackers that returned HTTP 404/403 to the curl probe (suggesting "dead") actually accept WSS upgrade fine (Trystero connects them all). **Lesson: HTTP-protocol probes (curl) are insufficient verification for WSS endpoints — they probe the wrong protocol. The right probe is the actual library calling the actual API in the actual browser. `preview_eval`-driven runtime probe is ~30s of wall-clock cost vs ~5min deploy-verify-rollback if multi-strategy was misconfigured. Pattern carry-forward: for any net-infrastructure change, add a `preview_eval` runtime probe to the PDR's VERIFICATION GATE section, not just typecheck + test + bundle-grep.**

- S44 #vite-inlines-dynamic-imports-when-flag-statically-true-no-lazy-chunk: Wrote `if (STRATEGY_FLAGS.torrent) { void import('@trystero-p2p/torrent').then(...) }` expecting Vite to emit a separate chunk for the torrent code (lazy-load per Council R2 S3γ). Vite instead INLINED the entire torrent code into the main bundle because the flag `STRATEGY_FLAGS.torrent === true` is statically derivable at build time — the dynamic-import-as-chunk-split heuristic only kicks in when the runtime branch is statically unknown. Net result: bundle 486 KB / 500 KB cap (under by 13 KB), eager-loaded multi-strategy, no lazy chunks. Also confirmed: `mqtt: false` → `import('@trystero-p2p/mqtt')` IS chunk-split-eligible at the source level but Vite tree-shook the entire branch because the flag is statically false, so the chunk file isn't even emitted. **Lesson: Vite's dynamic-import-as-chunk heuristic is more aggressive about inlining than expected when constants permit. If lazy-load is critical for bundle budget, gate with a non-static value (e.g. `import.meta.env.VITE_STRATEGY_TORRENT`) so the branch is unknowable at build time. Pattern carry-forward: when applying Council "lazy-load" advice, MEASURE the resulting bundle structure (`ls dist/assets/`) to confirm chunks were actually emitted — don't trust the dynamic-import syntax alone.**

- S44 #bundle-shibboleth-must-distinguish-upstream-defaults-from-our-config: L4b NEGATIVE shibboleth grep showed `damus.io: 1` in deployed bundle even though my `iceConfig.ts` `NOSTR_RELAYS` array doesn't include it. Investigation: `@trystero-p2p/nostr@0.25.0` bundles a `defaultRelayUrls` array containing 50+ public Nostr relays (damus, wine, primal, etc.) as upstream fallback. Our `relayConfig: { urls: NOSTR_RELAYS, redundancy: NOSTR_RELAYS.length }` passed to `joinRoom` OVERRIDES the default — so damus is dead string data in the bundle, never used at runtime. Runtime verification via `preview_eval` confirmed `NOSTR_RELAYS` exposes exactly our 7 relays. **Lesson: bundle-grep shibboleths conflate "string present in bundle bytes" with "string used at runtime" — they're necessary but not sufficient for verifying config correctness. Always pair NEGATIVE shibboleths with a runtime probe that reads the actual config constant. Pattern carry-forward: for any "we removed X from config" verification, include both (a) `grep -c X dist/assets/*.js` (necessary, catches accidental references in OUR source) AND (b) runtime `import` probe of the config constant (sufficient, distinguishes upstream-fallback-strings from our-config).**

- S44 #user-urgency-3-exclamations-authorized-full-tier-without-explicit-option-c-naming: User message: "fix all bugs! make sure 1v1 wors as intended! real time red spark vs blue spark! no turn based crap. make sure both playrs can connect and play as intended! be thorough!" 6 exclamation marks, plural "all bugs," "be thorough" quality bar. AskUserQuestion ABC tier picker had returned empty — so the message arrived as a fresh URGENCY directive after an interrupted choice. Interpreted as authorization for Option C (Full tier, most thorough). Wrote Scope Amendment to PDR, set `pdr_approved: true` + `unlock_source: user` + `deliberation_completed: true` at both top-level and per-priority, fired Council R1+R2, executed. Critical that the Scope Amendment block captures user's verbatim words — a future audit reading the PDR can see exactly what authorization shape mapped to Option C. **Lesson: user URGENCY signals are ALSO `go` signals; "fix it!" can be both a frustration ventilation AND a constitutional approval. The right protocol is "treat as `go`, but anchor the amendment in verbatim words for auditability." Pattern carry-forward: for any urgency-driven scope decision, the PDR Scope Amendment MUST quote the user verbatim so future-self can reconstruct intent without parental override.**

- SESSION #s44-bug-critical-2-multi-strategy-shipped-CONSTITUTIONAL: 10 files changed, +1335/-287 LOC, 1 commit `6f412f3`, deploy run `26367307504` SUCCESS (auto-triggered via push path filter). New deps `@trystero-p2p/{core,nostr,torrent,mqtt}@0.25.0` exact-pinned, removed `trystero@0.24.0` umbrella. Bundle 472.87→486.91 KB (+14.04 KB, 13.09 KB headroom under 500 cap). Tests 754/754 PASS unchanged. Typecheck CLEAN. 4-layer verification 10/10 PASS. Council R1+R2 MEDIUM verdict (architectural follow-ons documented as carry-forward). PRIME-AUDIT Δ1-Δ7 inline. Runtime verification via in-browser `preview_eval`: nostr 7/7 relays + torrent 3/3 trackers + mqtt disabled. **Token cost ~80K (Council R1 6K + R2 3K + execution+verification 40K + reflexion 8K + handoff 6K + analysis overhead). API: Grok ~2 calls (~$0.01 each ~$0.02), Gemini-2.5-pro 2 calls (~$0.05 each ~$0.10). Total API spend ~$0.12.** CLOSE GATED on user 2-peer smoke (S35-P11 carry, now 8 sessions overdue, will close BOTH on confirmation).

## 2026-05-24 — Session 43 (BUG-CRITICAL-2: P2P signaling broken — Trystero/Nostr relay decay diagnosed; PDR draft only, no source fix shipped; commit 023fea4)

- S43 #public-nostr-relay-ecosystem-is-decaying-multi-relay-set-needed: Empirical probe via dual-NetTransport in dev confirmed 3 of 6 production relays effectively unusable: `wss://relay.damus.io` actively rate-limits Trystero writes ("you are noting too much" — continuous warnings during 12s probe); `wss://nostr.wine` became paid-only ("restricted: sign up to write events"); `wss://relay.nostr.band` host-level unreachable (5s curl timeout). Among candidate replacements: `wss://eden.nostr.land` also paid now ("Pay on nostr.land for access"). The 4 fresh candidates that pass HTTPS probe (`nostr.mom`, `offchain.pub`, `nostr-pub.wellorder.net`, `relay.primal.net`) are UNVERIFIED for Trystero NIP-78 ephemeral-event compatibility — HTTP 200 ≠ WSS-write acceptance. **Lesson: free public infrastructure decays under shared abuse — Nostr's relay ecosystem is now ~50% paid/rate-limited vs S15 era when it was nearly all free-and-functional. Any deployment depending on N free public relays needs (a) a list >> what we need (redundancy budget), (b) periodic re-verification scheduled (not "first user smoke catches it 7 sessions later"), (c) observability surfacing per-relay accept/reject counters in production. Pattern carry-forward: codify "any external-dependency-on-free-public-infra MUST include health-check telemetry" as a deployment rule. The 2026-05-24 relay state is a snapshot; expect more decay every quarter.**

- S43 #trystero-0-24-torrent-mqtt-are-deprecation-stubs-only-nostr-functional: Inspected `node_modules/trystero/dist/torrent.mjs` and `mqtt.mjs` at version 0.24.0 — runtime code is `import deprecate_default from "./deprecate.mjs"; deprecate_default("torrent", "@trystero-p2p/torrent"); export {};`. NO `joinRoom` export. Both subpath exports are vestigial deprecation stubs. Real implementations moved to `@trystero-p2p/{torrent,mqtt,firebase,supabase,ipfs}@0.25.0` separate npm packages (published 2026-05-23, ONE DAY before this session). Only `trystero/nostr` is functional in 0.24.0 — re-exports `@trystero-p2p/nostr@0.24.0` (transitively installed via `trystero` dep). **Lesson: between version 0.23.x and 0.24.x, Trystero split its strategies into separate npm packages; the umbrella `trystero` package keeps the subpath exports but they're now empty stubs. Any "let's just switch to BitTorrent / MQTT" planning that doesn't `npm install @trystero-p2p/*` first will silently get a no-op. Pattern carry-forward: pin this fact in transport.ts source comment + audit any 3rd-party library at minor-version bumps for similar "stub remained, behavior moved" patterns. Especially load-bearing for libraries where the subpath import IS the strategy selector.**

- S43 #user-demanded-state-discovery-before-asking-them-to-test: User's message at S43 entry was *"first verify everything is working before tyelling me to go check it out!@"* — escalates Rule 21 §A.0 STATE-DISCOVERY GATE from architectural choice to user-facing protocol. Pre-S43, "Claude proposes fix → user tests → user reports back" was acceptable. Post-S43, user has explicitly rejected the "you go check" loop: when claiming a fix or scope-amending a PDR, Claude must REPRODUCE the bug locally (or maximally feasible substitute) BEFORE asking user to retest. Methodology fallbacks in priority order: (1) Playwright/preview MCP automated probe; (2) dev-server + dual-tab smoke; (3) curl/wscat protocol probe against live infrastructure; (4) ONLY THEN ask user. **Lesson: when reporting "I've identified the bug" or "the fix is ready," include the empirical reproduction evidence (logs, screenshots, probe output) in the same message. "Go try X" with no reproduction is the anti-pattern user explicitly forbade. Pattern carry-forward: amend session-pdca SKILL.md COMPLETION PROTOCOL — for any bug-PDR, the close-out message MUST include reproduction evidence (probe transcript or screenshot) AND fix-verification evidence (same probe re-run post-fix). User-retest becomes confirmation, not discovery.**

- S43 #dual-nettransport-same-window-probe-is-inconclusive-for-pairing: Ran 2 NetTransport instances in single browser window with identical roomCode + APP_ID. Expected behavior: both publish discovery events to Nostr, both subscribe to room topic, see each other's discovery, exchange WebRTC SDP via Nostr relay, pair via browser-loopback. Observed: 0 peers paired in 25s even with 7 healthy-no-rejection relays (4 fresh + 3 existing-good). Two non-exclusive possibilities: (a) Trystero/browser anti-loopback prevents same-window peer pairing (probe methodology limitation — false negative); (b) deeper signaling/ICE layer also broken (true positive). Cannot disambiguate without true 2-browser-process test. **Lesson: dual-Trystero-in-one-window probe can PROVE signaling is broken (visible relay-rejection warnings) but CANNOT prove signaling is working (zero-peer outcome is ambiguous). For Trystero/WebRTC validation, the gold standard is 2 separate browser PROCESSES (separate Chromium instances, not separate tabs in same Chromium), connected via real network stack. Pattern carry-forward: when authoring probe methodology in PDRs, classify each probe as "can prove broken" vs "can prove working" — never assume a single probe shape does both.**

- S43 #s35-p11-7-sessions-overdue-cost-real-money: The 2-peer 1v1 smoke test (S35-P11 carry) was deferred across S36→S37→S38→S39→S40→S41→S42 (7 sessions) before the user finally ran it in S43 and immediately surfaced BUG-CRITICAL-2. The relay decay had been silently accumulating — likely damus.io rate-limiting kicked in weeks ago, nostr.wine paid-conversion happened 2026-04-ish, eden.nostr.land paid 2026-05-ish. Each session that completed without the smoke shipped MORE work on top of a broken multiplayer foundation. **Lesson: USER-ACTION priorities deferred past 2-3 sessions accumulate hidden regressions. Treat carry-forward USER-ACTION as URGENT after 2 sessions, BLOCKER after 3. Each S36-S42 handoff blithely re-listed the smoke as priority #1 without escalating urgency or surfacing the regression risk. Pattern carry-forward: handoff skill should auto-flag any USER-ACTION carry-forward overdue >2 sessions with "⚠️ X sessions overdue — risk of accumulating undetected regression"; consider blocking session-close on overdue user-action items past 4 sessions until user either runs them or explicitly accepts the risk.**

- SESSION #s43-state-discovery-only-no-source-fix-shipped: Single commit 023fea4 — `.claude/launch.json` (port 16489→15709 fix) + `.claude/plans/IN-PROGRESS_S43_BUG-CRITICAL-2_p2p-signaling-broken.md` (PDR draft). ZERO source code changes; ZERO test runs; ZERO deploy. State-Discovery probes ran via dev-server + dual-NetTransport + preview MCP; probe scaffolding (`src/__probe.ts`) created then deleted same session. PDR drafted with 3 fix options (Micro relay-rotate ~5K / Standard rotate+telemetry+runbook ~15-20K / Full multi-strategy fallback ~35-45K) — pending user tier choice + `go` before Council deliberation or execution. **Token cost ~12-15K (State-Discovery + 4 probe iterations + PDR draft + handoff). No Council API spend (user gate not yet passed). Highest-leverage finding: confirmed S42 commit 6e3bfaf touched ZERO transport code — bug is pre-existing public-relay decay, not S42 regression. 4 carry-forward priorities open for next session: (a) BUG-CRITICAL-2 fix execution gated on user tier choice; (b) original S43-planned batch (vite/vitest CVE bump #2, Knip triage #5) still pending; (c) main.ts hypertrophy refactor #3; (d) chateau-guardian CI audit #4 (cross-project).**

<!-- S42 block pruned at S52 handoff (6 entries: #design-drift-can-ship-13-sessions-without-blueprint-amendment-catching-it, #council-r2-sharpened-shared-vs-owned-error-semantics, #race-counter-as-observability-not-just-debugging, #boot-then-smoke-runtime-verifiability-gate-applied, #first-Intent-wins-is-the-natural-resolution-for-trystero-p2p, SESSION #s42-bug-critical-1-stats; archived in .handoff-archive/HANDOFF_2026-05-24_S42close.md and commit 6e3bfaf) -->

<!-- S41 block pruned at S51 handoff (5 entries: #billing-block-root-cause-was-pro-quota-exhaustion-not-failed-payment, #deploy-verification-handshake-as-4-step-ladder, #path-filter-empirically-validated-by-no-redeploy-on-state-edits, #chateau-guardian-actions-bloat-is-separate-medium-term-issue, SESSION #s41-p1-verification-batch-stats; archived in .handoff-archive/HANDOFF_2026-05-24_S41close.md) -->

<!-- S40 block pruned at S50 handoff (6 entries: #deploy-claims-need-live-url-head-verifies-not-just-tests-green, #phantom-deployment-state-waiting-as-multi-day-concurrency-lock, #actions-billing-failure-presents-as-2-second-job-failure-with-empty-runner, #path-filter-beats-skip-commit-flag-for-doc-deploy-suppression, #compound-deploy-failures-need-empirical-layer-strip-not-conjecture-of-single-cause, SESSION #s40-deploy-pipeline-bug-stats; archived in .handoff-archive/HANDOFF_2026-05-24_S40close.md and commit f6cf27c) -->
<!-- S39 block pruned at S49 handoff (6 entries: #runtime-only-bug-needs-runtime-verification-not-just-unit-tests, #dedicated-control-signals-decouple-state-transitions-from-snapshot-reliability, #visible-to-user-diagnostics-over-debug-flag-required, #object-fit-contain-letterbox-coordinate-mapping-trap, #wire-protocol-handler-idempotence-via-state-gate, SESSION #s39-bugfix-batch-stats; archived in .handoff-archive/HANDOFF_2026-05-23_S39close.md) -->
<!-- S38 block pruned at S48 handoff (7 entries: #cross-project-audit-prompt-as-high-leverage-protocol, #audit-pass-2-self-check-catches-fix-induced-regressions, #record-of-union-literal-true-beats-set-of-string-for-closed-allowlists, #publish-subscribe-seam-closes-layer-violation-with-zero-runtime-cost, #defense-in-depth-at-the-peer-trust-boundary, #knip-unused-exports-need-per-symbol-chesterton-triage-not-blanket-deletion, SESSION #s38-audit-campaign-batch-stats; archived in .handoff-archive/HANDOFF_2026-05-21_S38close.md and commit history) -->
<!-- S37 Path B block pruned at S48 handoff (7 entries: #council-success-after-S36-cancellation-skills-run-alone-pattern-validated, #procedural-audio-synthesis-as-extensible-fartFreq-pattern, #table-driven-it-each-for-fsm-walk-tests-was-right-call-over-individual-its, #drain-parity-as-multiplayer-audio-guarantee-not-just-frame-parity, #signal-rubric-external-user-facing-fired-for-audio-quality-pdr, #counter-hook-on-session-state-prevents-direct-edit-write-pattern, SESSION #s37-pathb-batch-stats; archived in .handoff-archive/HANDOFF_2026-05-18_S37close.md) -->
<!-- S36 block pruned at S44 handoff (5 entries: #council-cancelled-mid-launch-via-parallel-tool-coupling, #transformation-arc-from-style-mismatch-as-strongest-creative-interpretation, #pure-frame-selector-as-side-of-render-coin-not-inside-renderer-class, #empirical-sync-test-between-two-deterministic-functions, #killCount-as-render-derived-state-not-display-flag; archived in .handoff-archive/HANDOFF_2026-05-17_S36close.md and reflexion entries are preserved in commit history) -->
<!-- S35 P0 block pruned at S43 handoff (5 entries: #integration-blind-spot-since-S15-because-unit-tests-bypass-main-ts-inline-gates, #asymmetric-host-joiner-setup-needs-symmetry-check-on-every-1v1-relevant-world-field, #prime-audit-all-gates-sweep-before-state-mutation-in-entry-point-handler, #bootstrap-gate-catch-22-pattern-codified, #user-driven-playtest-as-final-validation-gate-when-unit-tests-cannot-reach-the-failure-site; archived in .handoff-archive/HANDOFF_2026-05-17_S35close.md) -->
<!-- S34 Phase B block pruned at S43 handoff (5 entries: #audit-agent-false-positive-rate-9-percent-control-flow-skipped, #test-invariant-vs-runtime-guard-choose-by-reachability, #council-q-pre-resolve-saves-revision-round, #micro-batch-aggregate-doesnt-need-standard-tier-deliberation-rigor-but-protocol-mandates, SESSION #s34-phaseb-batch-stats; archived in .handoff-archive/HANDOFF_2026-05-16_S34close.md) -->
<!-- S34 Phase A block pruned at S41 handoff (5 entries: #audit-finding-false-positive-pattern-fires-twice-in-2-batches, #council-helper-extraction-tradeoffs-tested-vs-untested-dom-code, #voltkin-config-as-anvil-prereq-byte-exact-via-replay-determinism, #council-low-roi-suggestions-when-cleanup-batch-meets-architecture-pushback, SESSION #s34-batch-completion-stats; archived in .handoff-archive/HANDOFF_2026-05-16_S34close.md and prior handoffs) -->
<!-- S33 block pruned at S40 handoff (6 entries: #audit-finding-can-be-false-positive-when-existing-comment-documents-intent, #council-reasons-from-generic-best-practice-prime-audit-brings-domain-evidence, #replay-determinism-test-catches-its-own-first-run-nondeterminism, #replay-test-as-guard-for-byte-exact-refactor-claim, #a0-state-discovery-gate-catches-backlog-scope-deltas-before-user-go, SESSION #council-prime-audit-pipeline-cost-benefit-ratio-real-numbers; archived in .handoff-archive/HANDOFF_2026-05-16_S33close.md) -->
<!-- S32 block pruned at S40 handoff (3 entries: #browser-cache-first-look-for-multi-system-asset-loss, #empirical-controlled-test-before-scope-amendment, #read-backlog-before-scoping-emergency-pdr; archived in .handoff-archive/HANDOFF_2026-05-16_S32diagnostic.md) -->
<!-- S31 block pruned at S39 handoff (full 6 entries archived in .handoff-archive/HANDOFF_2026-05-16_S31close.md) -->

